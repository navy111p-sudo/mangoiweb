node_modules/
uploads/*.pdf
.env
.wrangler/
.dev.vars

*.bak_kpi
admin.html.bak_kpi
*.bak
*.bak2
_preview_dark.png
_preview_*.png

# test-harness 임시 트랜스파일 산출물
test-harness/.build/
*.bak_*
*.bak.*

# 정리 보관함 (확인 후 비우세요)
_trash_*/

# 서빙 자산 폴더에 백업(.bak) 재유입 방지 (2026-06)
cloudflare-deploy/public/**/*.bak*

# 검사·테스트 생성 산출물 (2026-07-11 정리)
cloudflare-deploy/.wrangler-drycheck/
test_report.html
qs.student.result.json

# 민감 로컬 작업물: DB덤프 백도어(시크릿+DB접속)·안드로이드 서명키·서명안내 (2026-07-11)
# ⚠ 절대 커밋 금지 — 커밋 시 전체 DB PII/결제 노출 및 앱 서명키 유출
망고아이 테스트와 배포/mango_export.php
망고아이 테스트와 배포/mangoi-release.keystore
*.keystore
mango_export.php

# wrangler dry-run 빌드 산출물 — 3만 줄짜리 번들 index.js (2026-07-14 정리)
.wrangler-dryrun/
dist-drycheck/

# 임시 미리보기·이전 버전 미디어 (원본은 로컬 보관)
.tmp_*
*.prev-*.png
*.prev-*.mp4

# 게임 소스 미디어 원본 (배포본은 public/ 아래 최적화본 사용)
# ⚠️ 실제 폴더는 «리포 루트» game_image/ 다 — cloudflare-deploy/ 경로만 적혀 있어
#    146MB 원본이 git status 를 계속 어지럽혔다(2026-08-27). 두 경로 다 막는다.
game_image/
cloudflare-deploy/game_image/
# 배경제거용 파이썬 가상환경(611MB, pip 로 재생성 가능)·프레임 추출 중간산출물
cloudflare-deploy/.venv-rembg/
cloudflare-deploy/temp/
# Mr.mango 원본 실사(6.6MB — build-mrmango-mask.py 입력, 로컬 보관)
cloudflare-deploy/Mr.Mango실사.png
# 삭제 대기 상자 (_trash_*/ 규칙과 이름이 달라 안 걸리던 것)
_to_delete/
cloudflare-deploy/menu_image/
cloudflare-deploy/parent_icon_src/
cloudflare-deploy/vocab_bg_src/
cloudflare-deploy/suspect_img_src/
cloudflare-deploy/크로마키_*.mp4

# 사용설명서 제작 작업장 — 대용량 미디어·생성 산출물 (video만 700MB)
망고아이_사용설명서/video/
망고아이_사용설명서/assets/
망고아이_사용설명서/qa/
망고아이_사용설명서/build_*/

# 파이썬 캐시
__pycache__/
*.pyc

# Claude Code — 개인 PC 전용 설정·작업물 (공유 설정은 .claude/settings.json)
.claude/settings.local.json
.claude/worktrees/
.claude/preview-server.js
