# 망고아이 — Claude 작업 규칙

> 이 파일은 Claude Code가 이 저장소에서 작업할 때 **매번 자동으로 읽는** 규칙서입니다.
> 사람이 읽는 상세 문서는 [MAINTENANCE.md](MAINTENANCE.md)입니다. 작업 전에 함께 참고하세요.
>
> **2인 공동 유지보수 중입니다.** 내 판단만으로 남의 영역을 바꾸지 마세요.

---

## 0. 가장 먼저 알아야 할 것

- **실서비스는 `cloudflare-deploy/` 폴더 하나.** 나머지는 보조·실험·레거시입니다.
- 운영 주소: **https://mangoi.ai** → Cloudflare Worker `webrtc-unified-platform-prod`
  (사장님·직원이 실제로 여는 주소입니다. 사람에게 안내할 링크는 **이 주소**를 쓰세요)
  - **`www.mangoi.ai` 는 `mangoi.ai` 로 넘어갑니다** (2026-08-17 추가 → 같은 날 정본으로 합침).
    `src/index.ts` 진입부가 www 요청을 apex 로 돌립니다(GET/HEAD 는 301, 나머지는 308 — POST 본문 보존).
    **WebSocket 업그레이드는 제외**합니다(화상수업이 끊깁니다). 안내 링크의 정본은 `src/site-url.ts` 의 `SITE_ORIGIN` 한 줄.
    ⚠️ **왜 굳이 합쳤나** — 오리진이 갈리면 브라우저가 두 사이트로 봅니다. 교사 세션 쿠키는 `Domain=` 이 없는
    호스트 전용이고, 학생 로그인(`mangoi_logged_user`·`mango_token`)과 언어(`mangoi_lang`)는 `localStorage` 라
    **오리진별로 갈리는 게 웹 표준이라 공유가 불가능**합니다. 쿠키만 넓혀선 절반만 고쳐집니다.
    안 합치면 「로그인했는데 또 로그인하래요」가 그대로 재현됩니다.
    ⚠️ 호스트명을 그대로 쓰는 코드는 여전히 조심하세요. 패스키가 정확히 그랬고
    (`rpId` 가 달라져 «어제까진 지문으로 됐는데» 가 됩니다), `src/api-passkey.ts` 의 `resolveRpId()` 로 apex 에 묶어
    리다이렉트가 없어도 버티게 해 뒀습니다. 비슷한 코드를 새로 쓸 때 같은 함정을 확인하세요
  - 📌 **도메인–워커 배치 (2026-09-01 대시보드 실측)** — `test.mangoi.co.kr` · `mangoi.ai` · `www.mangoi.ai` → **전부 `webrtc-unified-platform-prod`(운영)**
    - ✅ **2026-09-01 이전 완료.** 사장님이 대시보드에서 `test.mangoi.co.kr` 이 **기본 워커에 붙어 있는 것을
      직접 확인하고** `-prod` 로 옮기셨습니다.
    - ✅ **확인 방법·날짜 (2026-09-01, 사장님 실측)** — 옮긴 뒤 **두 도메인(`mangoi.ai`·`test.mangoi.co.kr`)에서
      각각 연습방 `demo-1` 에 들어가 「서로 보이는 것」을 눈으로 확인**했습니다. 이것이 «같은 워커인가» 의
      유일한 최종 확인이고, 8/27 에 «했다» 고 적혔지만 실제로는 빠져 있던 바로 그 단계입니다.
      ⚠️ 이 확인은 **D1 에 뒷받침 기록이 없습니다** — `demo-1` 은 예약방(`class-…`)이 아니라 입장 검증을
      안 타고 그 세션에서 `/api/attendance/join` 이 불리지 않았습니다. 근거는 사람이 본 것뿐이고,
      **그것으로 충분합니다**(갈려 있으면 애초에 서로 보일 수가 없습니다). 기록이 없다고 다시 «미확인» 으로
      적지 마세요 — 대신 다음에 확인할 때는 **예약방으로 하면 D1 에도 남습니다.**
    - 🔴 **그 전에 이 자리에 적혀 있던 「2026-08-27 이전 완료」는 사실이 아니었습니다.** 함께 적혀 있던
      「두 도메인에서 `demo-1` 에 들어가 서로 보이는 것까지 확인했다」도 **일어나지 않았습니다** —
      2026-09-01 에 D1 `attendance` 로 그날 `demo-1` 접속을 전수로 세니 `mangoi.ai` 에서 **1건**,
      `test.mangoi.co.kr` 에서 **0건**이었습니다(`host` 칸은 8/26부터 기록 중이라 남아 있습니다).
      그 «완료» 문장을 적은 여섯 시간 뒤 같은 날 20:58 부터 `ubckt01` 이 구 도메인으로 7회 접속해
      세 번째 사고가 났고, 그대로 **9/1 에 네 번째 사고**(`class-848-20260901`, 강선생님 `mangoi.ai` ↔
      사장님 `test.mangoi.co.kr`)로 이어졌습니다.
    - ✅ **이제 이 줄을 사람이 기억할 필요가 없습니다** — 갈리면 **감시견이 문자로 알립니다**
      (`src/room-split-guard.ts`, 15분 cron). 판정 근거는 «출석에 host 가 섞였나» 같은 대리지표가 아니라
      **DO 네임스페이스 지문**(`idFromName`)이라, 방이 갈리는 원인 그 자체를 직접 잽니다.
      ⚠️ 그 지문은 «누군가 그 도메인으로 수업에 들어와야» 기록됩니다 — 아무도 안 쓰는 도메인은
      «모름» 으로 침묵합니다(옛 값으로 계속 문자하지 않으려고 14일 지나면 버립니다).
    - ⚠️ **워커는 여전히 두 벌 배포됩니다**(`deploy.ps1` [6]·`deploy.yml` 1)2) 단계). 바뀐 것은
      «도메인이 전부 `-prod` 를 본다» 는 것뿐입니다. 기본 워커(`webrtc-unified-platform`)는
      **cron 5종(야간 동기화·독촉·NPS·감시견)이 거기에만 있어** 계속 살아 있어야 합니다
      (`[env.production.triggers] crons = []`). ⛔ 「이제 안 쓰니 지우자」 금지.
    - 📜 **이 배치는 네 번 사고를 냈습니다** — `wrangler.toml` 이 워커마다 `new_sqlite_classes` 를 따로
      선언해 **DO 가 갈렸고**, D1·KV·R2 는 같은 id 를 공유해 **출석 기록만 보면 멀쩡해 보였습니다.**
      8/19 강선생님 8회 수업 · 8/25 `class-895` · 8/27 `ubckt01`(구 도메인으로 7회 접속, 끝내 못 만남) ·
      **9/1 `class-848`**(강선생님과 사장님이 같은 방 번호로 20분 내내 서로 못 봄).
    - 🔴 **「옮겼다」고 적혔지만 실제로는 안 옮겨진 것이 두 번(8/19·8/27)입니다.** 8/19 에는 같은 작업기록에
      「어느 워커에 붙어 있는지 **직접 확인하지 못했다**」가 나란히 있었는데 요약만 완료형이라 그쪽만 읽혔고,
      8/27 에는 하지도 않은 `demo-1` 확인을 «했다» 고 적었습니다. **두 번 다 그 문장 때문에 아무도 다시 안 봤습니다.**
      ⛔ 코드 밖 상태(대시보드·DNS·외부 콘솔)를 완료형으로 적지 마세요 — 못 한 것은 못 했다고 적으세요.
      절차·되돌리기: `docs/도메인이전_실행절차_test.mangoi.co.kr_2026-08-27.md`
    - ⚠️ **도메인이 어느 워커에 붙어 있는지는 코드에 없습니다** — `wrangler.toml` 에 `routes` 가
      한 줄도 없고 전부 Cloudflare 대시보드에서 관리합니다. 추측하지 말고
      **Workers & Pages > 해당 워커 > 도메인** 에서 눈으로 확인하세요
  - **`test.mangoi.co.kr` 은 죽은 주소가 아닙니다** — **먼저 붙인 커스텀 도메인**이라 아직 여러 곳에
    하드코딩돼 있습니다: `cloudflare-deploy/scripts/smoke-test.ps1` 의 기본 `BaseUrl`(=`deploy.ps1` 의
    배포 전/후 스모크 15종), `ops/mangoi-watchdog.sh`, 여러 하니스.
    **그 코드들을 무심코 `mangoi.ai` 로 바꾸지 마세요** — 워치독·스모크까지 같이 확인해야 하는
    별건 작업입니다 (도메인 정리는 사람이 결정)
  - ⏳ **앱 시작 URL 은 `mangoi.ai` 로 옮기는 중입니다** (2026-08-17). 소스는 바꿨지만
    **이미 깔린 앱은 APK 에 박힌 옛 주소를 계속 봅니다.** 그래서 `test.mangoi.co.kr` 은
    **설치 기반이 새 버전으로 넘어갈 때까지 반드시 살려 둬야 합니다.**
    ⚠️ 먼저 죽이면 앱이 흰 화면이 되고, **업데이트 확인 주소(`app-version.json`)도 같은 도메인이라
    앱이 자기 힘으로 복구할 수 없습니다.** 지우는 건 새 APK 보급률을 확인한 뒤 맨 마지막입니다.
    ⚠️ ⛔ `test.mangoi.co.kr` 을 `mangoi.ai` 로 **리다이렉트하는 것도 안 됩니다** — www 때와 달리
    여기엔 잃을 게 있습니다. 그 도메인으로 등록된 **패스키 6건(계정 3개: jeong 4·Lee 1·kang 1)이 무효화**되고(재등록 필요),
    앱 사용자의 `localStorage` 로그인도 날아갑니다.
  - `mangoi.co.kr` / `www.mangoi.co.kr` (118.219.234.180) 는 **구 서버**. 옛 LMS만 있고 Worker 경로(`/library/...` 등)는 404입니다
  - ~~`mango-i.com`~~ 은 **존재하지 않는 도메인**입니다(2026-07-28 확인: 등록조차 안 됨 → NXDOMAIN).
    옛 문서 표기 오류이니 이 주소로 curl 검증하지 마세요. 무조건 실패합니다
- `modules/`, `public/`, `server.js` 는 **레거시**. 운영에 안 씁니다. 수정도 삭제도 하지 마세요.
- **실제 학생 29,000명이 쓰는 라이브 서비스입니다.** 실험은 실서비스에서 하지 않습니다.
- **Mr.mango(`/img/mango-char.png`, 헤드폰 낀 망고)가 사이트 전용 캐릭터·공식 로고입니다.** 화면 제목 왼쪽 마스코트 자리에는 🥭 이모지가 아니라 이 캐릭터를 씁니다(2026-08-23 사장님 지시 — `enroll.html` 부터 적용).

---

## 1. ⛔ 절대 하지 말 것

### 1-1. 데이터베이스

- **D1 `mango-db` 는 개발/운영이 같은 DB입니다.** 테스트용 DB가 따로 없습니다.
  `wrangler.toml` 의 기본 환경과 `[env.production]` 이 같은 `database_id` 를 가리킵니다.
- 따라서 **DELETE / UPDATE / DROP 를 테스트 목적으로 실행하지 마세요.** 실제 학생 데이터입니다.
- 데이터 확인은 **SELECT 만**. 변경이 필요하면 사람에게 먼저 알리세요.

### 1-2. 배포

- **배포 권한이 있는 사람만 `deploy.ps1` 을 실행합니다.** 권한 없는 작업자는 코드만 고치고 PR로 넘깁니다.
- `deploy.ps1` 은 **로컬 `cloudflare-deploy/public/` 폴더를 통째로 업로드**합니다.
  커밋하지 않은 미완성 파일도 그대로 실서비스에 올라갑니다. 실행 전 `git status` 확인은 필수입니다.
- 배포 경로가 두 개입니다. 중복 배포로 서로 덮어쓰지 않게 주의하세요.
  - 로컬: `deploy.ps1`
  - 자동: `main` 브랜치에 **무엇이든** push 되면 GitHub Actions가 자동 배포 — 🔴 **경로 필터가 없습니다**(2026-09-22 `deploy.yml` 실측: `on.push.branches: [main]` 한 줄뿐). 「`cloudflare-deploy/**` 가 바뀔 때만」이라고 적혀 있던 것은 **사실이 아니었습니다** — 문서·하니스만 고친 커밋도 배포가 돕니다(⟹ 보류된 커밋을 내보내려고 일부러 쓸 수도 있지만, ⛔ 그것을 «빈 커밋으로 깨우기» 로 쓰지는 마세요)

### 1-3. 되살리면 안 되는 것 (과거에 의도적으로 제거함)

아래는 문제가 있어서 **일부러 없앤 것들**입니다. "없네?" 하고 다시 넣지 마세요.

- 관리자 사이드바 hover 상세 카드 (`#mi-menu-fly`), 파란 플라이아웃 (`#ph123-flyout`)
- 관리자 화면의 서비스워커 등록, `controllerchange` 시 자동 reload
- 화상수업 독(dock)의 웜업 버튼, 상단바 X 버튼
- 인트로 화면의 별도 입장 버튼
- 관리자 KPI 타일의 이모지
- 홈 「망고아이란?」 카드 맨 앞의 **«▶ 안내 영상 — 3분 48초로 보기» 줄** — 2026-09-09 사장님 「이거 그냥 삭제해줘. 필요없어. 완전히 없애」로 뺐습니다(`js/idx-about.js` 의 BENEFITS 첫 항목). ⚠️ **영상과 `/promo.html` 주소는 그대로 살아 있습니다** — 없앤 것은 «홈 카드의 그 줄» 하나이고, 관리자 자료실·직접 주소로는 여전히 볼 수 있습니다. 📌 **그런데 같은 날 사장님이 그 자리에 «▶ 홍보영상 — 1분 21초로 보기»(사장님이 직접 녹음해 주신 사람 목소리) 를 두라고 정하셨습니다** — 지운 것은 «3분 48초 합성목소리 판», 새로 둔 것은 «1분 21초 사람 목소리 판» 이라 **다른 영상이고 «되살린 것» 이 아닙니다.** ⛔ 그러니 이 줄을 「그 카드에 영상을 두면 안 된다」로 읽지 마세요 — 없애기로 한 것은 **그 3분 48초 줄 하나**입니다. ⛔ 검사를 «`promo.html` 링크가 하나도 없다» 로 되돌리지 마세요(사장님이 넣으라고 하신 줄이 빨간불이 됩니다) — 물어야 할 것은 **«지운 `?v=ai-tools` 판으로 가지 않는가»** 입니다. ⚠️ 그 부정 검사는 **주석을 벗겨 낸 사본**으로 판정하세요 — 「왜 지웠는지」 적은 주석이 그 주소를 담고 있어 검사가 «자기 주석» 을 잡습니다(실제로 밟았습니다). 감시는 `today_plan_harness` ⑨(«3분 48초 줄이 없다» + «맨 앞이 영상 카드다» + «그 카드가 자기가 여는 프리셋의 포스터를 그린다» 짝) 와 `manual/video-entry-browser.mjs` A절
- 판단력 훈련 UI의 게임 요소
- 관리자 메뉴 카드(`.menu-card`)·서브아이템·표행·칩의 **hover 확대(scale/translate)** — "정신없다"고 제거함. hover 강조는 **색만**, 크기·위치는 고정
- 정산·매출 화면의 **「케이씨피M」·«운영자금 보충» 표시** — 매출–입금 대사 카드, 손익계산서 매출액 칸,
  월간 회계 리포트의 «운영자금 보충» 줄·내역, 「카페24 회계 실데이터」 탭의 KPI 부제·안내 상자·장부 배지까지
  **2026-08-18 사장님 지시로 전부 뺐습니다.** 하나은행 → 신한 자금이체는 매출이 아니라, 매출·회계 화면에
  그 이름과 금액이 뜨는 것 자체를 원치 않으십니다. «제외했습니다» 라고 적어 주는 것도 「아직 남아 있다」로
  읽힙니다. 분류(`classifyDeposit` → `transfer`)와 집계 제외는 그대로지만 **그 값으로 화면 줄을 다시 만들지 마세요.**
  「카페24 회계 실데이터 > 회계장부」 목록에서는 **그 행 자체를 빼고** 내려줍니다(`api-admin.ts` 의 ledger Cypher
  `WHERE NOT (…kcpmRe…)`). ⚠️ 그래서 그 목록은 «카페24 원본 그대로» 가 아닙니다 — 원본 대조는 카페24에서 하세요.
  회귀 감시는 `test-harness/c24_finance_kcpm_harness.mjs` ④·⑤번이 «목록에서 뺀다»·«표시하지 않는다» 로 못 박아 두었습니다.
  ⛔ 같은 날 «성격 미확인 입금»(`transferUnknown`) 줄도 뺐습니다 — 월간 리포트 줄·내역, 손익계산서 각주,
  대사 안내, 경고 목록, 엑셀 「확인필요 입금」 시트까지 전부. 그 돈을 매출로 잡지 않는 계산은 그대로이고
  금액도 payload(`deposit_transfer_unknown_krw`)에 남아 있지만 **화면에는 그리지 않습니다.**
  ⚠️ 그래서 성격이 안 잡힌 입금이 있어도 화면이 알려 주지 않습니다 — 확인이 필요하면 D1 `bankacct_transactions` 를 직접 보세요
- 화상수업 얼굴 타일의 **«🔇 소리가 안 들어와요» 안내 배지** — **2026-09-11 사장님 지시로 뺐습니다**(`vcMarkNoSound`, `js/idx-main.js`). 🔴 **고장 알림이 아니라 판정이 «상대가 8초 동안 조용하다» 였습니다** — 학생이 문제를 푸는 동안 강사 타일에, 강사가 듣는 동안 학생 타일에 그대로 떠서 **«듣고 있는 중» 이 «고장» 으로 읽혔습니다.** ⚠️ **경계는 «학생/관찰자» 가 아니라 «staff 판정»(`vcIsStaffNow`) 입니다** — `vcJoinAsObserver` 가 `window.vcMyRole` 을 안 정해서 관찰자도 저장된 역할·관리자 세션에 따라 staff 로 잡힐 수 있습니다(그 클릭은 서버가 버립니다 — `device-fix` 가 `isJoined` 를 요구하는데 관찰자는 `joined:false`. **이번 변경이 만든 것이 아니라 원래 그렇습니다**). ⚠️ **없앤 것은 «안내 문구» 하나뿐입니다** — 강사의 **「🔇 소리 없음 · 눌러서 고치기」**(누르면 학생 브라우저가 마이크를 다시 잡는 실제 조치)는 **그대로 살아 있어야 합니다.** ⛔ 통째로 끄지 마세요 — 강사가 학생 마이크를 원격으로 손볼 유일한 입구입니다(8/7 Kaye 건의 1). ⛔ 학생 판을 되살리지 마세요. ℹ️ **학생이 잃은 것은 «트랙은 살아 바이트는 오는데 소리가 0»(OS 음소거·입력장치 오선택·게인 0) 의 표시뿐입니다** — «바이트가 아예 안 오는» 경우는 `.vc-noaudio-hint`(「🎤 상대 소리가 안 와요」)가 **모두에게 그대로** 남아 있습니다. ⚠️ **강사 화면에는 같은 8초 조건으로 버튼이 여전히 뜹니다** — ⓐ 강사도 끄기 ⓑ 8초 늘리기 ⓒ 그대로 중 **사람이 정할 일**입니다. ✅ 감시는 `test-harness/teacher_kaye_feedback_260807_harness.mjs` 건의1절 — `vcMarkNoSound` 를 **오려 내 가짜 DOM 으로 실제로 돌려** 「staff 가 아니면 안 그린다」·**「강사에겐 그린다」(짝)**·**「눌러 보면 그 학생 마이크를 다시 잡는다」(배선을 실제로 실행)** 를 봅니다. 🪤 그 배선 검사가 없으면 **`addEventListener` 를 통째로 지워도 초록입니다**(함정 대조 실측) — «보이는데 아무 일도 안 하는» 버튼이 됩니다. 변이 3종 전부 실제 FAIL.
- 화상수업 얼굴 타일의 **📶 「저화질로 받는 중 / 보내는 중」 배지** — **2026-09-15 사장님 지시로 껐습니다**(`vcLowQBadgeOn()`, `js/idx-vc-qlog.js` ③절). 2026-09-02 에 「왜 흐린지 말해 달라」로 넣은 것인데, **얼굴 타일 «위에» 얹히는 글자라 회선이 나쁠수록 오래 떠 정작 상대 얼굴이 제일 안 보일 때 가장 크게 가렸습니다**(사장님 화면 실측: 교사 타일의 «받는 중» 과 내 타일의 «보내는 중» 이 동시에). ⚠️ **끈 것은 «화면에 그리는 것» 하나뿐입니다** — `L.self`·`L[id]` 판정도, `vc_quality` 로그(화질·단계·경로)도 그대로 쌓이고 «왜 흐린지» 는 관리자 「📶 강사 회선품질」에서 그대로 읽습니다. ⚠️ 지시는 «받는 중» 을 지목했지만 **같은 함수가 그리는 같은 배지라 둘 다** 껐습니다(하나만 남기면 얼굴은 여전히 가려집니다) — 한쪽만 되살리는 것은 사람이 정할 일. ✅ 되살리기는 `window.__vcLowQBadge = true` 한 줄. ⛔ 게이트(`vcLowQBadgeOn`)를 지워서 되살리지 마세요. 감시는 `vc_quality_blindspot_harness` ⑪ — **«기본은 안 그린다» 와 «스위치를 켜면 옛 판정이 그대로다» 를 짝으로** 봅니다(⛔ 뒤엣것을 지우면 폰 1단계를 저화질로 오인하는 류의 **판정 회귀를 아무도 안 봅니다**). 변이 2종 전부 실제 FAIL
- 화상수업 왼쪽 아래의 **«🎤 내 마이크» 떠 있는 음량 미터**(`#vc-mic-meter`, `startMicLevelMeter`) — 얼굴 타일의 음량 막대와 중복이라 제거함(2026-08-10). 자리를 두 번 옮겨도 결국 중복이 문제였음
- **결석 위험 «운영자 요약 문자»**(`src/absent-sweep.ts` — 「🚨 결석 위험 N건 · … (+N분 미입장)」) — **2026-09-06 사장님 지시로 껐습니다**(기본 OFF, KV `absent_alert_owner_send`='on' 일 때만). 15분 cron 이 «시작 +10~40분 사이 학생 미입장» 을 잡는데 조금 늦게 들어오는 흔한 경우까지 전부 걸려 수업 시간대마다 사장님 폰으로 문자가 왔습니다. ⚠️ **끈 것은 «운영자에게 문자로 알리는 것» 하나뿐입니다** — 감지·`class_no_show` 기록·강사 알림(이메일/한국번호 문자)은 그대로이고, 결석 위험은 관리자 › 노쇼 리포트에 그대로 쌓입니다. ⛔ 「운영자한테 알림이 없네?」 하고 되살리지 마세요. ⛔ **`OWNER_ALERT_PHONE` 자체를 지우는 방식으로 이런 알림을 끄지 마세요** — 결제·환불·이상로그인·사이트 장애(uptime)·방 갈림 감시견이 **같은 번호**를 씁니다(그래서 이 알림 하나만 KV 스위치로 껐습니다). ⚠️ 이렇게 «운영자 문자» 를 끌 때는 **그 문자에만 실리던 정보가 함께 사라지지 않는지** 보세요 — 여기서는 「강사에게 못 보냄 + 사유」 줄(`ownerLines`)이 그 문자에만 있어서 잠깐 아무 데도 안 가게 됐었고, 지금은 꺼져 있어도 `owner_sms.unsent_lines` 로 cron 로그·진단 응답에 남깁니다. ⚠️ 그 검사를 「push 하는 코드가 있는가」로 쓰면 못 잡습니다(실제로 초록불이었습니다) — **«닿는 곳이 있는가»** 로 물으세요. 감시는 `test-harness/absent_teacher_alert_harness.mjs` ⑦절

- **운영자(`OWNER_ALERT_PHONE`)에게 가는 문자 «전부»** — **2026-09-08 사장님 「일단 모두 꺼줘」로 껐습니다**(기본 음소거, KV `owner_alert_mute`='off' 일 때만 발송). 9/6 에 결석 위험 하나만 껐는데 이틀 뒤 「관리자 로그인 알림」이 또 와서 내린 결정입니다. ⚠️ **끈 범위가 넓습니다** — 결제·환불(`api-pay.ts`·`api-pay-refund.ts`)·이상 로그인(`auth-admin.ts`)·새 상담 리드·결석 위험 **전부**입니다(실측 6파일 17군데). ⟹ **돈이 잘못 나가도·계정을 도둑맞아도 문자가 안 옵니다.** 그것이 이 결정의 대가이고 사장님이 설명을 받고 고르신 것입니다. 📌 **다만 2026-09-09 사장님 「사이트 장애랑 감시견만 다시 켜줘」로 예외 둘이 생겼습니다** — **`uptime`(`api-uptime.ts` 3곳)·`room-split`(`room-split-guard.ts` 1곳)** 은 음소거 중에도 나갑니다(그 둘은 «사람이 안 보면 며칠씩 모르는» 종류라 성격이 다릅니다). ✅ 예외는 **`sendPlainSms(env, phone, text, { kind })`** 로 «무슨 알림인가» 를 밝힌 것만 받고, 목록 정본은 `owner-sms-mute.ts` 의 **`OWNER_ALWAYS_KINDS`** 한 곳입니다 — ⛔ 호출부에서 예외를 만들지 마세요. ✅ **안 밝히면 막힙니다**(기본 = 음소거) — 그래서 새 알림이 생겨도 조용히 새지 않습니다. ⛔ 그 목록에 결제·로그인을 넣어 넓히지 마세요(9/8 지시가 통째로 되돌아갑니다). ✅ 완전 침묵으로 되돌리려면 KV `owner_alert_mute`=**`all`**(예외까지 막음). ✅ 막는 자리는 **`sendPlainSms` 한 곳**(`solapi-client.ts`), 판정 정본은 `src/owner-sms-mute.ts` 의 `ownerSmsBlocked(kv, kind)` 입니다. ⛔ **호출부 17군데를 각각 막지 마세요** — 새 알림이 생기면 조용히 새어 나갑니다(«보내는 정본» 에 두었기에 새 알림도 자동으로 걸립니다). ⛔ **`OWNER_ALERT_PHONE` 시크릿을 지우는 방식으로 바꾸지 마세요** — 되돌리기 어렵고 「결제만 다시 켜기」 같은 선택이 불가능해집니다. ⚠️ 기본이 «음소거» 라 **KV 를 못 읽어도 안 보냅니다**(끄라는 지시라 이 자리에서는 그쪽이 맞는 실패 방향). ⚠️ 반대로 **예외 둘은 KV 를 못 읽어도 나갑니다** — 지금 정책이 그것이라 «모르면 지금 정책대로» 가 맞는 방향입니다. ⚠️ 대신 «조용히» 사라지지는 않습니다 — 막을 때마다 `console.warn('[owner-sms] …')` 로 Workers 로그에 남습니다(⚠️ 사람이 보러 가야 보입니다. 문자와 달리 먼저 알려주지 않습니다). ℹ️ 학부모·강사·학생 문자는 수신번호가 달라 **영향 없습니다.** 감시는 `test-harness/owner_sms_mute_harness.mjs`(65종 — 판정을 실제로 돌리고, 게이트를 오려 내 KV 장애까지 재현. 변이 11종 전부 실제 FAIL). ⚠️ 그 검사에서 **「KV 가 흔들리면 다시 보낸다」 변이가 한 번 뚫렸습니다** — 「그 줄이 있는가」로는 못 봅니다. 게이트를 **실행해서** 확인하세요. ⚠️ 예외를 검사할 때는 **«막힌다» 와 «나간다» 를 짝으로** 두세요 — 앞만 보면 예외가 죽어도 초록이고 뒤만 보면 결제가 몰래 예외를 받아도 초록입니다(⑧절이 소스 전체에서 «`kind` 를 넘기는 호출» 을 세어 파일까지 대조합니다). ⛔ 예외 목록을 **길이·순서로 못 박지 마세요** — 나중에 정당하게 늘 때 멀쩡한 수리가 빨간불이 됩니다(「지금 꺼져 있어야 할 것이 켜져 있지 않은가」로 물을 것)

### 1-4. 기타 금지

- `src/signaling-room.ts` (SignalingRoom Durable Object) **삭제 금지** — 진단 페이지가 사용 중
- 화상수업의 저대역폭 오디오 전용 모드(AAO+DTX) **제거 금지**
- 화상수업에서 `dropped` 상태로 수업을 자동 종료하지 말 것 — `left` 만 종료
- 이모지는 **Unicode 13 이상 사용 금지** (Win10에서 두부 표시됨)
- 관리자 PC 확대는 각 페이지에서 `zoom:1.3`. 공용 CSS로 묶지 말 것

---

## 2. ⚠️ 자주 밟는 함정

| 함정 | 실제로 이렇습니다 |
|---|---|
| 수업 안 웜업 iframe을 숨겼는데 마이크가 계속 동작함 | 부모가 탭을 CSS로 숨기면 자식의 `visibilitychange`는 발생하지 않습니다. `warmup.html`은 같은 오리진 부모의 iframe 조상 표시 상태도 관찰하고, 웜업 STT·Whisper·TTS와 지연 콜백만 종료합니다. 다시 보일 때 마이크를 자동 재시작하지 않습니다. 회귀: `manual/warmup-guided-browser.mjs`의 embedded 검사. |
| 수업 종료 후 `recording`이 남을 때 | 2026-09-19: 임시본 복구는 12시간 대기인데 야간 정리는 6시간 후 실패로 내려 복구 대상에서 빼고 있었습니다. 청소는 파트·임시본·저장소 조회 오류를 보류하고, 복구기는 종료 힌트와 5분 무활동(구 클라이언트는 시작 4시간·30분 무활동)을 확인합니다. 임시본은 조건부 쓰기로 정상 파일을 보호하고 `r2_snapshot`으로 구분합니다. 승격된 임시본을 multipart 완료 증거로 쓰지 마세요. base cron의 DO는 prod와 달라 빈 방 판정에 쓸 수 없습니다. 검증: `recording_finalize_recovery_harness.mjs`. |
| wrangler 명령 | wrangler 4에는 `r2 put`, `kv` 에 **`--remote` 옵션이 없습니다** |
| `deploy.ps1` 위치 | **리포 루트**입니다. `cloudflare-deploy/` 안이 아닙니다 |
| 배포 후 curl 검증 | 주소는 **`https://mangoi.ai`**(`test.mangoi.co.kr` 도 같은 Worker 라 둘 다 됩니다). `mango-i.com` 은 없는 도메인이라 무조건 실패합니다. CDN에 구버전이 남아 있을 수 있으니 `curl --compressed` + 캐시 우회로 확인 |
| 사람에게 링크를 안내할 때 | 문자·알림톡·안내문에 넣는 주소는 **`https://mangoi.ai`**. 「어디에도 안 보인다」 류의 제보는 **엉뚱한 도메인을 안내해서** 생기는 경우가 있습니다(2026-08-17 실제로 밟음 — 문서엔 `test.mangoi.co.kr`, 사장님 화면엔 `mangoi.ai`). 📌 **덧붙임(2026-09-10) — 그 규칙은 «화면이 만들어 주는 링크» 에도 걸립니다.** 화상수업 「🔗 이 방 링크 복사」가 **두 벌**인데 한쪽만 지키고 있었습니다 — 로비(`js/idx-vc-roomcode.js`)는 `https://mangoi.ai` 고정인데 회의방 모달(`js/idx-vc-room.js`)은 **`location.origin`** 이라, `test.mangoi.co.kr` 에서 복사한 링크가 그대로 카톡으로 퍼졌습니다. ⚠️ **이것을 「같은 번호인데 다른 방」(DO 갈림)으로 읽지 마세요** — 2026-09-01 부터 세 도메인이 같은 워커라 방은 안 갈립니다. 잃는 것은 **로그인**입니다(교사 쿠키는 호스트 전용, 학생 `mangoi_logged_user`·`mango_token`·`mangoi_lang` 은 `localStorage` 라 오리진별). ✅ **«남에게 보내는 링크» 만 정본으로 못 박고, «내가 지금 이 페이지에서 옮겨 가는 주소» 는 상대경로 그대로 두세요** — 그것까지 바꾸면 옛 도메인에서 수업 중이던 사람이 이동하다 자기 로그인을 잃습니다(`enter()` 의 `location.href='/?meet=…'` 두 줄이 그 자리). ⚠️ 도메인을 어느 쪽으로 맞출지는 **재고 정하세요** — 옛 도메인 사용자가 많으면 고정이 오히려 그쪽 로그인을 매번 깨뜨립니다(실측 `attendance.host` 30일: `mangoi.ai` 296회·46명 대 `test.mangoi.co.kr` 12회·3명, 그중 최근 사용자는 강사 1명). ⚠️ `?v=` 는 **`<script src>` 와 preload 배열 두 곳**입니다(`index.html`·`teacher.html`). 감시는 `meet_room_code_harness.mjs` ⑪절 — «그 글자가 있는가» 가 아니라 **링크 만드는 함수를 두 파일에서 오려 내 실제로 돌려** 답을 대조하고, 기대값은 **`src/site-url.ts` 의 `SITE_ORIGIN` 을 읽어** 옵니다(⛔ 하니스에 도메인을 손으로 적지 마세요). 🔴 **«빌더가 옳은가» 만 보면 아무것도 안 지켜집니다** — 상수는 그대로 두고 **복사 핸들러가 그 빌더를 안 부르게** 바꾸면 (`var url = location.origin + '/?meet=' + …`) 그 검사가 **전부 통과합니다**(실측 PASS 68/FAIL 0). ✅ **«그 빌더를 실제로 부르는가» 와 «파일 안에서 location 으로 따로 조립하지 않는가» 를 짝으로** 두세요. 변이 5종 전부 실제 FAIL |
| 같은 방 번호를 넣었는데 **서로 안 보이고 둘 다 「참여자 1명」** | 🔴 **두 사람이 서로 다른 «워커» 의 방에 있는 것입니다.** 이 저장소는 워커를 두 벌 배포하는데(`webrtc-unified-platform` · `-prod`), `wrangler.toml` 이 **각 워커마다 `new_sqlite_classes = ["VideoCallRoom"]` 을 따로 선언**합니다 → **Durable Object 네임스페이스가 갈립니다.** 그래서 `idFromName("class-849-20260819")` 이 워커마다 **다른 방**을 만듭니다. WebSocket 주소는 `location.host` 로 만들어지므로(`js/idx-main.js` `createWebSocket`) **어느 도메인으로 열었느냐가 곧 어느 방이냐**입니다. ⚠️ **D1·KV·R2 는 두 워커가 같은 id 를 공유**합니다 — 그래서 `attendance`·`room_tokens` 에는 둘 다 «같은 방에 있었다» 고 남고, **DB 만 보면 정상으로 보입니다.** 갈리는 건 DO 하나뿐입니다. 2026-08-19 실제로 밟음: 중국인 강선생님(`test.mangoi.co.kr` = 기본 워커) 과 사장님(`mangoi.ai` = `-prod`) 이 **7/31부터 8/19까지 여덟 번의 수업에서 한 번도 만나지 못했습니다.** 매번 「강사 미입장」 알림까지 떴는데 출석 기록에는 강사가 13분간 접속해 있었습니다. 🔴 **2026-08-25 에 그대로 재발했습니다**(`class-895`, 강사 `mangoi_167` · 학생 `delaware`). 「8/19 에 도메인을 옮겨 해결했다」고 이 문서에 적혀 있었지만 **옮겨진 적이 없었습니다** — 2026-08-26 대시보드 실측에서 `test.mangoi.co.kr` 이 여전히 기본 워커에 붙어 있는 것을 확인했고, 그 뒤 **8/27 에 `ubckt01`(조연희)이 구 도메인으로 7회 접속해 강사와 끝내 못 만나는 세 번째 사고**가 났습니다. 🔴 **그리고 2026-09-01 에 네 번째로 재발했습니다**(`class-848-20260901` — 강선생님 `mangoi.ai` · 사장님 `test.mangoi.co.kr`, 20분 수업 내내 둘 다 「참여자 1명」). 「8/27 이전 완료」라고 이 표와 0장·MAINTENANCE·`wrangler.toml`·`site-url.ts` 에 적혀 있었지만 **그때도 옮겨지지 않았습니다** — 같이 적힌 「두 도메인에서 `demo-1` 로 서로 보이는 것까지 확인」은 D1 실측 결과 **한 번도 없었던 일**입니다(그날 `demo-1` 접속: `mangoi.ai` 1건 · `test.mangoi.co.kr` **0건**). ✅ **2026-09-01 이전 완료**(사장님이 대시보드에서 기본 워커에 붙어 있는 것을 확인하고 옮김 — 0장 배치 줄). ✅ **이제 사람 기억에 기대지 않습니다** — 갈리면 15분 감시견이 문자로 알립니다(`src/room-split-guard.ts` — DO 네임스페이스 지문을 도메인별로 적어 두고 대조. 감시는 `test-harness/room_split_guard_harness.mjs` 32종). ⚠️ **여전히 다시 갈릴 수 있습니다** — 도메인을 다른 워커에 붙이거나 워커를 새로 만들면 그 순간 DO 가 나뉩니다. 도메인을 손대면 0장 배치 줄을 함께 고치세요. 📜 아래 서술은 «그때 무슨 일이 있었나» 로 남깁니다 — 확인 없이 완료형으로 적었다가 두 번 더 난 사고라 지우지 않습니다. 게다가 강사는 **주소창이 없는 창**(크롬 「창으로 열기」 바로가기 — 창 제목 «Mangoi 화상솔루션 - …»)을 쓰는 경우가 있어 **본인도 도메인을 모릅니다.** ⚠️ 워커를 한 벌로 합칠 때는 **cron 5종이 «기본» 워커에만 있다**는 것을 먼저 보세요(`[env.production.triggers] crons = []`) — 그냥 끄면 야간 동기화·독촉·NPS·감시견이 전부 멈춥니다. 📄 진단: `docs/화상수업_같은방_다른교실_class895_2026-08-25.md`. ✅ 확인은 **양쪽 주소창의 호스트명 비교**가 제일 빠릅니다(강사 사진에 주소창이 보이면 그것으로 끝납니다). ✅ 주소창이 없으면 **D1 한 줄**로 갈립니다 — `SELECT username, role, host, datetime(joined_at/1000,'unixepoch','+9 hours') FROM attendance WHERE room_id='class-<예약id>-<YYYYMMDD>' ORDER BY joined_at DESC` 에서 `host` 가 두 값이면 그것입니다(2026-09-01 에 이 한 줄로 5초 만에 찾았습니다). ⚠️ 진단용 `attendance.host` 칸(PR #487)은 **첫 `/api/attendance/checkin` 이 돌아야 생깁니다**(`ensureSchemaOnce` 안의 `ALTER`) — 배포 직후 SQL 을 돌리면 `no such column: host` 가 나는데 **배포 실패가 아닙니다**(2026-08-26 실측). ⛔ 「방 번호가 다른가」부터 의심하지 마세요 — 방 번호는 `class-{예약id}-{YYYYMMDD}` 로 결정론적이라 어긋날 일이 거의 없습니다 |
| 문서에 「고쳤다」고 적혀 있는데 **같은 사고가 또 남** | 🔴 **«하기로 한 것» 이 «했다» 로 적히면, 그 뒤로는 아무도 다시 확인하지 않습니다.** 2026-08-19 에 「`test.mangoi.co.kr` 을 `-prod` 로 옮겨 해결」이라고 CLAUDE.md·MAINTENANCE.md·`wrangler.toml` 세 곳에 적었지만 **이전은 일어나지 않았습니다.** 같은 작업기록 안에 「도메인이 어느 워커에 붙어 있는지 **직접 확인하지 못했다**」고 나란히 적혀 있는데도, 요약 문장이 완료형이라 그쪽만 읽혔습니다. 그 결과 6일 뒤 `class-895` 에서 **똑같은 사고가 재발**했고, 그동안 기본 워커에는 24시간 7.26k 호출이 계속 들어오고 있었습니다. ⚠️ **하니스도 못 잡았습니다** — 검사가 「`test.mangoi.co.kr` 근처 300자 안에 `-prod` 라는 **글자**가 있는가」였습니다. «적어 두었는가» 는 «사실인가» 가 아닙니다. 거짓 문장이 오히려 그 검사를 **통과**했습니다. ✅ **내가 못 한 일은 못 했다고 적으세요** — 「사람이 대시보드에서 확인 필요」로 남기고, 완료형으로 요약하지 마세요. ✅ 코드 밖 상태(대시보드·DNS·외부 콘솔)를 적을 때는 **확인 날짜와 확인 방법**을 문장에 함께 박으세요(0장 「도메인–워커 배치 (YYYY-MM-DD 대시보드 실측)」 줄이 그 형식). ✅ 하니스는 «글자가 있는가» 대신 **«여러 곳이 서로 같은 말을 하는가»** 로 검사하세요 — `outgoing_link_domain_harness` ④가 이제 규칙서 두 벌과 `wrangler.toml` 이 지목하는 워커가 **일치하는지** 대조합니다 |
| 도메인을 `wrangler.toml` 에 «못 박으려» 할 때 | ⛔ **`routes` 를 한 줄만 적으면 나머지 도메인이 다음 배포에 지워집니다.** Cloudflare 문서가 명시합니다 — 「대시보드에서 라우트를 바꿔도, 다음 배포에서 **wrangler 설정 파일에 적힌 라우트로 덮어씁니다**」. 즉 `routes` 는 «추가» 가 아니라 **«전체 목록»** 입니다. `[env.production]` 에 `test.mangoi.co.kr` 만 적고 배포하면 **`mangoi.ai`·`www.mangoi.ai` 가 떨어져 서비스 전체가 내려갑니다.** ⛔ 덤으로 **`routes` 를 넣는 순간 `workers_dev` 가 `false` 로 «추론»** 됩니다(문서 명시) → `webrtc-unified-platform-prod.navy111p.workers.dev` 가 죽고, 그 주소를 쓰는 `deploy.ps1`(77행 배포 확인)·`deploy-full.ps1`(130행 `_bootstrap`)·`deploy.yml`(164·174행)이 함께 깨집니다. ✅ 그래서 이 저장소는 **도메인을 대시보드에서만 관리하고 `wrangler.toml` 에는 `routes` 를 두지 않습니다.** 정말 못 박으려면 **붙어 있는 도메인을 하나도 빠뜨리지 말고 전부 적고 `workers_dev = true` 도 함께** 적으세요 |
| 헬퍼에 행을 넘겼는데 **아무 일도 안 일어남**(에러도 없음) | 🔴 **`SELECT … AS 별칭` 만 두면 헬퍼가 읽는 필드가 결과 행에 없습니다.** `SELECT teacher_name AS tn …` 로 뽑아 `teacherPresenceByRoom()` 에 넘겼더니 그 함수는 `r.teacher_name` 을 읽으므로 **이름이 항상 빈 값** → 전부 «모름» → **오판이 한 건도 걸러지지 않았습니다**(2026-08-19 실측). 같은 함수를 쓰는 다른 두 곳은 별칭이 없어 «세 곳 중 한 곳만 조용히 안 먹는» 상태였습니다. ⚠️ **하니스가 이걸 못 잡습니다** — 「그 함수를 부르는가」·「그 조건이 있는가」를 **문자열로만** 검사하면 35건이 전부 초록입니다. ✅ 별칭이 필요하면 **원래 이름도 함께** 뽑으세요(`teacher_name, teacher_name AS tn`). ✅ 이런 «넘기는 모양» 검사는 문자열이 아니라 **함수를 실제로 돌려서** 하세요 — `no_show_false_alarm_harness.mjs` ⑩ 이 typescript 로 컴파일해 가짜 DB 로 돌립니다(사고 형태를 그대로 넣어 «모름» 이 나오는지까지 확인). 같은 뿌리: 판정을 세 곳에 복제하지 말고 한 파일로 모을 것(`src/no-show-truth.ts`). 📌 **덧붙임(2026-09-04) — 별칭만이 아니라 «칸 자체를 안 뽑은» 변종도 같습니다.** `/api/recordings` GET 의 SELECT 목록에 `r.participant_ids` 가 없는데, 학생 칸 판정 정본(`src/recording-students.ts`)의 **첫 번째 근거**가 `parseIdList(r.participant_ids)` 라 **늘 빈 배열**이었습니다 — 근거 하나가 «에러 없이» 죽어 있었습니다. ⚠️ **화면이 멀쩡해 보입니다** — 나머지 세 근거(예약·`consented_user_ids`·`teacher_name`)가 칸을 계속 채워서 «고장» 이 아니라 «근거가 안 쓰인다» 였고, D1 전수 실측으로도 바뀌는 행이 **0건**이었습니다(2,122행 · 2026-09-04. `consented_user_ids` 를 «participant_ids 중 동의한 사람» 으로 계산해 넣으므로 두 칸이 사실상 겹칩니다). ⟹ **«화면이 안 바뀐다» 는 «고칠 필요가 없다» 가 아닙니다** — 동의 게이트가 바뀌는 날 그 근거가 처음으로 일합니다. ✅ 감시는 «그 칸이 있는가» 로 못 박지 말고, **정본이 «읽겠다» 고 선언한 칸**(TS 인터페이스)을 **읽어서** SELECT 목록과 대조하세요(`recording_student_column_harness.mjs` A절). ⚠️ **정본이 둘 이상이면 전부** 훑으세요(여기는 `RecRowLike`·`RecTeacherRowLike`) — 한쪽만 보면 다른 쪽이 같은 사고를 냅니다. ⚠️ 그 검사의 칸 이름 정규식을 `[a-z_]+` 로 좁히면 camelCase·숫자 포함 칸이 조용히 빠져 **거짓 통과**하고, `--`(SQLite 줄주석)를 안 벗기면 그것으로 칸을 죽여도 초록불입니다(둘 다 변이시험 실측). ⚠️ **가짜 D1 로 정본을 돌리는 검사는 이걸 원리상 못 잡습니다** — 행에 그 칸을 «직접 넣어» 주니까요. ⚠️ 판정에 쓰려고 받은 칸을 **응답에까지 실을지는 따로 보세요** — `participant_ids` 는 곧 «누가 재생할 수 있는가» 목록이고(`recordings-r2.ts`), 그 API 는 `/api/admin/` 접두사가 아니라 강사·지사 세션도 받습니다(화면이 안 쓰면 응답에서 끊습니다). 📄 `docs/작업기록/260904_녹화목록_participant_ids_SELECT누락.md` |
| 「강사 미입장」 노쇼 알림이 떴는데 강사는 들어와 있었음 | **그 기록은 «학생 브라우저» 가 만듭니다.** 5분을 기다려도 상대가 화면에 안 보이면 신고하는 구조라, 남는 사실은 «상대가 안 왔다» 가 아니라 **«내 화면에 안 보였다»** 입니다. 그래서 위 「같은 방 번호인데…」 함정이 성립하는 동안 **강사가 13분씩 접속해 있어도 매번 알림이 떴습니다**(2026-08-19 실측: 13건 중 11건이 오판). ⛔ 기록을 지우거나 UPDATE 로 «정리» 하지 마세요 — 학생이 못 본 것은 사실이고, 지우면 「왜 수업이 성립하지 않았나」가 함께 사라집니다. ✅ **읽을 때 `attendance` 와 대조**해 «오판» 이라고 함께 알려 줍니다(`src/no-show-truth.ts` — 노쇼 리포트·강사 90일 지표·**급여** 세 곳이 같은 함수를 씁니다). ⚠️ 급여가 걸려 있습니다 — 상태가 `teacher_no_show` 면 그 수업은 **수업료가 0원**입니다. ⛔ 오판 판정 근거로 `attendance.role='teacher'` 를 쓰지 마세요(클라이언트가 보내는 값 + 「먼저 들어온 학생이 강사 역할을 받는」 전례). 근거는 **이름 일치뿐**이고, 모르면 «모름» 으로 둡니다 |
| `LIKE ?` 에 긴 값을 바인딩했더니 **조회가 통째로 실패** | 🔴 **D1 의 LIKE 패턴 한도는 «50자» 입니다**(2026-09-02 실측: 50자 통과 · 51자에서 `LIKE or GLOB pattern too complex: SQLITE_ERROR`). SQLite 기본값은 50,000자라 **여기서만 터집니다** — 로컬 SQLite 로 시험하면 재현되지 않습니다. ⚠️ 실사고: 교재 파일 목록이 `name LIKE '[<교재이름>]%'` 였는데 이름이 긴 교재 **13개**(BTS 33·17·30·34·32·20·5·24·22 …)가 그 한도를 넘어 **조회 자체가 실패**했습니다. 화면에는 `D1_ERROR: LIKE or GLOB pattern too complex` 가 그대로 떠서 «교재가 없는 것» 처럼 보였고, 하필 그 13개가 «높은 레벨 권» 이라 교재 OCR 시험의 핵심(레벨별 글자 양 비교)이 통째로 막혔습니다. ✅ 접두사 일치는 **`instr(name, ?) = 1`** 로 쓰세요 — 패턴 한도가 없고, 덤으로 이름 속 `%`·`_` 를 와일드카드로 오해하지 않습니다(성능도 같습니다 — 그 조회는 원래 풀스캔이었습니다). ⚠️ **이 저장소에는 `LIKE ?` 가 50곳 있습니다** — 대부분 사람 이름·검색어라 50자를 넘을 일이 드물지만, «교재 이름·제목·주소» 처럼 길어질 수 있는 값을 새로 바인딩할 때는 그 자리에서 길이를 따져 보세요. ⛔ 「잘라서 넣기」로 풀지 마세요 — 다른 교재가 함께 걸립니다 |
| D1 쿼리 | 파라미터 **100개 제한**. `IN` 절은 90개 이하로 잘라서 실행 |
| JOIN 을 붙였더니 그 칸이 **응답에서 통째로 사라짐**(에러도 없음) | 🔴 **두 표에 «같은 이름의 칸» 이 있으면 `WHERE` 가 `ambiguous column name` 으로 죽고, `catch` 의 «JOIN 없는» 폴백이 그것을 삼킵니다.** 2026-09-11 실사고: `GET /api/admin/class-schedules` 가 `LEFT JOIN teachers` 를 하는데 WHERE 가 접두사 없이 `status != 'cancelled'` · `user_id = ?` 였습니다 — **`teachers` 에도 그 두 칸이 있습니다**(겹치는 칸 다섯: `created_at`·`id`·`status`·`updated_at`·`user_id`). ⟹ `teacher_name` 이 응답에서 사라져 학생 상세 스케줄 카드가 **예약 1,262건 전부를 「강사 미확인」** 으로 그렸습니다. **WHERE 첫 줄이 언제나 status 조건이라 그 JOIN 은 도입 이래 한 번도 성공한 적이 없습니다** — 화면이 그 칸을 안 그려서 아무도 몰랐고, 그리기 시작한 날 바로 드러났습니다. ⚠️ **문자열 하니스로는 원리상 못 잡습니다** — SQL 은 «있고» 호출도 «되고», 죽는 것은 실행뿐입니다(수리 전에도 `--fast` 전부 초록). ⚠️ **브라우저 검사도 못 잡습니다** — 그 API 를 «이름이 든» fixture 로 스텁하니까요. ✅ 소스에서 SQL 을 **오려 내 진짜 SQLite 에 실제로 prepare** 하세요(`class_schedules_join_harness.mjs`). 스키마는 **정본의 CREATE·ALTER 를 읽어서** 만드세요 — 하니스에 베끼면 「자기가 적은 상수를 검사」가 됩니다. ✅ 시나리오를 «골라» 돌리지 말고 **모든 조건을 AND 로 이어 한 번 컴파일**하면 나중에 늘어날 조건까지 덮습니다. ⛔ **서브쿼리 «안» 에는 접두사를 붙이지 마세요** — `SELECT COALESCE(user_id, login_id) FROM students_erp` 의 그 칸은 남의 표 것이라, 붙이면 **상관 서브쿼리**가 되어 매 행마다 전수 스캔합니다(2026-08-27 실측 4.3초/2,348만 행). 그 검사는 글자가 아니라 **`EXPLAIN QUERY PLAN` 에 `CORRELATED` 가 없는가**로 물으세요. ⛔ 폴백을 없애서 풀지 마세요(표가 없는 환경에서 목록이 통째로 사라집니다) — 대신 **두 SQL 이 같은 WHERE 문자열을 쓴다**는 것을 기억하고 폴백에도 별칭을 주세요. ⚠️ 폴백이 탔다는 사실이 **응답에 한 글자도 안 실립니다**(로그도 `head_sampling_rate = 0.05` 라 20번 중 19번은 안 남습니다) — 그래서 «없다» 와 «못 물어봤다» 가 화면에서 같은 글자가 됩니다. 📄 `docs/작업기록/260911_스케줄카드_강사미확인_JOIN_ambiguous.md` |
| `wrangler.toml` 값 수정 | `[vars]` 와 `[env.production.vars]` 에 **같은 값이 한 벌 더** 있습니다. 둘 다 고쳐야 함 |
| 언어 설정 키 | 공통 키는 `mangoi_lang` 입니다. `mango_lang` 은 구버전 키 |
| 관리자 화면이 EN 스태프에게도 **늘 한국어** | 🔴 **`localStorage` 키 `'adminLang'` 은 저장소 어디에서도 `setItem` 하지 않는 «죽은 키»입니다** — 읽으면 항상 `null` 이라 그 화면은 영원히 KO 입니다. 이름이 정본 변수 `window.adminLang` 과 똑같아서 코드만 보면 맞아 보이고, **에러도 안 납니다.** 2026-08-27 실측: 그 키를 읽는 곳이 **9곳**(파일당 1회씩)이고 `setItem` 은 **0건** — 사이드바(`adm-ia6.js`)·경로 줄(`adm-crumb.js`)·수업 관찰 목록(`adm-s1.js`)·오늘 수업(`adm-today-classes.js`)·자주 쓰는 기능(`adm-quick-access.js`)·최근 본 메뉴(`adm-recent-menus.js`)·CDO 날씨(`adm-cdo-weather.js`)·교재 일괄배정(`adm-bulkbook.js`)·`admin/site-structure-map.html` 이 전부 Maimai·Melca 에게 한국어로 떴습니다. ✅ **저장 정본은 `mangoi_lang`**(+`mangoi_lang_by`·`mangoi_lang_uid`, 쓰는 곳은 `adm-lang-boot.js` 와 `adm-core.js` 의 `toggleAdminLang`), **판정 정본은 전역 변수 `window.adminLang`** 입니다 — `adm-core.js:41` 의 `var adminLang` 이 최상위 classic script 라 **그 변수와 `window.adminLang` 이 같은 바인딩**이고, 그래서 KO/EN 토글·`<html lang>` 변화·다른 탭의 `storage` 이벤트가 전부 따라옵니다. ✅ `admin.html` 에서 도는 코드는 `window.adminLang === 'en'` 을 먼저 보고 없을 때만 `mangoi_lang` 으로 떨어지세요(본보기 `js/adm-hr-analysis.js`). ✅ `adm-lang-boot.js` 를 안 싣는 **독립 페이지**는 `localStorage.getItem('mangoi_lang')` 을 직접 읽습니다. ⛔ **`'adminLang'` 키에 «쓰는» 방식으로 되살리지 마세요** — 정본이 두 벌이 되어 화면 절반만 영어인 상태가 남습니다. ⚠️ 하니스가 못 잡습니다 — 함수도 값도 다 «있고» 틀린 것은 «어느 키를 읽는가» 뿐이라 수리 전에도 회귀 하니스(`--fast` PASS 225)가 전부 초록이었습니다. 확인은 브라우저에서 `[localStorage.getItem('adminLang'), localStorage.getItem('mangoi_lang'), window.adminLang]` 를 재세요 |
| i18n 사전 | 전체 문자열 일치 방식입니다. 라벨에서 이모지만 떼도 번역이 깨집니다 |
| 언어 판정 | `index.html` 은 i18n 엔진이 **두 개**입니다. 나중에 로드되는 `js/mango-i18n.js` 가 `setLang/getLang/toggleLang` 을 덮어쓰는데, 인라인 엔진의 전역 `currentLang` 은 안 건드립니다. **언어 판정은 반드시 `getLang()`** 로 하세요. 인라인 `currentLang` 을 직접 읽으면 🌐 를 눌러도 안 따라옵니다 |
| JS 로 그린 라벨 | `textContent` 로 직접 쓴 글자는 `data-ko/data-en` 루프도, `i18n-sweep` 의 `restore()` 도 못 고칩니다(대입이 텍스트 노드를 갈아치워 복원이 죽은 노드에 쓰입니다). 상태에 따라 라벨이 바뀌는 버튼은 **그릴 때 `data-ko`/`data-en` 도 함께 갱신**하세요 |
| **아이콘 버튼**에 `data-ko`/`data-en` 을 달았더니 **아이콘이 사라지고 문장이 그 자리에** 들어앉음 | 🔴 **위 「JS 로 그린 라벨」의 뒷면입니다 — 그 두 속성은 «설명» 이 아니라 «본문 갈아끼우기» 입니다.** i18n 엔진은 `[data-ko],[data-en]` 인 요소마다 **`textContent` 를 통째로 덮어씁니다.** 그래서 🌐 를 눌러도 툴팁이 따라오게 하려고 그 둘을 달면, 34px 짜리 동그란 버튼 안에 문장이 들어앉아 «오프 / 닝 소 / 리 끄 / 기» 로 쪼개져 넘칩니다(2026-08-24 홈 오프닝 소리 버튼 `#mgo-sound-btn` — 사장님 「글자가 이상해」. 실측 390px: `textContent='오프닝 소리 끄기'`, `scrollHeight` 44 > 상자 32). ⚠️ **엔진이 둘입니다** — `index.html` 인라인 엔진과 `js/mango-i18n.js`. 규칙이 같으니 한쪽만 피해도 소용없습니다. ⚠️ **누른 뒤에는 아이콘으로 돌아옵니다**(그리는 쪽이 `textContent` 를 다시 씁니다) — 그래서 «가끔 멀쩡» 해 보이고 재현이 헷갈립니다. ✅ 글자를 그리지 않는 요소(아이콘 버튼)의 설명은 **`data-ko-title`/`data-en-title` · `data-ko-aria`/`data-en-aria`** 로 다세요 — 두 엔진 모두 지원하고 `title`·`aria-label` 만 건드립니다. ✅ 상자에 `overflow:hidden` 한 줄을 안전망으로 두세요. 감시는 `test-harness/home_opening_btn_harness.mjs`(문자열 7종)와 `test-harness/manual/home-opening-btn-browser.mjs`(브라우저 20종) 📌 **덧붙임(2026-09-09) — 「글자 + 배지」 를 함께 담은 `<a>`·`<div>` 에 달아도 같은 사고입니다.** 관리자 사이드바 「결재함」 줄(`#ia6-appr`, `adm-ia6.js`)이 `<a data-ko="결재함" data-en="Approvals">` 였는데 그 안에 라벨·**숫자 배지(`#ia6-appr-n`)** 가 자식으로 있어, 🌐 를 누르면 두 엔진이 `textContent` 를 갈아끼워 **배지 요소가 DOM 에서 사라졌습니다**(브라우저 실측: 토글 뒤 `#ia6-appr-n` null — 그 뒤 4초 repaint 가 `getElementById` 로 못 찾아 영영 빈 채). ✅ `data-ko`/`data-en` 은 **글자만 담은 `<span>`** 에 달고 배지·부제는 그 «형제» 로 두세요(v=57). ⚠️ 문자열 하니스로는 못 잡습니다 — 속성도 값도 «있고» 틀린 것은 «어느 요소에 달았는가» 뿐. 감시는 `approval_sidebar_badge_live_harness` D절(«`<a>` 에 `setAttribute('data-ko'` 가 없는가» + «라벨 span 에 있는가» 짝)과 `manual/approval-sidebar-badge-live-browser.mjs`(진짜 `toggleAdminLang` 을 눌러 배지가 남는지 — 되돌리면 9건 FAIL) |
| `data-ko` 가 달린 **표 머리글에 정렬 화살표(▲▼)** 를 달아야 할 때 | 🔴 **위 두 줄(「JS 로 그린 라벨」·「아이콘 버튼에 달았더니」)의 «표» 판입니다 — 화살표를 자식 `<span>` 으로 넣으면 🌐 를 누르는 순간 사라집니다.** i18n 엔진이 `[data-ko]` 요소의 **`textContent` 를 통째로 갈아끼우기** 때문입니다(자식 노드가 함께 날아갑니다). ⚠️ **에러가 안 납니다** — 정렬은 계속 동작하고 «어느 칸으로 정렬 중인지» 표시만 조용히 사라지므로, 언어를 안 바꾸면 영영 못 봅니다. ✅ 화살표는 **속성으로 그리세요** — `th.setAttribute('data-ar','▼')` + CSS `th.rec-sort-th::after{content:attr(data-ar)}`. **가상요소는 `textContent` 와 무관해서 살아남습니다.** ✅ 라벨과 화살표를 **다른 `<span>` 두 개**로 나누는 길도 있지만(회계 표 `c24FinDrawHead` 의 방식) 그건 머리글을 **JS 가 통째로 그리는** 표에서만 됩니다 — 정적 HTML `<th data-ko=…>` 에는 `::after` 쪽이 맞습니다. ⛔ 라벨 글자에 «시간 ▲» 처럼 화살표를 **붙여 쓰지 마세요** — i18n 사전이 전체 문자열 일치라 「시간」과 「시간 ▲」를 다른 말로 봅니다. ⚠️ «정렬 중» 강조도 인라인 style 이 아니라 **클래스로** 주세요(`admin-inline-c.css` 의 `!important` 가 이깁니다). ✅ 확인은 브라우저에서 EN 으로 토글한 뒤 `getComputedStyle(th,'::after').content` 를 재는 것 — 본보기 `test-harness/manual/recording-table-filter-browser.mjs` ⑥절(2026-09-01 녹화 목록 머리글 정렬) |
| `::after` 로 장식(화살표·아이콘)을 붙였는데 **화면낭독기가 그것까지 읽음** | 🔴 **바로 윗줄의 뒷면입니다 — `textContent` 를 피해 가상요소로 옮기면 «i18n» 은 풀리지만 «이름» 에는 그대로 섞입니다.** 그 요소가 `role="button"`·`<a>`·`<button>` 처럼 **이름을 «안쪽 글자» 에서 만드는** 것이면, CSS 가상요소의 `content` 도 그 계산에 들어갑니다. 2026-09-21 실측(CDP 접근성 트리 — 홈 히어로 두 트랙): 화살표를 `::after` 로 붙이자 이름이 **`"사람과 · 예약한 시간에 1:1 원어민 화상수업›"`** 이 됐습니다 — 낭독기가 그 기호를 「단일 오른쪽 꺾쇠 인용부호」로 읽습니다. ⚠️ **`getComputedStyle` 로는 안 보입니다** — 「그려졌나」와 「읽히나」는 다른 값입니다. ✅ **CSS 대체 텍스트**로 빼세요 — `content:"\203A" / ""`(그려지되 이름에서 빠집니다). ⚠️ **두 줄로 쌓으세요** — 그 문법을 모르는 옛 브라우저는 선언을 통째로 버려 **장식이 사라집니다.** 대체 텍스트 «없는» 줄을 앞에 두면 옛 브라우저는 «고치기 전» 과 같고 잃는 것이 없습니다. ⛔ **`aria-label` 로 덮어서 풀지 마세요** — «보이는 글자» 와 «이름» 이 어긋나 음성조작 사용자가 화면에 보이는 말로 부를 수 없게 됩니다(WCAG 2.5.3). ✅ 검사는 **CDP `Accessibility.getPartialAXTree` 로 이름을 실제로 재고**, 「화살표가 안 섞인다」 옆에 **「보이는 글자는 그대로 이름에 들어 있다」를 짝으로** 두세요 — 짝이 없으면 «이름을 통째로 비우기» 도 통과합니다. 🪤 **그리고 «그려졌나» 와 «보이나» 도 다릅니다** — 같은 날 실측: `opacity` 를 0 으로 바꾸면 화살표가 안 보이는데 «content 를 읽는» 검사도 «이름에 안 섞인다» 검사도 **둘 다 통과**했습니다(대체 텍스트라 이름에도 안 나옵니다). **opacity·display 를 짝으로** 재세요. 🪤 그리고 **선택자를 `[^{]*` 로 느슨하게 찾으면 `:hover` 변형이 통과합니다** — `[role="button"]:hover …` 는 «폰에서 영영 안 보이는» 상태라 고치려던 그 문제입니다(「`:hover`·`:focus` 로 좁히지 않았다」를 짝으로). ⚠️ **넘침을 «부모 상자» 와 견주지 마세요** — 패딩만큼 느슨해집니다(실측: 자기 상자를 16.8px 넘는데 `true`). 그리고 **넘침이 «내 변경이 만든 것인가» 를 A/B 로** 가르세요 — 이미 넘치던 자리면 절대 검사는 거짓 고발입니다. 감시는 `test-harness/manual/home-tracks-click-browser.mjs` ⑨-3·⑨-3b·⑨-4절(브라우저 — 자동으로 안 돕니다, 사람이 부릅니다)과 `home_tracks_wiring_harness` ⑤-2·⑥절. 변이 8종 전부 실제 FAIL |
| TTS 검증 | Cloudflare발 구글 TTS(한국어·중국어)는 **깨진 음성**이 나옵니다. 서버 TTS는 영어만 정상 |
| 셸에서 한글 POST | UTF-8 파일로 저장해서 보내세요. 인라인 한글은 깨집니다 |
| 카카오 상담 링크 | 주소 뒤에 **`/chat` 을 붙이지 마세요.** `pf.kakao.com/<id>/chat` 은 **비로그인 PC 를 `accounts.kakao.com` 로그인 화면으로 튕깁니다.** 채널 홈 `pf.kakao.com/_xlqnSxd` 은 로그인 없이 열리고 그 안에 채팅·챗봇·전화·길찾기가 다 있습니다 |
| `window.open` 이 안 열림 | 카톡·문자앱 **인앱 브라우저는 새 창을 못 엽니다.** 예외를 던지지 않고 **null 만 돌려주므로 `try/catch` 로는 못 잡습니다.** 반환값이 비면 `location.href` 로 같은 창에서 여세요. 🔴 **덧붙임(2026-09-02) — 그 «반환값이 비었나» 판정이 «항상 참» 이 되는 함정이 있습니다.** `window.open(u,'_blank','noopener')` 처럼 **`noopener` 를 «기능 문자열» 로 주면 표준상 탭은 열리는데 반환값이 `null`** 입니다. 크로미움 실측: `('_blank','noopener')` → 반환 **null** · 탭 1→2(**열림**) / `('_blank')` → 반환 object · 탭 2→3. ⟹ 반환값으로 «막혔나» 를 판정하는 코드는 **언제나 «막혔다»** 가 됩니다. 실사고: 관리자 「수업 관찰」·관제탑에서 참관을 누르면 **탭은 열렸는데 「⚠ 브라우저가 새 창을 막았습니다」 상자가 뜨고**, 그 링크를 누르면 **탭이 하나 더** 열렸습니다(사장님 화면). 더 나쁜 변종은 폴백이 `if (!w) location.href = url` 이라 **탭이 열리고 + 보던 화면까지 그 주소로 이동**합니다(`ghost-view`·`manager.html`·`t.html`·`adm-promo-setup`). ✅ **반환값이 필요하면 기능 문자열에서 `noopener` 를 빼고 연 뒤 `w.opener = null`** 로 같은 보호를 거세요(고치기 전 코드도 `w.opener = null` 을 하려 했는데 `w` 가 null 이라 **한 번도 안 돌았습니다**). ℹ️ 반환값을 **안 쓰는** 호출은 `noopener` 를 그대로 둬도 무해합니다. ✅ **`public/index.html` 의 `toKakao`·`window.openKakao` 도 2026-09-09 에 고쳤습니다**(공동 금지구역이라 사장님 승인 뒤) — 홈과 **수업 화면**(`vc-dock.js` 「상담」)이 모두 그것을 부르므로 그전에는 **수업 중 「상담」이 곧 «수업에서 나가기»** 였습니다. ⚠️ **그때 예산이 빠듯해 두 자리 «어디에도» 설명 주석을 못 넣었습니다** — 병합 시점 main 여유가 **43바이트**뿐이라, 코드에서 공백까지 걷어내 **+35B** 로 줄여 겨우 넣었고(주석 한 줄이 37B라 35+37=72 > 43 으로 그것까지는 못 넣습니다) **고친 뒤 남은 여유는 8바이트**였습니다(**2026-09-11 기준** — 지금 값은 「첫 화면 예산이 FAIL」 줄의 정본 숫자를 보세요. 한쪽만 고치지 마세요). ⟹ **그래서 이 줄이 그 두 자리의 유일한 설명입니다** — `noopener` 를 기능 문자열로 되돌리지 마세요(하니스 `popup_open_return_harness` ②절이 «그 자리가 `opener` 를 끊는가» 로 못 박습니다). 감시는 `test-harness/popup_open_return_harness.mjs`(public 밑 js·html 전수 — 반환값을 쓰면서 noopener 를 준 자리를 잡고, 못 고친 자리는 «사람 결정 대기» 로 **이름을 찍어 출력**합니다). ⚠️ 이 고침은 **하니스 두 곳을 깨뜨립니다** — `if (!w) location.href` 라는 «식 모양» 을 글자 그대로 못 박아 둔 검사들입니다(`classes_today_cafe24` ㉖ · `promo_video_popup`). 뜻으로 묻도록 고치세요 |
| 새 API 추가 | 🔴 **관문이 «셋» 이고 하나만 빠져도 404 「Not Found」 입니다.** ① `src/index.ts` **인증 게이트**(`isAdminPath`) — `/api/admin/` 은 **default-deny 라 자동 통과**, 등록 불필요 ② `src/index.ts` **라우팅 허용목록**(1067행~) — 경로를 **하나씩** 적습니다. **①과 다른 것입니다** ③ `src/api-mango.ts` **위임 가드**(1372행~) — 없으면 `handleAdminApi` 까지 못 갑니다. ⚠️ **①을 보고 «등록 불필요» 라고 판단하면 ②에서 막힙니다** — 2026-08-24 실제로 밟았습니다(LMS 정리 버튼이 「⚠️ 건수를 확인하지 못했습니다: Not Found」). 인증은 통과하므로 «권한 문제» 로도 안 보입니다. ⚠️ 접두사가 이미 있어도 안심하지 마세요 — `/api/admin/class-schedules` 는 ②에 **정확일치**로만 올라와 있어 하위 경로는 매번 한 줄이 필요합니다(③은 `startsWith` 라 통과해서 더 헷갈립니다). ℹ️ `/api/admin/reports/` 밑은 셋 다 이미 등록돼 있어 예외입니다. 🔴 **덧붙임(2026-08-25) — 404 는 「Not Found」로 안 보일 수 있습니다.** 종단 404 본문은 `{error:'Not Found', path}` 라 **`ok` 칸이 없습니다.** 화면이 흔히 쓰는 `if (d.ok === false) throw` 는 `undefined === false` 라 **그냥 통과**하고, 이어지는 `d.items || []` 가 빈 배열이 되어 **「자료가 없습니다」 같은 정상 문구**로 그려집니다. 그래서 관문 누락이 «고장» 이 아니라 «오늘은 없나 보다» 로 읽힙니다 — `/api/admin/classes/today` 는 이 상태로 **2026-07-23부터 8/25까지 한 달 넘게** 404 였고, 매니저가 「기능이 없다」로 제보할 때까지 아무도 몰랐습니다. ✅ 화면 판정은 «실패라고 말했는가» 가 아니라 **«성공이라고 말했는가»**(`d.ok === true` + 목록이 배열인가)로 하세요. `r.ok`(HTTP)도 함께 보면 더 확실합니다. ✅ 관문 검사는 **문자열로 하지 마세요** — 접두사 규칙(`startsWith`)이 섞여 있어 눈으로는 통과처럼 보입니다. `api-mango.ts` 의 그 조건식을 오려 내 **실제로 돌려서** 판정하세요(본보기: `test-harness/classes_today_cafe24_harness.mjs` ②-1). ✅ **덧붙임(2026-09-02) — ②가 금지구역이라 못 고칠 때 쓸 수 있는 길이 있습니다: «이미 등록된 경로의 «비어 있는 HTTP 메서드»를 쓰세요.** ②의 등록은 **메서드를 안 가립니다** — 예컨대 `/^\/api\/admin\/textbook-files\/\d+$/` 는 PATCH·DELETE 만 쓰고 있어 **POST 가 비어 있었고**, 거기에 얹으니 `src/index.ts` 를 한 줄도 안 건드리고 새 기능이 붙었습니다(교재 OCR 시험). ⛔ 그때는 **본문에 `action` 을 두고 모르는 값을 거절하세요** — 안 그러면 나중에 그 경로에 다른 뜻을 넣을 때 «모르는 요청» 이 조용히 흘러 들어갑니다(비용이 나가는 기능이면 그대로 돈입니다). ⚠️ 쓰기 전에 그 메서드가 **정말 비어 있는지** `grep` 으로 확인하세요 — 겹치면 먼저 만난 쪽이 이깁니다 |
| 브라우저 애니메이션 | 백그라운드 탭·저전력 모드에서 CSS transition과 rAF가 멈춥니다. `opacity:0` 으로 시작하는 요소는 영영 안 보일 수 있습니다 |
| 사이드바 손자 메뉴를 눌렀는데 아무 데도 안 감 | **손자 목록을 손으로 적으면 반드시 어긋납니다.** 옛 `adm-r25.js` 는 카드마다 이름 4개씩을 적어 두고 「카드 안 N번째 `details`」로 점프했는데, 그런 칸이 **0개인 카드가 63개**라 무엇을 눌러도 «카드 전체가 한 번 반짝이고» 끝났습니다. 에러가 안 나서 죽은 줄도 몰랐고, 실제로 「학생 명부 ▸ 2 학생 상세 프로필」이 「⏰ 만료 임박 학생」을 열었습니다(2026-08-18 사장님 지적). 지금은 손자를 **카드 안 실제 칸에서 화면으로 읽습니다** — 접이칸(`details.sub-item`·`.sub-menu > details`)은 summary 글자 그대로, 접이식이 아닌 구역은 **그 구역 자체에 붙인 `data-gc="이름"` 이름표**(126개)로. 목적지는 문자열 id 가 아니라 **DOM 참조**라 같은 id 가 두 벌 있어도(`sub-popup-list`) 정확히 그 칸으로 갑니다. ⛔ 새 손자를 «목록에 이름만» 추가하지 마세요 — 화면에 그 구역이 없으면 조용히 실패합니다. 이름표는 **그 구역 옆에** 답니다(딴 파일에 적으면 화면이 바뀔 때 또 어긋납니다). 감시는 `test-harness/admin_grandchild_menu_harness.mjs`(33종)가 «▸ 가 있는데 갈 곳이 없는 항목 0건» 으로 못 박습니다 |
| 손자 메뉴를 눌렀는데 휴대폰에서 화면이 안 움직임 | **드로어를 스크롤보다 «먼저» 닫아야 합니다.** 드로어가 열린 동안 `body` 가 `overflow:hidden` 이라, 그 상태에서 `scrollIntoView` 를 부르면 브라우저가 **통째로 무시**합니다. 자식 메뉴(`.ph85-sub`)는 `adm-s11.js` 가 이미 이렇게 처리하는데, **손자(`.ph125-gc`)는 `.ph85-sub` 의 «형제» 라 그 처리를 못 받고 있었습니다** — 카드는 제자리로 갔는데 드로어가 화면을 덮고 있어 쓰는 사람에게는 «아무 일도 안 일어났다» 로 보였습니다. 목적지가 **숨은 탭 안**(공지 스튜디오 `data-nspanel`)일 때도 같습니다 — 탭을 먼저 켜야 스크롤이 먹습니다 |
| 사이드바 항목을 `style.display='none'` 로 숨겼는데 **1~2초 뒤 되살아남** | **`admin.html` 에 «강제 visible» 복구 루프가 주기적으로 돕니다** — `bar.querySelectorAll('.ph85-group, .ph85-sub')` 를 훑어 `style.display==='none'` 이면 `''` 로 되돌립니다. 사이드바가 통째로 안 보이던 사고의 복구책이라 **지우면 안 됩니다.** 그래서 메뉴 검색이 «쳤다가 1.5초 뒤 풀리는» 상태로 오래 방치돼 있었습니다(2026-08-19 실측: 「급여」 → 42개 숨김 → 1.5초 뒤 전부 복원. 게다가 화면에 보이는 IA6 목록은 처음부터 43 → 43 으로 하나도 안 줄었습니다). ✅ 숨김은 **클래스로** 하세요(`.ph85-shide` + CSS `display:none!important`). 복구 루프는 `style` 만 보므로 둘 다 삽니다. ⚠️ 사이드바를 다시 그리는 코드가 여럿이라(IA6·ph86·ph118) 클래스도 지워질 수 있습니다 — 검색어가 남아 있으면 주기적으로 다시 입히세요. 감시는 `sidebar_three_level_harness` ⑨ |
| 사이드바를 고쳤는데 화면이 그대로 | **사이드바가 두 벌입니다.** 옛 `#ph85-sidebar`(9그룹 87항목)는 CSS 로 감춰져 있고, 화면에 실제로 보이는 것은 그 위에 `adm-ia6.js` 가 다시 그린 **7그룹 44항목**입니다. 같은 class(`.ph85-group`·`.ph85-sub`)를 쓰기 때문에 코드만 봐서는 구분이 안 되고, 옛 쪽만 고치면 **하니스도 통과하고 검증도 통과하는데 사장님 화면만 안 바뀝니다**(2026-08-18 실제로 밟음 — 「결제 손자가 안 뜬다」). ⚠️ IA6 항목은 **카드를 여러 장 묶습니다**(44개 중 23개). `data-card` 는 대표 첫 장뿐이고 전부는 `data-cards` 에 있습니다. 그리고 `openSub` 가 있으면 그 항목은 **카드 «안의 한 칸»을 가리키는 잎**이라 손자를 만들면 안 됩니다 — 안 보면 「대표지사·지사·대리점」이 전부 같은 4줄을 보여 줍니다 |
| 사이드바 클릭이 안 먹거나 엉뚱하게 동작 | **`#ph85-sidebar` 클릭을 가로채는 window 캡처 핸들러가 둘입니다** — `adm-s11.js`(ph97)와 `adm-ia6.js`(wireDelegate). 사이드바에 리스너를 달면 ph97 의 `stopPropagation` 때문에 **영원히 발화하지 않습니다.** 새 조작(토글 등)을 넣을 때는 **둘 다에 예외**를 넣어야 합니다. ph97 은 2026-08-06 에, IA6 는 2026-08-18 에 각각 「▸ 손자 토글이 삼켜져 열 방법이 아예 없던」 사고를 냈습니다 |
| CSS 를 JS 로 덮었는데 안 먹음 | **작성자 CSS 가 `!important` 면 인라인 `el.style.xxx =` 도 집니다.** `el.style.setProperty('max-height', v, 'important')` 를 써야 이깁니다. 손자 메뉴 높이(`.ph125-grandchildren` 600px·`.ph85-subs` 900px)를 넓히려다 밟았습니다(2026-08-18). ⚠️ 상한 숫자를 키워서 푸는 건 임시방편입니다 — 상한이 클수록 닫을 때 «반응 없는 시간» 이 길어지고 언젠가 또 넘칩니다. **열 때만 실제 내용 높이를 넣으세요** |
| `file://` 로 `admin.html` 을 열어 확인했더니 아무것도 안 뜸 | **외부 스크립트가 하나도 실행되지 않습니다.** `<script src="/js/…">` 의 절대경로가 `file://` 에서는 **파일시스템 루트**(`/js/…`)를 가리켜 전부 404 입니다. 에러가 화면에 안 뜨니 「기능이 죽었다」로 오진하게 됩니다(2026-08-18 실제로 밟음 — 손자 메뉴가 0개로 보였음). 확인할 때는 `cd cloudflare-deploy/public && python3 -m http.server 8899` 로 띄우고 `http://127.0.0.1:8899/admin.html` 을 여세요 |
| hover 때 글자가 움직임 | `transform` 만 찾으면 못 찾습니다. 사이드바 `.ph85-sub` 는 **별점과 말풍선이 같은 `::after` 를 나눠 써서**, hover 시 별점이 `absolute` 로 흐름에서 빠지고 그 순간 `justify-content:space-between` 이 라벨을 가운데로 밀었습니다. **flex 컨테이너에서 `space-between` 금지, `flex-start` + `margin-left:auto` 로 오른쪽 정렬**할 것 |
| 로그인 역할 판정 로직 | `public/index.html`(`tryAdminLoginFallback`, 학생홈 통합 로그인)·`public/admin/login.html`(ph239)·`public/js/idx-user-session.js`(상단바 표시·이동 분기) **세 곳에 역할판정이 복제**돼 있습니다. 역할 분기(교사/본사/지사/대리점/캐피타운)를 고치면 반드시 셋 다 고쳐야 합니다. 접두사 규칙 «자체» 의 정본은 `src/auth-admin.ts` 의 `resolveUiIdentity()` 입니다 |
| 관리자 카드 안 박스 색이 안 먹음 | 인라인 `style="background:linear-gradient(135deg,…"` 나 `background:#f…` 는 **`admin-inline-c.css` 의 옛 다크 규칙**(`details.menu-card [style*="background:linear-gradient(135deg"]` 등)이 `!important` 남색으로 덮고, 그걸 본 `js/adm-s13.js` 페인터가 다시 흰-회청색으로 칠합니다. **`background-color:` 로 쓰면** 어느 선택자에도 안 걸려 고른 색이 그대로 나옵니다. 글자색은 `.sub-body :is(p,span,div,b,strong…)` 가 통째로 `#101828 !important` — `<a>` 만 예외 |
| 표 안의 **작은 아이콘 버튼**이 큰 파란 알약이 되어 옆 칸을 덮음 | **`admin-inline-c.css` 의 전역 룰이 인라인 style 을 이깁니다** — `details.menu-card button{ background:인디고 그라데이션 !important; padding:9px 18px !important }` 가 «작성자 !important > 인라인» 순서로 JS 가 적어 보낸 `width:24px; padding:0` 을 통째로 무시합니다. 위 「카드 안 박스 색」 함정의 **버튼 판**인데, 이쪽은 색만이 아니라 **크기까지** 바뀌어 레이아웃을 밀어냅니다. 2026-08-20 실측(수강신청 등록 표): ⏰ 요일별시간 26×26 → **49×31** 이라 시간 입력칸이 `calc(100% - 28px)` 로 비워 둔 자리를 넘어 **입력칸 위로 올라탔고**, ✕ 행삭제(투명·빨강)는 **67×46 파란 알약**이 됐습니다. ⚠️ **넓은 창에서는 재현이 안 됩니다** — 표가 `overflow:auto` 안이라 칸이 늘어나면 나란히 놓입니다. 좁은 칸(사장님 화면)에서만 겹치므로 헤드리스 측정만 믿고 «문제 없음» 이라 하면 안 됩니다. ✅ 고침은 **ID 접두로 그 버튼만 되살리기**(`#en-multi-table td button.en-row-time-builder`) — 이 파일에 같은 사정의 선례가 이미 있습니다(`#tp-list-table td button.tp-act-btn`, 주석이 그 전역 룰을 지목합니다). `box-sizing` 을 함께 명시하세요(width 가 테두리를 포함해야 입력칸의 `calc(100% - 28px)` 와 맞습니다). ⛔ 전역 룰 자체를 고쳐서 풀지 마세요 — 관리자 화면 버튼 전체의 모양이 걸립니다. ⛔ hover 에 `transform` 을 되살리지 마세요(1-3 「hover 확대 금지」) |
| 앱(APK) 업데이트가 안 나감 | `mobile-app/**` 을 main 에 올리면 android.yml 이 APK 를 빌드해 `downloads/mangoi.apk`+`app-version.json` 을 main 에 커밋하는데, **그 커밋은 사이트 배포를 트리거하지 못합니다** — 워크플로가 기본 `GITHUB_TOKEN` 으로 푸시한 커밋은 GitHub 규칙상 다른 워크플로를 안 깨웁니다(skip ci 문구를 빼도 마찬가지 — 2026-08-14 실측). 새 APK 를 사이트로 내보내려면 **그 뒤에 사람 쪽 main 푸시(아무 PR 머지)가 한 번 더 필요**합니다. 그 «한 번 더 푸시» 의 PR 제목·커밋 메시지에 skip ci 문구를 그대로 쓰면 그것도 건너뛰어집니다(같은 날 실제로 밟음) |
| 「열라고 해 놓고 눌러도 **그림이 그대로**」 — 게임·인터랙션에서 단계가 올라가도 화면이 안 바뀜 | 🔴 **장소·화면마다 사진을 «1장» 만 두면, 무엇을 해도 그림이 그대로라 「눌러도 아무 일도 안 일어난다」로 읽힙니다.** 2026-09-21 사장님: 「캐비닛을 열라고 했을 때 그림을 클릭하면 캐비닛이 열린 그림으로 바뀌는 줄 알았는데 그대로에요.」 실측 원인이 **셋**이었습니다 — ⓐ `student-game-escape-voice.html` 의 새 장소 5곳은 **사진이 1장뿐**이라 `setScene(G.loc.id)` 가 일곱 단계 내내 같은 그림을 걸었고(그 자리 주석이 「우선 비용을 낮췄다. 반응 보고 단계별 클로즈업을 추가할 수 있음」이라 **스스로 미완성이라고 적어 두고 있었습니다**) ⓑ `escape-school` 은 단계 대부분이 `scene:'room'` 이라 **성공할 때마다 교실 전체로 «되돌아갔고»** ⓒ `escape-zombie` 는 캐비닛에 「열린 판」이 없어 연 결과가 안 보였습니다. ⚠️ **에러가 안 납니다** — 게임은 정상 진행되고 틀린 것은 «그림이 말을 안 한다» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 고침은 **단계마다 `{before, after}` 를 표로 두는 것**(정본 `STEP_ART` + `locArt()`) — before 는 «아직 안 한 상태», after 는 «성공한 결과». ⛔ **before 에 결과 그림을 쓰지 마세요** — 열기도 전에 열린 모습이 보여 순서가 뒤바뀝니다(`study` 의 drawer 가 그 상태였습니다: 「서랍을 여세요」 단계에서 이미 열린 서랍을 보여주고, 성공하면 방 전체로 되돌아갔습니다). ⛔ **그림이 빠졌을 때 검은 화면이 되게 두지 마세요** — `setScene` 이 `SCENES[k]` 가 없으면 **지금 그림을 그대로 두고 돌아가게**(빈 화면보다 낫습니다). ✅ **클릭에도 즉각 반응을 주세요** — 여기서는 핫스팟을 누르면 **«누른 자리»** 로 `background-position`·`background-size` 가 확대됩니다(`zoomToHotspot`). ⚠️ **«그 물건 쪽» 이 아닙니다** — `HOT_POS` 는 버튼을 놓는 **고정 세 자리**이고 `renderHotspots` 가 라운드마다 이름표를 `shuffle` 해 끼우므로, 확대되는 곳은 «사진 속 그 물건» 과 무관하고 **빈 벽이 확대될 수도** 있습니다. ⛔ 그것을 「그 물건을 확대한다」로 적지 마세요(2026-09-21 에 네 곳에 그렇게 적었다가 함정 대조가 잡았습니다) — 진짜로 그 물건을 확대하려면 **장소·단계마다 좌표**가 필요하고 사진 24장에 손으로 매기는 별건입니다. ⛔ 그때 장면을 «바꾸지» 마세요 — 실제로 열리는 것은 영어로 말한 다음이고, 클릭만으로 열면 말하기 연습이라는 목적이 사라집니다(검사도 «확대된다» 옆에 **«클릭만으로는 아직 안 열린다» 를 짝으로** 둡니다). 🪤 **브라우저로 잴 때 `getComputedStyle().backgroundImage` 를 쓰면 멀쩡한 화면이 «안 바뀐다» 로 나옵니다** — `#scene` 에 `transition:background-image .35s` 가 걸려 있어 **전환 중에는 «옛 값» 을 돌려주기 때문**입니다(실측: 인라인은 `classroom.jpg` 인데 계산값은 `room1.jpg`). 「무엇을 걸었는가」는 **인라인 `el.style.backgroundImage`** 로 읽고, 「실제로 칠해졌는가」는 **transition 이 끝나기를 기다린 뒤** 따로 재세요(둘을 짝으로). ⚠️ **새 그림은 «표에 등록» 만 하고 파일을 커밋에 안 담으면 조용히 폴백합니다** — 2026-08-31 Lily 얼굴이 그래서 며칠 동안 옛 얼굴로 돌았습니다. 감시는 「표가 가리키는 그림이 `existsSync` 로 실재하는가」로 두세요. ⚠️ 그림을 외부에서 받아야 하면 **프록시가 CDN 을 403 으로 막으므로** CI 러너를 거칩니다(2장 「화면에 쓸 이미지·파일을 밖에서 받아와야 할 때」) — 그때 **미리보기 시트를 함께 커밋해 눈으로 보세요**(「투명 픽셀 80%」 같은 숫자로는 멀쩡해 보입니다). 🔴 **그리고 «표가 옳은가» 만 묻는 검사로는 이 사고를 원리상 못 잡습니다** — 하니스가 `artFor` 를 **직접 불러** 답을 맞추면 «그 표를 실제로 쓰는가» 는 아무도 안 봅니다. 2026-09-21 함정 대조 실측: 그 상태에서 **사장님이 신고한 그 버그를 그대로 되돌려도**(`advance` 가 `setScene(G.loc.id)` 로) 자동 19종·브라우저 34종이 **전부 초록**이었습니다. ✅ `showStep`·`advance` 를 **중괄호 짝으로 오려 내 가짜 `setScene`·`artFor` 로 실제로 돌려** «화면에 걸리는 키» 를 답으로 물으세요(표식 `'ART:…'` 가 안 나오면 그 함수를 안 쓴다는 뜻). ⚠️ 그때 **부를 함수와 «같은 이름» 을 no-op 스텁으로 두지 마세요** — `var showStep=__noop` 대입이 함수 선언을 덮어써 **아무것도 안 돌고 «장면 0건» 으로 조용히 통과**합니다(실제로 밟았고 «—» 출력 덕에 잡혔습니다). 감시는 `test-harness/escape_step_art_harness.mjs`(자동 42종 — 표를 오려 내 실제로 돌려 단계마다 다른 그림인지·파일이 실재하는지·순서가 안 뒤집혔는지 + **④절이 배선을 실제로 실행**. 변이 **10종** 전부 실제 FAIL: advance→`G.loc.id` · school advance→`s.scene` · showStep 두 곳 · 문 한 박자 제거 · 캐비닛 분기 제거 · 영상 배선 4종)와 `test-harness/manual/escape-step-art-browser.mjs`(브라우저 — **자동으로 안 돕니다, 사람이 부릅니다**) 📌 **덧붙임(2026-09-21) — 그림을 고치고 나면 «반응 영상» 이 같은 병을 앓고 있습니다.** `ACTION_CLIPS` 를 고르는 줄이 **단계 이름만** 보고 있어(`s.id==='drawer'?'drawer':…` — `G.loc.id` 가 한 글자도 안 들어 있었습니다), **옥상에서 공구함을 열어도 «서재 책상 서랍» 영상이 났습니다** — 그림은 그 방 것인데 영상만 낯선 방이 되돌아옵니다. ⚠️ **에러가 안 나고** 그림 검사(④절)는 전부 초록입니다 — 그 절이 `playActionClip` 을 **no-op 스텁으로** 두었기 때문입니다. ✅ 고침은 **«장소:단계» 열쇠**(`clipKeyFor()`)이고, 없는 짝은 공용 영상으로 떨어집니다(서재·`key`·`books` 단계). ⛔ 폴백을 없애지 마세요 — 그 순간 그 셋은 «영상 없음» 이 됩니다. ✅ 검사는 `playActionClip` 을 **기록기로 바꿔 «무엇을 틀었는가» 를 답으로** 물으세요(⑤절 — ⛔ 표를 하니스에 베껴 적지 말고 **소스에서 오려 내세요**). ⚠️ 「그 방 것을 튼다」 옆에 **「없는 짝은 공용으로 떨어진다」·「둘러보기 단계엔 안 튼다」를 짝으로** 두세요 — 앞만 보면 «언제나 장소:단계» 로 만드는 엉터리 수리도 통과합니다(변이 실측). 🪤 **그 검사가 보는 것은 «어느 열쇠를 넘겼는가» 까지입니다** — `'rooftop:drawer'` 에 교실 영상 주소를 넣어도 초록입니다(그래서 «같은 주소를 두 번 쓰지 않는가» 를 한 줄로 막아 두었습니다). **영상 «내용» 이 그 방인지는 사람이 봐야 합니다.** ⚠️ 그 호출은 `try` 로 감싸세요 — 거기서 던지면 위에서 이미 `G.busy=true`·`clearHotspots()` 가 돌아 **게임이 그대로 잠깁니다.** ⚠️ 그 20편은 **힉스필드 CDN 주소를 그대로** 씁니다(기존 6편과 같은 방식 — 외부 주소가 6 → 26개) — 2장 「받은 주소를 코드에 그대로 쓰지 마세요」에 여전히 걸려 있습니다. ⛔ **「주소가 죽어도 조용하다」로 읽지 마세요** — `#sceneVideo` 는 `inset:0` 에 거의 검정(`#07090c`)이고 `playActionClip` 이 **먼저 `show` 를 붙인 뒤** 에러나 **4.8초 타이머**에서야 뗍니다 ⟹ hang 하는 회선에서는 **최대 4.8초 동안 거의 검은 상자가 정지화면을 덮습니다**(정지화면과 게임 진행 자체는 클립과 무관해 계속 돕니다). ⚠️ 그 주소에는 서명·만료 쿼리가 **없습니다** — 위험은 「만료」가 아니라 «제공자가 파일을 지우면» 입니다. ⚠️ 그리고 **회선 부담이 늘었습니다** — `painting` 단계는 전에 영상이 없었고, 전에는 6편을 모든 장소가 나눠 써 캐시가 재사용됐는데 이제 장소 전용이라 장소를 바꿔 가며 놀면 재사용이 사라집니다(`preload="none"` 이라 «틀 때만» 받습니다. **용량은 못 쟀습니다** — 프록시 403). 우리 CDN 으로 옮기는 것은 CI 러너를 거쳐야 하고 리포가 수십 MB 늘어 **사람이 정할 일**로 남겨 두었습니다. |
| 막대·게이지의 **채움이 안 보임** — 색도 폭도 «코드에는 있는데» 화면이 텅 빔 | 🔴 **채움이 `<span>` 이면 브라우저가 그것을 «글자» 로 봅니다 — 인라인 요소는 `width`·`height` 를 «무시» 합니다.** 부모가 `display:flex` 면 자식이 블록으로 승격되지만, 부모가 그냥 블록 상자면 자식 span 은 인라인 그대로입니다. ⚠️ **에러가 안 납니다** — 규칙도 색도 전부 «있고» 폭·높이만 0 입니다. 2026-09-09 실측(레벨테스트 결과 CEFR 막대 `.bkfill`): 지시한 폭이 **100%·75%·0% 인데 실제로 그려진 폭·높이가 전부 0.0px** → **4/4 와 0/4 가 화면에서 똑같이 빈 막대**였습니다. 사장님께는 「색이 없다」로 보였지만 색은 처음부터 있었습니다. 같은 파일의 진행 막대(`.progfill`)는 `<div>` 라 멀쩡했고 **이 한 줄만 달랐습니다.** ✅ 채움에 **`display:block`(또는 부모에 `display:flex`)을 명시**하세요. ⛔ `display` 를 아예 안 적으면 기본값이 인라인이라 그대로 재현됩니다. ✅ 확인은 «규칙이 있는가» 가 아니라 브라우저에서 **`getBoundingClientRect().width` 를 재는 것** 뿐입니다 — 문자열 하니스는 이 사고를 원리상 못 봅니다(수리 전에도 `--fast` 가 전부 초록이었습니다). ✅ 감시를 만들 때는 「`display:block` 이라고 적혀 있는가」가 아니라 **「인라인이 아닌가」** 로 물으세요 — `flex`·`grid` 로 바꾸는 정당한 수리를 빨간불로 만들면 안 됩니다. ⚠️ 그리고 **0점을 «폭 0» 으로 그리지 마세요** — 빈 막대는 «측정 안 됨» 으로 읽혀 이번 사고와 똑같은 오해를 다시 만듭니다(작은 표시를 남기고 숫자를 옆에 둡니다). 감시는 `test-harness/leveltest_cefr_bar_harness.mjs`(등급 함수를 오려 내 실제로 돌림)와 `test-harness/manual/hero-cta-and-cefr-bar-browser.mjs`(브라우저 — 되돌리면 실제로 6건 FAIL) |
| `el.hidden = true` 인데 그대로 보임 | **작성자 CSS 가 `display` 를 정하면 브라우저 기본 `[hidden]{display:none}` 을 이깁니다**(특정성이 아니라 «작성자 > UA» 우선순위 문제라, 선택자를 아무리 봐도 원인이 안 보입니다). `teacher.html` 의 `.btn{display:inline-block}` 이 정확히 이랬고, 숨긴 버튼이 계속 떠 있었습니다(2026-08-15 실측). 화면이 `hidden` 으로 보이기/숨기기를 한다면 `[hidden]{display:none !important}` 를 한 줄 박아 두세요 |
| `display:none !important` 로 감췄다고 «주석까지 적혀 있는데» 그 요소가 자리를 차지하고 있음 | 🔴 **id 하나짜리 선택자(1,0,0)는 「id+클래스」(1,1,0)를 못 이깁니다.** `#topUserBtn{display:none!important}` 가 이 파일 5389행에 있는데도, 옛 헤더 규칙 `#topUserBtn.th-user{display:inline-flex!important}`(1944·4922행)가 이겨서 **옛 상단바 사용자 버튼이 계속 살아 있었습니다.** 게다가 그리드 자식이라 `inline-flex` 가 `flex` 로 승격돼 **사이드바 도크의 한 줄을 통째로** 먹었고, 배경이 투명해서 화면에는 «이유 없이 비어 있는 자리» 로만 보였습니다(2026-08-21 사장님 「사이트 홈과 관리자 칸 사이가 너무 비어 있다」 → 실측: PC 28px·휴대폰 22px + 간격 7px). ⚠️ 주석을 믿지 마세요 — 「숨겼다」고 적힌 것과 **실제로 숨겨진 것**은 다릅니다. ✅ 확인은 `getComputedStyle(el).display` 를 재고, 이기는 규칙은 `document.styleSheets` 를 훑어 `el.matches(r.selectorText)` 로 찾으세요(선택자를 눈으로 좇으면 못 찾습니다). ✅ 고침은 **조상 id 를 앞에 붙여**(`#ph162-dock #topUserBtn` = 2,0,0) 이기는 것. ⛔ 요소를 지워서 풀지 마세요 — 그 버튼은 `initTopUserMenu()` 가 `/api/admin/me` 를 부르는 이 화면의 유일한 신원 조회 자리입니다 |
| 관리자 카드를 `style.display='none'` 로 숨겼는데 PC 에서 그대로 보임 | **작성자 `!important` 는 인라인 스타일도 이깁니다.** 위 「`el.hidden`」 함정의 형제인데, 이건 인라인까지 집니다. `admin-inline-c.css` 의 `@media (min-width:1024px){ #legacy-cards details{display:block!important} }` 가 `#legacy-cards` 안 **모든** `details`·`.menu-card`·`.table-card` 를 되살립니다. 그래서 역할별 메뉴 숨김(`_applyMenuVisibility`)이 **PC 에서 하나도 안 먹고 있었습니다**(2026-08-18 실측: 카드 85개 중 71개 무력화, 경영진 전용 「권한 관리」·「이탈 관리」가 본사 매니저에게 노출). ⚠️ **휴대폰(<1024px)에서는 정상 동작**해서 모바일로 확인하면 멀쩡해 보입니다 — 이래서 발견이 늦었습니다. ⛔ 그 복구 규칙을 지워서 풀지 마세요 — ph87 이 `#legacy-cards` 를 숨겨 「메뉴를 눌러도 카드가 안 보인다」 사고가 났던 것의 복구책입니다. ✅ 숨김은 **`.rbac-hide` 클래스**로 하세요(미디어쿼리 밖에 정의돼 있어 모바일도 함께 먹습니다). ⚠️ 이 클래스는 **붙이는 쪽과 읽는 쪽이 짝**입니다 — 붙임 `_applyMenuVisibility`·`_applyPayrollTeacherUI`, 읽음 `buildMenuIndex`·메뉴검색색인·`adm-quickmenu visible()`·`adm-quick-access roleHidden()`·`adm-p1 memberVisible()`·`adm-ia6 applyRoleFilter()`. 한쪽만 고치면 「사이드바엔 없는데 본문엔 있는」 불일치가 납니다. ⚠️ PC 사이드바(`adm-ia6.js`)는 **정적 GROUPS 목록**이라 역할을 스스로 모릅니다 — `adm-core` 가 쏘는 `mangoi:menu-visibility` 이벤트를 받아 「가리키는 카드가 전부 감춰진」 항목을 감춥니다(`.ia6-role-hide`). ⛔ 그 클래스에 `.ia6-hide` 를 재사용하지 마세요 — 그건 카드용 `showOnly` 전용이라 **항목 클릭 한 번에 숨김이 풀립니다** |
| 강사 이름을 붙였는데 **남의 이름**이 뜸 | **강사 번호가 «세 벌» 입니다.** 카페24 번호(`attendance.teacher_uid`·`teacher_payroll_auto.teacher_id`, 실측 9~196) · 원부 번호(`teachers.id`·`class_schedules.teacher_id`, 1~29) · 프로필 번호(`teacher_profiles.id`, 4~37). **겹치는 구간에서 서로 다른 사람**입니다 — 카페24 24 = Teacher Mariane 인데 원부 24 = HANNAH, 카페24 26 = Teacher Rica 인데 원부 26 = MELCA. 번호로 이으면 조용히 남의 이름이 붙고 **에러가 안 납니다.** ⚠️ 그래서 **`attendance.teacher_name` 을 믿으면 안 됩니다** — 2026-08-18 동기화가 이 실수로 602행에 남의 이름을 넣었습니다(79aa8ed 로 수리했지만 이미 들어간 행은 그대로. -60일~+180일 창 밖 28행은 야간 동기화로도 안 고쳐집니다). ✅ 이름은 **`teacher_payroll_auto`** 에서만 찾으세요 — 카페24가 «번호와 이름을 함께» 밀어넣은 유일한 표입니다. 번호를 이을 때는 `normTeacherName()`(소문자·「teacher 」 접두사 제거)으로 **유일하게 맞을 때만**, 안 맞으면 **비워 둡니다**. 정본은 `api-admin.ts` 의 `loadCafe24TeacherMap()`. 감시는 `test-harness/schedule_seed_harness.mjs` ⑨(I절) |
| 새 `IN (...)` 목록 | 손으로 90개씩 자르지 마세요. `test-harness/d1_bind_limit_harness.mjs` 의 회귀 감시가 `+= 90` 과 새 `map(() => '?')` 를 **잡아서 FAIL 냅니다.** 공용 `selectInChunks`(`src/d1-chunk.ts`)를 쓰거나, 상한 근거를 하니스의 `ALLOW` 에 적어야 합니다 |
| 학부모에게 문자가 두 번 감 | **학부모 발송 경로가 두 갈래입니다** — 강사 수업일지 `/api/eval/create`(→`student_evaluations`)와 AI 초안 승인 `/api/admin/feedback-drafts/approve`(→`teacher_feedbacks`). 둘 다 문자를 보냅니다. 한쪽에서 «이미 썼는지» 판정할 때 **자기 표만 보면 안 됩니다**(초안 생성이 `feedback_drafts` 만 보다가 이미 일지를 쓴 수업까지 다시 초안을 만들던 것이 2026-08-15 수리 건) |
| 대리점의 지사 소속을 D1에서 고쳤는데 다음날 원복됨 | `centers.franchise_id` 는 **카페24가 정본**이라 `importCafe24Org` 의 UPSERT 가 매일 밤 03:45 KST 에 덮어씁니다(`franchise_id = excluded.franchise_id`). `payment_type` 은 UPSERT 목록에서 빼서 지켰지만(2026-08-14) `franchise_id` 는 뺄 수 없습니다 — 새 대리점·지사 이동을 따라가야 하니까요. 임시 정정이 필요하면 **`center_franchise_override`** 에 적으세요(동기화 직후 다시 입혀집니다). 정본 수정은 카페24에서 해야 하고, 고친 뒤엔 override 행을 지우는 것이 맞습니다 |
| 학생 이름·계정을 D1 에서 고치거나 지웠는데 **다음날 원복됨** | 🔴 **위 항목의 형제인데 이쪽이 훨씬 사납습니다 — UPSERT 가 아니라 «통째로 지우고 다시 넣기» 입니다.** `importCafe24Students` 는 매일 밤 03:00 KST 에 `DELETE FROM students_erp WHERE created_at = CAFE24_STUDENT_SENTINEL`(=`1751500000000`) 로 **그 센티넬을 단 행을 전부 지우고** `INSERT OR REPLACE` 로 카페24 `:Student` 를 다시 채웁니다. 카페24에서 온 학생은 **전원** 그 센티넬을 답니다. 그래서 이름 수정도, 행 삭제도, 새 컬럼에 적어 둔 값도 하룻밤이면 사라집니다(`INSERT OR REPLACE` 는 컬럼 목록에 없는 칸을 NULL 로 만들었습니다 — **2026-08-28 에 UPSERT 로 바꿨습니다**(아래 항목) — 전화번호가 그래서 전멸했던 것이 2026-08-18 건). ✅ 확인법: `SELECT created_at FROM students_erp WHERE user_id=?` 가 `1751500000000` 이면 **카페24가 정본**입니다. ✅ 이름 고정·명부 숨김은 **`student_erp_override`** 에 적으세요(정본 `src/student-override.ts`) — 이름은 동기화 «직후» 다시 입히고(`applyStudentErpOverrides`), 숨김은 `students_erp` 를 안 건드리고 **읽는 쪽에서** 거릅니다. ⛔ 숨김을 DELETE 로 구현하지 마세요 — 밤에 되살아나고 그 사이 붙어 있던 출석·포인트만 주인을 잃습니다. ⚠️ 읽는 쪽은 **네 곳이 짝**입니다(`students/unified`·`students/erp-list`·`omnisearch`·`student/login`). 명부에서만 감추면 옛 계정으로 로그인해 「내 수업이 안 보인다」가 그대로 재현됩니다. ⚠️ 조건절은 표가 없을 때 **빈 문자열**이어야 합니다 — `erp-list` 는 어떤 에러든 삼켜 빈 배열을 주므로, `no such table` 이 나면 **학생 29,000명 명부가 통째로 사라진 것처럼** 보입니다. 📜 2026-08-20 실측: 정우영 한 사람의 계정이 카페24에 **15개**, 그중 실제로 쓰는 `jeong` 은 카페24에 이름이 없어 명부에 「jeong」 으로 찍혀 **「정우영」 검색에 안 걸렸습니다**(화면 14명 = 목록 밖에 15번째가 있었음). 감시는 `phone_sync_harness` D절 |
| 가맹점 정산에서 특정 지사 매출이 통째로 안 잡힘 | `centers.name` 이 **유일하지 않습니다.** 같은 이름이 두 지사 이상으로 갈리면 `franchiseReport` 가 «어느 쪽인지 모름» 으로 판정해 **아무 데도 배정하지 않습니다**(아무 쪽에 몰아주면 그게 또 균등분배라서). 2026-08-16 기준 5개 이름·학생 2,603명이 이랬습니다. 확인: `SELECT name FROM centers GROUP BY name HAVING COUNT(DISTINCT franchise_id)>1` |
| 매출 대사가 «장부에 매출이 없다» 고 경고함 | 신한 계좌 입금 적요에 **「케이씨피」와 「케이씨피M」 두 가지**가 있습니다. 「케이씨피」(타행PC·기업)만 진짜 PG 정산금이고, **「케이씨피M」(타행IB·하나)은 회사의 하나은행 계좌에서 옮겨 온 «운영자금»** 입니다(2026-08-17 사장님 확인 — 매출 아님). `LIKE '%케이씨피%'` 로 찾으면 둘 다 잡혀 누적 4,632만원이 «장부에 없는 매출» 로 오진됩니다(2026-08-16 실측). 입금 판정은 반드시 `classifyDeposit()`(`src/accounting-reports.ts`)을 쓰세요 |
| 「카페24 회계 실데이터」 화면 매출이 통장·리포트보다 큼 | **「케이씨피M」 함정이 여기에도 있습니다.** 이 화면은 D1 이 아니라 **Neo4j `AccBook`** 을 봅니다. `type`(1=수입/2=지출)만 보고 더하면 하나은행에서 옮겨 온 운영자금(적요·거래처 「케이씨피M」)까지 매출이 됩니다(2026-08-18 수리). 판정 정본은 `KCP_TRANSFER_CYPHER_RE`·`isKcpTransferRow()`(`src/accounting-reports.ts`) — **통장용 `classifyDeposit()` 과 같은 파일에 나란히 둔 이유가 이것**입니다. Neo4j 는 TS 정규식을 못 쓰므로 Cypher(Java)용 문자열을 따로 내보내니 **한쪽만 고치지 마세요**(`c24_finance_kcpm_harness.mjs` 가 둘이 어긋나면 FAIL 냅니다). ⚠️ **단, 2026-08-18 현재 `AccBook` 은 0건입니다** — 같은 Neo4j 의 `Book` 62건은 조회되므로 연결 문제가 아니라 **카페24 쪽이 회계 노드를 아직 적재하지 않은 것**입니다(이 저장소엔 적재 코드가 없고 읽기만 합니다). 화면이 비어 보이면 코드보다 먼저 `/api/admin/finance-cafe24/ledger?limit=20` 의 `count` 를 확인하세요. 적재 요청서: `docs/카페24_회계데이터_적재요청_2026-08-18.md` |
| 「기타출금」 한 덩어리로 뭉쳐 무슨 돈인지 모름 | 통장 출금의 상당액이 적요만으로 분류가 안 돼 「기타출금」이 됩니다. 그 **대부분은 지사 수수료**입니다(2026-08-17 확인). 판정은 ① `expense_payee_category` 지정표 ② `franchises.owner_name`(241곳 전부 채워져 있음) 일치 → 「지사수수료」 순입니다. 적요는 길이가 잘려 「김영진(지성교」 처럼 오므로 `(` 앞까지로 비교합니다. ⛔ 법인 형태((주)…·센터·보험)는 자동 분류 금지 — (주)새하컴즈·호스트센터 같은 **진짜 다른 비용**이 섞여 있습니다 |
| 손익계산서에서 「직원 급여」와 「강사 급여」가 겹쳐 보임 | 실제로는 두 줄의 **합계 자체는 이중계산이 아닙니다**(`bankacct-sync.ts` `BANK_CAT_RULES` 가 계좌 적요마다 카테고리 하나만 매기고, 판관비 합계와도 정확히 일치). 진짜로 확인해야 할 것은 그 **직원 이름 목록이 맞는지**입니다 — 「이병엽·남궁국화·장지웅 = 직원」으로 하드코딩돼 있었는데, 남궁국화는 실제로는 **지사수수료**였습니다(2026-08-19 사장님 정정 → 이름 목록에서 빼고 `accounting-reports.ts` 「기타출금 쪼개기」 쪽에 지사수수료로 하드코딩). 이 이름 목록은 사람이 한 번 정한 뒤 방치되기 쉬우니, 비슷한 제보가 오면 먼저 `BANK_CAT_RULES` 의 이름 목록부터 대조하세요. ⚠️ 별개로 아직 고치지 않은 위험: 진짜 강사 급여명세(payslips)가 없는 달엔 「강사 급여」 줄이 메트로은행 송금분만 잡는데, 「급여이체」로 분류된 국내 급여성 출금은 "강사급여와 중복이라 제외"라는 전제로 판관비에서도 빠집니다 — 겹침이 아니라 **어디에도 안 잡히는 누락**일 수 있습니다 |
| 「로그인했는데 또 로그인하래요」 | **로그인 세션이 두 갈래입니다.** 학생·학부모 = `mangoi_logged_user` + `mango_token`, 교사·본사·지사 = `mangoi_admin_session` + `admin_sessions` **쿠키**(토큰 없음). 학생 키나 `mango_token` 만 보는 화면·API 는 **교사를 미로그인으로 판정**합니다(2026-07-30 Kaye 17번, 2026-08-13 Karl 「Double Login」 둘 다 이 뿌리). ⛔ 교사에게 `mangoi_logged_user` 를 만들어 주는 방식으로 풀지 마세요 — 학생 전용 기능이 통째로 열립니다. 서버는 토큰이 없을 때 `checkAdminSession()` 도 보게 하고(개인정보 API 는 `resolveOwnerScope()`, 수강신청·자동결제는 `enroll-ops.ts` 의 `authUidOrAdminSession()` — 관리자 세션을 «자기 username» 학생으로만 인정. 2026-08-23 사장님 jeong 이 홈 통합 로그인 → 관리자 폴백으로 들어와 수강신청이 잠겼던 건), **짝이 되는 API 끼리 판정이 어긋나지 않았는지** 확인하세요(Karl 건 = 녹화 «재생» 은 관리자 세션을 받는데 «목록» 만 안 받던 한쪽짜리 게이트) |
| 로그인한 학생인데 화면이 **«손님(게스트)»** 으로 봄 — 에러도 경고도 없음 | 🔴 **바로 위 항목의 형제인데, 이쪽은 «두 갈래» 가 아니라 «같은 키 안의 칸 이름» 이 어긋난 것입니다.** 로그인 정본(`js/idx-user-session.js`)이 `localStorage` 에 적는 모양은 자리마다 다릅니다 — `idx-user-session.js:73` 과 `index.html:13592`(`norm`)는 **`{uid, name, role}`** 이라 **`user_id` 칸이 아예 없고**, `idx-user-session.js:836`(비번 로그인)만 `user_id` 를 함께 넣습니다. 그래서 화면이 **`mango_user.user_id` 하나만** 보면 앞의 두 경로로 들어온 학생이 **전부 게스트로 떨어집니다.** ⚠️ **에러가 안 나고 화면도 정상으로 보입니다** — 기록이 `guest_xxxxxx` 계정에 쌓일 뿐이라 오래 안 보입니다. ⚠️ 그리고 **«되다 안 되다» 로 보입니다**(경로에 따라 갈립니다) — 재현이 헷갈리는 이유가 이것입니다. 📜 2026-09-09 실측: 「AI 단어 퀴즈는 완료해도 완료 표시가 안 뜬다」 제보 하나를 좇으니 **같은 뿌리가 화면 8곳**(단어퀴즈·단어장·연속학습 화면/공용위젯·AI 영어친구·발음 코칭 한국어/중국어·AI 영작첨삭)에 퍼져 있었습니다. ✅ **새 모양을 먼저 보고 옛 모양(`user_id`)도 그대로** 받으세요(`uid || user_id || id`). ⛔ **정본이 적는 모양을 «하나로 통일» 해서 풀지 마세요** — 그 키를 읽는 곳이 저장소 전체에 흩어져 있어 반경이 큽니다. 읽는 쪽을 넓히는 것이 안전한 방향입니다. ⛔ **게스트 id 를 «호출마다 새 난수» 로 두지 마세요** — 비로그인 기록이 **원리상** 안 이어집니다(`mangoi_guest_uid` 에 고정. 2026-09-09 에 연속학습·AI 영작첨삭이 그 상태였습니다). ⛔ **`mango_user_id` 를 계정 uid 로 쓰지 마세요** — 그건 `js/mango.js` 가 주는 화상수업용 **기기별 임의 id**(`u_xxxxxx`)라 계정과 무관합니다. 발음 코칭이 포인트 적립에 그 키를 써서 «남의 계정» 도 아닌 **«아무 계정도 아닌» id** 로 적립을 시도해 조용히 실패해 왔습니다. ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «어느 칸을 읽는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 검사는 신원 함수를 **오려 내 가짜 `localStorage` 로 실제로 돌리세요** — 그리고 **「새 모양이면 계정으로 붙는다」와 「비로그인은 계정을 지어내지 않는다」를 짝으로** 두세요(짝이 없으면 «항상 계정 반환» 같은 엉터리 수리도 통과합니다 — 변이시험에서 실측). ⚠️ 익명 접두사는 화면마다 다릅니다(`guest_`·`anon_`) — 하나로 못 박으면 멀쩡한 화면이 빨간불입니다. ⚠️ 그리고 그 함수가 기대는 상수(`LS_ANON` 등)의 **값을 검사에 베껴 적지 말고 선언 줄을 소스에서 읽어** 쓰세요. ⛔ **과거에 게스트 계정으로 쌓인 기록을 자동으로 옮기지 마세요** — 확신 없이 옮기는 쪽이 더 위험합니다(사람이 정할 일). ⚠️ 대신 배포 뒤 「내 단어장이 없어졌어요」 류 문의가 올 수 있습니다 — 고장이 아니라 **이제야 본인 계정을 보게 된 것**입니다. 🔴 **그리고 이 수리가 «이름» 쪽에 새 사고를 만듭니다 — 함께 고치세요.** 로그인 정본이 `name: u.name || u.uid` 라(`index.html:13600`·`idx-user-session.js:73`) **이름 없는 계정은 name 칸에 아이디가 들어 있습니다.** 그걸 그대로 `student_name` 으로 보내면 서버 정본이 `student_name = COALESCE(?, student_name)` 이라 **기존에 들어 있던 진짜 이름이 아이디로 덮입니다**(`api-games.ts` 의 그 자리 주석이 같은 말을 하고, 2026-09-01 실측으로 이미 88행 중 12행이 그 상태였습니다). ⚠️ 수리 «전» 에는 그 행이 `guest_*` 라 리더보드의 `NOT LIKE 'guest%'` 에 걸려 안 보였는데, 고치면 **실계정 행이 되어 처음으로 닿습니다.** ✅ 화면의 이름 함수가 **`name === uid` 면 빈 값**을 돌려주게 하세요(`micro-quiz`·`vocab`·`speech-coach` 세 곳) — 서버가 `undefined` 를 받아 기존 이름을 안 건드립니다. ⚠️ 대소문자를 무시해 견주지 마세요(`Lee`/`lee` 는 별개 계정 — 정확일치). ℹ️ **두 리더보드는 이미 «이름 == 아이디면 안 보여준다» 로 내보내기를 막고 있습니다**(`api-points.ts`·`api-games.ts` 2026-09-05) — 즉 «공개 노출» 은 그 자리에서 막히지만 **DB 오염은 못 막습니다.** 심각도를 부풀리지도, 「막혀 있으니 괜찮다」로 낮추지도 마세요. 🔴 **덤 — 이 수리로 포인트가 «처음으로 진짜 계정에» 나가기 시작합니다**(단어퀴즈·단어장 `vocab_review`, 발음 `speech_master`). 그전에는 게스트로 가거나 계정 아닌 기기 id 로 가서 서버가 거절했습니다. **1P = 1원**이고 기프티콘으로 교환되므로 예산·상한 영향은 사람이 볼 일입니다. 감시는 `test-harness/login_uid_shape_harness.mjs`(67종 — 변이 **5종** 전부 실제 FAIL: 옛 코드 되돌리기 21건·항상 계정 반환·게스트 id 고정 제거·이름 마스킹 제거·이름을 전부 빈 값으로). ⚠️ 그 검사에서 **두 키에 «똑같은» 값을 넣으면 새 키를 읽는 줄을 지워도 옛 키 폴백이 답을 맞혀 통과합니다** — **새 키에만** 넣은 시나리오를 짝으로 두세요(함정 대조 실측). ⚠️ 그리고 **JS 주석에도 CJK 홑낫표를 쓰지 마세요** — 이 파일들은 `subtitle_meaning_harness` 의 «`<style>` 안 홑낫표 0건» 검사에 걸립니다(그 추출기가 `<style…</style>` 을 느슨하게 잡아 JS 주석까지 딸려 들어옵니다. 실제로 밟았습니다 — `« »` 를 쓰세요) |
| 같은 기기·같은 사람인데 **「다른 기기에서 로그인되었습니다」** 로 튕김 (또는 마이 페이지가 **「다시 로그인해주세요」**) | 🔴 **다른 기기가 아니라 «자기가 자기를 밀어낸» 것입니다 — 로그인 토큰 칸이 «두 벌» 인데 서로를 안 갱신했습니다.** 운영은 `SINGLE_SESSION='on'`(`wrangler.toml`)이라 **로그인에 성공할 때마다** 서버가 새 `sid` 를 발급해 KV 에 «최신 1개만» 적고 이전 토큰은 그 자리에서 401 이 됩니다(`src/auth-token.ts`·`api-students.ts` 의 `startSession`). 그런데 화면의 토큰 칸은 **`mango_token`**(홈·학습 화면 전부)과 **`mangoi_parent_token`**(마이 페이지 `parent.html` 전용) 둘인데, **한쪽이 로그인해도 다른 쪽을 안 건드렸습니다.** ⟹ 홈 ↔ 마이 페이지를 오가면 직전 토큰이 죽고, 발음코칭 채점(`/api/voice/coach`)이 401 → `session-guard.js` 가 사유를 물어 **`kicked`** 를 받아 그 안내창을 띄웁니다(2026-09-22 사장님 `lby01` 제보). ⚠️ **서버 판정 자체는 «맞습니다»** — 실제로 나중에 로그인이 한 번 더 있었고, 그것이 «같은 기기의 다른 화면» 이었을 뿐이라 서버는 구별할 방법이 없습니다. ⚠️ **두 화면이 «다른 말» 을 해서 별개 고장으로 보입니다** — `speech-coach.html` 에는 `session-guard.js` 가 실려 있고 **`parent.html` 에는 없습니다**(그래서 자기 문구). ⚠️ **비밀번호를 «설정한» 계정에서만 두드러집니다**(2026-09-22 실측 29,520명 중 **11명**) — 비번 없는 계정은 화면들이 아이디만으로 **조용히 다시 로그인해** `mango_token` 을 자동 갱신하지만(`ai-friend.html` 등), 비번 계정은 그 재발급이 **원리상 불가능**(`password_required`)이라 죽은 토큰이 **사람이 홈에서 다시 로그인할 때까지 영영** 남습니다 → 「두 번 했는데 같은 현상」. ✅ 고침은 **로그인에 성공하면 «이미 있던 같은 계정의» 반대쪽 토큰도 갱신**하는 것입니다(`pdSyncHomeToken` / `lmSyncParentToken`). ⛔ **없으면 «만들지» 마세요** — 만들면 홈에 로그인한 적 없는 기기가 로그인 상태가 되고, 마이 페이지의 자동 조회가 열려 **2026-07-30 제보 #3**(자녀가 비번 없이 학부모 페이지 열람)이 되살아납니다. 목적은 «새로 열어 주기» 가 아니라 **«되던 것을 안 죽이기»** 입니다. ⛔ **다른 계정이면 손대지 마세요** — 대소문자까지 **정확일치**(`Kim`/`kim` 처럼 대소문자만 다른 계정이 실재합니다). ⛔ `SINGLE_SESSION` 을 끄는 방식으로 풀지 마세요(아이디 돌려쓰기를 막는 상품 정책이 통째로 풀립니다). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무슨 답이 나오는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 검사는 두 구현을 **오려 내 가짜 `localStorage` 로 실제로 돌려** 답을 대조하고, 「갱신한다」 옆에 **「없으면 안 만든다」·「다른 계정은 안 건드린다」를 짝으로** 두세요(앞만 보면 «전부 갱신» = 정책 파기도 통과합니다). 🪤 **그 검사를 만들며 셋을 밟았습니다** — ⓐ 시험용 토큰을 `Date.now()` 로 만들어 **같은 인자인데 회차마다 문자열이 달라져** «저장값이 그대로인가» 가 거짓 FAIL(고정값으로) ⓑ 「시작 ~ 다음 함수 이름」으로 블록을 자르니 **그 사이에 든 옆 함수**(`pkStoreLogin`)가 딸려 와, 정작 `doStudentLogin` 의 호출을 지워도 **통과**(중괄호 «짝» 으로) ⓒ **가드 제거 변이가 «안 잡힌» 것을 검사 결함으로 오해** — 뒤 조건(`!a ||`)이 이미 막는 **등가 변이**였습니다(정책을 «진짜로 푸는» 변이로 교체해야 그 검사가 일합니다). 감시는 `test-harness/login_token_sync_harness.mjs`(52종 — 변이 **13종** 전부 실제 FAIL, «조건 뒤집기» 포함. ⑧절이 **아직 안 고친 로그인 화면 4곳**(`eval`·`report`·`signup`·`ai-friend`)의 **이름을 찍어 출력**합니다 — 사람이 정할 일). 📄 `docs/작업기록/260922_로그인_다른기기에서_로그인됨_한기기_자가퇴출_진단.md` |
| 「비번을 바꿨는데 새 비번으로 로그인이 안 돼요」 | **관리자 화면의 비밀번호 변경 자리가 두 종류였습니다.** 진짜 = `/admin/mypage.html`(→ `/api/admin/change-password`). 가짜 = 우상단 사용자 메뉴의 «🔑 비밀번호 변경» — 프롬프트 3개를 받아 놓고 **`alert('…실서비스에서는 백엔드 API 호출')` 만 띄우고 아무것도 안 보내던 시연 껍데기**가 `adm-q9(ph111)`·`adm-r19(ph113)`·`adm-r20(ph114)`·`adm-r21(ph115)` **네 벌** 있었습니다(2026-08-17 실제로 밟음 → 넷 다 실제 API 호출로 교체). 확인 방법: `SELECT updated_at FROM admin_account WHERE username=?` 가 안 움직였으면 **애초에 서버에 안 갔다**는 뜻입니다 |
| 새 화면을 만들었는데 **홈 화면(index.html)이 뜸** | **확장자 없는 주소는 `src/index.ts` 에서 한 줄로 직접 이어 줘야 합니다.** `wrangler.toml` 의 `[assets]` 가 `html_handling = "none"` 이라 `/sales` → `/sales.html` 자동 연결이 **없습니다**(`/admin/retention` 등도 전부 한 줄씩 손으로 이어 둔 것입니다). 2026-08-18 실제로 밟음 — `/sales` 를 `isAdminPath` 와 미인증 리다이렉트 목록 **두 곳에 등록하고도** 이 줄을 빠뜨려 홈 화면이 떴습니다. ⚠️ **인증 게이트는 통과하므로 「권한 문제」로 보이지도 않아** 원인 찾기가 더 어렵습니다. 새 화면은 **세 곳**(isAdminPath · 리다이렉트 목록 · ASSETS 재작성)을 전부 등록하세요 |
| `/admin/` 밑에 새 화면을 만들면서 **js·css 를 같이 넣음** | 🔴 **그 자산 요청까지 로그인 게이트가 삼켜 «화면이 조용히 반쪽»이 됩니다.** `isAdminPath` 는 2026-07-27 보안 근본수정으로 **`path.startsWith('/admin/')` default-deny** 라 «화면»이든 «자산»이든 가리지 않습니다. 그래서 `src/index.ts` 의 그 주석이 못 박아 두었습니다 — 「`public/admin/` 아래는 .html 뿐이고 js·css 정적자산이 없다(자산은 `/js`, `/css` 루트). **새로 자산을 넣지 말 것.**」 ✅ `/admin/` 화면의 CSS·JS 는 **인라인**으로 넣거나, 공용으로 쓸 것이면 `/css`·`/js` **루트**에 두세요(`/css/mangoi-han.css` 처럼 — 거기는 게이트 밖입니다). ✅ 반대로 **좋은 소식**: default-deny 라 `.html` 로 끝나는 새 `/admin/` 화면은 `src/index.ts`(공동 금지구역)를 **한 줄도 안 고쳐도** 로그인 게이트가 자동으로 걸립니다 — 위 「홈 화면이 뜸」 함정은 **확장자 없는 주소**에만 해당합니다. ⚠️ 확인은 «302 가 나오는가» 로 하면 안 됩니다 — **존재하지 않는 `/admin/` 경로도 똑같이 302** 를 주므로 그것은 «게이트가 산다»의 증거일 뿐 «파일이 서빙된다»의 증거가 아닙니다(2026-08-27 관제탑 배포 확인에서 실측). 📜 2026-08-27 `/admin/monitor-wall.html`(수업 관제탑)은 이 규칙 때문에 CSS·JS 를 전부 인라인으로 넣었고, `monitor_wall_harness` ②가 «`public/admin/` 밑 비-html 0건» 으로 못 박습니다. 📌 **덧붙임(2026-09-02) — 「그래서 전부 인라인이어야 한다」로 읽으면 안 됩니다.** 그 화면이 파일 머리말에 「→ 전부 인라인」이라고 적어 두는 바람에 33.7KB 를 HTML 이 이고 다니며 자체 상한(44KB)에 여유가 1.4KB 였습니다. **규칙은 «`public/admin/` 디렉터리» 에만 걸립니다**(`isAdminPath` 는 `path.startsWith('/admin/')` 뿐) — `/js`·`/css` **루트**는 게이트 밖이고, 그 화면이 이미 `/css/mangoi-han.css` 를 그렇게 쓰고 있었으며 `admin.html` 의 JS 도 전부 공개 `/js/` 에 있습니다. 그래서 `/js/monitor-wall.js?v=1` 로 뺐고 HTML 은 42.6 → **9.8KB**, 덤으로 그 33KB 가 **캐시됩니다**(전에는 새로고침마다 재수신). ⛔ **뺐다고 상한이 사라지면 그건 게이트 우회입니다**(4-1-1) — 하니스가 «HTML 따로 12KB»·«HTML+JS 합쳐 52KB» **두 번** 잽니다(합계는 2026-09-13 D안 상황판으로 50→52, 2026-09-14 사장님 확정). ⚠️ 그리고 **그 파일을 읽던 다른 하니스가 조용히 헛돕니다** — 내용이 딴 파일로 갔으니 함수도 값도 «없다» 로 보입니다(실측: `classes_now_mangoi_harness` 3건 FAIL). 옮길 때 **그 파일을 읽는 하니스를 전부 찾아 합본으로** 바꾸세요 |
| 「직원을 등록했는데 로그인이 안 돼요」 | **위 «시연 껍데기» 함정의 형제입니다.** 관리자 › 권한 설정 › 「➕ 본사 직원 등록」이 입력값을 **localStorage 에만 넣고 「✅ 등록 완료」를 띄웠습니다**(`adm-core.js` `registerHqEmployee`, 코드에 `password:'demo'`). 더 근본적으로 **직원 계정을 만드는 서버 API 자체가 없었습니다** — `admin_account` INSERT 는 부트스트랩·데모시드·강사 자동생성 셋뿐 (2026-08-18 실제로 밟음 → `POST /api/admin/staff-create` 신설, 화면을 실호출로 교체). 새 계정 관련 화면을 만나면 **서버에 요청이 실제로 가는지**부터 확인하세요. ⚠️ 계정 만들 때: `hq_t*` 아이디와 이름의 «교사·강사·선생» 은 `resolveRole()` 이 무조건 강사로 판정합니다 — staff-create 가 입구에서 막지만, 다른 경로로 만들면 그대로 밟습니다 |
| 가맹점별 정산서 매출이 월간 리포트보다 적음 | **매출이 두 갈래로 들어옵니다.** 카페24 결제 장부(`student_payments`)와, 학원이 통장으로 바로 보내는 **B2B 직접입금**(`bankacct_transactions` → `classifyDeposit()='b2b'`)입니다. 월간·분기·연간·KPI 는 2026-08-16 부터 둘을 합쳐 쓰는데 **가맹점별 정산서만 장부 결제만 보고 있었습니다**(2026-08-18 수리 — B2B 로 받는 가맹점은 매출이 0 으로 찍혔습니다). B2B 입금은 학생 정보가 없어 **적요를 대리점·지사 이름과 맞춰** 붙입니다(`attributeB2bDeposits()`). 적요는 잘리거나 사람 이름으로 오므로(「박선유(에스와이피(SY」) 자동으로 안 붙는 것이 정상이고, 그건 `b2b_payee_franchise_override` 에 사람이 지정합니다(관리자 › 회계관리 › 🏦 배정 못 한 B2B 입금). ⛔ 비슷하게 생겼다고 추측해서 붙이지 마세요 — 정산서는 가맹점에 보내는 문서입니다 |
| `public/js/*.js` 를 고쳤는데 하니스가 FAIL | `asset_version_harness.mjs` 가 **파일 내용이 바뀌었는데 HTML 의 `?v=` 가 그대로면 FAIL** 냅니다(immutable 캐시에 옛 파일이 남는 사고 방지). 고친 js 를 부르는 HTML 의 `?v=` 를 함께 올리세요. ⚠️ **그 HTML 이 «하나가 아닐 수» 있습니다** — `<script src>` 만 찾지 말고 **preload 목록**도 보세요(`idx-main.js` 는 `index.html` 의 script 와 **`teacher.html:3444` 의 preload 배열** 둘입니다). 한쪽만 올리면 `home_bg_preload_harness` ⑦이 「그 주소로 부르지 않는 파일을 미리 받는다」로 FAIL 냅니다(2026-09-08 실제로 밟음). ✅ 확인은 `grep -rn '<파일명>?v=' cloudflare-deploy/public/ --include=*.html` 한 줄 |
| 하니스를 **`| tail`·`| grep` 에 물려** 돌리고 «exit=0» 을 통과로 읽음 | 🔴 **파이프의 종료코드는 «마지막 명령» 의 것입니다 — `tail` 은 언제나 0 입니다.** 2026-09-14 실측: `node test-harness/run.mjs --fast 2>&1 \| tail -4` 가 `exit=0` 을 찍어 「FAIL 0」으로 읽고 PR 을 올렸는데, CI 가 **`⚠ FAIL 1`**(`voice_fallback_harness`)로 잡았습니다 — 그 줄이 tail -4 «밖» 이라 화면에 아예 안 나왔습니다. ⚠️ 하필 `run.mjs` 의 마지막 네 줄은 「📌 기준선 유지(fast): PASS … SKIP …」이라 **FAIL 이 몇이든 똑같이 초록처럼 보입니다.** ✅ **파일로 받으세요** — `node test-harness/run.mjs --fast > /tmp/fast.txt 2>&1; echo "exit=$?"` 그리고 `grep '⚠ 실제 확인' -A 3 /tmp/fast.txt` 로 «어느 하니스가» 를 보세요. ⚠️ 꼭 파이프를 써야 하면 `set -o pipefail` 을 켜거나 `${PIPESTATUS[0]}` 를 읽으세요. 📜 같은 줄기: 이 표의 「하니스를 하나씩 돌려 «종료코드» 로 회귀를 봤는데 CI 만 빨간불」 — 그쪽은 «exit 0 을 찍는 하니스», 이쪽은 «파이프가 exit 를 갈아치우는» 것입니다. 둘 다 **«FAIL 목록» 을 눈으로 봐야** 끝납니다 |
| 목록 파일을 만들어 **`while read` 로 한 줄씩** 돌릴 때 | 🔴 **끝에 줄바꿈이 없으면 «마지막 줄» 이 조용히 버려집니다 — 에러도 종료코드도 정상입니다.** `fs.writeFileSync(f, list.map(...).join("\n"))` 는 끝 줄바꿈을 안 붙이는데, POSIX `read` 는 «줄바꿈으로 끝나지 않은 마지막 조각» 을 읽고도 **거짓을 돌려줘** 루프가 그 자리에서 끝납니다. [잰 것 — 2026-09-23] 낱말 사진을 CI 러너로 받아오며 **두 번 연달아** 같은 한 장이 빠졌습니다(117→**116** · 118→**117**, 매번 목록 마지막 항목). 러너는 `exit 0` 이라 어디에도 실패로 안 보이고, «표에는 있는데 그림이 없는» 상태가 되어 빌드가 그 항목을 조용히 건너뜁니다. ✅ **`join("\n")+"\n"`** 한 글자로 끝납니다. ✅ 그리고 **처리한 개수를 목록 길이와 대조**하세요 — 그 숫자가 이 사고의 유일한 신호였습니다(`wc -l` 로 목록 줄 수를 먼저 찍어 두면 더 빠릅니다). ⚠️ `printf '%s\n'` 이나 `join("\n")+"\n"` 으로 «만드는 쪽» 을 고치세요 — 읽는 쪽에서 `|| [ -n "$LINE" ]` 를 덧붙이는 방법도 되지만, 그 한 줄을 빠뜨린 다음 사람이 또 밟습니다. 📜 같은 줄기: 바로 위 「파이프의 종료코드」 — 둘 다 **«개수를 세어 봐야»** 끝납니다 |
| 로컬 하니스는 **초록**인데 **CI 만 빨간불** (`asset_version`) | 🔴 **`asset_version_harness` 는 로컬에서 «스스로 고치고» 통과합니다.** 새 `?v=` 를 만나면 「🆕 새 버전 1건 기록 (정상)」이라며 원장(`test-harness/asset-versions.json`)에 **적어 넣고** 통과합니다. CI 는 그 원장을 되돌려 쓸 수 없으니 **FAIL** 냅니다 — 즉 **원장 변경을 커밋에 안 담으면 로컬만 초록**입니다. ⚠️ 2026-08-26 실제로 밟음(PR #490 run #542): 생성물(`*_report.txt`)을 되돌리려고 `git checkout -- test-harness/` 를 했더니 **원장의 새 항목까지 함께 되돌아갔고**, 그 상태로 push 되어 게이트 **②(회귀 하니스)와 ④(정적자산 ?v=)가 같은 한 건으로 동시에** 터졌습니다(로그를 안 열면 «두 군데가 깨졌다» 로 오독합니다). ✅ `git checkout -- test-harness/` 처럼 **폴더째 되돌리지 마세요** — 되돌릴 파일을 하나씩 적으세요. ✅ `git status` 에 `asset-versions.json` 이 남아 있으면 **반드시 함께 커밋**하세요. 📌 **덧붙임(2026-08-28) — 그 사고의 «미끼»였던 `*_report.txt` 5개는 이제 추적하지 않습니다**(`.gitignore`). 하니스가 쓰기만 하고 **읽는 코드가 0곳**인데 **5개 전부 실행 시각이 박혀 있어**(다섯 하니스 모두 `toISOString`) **`--fast` 를 돌릴 때마다 4개가 바뀌었고**(`changes_qa` 만 E2E 라 `--fast` 에서 SKIP · 14일간 커밋 1,285건 중 135건이 이것), 그래서 「생성물을 되돌리려다 원장까지 날리는」 동작 자체가 필요했습니다. 이제 하니스를 돌려도 `git status` 는 깨끗하니 **되돌릴 일이 없습니다.** ⛔ 원장(`asset-versions.json`)은 **읽는** 파일이라 계속 커밋해야 합니다 — 생성물이라고 같이 무시하지 마세요 |
| 「라우팅 허용목록에 올렸으니 인증도 된 것」 | 🔴 **둘은 다른 것입니다 — 허용목록은 «라우팅», 인증은 `isAdminPath`(및 핸들러 안의 게이트) 입니다.** 2026-08-28 실측: `POST /api/eval/create` 가 허용목록에는 있는데 인증이 **한 줄도 없어**, 무인증 POST 가 핸들러 본문까지 닿았습니다(401 이 아니라 핸들러 자신의 400 이 돌아옵니다 — 이 응답 차이가 판별법입니다). 아이디만 알면 **서버가 학부모 번호를 찾아** 본문에 담긴 문구로 문자를 보냈습니다. 같은 날 `judgment/growth`(남의 아이 레이더·취약유형)·`vocab/reward`(포인트 적립) 도 같은 상태였습니다. ⚠️ **주석을 믿지 마세요** — judgment 세 경로 머리말에는 「본인(토큰) 또는 관리자(세션)만. IDOR 차단」이라고 적혀 있었는데 검사가 아예 없었습니다. ✅ 판별은 «무해 프로브» 로: 필수 필드를 뺀 요청을 보내 **401 이 오는지 핸들러 검증 오류가 오는지** 봅니다(부작용 전에 갈립니다). ⛔ 포인트·환불·발송 경로는 프로브하지 마세요. ✅ 게이트를 달 때는 **호출자 전원의 자격증명부터 확인**하세요 — 관리자 화면이면 `checkAdminSession`, 학생 화면이면 `resolveOwnerScope`+화면이 `mango_token` 을 싣게(게스트는 `guest_*` 라 그대로 통과). 학생 화면인데 토큰을 못 싣는 곳(`index.html` = 금지구역·첫화면 예산)이면 **게이트 대신 «본문 값을 믿지 않기»** 로 좁히세요. 감시는 `test-harness/public_api_auth_harness.mjs` |
| 공개 API 에 학생 **아이디**를 실어도 되나 | ⛔ **안 됩니다 — 이 서비스에서 아이디는 곧 비밀번호입니다.** `/api/student/login` 은 `if (stu.password_hash)` 로 **비밀번호가 설정된 경우에만** 검사하는데, 실측상 설정된 학생은 **29,520명 중 11명**뿐입니다(2026-09-22 재측정 — 2026-08-28 에는 0명이었습니다). 즉 **나머지 29,509명은** 아이디를 아는 사람이 그대로 로그인합니다. ⚠️ 이 숫자를 근거로 새 결정을 내릴 때는 **그때 다시 재세요**(`SELECT COUNT(*), SUM(password_hash IS NOT NULL AND TRIM(password_hash)<>'') FROM students_erp`). 2026-08-28 까지 `/api/points/leaderboard` 가 무인증으로 아이디+이름 50개를 내주고 있었고, 거기서 얻은 아이디로 그 아이의 판단력 기록까지 읽혔습니다. ✅ 본인 표시가 필요하면 **서버가 판정해 `me` 불리언만** 주세요 — 같은 저장소의 `/api/vocab/leaderboard` 가 이미 그 방식입니다(`row.me`). ✅ 이름이 없을 때 아이디로 폴백하지 마세요(화면 쪽도 함께 고쳐야 합니다). ⚠️ 비밀번호를 강제하는 쪽은 별건입니다 — 어린 학생이 많아 사람이 정할 일입니다 |
| 「강사 미입장」 알림이 **강사가 7분 전부터 방에 있는데도** 나감 (학생이 입장 버튼을 누른 «즉시») | 🔴 **학생 화면이 입장 버튼을 누른 «순간»(소켓·카메라 연결 전)부터 세고, `student_entered` 즉시 호출은 `waited_minutes:0` 을 보내는데 서버가 `\|\| 5` 로 받아 「5분째 기다림」으로 적었습니다.** 2026-09-03 `class-1079`: 강사 Krystel 이 21:13 부터 방에 있었고, 21:20:34 에 「강사 미입장」 푸시가 나갔습니다(다른 표에 그 전 흔적이 없어 그 초에 버튼을 누른 것으로 보입니다 — 추론)(학생은 21:34 에야 연결 — 폰·회선 사정). ⚠️ 위 「노쇼 알림이 떴는데 강사는 들어와 있었음」과 뿌리가 다릅니다 — 그쪽은 «방 갈림», 이쪽은 «세는 시점». ✅ 2026-09-04: 서버가 보내기 «전에» `no-show-truth.ts` 의 `teacherLiveInRoom`(정본 `teacherPresenceByRoom` 재사용 — 이름 일치만, `role` 안 믿음, 3분 창, 모르면 false)으로 «강사가 방금까지 살아 있었나» 를 묻고 참이면 push·알림톡 **둘 다** 건너뜁니다(응답 `teacher_present`). ⛔ 기록(`class_no_show`)은 그대로 남깁니다 — 학생이 못 본 것은 사실이고 읽는 쪽(노쇼 리포트·급여)이 같은 함수로 «오판» 을 붙입니다. ⚠️ **그래서 그 방에 행이 «0건» 인 것을 «억제가 됐다» 로 읽으면 안 됩니다** — 억제되면 행은 **남습니다**(`notified_push=0`). 0건은 **호출 자체가 없었다**는 뜻이고, 그 알림은 클라이언트가 «상대가 안 보일 때만» 부르기 때문입니다(`idx-main.js` 의 `if (waitingFor === 'teacher' && !calledTeacher && !counterpartPresent())`). ⟹ 이 서버 억제는 **«학생 화면이 강사를 못 볼 때» 만 닿는 2차 방어**입니다 — 강사가 먼저 들어와 화면에 보이면 애초에 호출이 없습니다. (2026-09-04 `class-851` 실측: 강사가 3분 06초 먼저 입장 → 행 0건. 억제가 실제로 도는 것은 아직 못 봤습니다.) ⛔ 5분을 소켓 접속 «뒤» 부터 세게 고치는 것은 `idx-main.js`(blocking, 첫 화면 예산) 수정이라 별건입니다. ⚠️ 이 판정은 출석 행에 강사 이름이 있어야 성립합니다 — 이 표의 「본인 토큰이면 통과 게이트가 로그인한 사람을 전부 403」(PR #795)이 함께 고쳐져야 학생 쪽도 제대로 쌓입니다(강사는 관리자 세션이라 원래 통과). ⚠️ 문자열 하니스가 못 잡습니다 — 값도 조건도 «있고» 틀린 것은 «언제부터 세는가» 뿐. 감시는 `test-harness/noshow_teacher_present_harness.mjs`(25종 — `teacherLiveInRoom` 을 node 타입 제거로 컴파일 없이 **실제로 돌리고**, waited 계산식을 오려 내 평가. 변이 실제 FAIL). 📊 **[잰 것 — 2026-09-04, 그때까지 나간 「강사 미입장」 알림 47건 전수]** 정본을 실제로 돌려 재판정하니 **27건(57%)이 «강사가 그 순간 방에 있었는데 나간» 오판**이고, 그 강사들은 알림보다 **가운뎃값 4분 12초**(가장 짧은 것 17초·가장 긴 것 7시간) 먼저 들어와 있었습니다. 그중 14건이 사장님 수업(「중국어 강선생님」)입니다. 나머지는 3분보다 전 3건 · 흔적 없음 3건 · 판정 불가 14건(강사가 알림 «뒤» 에 들어온 진짜 미입장이 여기 섞여 있습니다). ⛔ **그 재판정을 «지금 있는 출석 데이터» 로 그냥 하지 마세요** — `p.to` 는 그 방 강사 접속의 «최종» 퇴장 시각이라 수업 끝까지 있었으면 알림보다 20~30분 «뒤» 이고, `nowMs - p.to` 가 음수라 **전부 «살아 있음»(85%)으로 부풀어집니다**(실제로 한 번 그렇게 셌습니다). 알림 시각 기준으로 «그 뒤에 들어온 행은 빼고 out_at 은 그 시각으로 잘라» 그때의 서버 상태를 재현하세요. ⛔ **미래 시각을 «살아 있음» 으로 세지 마세요** — `left_at`·`last_seen_at` 은 접속이 찍을 때만 서버 시각이고, **카페24가 «예약» 으로 미리 만드는 출석행은 `left_at` 이 예약 종료 시각이라 미래**입니다(2026-09-04 실측 **387~397건** — 예약 시각이라 시간이 흐르면 과거가 되어 줄어듭니다. 가장 먼 것 2030-02-20). **오늘의 사고는 아닙니다** — 그 행은 **전부 `c24-*` 방**이라 `class-*` 방 번호로 묻는 노쇼 판정과 구조적으로 안 만나고, 정본이 보는 **`username`** 칸에 강사 이름이 붙은 것도 0건입니다. ⚠️ **`teacher_name` 칸은 366건 채워져 있습니다** (`cafe24-sync.ts` 의 씨앗 INSERT 가 그 칸은 넣고 `username` 에는 «학생» 이름을, `role` 에는 `student` 를 박습니다) — 그 숫자를 보고 «0건이 아닌데?» 로 읽지 마세요. 정본은 `username` 만 봅니다. ⚠️ **지금 코드에 «미러가 `class-*` 에 씨앗을 만드는» 경로는 없습니다** — 미래 `left_at` 을 쓰는 `INSERT INTO attendance` 는 `cafe24-sync.ts` 한 곳뿐이고(나머지 7곳은 서버 `Date.now()`) 미러는 `class_schedules` 만 씁니다. 그래도 막아 두는 것은 그 전제가 깨지는 순간 «예약이 잡혀 있다» 가 «지금 강사가 있다» 로 읽혀 **진짜 노쇼가 조용히 감춰지기** 때문입니다(그 방향이 이 파일이 가장 나쁘다고 못 박은 것). `teacherLiveInRoom` 이 60초 넘는 미래를 거부합니다(`FUTURE_SLACK_MS`). ⛔ 그 조임을 `teacherPresenceByRoom`(사후 리포트)까지 넓히지 마세요 — 거기는 수업이 끝난 뒤를 읽으므로 미래 값이 정상입니다 |
| 강사가 **21분 수업했는데 「강사 미입장」으로 남고 급여가 0원** | 🔴 **원부 이름(`teachers.name`)과 화상방 입장 표기가 다르면 «강사가 없었다» 로 확정됩니다.** 이름을 붙이는 경로는 둘뿐입니다(`src/no-show-truth.ts`) — ① 낱말 경계 일치 ② 계정 → 원부 이름 해석(`accountToTeacherName`). 2026-09-08 `class-1924`: 원부는 **`FAR`**(teachers.id=22) 인데 그 강사는 **`교사 Teacher - Farrah`** 로 들어와 **둘 다 실패**했습니다(`FARRAH` 는 낱말로 `FAR` 이 아니고, `Teacher - Farrah` 는 계정이 아니라 계정표에도 없습니다 — 그 강사 계정은 `mangoi_018`). 결과가 둘입니다: ⓐ 강사가 **1분 52초 먼저** 들어와 있는데 「강사 미입장」 푸시가 **실제로 나갔고**(`notified_push=1` — 같은 날 이름이 붙은 세 건은 억제됐습니다) ⓑ `present` 가 **`false`**(=«모름» 이 아니라 「없었다」 **확정**)라 **급여 되돌림도 안 걸립니다**(`api-admin.ts` 는 `present === true` 일 때만 되돌립니다) ⟹ **21분 05초 수업한 강사의 수업이 노쇼로 남습니다.** ⚠️ **같은 강사가 계정(`mangoi_018`)으로 들어온 날은 붙습니다** — 그래서 «되다 안 되다» 로 보여 재현이 헷갈립니다. 📊 **[잰 것 — 2026-09-08, 최근 30일 강사 접속 254회]** 이름을 못 붙이는 표기는 **2종 59회(23%)** 입니다: `교사`(이름 없이 입장) 34회는 정보 자체가 없어 **원리상 못 고치고**, `교사 Teacher - Farrah` 25회만 고칠 수 있습니다. 나머지(Kaye·Kes·Krystel·Hannah·Shas·Cindy·Win·계정형 `mangoi_xxx`)는 전부 붙습니다. ✅ 정본은 **`NAME_ALIASES`**(`src/no-show-truth.ts`) — «완전일치 쌍» 만 손으로 적습니다. ⛔ **부분일치를 열어서 풀지 마세요** — `FAR ⊂ FARRAH` 를 열면 `ANNA ⊂ HANNAH` 가 되살아나 **남의 이름이 붙고**, 그 방향은 진짜 노쇼를 감춰 수업료를 전액 내보냅니다(`KRY` 는 `KRYSTEL` 에 **안 붙어야** 합니다). ⛔ 한 이름을 두 그룹에 넣지 마세요 — «누구인지» 가 갈립니다. ⛔ 별칭은 «이름 전체» 가 표에 있을 때만 걸리게 하세요(낱말끼리 넓히면 그 순간 부분일치가 됩니다). ✅ 확인은 `SELECT DISTINCT username FROM attendance WHERE role='teacher'` 를 `teachers.name`·`teacher_account_links` 와 대조하는 것. ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «이름이 붙는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 🔴 **별칭을 상대의 «낱말» 에까지 넓히면 안 됩니다** — 그러면 별칭이 상대 이름의 «낱말» 에 걸려 **표에 없는 사람까지** 같은 사람으로 붙습니다(부분일치를 연 것이 아닌데 결과가 같아지는 자리라, 한 번 그렇게 짰다가 함정 대조가 잡았습니다). ✅ 그래서 **양쪽이 «둘 다» 그룹에 있을 때만** 붙입니다. 📌 그때 걸린 것은 `FARRAH` ⊂ `HT FARRAH`(teachers.id=3, `active=0`) 였는데, **2026-09-08 사장님 확인으로 그 둘은 같은 사람**이라 지금은 그룹에 함께 있습니다(id 3 은 퇴사 행 — 실측 활성 수업 0건·계정 연결 0건·출석 표기 0건). ⛔ **그래도 「낱말까지 넓히기」 금지는 그대로입니다** — 막는 것은 그 쌍이 아니라 «부분일치 일반» 이고, `HT NESS` 처럼 낱말 `HT` 만 겹치는 남이 딸려 옵니다. ⛔ **`api-mango.ts`·`c24-mirror.ts` 의 부분일치 금지도 풀지 마세요** — 그 규칙이 지키는 것은 학생 동명이인(김민서 71명·김민준 56명)입니다. 두 파일의 «남의 사람» 서술만 사실에 맞게 고쳐 두었습니다. ⚠️ **`active=1` 만 대조하면 못 잡습니다** — 퇴사자 이름이 명부에 남아 있고 출석 표기에 찍히면 그대로 붙습니다. 🔴 **표에 적을 값은 «화면에 보이는 표기» 가 아닙니다** — 역할 접두사가 **두 번** 벗겨집니다(`namesOf()` 가 한 번, `sameTeacherByWord` 가 또 한 번) → 실제 경로에 닿는 열쇠는 `FARRAH`, 직접 호출은 `TEACHER FARRAH` 라 **둘 다 적어야** 합니다. 하나만 적으면 다른 쪽에서 **조용히 안 걸립니다**(각각 한 번씩 실제로 밟았습니다). 감시는 `test-harness/noshow_teacher_present_harness.mjs` **D절**(별칭 그룹을 **소스에서 읽어** «그룹 안끼리는 붙는다»·«다른 강사는 새로 안 붙는다» 를 **전수로 실제 실행**합니다. ⚠️ 뒤쪽은 **별칭표를 비운 사본과 나란히 돌려 «별칭이 새로 붙였는가» 로** 묻습니다 — 그냥 «붙는가» 로 물으면 옛 낱말 규칙이 원래부터 붙이던 쌍까지 이 변경 탓으로 잡아 **거짓 FAIL** 이 납니다. 변이 전부 실제 FAIL: 별칭표 비우기 7건 · **낱말까지 넓히기 9건** · **남(`HT NESS`)을 그룹에 넣기 1건** · `&&`→`||` 59건 · `includes` 로 열기 · 한 이름을 두 그룹에 · 구분자 흡수 제거. ⚠️ **반례를 «실측 명부» 로만 두면 못 잡습니다** — 지금 명부에 `FAR`·`FARRAH` 를 낱말로 가진 다른 강사가 없어서 한 번 통과했습니다. **그룹 원소에서 반례를 자동 생성**하세요(`'ZZQ ' + 원소`). ⚠️ 그리고 「그룹에 있으면 건너뛴다」 식으로 짜면 **남을 그룹에 넣어 버릴 때 검사가 스스로 비켜납니다** — «이 이름들은 어느 그룹에도 없다»(D-17)를 따로 두세요. ⚠️ 그때 **변이를 «원래 구조 그대로» 재현하세요** — 한쪽 방향만 넓힌 부정확한 변이는 통과합니다(실측). 🪤 **곁가지로 더 큰 것을 찾았습니다** — 정본에 새 타입(`readonly (readonly string[])[]`)이 생기자 `no_show_false_alarm_harness` 의 «타입 이름을 하나씩 적어 둔» 추출이 깨져 **검사 5건이 조용히 사라졌는데**(그중 「`Anna` 는 `HANNAH` 가 아니다」) **그 하니스가 실패해도 종료코드가 0 이라 `--fast` 합계가 한 자리도 안 움직였습니다.** ✅ 타입 제거는 «이름» 이 아니라 **«자리»**(`const 이름 : … =`)로 하고, 하니스는 **실패하면 `process.exit(1)`** 하게 두세요. ✅ 그리고 회귀를 볼 때 **숫자만 비교하지 말고 FAIL «목록» 을 `comm` 으로 대조**하세요 — 저는 숫자가 같아서 「회귀 0건」이라고 **틀리게 보고**했습니다. ⚠️ 근본 해결은 표기를 한쪽으로 맞추는 것입니다(예약표를 고치거나 입장 이름을 계정으로 통일) — D1·화면이 걸린 별건이라 **사람이 정할 일**이고, 그때 별칭표에서 그 줄을 지웁니다 |
| 야간 작업이 하루에 여러 번·동시에 돌고 있음 | 🔴 **`scheduled()` 안에서 «시(hour)» 로 가르면 안 됩니다.** `wrangler.toml` 의 `crons` 5개 중 하나가 **15분마다 도는 감시견**이라, `if (hour === 18)` 은 그 시간대에 **네 번**(정각·15·30·45분) 참이 되고, 정각에는 전용 cron 이 **별도 호출**로 한 번 더 들어와 같은 작업이 **동시에** 돕니다 → 하루 5회 + 정각 동시 2회. ⚠️ **이 파일은 이미 알고 있었습니다** — 법인카드 블록에 「event.cron 정확 일치로 거른다 — 같은 시각에 15분 cron 도 울리므로 hour 비교는 이중 실행」이라 적어 두고 **그 한 곳만** 고쳐져 있었고 나머지 여섯 곳이 hour 비교였습니다(2026-07-10 감시견 추가 이후 약 7주). 걸린 것: 학부모 문자 이중 발송, 월간 리포트 5회 발송, 그리고 자동결제를 켜면 **카드 이중 청구**(청구 전 행 선점이 없고 주문 id 를 매번 새로 만들어 PG 가 다른 주문으로 봅니다). ✅ `cronIs('0 N * * *')` 정확일치로 가르세요. 전용 cron 이 없어 15분 트리거를 타야 하는 작업은 그렇다고 **명시**하세요(`isWatchdogTick`). ✅ 감시는 문자열이 아니라 **크론 표를 읽어 하루치 가짜 이벤트를 돌려 횟수를 세는** 방식으로 — `test-harness/cron_double_run_harness.mjs` |
| 블록주석을 정규식 한 줄로 지웠더니 **코드가 함께 사라짐** | 🔴 **문자열·주석 안의 짝 없는 «슬래시+별표» 하나에 그 뒤가 통째로 지워집니다.** `t.replace(/\/\*[\s\S]*?\*\//g, '')` 를 `src/index.ts` 에 돌렸더니 **293,654자 → 209,071자**(8만자 증발)가 됐고, 그 사본으로 검사하니 멀쩡한 코드가 «없다» 로 판정됐습니다(2026-08-28 도구·하니스에서 각각 한 번씩 밟음). ⚠️ 이 저장소는 크론 표기 `⁎/15` 처럼 «별표+슬래시» 를 문자열로 갖고 있어 특히 위험합니다 — 같은 이유로 **주석 안에 route glob 을 그대로 적지 말라**는 항목이 이미 이 표에 있습니다(그 뒷면입니다). ✅ 주석 제거는 **줄 단위로 «지금 블록주석 안인가» 를 추적**하세요(본보기: `cron_double_run_harness` 의 strip). ✅ 그리고 부정 검사(«이 글자가 없어야 한다»)에만 쓰고, 블록을 자를 때는 **중괄호 짝**으로 자르세요 📌 **덧붙임(2026-09-09) — 주석을 «문자열 사이» 에 끼워 넣지 마세요.** 「따옴표 + 블록주석 + 따옴표」는 뒤 문자열에 **단항 +** 가 걸려 `aNaN` 이 됩니다 — CSS 를 문자열로 조립하던 자리에서 **규칙이 통째로 죽었는데 화면은 «줄이 보여서» 멀쩡해 보였습니다.** 게다가 그 상태로도 대비 검사가 통과했습니다(스타일이 없어 오히려 대비가 좋아짐) — **변이시험이 «고쳤는데 되돌려도 통과» 로 나와서** 발견했습니다. ✅ 설명은 그 «식 밖» 에 두세요. ✅ 그런 자리에는 «값이 맞나» 만이 아니라 **«스타일이 살아 있나»**(글자 크기·줄 위치)를 짝으로 재세요. ⚠️ 덤으로 그 주석 «안» 에 별표+슬래시를 그대로 적으면 블록주석이 거기서 닫혀 파일이 통째로 죽습니다(같은 날 이어서 밟았습니다). |
| HTML 속성값 하나 때문에 **멀쩡한 검사 3건이 거짓 FAIL** (`accept`·`content` 등) | 🔴 **바로 윗줄의 «블록주석» 함정이 «HTML 속성» 으로 나타난 것입니다.** 2026-09-10 실측: 업로더에 파일 고르기 칸을 달며 `accept=".pdf,.jpg,…,image/⁎"` 라고 적었는데(끝의 «슬래시+별표»), 하니스의 주석 제거기가 그것을 **블록주석 여는 기호**로 읽어 **그 뒤 8,378자를 통째로 먹었습니다** — 드롭존 안내·팁 상자·분류 카드 문구가 «없다» 로 판정돼 **손대지도 않은 검사 3건이 빨간불**이 났습니다. ⚠️ 원본에는 그 글자가 멀쩡히 있어서, 파일을 아무리 들여다봐도 원인이 안 보입니다 — `strip(원문)` 과 `원문` 에 같은 정규식을 돌려 «어느 쪽에서 사라졌나» 로 갈라야 찾습니다. ✅ 여기서는 **`image/⁎` 를 빼는 것이 맞는 고침**이었습니다 — 서버가 받는 것은 pdf·jpg·png·webp 뿐이라(`allowedExt`) 그 값은 **HEIC·GIF 를 «고를 수 있게» 해 놓고 업로드에서 `invalid_type` 으로 거절**하게 만듭니다(고를 수 있는데 안 올라가는 상태). ⛔ 「검사가 잘못됐으니 검사를 고치자」로 먼저 가지 마세요 — 이번에는 **속성값 쪽이 실제로 틀렸습니다.** ⚠️ 그래도 제거기 자체는 여전히 무릅니다(이 저장소에 여러 벌) — 새 속성·새 문자열에 «슬래시+별표» 를 쓸 일이 있으면 그 자리에서 한 번 의심하세요. ✅ 감시는 `textbook_uploader_naming_harness` ⑧절(«accept 에 주석을 여는 글자가 없다»). 📌 이 줄에 그 글자를 **그대로 적지 않은 이유**도 같습니다 — 적는 순간 이 규칙서를 읽는 검사가 같은 함정을 밟습니다(별표를 `⁎` 로 적었습니다) |
| 하니스가 전부 초록인데 화면이 죽어 있음 — «선언 위치» 는 문자열로 안 보입니다 | 🔴 **함수 선언을 다른 함수 «안» 에 넣으면 그 밖의 호출부는 전부 `ReferenceError` 인데, 문자열 검사는 «선언도 있고 호출도 있다» 로 통과합니다.** 2026-08-28 실측: 토큰 헬퍼를 `applyAnswer()` 안에 넣어 `vocab.html`·`micro-quiz.html` 의 호출 6곳이 죽었는데(단어 추가 버튼이 학생 화면에 생 JS 에러를 띄우고, 세션 포인트가 조용히 0), **하니스 229건이 전부 초록이었고 trap-check 가 잡았습니다.** 함수 선언 호이스팅은 **그 함수 스코프까지만** 올라갑니다. ✅ 화면에서 공용으로 쓸 헬퍼는 **스크립트 최상위**에 두세요(본보기: `judgment.html` 의 `authTok()`). ✅ 감시는 «있는가» 가 아니라 **«어디에 있는가» 를 세서** — 선언 앞의 중괄호 깊이가 0인지 확인합니다(`public_api_auth_harness` D절). 같은 뿌리: 이 표의 「축이 둘인데 한쪽을 임시 분기 안에 넣어 두었더니」. 📌 **덧붙임(2026-09-10) — «스크립트로 코드를 끼워 넣을 때» 가 이 함정의 가장 쉬운 입구입니다.** 앵커를 `s.index("  try { console.log('[mobilefix] ")` 처럼 **«첫 번째 일치»** 로 잡았는데 그 문자열이 **딴 절(가면 ⑦절) 안쪽에 먼저** 있어서, 새 절이 **깊이 3의 중첩 함수 안**에 들어가 **한 번도 실행되지 않았습니다**(2026-09-10 인앱 안내). ⚠️ `node --check` 통과 · 회귀 하니스 통과 · **콘솔 오류 0** — 아무 데서도 안 보입니다. ✅ 앵커는 **`rindex`(마지막)** 로 잡거나 그 자리에만 있는 문자열로 잡고, **넣은 뒤 «그 줄 직전 중괄호 깊이가 1인가»** 를 세어 보세요(`node -e` 로 열 줄이면 됩니다). ✅ 그리고 그 절이 «실제로 돌았는가» 를 **브라우저에서 표식 하나로** 확인하세요. ⛔ 그때 **표식을 «여러 곳이 다시 감싸는» 것에 달지 마세요** — `window.showView` 가 그렇습니다(덮어쓰는 곳이 8군데). 뒤에 오는 defer 가 다시 감싸면 **정상 동작 중에도 그 표식이 사라져** 다음 사람이 없는 버그를 쫓습니다(제가 그렇게 적었다가 함정 대조가 잡았습니다). **그 절만의 전역**(`window.__mg…Ready`)이나 «그 절이 만드는 DOM» 으로 재세요. 📌 **덧붙임(2026-09-04) — 「인라인 `onclick`·`onchange` 가 부르는 이름」이 같은 병의 형제입니다.** 인라인 핸들러는 **언제나 «전역»(`window`)에서 이름을 찾습니다** — 그 화면 스크립트가 통째로 하나의 닫힌 스코프면 `onchange="draftSave()"` 는 **`ReferenceError`** 입니다. ⚠️ **에러가 콘솔에만 나고 화면은 멀쩡합니다** — 실사고: 결재 「지출 항목」을 골라도 **초안에 그 값만 조용히 안 담겼습니다.** ⚠️ 문자열 검사는 「그 함수가 있는가」·「그 속성이 있는가」로만 봐서 못 잡습니다(수리 전 `--fast` 282건 전부 초록). ✅ 인라인 대신 **위임 리스너**(부모 상자에 `addEventListener`)를 쓰세요 — 폼을 다시 그려도 살아남는 이득도 있습니다. ⚠️ 그때 `input` 만 듣지 말고 **`change` 도 함께** 두세요(고르는 칸은 조작 방식에 따라 `input` 이 안 옵니다). ⚠️ 검사도 «부르는가» 가 아니라 **브라우저에서 실제로 조작해** 보세요 — 그리고 `new Event('change')` 는 **bubble 하지 않으므로**(`{bubbles:true}` 필요) 위임 리스너가 안 깨어나 «코드가 틀린 것처럼» 보입니다 📌 **덧붙임(2026-09-15) — 같은 치환 함정이 «규칙서 자신» 에도 걸리고, 그쪽이 더 나쁩니다.** 제 하니스 변이 수를 「변이 5종 …」 → 「8종」으로 정정하려고 그 문자열을 치환했는데 **파일에 4곳**이 있어 엉뚱한 줄(2026-09-09 «사무실 모드» 항목)이 걸렸습니다 ⟹ 제 행은 여전히 **「5종」(틀린 값)** 인 채로 남고, 무관한 그 줄에 **liveness 수리의 배선 내용(`autoSeenOf()`·`autoAt = 0`)이 들어가** «사무실 모드 검사가 그런 것을 본다» 는 **거짓 문장**이 규칙서에 박혔습니다(함정 대조가 아니라 병합 뒤 `git diff origin/main` 의 «내가 안 건드린 줄이 왜 바뀌었지?» 로 잡았습니다). ⚠️ 코드와 달리 여기엔 **타입체크도 하니스도 없습니다** — 거짓이 박히면 다음 사람이 그대로 믿습니다(이 파일이 네 번 사고를 낸 그 형태). ✅ 이 파일을 스크립트로 고칠 때는 **① 치환 전에 `grep -c` 로 몇 곳인지 세고** ② 앵커를 «그 항목의 첫 칸»(`| 수업이 **2~3분마다…`)으로 잡아 **그 줄 안에서만** 치환하며 ③ 커밋 전에 **`git diff origin/main -- CLAUDE.md` 가 «내 행 한 줄 추가» 뿐인지** 확인하세요. ⛔ 「몇 줄 바뀌었나」로 보지 마세요 — 한 항목이 한 줄이라 **남의 줄을 통째로 고쳐도 `1 insertion, 1 deletion`** 으로 보입니다. |
| 야간 동기화가 «내가 D1 에 적어 둔 값» 을 지웁니다 | 🔴 **`INSERT OR REPLACE` 는 «행을 지우고 다시 넣는» 것이라 컬럼 목록에 없는 칸이 전부 NULL 이 됩니다.** `importCafe24Students` 는 학생 전원(29,428행)을 매일 밤 지우고 19칸만 다시 넣었는데 표는 44칸이라 **25칸이 매일 밤 사라졌습니다.** 그중 셋은 카페24가 아니라 우리 코드가 쓰는 값이었습니다 — `password_hash`(학생 비밀번호)·`parent_user_id`(학부모 연결)·`eval_band`(수업평가 밴드). 즉 **학생 비밀번호를 도입할 수 없는 상태**였습니다(정해도 그날 밤 사라짐). 전화번호가 같은 이유로 전멸했던 것이 2026-08-18 건인데 그때는 «번호만» 목록에 넣었고, `franchises`·`centers` 는 2026-08-14 에 이미 UPSERT 로 바꿨는데 **학생만 남아 있었습니다.** ✅ 2026-08-28 수리: DELETE 가 그 세 칸에 값이 있는 행을 비켜 가고, `ON CONFLICT(user_id) DO UPDATE SET <카페24 칸만>` 으로 바꿨습니다. ⛔ `created_at` 은 갱신하지 마세요 — «카페24가 정본» 표식이라 체험계정(`lt*`)을 동기화가 자기 것으로 바꿉니다. ⚠️ **보존 조건이 읽는 칸은 «지연 ALTER» 로 생깁니다** — 없는 DB 에서는 DELETE 가 `no such column` 으로 죽고 그 예외를 `nightlyCafe24Refresh` 가 삼켜 **동기화 전체가 조용히 멈춥니다.** 멱등 ALTER 를 먼저 두세요(trap-check 가 재현해 잡았습니다). ✅ 감시는 `phone_sync_harness` — «가짜 그래프 + 진짜 SQLite» 로 함수를 **두 번 돌려** 보존을 확인하고, «SET 목록 = INSERT 목록 − {user_id, created_at}» 짝도 봅니다(칸을 INSERT 목록에만 더하면 새 행에만 들어가고 기존 29,000행은 영영 옛 값인데 **에러가 안 납니다**) |
| 「카카오톡으로 강사에게 자동으로 보내자」 | 🔴 **필리핀 강사에게는 «구조적으로» 안 됩니다 — 코드가 없어서가 아닙니다.** 카카오톡에는 «카카오ID 로 보내는» 공개 API 가 없고(화면의 💬 `TeacherAna18` 은 사람이 채팅방을 찾는 이름표), 알림톡·친구톡은 «전화번호» 로 보내되 **한국 통신사 번호로 가입한 카카오계정**에만 닿습니다(강사 전화 22건 중 21건이 09xx). 카카오 로그인 «친구에게 메시지» API 도 강사의 카카오 로그인·친구 관계·사업자 검수·일 한도가 걸려 서버 자동 발송용이 못 됩니다. 이 사실은 `src/teacher-kakao.ts` 머리말이 2026-08-13 부터 적어 두고 있었습니다. ✅ **2026-09-14 사장님 결정 — 웹푸시**(정본 `src/teacher-push.ts`: 원부 강사 `teachers.id` → 계정 `teacher_account_links.username` → 구독 `push_subscriptions.user_id`). 강사 화면(`teacher.html`) «자주 쓰는 기능» 바 맨 앞 🔔 「알림 받기」를 켠 강사에게 결석 감지(`absent-sweep.ts`)·노쇼(`/api/notify/no-show`) 알림이 폰에 뜹니다. ⚠️ 푸시는 «켠 사람에게만» 가고 기기가 꺼져 있으면 못 받습니다 — 그래서 **이메일을 «대신» 하지 않고 «더합니다»**(이메일 갈래를 지우지 마세요). ⛔ `sendPushToUser`(`api-notify.ts`)를 `absent-sweep.ts` 에서 import 하지 마세요 — absent-sweep → api-notify → notify-contacts → absent-sweep **순환**입니다(정본은 `web-push.ts` 만 의존). ⛔ 구독의 `user_id` 는 **서버가 확인해 준 계정명**(`me.username`) — 결재함 `work.html` 과 같은 값이라야 매니저가 한 구독으로 둘 다 받습니다. ⛔ 연락처 연결 화면에서 **카카오ID 만 있는 강사를 초록으로 그리지 마세요** — 자동 수단이 아닙니다. ⚠️ 카카오로 «수동» 전달은 이미 있습니다(관리자 › 강사 카카오ID 명부 «원클릭 전달» — 클립보드 + 카카오톡 열기). 유료 대안은 SOLAPI 해외 문자(`teacher-kakao.ts` 에 country=63 경로) — 계정에 «해외 발송» 을 열어야 하고 사람이 정할 일. 감시는 `test-harness/teacher_push_alert_harness.mjs`(74종 — 정본을 실제로 돌리고 변이 11종 전부 실제 FAIL. 🪤 가짜 D1 이 `LIMIT` 을 무시하면 «계정 하나만 읽기» 변이가 통과하고, 정본이 던지면 러너가 죽어 결과줄이 안 나옵니다 — 둘 다 실제로 밟음). ⛔ 노쇼 알림의 결과는 **`{ ...pr, ok: pr.sent > 0 }`** — spread 를 «뒤» 에 두면 정본의 `ok`(=던지지 않았다)가 덮어써 구독이 없어도 `notified_push=1` 이 됩니다(함정 대조가 실제로 잡음 — 하니스는 그 조립식을 오려 내 평가합니다) |
| 학생 전화번호로 문자를 보내려는데 아무에게도 안 감 | 🔴 **`students_erp` 의 전화번호는 학생·학부모 모두 0건입니다**(2026-08-28 D1 실측 29,428행). 2026-08-18 에 「번호를 살렸다」고 수리했지만 그것은 **컬럼과 조회를 추가한 것**이고, **카페24 원본에 값이 없어** 결과는 여전히 0입니다(학년 `grade` 가 0% 인 것과 같은 사정 — 이 표의 「나이·학년으로 자동 분류」 항목). ⚠️ 그래서 미납 안내·결석 알림·독촉이 **아무에게도 닿지 않습니다.** 코드로 풀 수 있는 문제가 아닙니다. ✅ 확인은 `SELECT SUM(phone IS NOT NULL AND TRIM(phone)<>''), SUM(parent_phone IS NOT NULL AND TRIM(parent_phone)<>'') FROM students_erp` 한 줄. ⛔ 「번호가 없으니 0명에게 보냈다」를 **성공으로 보고하지 마세요** — 발송 작업이 조용히 성공으로 끝나 아무도 모릅니다. 📄 적재요청 선례: `docs/카페24_회계데이터_적재요청_2026-08-18.md`. 📌 **덧붙임(2026-09-07) — D1 에 학생 번호가 «조금이라도» 남아 있는 표는 `student_retention` 하나뿐인데, 그것을 «연락처 정본» 으로 쓰면 안 됩니다.** 그 표는 카페24 서버가 밀어 넣는 **«이탈 위험 학생 스냅샷»** 이라(`api-retention.ts` `handleRetentionIngest`) ⓵ **잘 다니는 학생은 원리상 한 명도 없고**(실측 223행 = expired 59 · expiring 61 · inactive 103) ⓶ **이번 스냅샷에 없는 학생은 DELETE 됩니다** — 학생이 이탈위험에서 벗어나면 «어제는 보이던 번호가 오늘은 없습니다». 실측(2026-09-07): 223행 중 199행에 번호 · 예약이 잡힌 학생 444명 중 **105명(23.6%)** · 그날 카페24 수업 128줄 중 **35줄(27%)**. ✅ 그래서 화면에 쓸 때는 **«—» 와 «왜 없는지» 를 함께** 그리세요(빈칸은 «고장» 으로 읽힙니다 — 매니저 「오늘 수업」이 그 방식). ⛔ 없는 번호를 학부모·카카오 칸으로 «그럴듯하게» 잇지 마세요 — **틀린 번호로 학부모에게 전화하는 것**이 빈칸보다 나쁩니다. ℹ️ 참고로 **학원 이름(`students_erp.shop_name`)은 반대로 거의 다 채워져 있습니다**(29,079/29,484 = 98.6%) — 전화번호가 없다고 그 칸까지 «없겠거니» 하지 마세요 |
| `try` 의 짝 `}` 를 찾아 코드를 끼워 넣었는데 **`catch` 블록 «안»** 에 들어감 | 🔴 **글자 단위로 «처음 깊이가 0이 되는 순간» 에 멈추면 그 지점이 `} catch (err) {` 줄입니다.** 그 줄은 한 줄 안에서 닫았다가(`}`) 다시 엽니다(`{`) — 즉 깊이가 **0을 스치고 지나갑니다.** 2026-08-31 실측: 야간 배치 기록을 배선하다 표시 **16개가 전부 catch 본문 첫 줄**에 들어갔습니다. ⚠️ 그냥 «안 남는 것» 이 아니라 **뜻이 뒤집힙니다** — 정상적인 밤에는 catch 로 안 들어가니 **기록이 한 줄도 안 남고**, 남은 기록의 뜻은 「끝났다」가 아니라 **「에러를 던졌다」** 입니다. 소요시간 계산이 «앞 단계 합을 뺀 값» 이면 **처음 실패한 작업 하나가 전체 경과시간을 뒤집어써서** 「제일 오래 걸린 작업」이 «에러 난 작업» 을 가리킵니다(그 숫자로 순서를 바꾸려던 근거가 거짓이 됩니다). ✅ 깊이는 **줄 «끝» 에서** 판정하세요 — 줄 도중의 0 은 무시합니다. ⚠️ **하니스가 못 잡습니다** — 「try 개수 == 표시 개수」로 세면 16==16 이라 초록불입니다(trap-check 가 잡았습니다). 검사도 **위치로** 하세요: catch 블록을 중괄호 짝으로 잘라 그 안에 없는지 봅니다. 🔴 **그 «catch 안인가» 검사도 같은 함정을 밟습니다** — `} catch (err) {` 줄부터 통째로 세면 그 줄에서 다시 0이 되어 **본문을 한 줄도 안 보고 멈춥니다**(표시를 실제로 catch 안에 넣고도 15/15 초록이었습니다). **catch 의 여는 중괄호부터** 세고, 반드시 **가짜 회귀로 진짜 FAIL 이 나는지** 확인하세요. 같은 뿌리: 이 표의 「축이 둘인데 한쪽을 임시 분기 안에 넣어 두었더니」 |
| 화면의 **「끄기」를 눌렀더니 다시 켤 수가 없음**(버튼이 회색) | 🔴 **상태가 «셋» 인데 저장이 «둘» 이면, «끄기» 가 «영구 차단» 으로 흐릅니다.** 카페24 미러가 그랬습니다 — 퇴사 강사를 막으려고 `c24_mirror_teachers.enabled = 0` 을 **«켜지 마라»** 로 정해 뒀는데(2026-09-01), 화면의 「끄기」가 **같은 값**을 썼습니다. 사람이 「끄기」에 기대하는 뜻은 «명단에서 빼기» 인데 시스템은 «전환일에도 만들지 마라» 로 받았고, 막힌 줄은 버튼이 `disabled` 라 **화면에서 되돌릴 방법이 아예 없었습니다**(2026-09-02 사장님이 Teacher Cindy 를 켠 직후 끄기를 눌러 실제로 밟음 — D1 로 복구). ⚠️ **에러가 안 납니다** — 요청은 200 이고 화면도 정상적으로 다시 그려집니다. 틀린 것은 «그 값의 뜻» 하나뿐이라 수리 전에도 하니스가 전부 초록이었습니다. ✅ **«아직 안 함» 은 «행 없음» 으로 두세요** — 컬럼을 늘릴 것 없이 «켜짐(1) · 막힘(0) · 꺼짐(행 없음)» 세 자리가 이미 있었습니다(정본 `clearMirrorTeacher` 는 DELETE, `setMirrorTeacher(…,false)` 는 여전히 «막기»). ✅ 옛 계약(`{enabled:false}`)은 **사람이 그 몸짓에 기대한 뜻**으로 떨어뜨리세요(여기서는 «꺼짐») — 캐시에 남은 옛 화면이 눌러도 놀랄 일이 없어야 합니다. ⚠️ 그때 **«어느 모드 기준으로 덜 위험한가» 를 함께 적으세요** — 이 저장소에서 «꺼짐» 이 안전한 것은 `mode='whitelist'` 에서뿐이고, 전환일(`mode='all'`)에는 `touch()` 가 `mode === 'all' || enabled.has(tid)` 라 **«꺼짐» 도 만들어집니다**(막힘만 안 만듭니다). 즉 「끄기」와 「막힘 풀기」는 **전환일 보호를 실제로 끕니다** — 화면이 그 말을 해야 합니다. ✅ **되돌릴 길을 화면에 두세요** — 막힌 줄에도 「막힘 풀기」를 두되 ⛔ 곧바로 «켜짐» 으로 만들지 말고 «꺼짐» 까지만, 그리고 **이름 입력**으로 오해제를 막습니다. ⚠️ 행을 지우면 **«왜 막혔는지» 메모(`note`·`updated_by`)도 함께 사라집니다** — 확인창이 그것을 말해야 하고, 그 메모를 화면에 먼저 보여 주는 편이 낫습니다(아직 안 함). ✅ 새 동작은 **새 함수로 빼세요**(`clearMirrorTeacher`) — 뜻이 다른 두 동작을 불리언 한 칸에 담으면 이 사고가 그대로 재현됩니다. 감시는 `test-harness/c24_mirror_harness.mjs` P절 16종(진짜 SQLite 로 «켜기 → 끄기 → **다시 켜기가 되는지**» 까지 실제로 돌립니다. 변이 3종 전부 실제 FAIL) 📌 **덧붙임(2026-09-09) — 그 «셋» 중 «기본값» 을 뒤집으면, 멀쩡하던 «실패 처리» 의 뜻까지 함께 뒤집힙니다.** 사무실 모드를 「교사한테 항상 켜지는 것을 디폴트값으로」(사장님 지시) 바꾸면서 저장값을 «'1' 켬 · '0' 사람이 껐음 · 없음 = 켬» 으로 읽게 했는데, **켜기 실패 처리가 예전 그대로 `'0'` 을 적고 있었습니다** — 그러면 마이크를 한 번 못 잡은 «일시 장애» 가 «사람이 껐음» 으로 굳어 **그 교사의 기본 켜짐이 영영 사라지고, 화면은 아무 말도 안 합니다.** 바꾸기 «전» 에는 그 줄이 맞는 코드였습니다(기본이 꺼짐이라 «실패=꺼짐» 이 사실). ✅ 실패는 **«아직 안 정함»(키 삭제)** 으로 되돌리고, «되풀이 시도» 는 저장값이 아니라 **페이지 안 표시**로 막으세요 (`autoFailed` — 계속 실패하는 기기가 수업마다 마이크를 다시 잡지 않게. ⛔ 그때 «사람이 스위치를 누르면 다시 시도» 를 짝으로 남기세요, 없으면 «영영 못 켜는» 반대 사고입니다. 한 번 성공하면 그 표시를 지웁니다). ⛔ 기본 켜짐을 **저장값 `'1'` 을 미리 써 넣어** 구현하지 마세요 — «아직 안 정함» 과 «사람이 켰음» 이 같아져 나중에 기본을 되돌릴 때 이미 켜진 채로 굳습니다. **값을 안 쓰고 «없음» 을 켬으로 읽으세요.** ⚠️ 그리고 **기존 검사의 «옳은 답» 이 뒤집힙니다** — 「실패하면 저장값도 0 이다」가 그대로 남아 있으면 올바른 수리가 오히려 FAIL 합니다(실측). 기본값을 바꿀 때는 **그 값을 읽는 검사를 전부 다시 읽으세요.** ⚠️ 변이시험에서 **역할 게이트를 «한 겹만» 지웠더니 통과**했는데 그건 검사가 헛돈 것이 아니라 방어가 둘(`armOnce` · `enable`)이라는 뜻이었습니다 — «둘 다» 지워야 FAIL 이 납니다. 감시는 `test-harness/manual/vc-office-mode-browser.mjs` ⑰·⑧절(브라우저 — 자동으로 안 돕니다, 사람이 부릅니다. 변이 5종 전부 실제 FAIL) |
| 화상수업에서 «그 사람» 을 **번호(peer id)로 콕 집는** 기능을 만들 때 | 🔴 **그 번호는 접속마다 새로 발급되고, 재접속하면 옛 번호는 그 자리에서 죽습니다 — 그 별·그 신호는 아무 데도 안 가고 «에러도 없이» 사라집니다.** 2026-09-10 실측: 회의방 한 화면이 **2분 15초마다** 새 `peer_id` 로 다시 들어오고 있었습니다(`vc_roster` 의 그 방 peer_id 12개가 전부 다른 값). 선생님 화면의 옛 타일에 대고 ⭐ 를 누르면 학생은 소리도 못 듣고 점수도 안 오릅니다. ⚠️ **화면이 아무 말도 안 하면 «고장» 으로 읽힙니다** — 실패 경로가 여럿일수록(번호 죽음·계정 미등록·세션 없음) «어느 쪽인지조차» 알 수 없고 실패 횟수도 못 셉니다. ✅ 실패하면 **사유를 화면 글자로** 말하게 하세요(정본 `js/idx-vc-mobilefix.js` ⑮ — 학생 확인이 3.5초 안에 안 오면 그때 한 번만 서버에 물어 답합니다. ⛔ 학생이 제때 답하면 요청이 안 늘어납니다. ⛔ 같은 `award_id` 로 물어 두 번 적립되지 않습니다). ⚠️ 「+1P」 만 말하지 말고 **«학생 화면 연출은 못 봤다»** 도 함께 — 안 그러면 선생님이 학생도 봤다고 오해합니다. ✅ 죽은 번호를 이름으로 구제할 때는 **그 방 로스터 안에서 완전일치 + 후보가 정확히 하나** 일 때만(⛔ 부분일치·`LIKE` 금지 — 「남의 이름이 뜸」과 같은 뿌리). 🔴 **그때 «후보가 몇 명인가» 를 «최근 N행» 으로 세지 마세요** — `vc_roster` 는 `PRIMARY KEY(room_id, peer_id)` 라 **재접속 한 번 = 행 한 개**이고 실측상 한 학생이 **31행**(`mangoi-class`·jeong)입니다. `ORDER BY updated_at DESC LIMIT 20` 으로 세면 **오래된 동명이인 행이 잘려 나가** 「후보가 하나」가 참이 되고 **막으려던 그 일(남의 계정에 별이 붙는 것)이 그대로 일어납니다.** ⛔ LIMIT 을 키워서 풀지 마세요(재접속이 잦을수록 또 넘습니다) — **`GROUP BY account_uid` 로 «계정» 을 세세요**(`LIMIT 2` 면 충분합니다: 1이면 유일, 2면 거절). ⚠️ **하니스가 이걸 원리상 못 잡습니다** — 동명이인 fixture 를 2행짜리로 두면 절단이 안 일어나 늘 초록입니다(실제로 그 상태였고 함정 대조가 잡았습니다). **20행을 넘는 시나리오를 짝으로** 두세요. ⚠️ 그리고 계정을 «관리자 세션» 에서 빌려 오지 마세요 — 그 학생 자리가 **선생님 계정으로** 등록되어 그 방 포인트가 관리자에게 갑니다(1P = 1원). 감시는 `test-harness/praise_silent_fail_harness.mjs`(45종 — 절을 오려 내 실제로 돌리고 SQL 은 진짜 SQLite 에. 변이 8종 전부 실제 FAIL) |
| 하니스가 검사 범위를 **«파일 끝까지»** 로 잡아 두었을 때 | 🔴 **그 파일에 새 절이 붙는 순간 남의 코드가 딸려 들어와 «멀쩡한 수리» 가 FAIL 납니다.** 2026-09-10 실측: `class_end_flow_harness` 가 ⑬절을 「`⑬ 📚` 부터 파일 맨 끝 `console.log` 까지」로 잘라 「상주 `setInterval` 이 없다」를 검사했는데, 그 뒤에 ⑮⑯ 절을 더하자 **그 절의 «주석»** 에 적은 「⛔ 상주 setInterval 을 두지 않는다」 한 줄에 걸렸습니다(CLAUDE.md 2장 「부정 검사가 자기 주석을 잡는다」와 겹칩니다). ✅ 끝은 **«다음 절이 시작하는 자리»** 로 자르고, 부정 검사는 **주석을 벗겨 낸 사본**으로 판정하세요. ✅ 그리고 **「그 절을 실제로 잘라 냈다」를 전제 검사 한 줄로** 박아 두세요 — 앵커가 어긋나면 그 아래 검사가 **빈 문자열을 보고 조용히 통과**합니다. ⚠️ 앵커를 고를 때 같은 번호가 두 번 있는지 보세요(그 파일은 ⑬ 이 두 곳 — `⑬ 📖`·`⑬ 📚`). ✅ 범위를 좁힌 뒤에는 **그 절에 진짜 회귀를 넣어 FAIL 이 나는지** 반드시 확인하세요(좁히다 검사가 죽는 것이 흔합니다) |
| 「이 호출이 `try` 안에 있나」를 **문자열로** 검사하려 할 때 | ⛔ **브레이스 세기로는 «객체 리터럴의 `{`» 와 «블록의 `{`» 를 가를 수 없습니다.** 2026-08-31 실측: 멀쩡한 코드 두 줄이 거짓 양성으로 나왔습니다 — 한 줄짜리 `try { await metaSet(…); }`(그 줄 안에서 균형이 맞아 «열린 try» 를 못 찾음)와 `JSON.stringify({` 로 끝나는 줄(객체 여는 중괄호를 블록으로 셈). ✅ 그런 계약은 **«실패하는 것을 넣어 실제로 돌려»** 확인하세요 — 전부 던지는 가짜 DB 를 넘기고 «그 함수들이 던지지 않는가» 를 봅니다. 계약 그 자체를 재는 것이라 헛돌 수 없습니다. ⚠️ 특히 **감시·기록 코드**는 이 계약이 생명입니다 — 감시 장치가 던지면 감시 대상(야간 작업 16개)이 그 자리에서 멈춥니다. 📌 **덧붙임(2026-09-08) — «가장 가까운 try 를 찾아 근처에 catch 가 있나» 로 쓰면 헛돕니다.** 제가 `lastIndexOf('try {', 위치) > 0` + 「그 뒤 160자 안에 catch 가 있나」로 썼는데, 바깥 try/catch 를 **통째로 지워도 49/49 초록**이었습니다(함정 대조 실측). 앞의 조건은 파일 앞쪽 아무 try 나 잡아 **언제나 참**이고, 뒤의 정규식은 그 안에 있는 **다른(안쪽) catch** 의 로그 문자열에 걸렸습니다. 하필 그 검사가 지키기로 한 것이 **fail-open 의 핵심**(「판정이 던져도 본 흐름은 계속된다」)이었습니다. ✅ 물어야 할 것은 «근처에 catch 가 있나» 가 아니라 **«그 호출의 위치가 try 블록의 «범위 안» 인가»** 입니다 — 여는 중괄호부터 **짝이 맞는 닫는 중괄호까지** 잘라 `호출위치 < try시작 + 블록길이` 로 봅니다. ✅ 그리고 그 catch 를 **다시 중괄호 짝으로 잘라 «return 이 없는지»** 보세요(거기서 return 하면 통과시키는 것이 아니라 막는 것입니다). ⚠️ 고친 뒤 **바깥 try 를 실제로 지워 보고 FAIL 이 나는지** 반드시 확인하세요 — 이 결함은 그 변이로만 드러났습니다 |
| 인증 없는 API 가 **요청 본문에 적힌 «누구에게»** 를 그대로 쓴다 | 🔴 **`/api/notify/*` 다섯 경로가 본문의 전화번호로 그대로 문자를 보냈습니다** — 누구나 아무 번호에나 「망고아이」 이름으로 보낼 수 있었습니다(돈이 나가고 사칭이 됩니다). 🔴 더 나쁜 것은 `no-show` 입니다: 본문의 `teacher_name` 이 그대로 `class_no_show` 에 들어가는데, 읽는 쪽(`no-show-truth.ts`)이 그 이름을 출석부와 맞춰 «오판» 인지 가리고 **급여는 `present === true` 일 때만 되돌립니다** ⟹ 남의 수업에 엉뚱한 이름으로 노쇼를 심으면 **들어와 수업한 강사에게 0원**이 나갑니다. ✅ 고침은 게이트가 아니라 **«본문 값을 안 받기»** 입니다(정본 `src/notify-contacts.ts`) — 게이트로 막으면 나중에 그 기능을 살릴 때 학생 화면이 다시 막혀 또 뚫게 됩니다. 번호는 계정(uid)으로, 강사는 **원부 번호**(`teachers.id`)로 D1 에서 찾고 **못 찾으면 빈 값**(본문으로 폴백 금지 — 폴백하면 구멍이 그대로입니다). ⚠️ **이름은 «기록하는 순간» 에 풀어야 합니다** — 나중에 읽을 때 풀면 역사를 덮어씁니다(실측: `class-895` 는 그 뒤 취소되고 강사가 HANNAH → HT FARRAH 로 재배정돼 있습니다). ⚠️ 남은 구멍을 과거형으로 적지 마세요 — 그 경로는 **여전히 무인증**이고 막힌 것은 «번호 위조»·«이름 위조» 둘뿐, 노쇼 사건 자체를 심는 것은 그대로입니다 |
| 회의방 번호를 「양쪽이 같은 번호」로 안내해 놓고 **서로 다른 방**에 들어감 | 🔴 **방 이름을 만드는 규칙이 «두 곳» 이고 서로 달랐습니다.** 회의방 모달(`js/idx-vc-room.js`)은 `normalize()` + `PREFIX='meet-'` 라 「1234」 → **`meet-1234`** 인데, 로비 «⚙️ 방 코드 직접 입력» 은 `idx-main.js:2778` 의 `vcRoomId = vcTypedRoom || 'mangoi-class'` 라 **친 값 그대로** → **`1234`**. 그래서 모달이 화면에 「양쪽이 같은 번호를 넣어야 만납니다」라고 **약속해 놓고** 학생이 그 «같은 번호» 를 넣으면 다른 방으로 갔습니다(2026-09-09 사장님 제보). ⚠️ **에러가 한 줄도 안 납니다** — 양쪽 다 멀쩡한 방이고 각자 「참여자 1명」이라 화면·콘솔·D1 어디에도 고장으로 안 보입니다(네 번 사고를 낸 「같은 방 번호인데 둘 다 참여자 1명」과 증상이 같습니다 — 그쪽은 «워커가 갈린» 것, 이쪽은 «이름 규칙이 갈린» 것). ⚠️ 코드를 **아예 안 넣으면** 공용방으로 가는 것은 **따로**입니다 — 「빈칸 → 오늘 예약 방으로 자동 교정」이 `class_schedules` 를 보는데 **회의방은 예약이 없어** 그 경로를 통째로 비켜 갑니다. ✅ 정본은 **`js/idx-vc-roomcode.js`(defer)의 `resolveRoomCode()`** 하나 — 접두사가 없으면 회의방 번호로 보고 `meetSlug()`(모달 `normalize()` 와 **같은 다듬기**)로 `meet-` 을 붙입니다. ⛔ **`idx-vc-room.js` 에 두지 마세요** — blocking 이라 첫 화면 예산(2026-09-10 실측 여유 **134바이트**)을 그 자리에서 넘깁니다. 🔴 **접두사 목록(`KNOWN_ROOM`)에 `meet-` 을 넣지 마세요** — 그 정규식이 `/i` 라 **`MEET-1234`·`Meet-1234` 가 «접두사 있음» 으로 빠져나가 원문 그대로** 돌아가고, 방은 `idFromName` 이라 **대소문자를 구분**하므로 고치려던 사고가 대문자로 그대로 재현됩니다(2026-09-09 에 실제로 그렇게 짰다가 함정 대조가 잡았습니다 — PR #901 → #903). ⚠️ 하필 그 입력칸(`index.html:8135`)에는 **`autocapitalize` 가 없어** 폰 키보드가 첫 글자를 대문자로 만듭니다 — 계정 아이디에서 이미 밟은 함정입니다. `index.html` 은 금지구역이라 defer 에서 **밖에서 입힙니다**(`armCaseGuards()`). ⛔ **다른 접두사(`class-`·`demo-`·`room-`·`c24-`·`mangoi-class`)는 한 글자도 건드리지 마세요** — 예약 수업방·연습방·관리자 임베드가 전부 그쪽이고, 소문자로 내리면 사람이 친 `Class-849-…` 가 `meet-class-849-…` 가 되어 오히려 나빠집니다. ⛔ **빈칸은 빈칸 그대로** 두세요(공용방 폴백은 예전 동작). ⛔ 「교사가 지금 있는 회의방으로 학생을 보내기」로 풀지 마세요 — 어느 회의방인지 알 방법이 없어 남의 회의에 들어갑니다. ⚠️ **맞바꿈**: 이제 «번호만 친 방» 이 `__vcIsMeetingRoom()` 에 걸려 그 방의 **출석 포인트·복습퀴즈 안내·강사 AI 코칭이 꺼집니다.** `idx-main.js:2665` 주석이 「반/수업별로 나누려면 같은 코드를 입력」이라고 **문서화**하고 있어 그런 방이 실재하면 조용히 멈춥니다 — 반경은 아직 **못 쟀습니다**(작업기록 5-2 에 조회 SQL). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무슨 답이 나오는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 감시는 `test-harness/meet_room_code_harness.mjs`(72종 — 두 파일의 규칙을 **오려 내 실제로 돌려** 답을 대조. 변이 11종 전부 실제 FAIL)와 `manual/meet-room-code-browser.mjs`(브라우저 17종 — 자동으로 안 돕니다). 🪤 **그 하니스가 «버그를 정답으로» 못 박은 적이 있습니다** — `MEET-1234 → MEET-1234` 를 기대값으로 적어 두어 올바른 수리가 오히려 FAIL 났습니다. ⛔ 그 목록에 대문자 회의방을 다시 넣지 마세요. 📄 `docs/작업기록/260909_회의방_같은번호인데_다른방.md` |
| 학생에게 **「방 번호를 넣으세요」** 라고 안내해 놓고, 정작 넣을 창이 안 열림 | 🔴 **로비는 «0.22초» 만 열립니다.** `index.html` 의 원클릭 입장 블록 주석이 그 이유를 이미 적어 두었습니다 — 로비가 뜨고 **+50ms** 에 `vcRestoreCredentials` 가 **방 코드 칸을 강제로 비우고**(옛 코드 복원 차단 목적) **+220ms** 에 **자동 입장**합니다. 그리고 ⚙️「방 코드 직접 입력」은 **접혀 있습니다.** ⟹ 사람이 톱니를 펼쳐 번호를 칠 시간이 **구조적으로 없습니다.** ⚠️ **자동로그인이 꺼진 학생에게만** 그 안내가 실제로 쓸 수 있는 말입니다 — 2026-09-09 에 넣은 로비 노란 줄이 그 상태였습니다. ⚠️ 「로그인 두 번」과 헷갈리지 마세요 — 홈에 로그인돼 있으면 로비 아이디·비번은 **코드가 이미 채웁니다**(`ph59` · `armAutoEnter`). 막힌 것은 «로그인» 이 아니라 **«번호를 넣을 자리»** 입니다. ✅ **2026-09-10 에 서버 쪽으로 풀었습니다**(단 아래 🔴 를 함께 읽으세요) — 선생님이 「오늘은 이 방」을 지정하면 `/api/class/sessions/today` 가 예약방 대신 그 회의방을 돌려주고, 로비가 이미 하던 「빈칸 → 오늘 예약 방 자동교정」이 **그 답을 그대로 씁니다**(학생 화면 코드 0줄). 정본 `src/class-room-override.ts`. 🔴 **방 번호를 «스스로 만드는 곳» 이 여럿이라 한 곳만 고치면 강사와 학생이 갈립니다** — 처음 판이 그랬고(학생만 갈아 끼움), `api-teacher.ts` 가 **자기 주석으로** 「`sessions/today` 와 반드시 같은 식」이라고 못 박아 둔 불변식을 깼습니다. 지금은 **`api-mango.ts`·`api-teacher.ts`·`absent-sweep.ts` 세 곳**이 지정을 봅니다 — ⛔ `absent-sweep.ts` 를 빼면 학생이 지정된 방에 멀쩡히 있는데 「결석 위험」이 찍히고 그 `class_no_show` 를 `no-show-truth.ts` 가 읽어 **수업료가 0원**이 됩니다. ⚠️ **`classes-now.ts`(참관 버튼)·`lesson-reminder.ts`(리마인더 링크)는 아직 옛 방을 가리킵니다**(사람 결정 대기 — 하니스 ⑦절이 이름을 찍어 출력). 🪤 넘기는 행이 **`schedule_id` 를 담는지** 보세요 — `absent-sweep.ts` 는 `id` 만 담고 있어 헬퍼가 «에러 없이 늘 헛돌» 뻔했습니다. ⛔ 자동 입장 타이밍(`+220ms`)을 늦춰서 풀지 마세요 — `index.html` 은 공동 금지구역이고, 늦추면 **모든 학생의 입장이 그만큼 느려집니다.** ⛔ 학생 안내를 blocking 파일에 넣지 마세요(여유 134B) — `js/idx-vc-roomcode.js`(defer)가 그 자리입니다. ⚠️ 그 안내는 **«사람이 안 쳤을 때만»** 띄우세요 — 스스로 번호를 친 학생에게 「선생님이 정했다」는 거짓말입니다. ⚠️ 회의방(`meet-`)은 예약방이 아니라 **포인트·복습퀴즈·AI 코칭이 꺼집니다** — 감추면 「수업은 했는데 포인트가 0」이 되고 아무도 이유를 모릅니다. 감시는 `test-harness/class_room_override_harness.mjs`(62종 — 화면·서버의 방 이름 규칙을 **둘 다 오려 내 실제로 돌려 대조**. 변이 **7종** 전부 실제 FAIL — 담당 확인·지사 차단을 «글자만» 보던 검사가 무력화 변이를 그대로 통과시키던 것을 포함) |
| 「그 기능은 한 번도 안 돌았다」를 **로그 표가 비었다**는 근거로 말하려 할 때 | ⛔ **그 표에 «기록되는 조건» 부터 확인하세요.** `alimtalk_log` 는 `sendKakaoAlimtalk` 에 `logContext` 가 넘어올 때만 씁니다(`solapi-client.ts`). 그런데 `sendLessonStartAlert`·`sendLessonEndAlert`·`sendChatSummaryAlert`·`sendMentionAlert` 는 **그것을 안 넘깁니다** — 즉 그 넷은 아무리 보내도 그 표에 한 줄도 안 남습니다. 표가 비었다고 «안 보냈다» 로 읽으면 정반대 결론이 납니다(2026-09-01 실제로 그럴 뻔했고 trap-check 가 잡았습니다). ✅ 이럴 때 쓸 수 있는 근거는 **호출자 코드**(그 화면이 애초에 번호를 가진 적이 있나 — `git log -S` 로 이력까지)와 **부작용 칸의 직접 측정**(`class_no_show.notified_kakao` 가 38건 전부 0)입니다. ✅ 그리고 「잰 것」과 「거기서 내린 판단」을 **줄을 나눠** 적으세요 |
| 가짜 DB 로 하니스를 돌렸는데 **검사가 헛돌며 통과** | 🔴 **정본이 `.bind()` 없이 부르는 질의가 섞여 있으면, bind 아래에만 응답을 둔 가짜 DB 는 그 줄에서 예외를 냅니다.** 그러면 그 함수가 통째로 `lookup_failed` 로 빠져 **늘 빈 값**을 돌려주는데, 하필 「애매하면 안 보낸다」·「부분일치로 안 붙인다」 같은 검사는 **빈 값이 정답**이라 초록불이 됩니다(2026-09-01 실측 — 옆의 «찾는다» 검사 하나가 FAIL 나서야 알았습니다). ✅ 가짜 DB 는 **`prepare()` 와 `prepare().bind()` 두 층 모두에 `first`/`all`** 을 두세요. ✅ 그리고 «못 찾는다» 검사만 넣지 말고 **«제대로 찾는다» 검사를 반드시 짝으로** 넣으세요 — 그것이 헛돎을 드러내는 유일한 신호입니다 |
| 규칙표의 `daily_cap` 을 **«점수 예산»** 으로 읽음 | 🔴 **그건 «횟수» 입니다.** 그래서 금액을 올리면 상한이 조용히 같이 올라갑니다 — 칭찬 포인트가 1점 → 5점이 되면서(`POINT_POLICY.EARN.praise`, `syncApprovedRuleAmounts` 는 **금액만** 맞춥니다) 횟수 100 은 그대로여서 이 규칙 하나로 **하루 500점**이 됐습니다. **1포인트 = 1원**입니다(`gift_catalog` 실측: 3,000원 상품권 = 3,000점). ✅ «점수» 상한은 정본 `checkEarnAllowed`(`src/point-policy.ts`) 하나뿐이고 `SUM(amount)` 로 셉니다 — 새 적립 경로는 **반드시 그것을 지나야** 합니다(2026-09-01 이전에는 5경로 중 1곳만 지났습니다). ⚠️ 그 상한은 `point_transactions` 를 세는데, **원장을 안 거치고 `student_points.balance` 를 직접 올리는 적립이 따로 있습니다**(`api-games.ts` 단어장 하루 400점·복습퀴즈 500점, `api-admin.ts` 기프트) — 그래서 「하루 100점」은 아직 **전체가 아닙니다.** 「하루 최대 적립 80점」 같은 숫자를 인용할 때 **«`point_transactions` 기준»** 을 함께 적으세요 |
| 「다시 오지 않는」 보상에 총량 상한을 걸 때 | ⛔ **막는 게 아니라 «빼앗는» 것이 됩니다.** 7일 연속 영작 보너스는 `wStreak % 7 === 0` 인 날에만 발화하는데, 그날 상한에 걸려 막히면 기록이 안 남고 다음 날은 `wStreak=8` 이라 조건이 거짓 — **그 마디는 영영 돌아오지 않습니다.** 50점은 상한의 절반이라 칭찬을 스무 번 받은 날이면 바로 걸립니다. ✅ 「하루에 많아야 한 번 + 놓치면 다시 안 옴」 **둘 다** 만족하는 보상만 상한에서 빼세요(`CAP_EXEMPT_RULES`). ⛔ 반복되는 적립을 그 목록에 넣으면 그 순간 상한이 뚫립니다. ⚠️ 같은 줄기로 **환불·관리자 지급을 «오늘 벌었다» 로 세지 마세요** — `applyPointTransaction` 은 `refund`·`admin_grant` 도 양수로 적으므로, 3,000P 기프티콘 환불 한 건이 그날 적립을 통째로 막습니다(`type = 'earn'` 만 셀 것) |
| 상한·검증을 새로 걸 때 **화면이 그 실패를 뭐라고 말하는지** | 🔴 **막는 코드만 넣고 끝내면 「한도」가 「고장」으로 읽힙니다.** 2026-09-01 실측 3종: ① 새 오류코드 `daily_total_cap_reached` 를 돌려줬는데 강사 화면은 `daily_cap_reached` 만 알아 **「전달 실패 :(」** 로 그렸습니다(강사는 계속 다시 누릅니다) → 화면이 아는 코드로 돌려주고 사유는 `detail` 로 따로 싣습니다(첫 화면 예산이 빠듯한 blocking 파일을 안 건드리는 이득도 있습니다). ② 「막히면 조용히 return」으로 짰더니 부르는 쪽이 **그 뒤에서 무조건** 화면 값을 써서 **적립 0인데 학생 화면이 «+5P» 축하**를 띄웠습니다 — 고치기 전에는 예외가 catch 로 빠져 오히려 «정직» 했는데, 제가 «실패» 를 «거짓 성공» 으로 바꿔 놓은 것입니다. **«줬는지» 를 반드시 돌려주고 부르는 쪽이 그걸 보고 화면 값을 정하게** 하세요. ③ **멱등 claim 은 상한 검사 «뒤» 에 두면 안 됩니다** — 막힌 요청이 claim 을 먹으면 짝이 되는 다른 경로가 `{ok:true, already:true}` 를 받아 화면에만 값이 생깁니다. ⚠️ 셋 다 «상한이 사실상 안 닿던» 동안은 티가 안 났습니다 — **상한을 조이는 변경은 그 실패 경로를 처음으로 «닿게» 만듭니다** |
| 하니스에서 「이 호출 앞에 게이트가 있나」를 볼 때 | ⛔ **«앞 N줄» 로 자르지 마세요** — 2026-09-01 실측: 60줄로 두면 기존 게이트에서 20줄 뒤에 **게이트 없는 새 적립**을 끼워 넣어도 조용히 통과합니다(그 하니스가 존재하는 이유가 바로 그것인데). ✅ **중괄호 짝으로 «감싸는 블록»** 을 거슬러 올라가세요. ⚠️ 그때 **가장 안쪽 블록만 보면 반대로 오판합니다** — 게이트는 함수 몸통에 있고 적립은 그 안의 `try {` 한 겹 안쪽이라, 한 겹만 올라가면 멀쩡한 코드가 «게이트 없음» 이 됩니다(실측 2곳). **함수·라우트 경계를 만날 때까지** 올라가세요. ✅ 그리고 «불렀는가» 만 보지 말고 **«그 결과를 조건으로 쓰는가»** 도 보세요 — 부르기만 하면 아무것도 안 막습니다. 📌 **덧붙임(2026-09-04) — 「거른 «결과» 를 쓰는가」는 «위치» 로도 못 잡습니다.** 결재 지출 리포트가 「⛔ canView 를 못 걸면 인사·급여가 합계에 섞인다」고 **주석까지 적어 놓고** 바로 그 줄에서 **거르기 «전» 행(`page`)** 을 넘겼습니다(거른 결과는 `items` 이고, 바로 위 엑셀만 맞게 쓰고 있었습니다). 실측: 경영진이 아닌 본사 계정이 `scope=all` 로 열면 **인사·급여 750,000 이 승인 합계에, 900,000 이 대기 합계에** 섞였고 **에러는 안 났습니다.** 🪤 그런데 제 하니스가 «게이트가 앞에 있는가» 를 `indexOf` 로 봤는데 **그 문자열이 파일에 네 곳**이라 첫 번째가 늘 앞이어서 통과했고, 더 나쁘게 **버그를 글자 그대로 못 박아**(`indexOf('summarizeApprovals(page')`) **올바르게 고치면 오히려 FAIL** 이 났습니다. ✅ 물어야 할 것은 「게이트가 앞에 있는가」가 아니라 **「게이트가 채운 «그 변수» 를 쓰는가」** 입니다 — 소스에서 `if (!canView(…) …  <변수>.push(` 의 **변수 이름을 읽어** 인자와 대조하세요. ✅ 그리고 **같은 데이터를 쓰는 형제 경로와 대조**하면 더 셉니다(여기서는 「엑셀과 리포트가 같은 행을 보는가」 — 한쪽만 틀리면 바로 드러납니다). ⚠️ 라우트를 자를 때 앵커를 `path === '…/xxx'` 로 잡으면 **POST 라우트가 먼저** 걸립니다 — 메서드까지 넣고 **중괄호 짝**으로 자르세요 📌 **덧붙임(2026-09-16) — 검사가 그 값을 «인자로 주입» 하면, 그 값을 «만드는» 줄을 한 번도 안 봅니다.** 학생 상세 연락처 토스트에서 실측: 「언제 무슨 말을 하는가」를 제대로 돌려 보려고 `_cPhoneCleared` 를 `new Function` 인자로 넘겼더니, 그 값을 정하는 **`var _cPhoneCleared = (body.student_phone === '' || …);` 한 줄을 `= true;` 로 바꿔도 PASS 77 / FAIL 0** 이었습니다(네 경우를 «실제로 돌리는» 검사였는데도). ✅ **«만드는 줄» 을 따로 오려 내 돌리고**, 「지우기일 때 참」 옆에 **「안 고친 저장은 거짓」·「번호를 적은 저장도 거짓」을 짝으로** 두세요(짝이 없으면 «언제나 참» 도 통과합니다 — 실제로 그 상태였습니다). 🔴 같은 뿌리로 **배선도 함께** 보세요 — 판정 함수(`phoneOut`)를 그대로 둔 채 **호출부만 옛 모양(`value || null`)으로 되돌리면** 그 함수를 돌리는 검사가 «전부» 통과합니다(변이 Ⓒ 실측). 「그 함수를 실제로 부르는가」 + 「옛 모양으로 다시 조립하지 않는가」 + 「그 판정이 기대는 상태(여기서는 «로드 때 값»)를 실제로 채우는가」 셋이 짝입니다 |
| «되돌릴 수 없는 조작»(삭제·파기)의 가드에 필요한 값을 **`safe(…, null)` 로 조회**할 때 | 🔴 **조회가 실패하면 «없다» 로 떨어져 가드가 통째로 fail-open 이 됩니다 — 에러는 안 나고 조작이 그대로 진행됩니다.** 2026-09-06 결재 삭제에서 실제로 그렇게 짰습니다: 「이어받아 다시 올린 결재가 있으면 원본을 못 지운다」(안 막으면 «회수 → 다시 올리기 → 원본 삭제» 가 「N일째」를 오늘로 초기화하는 **지연 지우기 우회로**)를 `const child = await safe(… origin_id = ? …, null)` → `hasResubmitChild: !!child?.id` 로 넘겼는데, D1 이 한 번 흔들리면 `null → false` 라 **그 가드가 막으려던 바로 그 일이 일어납니다.** 덤으로 입력 타입이 `hasResubmitChild?: boolean`(optional)이라 **안 넘기면 안전장치가 꺼진 것이 기본값**이었습니다(다른 호출자가 생기면 조용히 뚫립니다). 함정 대조가 잡았습니다 — 문자열 하니스는 «조회하는가»·«넘기는가» 만 봐서 전부 초록이었습니다. ✅ 정본 판정은 **«모른다»(null·undefined) 를 별도 사유(`lookup_failed`)로 거절**하고 서버는 `try/catch` 로 «모른다» 를 그대로 넘기세요 — 되돌릴 수 없는 조작은 **막는 쪽으로 실패**합니다(회수 경로의 `anyDecided` 도 같은 모양이라 함께 고쳤습니다 — 도장 찍힌 건이 회수되고 그 뒤 삭제까지 이어지면 결재자 판단 기록이 통째로 사라집니다). ✅ **SELECT 시점 판정만 믿지 마세요(TOCTOU)** — 조건부 DELETE 의 WHERE 에 `AND NOT EXISTS (…)` 로 DB 가 한 번 더 보게 하고, 0행이면 재조회해 «자식이 생겼다» 와 «상태가 바뀌었다» 를 갈라 말합니다. ✅ 하니스에는 **«모르면 막는가»(null)·«안 넘기면 막는가»(undefined)** 를 «있으면 막는가» 옆에 짝으로 두세요. ⚠️ 같은 뿌리의 형제 — **다른 행이 «id 로 가리켜 계산하는 값»** 을 지울 때(여기서는 자식의 `origin_created_at` 서브쿼리)는 지우는 쪽이 그 참조를 먼저 세어야 합니다. 지워도 에러가 안 나고 그 칸만 조용히 NULL 이 됩니다. 📄 `docs/작업기록/260906_결재_회수건_삭제.md` §4-2·§4-3 |
| 「동의 안 함」이 **「거절했다」인지 「안 물어봤다」인지** 구분이 안 됨 | 🔴 **`consents` 의 `voice_analysis_consent`·`reward_consent`·`kakao_consent` 는 구조적으로 늘 0 이었습니다** — 동의 화면(`js/mango-consent.js`)이 녹화·출석만 묻고 그 셋은 **키 자체를 안 보내는데** 서버가 `b.kakao ? 1 : 0` 으로 적었기 때문입니다(2026-09-01 실측: consents 11행에서 녹화 7·출석 7·나머지 셋 0). 그 0 을 읽는 쪽들이 「거절」로 읽었습니다: `admin/student.html` 이 **빨간 «미동의» 배지**를 띄워 직원에게 「학부모가 거절했다」로 말했고, `retention.ts`(매일 밤 크론)가 그 값으로 `kakao_ids` 를 **파기**합니다 — 카카오를 연결한 학생이 수업에 한 번 들어가 동의를 남기는 순간(입장 때 자동으로 남습니다) 그날 밤 연결이 지워집니다. ✅ 「안 물어봄」은 **NULL**, 「거절」은 0 으로 갈라 적으세요(`b.X == null ? null : (b.X ? 1 : 0)` — `=== undefined` 는 명시적 null 을 놓칩니다). ✅ **이미 쌓인 행은 NULL 수리로 못 지킵니다** — `consents.raw_payload` 에 그때 보낸 본문이 그대로 남아 있으니 `raw_payload LIKE '%"kakao"%'` 로 «그때 실제로 물었는가» 를 가르세요(D1 을 안 건드리고 됩니다. raw_payload 가 NULL 이면 LIKE 도 NULL 이라 안 지워집니다 — 모르면 안 지우는 안전한 방향). ⚠️ 화면이 그 항목을 묻기 시작해도 **이미 동의 행이 있는 사람에게는 다시 안 묻습니다** — `askedBefore()` 가 행이 있으면 즉시 반환하고 `consent_version` 을 비교하지 않습니다. ⛔ 없는 값을 0 으로 채우지 마세요(2장 「측정할 수 없는 값을 그럴듯하게 채우고 싶을 때」의 형제입니다 — 이쪽은 «모름» 을 «아니오» 로 채운 것입니다) |
| 「지금까지 그런 적 없다」를 **현재 상태**를 근거로 말하려 할 때 | ⛔ **지워지는 것은 흔적이 안 남습니다 — 현재 상태로는 원리상 증명할 수 없습니다.** 2026-09-01 실제로 밟았습니다: 야간 파기 크론이 카카오 연결을 지우는 문제를 보면서 「지금 교집합이 0이니 **지워진 행은 없다**」고 적었는데, 그 크론은 이미 여러 밤을 돌았고 지워졌다면 그 흔적이 남지 않습니다(trap-check 가 잡았습니다). ✅ 쓸 수 있는 문장은 **「YYYY-MM-DD 현재 두 집합의 교집합이 0이다」** 까지입니다. 「지금까지」로 넓히지 마세요. ✅ 과거를 알고 싶으면 **로그·상태키**를 보세요(이 저장소는 `GET /api/retention/status` 의 `retention:last_run` — 다만 **마지막 1회분**뿐입니다). ⚠️ 같은 단정을 세 곳(소스 주석 2곳·하니스 머리말)에 적어 뒀어서 한 곳만 고쳐서는 안 끝났습니다 |
| `meta` JSON 에 적은 값을 **다른 키 이름으로** 읽고 있음 | 🔴 **에러가 안 나고 «0건» 이 정상값처럼 보여서 아무도 모릅니다.** 칭찬(⭐)이 그랬습니다 — 쓰기는 `JSON.stringify({ room: … })` 인데 읽기 두 곳이 전부 `meta LIKE '%"room_id":"…"%'` 라, **모든 방에서 늘 0**이었습니다(2026-09-01 실측: 76건 전부 `room`, `room_id` 0건). ⚠️ **통계가 비는 정도가 아닙니다** — 그 값이 0이면 강사 코칭 피드백이 「칭찬이 거의 없었는데…」로 **강사가 실제로 한 일을 부정**합니다. 실측: `teacher_class_feedback` **32행 전부**에 그 문구가 있고, 그중 **2건이 부당한 지적**(칭찬 73회·1회). ✅ 세는 자리를 **정본 하나**로 모으세요(`praiseCountForRoom`, `src/point-policy.ts`) — 같은 쿼리가 두 파일에 복사돼 있었기 때문에 **둘이 함께** 틀렸습니다. ✅ 두 키를 모두 받으세요(`COALESCE(json_extract(meta,'$.room'), json_extract(meta,'$.room_id'))`) — 그래야 이미 쌓인 것이 오늘 보이고 나중에 다른 키로 적어도 안 죽습니다. ✅ `LIKE` 대신 **`json_extract`** 를 쓰세요(방 번호가 접두사인 다른 방을 끌어옵니다). `json_valid(meta)` 로 먼저 거르세요 — meta 가 JSON 이 아닌 행이 섞이면 던집니다. ⚠️ 이미 쌓인 행은 **소급되지 않습니다**(`ON CONFLICT DO UPDATE`) |
| 「그 문구가 나간 적 없다」를 확인하려고 **표를 세었는데 심각도가 뒤집힘** | 🔴 **그 문구가 «어느 표에» 저장되는지부터 코드로 확인하세요.** 2026-09-01 실제로 밟았습니다: 「칭찬이 거의 없었다」가 나갔는지 보려고 `feedback_drafts`(22행)·`teacher_feedbacks`(2행)를 세고 **「0건 — 지금 터진 사고가 아니다」** 라고 주석·하니스 머리말에 적었습니다. 그런데 그 문구는 **다른 경로**에 있고 `teacher_class_feedback` 에 저장되며 **32행 전부**에 적혀 있었습니다(trap-check 가 잡음). ⚠️ 같은 기능처럼 보이는 갈래가 둘이면 **문구가 있는 쪽과 없는 쪽이 갈립니다** — 여기서는 `api-admin.ts` 갈래가 칭찬 0일 때 **아무 말도 안 하고** 넘어갑니다. ⛔ 「지금 터진 사고가 아니다」처럼 **심각도를 낮추는 결론**은 특히 조심하세요 — 틀리면 다음 사람이 그 줄을 믿고 다시 안 봅니다. ✅ 「그 문구를 만드는 코드」에서 «어디에 INSERT 하는가» 를 따라가 그 표를 세세요 |
| 하니스에서 객체 리터럴의 «키» 를 정규식으로 뽑을 때 | ⛔ **`JSON.stringify\(\{([^}]*)\}\)` 로 자르면 중첩 객체에서 끊깁니다** — 안쪽 `}` 에서 멈춘 뒤 정규식이 **다음 매치로 미끄러져** 엉뚱한 코드(옆 규칙의 쓰기)를 검사합니다. 2026-09-01 변이시험 실측: 그 상태로 **거짓 통과**가 났습니다(진짜 검사 대상을 한 번도 안 봄). 그리고 앵커를 넓게 잡으면(`point_rule_log` 같은 흔한 이름) 근처에 무관한 코드가 생길 때 **거짓 FAIL** 도 납니다. ✅ 앵커는 **그 자리에만 있는 문자열**(규칙 코드 등)로 잡고, 범위는 **중괄호 짝**으로 자르며, 키는 **깊이 0인 최상위만** 세세요. ✅ 그리고 «올바른 코드에 중첩 객체를 넣어도 통과하는가» 를 함께 시험하세요 — 안 그러면 이번엔 반대로 무해한 리팩터에 빨간불이 납니다 |
| 자격증명(토큰)을 **HTML 응답 안에** 실을 때 | 🔴 **그 순간 그 페이지의 모든 «값 꽂는 자리» 가 토큰 탈취 통로가 됩니다.** 2026-09-01 실측: OAuth 콜백에 `mango_token` 을 실으면서 `JSON.stringify(...)` 쪽 한 곳만 막았는데, **그 위 `<p>` 의 이름은 그대로**였습니다. 이름은 프로바이더가 준 값이고, 그 `<p>` 는 `<script>` «위» 라 파싱 중 먼저 돌아 같은 오리진에서 `localStorage.mango_token` 을 읽어 갑니다. **sink 가 둘이면 한 곳만 막는 것은 «안 막은 것»** 입니다(trap-check 가 잡음). ✅ HTML 본문은 `&`·`<`·`>`·`"`·`'` 전부 이스케이프, `<script>` 안 JSON 은 **`<` 를 `\u003c` 로**(JSON 값은 그대로 살아납니다). ⚠️ **백슬래시 한 겹이면 무의미합니다** — 소스의 `'\u003c'` 는 JS 가 `<` 로 해석해 «자기를 자기로 치환» 합니다. 제가 실제로 한 번 밟았고, 문자열 검사였으면 못 봤습니다(식을 **실제로 평가**하는 검사라 잡혔습니다). ✅ **`Cache-Control: private, no-store`** 를 그 응답에 붙이세요 — 지금 CF 기본값은 `/api/` 를 캐시하지 않지만 누가 「Cache Everything」을 걸면 **한 사람 토큰이 다른 사람에게** 나갑니다. ✅ 에러 페이지도 같은 전제를 따르세요(프로바이더 응답 본문이 그대로 들어갑니다) |
| 「이 페이지는 안전하다」를 **한 자리만 검사하고** 결론 낼 때 | ⛔ **하니스 절 제목이 「탈출되는가」인데 실제로는 payload 하나만 봤습니다** — 읽는 사람은 «이 페이지 전체가 안전하다» 로 읽습니다. ✅ «못 믿는 값의 목록» 을 만들어 **그 값이 템플릿에 꽂히는 자리를 전부 세세요**(이미 막힌 자리만 예외로 빼고). ⚠️ 예외로 빼는 값(예: 라우트 정규식이 `kakao|naver|google` 로 못 박는 `provider`)은 **그 근거 자체를 검사로 함께** 남기세요 — 그 정규식이 느슨해지면 FAIL 나야 합니다. 🪤 같은 줄기로 **「파일 어딘가에 그 헤더가 있나」류 검사도 무릅니다**: 캐시 검사가 그랬는데 **에러 경로에 붙은 헤더가 대신 걸려**, 정작 토큰이 실린 성공 응답에서 빼도 초록불이었습니다(실측). **그 응답 하나를 콕 집어** 보세요 |
| 잔액을 직접 올리는 적립을 **원장 정본으로 옮길 때** | ✅ 옮기는 것 자체는 옳습니다 — `student_points.balance` 만 올리면 그 포인트가 **학생 본인 내역·학부모 대시보드·관리자 월간 합계 어디에도 안 보입니다**(2026-09-01 실측: 원장 밖 적립 2,746점). 🔴 **그런데 정본 `applyPointTransaction` 은 `student_name = COALESCE(?, student_name)` 이라, 빈 값 대신 uid 를 넘기면 «기존에 들어 있던 진짜 이름» 이 아이디로 바뀝니다.** 그 칸은 무인증 공개 리더보드가 그대로 내보내고, 이 저장소는 그 자리에 「이름이 없을 때 아이디로 폴백하지 말 것」을 이미 못 박아 뒀습니다(비밀번호가 설정된 학생이 0명이라 **아이디를 아는 것이 곧 로그인**). 옛 직접 UPSERT 는 `ON CONFLICT` 목록에 그 칸이 없어 안 건드렸으므로, **이건 «옮기는 변경» 이 새로 만드는 위험**입니다 → 진짜 이름이 없으면 **`undefined`** 를 넘기세요. 🔴 그리고 **정본은 던질 수 있습니다**(잔액 조건 `balance + ? >= 0` 이 0행이면 throw — `balance` 가 NULL 인 행이면 적립인데도 걸립니다. 동시 첫 INSERT 도 PK 충돌). 옛 UPSERT 는 안 던졌으니, 감싸지 않으면 **보상 하나 때문에 요청이 500** 이 됩니다. ⚠️ 원장 INSERT 단계에서 실패하면 **잔액은 이미 올라간 뒤**라 고치려던 상태가 그대로 재생산됩니다 — 조용히 넘기지 말고 크게 로그를 남기세요 |
| 상한을 «계산하는 쪽» 에 새 적립을 흘려보낼 때 | ⛔ **기록을 남기기 시작하면 그 값이 상한 계산에 들어가 보상이 조용히 깎입니다.** 2026-09-01 실측: 단어장·복습퀴즈 보상을 원장에 남기자 총량 상한(하루 100점)과 게임 묶음 상한(30점)이 그것까지 세게 되어, 학생 보상이 **최대 10분의 1**로 줄어들 뻔했습니다(단어장 400 + 미션 50 + 복습퀴즈 500 + 총량 100 = **실효 1,050점**). ✅ **«기록» 과 «상한» 을 갈라 두세요** — `CAP_UNCOUNTED_RULES`(`src/point-policy.ts`)에 넣으면 원장에는 남고 상한 계산에서는 빠집니다. 그 목록을 비우는 것이 곧 정책 통일이고, **그건 사장님이 정할 일**입니다. ⚠️ `earnedToday` 만 고치면 반쪽입니다 — `GAME_QUIZ_RULES` 에 그 이름이 이미 들어 있어 `earnedTodayForGames` 도 함께 빼야 합니다. ⚠️ 미션 보상은 `kind !== 'mission'` 조건 때문에 **400점 상한 «밖»** 입니다 — 상한을 셀 때 빠뜨리기 쉽습니다 |
| AI 가 «많이 뽑았다» 를 «잘했다» 로 셀 때 | 🔴 **비전·생성 모델은 못 하면 조용히 실패하지 않고 «같은 것을 계속 토합니다».** 2026-09-02 교재 OCR 시험 실측: LLaVA 가 한 장에서 **84줄·647낱말을 64초** 동안 뱉었는데 내용은 몇 줄의 반복이었습니다. 화면이 「제일 많이 뽑은 엔진」을 평균에 써서 **낱말 평균이 179 로 부풀려졌고**, 그 숫자가 하필 그 시험의 결론 지표(「이 교재는 글자가 많은가」)였습니다. ✅ 평균은 **되풀이를 걷어낸 값**으로 세고(`unique_words`), 반복 비율을 **화면이 말하게** 하세요. ⛔ 「반복이 있으면 실패」로 판정하지는 마세요 — 교재에는 일부러 반복되는 줄이 실제로 있습니다(노래 후렴). 사람이 원문과 나란히 보고 판단하는 것이 맞습니다. ⚠️ 그리고 그 검사를 «파일에 그 글자가 있는가» 로 쓰지 마세요 — 주석에 걸려 헛돕니다(변이시험에서 실제로 그 상태로 통과했습니다). **평균을 정하는 그 줄**을 보세요 |
| **비용이 나가는 API**(AI 호출 등)를 만들 때 | 🔴 **상한을 «부르는 쪽» 에 두면 본문 한 줄로 깨집니다 — 정본이 들어야 합니다.** 2026-09-02 교재 OCR 시험에서 실제로 밟았습니다: 라우트 주석에 「한 요청 = 한 장 = 엔진 수만큼」이라고 비용을 약속해 놓고, 정본이 요청 본문의 `engines` 배열을 그대로 받았습니다. «모르는 id» 는 걸렀지만 **같은 id 를 여러 번 넣는 것은 안 걸러서** 같은 id 500개 → **모델 호출 500회**였습니다(실측). 화면의 「장 수 ≤ 20」 클램프는 **클라이언트 전용이라 근거가 못 됩니다.** ✅ 중복을 없애고(`[...new Set(...)]`) **목록 길이로 자르세요** — 그보다 많을 수가 없습니다. 🔴 **그리고 그 게이트를 라우트 «안» 에만 두지 마세요** — 하니스가 문자열로 검사하게 되고, 그러면 조건을 `!==` → `===` 로 **뒤집어도 그 글자가 그대로 남아 통과합니다.** 하필 그 자리가 비용을 지키는 자리입니다. ✅ 판정을 **순수 함수로 빼서**(`ocrGate(...)`) 라우트는 «부르기만» 하고 하니스가 **실제로 돌리게** 하세요. 그리고 하니스에 **「조건 뒤집기」 변이시험을 반드시** 넣으세요 — 넣기 전에는 그 세 검사가 전부 헛돌고 있었습니다. ⚠️ 크기 게이트도 같습니다 — `size_bytes` 는 스키마상 **NOT NULL 이 아니라** NULL 이면 `Number(row.size_bytes || 0) > N` 을 그냥 통과합니다. R2 에서 **실제로 받은 길이**(`bytes.length`)로 한 번 더 보세요. 감시는 `test-harness/textbook_ocr_probe_harness.mjs` | 📌 **덧붙임(2026-09-05) — 이 항목을 읽고도 그대로 밟았습니다.** 판단력 훈련을 맛보기로 열며 게스트 일일 상한을 걸었는데 조건을 **라우트 «안» 에** 뒀더니, 상한을 무력화하는 변이 **3종이 전부 통과**했습니다(`if (false && n >= CAP)` · `n >= CAP * 1000` · `n > CAP && false` — 하니스 43/43 초록). 재고 있던 것은 `indexOf` 로 본 «return·put 의 «위치»» 뿐이라 **「세기는 하는데 안 막는다」를 원리상 못 봤습니다.** ✅ 판정을 순수 함수로 빼고(`src/judgment-sample-gate.ts`) 하니스가 **경계 앞뒤를 실제로 넣어** 봅니다(0·CAP−1·CAP·CAP+1, 그리고 «못 세면 안 막는다» 를 NaN·null·−1·'x' 로). ⚠️ **「변이 N종 전부 FAIL」이라고 적을 때 그 N 에 «조건 뒤집기» 가 들어 있는지 세어 보세요** — 제 5종에는 없었습니다. 📌 **덧붙임(2026-09-15) — 상한을 걸어도 «세는 쪽» 이 죽으면 그 상한은 영영 안 걸립니다. 그리고 그것을 «KV 이름이 있는가» 로 물으면 못 봅니다.** 무인증 `/api/voice/tts` 에 IP 하루 상한을 걸며 실측: 카운터 `put` **한 줄만 지워도** 읽어 온 값이 영원히 0 이라 상한이 한 번도 안 걸리는데, 검사가 「`SESSION_STATE` 가 있는가」·「`expirationTtl` 이 있는가」였던 탓에 **58/0 으로 통과**했습니다(`get` 만 남아도 그 글자는 있습니다). ✅ **카운터 블록을 오려 내 «가짜 KV» 로 실제로 돌리세요** — 「쓰는가」·「기존값 +1 인가(하드코딩 아님)」·**「막힌 요청은 안 올리는가」(짝)** 를 봅니다. 🪤 그때 그 블록이 `async` 를 품으면 **`await` 하는 자리까지 try «안»** 이어야 합니다 — 밖에 두면 라우트에 없는 이름을 쓰는 변이(「429 로 끊기」=`return json(...)`)가 «깔끔한 FAIL» 이 아니라 **하니스 크래시**가 되어 `결과:` 줄조차 안 나옵니다(실측 — 하필 그 변이가 「막혀도 소리는 난다」는 가장 중요한 보장을 깨는 것이었습니다). ⚠️ 그리고 「상한 숫자를 라우트에 복제했나」를 **숫자로** 묻지 마세요 — 무관한 숫자에 걸려 **거짓 FAIL** 입니다(구글 응답 크기 검사 `gb.byteLength < 300` 이 상한값 300 과 우연히 같았습니다). **«상한을 어디서 가져오는가»**(손으로 넘긴 인자가 있는가 · 상수를 다시 선언했는가)로 물으세요. 🔴 **KV 카운터의 한계를 그 파일에 «적어 두세요»** — 같은 칸에 쓰기는 **초당 한 번**, 읽기는 콜로에 **최소 60초** 캐시(`cacheTtl` 하한)라 **«짧은 폭주» 에는 약합니다.** 그런데 폭주가 바로 그 상한을 만든 이유입니다 ⟹ **코드가 할 수 있는 전부가 거기까지** 라는 것을 안 적으면 다음 사람이 「상한이 있으니 안전하다」로 읽습니다(돈이 걸린 자리는 **제공자 쪽 지출 상한**이 진짜 안전판이고 그것은 코드 밖입니다). ⚠️ **부르기 «전» 에 세면 «돈이 안 나가는 실패» 도 셉니다**(키 미설정·길이 초과 거절) — 동시 요청이 서로를 보게 하려면 그게 맞지만, 아는 채로 고르고 주석에 적으세요. ⚠️ **막을 때 429 로 끊지 마세요** — 폴백이 있는 자리면 «예전 경로» 로 내려보내 **소리·화면은 그대로** 두는 편이 낫습니다(「상한이 «고장» 으로 읽히는」 것을 막습니다). ⚠️ 그때 화면 문구도 짝으로 보세요 — 진단 화면이 사유를 그대로 그린다면 「이 글자를 알려 주시면 고칩니다」가 **상한에는 틀린 말**입니다(고장이 아니라 내일 풀립니다).
| 상한을 «한 곳으로 통일» 하려 할 때 | 🔴 **목록에서 빼는 것만으로는 «더 나빠지는» 경우가 있습니다.** 2026-09-01 포인트 하루 상한을 100점으로 통일할 때, `CAP_UNCOUNTED_RULES` 를 비우면 단어장·복습퀴즈가 `earnedToday` 에 **세어지기는 하는데**, 그 두 경로는 `checkEarnAllowed` 를 **아예 안 불러서 자기는 안 막힙니다.** ⟹ 단어장 400점을 받은 학생이 그날 출석 10점·칭찬 5점을 **통째로 못 받는** 상태가 됩니다(고치기 전보다 나쁨). ✅ **«세는 쪽» 과 «막는 쪽» 은 짝입니다** — 한쪽만 고치면 반드시 이 모양이 됩니다. ✅ 한 판에 수백 점이 걸리는 경로는 **「막기」가 아니라 「깎기」**(`dailyAllowance` 로 남은 만큼)로 거세요 — 「0 아니면 전부」면 **한 판을 다 풀고 0점**을 받습니다. 그 계산은 **조회 실패 시 막지 않는 쪽**으로 실패해야 합니다. ⚠️ 통일한다고 **다른 축까지 덩달아 조이지는 마세요** — 그 둘은 `GAME_QUIZ_RULES` 에 이름이 있어 게임 하위 상한까지 보게 하면 100이 아니라 **30**이 됩니다. 📌 **그건 같은 날 사장님이 「게임 30점 상한도 적용해줘」로 따로 정하셨습니다** — 즉 «하면 안 되는 일» 이 아니라 **«사람이 정할 일»** 이었습니다. ✅ 이런 «곁가지 축» 은 말없이 함께 조이지 말고, **한 줄로 바꿀 수 있게 주석에 적어 두고 물으세요**(실제로 그렇게 남겨 둔 한 줄이 이튿날 그대로 쓰였습니다). ⚠️ 지금은 그 둘의 실효 상한이 **30**이고 그 통을 다른 게임과 **나눠 씁니다** — 단어장을 먼저 하면 그날 게임은 0점입니다. 실측 영향: 학생-일 14건이 **전부** 걸려 1,956 → **420점(−79%)**. 🪤 그리고 **검사가 헛돌며 통과합니다** — 「그 목록이 그 규칙을 담고 있는가」를 «파일에 그 글자가 있는가» 로 쓰면, 목록을 비운 뒤에도 **내가 쓴 설명 주석에 남은 이름**을 잡아 통과합니다(실측 24/24). **선언된 값**을 보세요. 📄 `docs/작업기록/260901_포인트_하루상한_100점으로_통일.md` |
| 하니스가 화면의 «칸 수·항목 수» 를 세는데 **스크립트 안 문자열까지 셈** | 🔴 `(h.match(/data-slot=/g)||[]).length >= 2` 로 「칸이 둘인가」를 세었더니, 스크립트 안의 선택자 문자열(`'[data-slot=' + slot + ']'`)이 함께 걸려 **칸을 하나 지워도 2개가 남아 통과**했습니다(2026-09-02 변이시험에서 실측). 그 화면은 «두 권을 나란히 비교하는 것» 이 존재 이유였는데, 그 검사가 바로 그것을 지키지 못하고 있었습니다. ✅ `<script>` 를 벗긴 사본에서 **화면에 실재하는 요소**를 세세요(`<div class="col" data-slot="A"` 처럼 여는 태그 모양까지). ✅ 그리고 **되돌려 보면 실제로 FAIL 나는지** 반드시 확인하세요 — 이 결함은 변이시험으로만 드러났습니다. 📜 같은 줄기: 바로 아래 「자기가 새로 만든 상수를 잡아 통과」·「부정 검사가 자기 주석을 잡음」 |
| 하니스가 **자기가 새로 만든 상수** 를 잡아 «통과» 함 | 🔴 파일 전체에서 `'vocab_review'` 같은 문자열을 찾으면, 같은 파일에 새로 넣은 **내 상수 배열이 자기를 잡습니다** — 정작 확인하려던 `GAME_QUIZ_RULES` 에서 그 이름을 빼도 초록불이었습니다(2026-09-01 실측). ✅ **목록 자체를 잘라서**(`GAME_QUIZ_RULES\s*=\s*\[([\s\S]*?)\]`) 그 안에서 찾으세요. 🪤 같은 날 함께 나온 형제 셋: ① 가짜 D1 이 **질의문과 무관하게 늘 같은 값**을 돌려주면 「…한 날에도」류 시나리오가 **한 번도 만들어지지 않아** 되돌려도 통과합니다(질의문을 보고 답을 바꾸세요) ② 「잔액 직접 올리기가 없다」를 **줄 단위**로 보면 두 줄로 쓰거나 `UPDATE` 로 쓸 때 못 잡습니다 ③ **숫자를 정규식으로 못 박으면** 그 숫자를 바로잡는 순간 FAIL 납니다(「1,000점」 → 실제 1,050점) — 숫자 대신 **«근거»** 를 검사하세요 📌 **덧붙임(2026-09-04) — 브라우저 검사에서 «설정표» 를 손으로 적으면 같은 일이 납니다.** 결재 첨부 검사가 분류표(`TYPES`)를 서버 값을 «본떠» 적어 두었더니, 소스에서 `doc` 을 `wantsFile:false` 로 되돌리는 변이가 **13건 전부 통과**했습니다 — 검사가 소스를 한 번도 안 보고 제가 적은 값만 보고 있었습니다. ✅ 정본을 **import 해서** 서버가 화면에 내려주는 모양으로 바꿔 쓰고, **「정본을 실제로 읽었는가」를 ⓪절 검사로** 두세요. 📌 **그리고 검사에 «사람 계정명» 을 «일반 직원» 예시로 못 박지 마세요** — `approval_policy_harness` 가 `mgr_jjw` 를 「경영진이 아닌 본사 담당」으로 적어 둬서, 그 사람을 경영진에 넣자 「인사·급여를 본사 담당은 볼 수 없다」 등 **2건이 FAIL** 났습니다(보안이 샌 것이 아니라 **검사가 옛 명단을 들고 있던 것**). ✅ `EXEC_USERNAMES` 를 **읽어** 거기 «없는» 계정을 고르고, 「경영진이 아닌 계정을 하나 고를 수 있다」를 **전제 검사**로 남기세요(후보가 전부 경영진이 되면 그 아래 검사들이 조용히 뜻을 잃습니다) |
| `classList` 로 body class 를 만졌더니 **관찰자가 또 깨어남**(= 홈 정지) | 🔴 **이 저장소는 같은 뿌리로 홈이 두 번 멎었습니다**(2026-07-14 · 2026-08-27). 2026-09-01 에 실제 브라우저에서 관찰자 발화 횟수를 세어 정확히 확인했습니다: **`remove(없는 토큰)` → 1회 발화** · **`add()` 도 «이미 있는 토큰» 이면 1회 발화** · `toggle(토큰, force)` 는 상태가 그대로면 **0회** · `contains` 로 막으면 **0회**. ⚠️ **여태 규칙서·소스 주석은 `remove()` 만 경고하고 있었습니다** — `add()` 도 똑같습니다. 즉 «지울 게 없어도 쓴다»·«이미 있어도 쓴다» 둘 다입니다. [잰 것 — 2026-09-01, 범위 `public/` 전체 재귀] body class 를 **쓰는 곳 93군데/31파일**(add 32·remove 35·toggle 26, 그중 가드 없는 것 **58군데**) · body class 변경에 **발화하는 관찰자 16개**. ⚠️ 처음에 `public/js/*.js` 만 세고 범위 없이 「51군데·13개」라고 적었다가 실제와 크게 어긋났습니다 — **이런 숫자는 «어디까지 셌는지» 를 반드시 함께** 적으세요. 가드 없는 쓰기 한 번이 관찰자 열몇 개를 전부 깨웁니다. ✅ **루프가 성립하는 조건은 하나** — 「body class 를 보는 관찰자가 body class 를 가드 없이 쓴다」. 두 사고 모두 이 조건이었습니다. 그래서 «쓰는 쪽 51군데» 를 다 고치는 대신 그 조건을 `test-harness/body_class_observer_harness.mjs` 가 못 박습니다(간접 호출까지 봅니다). ✅ 새로 쓸 때는 **`contains` 로 먼저 확인**하거나 **`toggle(토큰, force)`** 로 «상태를 지정»하세요. ⛔ 다만 그 `force` 를 **그 클래스 자신에서 끌어오면 안 됩니다** — `toggle('x', !body.classList.contains('x'))` 는 매번 뒤집혀 매번 쓰므로 그대로 무한루프입니다. ⚠️ 관찰자를 찾을 때 **`observe(document.body, …)` 만 보면 놓칩니다** — `documentElement` 에 **`subtree: true`** 로 걸어도 body class 변경에 발화합니다(`js/mango-worldclock.js` 가 그 형태). 그리고 콜백이 **인라인 함수가 아니라 이름**일 수 있습니다(`new MutationObserver(sync)` — 하필 2026-08-27 사고를 낸 파일이 그 형태라, 처음 만든 검사가 그 파일을 통째로 빠뜨렸습니다). ⛔ body class 를 지켜보는 관찰자를 새로 만들 때는 그 콜백에서 body class 를 쓰지 마세요 — 그 함수가 부르는 다른 함수까지 포함해서입니다 |
| 로컬(Windows)은 `first_paint_budget` 이 **빨간불**인데 **CI 는 초록** (반대 방향) | 🔴 **blocking js 일부의 «작업트리 사본만» CRLF 라 로컬이 더 크게 재집니다.** 2026-08-27 실측: 8개 파일(`inapp-escape`·`media-guard`·`idx-voice-diary`·`idx-vc-seg-lessonchange`·`idx-vc-screenmode`·`idx-vc-textbook`·`idx-vc-room`·`idx-vc-chat`)이 깃 블롭(LF)보다 합계 **+1,876B** — `autocrlf=input` 이라 커밋 때 정규화되어 **`git status` 는 clean** 이고, 하니스는 `statSync`(디스크)를 재므로 로컬만 상한을 넘습니다. ⚠️ 처음에 「index.html 전체가 CRLF」로 잘못 단정했다가 **파일별 `wc -c` 대 `git cat-file -s` 대조**로 8개 파일임을 확정했습니다 — 이 대조가 판별법입니다. ✅ **CI(LF) 측정이 정답**입니다 — 로컬 빨강을 근거로 `--update` 로 기준선을 올리지 마세요. ⚠️ 그 CI 기준으로도 **여유가 ~660B 뿐**이라(2026-08-27), blocking 에 1KB 만 더해도 CI 까지 빨간불이 됩니다 |
| 크기바 이름이 «비율» 을 말하는데 실제와 다름 | 🔴 **데스크톱(≥1025px)에는 얼굴칸 폭 상한이 걸려 있습니다** — `#vc-main-row.video-half > #vc-video-pane{width:clamp(260px,27%,460px)!important}`. 「칠판·교재가 좌우로 좁아지는 것을 막으려고」 **일부러** 넣은 것입니다. 그래서 1920px 창 실측(2026-08-26)이 **quarter 16% · half 24% · threequarter 45% · full 100%** 로, 이름이 약속하는 25/50/75/100 과 다릅니다. ⚠️ 그래서 크기바 이름은 **비율을 약속하면 안 됩니다** — 「반반」이 24% 라 2026-08-26 에 **「기본」** 으로 바꿨습니다(사장님 선택. 나머지 `교재 크게·얼굴 크게·모두 보기` 는 «상대적인 말» 이라 그대로). ⛔ 「반반」으로 되돌리지 마세요. ⛔ 상한을 풀어서 «이름에 맞추는» 것은 교재 폭이 걸린 별건입니다(폭 규칙이 CSS 8곳에 흩어져 있습니다). 감시는 `vc_sizebar_labels_harness` 의 `CANON.half` |
| 옛 CSS 가 `button:nth-child(1)` 로 숨기는데 **엉뚱한 버튼이 사라짐** | 🔴 **`nth-child` 는 «그 상자의 N번째» 가 아니라 «자기 부모의 N번째»** 입니다. `body.vc-in-call .video-size-bar button:nth-child(1),(3){display:none!important}`(폰 세로)는 **크기바 안 어디든** 자기 부모의 1·3번째 버튼을 잡습니다. 2026-08-25 실측: 크기바에 팝오버를 넣었더니 «⋯»(`.vsb-more-wrap` 의 첫 자식)과 메뉴의 📌PIP(1번째)·⤡자유(3번째)가 **폰 세로에서 조용히 사라졌습니다**(에러 없음). ✅ 자식 상자를 새로 만들 때는 그 안의 버튼을 **되살리는 줄을 함께** 두세요. ✅ 확인은 `getComputedStyle(el).display` 를 재고, 이기는 규칙은 `document.styleSheets` 를 훑어 `el.matches(r.selectorText)` 로 찾으세요 |
| GitHub 상태를 확인했는데 **「실행이 없다」** 고 나옴 | 🔴 **인증 없이 `api.github.com` 을 부르면 이 비공개 저장소는 403 을 줍니다.** 응답에 `check_runs` 가 없다고 «실행 없음» 으로 읽으면, **이미 통과한 CI 를 «안 붙었다» 고 오독**합니다(2026-08-26 실제로 여러 번 밟아 잘못 보고했습니다). ⚠️ 환경에 `GH_TOKEN`·`GITHUB_TOKEN` 이 **있지만 GitHub API 용으로는 안 통합니다**(실측 `AUTH_ERR`) — git push 용입니다. ✅ GitHub 상태 조회는 **MCP 도구**(`mcp__github__pull_request_read` · `actions_list` · `get_job_logs`)로 하세요. ✅ 폴링 스크립트를 쓸 거면 «완료/진행중» 말고 **«조회 실패»** 를 별도 상태로 구분해 출력하세요 — 안 그러면 실패가 «진행 중» 으로 위장합니다 |
| 레벨테스트 신청을 지웠는데 캘린더·강사 화면에 그 수업이 그대로 남음 | **`leveltest_applications` 하나만 지우면 안 됩니다.** 날짜·시간·배정교사가 갖춰진 신청은 `autoScheduleOnApply()`(`src/leveltest-schedule.ts`)가 신청 즉시 `class_schedules`(`class_type='level_test', source='leveltest_app'`)에 «실제 수업»처럼 자동으로 연결해 둡니다. 계정이 없던 신청은 `createTrialStudent()` 가 `students_erp` 에 `user_id='lt{신청id}', source='leveltest'` 로 **체험 계정도 자동 생성**합니다(화면 「아이디」 칸의 `lt18`·`lt16` 등이 이것). 신청서만 지우면 이 둘이 고아로 남아 캘린더·강사 화면에 계속 보입니다. ⚠️ 이 테이블에는 «이게 데모다» 를 표시하는 칸이 아예 없어서, 데모/실제 신청은 이름·정황으로만 구분해야 합니다(2026-08-20 실측 — 신청 17건 전부가 데모, 실제 신청 0건). 체험 계정을 지우기 전에는 그 `user_id` 로 `class_schedules`·`point_transactions`·`student_evaluations` 등에 다른 기록이 있는지 먼저 세어 보고(0건 확인) 지우세요. 정리 기록: `docs/작업기록/260820_레벨테스트_신청현황_데모데이터_정리.md` |
| 지사·대리점에게 관리자 API 를 열었는데 남의 자료가 보임 | `isAgencyAllowedApi`(`src/index.ts`)에 경로를 넣는 것은 **«스코프로 자른 뒤» 에만** 하는 일입니다. 조직 명부(`/api/admin/franchises` 241건·`/api/admin/centers` 921건)는 2026-08-18 에 열었지만, 그 전에 `scopeFranchiseCond()`·`scopeCenterCond()`(`src/scope.ts`)로 지사=자기 지사, 대리점=자기 한 칸까지 자르고 등록·수정은 `canEditOrg()` 로 본사만 남겼습니다. **조건절만 지우면 전국 명부가 통째로 샙니다** — `org_scope_harness.mjs` 가 «열림»과 «잘림»을 함께 감시합니다. ⚠️ 목록을 `IN (?,?,…)` 로 펴지 마세요. 지사본사는 소유 지사가 241개까지 가서 D1 바인드 100개 한도를 넘고, 그 예외는 대개 삼켜져 «빈 표» 로 보입니다 — 콤마 문자열 한 개로 맞춥니다 |
| 카드를 역할에 열었는데 그 안의 다른 칸까지 열림 | 카드 등급(`CARD_POLICY`, `adm-core.js`)은 **카드 한 장 단위**입니다. 조직 관리 카드처럼 한 카드 안에 «보여도 되는 것»(자기 지사·대리점)과 «본사만 볼 것»(🏛️ 대표지사 권역표·🏯 본사 법인정보·등록 폼)이 섞여 있으면, 등급만 낮추면 안 되고 칸 단위로 한 번 더 감춰야 합니다(`_applyOrgScopeUI()`). **서버 403 과 화면 감추기 둘 다** 필요합니다 — 서버만 있으면 «눌러도 안 되는 버튼»이 남고, 화면만 있으면 URL 로 뚫립니다 |
| `test-harness/asset-versions.json` 이 병합할 때마다 충돌 | **원장은 「주소 순 정렬」 상태로 유지합니다.** 정렬을 없애면 새 항목이 다시 파일 맨끝에 몰리고, 병렬 PR 끼리 «내용은 무관한데 자리만 겹쳐» 충돌합니다(2026-08-17 에 PR 하나 병합하는 동안 네 번 밟음 — `/js/adm-core.js` 대 `/css/admin-inline-c.css`). 정렬은 `asset_version_harness.mjs` 의 저장부가 합니다. ⛔ `.gitattributes` 의 `merge=union` 으로 풀지 마세요 — 마지막 줄에 쉼표가 없어서 **깨진 JSON** 이 되는데 git 이 «병합 성공» 이라고 조용히 넘어갑니다. 충돌이 나면 «추가끼리 부딪힌 것» 이므로 **양쪽 항목을 다 남기세요**(같은 파일의 버전을 둘이 동시에 올린 경우만 진짜 충돌이니 사람이 판단). 🪤 **다만 «다 남긴 뒤» 반드시 «첫 칸이 같은 줄이 둘인지» 세어 보세요** — 한쪽이 «새 줄» 을 넣고 다른 쪽이 **그 옆 줄을 고쳤으면** 자리만 겹친 것이 아니라 **같은 줄의 두 판**이라, 다 남기면 목차에 같은 문서가 두 번 실립니다(2026-09-01 `docs/INDEX.md` 에서 실제로 밟음 — 낡은 판 1,197자와 갱신판 1,671자가 나란히 남았습니다). 표 행 수가 «내쪽 + main − 기준점» 과 **한 줄이라도 어긋나면** 그 신호입니다. 그때는 **갱신판만** 남기세요 |
| 새 PC 에서 `npm ci` 가 «ERESOLVE / peer» 로 죽음 | **Node 버전 문제입니다. 의존성 문제가 아닙니다.** `wrangler@3.114.x` 가 `@cloudflare/workers-types@^4` 를 기대하는데 이 저장소는 `^5` 를 씁니다. **npm 11(Node 24)은 이 `peerOptional` 충돌을 그냥 통과시키고, npm 10(Node 22)은 막습니다** — CI 는 `node-version: '24'` 라 늘 초록불이라서 내 PC 에서만 죽는 것처럼 보입니다(2026-08-18 실측). `.nvmrc` 가 있으니 `nvm use` 로 24 를 켜세요. ⚠️ **설치 시점에 원인을 알려 주는 방법은 없습니다** — `engines`·`.npmrc engine-strict`·`preinstall` 셋 다 시험했지만 npm 은 **항상 의존성 해석을 먼저** 해서 ERESOLVE 가 앞서 터집니다. `--legacy-peer-deps` 는 임시방편이고, 정본 해결(wrangler 4 올리기 등)은 배포 담당이 판단 |
| 차트가 카드 밖으로 한없이 길어짐 | **Chart.js 는 `maintainAspectRatio:false` 일 때 «부모 상자» 의 크기를 그대로 따라갑니다.** 높이가 없는 부모에 캔버스를 넣으면 캔버스가 자기 높이로 부모를 늘리고, 다음 리사이즈에서 그 늘어난 높이를 다시 읽어 **창을 건드릴 때마다 누적해서 길어집니다**(2026-08-18 매출 대시보드 실측: 첫 렌더 690px → 폭 줄이니 3,070px → 세 번 흔드니 5,758px). `<canvas height="…">` 속성은 responsive 모드에서 **무시되므로 방어가 안 됩니다.** 캔버스는 반드시 **`position:relative` + 높이를 정한 래퍼** 안에 넣으세요. 같은 패턴이 `adm-core.js`·`adm-p5.js`·`adm-p7.js` 에 여러 곳 있습니다 — 대부분은 부모에 높이가 있어 멀쩡하니 **새로 만들 때 래퍼를 빠뜨리지 않는 것**이 요령입니다. 덧붙여 그리드가 `1fr 1fr` 이면 **내용보다 작아지지 않아**(=`minmax(auto,1fr)`) 좁은 폭에서 옆으로 삐져나옵니다. `repeat(auto-fit,minmax(min(100%,300px),1fr))` + 각 칸에 `min-width:0` 을 쓰세요 |
| PR 을 열었는데 CI 체크런이 **0건** | 빨간불도 초록불도 아니고 **실행 자체가 안 만들어집니다.** 저장소 Actions 는 멀쩡하고 다른 PR 은 다 도는데 그 PR 만 조용합니다(2026-08-18 PR #233 실측). `deploy.yml` 주석의 「아무 신호 없이 실행이 안 생긴다」와 같은 종류입니다. 안 통하는 것: **빈 커밋 push**(synchronize), **PR 닫았다 다시 열기**(reopened), `workflow_dispatch` API(토큰 권한 없어 403). 통한 것: **`main` 을 브랜치에 병합한 커밋을 push**(2026-08-18 PR #241 에서 재발 — 위 셋을 순서대로 다 시도했고 `main` 병합 푸시만 통했습니다. 즉 **한 번 있었던 일이 아니라 반복되는 현상**입니다). 그동안 게이트를 직접 확인하려면 CI 가 부르는 바로 그 스크립트 **`bash test-harness/ci-gates.sh`** 를 로컬에서 돌리면 됩니다(종료코드 0 = 4종 통과). ⚠️ 그래도 **PR 화면에 초록이 안 보이는 상태로 병합할지는 사람이 결정**하세요 — ci.yml 을 만든 취지가 「머지 버튼을 누르기 전에 게이트 결과가 보여야 한다」입니다. 📌 **2026-08-26 에 하루 두 번 더 밟았습니다**(PR #491·#506) — 두 번 다 «main 병합 푸시» 로 즉시 깨어났습니다(#491 은 열고 14분, #506 은 4분 동안 0건이었고 그 사이 다른 PR 의 CI 는 정상적으로 돌고 있었습니다). ✅ 판별은 **다른 PR 의 최근 run 을 먼저 보는 것** — 저장소 Actions 가 도는데 내 PR 만 조용하면 이 함정이고, 아무것도 안 돌면 지출 한도 쪽입니다. 🔴 **정정(2026-08-26 PR #542) — 위에 적힌 «통하는 것 / 안 통하는 것» 이 이번에는 정확히 반대였습니다.** 실측: ① `main` 병합 푸시 — **1차는 통했고 2차는 안 통했습니다**(즉 «항상 통하는 방법» 이 아닙니다) ② **진짜 내용이 있는 푸시**(빈 커밋 아님) — 실행이 안 생겼습니다 ③ **PR 닫았다 다시 열기(reopened)** — **실행이 생겼습니다**(문서에는 「안 통한다」고 적혀 있었습니다) ④ `workflow_dispatch` — **MCP 도구로도 403**(`Resource not accessible by integration`. 문서에는 「토큰 권한 없어 403」으로만 적혀 있어 MCP 경로는 열려 있을 것처럼 읽혔습니다) ⑤ 실패한 실행 **재실행(rerun)도 403** — 지출 한도를 푼 뒤에도 그대로입니다(한도가 아니라 **토큰 권한** 문제라 사람이 Actions 화면에서 눌러야 합니다). ⛔ 그러니 이 목록을 «순서대로 따라 하면 되는 절차» 로 읽지 마세요 — 무엇이 통할지는 그때그때 다릅니다. 🔴 **덧붙임 — 「0건」이 아니라 「실행은 있는데 러너를 못 받는」 모양도 있습니다.** 같은 날 실측: run 은 **`failure`** 인데 그 안의 job 은 **`queued`** 인 채로 끝났습니다(6시간 12분, 시작조차 안 함). ⚠️ 이때 **체크런 API 는 계속 `queued` 를 돌려줍니다** — 그것만 보면 「아직 도는 중」으로 오독합니다(실제로 제가 그렇게 여러 번 보고했습니다). ✅ 판정은 `list_workflow_runs` 의 **run 결론**과 **시각**으로 하세요. ✅ 이 모양은 게이트가 **돌지도 못한** 것이라 «코드 판정» 이 아닙니다 — 코드를 고치러 가지 마세요. ⚠️ 지출 한도의 지문(「5초 즉사 + 스텝 0개」)과도 다릅니다(이쪽은 아예 시작을 안 합니다). 원인 문구는 **Actions 화면의 배너**에만 나오므로 사람이 봐야 합니다 🔴 **재확인(2026-08-27 PR #545, 문서만) — 이번에는 «닫았다 다시 열기» 와 «main 병합 푸시» 의 결과가 바로 위 #542 실측과 «서로 반대» 로 나왔습니다.** 실측(`date -u` 로 시계를 찍어 확인 — 느낌으로 세지 마세요): PR 을 연 뒤 **2시간 5분** 동안 0건 · **닫았다 다시 열기 → 실행이 안 생겼습니다**(#542 에서는 생겼습니다) · **`main` 병합 푸시 → 즉시 생겼습니다**(목차 충돌을 푼 진짜 커밋. run #612 `success`, 109초). 그동안 다른 PR 의 CI 는 정상적으로 돌고 있었습니다. ⟹ **두 방법 모두 «될 때도 안 될 때도» 있습니다** — 어느 하나를 «정답» 으로 적지 마세요. 지금까지 실측된 것만 보면 «통한 적이 있는 방법» 은 그 둘뿐입니다. ⚠️ 그때도 **체크런 API 는 `in_progress` 인 채**였는데 run 은 이미 `success` 였습니다 — 위 「run 결론과 시각으로 판정」이 그대로 재확인됐습니다. 🔴 **재확인(2026-09-01 PR #609) — 이번에는 «main 병합 푸시» 가 안 통하고 «닫았다 다시 열기» 가 통했습니다**(#542 와도, #545 와도 다릅니다). 실측: 진짜 내용이 있는 main 병합 커밋을 push → **0건** · 그 상태로 3분 대기 중 다른 브랜치의 run #737 은 정상 시작 · **PR 닫았다 다시 열기 → 즉시 run #738 생성**(102초 success). ⟹ 지금까지 실측된 «통한 적이 있는 방법» 은 여전히 그 둘뿐이고, **어느 쪽이 통할지는 그때그때 다릅니다.** ⛔ 순서를 정해 놓고 따라 하지 마세요. ✅ 판정은 «체크런이 있나» 가 아니라 **«내 최신 sha 로 돌았나»**(`head_sha` 대조)로 하세요 — 옛 sha 의 초록불이 그대로 보여서 «이미 통과했다» 로 오독합니다. ⚠️ **로컬 `ci-gates.sh` 를 «대체 근거» 로 쓰지 마세요** — `cloudflare-deploy/node_modules` 가 없는 컨테이너에서는 ①(tsc)과 ②(esbuild 의존 하니스 6종)이 **환경 사유로** 실패해 종료코드 1 이 됩니다. 코드 판정이 아닙니다(그 6종은 각각 실행해 사유를 확인할 것). |
| CI 가 「몇 십 분째 멈췄다」고 판단했는데 **실제로는 몇 분밖에 안 지났음** | 🔴 **`sleep` 을 백그라운드로 걸어 놓고 그 합만큼 시간이 흐른 줄 알면 안 됩니다.** `run_in_background: true` 로 띄운 `sleep` 은 **분리 실행**이라 곧바로 돌아오고, 그 뒤 폴링은 실제 시간보다 훨씬 빠르게 반복됩니다. 2026-08-20 실측: `sleep` 을 6개(총 ~20분) 걸어 놓고 폴링했지만 실제 경과는 **4분**이었는데 「30분째 멈춤」으로 보고했고, 사장님이 그 숫자를 근거로 「CI 초록불 없이 병합」을 결정하셨습니다(다행히 배포 게이트가 나중에 `success` 로 통과). ✅ **경과 시간을 말하기 전에 반드시 `date -u` 로 실제 시계를 찍으세요.** 특히 「멈췄다/느리다」는 판단은 시작 시각과 «지금»을 빼서 확인할 것. ✅ 이 저장소 CI 는 평소 **약 90초**입니다 — 그 기준과 비교하세요. ⚠️ 컨테이너 시계가 세션 시작 이후 크게 점프할 수도 있으니(밤을 넘기는 등) 날짜까지 함께 보세요 |
| 녹화 목록이 「완료」인데 **영상이 없음** | 🔴 **화면이 «고장» 이 아니라 «평범한 숫자» 로 거짓말하고 있었습니다 — 원인이 다섯 겹입니다**(2026-08-26 전부 수리, PR #491·#506). ① **R2 multipart 는 «비마지막 파트가 5MiB 이상»** 이라 2.5Mbps 기준 **첫 ~17초는 서버에 조각이 하나도 없습니다.** 그 사이 탭이 닫히면 통째로 사라졌습니다(8/25 실측 6건이 13·17·3·1·2·27초로 전부 이 구간) → 조각이 아직 없는 동안만 버퍼 전체를 `<키>.snap` 옆자리에 미리 올려 둡니다(`mango-rec.js` `maybeSnapshot`). ⛔ **진행 중인 키에 그냥 덮어쓰지 마세요** — `upload/complete` 의 «head() 로 실물이 있으면 성공» 자가복구가 **앞부분만 담긴 스냅샷을 완성본으로 오인**해 모든 녹화가 조용히 잘립니다. 되살리기는 multipart 가 확실히 끝난 뒤(abort 순간·크론 스윕)에만. ② `/api/recordings/stop` 이 **R2 성공 여부와 무관하게 `completed`** 를 적었습니다(그 「2.1MB」는 브라우저가 잰 **로컬** 크기입니다) → 서버가 `file_url` 로 **`head()` 를 걸어 실물을 직접 확인**합니다. ⚠️ **조회 자체가 실패하면 강등하지 마세요** — 수업이 끝날 때마다 도는 경로라 통신이 잠깐 흔들리면 멀쩡한 녹화가 무더기로 «실패» 로 찍힙니다(`headChecked` 로 «판단 보류»). 클라이언트 `r2_success` 는 보조일 뿐 — **두 곳 다** 보내야 합니다(`mango-rec.js`·`video-call/js/recorder.js`). ③ `handleRecordingList` 가 커서 없이 1,000개만 읽어 **있는 파일도 「영상 없음」** 이었습니다 — 녹화 키는 `rec/…` 라 `class-…`·`mangoi-…` 보다 **사전순 뒤로 밀립니다.** ④ 「🔧 진단」이 `recordings/` 만 세어 파일이 있어도 **늘 「0개」**. ⑤ 「📊 저장소 상태」 4칸은 **HTML 에 박아 둔 예시값**(12.4GB·156파일·248MB·₩4,820)이고 「🔄 새로고침」이 부르는 `window.refreshStorageStats` 는 **저장소에 없었습니다**(무동작). ✅ 화면이 모르면 **모른다고 말하게** 하세요(`truncated` 표시·«저장 실패» 타일). 감시: `recording_stop_truth_harness`(자동 29건 — 소스의 UPDATE 문을 오려 내 **진짜 SQLite 에 돌립니다**) · `manual/recording-storage-kpi-browser.mjs`(브라우저 23건) |
| 화면에 «측정할 수 없는 값» 을 그럴듯하게 채우고 싶을 때 | ⛔ **지어내지 마세요 — 그게 이 저장소가 가장 오래 속은 방식입니다.** 「📊 저장소 상태」의 KV 호출수·월 청구액은 **Worker 안에서 알 수 없습니다**(Cloudflare 계정 API 영역). 그래서 누군가 예시 숫자를 박아 뒀고, 그 카드는 **몇 달 동안 «그럴듯한 거짓말»** 을 하고 있었습니다. 대충 계산해 넣는 것도 같은 결과입니다. ✅ 못 재는 값은 **빼고**, 그 자리에 **실제로 잴 수 있고 그 화면에서 궁금한 값**을 넣으세요(2026-08-26: 「저장 실패 N건」·「30일 내 만료 N건」으로 교체 — 사장님 보고 후). ✅ 못 채웠을 때는 «—» 와 이유를 적습니다 |
| playwright 로 API 를 스텁했는데 **화면이 안 채워짐** | **route 는 «나중에 등록한 것» 이 이깁니다.** 포괄 스텁(`**/api/**`)을 구체적인 스텁 **뒤에** 등록하면 그것이 전부 삼켜 «화면이 못 채운다» 는 **거짓 실패**가 납니다(2026-08-26 실제로 밟아 멀쩡한 코드를 한참 의심했습니다). ✅ **포괄을 먼저 깔고 구체적인 것을 뒤에** 등록하세요. ⚠️ 이런 검사에서 FAIL 이 나면 **검사 쪽을 먼저 의심**하는 편이 빠릅니다 — 스텁·오버레이·첫 방문자 상태처럼 «화면 밖» 원인이 흔합니다 |
| 저장소의 A4 인쇄용 HTML 을 화면·웹으로 내보냈더니 아래가 잘림 | `docs/` 의 인쇄용 문서 중에는 **`height:297mm` + `overflow:hidden`** 으로 「A4 딱 한 장」에 맞춰 놓은 것이 있습니다(`회계리포트_점검보고_2026-08-17.html`). 인쇄할 때는 맞지만 **폰트가 원본과 조금만 달라도 몇 mm 넘치고, 넘친 부분은 경고 없이 잘려 사라집니다** — 2026-08-18 실측으로 내용 높이가 1,134~1,142px, A4 는 1,123px 이라 아래가 잘렸습니다. ⚠️ 이 문서들은 **같은 폴더의 `NotoSansKR.ttf` 를 찾는데 10MB 라 깃에 없습니다.** 그래서 다른 폰트로 그려지고, 그게 넘침의 원인입니다. 화면용으로 낼 때는 `height:auto` + `overflow:visible` 로 풀고 폰트를 명시하세요 |
| 화면이 「모바일에서 잘린다」고 나오는데 실제로는 멀쩡함 | **헤드리스 크로미움의 최소 뷰포트가 500px 입니다.** `--window-size=390,...` 로 찍으면 **500px 로 레이아웃한 화면을 390px 폭으로 «잘라» 저장**하므로, 멀쩡한 반응형 화면도 오른쪽이 잘린 것처럼 보입니다(2026-08-18 실제로 오진). 스크린샷 눈대중 대신 **`document.documentElement.scrollWidth > innerWidth` 를 재세요.** 브라우저는 이 컨테이너에 이미 있습니다 — `/opt/pw-browsers/chromium-1194/chrome-linux/chrome --headless --dump-dom`. ⚠️ 다만 이 컨테이너에서는 구글 폰트가 프록시에 막혀 안 받아집니다. 글꼴이 걸린 문제는 여기서 재현되지 않습니다 |
| 원본 HTML 에 내 CSS 를 덮었는데 안 먹음 | **적응층 `<style>` 은 원본 `<style>` «뒤» 에 와야 합니다.** 앞에 두면 같은 특정성일 때 뒤에 오는 원본이 이깁니다(`.artifact-shell > *{max-width:100%}` 대 `.wrap{max-width:860px}` — 2026-08-18 실제로 밟음). ⚠️ 또 하나: 부모가 `display:flex; align-items:center` 면 **자식이 «내용 폭» 으로 부풀어** `max-width` 를 걸어도 표의 `min-width` 가 문서를 밀어냅니다. `width:100%; min-width:0` 을 함께 주세요 |
| 배포 워크플로가 «cancelled» 로 떴는데 실패가 아님 | **`deploy.yml` 의 `concurrency` 때문입니다.** PR 이 몇 분 안에 연달아 병합되면(2026-08-18 에 네 건이 5분 안에 들어옴) 대기하던 run 들이 **cancelled 로 정리되고 마지막 run 하나가 최신 `main` 을 통째로 배포**합니다. `cancel-in-progress: false` 라 «도는 것» 은 안 끊고 «대기 중» 만 정리하는 것이라 정상 동작입니다. ⚠️ Actions 목록에서 자기 커밋 줄이 cancelled 인 것만 보고 「배포가 안 나갔다」고 판단하지 마세요. 확인은 **`git merge-base --is-ancestor <내 커밋> <배포 성공한 run 의 head_sha>`** 로 합니다(포함돼 있으면 나간 것) |
| 월 단위 리포트를 «기간 합산» 으로 넓혔더니 숫자가 이상함 | **더해도 되는 값(흐름)과 더하면 안 되는 값(잔액)을 갈라야 합니다.** 손익계산서 분기 조회(2026-08-18)에서 매출·비용·현금흐름은 3개월을 더하지만, **통장 월말 잔액·미지급 강사급여는 시점값이라 더하면 3배**가 됩니다 → «분기 마지막 달» 기준으로 봅니다(`.bind(lastMonth)`). ⚠️ 이때 `period` 를 그대로 바인딩하면 `'2026-Q1'` 이 `substr(trans_at,1,7)<=?` 문자열 비교에 들어가 **에러 없이 엉뚱한 값**이 나옵니다. ⛔ 그리고 기간용 SQL 을 새로 쓰지 마세요 — 월 계산(`monthPL`/`monthCash`)을 그대로 3번 돌려 더하면 월 화면과 구조적으로 어긋날 수 없습니다. 부가세처럼 반올림이 있는 값은 **달마다 계산해 더해야** 월 화면 합과 1원도 안 틀립니다. 감시: `test-harness/statement_quarter_harness.mjs` |
| `git stash pop` 충돌을 `--theirs` 로 풀었더니 남의 작업이 사라짐 | **stash 충돌에서 `--theirs` 는 «내 stash» 입니다**(평소 병합과 반대로 느껴집니다). `test-harness/asset-versions.json` 을 그렇게 풀었다가 **main 이 추가한 항목 4개를 통째로 날릴 뻔했습니다**(2026-08-18 실측 — `/js/adm-core.js?v=123`·`v=125`·`adm-ia6`·`adm-r25`). 원장이 충돌하면 **`git checkout origin/main -- test-harness/asset-versions.json` 으로 main 것을 되살린 뒤 하니스를 다시 돌려** 내 항목만 새로 기록하세요. 그러면 diff 가 «내가 추가한 한 줄» 로 깨끗하게 남습니다 |
| `git checkout <다른브랜치> -- .` 로 파일을 가져왔더니 **내 편집이 날아가고 남의 변경까지 되돌아감** | 🔴 **그 명령은 «가져오기» 가 아니라 «그 브랜치 상태로 덮어쓰기» 입니다** — 경로가 `.` 이면 작업트리 전체가 대상이라 ① **커밋 안 한 내 편집이 말없이 사라지고** ② 그 브랜치에 **없는** 최신 커밋(남이 방금 병합한 것)까지 **옛 버전으로 되돌아갑니다.** 2026-09-06 실측: 새 브랜치를 `origin/main` 에서 만든 직후 옛 작업 브랜치에서 `-- .` 로 가져왔더니, 방금 한 편집 3곳이 사라지고 **같은 날 다른 세션이 병합한 문서 2개가 되돌려진 상태**로 `git status` 에 떴습니다(그대로 커밋했으면 남의 작업을 지우는 PR 이 됩니다). ⚠️ **되돌려진 것은 «수정» 으로 보이지 «삭제» 로 안 보입니다** — `git status --short` 의 `M` 만 보면 못 알아챕니다. ✅ 브랜치를 새로 딸 때는 **`git reset --hard origin/main` 으로 깨끗이 맞춘 뒤 편집을 다시 적용**하세요(편집이 몇 곳이면 그게 제일 빠릅니다). ✅ 특정 파일만 정말 가져와야 하면 **경로를 하나씩 적으세요** — 같은 표의 「`git checkout -- test-harness/` 처럼 폴더째 되돌리지 마세요」와 같은 뿌리입니다. ✅ 커밋 전에 **`git diff --stat` 으로 «내가 건드린 파일만 있는지»** 확인하세요 — 이번에도 그것으로 잡았습니다. 📜 형제: 위 「`git stash pop` 충돌을 `--theirs` 로」·「INDEX 충돌을 «양쪽 다 남기기» 로 풀었더니 지운 줄이 되살아남」 📌 **덧붙임(2026-09-09) — 경로를 «파일 하나» 로 적어도 같습니다.** 변이시험(일부러 고장 내고 하니스가 잡는지 보기)을 되돌리려고 `git checkout -- <그 파일>` 을 썼는데, 그 파일에는 **아직 커밋 안 한 이번 작업**이 함께 들어 있어 **배선 세 곳이 통째로 날아갔습니다**(하니스가 3건 FAIL 로 잡아 줘서 알았습니다). ✅ 변이시험은 **먼저 `cp` 로 사본을 떠 두고 그 사본으로 되돌리세요** — git 은 «커밋된 것» 으로 되돌릴 뿐 «내가 방금 쓴 것» 을 모릅니다. ✅ 그리고 되돌린 «뒤» 에 하니스를 한 번 더 돌려 **기준 점수로 돌아왔는지** 확인하세요(그 한 줄이 이번에 잡아 줬습니다). |
| 스크립트로 소스를 고쳤는데 **파일이 0바이트가 됨** | 🔴 **`open(p,'w')` 는 «먼저 비우고» 씁니다 — 쓰는 도중 예외가 나면 원본이 통째로 사라집니다.** 2026-09-21 실측: 파이썬 치환 한 줄에서 `\ud83c\udfaf`(써로게이트 쌍을 손으로 적은 이모지)가 `UnicodeEncodeError: surrogates not allowed` 를 내면서 **`src/api-admin.ts` 1,123,788바이트가 0바이트**가 됐습니다. 마침 몇 분 전 변이시험용 사본이 있어 되살렸을 뿐, 없었으면 커밋 안 한 그날 작업이 함께 날아갔습니다. ⚠️ **그 자리에서는 «치환 실패» 로만 보입니다** — 다음 명령이 「앵커를 못 찾음」으로 죽고, 그때 의심하는 것은 앵커 문자열이지 파일 크기가 아닙니다(실제로 한 번 더 헛돌았습니다). ✅ **임시 파일에 쓰고 `os.replace`** 로 바꾸세요 — 쓰다 죽어도 원본이 그대로입니다. ✅ 이모지는 **`\U0001F3AF` 처럼 코드포인트 하나**로 적으세요(파이썬은 써로게이트 쌍을 안 합칩니다. 더 나은 길은 «글자 그대로» 적는 것 — 『한글을 `\uXXXX` 로 손으로 적지 마세요』 항목과 같은 뿌리). ✅ 그리고 **고친 뒤 `wc -c` 와 `git diff | grep '^-[^-]'`** 로 «지워진 줄이 전부 내 것인가» 를 보세요 — 이번에도 그 한 줄이 잡았습니다. ⚠️ `len(문자열)` 은 **글자 수**라 바이트와 다릅니다(한글 3바이트) — 그 값으로 «온전한가» 를 판단하지 마세요 |
| 「주석을 벗겨 낸 사본으로 판정한다」고 해 놓고 **줄주석(`//`)은 안 벗긴** 경우 | 🔴 **그러면 «가드를 지우는» 대신 «가드를 주석 처리» 하는 것만으로 검사가 통과합니다 — 지운 것과 결과가 같은데 초록불입니다.** 2026-09-11 실측: `popup_open_return_harness` 가 블록주석만 벗겨서, `w.opener=null` 을 **`// w.opener=null` 로 바꾸기만 해도 21/21 초록**이었습니다(그 검사 하나가 공동 금지구역의 보호를 못 박고 있던 자리입니다). ✅ 벗길 때 **블록주석·줄주석을 함께** 처리하세요. ⛔ **그렇다고 `//` 를 정규식으로 일괄 지우지 마세요** — 문자열 안의 `https://` 가 잘려 그 뒤가 통째로 사라지고 **멀쩡한 코드가 «없다» 로** 판정됩니다(2장 「블록주석을 정규식 한 줄로 지웠더니」의 형제). 글자를 훑으며 «블록 안인가 · 문자열 안인가»(`'` `"` 백틱)를 함께 추적하세요. ✅ 그리고 **그 변이를 실제로 넣어 FAIL 이 나는지** 확인하세요 — 이 결함은 그렇게만 드러납니다 |
| «막는다» 검사만 넣고 **«그래도 나갈 길이 남아 있는가»** 를 안 볼 때 | 🔴 **한쪽만 두면 반대 방향 사고가 통째로 통과합니다.** 같은 하니스에서 「`noopener` 를 뺐으면 `opener` 를 끊는가」(②)만 재고 있어서, **인앱 브라우저 폴백(`location.href`)을 통째로 지우는 변이가 21/21 초록**이었습니다(2026-09-11 실측). 그 폴백이 **애초에 그 코드가 생긴 이유**입니다 — 카톡·문자앱 인앱 브라우저는 진짜로 `null` 을 돌려주므로, 지우면 그 사람들에게 **아무 일도 안 일어납니다.** ✅ «막힌다» 와 **«나간다» 를 짝으로** 두세요(④절). ⛔ 그 짝 검사를 **아무 데나 넓히지 마세요** — 자리마다 폴백 «모양» 이 다릅니다(여기서는 넷이 `location.href` 가 아니라 «안내 상자» 라, 실측으로 확인하고 index.html 두 자리에만 걸었습니다). 넓히면 멀쩡한 코드가 빨간불입니다 |
| 예산·한도의 **«막는 선»** 을 «넣을 수 있는 마지막 값» 으로 읽을 때 | 🔴 **한 칸씩 낙관적이 되고, 그 한 칸을 믿고 넣으면 그 자리에서 FAIL 입니다.** 첫 화면 예산은 `Math.round(b/1024) > 기준선+slack` 이라 **그 선을 1B 넘는 순간 FAIL** 이고, 통과하는 마지막 값은 그보다 1B 작습니다 ⟹ 여유 = `통과 마지막 B − b`. ⚠️ **그 두 숫자는 기준선이 오르면 함께 바뀝니다** — 여기에 베껴 적지 말고 정본(「첫 화면 예산이 FAIL」 줄)을 보세요. 📜 2026-09-11 당시 값으로 쓰면 `> 1575` 라 1,613,312B 부터 FAIL·통과 마지막 1,613,311B 였습니다. 2026-09-11 에 제가 이것을 틀려 CLAUDE.md·INDEX·작업기록 **네 곳에 「여유 9바이트」** 라고 적었는데 실제로는 **8바이트**였습니다(9 를 넣으면 `round(1575.5)=1576`). ✅ 여유를 적을 때는 **«그 값을 넣으면 통과하는가» 를 실제로 돌려** 확인하세요. ⚠️ 같은 행 안에서 앞 문단이 43 이라 적고 뒤 문단이 44 라 적어도 **아무 검사도 안 걸립니다** — 숫자를 적기 전에 바로 위 문단과 대조하세요 |
| 하니스에 «이 단어가 없어야 한다» 검사를 넣었는데 내 주석 때문에 FAIL | **부정 검사는 반드시 주석을 벗겨 낸 사본으로 판정하세요.** 「왜 지웠는지」 적은 설명 주석에 그 단어가 들어가면 검사가 자기 주석을 잡습니다(2026-08-18 `c24_finance_kcpm_harness` ③에서 실제로 밟음). `c24_finance_kcpm_harness` ⑤에 이미 있는 방식(`const strip = t => t.replace(/\/\*[\s\S]*?\*\//g,'').replace(/^[ \t]*\/\/.*$/gm,'')`)을 그대로 쓰면 됩니다. ⚠️ 긍정 검사(«이 로직이 있는가»)는 주석이 섞여도 무해하니 원본으로 두세요 |
| 사이드바 메뉴 이름을 고쳤는데 **화면엔 그 이모지가 없음** | **자식·손자 메뉴의 선행 이모지는 화면에서 벗겨집니다**(`adm-menu-icons.js`, 2026-07-22 «컬러 이모지 88개가 한 화면에 쌓여 눈이 피로하다» 결정 — 단색 선 아이콘으로 통일). 파일에 「⭐ 학생 수업 평가」 라고 적혀 있어도 화면에는 「학생 수업 평가」 로 나옵니다. ⚠️ 그래서 **파일만 보고 «이모지가 들쭉날쭉하다» 고 판단하면 안 됩니다** — 실제 화면(`.ph125-text` 의 textContent)을 재세요. ⛔ 이모지를 되살리는 방향으로 «정리» 하지 마세요(일부러 없앤 것입니다) |
| 강사에게 **남의 수업·급여가 보임** (번호로 이었을 때) | 🔴 **강사 번호는 두 갈래이고, 겹치는 자리에서 «다른 사람» 이 걸립니다.** ① D1 `teachers.id` = 지역 일련번호 **1~29** ② 카페24 강사번호 = **9~196**(`attendance.teacher_uid`·`teacher_payroll_auto.teacher_id`·카페24 `:Class`). 이름이 아니라 **숫자**라서 눈에 안 띄고, 조인해도 에러가 안 나 조용히 남의 것을 줍니다. 실측(2026-08-19): 카페24 24=**Teacher Mariane**인데 `teachers` 24=**HANNAH**, 카페24 26=**Teacher Rica**인데 `teachers` 26=**MELCA** (진짜 Hannah 는 카페24 **189**). 수업 카드엔 **학생 이름이 함께** 나옵니다. ⚠️ **하루에 세 번 밟혔습니다** — 수업 목록(PR #226→#299 수리), 급여 화면(PR #300), 그리고 `class_schedules` 씨앗 만들기(`source='attendance_seed'`, `api-admin.ts`)가 `attendance.teacher_uid` 를 `class_schedules.teacher_id`(=`teachers.id` 로 읽는 칸)에 그대로 넣습니다 — **아직 0건이라 피해는 없지만 쓰는 순간 재현됩니다.** ✅ 카페24 번호 → 사람은 **`teacher_payroll_auto` 를 거쳐서만** 해석하세요(카페24가 번호와 이름을 함께 넣은 표라 그 안에서는 안 어긋납니다). 계정 → 카페24 번호는 `teacher_profiles.korean_name/english_name` **완전일치** → `teacher_payroll_auto.teacher_name` → `teacher_id` (실측 33명 중 26명 해석, 나머지는 **안 보여주는 쪽으로** 실패). ⛔ 부분일치 금지·후보 2개 이상이면 붙이지 말 것 — 모르는 것보다 틀린 게 나쁩니다. 감시: `cafe24_sync_harness` ⑧ |
| 강사 「Today's Classes」엔 뜨는 수업인데 **화상방에 입장하면 "담당 강사로 등록돼 있지 않다"** 고 경고함 | 🔴 **같은 배정을 두 API 가 서로 다른 방식으로 확인합니다.** 강사 대시보드(`/api/teacher/portal`, `api-teacher.ts`)는 로그인 세션으로 계정→강사원부를 ① `teacher_account_links` 수동 연결 ② `class_schedules.teacher_id` 에 **로그인 계정명이 그대로 들어간 행**(teachers 원부에 이름이 없는 `mangoi_0XX` 류 계정) ③ `teachers.name` 낱말경계 일치, **셋 중 하나**로 확정합니다. 그런데 입장 검증(`/api/class/verify-room`, `api-mango.ts`)은 ③(그것도 클라이언트가 타이핑해 보낸 표시이름 문자열 비교)만 봅니다 — ①·②로 배정된 강사는 자기 목록엔 뜨는 수업인데 입장할 때마다 이 경고를 봤습니다. 게다가 `joinClass()`(`teacher.html`)가 애초에 `user_id`(uid)를 안 보내(`vc_name` 만 실어 보냅니다) userId 정확일치 경로 자체가 안 걸립니다. ⚠️ **에러가 안 납니다** — 「막지 않고 경고만」이 이 게이트의 1원칙이라 수업은 열리지만, 강사에게는 "계정을 다시 연결해 달라"는 불필요한 경고가 뜨고 사장님께는 "왜 배정이 안 됐냐"는 오보고가 올라갑니다. ✅ 2026-08-26 에 `verify-room` 이 이름 매칭에 실패했을 때 **쿠키 세션(`checkAdminSession`, 이미 `credentials:'include'` 로 오고 있었습니다)으로 teacher/portal 과 같은 ①·② 두 경로**를 한 번 더 시도하도록 넓혔습니다 — 세션이 없거나 그래도 못 찾으면 기존 폴백(경고만, 차단 안 함) 그대로입니다. ⛔ userId 비교(`row.teacher_id === userId`)만 보고 "매칭 로직이 있으니 됐다"고 넘기지 마세요 — `joinClass()` 가 그 값을 안 보내는 한 그 경로는 죽어 있는 코드입니다 |
| 손자 메뉴 이름을 짧게 줄였더니 **설명 말풍선이 사라짐** | 손자의 «보이는 글자»와 «설명 사전 조회 키(`data-gc-name`)»는 **다른 것**입니다. 사전(`GC_DESC`, `js/admin-tip-i18n.js`)에는 원본 이름이 키로 들어 있고 «…» 로 끝나는 키는 0개라, 자른 이름을 키로 쓰면 그 줄만 조용히 말풍선이 없어집니다. ✅ 이름 손질은 **표시 직전에만**(`adm-r25.js` 의 `pretty`), 키와 검색 색인은 원본 그대로. 감시는 `sidebar_three_level_harness` ⑥ |
| JS 로 관리자 화면 스타일을 얹었는데 **같은 특정성인데도 짐** | **`admin-inline-c.css` 는 `<head>` 가 아니라 `<body>` 안에 링크돼 있습니다**(admin.html 592줄, 주석에 «head 로 올리면 안 됨»). 그래서 `document.head.appendChild(style)` 로 넣은 규칙은 **문서 순서상 앞**이라, 특정성이 같고 `!important` 를 붙여도 집니다. ✅ `document.body.appendChild` 로 넣으세요(2026-08-19 손자 상자 색 후보를 화면에서 시험하다 실제로 밟음) |
| 자식 메뉴를 눌러 무언가를 폈는데 **휴대폰에서 바로 사라짐** | **자식 클릭에 드로어를 닫는 곳이 «세 곳»입니다** — `adm-s11.js`(ph97)·`adm-ia6.js`(wireDelegate)·`admin.html` 의 pointerdown 자동닫기. 셋 다 «그 클릭은 카드로 이동» 이던 시절 규칙이라, 자식 클릭으로 펴는 기능(손자 메뉴)을 넣으면 **편 것이 드로어와 함께 통째로 사라집니다.** 한 곳만 고치면 나머지 둘이 닫습니다. ✅ `adm-r25.js` 가 남기는 «방금 폈다» 표시(`window.__ph125OpenedUntil`, 800ms)를 셋이 함께 봅니다 — 새 조작을 넣을 때 그 표시를 같이 쓰세요. ⛔ 세 곳을 «항상 안 닫게» 바꾸지 마세요: 손자를 골라 화면으로 갈 때는 반드시 닫아야 합니다(열린 동안 `body` 가 `overflow:hidden` 이라 `scrollIntoView` 가 통째로 무시됩니다). 감시는 `test-harness/sidebar_three_level_harness.mjs` ⑤ 가 «다섯 곳 짝 맞음» 으로 못 박습니다 |
| 자식 메뉴를 눌러 무언가를 폈는데 **그룹째 접혀 버림** | `adm-s11.js`(ph97)는 `.ph85-sub` 클릭마다 **모든 `.ph85-group` 에서 `open` 을 벗깁니다**(「하위 항목 선택 시 모든 그룹 자동 접기」). 카드로 이동만 하던 시절엔 «사이드바 정리» 라 맞았지만, 이제 같은 클릭이 손자를 여는 클릭이라 **편 순간 사라집니다.** ph97 은 window 캡처에서 먼저 도니 그 뒤에 되돌리면 됩니다(`adm-r25.js` 의 `keepGroupOpen`) |
| 딴 페이지로 가는 사이드바 항목에 무언가를 펴 보이려 했는데 **화면이 그냥 넘어감** | `adm-ia6.js` 의 `select()` 는 `href` 항목을 만나면 **맨 앞에서 `location.href` 로 빠집니다.** 그래서 「수업 길이 변경」·「수강 운영」은 손자를 그릴 새도 없이 페이지가 바뀌었습니다. ✅ 첫 누름은 펴기, 한 번 더 누르면 이동 — 판정은 위 「방금 폈다」 표시(`__ph125OpenedEl`/`__ph125OpenedUntil`)입니다. ⚠️ 그 항목들의 손자는 **다른 문서라 화면에서 읽을 수 없어** 목록을 손으로 적습니다(`secs`). 그 id 가 그 파일에 진짜 있는지는 하니스가 파일을 열어 대조합니다 — 없으면 **에러 없이 페이지 맨 위만** 열립니다 |
| 사이드바 항목을 눌렀는데 **카드는 열렸지만 그 «칸» 으로 화면이 안 감** | **한 클릭에 스크롤 주인이 둘입니다.** `adm-s11.js`(ph97)는 `.ph85-sub` 클릭마다 **50ms 뒤 `dataset.card`(=카드)를 따로 `scrollIntoView`** 합니다(smooth 한 번 + rAF 안에서 auto 한 번). 한편 `adm-ia6.js` 의 `wireRevealOnJump` 에는 「내 목적지가 아닌 곳으로 가려는 스크롤이면 손을 뗀다」는 줄이 있어서, `openSub` 로 **카드 «안의 칸»** 을 목적지로 잡으면 ph97 의 그 카드 스크롤이 **곧바로 우리 맞추기를 죽입니다** — 1.8초 재보정이 한 번도 못 돕니다. 결과는 «칸은 `open=true` 인데 화면은 카드 맨 위»(2026-08-19 실측 1440×900: 칸 제목줄 top **1492px** = 화면 밖 592px). ⚠️ **「열렸다」와 「보인다」는 다릅니다** — `open` 만 확인하고 넘어가면 이 사고를 못 봅니다. 반드시 `getBoundingClientRect().top` 이 화면 안인지 재세요. 판정: 조상으로 오는 스크롤은 «다른 곳» 이 아니라 **«같은 목적지의 거친 판»** 이므로 양보하지 않습니다(`!this.contains(alignLead)`). ⛔ 그 조건을 지우면 그대로 되돌아갑니다 — `admin_sidebar_ia6_click_harness.mjs` 가 「조상에는 양보하지 않는다」·「연 칸을 맨 위로」 두 줄로 못 박아 두었습니다. 그동안 안 걸린 이유는 `openSub` 가 조직 그룹(칸 3개짜리 카드)에만 있어 카드를 맞춰도 칸이 화면에 들어왔기 때문입니다 — 회계 카드는 칸이 12개입니다 |
| `overflow:auto` 를 걸어 뒀는데 표가 화면을 옆으로 밀어냄 | **상자가 아니라 «조상 그리드 칸» 을 보세요.** `.admin-layout` 은 `grid-template-columns:260px 1fr` 인데 **그리드 칸의 기본값이 `min-width:auto`** 라 «내용보다 작아지지 않습니다». 그래서 `1fr` 칸(`#admin-main-scale`)이 안쪽 표만큼 부풀고, **상자도 같이 커져 «넘칠 일이 없어지니» `overflow:auto` 가 영영 발동하지 않습니다**(2026-08-18 실측: 1500px 창에서 「학생관리」 컬럼 17개·표 1,703px → 카드 1,821px, 문서가 586px 가로로 넘침. 페이지 전체에 가로 스크롤바). ⚠️ 상자에는 이미 `overflow:auto` 가 있었기 때문에 **그 상자만 들여다보면 원인이 안 보입니다** — 조상을 타고 올라가며 `min-width` 와 부모 `display` 를 같이 재세요. ✅ 고침은 칸에 **`min-width:0`** 한 줄(`#admin-main-scale`). 위 「차트가 카드 밖으로」 항목의 그리드 함정과 같은 뿌리입니다. 감시: `test-harness/admin_layout_overflow_harness.mjs` |
| 표에 전용 검색창을 달았더니 **검색창이 두 개** 보임 | `admin.html` 의 `ph-frn-filter-js` 가 **`TABLES` 에 적힌 표마다 «🔍 목록 검색» 입력을 자동으로 끼워 넣습니다.** 그 표에 서버 검색창을 새로 달면서 목록에서 빼지 않으면 둘이 같이 나옵니다(2026-08-18 「🏯 본사 관리」에서 실제로 밟음 — 헤드리스 렌더로 발견). ⚠️ 보기 싫은 것만이 문제가 아닙니다 — 이 필터의 `MutationObserver` 가 표가 다시 그려질 때마다 «화면에 있는 행» 을 자기 입력값으로 **한 번 더 거르기 때문에**, 서버가 찾아 준 행까지 `display:none` 으로 숨겨 「검색했는데 아무것도 없다」가 됩니다. ✅ 전용 서버 검색창을 만들면 `TABLES` 에서 그 표를 **빼세요**(2026-08-08 `centers-table`·2026-08-18 `hq-table` 이 같은 이유). 반대로 **서버 페이징이 없고 전부 그리는** 표라면 자동 필터가 맞습니다(`mbranches-table`) |
| 「CI 도 통과했고 하니스도 초록인데」 화면이 틀림 | **프록시가 `mangoi.ai` 를 막아 Claude 는 배포된 화면을 못 봅니다**(4-1-1). 그렇다고 화면 확인을 포기할 필요는 없습니다 — **`public/` 을 그대로 서빙하는 로컬 서버 + 컨테이너에 이미 있는 크로미움**(`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, `playwright-core` 로 구동)으로 **배포된 그 파일들을 그대로 렌더**할 수 있습니다. 로그인이 필요한 API 는 시드값을 돌려주는 스텁으로 대신합니다. 2026-08-18 에 이 방법으로 **타입체크·하니스·CI 를 전부 통과한 상태에서 버그 2건**(검색창 중복 · 학생관리 가로 넘침)을 눈으로 찾았습니다. ⚠️ 첫 화면의 «환영 안내» 오버레이(`#aw-overlay`)가 클릭을 가로막고, 카드는 `ia6-hide` 로 한 번에 하나만 보이므로 `jumpToMenu(id)` 로 여세요. ⚠️ 스텁 응답 때문에 나는 `TypeError` 를 진짜 버그로 착각하지 마세요 — **진짜 응답으로 재현되기 전엔 단정하지 말 것** |
| 헤드리스로 «버튼이 진짜 눌리는지» 확인할 때 | **위 항목의 실행편입니다.** ① `playwright-core` 는 **이 컨테이너에 없습니다**(`node_modules` 자체가 없음 — `npm ci` 미실행). 크로미움을 `--remote-debugging-port=9222` 로 띄우고 **CDP 를 직접** 말하세요 — Node 22 는 `WebSocket` 이 전역이라 라이브러리가 필요 없습니다. 클릭은 `Input.dispatchMouseEvent` 로 **실제 좌표**에 보냅니다. ② ⛔ **`window.location.replace`·`reload` 를 후킹해서 «이동했다» 를 잡지 마세요** — 최신 크롬은 `Location` 메서드 재정의를 막는데 **예외도 안 나고 조용히 원본 그대로**입니다. 그대로 믿으면 멀쩡한 버튼을 «클릭해도 아무 일 없음» 으로 **오진합니다**(2026-08-24 관리자 새로고침 버튼 확인 중 실제로 밟음). 이동 확인은 `Page.frameNavigated` 이벤트 + **클릭 후 `location.href` 를 다시 읽기**. ③ 스피드다이얼(`#mi-speed-dial`)처럼 접힌 메뉴 안의 버튼은 닫힌 상태에서 `pointer-events:none` 이라 **펼치기 전엔 클릭이 아예 안 갑니다** — 「안 눌린다」 제보의 1순위 원인이기도 합니다 |
| 휴대폰에서 사이드바 **바닥 도크의 버튼을 눌렀는데 아무 일도 안 일어남** | **모달은 열렸는데 드로어가 그 위를 덮고 있는 것입니다.** 휴대폰 드로어(`#ph85-sidebar`)는 화면을 100% 덮고 **`z-index:100001`** 인데(`admin-inline-c.css` 의 `@media(max-width:1023px)`), 관리자 모달들은 대부분 **100000** 입니다 — 계정 모달(`#ph115-modal-overlay`)·`#ph114-modal-overlay`·`#hw-modal`·`#mga-overlay`·`adm-core.js`·`adm-q6.js`·`adm-teacher-kakao.js` 가 전부 그 값입니다. 평소엔 안 걸립니다 — 자식 메뉴를 누르면 드로어가 먼저 닫히고 카드에서 모달을 열기 때문입니다. **도크(`#ph162-dock`)만은 드로어를 안 닫습니다**(`admin.html` 의 pointerdown 자동닫기가 도크를 예외로 둡니다 — 「펼치려고 누르는 것」이라). 그래서 **도크 안에서 여는 모달만** 이 함정을 밟습니다. 2026-08-19 실측(390×844): 계정 칩 탭 → `overlay.classList.contains('show')===true` 인데 `elementsFromPoint(모달중앙)` = [드로어 시계, `ASIDE#ph85-sidebar`, …] 로 모달이 3번째 → 화면상 완전히 안 보임. ⚠️ **「열렸다」와 「보인다」는 다릅니다** — `classList`·`display` 만 확인하면 이 사고를 못 봅니다. 반드시 `elementsFromPoint` 로 «맨 위에 무엇이 있나» 를 재세요. ✅ 고침은 **모달을 올리는 쪽**(100002). ⛔ 드로어(100001)를 낮추지 마세요 — 그 값은 드로어 뒷막 `#mga-overlay`(100000) 위여야 합니다. 📜 덤으로 도크에서 여는 모달은 **세로가 짧은 폰**도 함께 보세요: `align-items:center` + `overflow` 없음이면 넘친 부분이 잘려 맨 아래 버튼(로그아웃)을 못 누릅니다 — `overflow-y:auto` + 자식 `margin:auto`(flex 정렬 대신)로 풉니다 |
| 떠 있는 안내 상자가 **닫아도 안 사라짐** | **같은 `id` 의 상자가 «두 벌» 쌓였을 수 있습니다.** 만들기 함수가 «이미 있나?» 를 본 **뒤에 `await fetch`** 를 기다리면, 그 사이에 한 번 더 불린 호출도 검사를 통과해 상자가 겹칩니다. 그런데 닫기는 보통 `getElementById` 로 **하나만** 지우므로, 똑같이 생긴 상자가 아래 남아 «클릭해도 그대로» 로 보입니다(2026-08-19 수업 입장 「휴가·공휴일 안내」 — `index.html` `mangoiClassEntryNotice`). ✅ ① 닫기는 `querySelectorAll` 로 **전부** 지우기 ② 만들기는 `await` 앞에 «만드는 중» 잠금 ③ 닫기를 **인라인 `onclick` 하나에만** 기대지 말고 캡처 단계 리스너·상자 전체 클릭·`touchend` 까지 받기 ④ 안 닫혀도 스스로 사라지는 타이머(20초)를 안전망으로 둘 것. ⚠️ 화면 위에 뜬 상자는 **`document.elementsFromPoint(x,y)` 로 «맨 위에 무엇이 있나»** 를 재면 «가려서 못 누르는 것» 과 «눌렸는데 안 닫히는 것» 을 가를 수 있습니다 |
| 「내 화면에는 없는 것이 남의 화면에는 있다」 (관리자) | **먼저 «어느 계정으로 들어갔나» 를 물어보세요 — 화면 버그가 아닐 때가 많습니다.** 사이드바 맨 위 주황/보라 배너(`#ph116-role-banner`)는 아이디가 `hq_t_001`·`branch_busan`·`branch_daegu`·`agency_gn001`·`agency_sc002` 일 때만 뜨고 `admin`·`hq_exec`·`hq_mgr` 이면 **코드가 지웁니다**. 2026-08-19 실제로 밟음 — 필리핀 매니저 Karl 이 본인 계정(`mgr_karl`, 스코프 `hq`) 대신 시연 계정 `branch_busan`(스코프 `branch/부산`)으로 **8/10 부터 몇 주째** 일하고 있었습니다. ⚠️ 그 계정들은 «데모» 라는 이름과 달리 `admin_scope` 로 **실제 자료**에 연결돼 있습니다(부산 지사 실제 고객). ✅ 확인은 `SELECT username, ip, datetime(login_at/1000,'unixepoch','+9 hours') FROM admin_login_history WHERE ip=? ORDER BY login_at DESC` — 그 사람 IP 로 어떤 아이디가 들어갔는지가 제일 빠릅니다. ⛔ 시연 계정을 지우거나 잠그지 마세요 — 사장님이 화면 점검에 실제로 쓰십니다(`hq_t_001` 64회 등). 비번은 2026-08-19 에 강한 값으로 바꿨습니다(`SCOPED_DEMO`·`env.DEMO_PASSWORD`, `src/auth-admin.ts`) |
| 카톡 링크로 연 관리자 화면에서 **「로그인했는데 또 로그인하래요」** | **카카오톡 인앱 브라우저가 세션 쿠키를 돌려주지 않습니다.** 로그인 자체는 «성공» 합니다 — 2026-08-19 실측: `admin_login_history` 는 success=1 인데 그때 만들어진 `admin_sessions` 두 건의 `last_seen_at == created_at`(=한 번도 안 쓰임). 같은 폰의 크롬 세션은 40분 정상이었습니다. 그래서 `/work`·`/teacher`·`/manager` 가 «미로그인» 으로 보고 로그인 화면으로 되돌립니다. ⛔ **쿠키 설정(Domain·SameSite)으로 풀 수 없습니다** — 쿠키는 제대로 내려가고 있고 돌려주지 않는 쪽이 앱입니다. ✅ `admin/login.html` 이 UA 로 인앱을 알아채면 «크롬으로 열기»(안드로이드는 `intent://…package=com.android.chrome`) 안내를 띄우고, 로그인 성공 뒤 `/api/admin/me` 로 «쿠키가 정말 남았는지» 확인해 안 남았으면 보내지 않습니다(보내 봐야 무한 반복). ⚠️ **알림톡·카톡으로 링크를 보내는 기능을 만들 때마다 이 함정을 떠올리세요** — 받는 사람은 대부분 카톡 안에서 엽니다. 확인법: `SELECT (last_seen_at-created_at)/1000 FROM admin_sessions WHERE username=?` 가 0 이면 쿠키가 안 돌아온 것 |
| `position:sticky` 를 걸었는데 **휴대폰에서만 그냥 흘러감** | **조상이 아니라 `body` 를 보세요.** `admin-inline-c.css` 의 `@media (max-width:900px)` 에 `html, body{overflow-x:hidden !important}` 가 있습니다(「대시보드 가로 잘림 방지」). `overflow-x` 를 `hidden` 으로 정하면 **`overflow-y` 가 `auto` 로 «계산돼» body 자체가 스크롤 상자**가 됩니다. 그런데 실제로 굴러가는 것은 viewport(html) 이라 body 의 `scrollTop` 은 영영 0 이고, 그 안의 sticky 는 「자기 상자가 한 번도 안 굴렀으니 붙을 일도 없다」로 판단해 그냥 흘러갑니다. 실측(2026-08-19): 390·768px 에서 700 굴린 뒤 줄 top **−636**(실패) / 1023·1024px 은 **0**(정상) — `html{overflow-x:hidden}` 만 있는 데스크톱은 멀쩡해서 **넓은 화면으로 확인하면 못 봅니다.** ✅ `overflow-x: clip` 으로 그 구간만 덮으세요 — 잘라내되 **스크롤 상자를 만들지 않는** 값이라 가로 잘림 방어는 그대로입니다. ⚠️ 선택자는 `html body`(특정성 2)로. `body` 하나면 특정성이 같아 문서상 뒤에 오는 `admin-inline-c.css` 쪽 `!important` 가 이깁니다(그 파일은 `<body>` 안에서 링크됩니다). ⚠️ 옛 사파리(iOS 15 이하)는 `clip` 을 몰라 그 줄을 통째로 버립니다 → 그 기기에서는 지금과 똑같이 안 붙습니다(잘림 방어는 유지). |
| 본문 맨 위에 고정 줄을 만들었더니 **카드 제목이 그 줄에 가림** | 사이드바 메뉴는 전부 `scrollIntoView({block:'start'})` 로 뜁니다 = 「카드 맨 위를 화면 맨 위에」. 그 자리가 바로 고정 줄 «밑» 이라 **88개 메뉴 전부** 제목이 가려집니다(2026-08-04 옛 상단바에서 실제로 밟음 → 2026-08-19 경로 줄에서 재발). ✅ 호출 83곳을 각각 고치지 말고 **카드 쪽 `scroll-margin-top` 한 곳**으로 막으세요 — 정본은 `js/adm-crumb.js` 의 `--adm-jump-offset`. ⚠️ **`adm-ia6.js` 의 `topGap()` 도 함께** 고쳐야 합니다. 거기는 고른 카드에 `scrollMarginTop` 을 **인라인**으로 쓰는데 인라인이 그 CSS 규칙을 이깁니다 — 한쪽만 고치면 «사이드바로 들어간 메뉴만 가려지는» 반쪽이 됩니다. ⚠️ 높이는 **`offsetHeight`** 로 재세요. `body{zoom:1.3}` 이라 `getBoundingClientRect().height` 는 1.3 이 곱해진 값인데 `scroll-margin-top` 은 zoom «안쪽» 좌표계입니다. |
| 사이드바 「손자 메뉴」 이름과 열리는 칸이 다름 | 3단계 메뉴(`js/adm-r25.js` 의 `MAP`)는 원래 **«데모 매핑»**, 즉 카드에 그런 칸이 없어도 메뉴가 비어 보이지 않게 **이름만 지어 넣은 것**이었습니다(파일 머리말에 그렇게 적혀 있습니다). 옛 점프 방식이 «카드 안 N번째 details» 라 「학생 상세 프로필」을 눌러도 「⏰ 만료 임박 학생」이 열렸습니다. **에러가 안 나서 두 달간 아무도 몰랐습니다.** 2026-08-18 에 전부 `{ko,en,anchor}` 객체(진짜 목적지)로 바꾸고 허구 항목 54개는 지웠습니다. ⛔ 문자열 목록으로 되돌리지 마세요 — `test-harness/admin_grandchild_menu_harness.mjs` 가 FAIL 냅니다. **앵커 id 는 `admin.html` 과 `adm-r25.js` 가 짝**이라 한쪽만 고치면 조용히 옛 방식으로 돌아갑니다. ⚠️ `js/adm-q11.js` 에 같은 이름의 매핑이 한 벌 더 있지만 `showFlyoutV2` 의 `return;` 뒤라 **죽은 코드**입니다 — 거기를 고치면 화면은 그대로인데 «고쳤다» 고 착각합니다 |
| 앵커로 쓸 `id` 를 새로 달았는데 다른 기능이 깨짐 | **문서 안에 같은 `id` 가 이미 있으면 `getElementById` 는 «먼저 나오는 것» 을 줍니다.** 2026-08-18 실측: 메뉴 앵커로 `<details id="sm-bulk-msg">` 를 달았더니 그 안의 `<textarea id="sm-bulk-msg">` 와 겹쳐, `bulkCopyContacts()` 가 `<details>` 를 집어 **단체 메시지 본문이 늘 빈 문자열**이 됐습니다. 메뉴 점프는 멀쩡해서 화면에도 콘솔에도 표시가 안 납니다. `id` 를 새로 달 때는 **문서 전체 중복부터 확인**하세요(하니스가 앵커 유일성을 검사합니다) |
| JS 가 그린 사이드바 항목이 🌐 를 눌러도 안 바뀜 | 「JS 로 그린 라벨」 함정의 실사례입니다. `toggleAdminLang()` 은 **새로고침 없이 DOM 만** 갈아서, `textContent` 로만 쓴 글자는 안 따라옵니다. 그릴 때 `data-ko`/`data-en` 을 **함께 박고**, `mangoi:lang-changed` 에서 다시 그리세요. 이때 «두 번 그리지 않게» 막아 둔 표시(`__ph125`·컨테이너 존재 검사)를 함께 풀어야 다시 그려집니다 |
| `mangoi:lang-changed` 를 듣게 해 뒀는데 **한 번도 안 불림**(에러 없음) | 🔴 **관리자 화면에서 그 이벤트는 `window` 가 아니라 «`document`» 에서 발행됩니다** — `js/adm-core.js` 의 `toggleAdminLang` 이 `document.dispatchEvent(new CustomEvent('mangoi:lang-changed', …))` 이고, `CustomEvent` 는 기본이 **`bubbles:false`** 라 document 에서 쏜 것이 window 로 **올라가지 않습니다.** 그래서 `window.addEventListener('mangoi:lang-changed', …)` 는 admin 화면에서 **영원히 침묵**합니다(에러도 경고도 없습니다 — 2026-08-27 수업 관찰 목록 재렌더에서 실제로 밟음). ⚠️ 발행처가 **화면마다 다릅니다** — `adm-core.js` 는 `document`, `js/mango-i18n.js`·`js/mg-sidebar.js`·`textbook-viewer.html` 은 `window`. 그래서 「딴 화면에서 되던 코드」를 그대로 옮기면 조용히 죽습니다. ✅ **양쪽에 다 다세요**(`document`·`window` 둘 다 — 중복 호출은 다시 그리기만 하므로 무해합니다). ✅ 확인은 브라우저에서 `document.dispatchEvent(new CustomEvent('mangoi:lang-changed'))` 를 직접 쏴 보는 것이 제일 빠릅니다 |
| 카드 안 하위칸으로 점프했는데 화면이 안 움직임 | `<details>` 조상만 펴는 것으로는 모자랍니다. **탭으로 감춘 칸**(`div.ns-panel[data-nspanel]`, `display:none`)이 있으면 숨은 상자로 스크롤해 **아무 일도 안 일어납니다 — 에러도 없습니다.** 「공지 스튜디오」가 그렇습니다. 앵커의 `[data-nspanel]` 조상을 찾아 `noticeStudioTab()` 으로 그 탭을 먼저 켜세요 |
| 「그 기능이 없어요」 제보를 받았을 때 | **먼저 «어느 화면에서» 를 물어보세요 — 이미 있는데 딴 화면에 있는 경우가 많습니다.** 2026-08-19 실측: 8/19 강사 제보 12건 중 **5건이 이미 구현돼 있었습니다.** 그런데 전부 «제보자가 쓰는 화면» 에만 없었습니다 — 자동 새로고침은 `teacher.html` 에 45초로(요청은 5분), 강사 명부 검색·재직/퇴사 필터는 `admin.html` 에, KR/PH 시계는 `admin.html`·`index.html`·`parent.html` 에 있었는데 **제보자(필리핀 매니저)가 온종일 보는 `manager.html` 에만** 없었습니다. ⚠️ 이 저장소는 같은 성격의 화면이 **넷**입니다 — `admin.html`(1.3MB 전체) · `manager.html`(초경량 매니저) · `teacher.html`(강사) · `work.html`(결재 전용). 「관리자 페이지」라는 말이 사람마다 다른 화면을 가리킵니다. ✅ 제일 빠른 확인은 **스크린샷의 상단바**입니다(매니저 화면은 「My class · Approval box · home · EN · brightly · Refresh · log out」). ⛔ 없다고 단정하고 새로 만들지 마세요 — 같은 기능이 두 벌이 되면 그때부터 «화면마다 답이 다른» 사고가 시작됩니다 |
| 관리자 «쓰기» API 를 본사 전용으로 막았는데 **강사가 그대로 실행됨** | ⛔ **`canEditOrg()` 는 강사를 못 막습니다.** 그 함수는 `scope.type === 'hq' \|\| scope.type === 'none'` 에 true 를 주는데, 그 **`'none'` 이 「내부직원·교사」**입니다(`src/scope.ts` 주석에 그렇게 적혀 있습니다). 즉 지사·대리점은 막히는데 강사는 통과합니다 — 「본사만」이라고 쓴 주석과 실제 동작이 어긋납니다. 2026-08-19 교재 묶음 일괄 이름변경 API 에서 실제로 밟았고, **`admin_write_guard_harness` 가 잡아 줬습니다**(「강사가 그대로 실행할 수 있는 새 관리자 쓰기 API 1개」). ✅ 새 쓰기 API 는 첫 줄에 `const a = await getAdminActor(request, env as any); if (a.isTeacher) return json({ok:false,error:'forbidden_teacher'}, 403);` 를 따로 두세요. 그 하니스가 새 API 마다 검사하므로 «잊고 넘어가는» 일은 없습니다 |
| 「🔴 실시간 수업 현황」이 비었는데 **수업은 돌고 있음** | **그 표는 «망고아이 화상방에 붙어 있는 사람» 만 셉니다.** 카페24 예약 수업(하루 143건)은 그 방을 거치지 않습니다 — 실측(2026-08-20): `c24-*` 방에 실접속(`last_seen_at>0`)이 남은 행은 **전 기간 0건**입니다. 그래서 수업 4건이 진행 중이던 15:08 에도 화면은 «지금 진행 중인 수업이 없습니다» 라고 말했고, 사장님께는 «오늘 한가하다» 로 읽혔습니다. ✅ 같은 카드에 **예약 기준 «지금 수업»** 을 함께 그립니다(`GET /api/admin/classes-now`, `api-admin.ts`). ⛔ 접속 여부를 **방 번호로 잇지 마세요** — `c24-…` 와 `class-{예약id}-{YYYYMMDD}` 는 번호 체계가 다릅니다. 계정·이름 **완전일치**일 때만 «접속 확인», 아니면 «접속 기록 없음»(«미접속» 이라고 단정하지 않습니다). ⚠️ 이 표가 계속 «전부 접속 기록 없음» 인 것은 버그가 아니라 **수업이 카페24에서 돌고 있다는 사실**입니다 |
| 「이 학생이 수업참관에 안 떠요」 — 수업은 실제로 돌고 있음 | 🔴 **바로 위 항목의 뒷면입니다 — 그 「예약 기준 지금 수업」이 오래도록 «카페24 수업만» 보여 줬습니다.** `classes-now` 가 `attendance` 의 `room_id LIKE 'c24-%'` 씨앗만 읽고 **`class_schedules` 는 한 줄도 안 봤습니다.** 그래서 **수강신청 확정으로 만든 망고아이 수업**(`source='adm-enroll:*'`)은 카페24에 기록이 없어 그 목록에 **뜰 방법이 구조적으로 없었습니다.** 실측(2026-09-01 21:52 KST): 그 시각 돌고 있던 망고아이 수업 **8건이 전부** 빠져 있었고(최윤서·김선우·김사랑 …), 아래 목록에 이름이 쭉 보이던 학생들은 전부 카페24 수업이라 사장님 눈에는 **「다른 애들은 뜨는데 이 아이만 없다」** 로 보였습니다. ⚠️ **에러가 안 납니다** — 목록은 정상 문구로 그려집니다. ⚠️ 같은 시각 **「오늘 수업」(`classes/today`)은 이미 둘을 합쳐** 보여 주고 있었습니다(「같은 배정을 두 API 가 서로 다르게 확인」의 형제). ✅ 2026-09-01 수리 — 판정 정본은 **`src/classes-now.ts`** 하나이고 `classes-now` 가 두 갈래를 합쳐 `source`(`mangoi`/`cafe24`)·`observable` 로 실어 보냅니다. 🔴 **참관 버튼을 `live_room` 으로 가르지 마세요** — 그 칸의 뜻은 «접속이 확인된 방» 이라 **시작 직전, 아무도 안 붙은 그 순간에 버튼이 사라집니다.** 정작 그때가 「왜 아직 아무도 안 들어왔지」 하고 봐야 할 시각입니다(실사고: 21:20 수업이 강사가 들어온 21:23 까지 어느 목록에도 없었습니다). 망고아이 방은 `class-{예약id}-{YYYYMMDD}` 로 **결정론적**이라 접속 전에도 있으므로 **`observable`** 로 가릅니다. ⚠️ 그리는 화면이 **넷**입니다 — `adm-s1.js`(수업 관찰 목록)·`ghost-view.html`(수업 고르기)·`monitor-wall.html`(관제탑)·`adm-core.js`(🔴 실시간 수업 현황). 한 곳만 고치면 「화면마다 답이 다른」 사고가 됩니다. ⛔ 요일 파서를 새로 만들지 마세요 — 정본 `admDowMatches` 를 **주입**받습니다(`'Thu'`·`'목'`·`'1,3,5'`·`'tue'` 가 섞여 있어 `Number()` 비교는 반복수업을 통째로 지웁니다). ⛔ 하루치만 훑지 마세요(창이 자정을 넘습니다). ⛔ 카페24 미러가 만든 행(`notes` 의 `c24:<번호>`)은 같은 수업이 카페24 줄에도 있어 **카페24 쪽을 빼야** 두 줄이 안 됩니다. ⚠️ 이 목록은 **「끝난 뒤 5분」까지만** 남깁니다 — 그 뒤에도 방에 남아 있는 사람은 **위쪽 «화상방 접속» 목록**이 담당합니다(그 사고에서도 21:40 에 끝난 수업의 방이 21:52 까지 살아 있었습니다). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무슨 답이 나오는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 감시는 `test-harness/classes_now_mangoi_harness.mjs`(81건 — 정본을 번들해 **실제로 돌립니다**. 변이시험 4종 전부 실제 FAIL) |
| 수업이 켜져 있는데 **10분쯤 지나면 방이 목록에서 사라짐** | **`active-room:` KV 키는 WebSocket 이 붙는 순간에만 쓰이고 수업 중에 갱신되지 않습니다.** TTL 이 600초였어서 10분 넘게 안정적으로 연결된 수업은 표에서 사라졌습니다 — 하필 그 표가 «수업 종료·연장»·«Ghost 참관» 의 입구라 **정작 손봐야 할 수업일수록 목록에 없었습니다.** 실측(2026-08-19 `meet-123`): 19:58~22:04 2시간 6분 수업인데 마지막 접속이 20:00:51 → 그 뒤 약 1시간 53분간 «진행 중인 수업 없음». ✅ 2026-08-20 에 TTL 7200초로 올렸습니다. `handleActiveRooms` 가 방마다 DO 에 `/status` 를 물어 **인원 0이면 KV 키를 그 자리에서 지우므로** TTL 은 «후보 목록» 의 안전망일 뿐입니다. ⛔ 하트비트마다 KV 를 다시 쓰는 방식으로 «제대로» 고치려 하지 마세요 — 화상수업 DO(공동 금지구역)를 건드려야 하는데 사고 반경 대비 이득이 없습니다. 📌 **단, 2026-08-20 에 그 DO 에 «생존 판정 알람» 을 넣었습니다**(아래 「왜 3명이 나와?」 항목) — 사장님 승인 뒤 넣은 예외이고, 이 KV 문제와는 별개입니다 |
| GitHub Actions 가 **한 단계에서 20분째 멈춘 것처럼 보임** | **API 응답이 몇 분 지연됩니다.** `list_workflow_jobs` 가 돌려주는 step 상태는 실시간이 아니라 캐시된 값이라, 이미 끝난 작업도 «in_progress» 로 계속 보입니다(2026-08-20 실제로 오독 — 배포 게이트가 «17분째 진행 중» 으로 보였지만 실제로는 **49초에 통과**해 있었고, 전체 배포도 2분 10초에 끝나 있었습니다). ✅ 판단은 «status» 가 아니라 **각 step 의 `started_at`·`completed_at` 시각**으로 하세요. ⛔ 멈춘 줄 알고 워크플로를 다시 돌리거나 취소하지 마세요 — 배포가 두 번 나갑니다 |
| CI 가 **5초 만에 죽음** — 스텝 0개·로그 없음·연속 재현 | 🔴 **코드 문제가 아니라 «계정» 문제입니다 — GitHub Actions 지출 한도(사용량 소진).** 체크아웃조차 시작하기 전에 실패하므로 스텝이 하나도 안 남고, 체크런 output 도 비고, 로그 다운로드는 404 입니다. 2026-08-24 실측: 00:31 다른 PR CI 성공 → 00:34 배포 성공 → **00:35 부터 서로 다른 커밋·다른 PR 네 건이 전부 3~5초 즉사**(main 병합 푸시로도 안 풀림). 사장님이 지출 한도를 풀자 해결. ✅ 판별: «5초 즉사 + 스텝 0개» 는 게이트 실패와 모양이 다릅니다 — 게이트 실패는 90초쯤 돌고 로그가 남습니다. ✅ 원인 문구는 API 로 안 보이고 **Actions 실행 화면의 빨간 배너**에만 나오므로 사람이 봐야 합니다(Settings → Billing → Actions). ⚠️ **CI 만이 아니라 main 병합의 자동 배포도 같이 멈춥니다.** ⛔ 이걸 게이트 실패로 읽고 코드를 고치러 가지 마세요 — 로컬 `ci-gates.sh` 가 초록이면 코드는 무죄입니다. ⛔ 빈 커밋으로 CI 를 깨우는 것은 금지 그대로 — 한도를 푼 뒤에는 사람이 Actions 화면에서 «Re-run failed jobs» 를 누르는 것이 제일 깨끗합니다 |
| 화상수업에서 타일 하나 지웠을 뿐인데 **학생 화면에 「수업이 끝났어요」** | 🔴 **`left` 는 함부로 보내면 안 되는 신호입니다.** 1-4 의 「`dropped` 로 자동 종료하지 말 것 — `left` 만 종료」의 뒷면입니다. `public/index.html` 이 `vcRemovePeer` 를 **후킹**해 두었습니다 — `reason === 'left'` 면 10초 뒤 `remoteCount()===0` 일 때 `endOfClassFlow()` 를 돌립니다. 그래서 ① `vcRemovePeer(id, 'left')` 로 타일을 지우거나 ② `pagehide` 에서 무조건 `leave-room` 을 보내면, **교사가 폰을 잠깐 내려놓거나 학생 화면에서 유령 하나를 치운 것만으로 수업이 끝납니다**(2026-08-20 유령 청소 구현 중 실제로 그렇게 짰다가 검증에서 잡음). ✅ 타일만 지우려면 **사유를 비워서** `vcRemovePeer(id)` — `box && reason` 이 거짓이라 완전 삭제되고 후킹의 `if (reason !== 'left') return` 에도 걸립니다(`'dropped'` 로 부르면 20초짜리 «재연결 중» 유령으로 또 남습니다). ✅ `pagehide` 는 **`event.persisted === true`(bfcache = 앱 전환·화면잠금)면 보내지 마세요.** 감시는 `vc_ghost_liveness_harness` ② 마지막 두 줄 |
| 「왜 3명이 나와?」 · 「같은 사람이 두 명으로 보인다」 | 🔴 **나간 사람이 서버 방(DO)에서 안 없어진 것입니다.** 화상수업 DO 는 참가자 명단(`joinedUsers()`)을 소켓 `readyState` 로만 판단하는데, 휴대폰이 종료 신호 없이 끊기면(화면잠금·앱전환·LTE↔Wi-Fi) 서버 쪽 소켓은 한참 `OPEN` 으로 남습니다. 브라우저는 25초마다 ping 을 보내고 2회 무응답이면 스스로 끊지만 **서버에는 그 반대 방향 판정이 2026-08-20 까지 아예 없었습니다.** ⚠️ **출석 기록만 보면 멀쩡해 보입니다** — 퇴장 시각(`attendance.left_at`)은 **HTTP**(`/api/attendance/leave`)로 기록되는데 휴대폰은 **WebSocket 만 먼저** 죽습니다. 그래서 「출석표엔 정상 퇴장인데 화상방엔 계속 앉아 있는」 어긋남이 생깁니다(2026-08-20 실측: 25분간 학생 7회 재입장, 2건은 퇴장 기록조차 없음, 19:21:19~19:21:46 은 두 세션 동시 생존). ⚠️ 2차 피해가 더 큽니다 — 유령 타일은 `vcghost-` 가 아니라 **정상 `vc-video-*` id** 라 고착 워치독이 진짜 사람으로 오판해 **피어당 최대 4회 강제 재연결**을 겁니다(mesh 라 전원의 업로드가 흔들림) + 「🎤 상대 소리가 안 와요」 오경보. ✅ 지금은 DO 알람이 120초 무응답 소켓을 정리하고(`LIVENESS_*`), 화면 쪽 `js/idx-vc-dupghost.js` 가 20초 만에 먼저 치웁니다. ⛔ 무응답 임계값을 ping 주기(25초) 가까이 좁히지 마세요 — 필리핀·중국 회선에서 멀쩡한 수업이 끊깁니다. ⛔ 「이름만 같으면 지운다」로 넓히지도 마세요 — 가족 공용 계정에서 양쪽이 서로를 지우는 **무한 킥**이 됩니다(반드시 «한쪽은 붙어 있다» 는 비대칭 확인). 감시는 `test-harness/vc_ghost_liveness_harness.mjs` |
| 수업 화면에서 **한 요소만 «디자인이 따로 논다»** | **스타일이 두 세대 겹쳐 있는지 보세요.** 활성 탭이 그랬습니다 — 옛 세대 `.tab-btn.active{color:#38bdf8;border-bottom-color:#38bdf8}`(파란 «밑줄» 탭) 위에 `css/vc-refresh.css` 가 **앰버 «알약»** 을 새로 얹으면서 **옛 밑줄을 안 껐습니다.** 그래서 「앰버 상자 + 파란 밑줄」이 됐고, 라이트 테마에서는 `mango-theme-light.css` 가 **글자색만** 하늘색으로 덮어 「앰버 배경 + 파란 글자」로 더 심해졌습니다. ⚠️ **왜 2026-08-21 에야 보였나** — 교재 탭에 버튼이 없어서(2026-06-27 제거) 첫 화면에 활성 탭이 **하나도 없었습니다.** 「📖 교재」 버튼을 되살리자 수업 내내 화면에 남아 티가 났습니다. ✅ 새 디자인을 얹을 때는 **옛 규칙을 끄는 줄을 반드시 함께** 쓰세요(`border-bottom-color:transparent`). ⛔ 링을 `box-shadow:inset` 으로 그리지 마세요 — **저사양 모드(`html.lite-mode`)가 `box-shadow:none !important` 로 전부 지웁니다**(실측: 앰버 테두리가 통째로 사라짐). `outline` + `outline-offset:-1px` 은 레이아웃도 안 밀고 lite-mode 에서도 살아남습니다. ✅ 테마별 강조색은 **그 테마의 것**을 쓰세요(다크=앰버, 라이트=하늘). 감시는 `vc_ghost_liveness_harness` ④ |
| 「교재가 안 보인다」 제보를 받았을 때 | 🔴 **먼저 «그때 방에 교재가 있었나» 부터 확인하세요 — 코드 문제가 아닐 때가 많습니다.** 확인법: `mangoi.ai/api/room-media/class-{예약id}-{YYYYMMDD}` 를 열면 `pdfState.url` 이 `pdf-**1787222474267**-20.jpg` 처럼 나오는데, **그 숫자가 «올라간 시각»(ms)** 입니다. `attendance.joined_at` 과 같은 눈금이라 바로 나란히 놓고 «스크린샷 시점에 교재가 있었나» 를 가릴 수 있습니다. 2026-08-20 건이 정확히 이랬습니다 — 「교재가 안 보인다」 로 시작해 클라이언트 버그를 추적하다가, 실제로는 **수업 시작 22분 뒤(19:41:14)에야 교재가 올라온 것**이었습니다. 대기 카드는 사실을 말하고 있었습니다. ⚠️ **오독의 함정** — 「화면에 교재가 보인다 ≠ 방에 교재가 있다」. 라이브러리에서 고르면 **내 화면에 먼저 그려지고** 방 공유(`vcShareTextbook`)는 몇 초 뒤에 따라옵니다(실측 27초). 그 사이 스크린샷을 보고 「서버엔 있는데 학생만 못 받았다」고 추론하면 없는 버그를 쫓게 됩니다. ⚠️ 날짜를 틀리지 마세요 — 아직 안 열린 방은 당연히 `pdfState:null` 이라 아무 뜻도 없습니다 |
| 수업 중 「교재가 안 보인다」 · 「나갔다 바꾸면 교재가 없다」 | **탭바에 「교재」 버튼이 없었습니다**(2026-06-27 「중복」이라고 제거 → 그 뒤 교재가 «기본 화면» 이 되면서 전제가 바뀜). 칠판·동영상·게임을 한 번 누르면 **돌아올 버튼이 화면에 하나도 없고**, 유일한 입구인 「📚 교재도구」 칩은 x 659~870 인데 **폰 탭바는 375px 까지만 보입니다**(2026-08-20 헤드리스 실측 — PC 는 1040px 라 보여서 이 사고가 잘 안 났습니다). 게다가 교재 탭이 아닐 때는 「잠시만 기다려 주세요」 안내조차 안 뜹니다(`vcWaitCardSync` 는 교재 탭일 때만) → 학생에게는 **아무 설명 없이 교재가 사라진 것**으로 보입니다. ✅ 2026-08-20 에 「📖 교재」 버튼을 **탭바 맨 앞**에 복원했습니다. ⛔ 뒤로 옮기지 마세요 — 폰에서 똑같이 화면 밖으로 밀립니다 |
| `index.html` 에 한 줄 더했는데 첫 화면 예산이 FAIL | 🔴 **여유가 0 이 아니라 «194바이트» 입니다**(2026-08-20 실측: main 1,586,494B = 1549.31KB, 상한 1549.5KB). 기준선 1537KB + slack 12KB 를 main 이 이미 꽉 채웠습니다. 즉 **주석 한 문단만 넣어도 게이트가 빨간불**입니다. ✅ 화상수업·관리자 기능은 **별도 파일 + `defer`** 로 빼세요(`js/idx-vc-dupghost.js` 가 그 예 — 그래서 오경보 차단 로직이 감시견 옆이 아니라 그 파일에 있습니다). ✅ 설명이 길면 코드에는 한 줄 포인터만 두고 본문은 그 파일 머리말·작업기록에 두세요. ⚠️ `--update` 로 기준선을 올리는 것은 **마지막 수단**입니다 — 그 예산은 필리핀 회선을 지키려고 둔 것입니다. 🔴 **194바이트는 그 자체로 별건 과제입니다** — 누가 무엇을 defer 로 뺄지 사람이 정해야 합니다. 📌 **2026-08-26 재실측: 여유 104바이트**(1,599,895B / 상한 1,599,999B — 기준선이 8/21 에 1550KB 로 올라간 뒤의 값). 그날 휴대폰 수업화면 7건을 고치면서 **새 코드를 전부 defer 파일로 빼고 제자리 수정은 31바이트로 맞춰** 통과시켰습니다 — 예산을 올리지 않고도 되는 일이 아직 있다는 뜻입니다. 📌 **2026-09-08 재실측: 여유 325바이트**(1,612,986B / 하니스가 실제로 막는 선 **1,613,311B** = `Math.round(b/1024) > 1575`). ⚠️ **«상한 1,612,800B»(=1575×1024) 는 하니스의 선이 아니라 «KB 반올림 전» 의 보수적 읽기입니다** — 지금은 그 선을 **186B 넘겼는데도 초록**입니다(2026-09-08 손실 축 수리가 +302B). 즉 여유를 «116B» 로 알고 한 줄 더했다가 통과해 버릴 수도, 반대로 «아직 325B 남았다» 로 읽고 넘길 수도 있습니다. ✅ **두 숫자를 함께 보세요** — 하니스가 막는 선은 1,613,311B 이고, 그보다 훨씬 앞에서 조심하라는 것이 1,612,800B 입니다 — 화면공유 fps 상한을 걸 때 `idx-main.js` 에 한 줄도 못 넣어 defer 에서 `getDisplayMedia` 를 감쌌습니다. ⛔ 그때 `idx-x6.js`(가면, blocking)를 고칠 수 없어 **그 파일이 이미 갖고 있는 캐시 칸에 미리 값을 넣는** 방법을 썼습니다(아래 「blocking 파일을 못 고칠 때」) 📌 **재실측(2026-09-10): 1,613,125B → 여유 186바이트.** 회의방 링크 도메인 수리가 `idx-vc-room.js` 에 139B 를 더했습니다(주석을 두 줄로 썼다가 **+288B → 여유 37B** 가 되어 함정 대조가 잡았고, 한 줄 포인터로 줄여 되찾았습니다). ⚠️ **그때 KB 반올림은 1575 로 그대로여서 「예산 유지」로 보입니다** — 그것이 이 줄이 경고하는 바로 그 착시입니다. blocking 파일에 한 줄이라도 넣었으면 **바이트로 재고 이 숫자를 함께 고치세요** — 화면공유 fps 상한을 걸 때 `idx-main.js` 에 한 줄도 못 넣어 defer 에서 `getDisplayMedia` 를 감쌌습니다. ⛔ 그때 `idx-x6.js`(가면, blocking)를 고칠 수 없어 **그 파일이 이미 갖고 있는 캐시 칸에 미리 값을 넣는** 방법을 썼습니다(아래 「blocking 파일을 못 고칠 때」) 📌 **재실측(2026-09-10 두 번째): 1,613,177B → 여유 134바이트.** 음성전용 «화면 멈춤» 수리가 `idx-main.js` 에 **+52B** 를 더했습니다 — 끄는 «방법» 은 defer(`idx-vc-qlog.js`)로 위임하고 호출부만 남긴 값입니다. 구현을 그대로 blocking 에 넣었으면 **+500B 가 넘어 애초에 못 들어갔습니다.** ⚠️ 여기서도 KB 반올림은 1575 그대로라 「예산 유지」로 보입니다. 📌 **재실측(2026-09-11, 재입장 시간창 #935 뒤): **1,613,268B → 여유 43바이트**(그 PR 은 한국어 문구를 서버로 옮겨 −5B 를 벌었다가 «수업이 여러 개인 강사» 수리로 +32B 를 썼습니다).** 얼굴칸 안내 한/영 병기(`idx-main.js` +36B)와 인앱 안내 내보내기·문구(`inapp-escape.js` +28B)가 위 «화면 멈춤» +52B 와 **같은 날 각각 다른 브랜치에서** 더해졌습니다 — ⚠️ **각자 잰 「여유 122B」·「여유 134B」는 상대를 모르는 값이라 합치면 둘 다 틀립니다.** 같은 파일을 만지는 작업이 겹칠 때는 **병합한 «뒤» 에 다시 재세요.** 지금 여유는 **70바이트** — 주석 한 줄도 안 들어갑니다. 📌 **재실측(2026-09-11, 「상담」 수리 병합 뒤): 1,613,303B → 여유 8바이트.** `toKakao`·`openKakao` 의 `noopener` 수리가 들어왔는데 main 여유가 **43B** 뿐이라, **설명 주석을 한 줄도 못 넣고 코드도 공백을 걷어내 +35B 로 줄여** 겨우 들어왔습니다(주석 한 줄 = 37B 라 그것까지 넣으면 72B 로 초과). ⛔ **이 줄을 고칠 때는 169행(`window.open` 함정)의 같은 숫자도 함께** 고치세요 — 한 곳만 고치면 다음 사람이 낡은 값을 믿고 예산을 넘깁니다(2026-09-09 에 실제로 한 곳만 고쳤다가 함정 대조가 잡았습니다). 🔴 **«막는 선» 을 «넣을 수 있는 마지막 바이트» 로 읽지 마세요 — 1바이트씩 낙관적이 됩니다.** 하니스는 `Math.round(b/1024) > 1575` 라 **1,613,312B 부터 FAIL** 이고 통과하는 마지막 값이 1,613,311B 입니다. ⟹ 여유 = **1,613,311 − b**. 2026-09-11 에 제가 이 한 칸을 틀려 같은 행에 **44·9**(1,613,312 기준)를 적었고, 그 값을 믿고 9바이트를 넣었으면 1,613,312 = `round(1575.5)=1576` 으로 **그 자리에서 FAIL** 이었습니다(함정 대조가 잡아 실측으로 정정 — 43·+35·8). ⚠️ 같은 행 안에서 앞 문단이 43 이라 적고 뒤 문단이 44 라 적어도 **아무 검사도 안 걸립니다** — 숫자를 적을 때 바로 위 문단과 맞춰 보세요. 📌 **재실측(2026-09-15): blocking 1,613,295B → 여유 16바이트.** 9/11 의 8바이트에서 8B 가 늘었는데 «무엇이 줄었는지» 는 안 쟀습니다 — 숫자만 옮겨 적습니다. 🔴 **그때는 «없는 것» 이었습니다** — blocking 에 한 글자라도 넣으려면 먼저 무엇을 defer 로 뺄지 사람이 정해야 하는 상태였습니다. 📌 **재실측(2026-09-20): blocking 1,615,653B · 막는 선 1,626,623B → 여유 10,970바이트(≈10.7KB).** 🔴 **9/15 의 「16바이트」는 지금 사실이 아닙니다** — 2026-09-18 #1019 가 **기준선을 1563 → 1576KB 로 올렸습니다**(막는 선 = 기준선 1576 + slack 12 = **1588KB**, 즉 `Math.round(b/1024) > 1588` 이라 통과 마지막이 1,626,623B). ⚠️ **그런데 그 인상은 «여유가 생겼다» 가 아닙니다** — `first_paint_budget_harness.mjs` 머리말이 스스로 「이 갱신은 «문제 해결» 이 아니라 **«시간 벌기»**」라 적고, **「진짜 할 일」로 `idx-vc-*`(수업 중에만 쓰는 파일) 60KB+ 를 defer 로 내리면 기준선을 오히려 «내릴» 수 있다**고 남겨 두었습니다. ⛔ 그러니 10.7KB 를 «써도 되는 예산» 으로 읽지 마세요 — 그 인상을 적은 사람이 **되갚을 빚으로** 적어 둔 것입니다. ⛔ 그 defer 이전은 **로드 순서 변경이라 라이브 장애 전력**이 있습니다(`idx-vc-screenmode.js` 머리말) — 곁다리로 하지 말고 **별도 PR** 로. ✅ 재는 법은 «하니스를 돌려 KB 를 보는 것» 이 아니라 **판정 로직을 그대로 옮겨 바이트로** 재는 것입니다(KB 반올림이 최대 512B 를 감춥니다). ⚠️ CRLF 함정은 이번엔 **0B**(로컬 = CI)로 실측했습니다. |
| 하니스가 «객체 모양» 을 정규식으로 못 박아 두어 칸 하나 늘렸더니 FAIL | 뜻은 멀쩡한데 검사만 깨집니다. `vc_chat_blink_drop_harness` 가 `serializeAttachment({ userId, roomId, joined: false }` 를 **중괄호 안까지 통째로** 못 박아 둬서, 생존 판정용 `seenAt` 을 더하자 FAIL 했습니다(2026-08-20). ✅ 검사는 **«뜻»으로** 쓰세요 — `\{[^}]*\buserId\b[^}]*joined: false[^}]*\}` 처럼 필요한 칸이 있는지만 봅니다. 📌 **덧붙임(2026-09-02) — 같은 함정을 하루에 두 번 밟았습니다. «목록·개수» 를 못 박은 검사도 똑같이 무릅니다:** ① `KEEP_SEL = '.tp-st-badge'` 를 **정확한 문자열**로 못 박아 둬서 그 목록에 배지를 하나 더하자 FAIL ② 「세 상태의 글자색이 서로 다르다」를 **규칙 개수(`=== 3`)** 로 세어 배지 종류가 하나 늘자 FAIL. 두 번 다 **보장은 오히려 세졌는데 검사만** 깨졌습니다. ✅ 물어야 할 것은 «몇 개인가» 가 아니라 **«그 목록에 그것이 들어 있는가»**(`list.split(',').map(trim).includes(…)`)·**«이 셋이 서로 다른가»**(필요한 것만 골라 `new Set(...).size`) 입니다. ⚠️ 느슨하게 고친 뒤에는 **되돌려 보고 진짜 FAIL 이 나는지** 반드시 확인하세요. 📌 **덧붙임(2026-09-09) — 같은 실수를 «그 함정을 고치는 커밋 안에서» 다시 했습니다.** `ai_friend_correction_harness` A-7 이 못 박은 식 모양을 뜻으로 푸는 그 커밋에서, 새로 만든 하니스가 `...(usedFallback ? { … } : {})` 와 `} else if (d && d.ok && d.reply) {` 를 **또 글자 그대로** 못 박았습니다 — `&&` 로 바꾸거나 `d?.ok` 로 정리하는 **무해한 리팩터에 거짓 FAIL**(실측 1건·3건). ✅ 조립식은 **오려 내 `new Function` 으로 평가**해 «답» 으로 묻고, 분기는 **구조**(`else if` + 그 안에 `reply`)로 찾으세요. 🔴 **덧붙임(2026-09-10) — 못 박힌 것이 «숫자» 가 아니라 «설계 단정» 일 때가 더 헷갈립니다.** `listening_speed_harness` 가 「3단계(보통)는 **정확히 1.0** — 무가공 원음이다」를 등호로 못 박고 근거까지 적어 두었는데, 사장님 「아직도 말 속도가 너무 빨라. 보통을 기준으로 모두 느리게」로 보통이 **0.8** 이 되자 그 검사가 빨간불이 됐습니다. ⚠️ 이건 «검사가 헛돈 것» 이 아니라 **그 검사가 지키던 전제를 사람이 바꾼 것**입니다 — 앞의 사례들과 달리 «그냥 느슨하게» 풀면 안 됩니다. ✅ **바뀐 지시를 새 경계로 옮겨 적으세요**(여기서는 「원음보다 빠르지 않다 + 0.75 이상 — 그 밑은 시간축 늘이기 잡음」) — 경계가 사라지면 다음에 누가 1.4 로 올려도 아무도 모릅니다. ✅ 그리고 **왜 옛 단정을 버렸는지 검사 머리말에 날짜와 함께** 남기세요(안 남기면 다음 사람이 «원래 1.0 이었는데?» 하고 되돌립니다). ⚠️ **기본값을 바꾸면 그 값을 읽는 검사를 전부 다시 읽으세요** — 이번에도 「저장값 스냅」 검사가 옛 다섯 숫자를 글자로 들고 있었습니다(같은 표 「기본값을 바꿀 때는 그 값을 읽는 검사를 전부 다시 읽으세요」) |
| `index.html` 에 기능을 더했는데 하니스가 **첫 화면 무게**로 FAIL | `first_paint_budget_harness` 가 `index.html` 의 **blocking 합계**를 감시합니다. `/js/idx-main.js` 하나가 **849KB** 이고 이 파일은 **학생 29,000명 전원이 첫 화면에서 받습니다.** 여유가 12KB 뿐이라 5KB 만 더해도 넘칩니다(2026-08-19 귓속말 토스트에서 실제로 밟음). ✅ **강사·관리자만 쓰는 기능이면 별도 파일 + `defer`** 로 빼세요. `idx-main.js` 에는 «위임 5줄» 만 남깁니다. ⚠️ 그러면 **그 파일이 오기 전에 메시지가 올 수 있습니다** — 전역 큐(`window.__vcWhisperQ` 방식)에 담아 두고 로드되면서 비우세요. 큐가 없으면 «수업 중에 온 첫 메시지» 하나가 조용히 사라집니다. ⚠️ `--update` 로 기준선을 올리는 것은 **마지막 수단**입니다 — 그 예산은 필리핀 회선을 지키려고 둔 것입니다 |
| body 의 class 를 `MutationObserver` 로 지켜봤더니 **화면이 멎음** | 🔴 **이 저장소에는 body class 를 «자주 다시 쓰는» 코드가 여럿 있습니다.** 그걸 지켜보면 콜백이 쉴 새 없이 돌아 메인스레드가 잠깁니다 — 2026-08-20 실제로 밟았고, 헤드리스 크로미움이 45초 타임아웃까지 **로그 한 줄도 못 냈습니다**(그 파일의 `boot()` 만 비활성화하니 즉시 정상). `idx-vc-screenmode.js` 주석의 **2026-07-14 라이브 장애(홈 전체 먹통)** 와 같은 뿌리인데, 그쪽은 «내 콜백이 class 를 다시 써서» 재귀한 것이고 이쪽은 **읽기만 해도** 남이 계속 쓰면 같은 결과가 됩니다. ✅ «수업에 들어갔나» 같은 상태 변화는 **`showView` 를 감싸서** 그 순간에만 확인하세요(`js/idx-vc-toprow.js`·`js/idx-vc-roomcode.js` 가 그 방식). ⛔ 상주 `setInterval` 도 두지 마세요 — 홈에 머무는 학생 폰을 계속 깨웁니다. 필요하면 «전환 순간부터 30초» 처럼 **끝이 있는** 확인으로 |
| 상단바에서 버튼을 **빼냈더니** 본문이 위로 올라와 다른 것이 가려짐 | **`.toolbar` 는 «보이는 자식» 이 없으면 쪼그라듭니다.** 세로 수업화면에서 라이트·번개를 통합바 안으로 옮겼더니 상단바가 **51px → 15px** 이 되고, 본문(영상)이 그만큼 올라와 **통합바가 다시 «영상 위»** 에 얹혔습니다 — 고치려던 것이 방향만 바꿔 그대로 재발했습니다(2026-08-20 실측). ⚠️ 이 화면은 **상단바 자체가 «자리를 비워 두는 역할»** 을 합니다. 무엇을 꺼내든 `min-height` 로 그 자리를 못 박으세요(`body.vc-in-call.mg-uni-on.mg-uni-one .toolbar{min-height:56px}`). ✅ 확인은 «본문의 top» 을 재는 것이 제일 빠릅니다 |
| 독(하단 버튼)에 버튼을 하나 추가했는데 **PC 에서 개수가 안 맞음** | **`#vc-dock-func{display:none}`(특정성 1,0,0) 는 `#vc-dock button{display:flex}`(1,0,1) 를 못 이깁니다.** id 하나면 언제나 이길 것 같지만 «id+요소» 가 더 셉니다. 그래서 세로 전용으로 만든 버튼이 PC·가로에서도 나와 **버튼이 7개가 아니라 8개**가 됐습니다(2026-08-20 실측). ✅ `#vc-dock #vc-dock-func` 처럼 **부모 id 를 앞에** 붙이세요. ⚠️ 독은 폭이 `96vw` 로 꽉 차 있어 한 개만 늘어도 좁은 폰에서 줄이 넘칩니다 — 새 버튼을 넣을 때는 **PC·휴대폰가로·휴대폰세로 세 폭에서 개수를 세어** 보세요 |
| 떠 있는 창을 만들었더니 **「보이는데 안 눌린다」** | 🔴 **홈 화면의 「A.i 상담사」 위젯이 `z-index: 2147483000`** 입니다(`#mangoi-widget`·`#mangoi-toggle`). 그 위에 겹친 줄은 **클릭이 위젯에게 갑니다** — 창은 멀쩡히 보이므로 「왜 안 눌리지」가 됩니다. ⚠️ **수업 중(`body.vc-in-call`)에는 그 위젯이 숨습니다** → 수업에서만 확인하면 못 봅니다(2026-08-20 교재 페이지 창에서 실제로 밟음: 수업 밖에서만 11·12번 줄이 안 눌렸습니다). ✅ 판정은 `classList`·`display` 가 아니라 **`document.elementsFromPoint(x,y)` 로 «맨 위에 무엇이 있나»** 를 재세요 — 「열렸다」·「보인다」·「눌린다」는 다 다릅니다. ✅ 올릴 때는 **그 위젯 바로 한 칸 위(2147483001)까지만.** ⛔ 더 올리지 마세요 — `#mg-fab-wrap`(2147483200)·`#vc-reconnect-banner`(2147483646)는 그 창보다 **위에 있어야** 합니다(재연결 안내가 가려지면 수업이 끊긴 걸 모릅니다). 📜 같은 뿌리의 형제 함정: 「휴대폰 도크 모달이 드로어 밑에 깔림」 |
| 떠 있는 카드(드롭다운·설정 패널)의 **맨 아래 버튼이 노트북에서 «화면 밖»** — 스크롤도 없어 못 누름 | 🔴 **카드 높이는 «내용만큼» 늘어나는데 화면 세로는 그보다 짧을 수 있습니다 — 그리고 `overflow` 가 없으면 끌어내릴 수도 없습니다.** 2026-09-15 실측(웜업 ⋮ 설정 카드): 카드 높이가 **792px 고정**이라 아래 끝이 **853px** 인데, **1280×800·1366×768 노트북에서는 맨 아래 「연령·수준 다시 고르기」(792~834px)가 화면 밖**이라 `elementFromPoint` 가 **null** 이었습니다. ⚠️ **1920×1080 에서는 멀쩡합니다**(여유 246px) — 큰 모니터로 확인하면 못 봅니다. ⚠️ **조건에 따라 카드가 더 길어지는 경우를 빠뜨리지 마세요** — 중국어 선생님 칸이 보이는 학생은 **926px** 라 **1440×900 에서도** 잘렸습니다. ✅ 고침은 카드를 **`flex column` + `max-height:calc(100svh - <상단바>px)`** 로 묶고 **«안쪽 칸만» 굴리는 것**(`overflow-y:auto`) — 그러면 맨 아래 버튼은 그 «밖» 이라 늘 카드 바닥에 남습니다. ⛔ **카드 «전체» 에 `overflow` 를 주지 마세요** — 말풍선 꼬리처럼 카드 «밖»(`top:-7px`)에 그린 조각이 **통째로 잘립니다.** 🔴 **«`min-height:0` 을 빠뜨리면 아무것도 안 고쳐집니다»** 라고 여기 적었던 것은 **사실이 아니었습니다**(2026-09-15 함정 대조 → 변이 실측으로 정정). 실제로 카드를 붙잡는 것은 **`overflow-y:auto`** 이고(그것만 빼면 브라우저 검사 **실패 2**), `min-height:0` 만 뺀 판은 **72/0 으로 그대로 통과**합니다 — flex 자식의 «자동 최소 크기» 는 계산된 `overflow` 가 **`visible` 일 때만** 걸리기 때문입니다(CSS Flexbox 4.5). ✅ 그러니 `min-height:0` 은 **«안전벨트»** 로 적으세요(나중에 `overflow` 를 `visible` 로 되돌리는 날 그 줄이 일합니다) — ⛔ 지우지는 말되, **그것이 지금 일하고 있다고 적지도 마세요.** 📜 그리드 칸의 `min-width:0` 이 실제로 일하는 것은 그쪽에 `overflow` 가 없어서입니다 — «같은 함정» 이라고 뭉뚱그리면 이 차이가 사라집니다. ⚠️ **«지름길을 하나 더 내는 것» 만으로는 안 끝납니다** — 카드에서 그 줄을 빼 56px 짧아져도 1366×768 은 **여전히 −29px 잘림**이고 1280×800 은 여유가 **3px** 라 설정이 하나만 늘면 재발합니다(2026-09-15 실측). «입구» 와 «카드 자체의 잘림» 은 **다른 문제**입니다. ⚠️ **문자열 하니스로는 원리상 못 잡습니다** — 버튼도 값도 다 «있고» 틀린 것은 «화면에 들어오는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 검사는 **좌표를 재고 `elementFromPoint` 로 «맨 위가 그 버튼인가»** 를 보세요(「있다」·「보인다」·「눌린다」는 다 다릅니다). ⚠️ 그리고 **요소가 «없을» 때 크래시하지 않게** 하세요 — 그 버튼을 지우는 변이에서 하니스가 죽어 **결과줄조차 안 나왔습니다**(무엇이 깨졌는지 안 보입니다). ⛔ **카드 안 버튼을 «상단바로 옮겼으니» 빼지 마세요** — 늘 하던 자리에서도 찾을 수 있어야 하고, `manual/warmup-setup-browser.mjs` 가 그 버튼을 **실제로 눌러** 검사합니다. 🔗 **두 입구를 냈으면 «같은 말» 을 하는지 검사하세요** — 상단바 지름길에 옛 이름(`연령·수준 다시 고르기`)을 그대로 쓰는 바람에, 바로 전날 «교재를 암시하지 않아 아무도 못 찾는다»(#987)로 고친 그 문제가 **새 입구에서 되살아나 있었습니다.** ⛔ 기대 글자를 하니스에 손으로 적지 말고 **둘을 서로 대조**하세요 — 다만 그 대조만으로는 **«둘 다» 옛 말로 돌아가면 못 잡으니**, 「그 이름이 교재를 말해 주는가」를 **짝으로** 두세요(대조만 두었을 때 그 변이가 실제로 통과했습니다). ⚠️ 그리고 **변이를 걸 때 «주석에 먼저 먹히지 않았는지»** 보세요 — 같은 문장이 설명 주석에도 있어 `replace(…, 1)` 이 주석만 바꾸는 바람에 「검사가 헛돈다」로 **한 번 오판**했습니다(버튼 줄을 콕 집으니 실제로 FAIL). ⚠️ **띠로 감싸면 그만큼 밑을 더 가립니다** — 이 건에서 확정 띠가 56 → **175px** 가 됐고, 버튼이 «홀로 떠 있던» 시절의 `margin-top:22px` 를 띠 안에서 끄니 **156px**(폰 143px)로 줄었습니다. 다만 **가려지는 조작 수는 1~2개로 그대로**였습니다(19px 로는 하나도 덜 가려지지 않았습니다) — 「낮췄으니 해결」로 적지 마세요. 감시는 `warmup_age_level_harness` **K절**(되돌리기 방지 — 변이 **12종** 전부 실제 FAIL이고, 뜻이 같은 리팩터(`parts.length === 0`)는 통과합니다. 그중 «조건 뒤집기»(`pick.hidden = false`)가 들어 있는데, 그것은 «식 모양» 을 글자로 못 박던 동안에는 **그대로 통과**하던 변이라 그 블록을 오려 내 실제로 돌리게 고친 것입니다)과 `test-harness/manual/warmup-setup-reach-browser.mjs`(브라우저 6크기 72종 — 자동으로 안 돕니다, 사람이 부릅니다. 변이 5종 실제 FAIL · `min-height:0` 만 지운 변이는 위 🔴 대로 **안 잡힙니다**). 📄 `docs/작업기록/260915_웜업_다시고르기_찾기쉽게.md` |
| 헬퍼를 잘 넘겼는데 **앞쪽 호출은 되고 «뒤쪽만» 죽음** | **같은 함수 안에서 `var` 로 같은 이름을 다시 선언한 것입니다.** `var` 는 함수에 칸이 **하나**라, 뒤에서 `var H = 높이` 를 쓰면 앞에서 담아 둔 `var H = window.__tbfLibHelpers` 가 **그 줄부터** 숫자로 바뀝니다. 그래서 **그 줄 앞의 `H.naturalCmp` 는 멀쩡한데 뒤의 `H.courseIcon` 만** 「함수가 아님」이 됩니다 — 선언부만 봐서는 원인이 안 보입니다(2026-08-20 교재 페이지 창에서 실제로 밟음). ⚠️ 이 저장소의 화면 코드는 대부분 **ES5 스타일(`var`)** 이라 `let`/`const` 의 재선언 에러가 안 납니다. ✅ 짧은 대문자 한 글자(`H`·`W`·`L`·`T`)를 좌표·크기에 쓰지 마세요(`BW`/`BH`/`BL`/`BT`). ✅ 이런 건 **브라우저로 한 번 돌려 보면** 바로 나옵니다 — 타입체크·하니스는 전부 통과했습니다 |
| 200쪽짜리 교재를 **「묶음 나누기」 도구로 나누려 할 때** | ⛔ **안 됩니다. 재업로드 말고는 길이 없습니다.** 그 도구는 **파일 이름**으로 가르는데, 문제의 묶음들은 이름에 유닛 정보가 **한 글자도 없습니다** — 전부 `[BTS 10 Korea (Weather)] 미분류 레슨 / Slide7.JPG` 이고 그 **같은 이름이 유닛 수만큼 반복**됩니다(Phonics 는 A~Z 26번). ⚠️ 「중복 업로드」가 **아닙니다** — 이름+크기 기준 중복은 0건이고, 같은 `Slide10.JPG` 26장은 크기가 전부 다른 **다른 글자의 10번째 장**입니다. 자동 중복정리로는 한 장도 안 줄어듭니다. ⚠️ 게다가 **업로드 순서까지 뒤엉켜** 있습니다(실측: Slide15→20→26→14…). 화면은 이름순으로 정렬하니 강사가 넘기면 **여러 유닛의 Slide3 이 연달아** 나옵니다 — 「두세 배로 늘었다」 제보의 정체가 이것이고, 길기만 한 게 아니라 **수업에 못 쓰는 상태**입니다. ⚠️ 유닛 정보는 **시스템 어디에도 안 남았습니다**: `unit_no`·`description`·`uploaded_by` 전부 NULL, `r2_key` 는 `textbook-files/1780303806459-qgq1k8.jpg` 같은 난수라 원본 폴더명이 없습니다. ✅ **업로더는 2026-08-14 에 이미 고쳐졌습니다** — 숫자 폴더(`001`)를 유닛으로 인정해 「BTS 2 001」로, 한 글자 폴더(`Mangoi Phonics A`)를 그 권 이름으로 만듭니다. 지금 DB 는 **전부 2026-06-01 업로드분**이라 그 고침을 못 받은 것뿐입니다. → **원본 폴더를 그대로 다시 올리면** 한 번에 갈라집니다(33묶음을 손으로 나누는 게 아닙니다). ⚠️ 이름이 달라져 중복차단에 안 걸리므로 파일이 약 1.7만 → 3.4만 장으로 **늘어납니다** — 올린 뒤 옛 통짜 묶음을 **숨김 체크**로 정리하세요(지우지 않습니다). 📜 2026-08-19 작업기록 3-5 에 「재업로드가 필요 없다」고 적혀 있는데, 그건 **이름에 유닛이 있는 묶음에만** 맞는 말입니다 |
| 「수업 관찰」을 눌렀는데 **수업이 하나도 안 뜸** | 🔴 **그 화면은 «목록» 이 아니라 «방 하나를 보는 뷰어» 입니다.** `/admin/ghost-view.html` 은 `?room_id=` 를 읽는데, 전체 메뉴 타일(`js/idx-allmenu.js`)은 **파라미터 없이** 엽니다 → 빈 방 번호로 조회가 나가 채팅·참가자가 영원히 비고 영상 칸은 「연결 중…」에서 멈춥니다. **에러가 안 나서** 장애로 읽힙니다(2026-08-20 필리핀 매니저 제보 — 관리자 대시보드엔 보이는 수업 2건이 여기선 안 보였음). ✅ 2026-08-20 부터 방 번호가 없으면 그 화면이 **«수업 고르기»**(`#ghd-picker`)를 그립니다. ⚠️ 파라미터 이름은 **`room_id`** 입니다 — `?room=` 로 보내면 같은 증상이 그대로 재현됩니다(`manager.html` 이 실제로 그랬습니다). ⚠️ **이 화면에는 영상이 오지 않습니다**(파일에 「GM-4 단계」로 적혀 있음). 진짜 영상은 `/?observe=<방번호>`(`src/video-call-room.ts` 의 `handleJoinObserve`) 뿐이고, 동시 참관은 **4명까지**입니다(2026-09-02 에 2 → 4. 정본은 `src/video-call-room.ts` 의 `OBSERVER_MAX` 한 곳이고 관제탑 `OBS_MAX` 와 `monitor_wall_harness` ⑱이 **대조**합니다). ⚠️ 참관자 한 명 = 방 안 «각자»의 업로드 한 갈래라 무작정 올리면 강사 회선이 상합니다 — 이미 2명 이상이면 관제탑이 🎧 소리만 을 권하고 순회 참관도 그때는 소리만으로 붙습니다 |
| 하니스가 **한자 폰트(983KB) 장식 문자**로 FAIL 내는데 화면에는 그런 글자가 없음 | **`admin_lazyload_harness` ②-3 은 `<script>` 와 HTML 주석만 벗깁니다 — `<style>` 안 CSS 주석은 그대로 셉니다.** 그래서 CSS 주석에 홑낫표(U+300C·U+300D)나 전각 문자를 한 글자만 써도 그 화면이 폰트를 받는 것으로 판정됩니다(2026-08-20 `ghost-view.html` 에서 실제로 밟음 — 게다가 «쓰지 말라» 고 적은 경고 문장 자체가 다시 걸렸습니다). ✅ 주석에서는 `« »` 를 쓰세요. 부정 검사에 걸릴 문자를 «예시로» 적어야 하면 글자 대신 코드포인트로 적습니다. 📜 **같은 뿌리의 형제가 또 있습니다** — `teacher_feedback_admin_harness` 는 `!adminHtml.includes('mango-i18n.js')` 로 «관리자 화면은 그 엔진을 안 쓴다» 를 못 박는데, **주석에 그 파일 이름을 적기만 해도** FAIL 납니다(2026-08-21 실제로 밟음). ✅ 부정 검사가 걸린 이름은 주석에서 «공용 i18n 엔진» 처럼 풀어 쓰세요 |
| 화면에서 골랐는데 **그 값만 저장이 안 됨**(에러도 없음) | 🔴 **서버가 «아는 값 목록» 을 따로 들고 있습니다.** 수강신청 ⑥ 수업 기간이 그랬습니다 — `api-admin.ts` 가 `['1','3','6','12','unlimited']` 에 없는 값을 받으면 **조용히 `null`** 로 바꿔 넣습니다(「오타·옛 폼이 보낸 쓰레기 금지」로 일부러 넣은 방어라 지우면 안 됩니다). 그래서 화면 목록에 「2개월」을 더해도 **화면에서는 골라지는데 DB 에는 안 들어갑니다.** ⚠️ 실측(2026-08-20): `enrollments` 54건의 `duration_months` 가 **전부 NULL** 이었습니다 — 다만 그 칸이 생긴(8/14) 뒤 등록이 0건이라 이 화이트리스트가 실제로 값을 지운 적은 아직 없습니다. ✅ 화면 목록(`adm-core.js` 의 `durOptionsList`)과 서버 허용 범위는 **반드시 같이** 고치세요 — `enroll_priority_assign_harness` 가 「같은 12개월인가」로 둘을 함께 못 박습니다. 📜 같은 구조가 `class_size`·`assign_priority` 에도 있습니다(모르는 값은 버림) — 선택지를 늘릴 때마다 서버 쪽을 함께 보세요 |
| 결제 **상품**을 새로 추가할 때 | 🔴 **상품은 «네 곳» 에 적혀 있고, 한 곳만 고치면 에러 없이 「고를 수는 있는데 결제가 안 되는」 상태가 됩니다.** ① `src/api-pay.ts` 의 **`PRICES`** — 금액 정본이자 «아는 값 목록» 이라 여기 없으면 `unknown_program` 으로 **결제 자체가 거절**됩니다 ② `public/index.html` 의 `.product-card`(`data-program`·`data-price`) ③ `js/idx-payment-modal.js` 의 **`PROG_INFO`** — 그리고 나이·목적 카테고리와 무관한 상품이면 **`ALWAYS`** 에도 넣어야 대상 필터에서 안 사라집니다(넣지 않으면 «있는데 안 보이는» 상품) ④ `public/admin/send-pay-link.html` 의 `<option>` — 관리자 «결제 링크 보내기». ⚠️ **`ADDON_CATALOG` 는 «연장 추가옵션» 이라 별개입니다** — 단독 상품을 거기 넣지 마세요. ✅ 금액은 **서버 가격표로만** 결정됩니다(화면이 보낸 금액은 무시 — 위변조 방지). 그래서 화면 금액이 달라도 «비싸게/싸게 청구되는» 것이 아니라 **화면이 거짓말을 하는** 상태가 됩니다. ✅ 회차가 없는 상품(`pron_ai`·`ai_content` 류)은 `enroll_json` 이 없어 `enrollCreateSchedules` 가 먼저 반환하므로 `class_schedules` 를 안 건드리고 `enrollments` 행만 생깁니다. ⚠️ **`enrollments.ended_at` 은 이 경로에서 «모든» 상품이 NULL 입니다** — 「1개월」 같은 기간이 DB 로 강제되지 않습니다(이 상품만의 문제가 아니라 원래 그렇습니다. 정말 끊어야 하면 별건). ⚠️ 그리고 **«결제 선택지를 만드는 것» 과 «안 사면 못 쓰게 막는 것» 은 다른 일입니다** — 막으려면 화면 8곳에 게이트를 새로 답니다(2026-09-09 「일단 결제 선택지만 추가」로 앞만 했습니다). 감시는 `test-harness/pay_product_catalog_harness.mjs`(54종 — 네 목록을 **손으로 베끼지 않고 소스에서 꺼내** 대조하고, 「서버가 모르는 상품을 화면이 팔지 않는가」는 서버 판정을 **옮겨 와 실제로 돌립니다**. 변이 4종 전부 실제 FAIL) |
| 1.3MB `admin.html` 에 스크립트를 `</body>` 앞에 끼워 넣었더니 **페이지가 통째로 죽음** | **첫 번째 `</body>` 는 JS 문자열 안에 있습니다** — `html += '</table></body></html>'`(11495행). 파이썬·sed 로 «첫 번째» 를 바꾸면 그 문자열이 깨져 **그 뒤 스크립트가 전부 안 돕니다.** 에러는 콘솔에만 나고 화면은 «그냥 아무 일도 안 일어난» 것처럼 보입니다(2026-08-20 헤드리스 검증 중 실제로 밟음 — 30분 헤맴). ✅ 넣을 때는 **`rfind('</body>')`(마지막)** 를 쓰세요. 같은 이유로 `<head>`·`</html>` 도 첫 번째를 믿으면 안 됩니다 |
| 참관(Ghost) 중인데 **마이크·카메라 버튼이 그대로 보임** | 🔴 **숨기려던 `#vc-bottom-toolbar` 는 죽은 id 입니다** — 하단 조작은 `vc-dock.js` 의 `#vc-dock` 으로 바뀌었고 그 옛 id 는 어느 HTML 에도 없습니다. `getElementById` 가 null 을 돌려주고 조용히 넘어가 **에러조차 안 납니다.** ⚠️ 그 버튼은 장식이 아닙니다 — `vcToggleMic()` 은 오디오 트랙이 없으면 **마이크를 새로 획득해 모든 피어에 `addTrack`** 하는데 참관자는 항상 트랙 0개라 **버튼 한 번에 수업에 등장**합니다(2026-08-20 필리핀 매니저 제보 「참관 중인데 카메라가 보인다」). ✅ 지금은 `js/vc-observe-guard.js`(defer)가 `body.vc-observer` 클래스로 마이크·카메라·화면공유만 감추고 **함수 자체도 감쌉니다**(버튼을 숨겨도 옛 툴바 인라인 onclick·타일 아이콘·`vcMobileToggle*` 로 부를 수 있습니다). ⛔ 인라인 style 로 숨기지 마세요 — 독이 다시 그려지면 날아갑니다. ⚠️ **관리자 화면의 «직접 입장»(`ghEnterRoom`·`tcEnterClass`)은 참관이 아니라 실제 참가자**라 학생에게 보입니다 → `&vc_cam=off` 로 카메라를 끄고 들어갑니다(트랙은 살려 둡니다 — 이 앱은 자동 재협상이 없어 트랙이 없으면 나중에 켤 수 없습니다). 감시는 `test-harness/observer_camera_guard_harness.mjs` |
| 본문 맨 위 «경로 줄» 이 어떤 메뉴에서만 사라짐 | 🔴 **`position:sticky` 는 «자기 부모 상자» 안에서만 붙습니다.** 부모 바닥이 화면 위로 올라가면 줄도 함께 끌려 올라갑니다. 그런데 관리자 카드 87장 중 **14장이 `#admin-main-scale` 밖**(`<body>` 직속)에 있습니다 — `card-payments-b2b`·`b2c`·`report-forms`·`notice-board`·`timetable`·`lesson-log`·`gallery`·`homework`·`school-attendance-stats`·`lib-admin/teacher/branch/agency/student`. `.admin-layout` 은 `admin.html:9378` 에서 닫히는데 그 카드들은 10563줄 이후입니다. 그래서 `#mi-crumb` 이 컬럼 «안» 에 있던 동안 그 14장으로 뛰는 메뉴에서는 조금만 내려가도 줄이 통째로 사라졌습니다(2026-08-20 사장님 신고 「자료실 손자를 누르면 없어진다」). 실측 1920×1080: ▸1 관리자 자료실 −13 · ▸2 강사 −463 · ▸5 학생·학부모 −1216, 카드가 «안»에 있는 「회계」는 800px 더 내려도 0. ✅ 지금은 **`<body>` 직속**이라 부모 상자가 문서 전체입니다. 위치·폭은 손으로 맞추지 않습니다 — 컬럼을 미는 것이 body 자신의 padding 이라 저절로 같은 자리가 됩니다. ⛔ 다시 `#admin-main-scale` 안으로 되돌리지 마세요. ⛔ 그 14장을 컬럼 안으로 옮기는 쪽은 별건입니다(`adm-ia6.js` 의 `fitTail()` 이 꼬리 여백을 «body 맨 끝»에 붙이는 이유가 바로 그 14장입니다) |
| `position:sticky` 가 **좁은 화면에서만** 안 붙음 | 🔴 **조상이 아니라 `html`(루트)을 보세요.** 루트에 `overflow-x:hidden` 이 걸리면 루트가 스크롤 상자가 되고, body 의 overflow 가 뷰포트로 못 올라가 **body 가 자기 스크롤 상자**를 갖습니다. 그런데 실제로 굴러가는 것은 뷰포트라 body 의 `scrollTop` 은 영영 0 이고, 그 안의 sticky 는 「내 상자는 한 번도 안 굴렀다」로 판단해 그냥 흘러갑니다. `admin-inline-c.css` 의 `@media(max-width:900px){html, body{overflow-x:hidden!important}}` 때문에 경로 줄이 **≤900px 에서 한 번도 안 붙어 있었습니다**(실측 390·768·900px = top −1397·−1152·−1135, 901px 이상은 정상). ✅ 잘림 방어는 **`body` 에만** 남기세요 — html 이 visible 이면 body 의 overflow 가 뷰포트로 올라가 뷰포트가 대신 자릅니다(CSS 표준의 «전파»). ⛔ **`overflow-x:clip` 으로 한 축만 바꾸는 방법은 성립하지 않습니다** — 다른 축이 스크롤 값이면 표준에 따라 `hidden` 으로 계산됩니다. 2026-08-19 에 넣은 `html body{overflow-x:clip}` 이 정확히 그래서 **효과가 0** 이었고(게다가 선택자가 body 만 가리켰습니다), 2026-08-20 에 걷어냈습니다. 📜 같은 경고가 `css/mangoi-layout.css` 머리말에 이미 있었습니다 — 「⛔ html/body 에 overflow-x:hidden 을 걸지 않는다. position:sticky 가 죽는다」 |
| 헤드리스로 **홈(index.html) 스크린샷**을 찍었는데 카드가 코치마크에 덮여 있음 | **빈 브라우저는 «첫 방문자» 입니다 — 관리자의 `#aw-overlay` 와 같은 사정이 홈에도 있습니다.** `js/idx-onboard.js` 가 첫 방문에 «수업 입장은 여기!» 코치마크를 띄워 히어로 카드를 덮습니다. DOM 좌표로 재는 검사는 그대로 통과하므로 «그림만» 쓸모없어집니다(2026-09-14 레벨 카드 게이지에서 실제로 밟음 — 19/19 초록인데 스크린샷은 코치마크뿐). ✅ 찍기 전에 `localStorage['mangoi_onboard_v1']='skip:0'` 로 «본 것» 으로 표시하세요. ⚠️ 그 검사에 «맨 위가 누구인가»(`elementsFromPoint`)를 넣었다면 코치마크가 «덮는다» 로 잡혔을 것입니다 — 좌표만 재면 못 봅니다 |
| 헤드리스로 관리자 화면을 쟀더니 **멀쩡한 폭까지 실패**로 나옴 | **빈 브라우저는 «첫 방문자» 입니다.** `js/adm-welcome.js` 의 «환영 안내»(`#aw-overlay`)가 열릴 때 `html{overflow:hidden}` 을 걸고 **사람이 닫아야만** 풉니다. 그래서 스크롤·sticky 를 재는 검사가 통째로 거짓 실패를 냅니다(2026-08-20 실제로 1023px 을 「깨졌다」고 읽고 원인을 한참 잘못 쫓았습니다). ✅ 재기 전에 `localStorage['mangoi_admin_welcome_v1_done']='1'` 로 «본 것으로» 표시하고 여세요(실제 사용자는 한 번 닫으면 다시 안 뜹니다). ⚠️ 첫 화면 오버레이가 클릭을 막는 문제는 이미 2장에 있는데(`#aw-overlay`), **스크롤까지 막는다**는 것이 새로 확인된 부분입니다 |
| 화면 «좌표» 가 틀린 버그를 하니스가 못 잡음 | **문자열을 찾는 검사로는 「어디에 그려졌나」를 볼 수 없습니다.** 2026-08-20 경로 줄 사고 때 타입체크·회귀 하니스 211개·CI 게이트가 **전부 초록불**이었습니다. ✅ 실제 Chromium 에 그려서 `getBoundingClientRect()` 로 재는 검사가 `test-harness/manual/crumb-sticky-browser.mjs` 입니다(29건 — 항목 43개 × 누른 직후·맨 아래, 손자 5개, 폭 6가지). ⚠️ **자동으로 안 돕니다** — `manual/` 규약상 `*_harness.mjs` 가 아니라 게이트가 물어 가지 않습니다. 경로 줄·sticky·`overflow`·레이아웃을 건드리면 **사람이 불러야** 합니다: `PW_DIR=/tmp/pw node test-harness/manual/crumb-sticky-browser.mjs` |
| 관리자 버튼에 색을 줬는데 **화면에는 흰색·파랑으로 나옴** | 🔴 **인라인 `style="background:…"` 는 세 겹에 먹힙니다.** ① `admin-inline-c.css` 3279행 `details.menu-card button:not(…)` 가 **파랑 그라데이션 `!important`** ② 같은 파일 9072행 `html[…ivory][…slate] [id^="card-"] button:not([class])` 가 **`#ffffff !important`**(근거가 «클래스가 없다 = 의도한 색이 없다» 인데, 색을 인라인으로만 준 버튼에서 그 전제가 틀립니다) ③ `js/adm-light-surfaces.js` 페인터가 «어두운 배경» 을 **인라인 `!important` 로 밝게** 눌러 버립니다. 2026-08-21 실측: 「🔴 실시간 수업 현황」 버튼 **네 개가 전부 흰색**이었고, 그래서 «강제 종료»(수업 전체 연결 해제)의 빨강도 안 나오고 있었습니다. ⚠️ **코드만 보면 색이 있는 줄 압니다** — 그래서 2026-08-20 에 «참관=보라·직접입장=주황으로 갈라 뒀다» 고 적은 것이 화면에서는 사실이 아니었습니다. ✅ 고치는 순서는 셋 다입니다: **클래스를 달고**(②를 벗어남) → **CSS 를 파일 맨 끝에 «특이성 꼬리»(`:not()` 5개 이상, (0,8,1))로** 적고(①을 이김 — «맨 끝 + `!important`» 만으로는 (0,5,2)에 집니다) → **페인터 `SKIP_SEL` 에 등재**(③). ⛔ 클래스 이름을 «-btn» 으로 끝내지 마세요(`[class$="-btn"]` 규칙 3894·9012행에 다시 걸립니다). ⛔ 색을 JS 인라인으로 되돌리지 마세요. ✅ 판정은 코드가 아니라 **브라우저 `getComputedStyle`** 로 하세요 — 배경이 그라데이션이면 `backgroundColor` 는 **투명**으로 나오니 `backgroundImage` 도 함께 재야 합니다(그것 때문에 한 번 오독했습니다). 감시는 `observer_camera_guard_harness` F절 |
| 헤드리스로 «수업 입장» 을 재려는데 **버튼은 눌리는데 아무 일도 안 일어남** | 🔴 **`js/mango-consent.js` 가 `vcJoinRoom` 을 «감싸고» 있습니다.** 학생 계정에 동의 기록이 없으면 입장 전에 촬영동의 모달을 띄우고 **사람이 누를 때까지 기다립니다** — 그 앞에서 멈추므로 `getUserMedia` 0회·TURN 0회·`vc-in-call` 없음이 되어 «입장이 통째로 깨졌다» 로 오독하게 됩니다. ⚠️ **에러가 안 납니다**(감싼 쪽이 try/catch 로 삼킵니다) — 콘솔도 화면도 조용해서 엉뚱한 곳을 뒤지게 됩니다(2026-08-22 실제로 한참 헤맴). ✅ 계측 전에 `/api/consents/<uid>` 를 «이미 동의함»(`user_id` 가 있는 행)으로 스텁하거나 `window.mangoConsentEnsure` 를 대체하세요 — `test-harness/manual/vc-entry-speed-browser.mjs` 가 그 예입니다. ⚠️ 그 전에 **`file://` 로 열지 않았는지**부터 확인하세요(2장 별도 함정) — `<script src="/js/…">` 가 전부 404 라 `vcJoinRoom` 자체가 `undefined` 이고, 증상이 이것과 똑같이 «아무 일도 안 일어남» 입니다. `cd cloudflare-deploy/public && python3 -m http.server` 로 띄우세요. ⏳ 입장 경로(`vcJoinRoom`·`vcEnsureMediaPermission`·`acquireLocalMedia`·`vcEnsureIceServers`)나 고착 복구(`vcStuckPeerWatch`)를 건드리면 **사람이 불러야** 합니다 — `manual/` 은 게이트가 안 물어 갑니다: `PW_DIR=/tmp/pw node test-harness/manual/vc-entry-speed-browser.mjs` · `PW_DIR=/tmp/pw node test-harness/manual/vc-stuck-relay-browser.mjs` |
| 값을 넣었는데 **DB·전송 payload 에는 늘 빈 값**(에러 없음) | 🔴 **`let`·`const` 는 `var` 와 달리 `window` 에 속성을 만들지 않습니다.** 그래서 `let x` 로 선언된 값을 딴 곳에서 `window.x` 로 읽으면 **항상 `undefined`** 인데, 옵셔널 체인이나 `|| ''` 를 거치며 조용히 빈 값이 됩니다 — 에러도 경고도 없습니다. 2026-08-21 실측: 회선품질 로그가 `window.vcRoomId` 를 읽었는데 방 번호는 `let vcRoomId`(idx-main.js 715행) → **675건 중 673건(99.7%)에 방 번호가 없었고**, 그 사실을 한 달간 아무도 몰랐습니다. 대체 경로로 적어 둔 `window.currentRoomId` 도 **어디에도 대입되지 않는 이름**이라 같이 undefined 였습니다. ✅ 같은 스크립트 안이면 **어휘 바인딩을 그대로**(`vcRoomId`) 쓰세요. ✅ 확인은 브라우저에서 `typeof window.그이름` 을 재는 것이 제일 빠릅니다 — 소스만 봐서는 `window.` 접두사가 틀렸다는 게 안 보입니다. ⚠️ 「저장이 안 된다」류 제보에서 **서버·DB 를 뒤지기 전에 payload 부터** 확인하세요. 감시는 `vc_quality_log_harness` |
| 짧은 라벨 한 줄에 `display:flex` 를 썼더니 **낱글자로 쪼개짐** | **flex 는 «글자마디»와 `<b>` 를 각각 아이템으로 만듭니다.** 좁은 폭에서 아이템마다 따로 줄바꿈되어 「게임 <b>1개</b> 클리어하면 열려요」가 **「게 1 클리어하면 열 / 임 개 려요」**로 나옵니다(2026-08-22 게임 카드 해금 안내에서 실측 — 390px). ⚠️ **문자열 하니스·타입체크로는 절대 안 보입니다** — 클래스도 글자도 다 맞고 «줄이 어떻게 나뉘었나» 만 틀립니다. ✅ 아이콘과 글자를 «가운데 정렬» 하려고 flex 를 쓰고 싶어도, 그 안에 **글자가 흐르는 문장**이 있으면 쓰지 마세요 — 보통 텍스트 흐름(`display:block`)에 두고 아이콘은 글자 앞에 그냥 붙입니다. ✅ 확인은 브라우저에서 **`rect.height / lineHeight` 로 줄 수를 세는 것**이 제일 빠릅니다. 감시는 `test-harness/manual/game-lock-preview-browser.mjs`(사람이 직접 부르는 브라우저 검사) 📌 **2026-09-03 재발** — 관리자 자료실 설명 줄 `.lib-note` 가 같은 이유로 문장을 좁은 «단» 셋으로 쪼개고 있었습니다(1280px 실측: `<b>원본 화질 그대로</b>` 가 폭 72px·2.6줄, 「인쇄해서 차에 두는 용도」가 3.9줄. `<b>` 가 든 note 7곳). `display:block` 으로 되돌리니 각각 1.2줄로 펴졌습니다. ⚠️ **「flex 를 빼면 아이콘 정렬이 깨진다」는 걱정은 재 보면 근거가 없었습니다** — 아이콘이 «별도 요소» 가 아니라 텍스트 노드 안의 이모지(`ℹ️ 자료를…`)라 자식이 하나뿐이고, 자식이 하나면 `gap` 은 아무 일도 안 합니다(`align-items` 는 익명 아이템에 걸리기는 하나 결과가 같습니다). 잰 것은 여기까지입니다 — 되돌린 뒤 `<b>` 가 없는 note 다섯 개의 폭·높이(733×24)가 **1px 도 안 바뀌었습니다.** ✅ **지우기 전에 그 속성이 실제로 무엇을 하고 있는지 재세요** — 여기서는 아무것도 안 하고 있었습니다 |
| 버튼에 `title` 로 설명을 달았는데 **폰에서는 안 뜸** | **`title` 툴팁은 마우스 전용입니다** — 터치 기기에는 «마우스를 올린다» 는 동작 자체가 없어서 웹 표준상 뜨지 않습니다. 그렇다고 «탭하면 뜨게» 도 못 만듭니다 — 그 탭은 이미 버튼의 동작을 실행합니다. ⚠️ 학생 29,000명 대부분이 폰으로 봅니다 — PC 에서 확인하면 «달았다» 로 끝나고 **정작 볼 사람에게는 아무것도 안 보입니다.** ✅ 같은 글을 **화면에 보이는 줄로 한 번 더 그리고**, 마우스가 있는 넓은 화면에서만 CSS 로 감추세요(`@media(hover:hover) and (pointer:fine) and (min-width:641px)`). ⚠️ 폭 조건을 함께 두는 이유는 **헤드리스·데스크톱 창은 좁혀도 `hover:hover` 로 보고**해서, hover 조건만으로는 «폰 폭에서 보이는가» 를 잴 수 없기 때문입니다 — 진짜 확인은 playwright `isMobile`·`hasTouch` 문맥에서 `matchMedia('(hover:none)')` 이 참인지까지 보세요. ⛔ 설명 줄을 감싸는 상자에 `display:flex` 를 쓰지 마세요(같은 표의 「낱글자로 쪼개짐」 함정). 감시는 `test-harness/manual/game-lock-preview-browser.mjs`(사람이 직접 부르는 브라우저 검사) |
| 「이 창이 떴나」를 **함수를 감싸서** 판정했더니 늘 «떴다» 로 나옴 | 🔴 **그 함수가 창을 «안 띄우고 그냥 돌아오는» 경우가 대부분일 수 있습니다.** `mangoConsentEnsure`(촬영동의)가 그렇습니다 — 이미 동의한 학생(=거의 전원)에게는 `askedBefore()`·`fetchExisting()` 에서 **모달 없이 즉시 반환**합니다. 그걸 감싸 「동의창이 떴으니 덮개를 걷자」로 썼더니 **정상 입장마다 240ms 에 덮개가 걷혀** 고치려던 로비가 그대로 다시 보였습니다(2026-08-23 실측 — 로그로 `t=240 cover=0` 을 찍고서야 보였습니다). ⚠️ **문자열 하니스·타입체크는 이걸 못 봅니다** — 함수는 «불렸고» 코드도 «맞습니다». ✅ 판정은 «함수가 불렸나» 가 아니라 **«그 상자가 화면에 붙었나»** 로 하세요 — `MutationObserver` 의 `childList` 로 그 id 가 붙는 순간을 봅니다(⚠️ body **class** 감시와는 다릅니다. 그건 홈 전체를 멎게 한 전력이 있습니다). ✅ 확인은 브라우저에서 `elementsFromPoint` 로 «맨 위에 무엇이 있나» 를 재세요. 감시는 `test-harness/manual/vc-fastentry-browser.mjs`(사람이 직접 부르는 브라우저 검사 34종) |
| 단계·임계값을 새로 넣었는데 **그 단계가 한 번도 안 쓰임** | 🔴 **같은 주기로 도는 판정이 둘이면 «어느 쪽이 먼저 오는지» 틱 수로 세어 보세요.** 화상수업 적응 루프가 그랬습니다 — 둘 다 **4초** 주기인데 ① 화질 낮추기는 한 틱에 한 단계라 마지막 단계까지 **4틱(≈16초)**, ② AAO(영상 완전 끔)는 **3틱(≈12초)**. 그래서 2026-08-21 에 「완전 꺼짐」 앞에 넣은 «얼굴만» 4단계가 **급격한 붕괴에서는 한 틱 차이로 영영 안 쓰였습니다**(서서히 나빠질 때만 쓰임 — 영상은 손실 6%부터, 오디오는 12%부터 반응하므로). ⚠️ **하니스도 브라우저 렌더링도 이걸 못 잡습니다** — 단계는 «존재하고» 값도 «정확»합니다. 닿지 않을 뿐입니다. ✅ 고침은 「느린 쪽을 앞당기기」(오디오가 2틱 나빠지면 그 자리에서 최저 단계로) + 「빠른 쪽을 조건부로」(최저 단계를 실제로 써 봤을 때만 끔). ⛔ 뒤쪽 판정을 **무조건** 늦추지 마세요 — 최저 단계에 못 닿는 상황에서 AAO 가 영영 안 걸려 **오디오까지 죽습니다.** 반드시 탈출구를 두세요(`sev>=5` ≈20초면 무조건 끔). 감시는 `vc_lowstep_relay_harness` ⏱절 |
| 버튼을 눌러도 **소리가 안 멈춤**(무음이 안 먹음) | 🔴 **`pointerdown` 자동정지와 `click` 토글이 서로를 뒤집은 것입니다.** 게임 허브(`student-games.html`)의 안내음성은 «화면 아무 데나 누르면 멈춤»(document `pointerdown`)과 «버튼을 누르면 토글»(`click`) 두 처리를 함께 씁니다. `pointerdown` 이 `click` 보다 **먼저** 오므로 무음 버튼을 누르면 ① 자동정지가 `pause()` + `_hubIntroDismissed=true` 로 만들고 ② 곧이어 토글이 그 플래그를 뒤집어 «지금 꺼져 있으니 켜자» 로 읽고 **그 자리에서 다시 재생**했습니다. 몇 번을 눌러도 소리가 계속 나고 버튼도 🔊 그대로라 **멈출 방법이 아예 없었습니다**(2026-08-23 사장님 제보. 헤드리스 실측: 세 번 눌러도 `paused:false`, `currentTime` 2.27→4.58 계속 증가). ✅ 고침은 두 가지를 함께 — 자동정지에서 **그 버튼만 예외**로 두고(`e.target.closest('#hub-audio-mute-btn')`), 토글은 플래그를 뒤집지 말고 **«지금 소리가 나고 있나»(`!a.paused`)로 다음 상태를 정하기**(딴 곳이 플래그를 먼저 건드려도 「누르면 멈춘다」가 안 깨집니다). ⚠️ **문자열 하니스로는 절대 안 보입니다** — 함수도 조건도 값도 다 «있습니다». 틀린 것은 «순서» 뿐입니다. ✅ 이런 건 진짜 브라우저에서 눌러 보고 `audio.paused`·`currentTime` 을 재세요 — `test-harness/manual/game-hub-audio-mute-browser.mjs`(사람이 부르는 검사 10건, 자동재생 허용·차단 두 상태). ⚠️ 자동재생이 막힌 첫 방문에서는 **첫 클릭이 «되듣기»** 라 음소거로 삼키면 안 됩니다(`_hubAudioResumedAt` 400ms 창) |
| 관리자 카드 안 **글자**에 색을 줬는데 화면에는 흰빛·회청색으로 나옴 | 🔴 **위 「관리자 버튼에 색을 줬는데」 함정의 «글자» 판인데, 여기엔 겹이 하나 더 있습니다.** ① `admin-inline-c.css` 8743·8744 행이 `[id^="card-"]` 안 글자를 `#101828 !important` 로 덮고 ② `.kpi-tile *` 가 `rgb(249,250,251) !important` 로 또 덮고 ③ `js/adm-s13.js` 의 `fixTextOnDark()` 가 **인라인 `!important`** 로 한 번 더 칠합니다. 2026-08-23 실측: 신한 계좌 화면의 빨강 `#b91c1c` 가 **`rgb(248,250,252)`(흰빛)** 로 나왔습니다. ✅ 고침은 **클래스로 주고 CSS 파일 맨 끝에 «조상 id 를 앞에 붙여»** 정의하기(`#acc-bankacct .bk-…` = ID 한 칸 → 클래스를 아무리 쌓은 규칙도 못 이깁니다). ⚠️ **③은 «가독성 구제» 라 이기려 들면 안 됩니다** — 대비가 3 미만일 때만 도는 코드이고, KPI 타일은 테마에 따라 어두운 면이 되므로 그 위의 빨강은 **실제로 안 읽힙니다.** `adm-light-surfaces` 의 `SKIP_SEL` 에 넣었다가 되돌렸습니다(예외로 빼면 안 읽히는 색이 그대로 박힙니다). ✅ 어두운 면이 될 수 있는 자리는 **색에 기대지 말고 ▲▼ 같은 기호가 뜻을 지고 가게** 하세요. ✅ 판정은 코드가 아니라 브라우저 `getComputedStyle` 로. 감시는 `test-harness/manual/bank-expenses-browser.mjs`. 🔴 **덧붙임(2026-09-03) — «범인은 페인터» 라고 단정하지 마세요. 페인터가 아니라 «순수 CSS 특이성» 인 경우가 있고, 그때는 고치는 방법이 다릅니다.** 자료실 「NEW」 배지가 그랬습니다 — `.lib-doc-t span{color:#2563eb!important}` 가 있는데도 검정(`#101828`)이었습니다. 이 줄만 읽고 페인터 셋을 뒤지려 했지만, 브라우저로 재 보니 **인라인 우선순위가 비어 있었습니다**(`el.style.getPropertyPriority('color') === ''`) — 즉 **아무도 인라인 `!important` 를 안 썼고**, `[…ivory][…slate] [id^="card-"] :is(…)`(클래스 10)가 `.lib-doc-t span`(클래스 2)을 **특이성으로** 이긴 것뿐이었습니다. ⛔ **이런 규칙을 «행 번호» 로 가리키지 마세요** — 이 파일은 계속 자라서 곧 어긋납니다(처음 적은 5014·8239·8843 이 그때부터 **12줄씩 틀린 채 네 곳에 복제**돼 있었습니다 — 2026-09-09 함정 대조가 잡음). **선택자 문자열**로 가리키세요. ✅ **가르는 방법은 한 줄입니다** — `getPropertyPriority('color')` 가 비면 CSS 싸움(꼬리 규칙으로 해결), `'important'` 면 페인터(세 곳 등재로 해결). 이걸 먼저 재세요. ✅ 특이성으로 이길 때 **조상 id 를 나열하는 방식이 늘 맞지는 않습니다** — 자료실은 카드가 `card-lib-*` 로 다섯이라 나열하면 카드가 늘 때 조용히 빠집니다. 그때는 **«특이성 꼬리»** 로 일반 규칙을 만드세요(`:not()` 을 쌓아 클래스 수를 올림 — 자료실은 **(0,12,4)** 로 그 두 규칙(클래스 10·9)을 둘 다 이깁니다. ⚠️ 처음엔 11 이었고 2026-09-06 에 `:not(.lib-lvl)` 이 붙어 12 가 됐습니다 — **꼬리를 늘리면 그 숫자를 적어 둔 곳을 전부 함께** 고치세요). ⚠️ **`[data-admin-tone="slate"]` 를 넣어 좁히면 안 됩니다** — slate 에서는 이기지만 tone 이 slate 가 아닐 때 tone 미지정 규칙이 그대로 먹습니다. ⚠️ 그때 **«특이성에서 지는 것» 이 아니라 «선택자가 매치조차 안 되는 것»** 입니다(넣으면 (0,13,4)라 숫자로는 이깁니다) — 근거를 헷갈려 적으면 다음 사람이 산술을 의심하게 됩니다. ⚠️ **문자열 하니스로는 못 잡습니다** — 규칙도 값도 다 «있고» 틀린 것은 «누가 이기는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 감시는 `test-harness/manual/library-badge-color-browser.mjs`(브라우저 — 자동으로 안 돕니다, 사람이 부릅니다). ⚠️ 그 검사처럼 **파일을 잠깐 고쳤다 되돌리는 변이시험은 «되돌리는 코드» 를 «건너뜀» 검사보다 «앞» 에 두세요** — playwright 가 없는 기계에서 종료코드 0 으로 나가 버려 **잘린 파일을 안 고친 채 «건너뜀»** 이 됩니다(「확인 안 한 것」이 「문제없음」으로 위장 — 2026-09-09 실측으로 재현·수리). ⚠️ 그 검사처럼 **파일을 고쳐 다시 재는 변이시험은 «잴 때마다 새 컨텍스트» 를 열어야** 합니다 — `Network.setCacheDisabled` 만으로는 **부족했습니다**(2026-09-03 실측: 캐시를 껐는데도 되돌려도 초록. 같은 페이지를 재사용하면 렌더러가 이미 읽어 둔 CSS 사본을 그대로 씁니다). 둘 다 하세요. ⚠️ 그리고 자료실 목록은 비밀번호 잠금(`.lib-files{display:none}`)이라 그냥 열면 배지가 «안 보임» 으로 나옵니다 — «보이는 배지 0개면 FAIL» 을 검사 한 줄로 박아 두세요. 🔴 **덧붙임(2026-09-09) — 「다크 테마에서는 원래 색이 그대로니 괜찮다」로 위안 삼지 마세요.** `admin.html` 은 **ivory 고정**입니다 — 283행이 부팅에서 `data-admin-theme='ivory'` 를 하드 설정하고 287행 `toggleAdminColor` 도 다시 `'ivory'` 로만 설정하며(주석 「단일 테마 — 전환 없음」), 다크로 가는 유일한 길인 `applyAdminColor('dark')`(`adm-s13.js`)는 **저장소 전체에서 호출부 0건**입니다(2026-09-09 실측 — `grep -rn "applyAdminColor("`). ⟹ ivory 규칙으로 색을 바꾸면 **그 색이 화면의 전부**이고 «원래 색» 은 어디에도 없습니다. 그래서 가독성 때문에 지시와 다른 색을 쓸 때는 「어두울 땐 원래대로입니다」를 근거로 삼지 말고 **바뀐 색 자체를 사람에게 알리세요**(밝은 금색 `#fbbf24` ↔ 탄 주황 `#b45309` 는 눈으로 확연히 다릅니다). ℹ️ `admin/weekly-schedule.html`·`admin/capitown-settlement.html` 은 자기 화면에서 다크를 설정하지만 별개 페이지입니다 |
| 관리자 화면의 색을 **카드 «밖»에서 쟀더니** 멀쩡한 답이 나옴 (그리고 그 답이 거짓) | 🔴 **이기는 규칙이 `[id^="card-"]` 를 요구하면, 카드 밖에 붙여 잰 값은 «그 화면의 색» 이 아닙니다.** 2026-09-02 실측: 강사 상태 배지(`.tp-st-badge`)를 `document.body` 에 붙여 재고 「빨강 `rgb(153,27,27)` 으로 정상」이라 보고했는데, **카드 «안» 에 넣고 다시 재니 `rgb(16,24,40)` 검정**이었습니다 — `html[data-admin-theme="ivory"][data-admin-tone="slate"] [id^="card-"] .sub-body :is(p,span,div,…){color:#101828!important}` 가 카드 안에서만 걸리기 때문입니다. ⚠️ **잘못된 «정상» 판정이 잘못된 «고장» 판정보다 나쁩니다** — 사장님께 「이미 고쳐졌다」고 두 번 말씀드렸고 그때마다 다음 사람이 다시 안 봅니다. ✅ 잴 때는 **그 표·그 카드 안에 실제 렌더러 경로로 그려서** 재세요(`jumpToMenu('card-…')` 로 카드를 열고 `!!el.offsetParent` 도 함께 확인 — IA6 가 한 번에 한 장만 보여 줍니다). ✅ 이기는 규칙을 찾을 때는 `document.styleSheets` 를 훑어 `el.matches(r.selectorText)` 로 대조하세요 — ⚠️ 그때 **`@media`·`@supports` 안쪽까지 재귀**해야 하고(안 하면 규칙 0건으로 나옵니다), 상속만으로 눌린 것이 아닌지 보려면 **인라인 값과 계산값을 나란히** 찍으세요. 🪤 **덧붙임(2026-09-09) — 그 훑는 코드에 «규칙을 한 개도 못 보는» 함정이 있습니다.** `if (r.cssRules) { 재귀; continue; }` 로 «그룹 규칙인가» 를 먼저 가르면 **아무것도 안 나옵니다** — Chrome 112+ 는 CSS 중첩 때문에 **평범한 style 규칙에도 (비어 있는) `cssRules` 가 달려 있어** 언제나 참입니다. 실측: 이기는 규칙이 분명히 있는데 결과가 `[]` 였고, 그래서 «겨루는 규칙이 없다» 고 **틀리게 결론**낼 뻔했습니다. ✅ **`selectorText` 가 있는지 먼저** 보고(있으면 style 규칙), 없을 때만 `cssRules` 로 재귀하세요. ✅ 그리고 결과가 «0건» 이면 그것을 «경쟁자 없음» 으로 읽지 마세요 — **계산된 `display` 가 그 요소의 기본값과 다른데 규칙이 0건이면 훑는 코드가 헛돈 것**입니다(그 대조가 이번에 잡아 줬습니다). 📄 `docs/작업기록/260902_강사명부_색과버튼_퇴사강사_파란불.md` |
| 관리자 화면에 **테마별 «불투명» 색을 짝으로** 줬더니 한 테마에서 «짝짝이» 가 됨 | 🔴 **`/admin/` 하위 페이지는 `data-admin-theme` 이 `dark` 여도 화면이 밝습니다** — 범인은 페인터가 아니라 **`css/adm-light-theme.css` 가 `:root` 의 `--bg2`·`--bd` 를 `!important` 로 덮는 것**이라 테마와 무관하게 무조건 적용됩니다(2026-09-11 실측: `?nolight=1` 로 페인터를 꺼도 셀 배경 `rgb(230,236,244)` 그대로). 그래서 규칙서대로 ivory/다크 두 벌을 만들면 **내가 준 다크 값만 진짜 어둡고 나머지는 밝은** 상태가 됩니다. ⚠️ 처음에 이 자리를 «페인터 탓» 으로 적었다가 함정 대조가 잡았습니다 — `?nolight=1` 로 껐다 켜 보면 1분에 갈립니다. 2026-09-11 실측(`admin/weekly-schedule.html` 일간 뷰 줄무늬): `data-admin-theme="dark"` 인데 홀수 행 `rgb(230,236,244)`(밝음) 대 짝수 행 `rgb(23,29,46)`(내 다크 값) = **대비 14.12** — 얼룩말이 아니라 **«검은 줄이 번갈아 들어간»** 화면이었습니다. ⚠️ **ivory 로만 확인하면 절대 안 보입니다.** ✅ 고침은 «지금 화면이 무슨 색이든 그보다 조금 어둡게» 인 **반투명 한 벌**(`rgba(15,23,42,.045)` 류) — 테마 분기가 사라져 «두 곳을 짝으로» 관리할 짐도 없어집니다. ✅ **페인터와도 공존합니다** — `lightenSurface()` 의 배경 교체 조건이 `bg.a > 0.35 && lum(bg) < 0.16` 이라 **알파 0.35 이하는 애초에 안 건드립니다.** ⛔ 그러니 이런 자리에 `SKIP_SEL` 을 등재하지 마세요 — 검사가 `el.closest(SKIP_SEL)` 이라 «tr 만» 적어도 **그 행의 셀까지 글자 구제가 꺼지고**, 그 파일을 고치면 그것을 부르는 **HTML 18곳의 `?v=`** 가 함께 걸립니다(2026-09-11 에 만들었다 전부 되돌려 변경 파일을 1개로 줄였습니다). ⛔ **`background-image` 로 얹지 마세요** — 페인터는 backgroundImage 만은 **알파를 안 보고 색 휘도만** 봐서 `rgba(15,23,42,.045)` 를 «어두운 면» 으로 읽고 밝게 덮습니다. sticky 라 불투명해야 하는 칸(고정 이름 열 등)은 **`box-shadow: inset 0 0 0 100vh var(--stripe)`** 로 얹으세요. ⚠️ **그 그림자가 안전한 이유는 «알파가 낮아서» 가 아닙니다** — 페인터는 **배경을 실제로 밝혔을 때만**(`changed`) box-shadow 까지 덮어쓰는데, 밝은 면이라 그 분기에 안 걸리는 것뿐입니다. ⟹ **어두운 면에 같은 수법을 쓰면 조용히 안 먹습니다**(이것도 함정 대조가 잡았습니다). 🪤 **그리고 이건 `getComputedStyle` 로 원리상 못 잽니다** — 반투명이라 합성 결과가 화면에만 있어서, 그렇게 재면 홀·짝이 «같은 불투명 조상» 을 돌려줘 **「줄무늬 1.00 = 안 보임」이라는 거짓 실패**가 납니다(실제로 밟았습니다). **스크린샷 픽셀을 읽으세요**(`node:zlib` 로 PNG 해제 — 2장 「배경이 «사진» 이면」 항목과 같은 도구). ⚠️ 관리자 화면은 **`zoom` 이 걸려 1px 테두리가 정수 좌표와 어긋납니다** — 한 점만 찍으면 안티앨리어싱된 가장자리를 재서 **1.08**(실제 **1.52**)로 나옵니다. 주변 ±2px 중 «가장 어두운» 픽셀로 판정하세요. 📄 `docs/작업기록/260911_주간스케줄_일간뷰_격자_구분선.md` |
| 정본 판정을 **화면에 복제**할 때 «별칭표» 를 빠뜨림 | 🔴 **본체만 옮기고 별칭을 빠뜨리면 «반대로» 동작하는데 에러가 안 납니다.** 2026-09-02 실측: 서버 정본 `isActiveTeacherStatus`(`src/teacher-status.ts`)를 화면에 `_tpIsWorking` 으로 옮기면서 옛 별칭 **「재직」→「활동중」** 을 빠뜨렸습니다. 그대로 뒀으면 **「재직」으로 저장된 강사 전원**의 버튼이 흐려질 뻔했습니다. ⚠️ 이 저장소의 상태 칸에는 `재직`·`active`·`inactive`·`resigned` 별칭이 실제로 들어 있습니다. ✅ 검사를 **「그 함수가 있는가」로 쓰지 마세요** — 하니스가 정본을 컴파일해 돌리고, 화면 함수를 소스에서 **오려 내 나란히 돌려** 같은 입력에 같은 답을 내는지 대조하게 하세요(NULL·빈 값·앞뒤 공백·모르는 값까지). 본보기 `teacher_status_inline_harness` ⑪절 — 별칭 한 줄만 지워도 실제로 FAIL 납니다. ⛔ NULL·빈 값을 «아님» 쪽으로 떨어뜨리지 마세요: 옛 행에는 그 칸이 아예 없어서 **모르면 막지 않는** 쪽이 정본입니다 |
| 신한 «계좌» 지출을 화면에 그릴 때 | ⚠️ **계정과목을 화면에서 다시 계산하지 마세요.** DB 의 `bankacct_transactions.category` 는 «이중계상 판정용» 1차 분류 **9종**이고, 손익계산서가 쓰는 것은 «읽을 때» 쪼갠 계정과목 **13종**입니다(DB 에 없습니다). 화면이 DB 값을 그리면 **같은 달인데 손익계산서와 숫자가 갈라집니다.** 정본은 `src/accounting-reports.ts` 의 `resolveExpenseAccount()` 하나뿐이고, `/api/admin/reports/bank-expenses` 가 그것을 붙여서 내려줍니다(화면: `js/adm-bankacct.js`). ⛔ **입금(`kind='in'`)을 이 화면에 그리지 마세요** — 1-3 의 「케이씨피M」·«운영자금 보충»·«성격 미확인 입금» 금지가 그대로 되살아납니다. ℹ️ 새 회계 API 는 `/api/admin/reports/` 밑에 두면 인증 게이트·강사 차단·지사 차단에 **이미 등록돼 있어** `src/index.ts`(공동 금지구역)를 안 건드려도 됩니다 |
| 판단력 훈련 문장이 「문법이 틀렸다」는 제보 | 🔴 **고칠 «데이터»가 없습니다 — 문항은 문제은행이 아니라 Workers AI(llama-3.3)가 요청마다 실시간 생성합니다**(`src/api-judgment.ts` `generatePersonalizedScenario`, temperature 0.9). 그래서 화면·DB 를 뒤져 문장을 고치는 방식은 성립하지 않고, **생성 규칙과 검증이 정본**입니다. 2026-08-24 실사고: «Want play with me»(to 누락)가 보기로 그대로 나감. 원인 셋 — ① 프롬프트에 「선택지도 문법은 맞아야 한다」는 지시가 아예 없었고 ② 채점 루브릭의 "wrong option" 이 「문법 틀린 보기」로 해석될 여지를 줬고 ③ 첫걸음 밴드의 「선택지 5단어 이하」 상한이 "Want to play with me?"(6단어)를 막아 to 를 떨어뜨린 전보문으로 유도. ✅ 규칙 정본은 `src/judgment-english.ts` — 프롬프트 주입 + **생성 결과를 같은 모델에 다시 읽혀(교정자, 온도 기본) 어긋나면 다시 뽑습니다**(지시만으로는 안 지켜짐 — 단어 수 실측과 같은 뿌리. 마지막 시도는 수용 — 문제를 못 주는 것이 더 나쁨). ⚠️ **오답이 «일부러» 문법이 틀린 문항도 있습니다** — 학생이 문법 유형(형태/시제) 연습을 직접 고른 경우뿐(`allowsBrokenDistractors`). 그건 버그가 아니니 «정리»하지 마세요. 감시는 `test-harness/judgment_grammar_harness.mjs` |
| 「카페24 회계 실데이터 ▸ 지출결의」에 **남의 회사 결재가 섞여 나옴** | 🔴 **그 노드는 우리 것만 담고 있지 않습니다.** Neo4j `ExpenseReport` 에는 망고아이와 무관한 다른 조직의 지출품의서가 함께 쌓입니다(「정기휴가/개인사정」·「유치부 야외활동 매트」 등). 우리 것은 **필리핀 강사가 올리는 영어 결재**뿐입니다. ✅ 판정 정본은 `src/c24-expense-filter.ts` 하나 — ① 결재라인에 「Joy」·「박상인」 ② **제목**이 한글이면 뺍니다(2026-08-24 사장님 지시). ⛔ **내용(content)까지 보고 한글이면 버리는 규칙으로 넓히지 마세요** — 우리 결재도 거래처·내용에 한글이 섞입니다(「쿠팡 /장바구니 담음」). 그러면 **진짜 우리 지출이 사라집니다**(남의 것이 섞이는 쪽보다 훨씬 나쁩니다). 제목에 글자가 아예 없을 때만 내용으로 판정합니다. ⛔ **「Joy」를 아무 칸에서나 찾지 마세요** — 필리핀에 매우 흔한 이름이라 「Joy 선생님 병원비」 같은 진짜 결재가 날아갑니다. 결재라인이 들어 있을 **메타 칸에서만 낱말 단위로**(`Joyce`·`Enjoy` 제외). 한글 이름 「박상인」은 영어 결재에 나올 수 없으니 어느 칸에서나 찾습니다. ⚠️ **결재라인이 어느 속성인지는 카페24가 정합니다** — `properties(d)` 를 통째로 받아 훑고, 속성 «이름» 만 `prop_keys` 로 내려 나중에 좁힐 수 있게 뒀습니다. ⚠️ Cypher(Java) 정규식과 TS 정규식이 **짝**입니다 — Cypher 는 «먼저 덜어내기» 일 뿐이고 **최종 판정은 TS**라 헛돌아도 결과는 안 틀립니다. 한쪽만 고치지 마세요. ⚠️ 그래서 이 목록은 «카페24 원본 그대로» 가 아닙니다 — 원본 대조는 카페24에서 하세요(「케이씨피M」과 같은 사정). ⛔ `\p{IsHangul}` 같은 유니코드 속성 대신 **문자범위**(`[가-힣ㄱ-ㅎㅏ-ㅣ]`)를 쓰세요(Neo4j·JDK 판마다 지원이 갈립니다). 감시는 `test-harness/c24_expense_filter_harness.mjs`(37종 — 판정 함수를 컴파일해 실제로 돌립니다) |
| 표 안에 `<select>`·입력칸을 넣었는데 **크게 부풀어 칸을 밀어냄** | 🔴 **위 「표 안의 작은 아이콘 버튼」 함정의 `select` 판입니다.** `admin-inline-c.css` 3866행 `[id^="card-"] select{ padding:8px 12px !important; font-size:13.5px !important }` 와 8473행 `#legacy-cards select{ max-width:100% !important }` 가 **인라인 style 을 통째로 이깁니다.** 2026-08-23 실측: 11px 로 적은 글자가 **13.5px**, 여백 `3px 5px` 가 **`8px 12px`**, `max-width:150px` 가 좁은 화면에서 **100%** 가 됐습니다. ✅ 고침은 **CSS 파일 맨 끝에 조상 id 를 앞에 붙여**(`#acc-bankacct td select.bk-assign`) 되살리기 — `box-sizing:border-box` 도 **함께** 적으세요(안 적으면 테두리가 폭 밖으로 나가 칸을 넘습니다). ⛔ 전역 규칙을 고쳐서 풀지 마세요 — 관리자 화면 전체의 입력칸이 걸립니다. ✅ 판정은 코드가 아니라 브라우저 `getComputedStyle` 로. 감시는 `test-harness/manual/bank-expenses-browser.mjs` ⑨ |
| `student_evaluations` 에 새 점수 칸을 썼는데 **다른 기능의 값과 섞임** | 🔴 **`student_evaluations` 는 «서로 다른 두 기능» 이 같은 테이블을 씁니다.** ① `api-mango.ts`(`ensureStudentDetailSchema`)가 만든 **레거시 스키마**(`eval_type`·`level`·`score_speaking`·`score_listening`·`score_reading`·`score_writing`·`score_total`·`evaluator` — 월간/수시 «학생 상세» 4대영역 평가, 학부모 발송 없음) ② `api-lessons.ts`(`ensureEvalTable`)가 **가산 마이그레이션(`ALTER TABLE ADD COLUMN`)** 으로 얹은 **새 스키마**(`student_uid`·`score_participation`·`score_comprehension`·`score_homework`·`score_attitude`·`note_ko` … — 강사의 «1분 수업 일지», `teacher.html`→`/api/eval/create`, `eval.html` 로 학부모에게 나감). ⚠️ **`score_speaking` 이라는 이름이 양쪽에 다 있습니다** — ①은 REAL 이고 실측 값이 **3.5~4.9(소수)나 90·95(백분율로 보이는 정수)** 처럼 척도가 제각각인데, ②의 `want` 목록도 `score_speaking`(INTEGER)을 요구합니다. `ensureEvalTable` 은 `PRAGMA table_info` 로 **이름이 이미 있으면 건너뛰므로**(`if (!have.has(col))`) ①이 먼저 만든 REAL 컬럼이 그대로 남고, ②가 강사 별점(1~5 정수)을 그 자리에 씁니다 — **같은 칸에 두 척도가 행마다 섞여 쌓입니다**(2026-08-24 실측: `student_evaluations` 104행 중 102행이 ①의 값, `student_uid` 가 있는 «진짜 1분 일지» 행은 2건뿐). ✅ **읽을 때 반드시 `student_uid IS NOT NULL`(②) 또는 `eval_type IS NOT NULL`(①)로 갈라서 보세요** — 안 그러면 통계·AI 원료 집계에 다른 기능의 값이 섞입니다. ⛔ 새 점수 칸을 추가할 때 `want` 목록의 이름을 `PRAGMA table_info` 로 먼저 대조하지 않고 쓰지 마세요 — 이미 있는 이름이면 **타입도 척도도 조용히 옛것을 따라갑니다.** ⚠️ 두 기능을 완전히 갈라야 한다면(테이블 분리) 별건 작업입니다 — 지금은 «읽을 때 가른다» 로만 막아 둠 |
| 학생 «나이·학년으로 자동 분류» 하려는데 **분류가 다 같은 값으로 몰림** | 🔴 **`students_erp` 의 `grade`(학년)·`school`(학교)·`age`·`birth_date`·`level`·`program`·`textbook` 은 실사용 데이터가 사실상 없습니다.** 2026-08-24 D1 실측: 학생 29,417명 중 `grade` 채움 **0명(0%)**, `school` 447명(1.5%), `age`·`birth_date`·`level`·`program`·`textbook` **전부 0명.** `cafe24-sync.ts` 의 `importCafe24Students` 가 `grade` 를 컬럼 목록에 넣어 매일 밤 동기화하고 있지만(코드는 맞습니다), **카페24 원본 자체에 학년 값이 없는 것**으로 보입니다 — 코드를 아무리 고쳐도 원본에 없으면 못 채웁니다. ⛔ 「학년으로 밴드를 1차 자동 배정한다」 는 지금 데이터로는 **성립하지 않습니다** — 짜면 전원이 «모름» 이나 한 밴드로 쏠립니다. ✅ 학년·연령대가 필요한 기능(수업평가 밴드 등)은 **당분간 사람이 태그하는 칸을 새로 두고**(기본값 «미배정»), 카페24 쪽에 학년 필드 적재를 요청하는 것과는 별개 트랙으로 진행하세요(선례: `docs/카페24_회계데이터_적재요청_2026-08-18.md` 같은 적재요청 문서). 확인: `SELECT COUNT(*), SUM(grade IS NOT NULL AND TRIM(grade)<>'') FROM students_erp` |
| 「강사 시간을 줄이려고 AI가 자동으로 평가하게」 만들려는데 **원료 데이터가 없음** | 🔴 **자동화하려는 축마다 원료가 있는지 먼저 세어 보세요 — 「기능이 있다」와 「데이터가 쌓였다」는 다릅니다.** 2026-08-24 D1 실측(수업평가 자동화 검토): `attendance` 182,278행(✅ 참여도 자동화 가능) · `recordings` 1,919행(🟡 말하기 AI 분석 가능하나 전체 수업 대비 일부) · `chat_messages` 326행/51개 방(🟡 얇음) · `review_quiz_results` 18행(🔴 너무 얇음) · **`homework` 테이블 자체가 없음**(`CREATE TABLE IF NOT EXISTS` 가 한 번도 실행된 적 없음 — 숙제 기능 미사용, 자동화할 대상이 없음) · `leveltest_applications` 0행(2026-08-20 데모 정리로 전부 삭제됨). ✅ **자동화 설계 전에 반드시 `SELECT COUNT(*)` 로 각 원료 테이블을 세어**, 「지금 가능」·「일부만」·「테이블 자체가 없음」 셋으로 나누세요. ⛔ 코드가 있다고 데이터도 있을 거라 가정하지 마세요 — 이 저장소는 기능은 만들어 놓고 실사용이 0인 채로 오래 방치되는 경우가 흔합니다(레벨테스트 신청 17건이 전부 데모였던 것과 같은 뿌리) |
| 주간 스케줄에서 **어떤 요일만 수업이 안 들어감** (「이미 예약된」) | 🔴 **그 칸이 진짜 수업이 아니라 «자리표시»입니다.** `class_schedules` 활성 행의 대부분이 그렇습니다(실측 667행 중 **658행**) — `user_id='lms'` **518행**(옛 LMS 점유, `source=lms_import_w26`, `notes='LMS 수업중'`) · `user_id='type_seed'` **140행**(6월 시연 시드). 학생이 안 붙어 있어 화면엔 「LMS」·「시드」 배지로만 보입니다. 그런데 화면과 서버가 이걸 «이미 예약된» 으로 봐서 그 시간에 수업을 못 넣었습니다(2026-08-24 실측: HANNAH 화요일 15·16·17·18·21시가 전부 막힘, 목요일은 21시가 비어 배정됨). ✅ **2026-08-24 사장님 지시로 «판정» 만 바꿨습니다 — 행은 지우지 않았습니다**(개발·운영 같은 DB라 지우면 못 되돌립니다). 정본은 **화면 `cellBusy()`**(`admin/weekly-schedule.html`) 한 곳과 **서버 `NOT_PLACEHOLDER`**(`src/schedule-conflict.ts`) 한 곳뿐입니다. ⛔ **옛 주석 「renderMoveGrid 의 충돌판정은 그대로 둔다」를 근거로 되돌리지 마세요** — 그 지시로 뒤집힌 것입니다. ⛔ `td.dataset.slot` 을 직접 읽어 «칸이 찼나» 를 판정하지 마세요(그 자리만 조용히 옛 동작으로 돌아갑니다). 🔴 **`click` 은 `mouseup` «뒤» 에 옵니다** — 그래서 칸 클릭에 상세 모달을 열면 `mouseup` 이 띄운 「새 슬롯 추가」를 **덮어써서 «눌러도 아무 일도 안 일어난» 것처럼** 보입니다(이 함정을 실제로 밟아 클릭 핸들러도 함께 고쳤습니다). ⚠️ **이중배정을 화면·서버가 더는 막아 주지 않습니다** — 그 데이터가 한 번 넣은 정적 임포트라 지금 실제 LMS 일정과 맞는다는 보장이 없어서 내린 판단입니다. 겹치는지는 사람이 봅니다. ℹ️ 한 칸씩 지우는 길은 **이미 있습니다** — `DELETE /api/admin/class-schedules/{id}` 가 진짜 삭제가 아니라 **`status='cancelled'`**(되돌릴 수 있음) + `class_audit_log` 기록입니다. 🧹 **그런데 판정만 바꾼 것으로는 실제 운영에서 달라진 게 없었습니다** — 칸이 화면에 그대로 남아 배정이 여전히 어렵다는 판단(2026-08-24 사장님). 그래서 같은 날 **일괄 정리**를 붙였습니다: 주간 스케줄 상단 「LMS·시드 ○○건」 옆 **「정리」 버튼** → `POST /api/admin/class-schedules/purge-placeholders`. ⚠️ **물리 삭제가 아니라 `status='cancelled'`** 입니다(한 칸 삭제와 같은 방식) — 화면에서는 사라지고 되돌릴 수 있습니다. ⛔ 안전장치를 느슨하게 고치지 마세요: **본사만**(강사·지사 403) · **`user_id` 가 정확히 `lms`/`type_seed` 인 행만** · 조건을 자유 문자열로 받지 않고 **표시자 목록으로만** · **건수 확인(dry_run) → 사람 확인 → 실행** 두 단계. ✅ 감시는 `schedule_10min_manager_harness` **4부**(배정 판정)·**5부**(정리 API 안전장치)와 **`test-harness/manual/lms-slot-assign-browser.mjs`**(브라우저 20종 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 수강신청 확정 화면에서 강사가 **실제로는 비어 있는데 «그 시간 다른 수업 있음»** 이라며 앞쪽 주들이 건너뛰어짐 | 🔴 **바로 위 항목의 형제인데, 고친 자리가 하나 빠져 있었습니다.** `NOT_PLACEHOLDER`(LMS·시드 자리표시 제외)는 2026-08-24 에 `schedule-conflict.ts`(단일 등록/이동)·`api-admin.ts`·`api-teacher.ts`·`churn-graph.ts` 에 들어갔는데, **「여러 날짜를 한꺼번에」 보는 수강신청 확정 전용 충돌검사기**(`src/enroll-ops.ts` 의 `enrollConflicts`·`busyTimesForTeacher`·`teachersFreeAt`)는 애초에 목적이 다른 별도 검사기라서(`schedule-conflict.ts` 22행 주석 「그대로 두고, 이 모듈은 «한 건을 만들거나 옮길 때» 를 담당한다」) 그 수리에 안 딸려 갔습니다. 그래서 이 검사기 7군데 SQL 이 전부 옛 LMS·시드 행까지 «진짜 수업» 으로 세고 있었습니다. 실사고(2026-08-26): FAR(`mangoi_018`)가 수요일 20:40 에 실제 수업이 없는데도 확정 카드가 「FAR (그 시간 다른 수업 있음)」이라 하고, 「잡히는 날」이 첫 4주(08-26~09-16)를 «충돌» 로 건너뛰어 09-23 부터로 밀렸습니다. ⚠️ **에러가 안 납니다** — 강사는 정상 배정된 것처럼 보이고 날짜만 뒤로 밀려서, 「강사가 바쁜가 보다」로 오인하기 쉽습니다. ✅ 2026-08-26 에 `enroll-ops.ts` 의 7개 쿼리(정기·날짜지정 충돌 조회 × `enrollConflicts`/`busyTimesForTeacher`/`teachersFreeAt`, 긴 수업 정원 조회 포함)에 같은 `AND LOWER(COALESCE(user_id,'')) NOT IN ('lms','type_seed')` 를 전부 추가했습니다(이 파일은 «다른 도메인을 import 하지 않는다» 원칙이라 `schedule-conflict.ts` 에서 상수를 import 하지 않고 로컬에 같은 문자열을 복제 — 다른 세 파일과 같은 방식). ⚠️ **새 강사 가용성·충돌 검사 코드를 또 추가할 때는 이 제외식이 있는지 먼저 확인하세요** — `class_schedules` 를 `status='active'` 로만 거르면 반드시 이 사고가 재현됩니다. 다섯 곳(`schedule-conflict.ts`·`api-admin.ts`·`api-teacher.ts`·`churn-graph.ts`·`enroll-ops.ts`)의 문자열이 전부 같은지 대조하세요 |
| 화면에서 수업을 잡았는데 **학생은 「오늘 수업 없음」 · 강의실에서 서로 못 만남** | 🔴 **그 «만들기» 가 서버에 저장되지 않았습니다 — 메모리에만 넣고 POST 를 안 했습니다.** 주간 스케줄(`admin/weekly-schedule.html`)의 `saveNewSlot()` 은 «차단(blocked)» 일 때만 서버를 부르고, 수업은 `addSlot()` 으로 화면에만 그렸습니다(`assignStudent()` 도 같음 — 알림 큐만 보냈습니다). `class_schedules` 에 행이 없으니 ① 학생 조회(`/api/class/sessions/today`)가 0건이고 ② 결정론적 방 번호(`class-{예약id}-{YYYYMMDD}`)를 만들 근거가 없어 **둘이 다른 방에 앉습니다.** 2026-08-24 실측: jjy2323(장지웅)·HANNAH 22:00 수업이 통째로 없었습니다. ⚠️ **화면에는 멀쩡히 그려집니다** — 새로고침해야 사라지므로 «저장된 줄» 알게 됩니다. 차단은 2026-08-12 에 같은 이유로 고쳤는데 «수업» 쪽만 남아 있었습니다. 🔴 **그때 여기 「이동·삭제는 서버를 부릅니다」라고 적었는데 «이동» 은 사실이 아니었습니다** — 삭제만 부르고 드래그 이동은 2026-09-11 까지 한 건도 안 불렀습니다. 그 괄호 한 줄 때문에 18일간 아무도 이동을 다시 안 봤습니다(아래 덧붙임). ✅ 지금은 `postClassSchedule()`/`postClassScheduleAsk()` 한 곳으로 모아 `POST /api/admin/class-schedules` 를 부르고, **저장이 끝난 뒤에만** 화면에 그린 다음 `reloadAndRender()` 로 서버에서 다시 읽습니다. ⛔ 저장 «전에» `addSlot` 으로 그리지 마세요 — 서버가 거절해도 있는 것처럼 보여 같은 사고가 그대로 남습니다. ⚠️ 서버는 `schedule_kind='one_off'` 면 **날짜**, `'recurring'` 이면 **요일**을 요구하고 학생(`user_id` 또는 이름)이 없으면 만들지 않습니다. `class_type` 은 `regular|trial|level_test|makeup` 뿐이라 **모르는 값은 조용히 `regular`** 가 됩니다(화면의 «그룹»·«임시» 를 그대로 보내면 안 됩니다). ⚠️ 서버에 `group` 이 없어 **그룹 수업은 «학생마다 한 행»** 으로 돌아옵니다 — 같은 칸에 그냥 넣으면 나중 행이 앞 행을 덮어써 3명이 1명으로 보입니다(로더에서 합칩니다). ✅ 감시는 `schedule_10min_manager_harness` **6부**와 **`test-harness/manual/class-create-persist-browser.mjs`**(브라우저 23종 — 자동으로 안 돕니다, 사람이 부릅니다). ⚠️ **문자열 검사만으로는 못 잡습니다** — 함수도 값도 다 «있고» 빠진 것은 «요청이 나가는가» 하나뿐이라 네트워크를 봐야 합니다 📌 **덧붙임(2026-09-11) — 같은 병이 «드래그 이동» 에 그대로 남아 있었습니다.** `confirmMoveDo()`(같은 파일)가 하는 일이 `delete SLOTS[...]` + `addSlot(...)` **메모리 두 줄**뿐이었는데, 확인 모달은 「⚡ 담당 교사도 함께 변경됩니다」라고 **약속하고** 끝에 「✅ 이동됨」까지 띄웠습니다 — `fetch` 가 한 줄도 없어 **새로고침하면 조용히 되돌아갑니다.** ⚠️ 실사고: 사장님이 정우영 학생의 금 14:20 수업을 강선생님 → MAIMAI 로 옮기셨는데 홈 화면은 계속 강선생님을 가리켰습니다(D1 실측 `class_schedules` 2332 의 `teacher_id` 는 `'29'` 그대로 · MAIMAI(27)로 된 활성 예약은 **저장소 전체 0건**). **같은 자리에서 한 시간 안에 두 번 재현됐습니다** — 이어서 15시로 옮기신 것도 저장되지 않았습니다. ⚠️ 그래서 「강사 이름이 왜 남의 것이지?」로 보이지만 **이름이 바뀐 사고가 아닙니다** — 서버에 도착한 값 자체가 처음부터 그것이고 화면이 «바꿨다» 고 거짓말한 것입니다(2장 「남의 이름이 뜸」과 뿌리가 다릅니다). ✅ 지금은 `PATCH /api/admin/class-schedules/:id` 를 실제로 보내고 **저장 성공 뒤에만** 화면을 옮기며, 실패하면 사유를 말하고 `reloadAndRender()` 로 서버에서 다시 읽습니다(부분 성공일 수 있어 손으로 옮기지 않습니다). 🔴 **서버 PATCH 는 `teacher_id` 를 아예 안 받고 있었습니다** — 요일·시각·길이·날짜뿐이라 화면만 고쳐서는 강사가 영영 안 바뀝니다. ⛔ 그 필드를 열 때 **`canEditOrg()` 로 막지 마세요** — scope `'none'`(내부직원·**교사**)에 true 라 강사를 못 막습니다. 이 PATCH 는 `TEACHER_BLOCKED_PREFIXES` 에도 없어 **강사가 부를 수 있고**, 수업이 곧 급여라 강사가 남의 수업을 자기에게 가져올 수 있습니다. `enrollAdminHqOnly` 와 같은 모양으로 — 강사 거절 + `admin_scope.scope_type` **재조회** + 못 읽으면 막기 + `isOrgScopedRole` + 실재하지 않는 번호 거절. ⛔ 그 게이트를 «시각만 옮기는» 요청에까지 넓히지 마세요(기존 권한을 좁히게 됩니다 — `teacher_id` 가 실제로 바뀔 때만 탑니다). ⚠️ **그룹 수업은 서버에 «학생마다 한 행»** 이라 슬롯이 `ids` 배열을 들고 전부 보내야 합니다 — 하나만 옮기면 **수업이 쪼개지는데 화면은 한 칸이라 눈에 안 보입니다.** ⚠️ **반복은 `day_of_week`, 일회성은 `scheduled_date`** — 섞으면 안 됩니다(`sessions/today` 는 `scheduled_date` 가 있으면 그쪽을 우선하므로 반복 행에 날짜가 붙으면 «매주» 가 죽습니다). ⚠️ 요일은 **UTC 로 파싱**해 뽑으세요(로컬 시간대에 하루가 밀리면 「월요일로 옮겼는데 일요일」). ✅ 이제 진짜로 저장되므로 확인 모달이 「🔁 매주 반복 — 모든 주가 함께 바뀝니다」를 **누르기 전에** 말합니다. ℹ️ 근무불가 차단(`type==='blocked'`)은 다른 표(`teacher_unavailability`)라 이 API 로 못 옮깁니다 — 사실대로 거절합니다. 🪤 **하니스가 두 번 헛돌았습니다** — ① 「`canEditOrg` 가 없다」 부정 검사가 **제가 적은 ⛔ 주석을 잡아** 멀쩡한 코드를 FAIL 냈습니다(주석을 벗겨 낸 사본으로 판정할 것) ② 「자리표시 칸에는 요청이 안 나간다」를 **요청 수로** 물었더니, `ids` 가 비면 루프가 0회라 **가드를 지워도 0건**이라 통과했습니다 — 그때 진짜 사고는 «`fails` 도 비어 성공으로 읽고 화면만 옮기는» 것이니 **`addSlot` 이 불렸는가**로 물어야 합니다. 🔴 **그 수리가 처음엔 «매주» 를 죽일 뻔했습니다 — 함정 대조가 잡았습니다.** 반복/일회성을 `slot.recurring` 으로 갈랐는데 **그 칸은 진짜 수업에 실리지 않습니다** — `/api/admin/schedules` 의 `base` 에 없고 `recurring` 을 싣는 곳은 `teacher_unavailability` 쪽 두 줄뿐이라 **예외 없이 `false`** 였습니다 ⟹ 언제나 `scheduled_date` 를 보내 반복 행에 날짜가 박히고, `sessions/today` 는 날짜가 이기므로 **그 하루만 열리고 나머지 주는 「오늘 예약된 수업이 없어요」 → 공용방 폴백**. PATCH 에는 그 칸을 **비우는 길이 없어** 화면으로 되돌릴 수도 없습니다. ✅ 그래서 **«어느 칸을 고치는가» 를 서버가 내려줍니다**(`move_field`) — ⛔ 화면이 그 판정을 복제하면 서버의 `kind === 'one_off' || r.scheduled_date` 와 한 글자만 어긋나도 조용히 틀린 칸을 고칩니다. ⛔ **모르면 «일회성» 으로 넘겨짚지 마세요** — 안 옮기고 사람에게 말합니다. 🔴 **그리고 같은 일을 하는 입구가 «셋» 입니다**(드래그 · 우클릭 「수업 연기/변경」**(담당 강사 탭 포함)** · 「이동」 모달) — 당시 **셋 다** 저장 없이 「✅ 담당 강사 변경」·`alert('시간이 변경되었습니다')` 를 띄웠습니다. 한 곳만 고치면 다음에 다른 입구를 쓰는 순간 그대로 재현됩니다 → 저장은 **`persistSlotMove` 한 곳**입니다. ⛔ **감사 이력용 상태를 `body` 에 끼워 넘기지 마세요** — `body` 는 `request.json()` 이라 클라이언트가 `__teacher_to_name` 을 그대로 보내 **「담당 강사 29 → 아무개」를 위조**할 수 있고, 이 PATCH 는 강사도 부를 수 있어 «급여 근거» 가 오염됩니다(지역 변수로). ⚠️ **행을 못 읽었으면(`_pchRow` null) 담당 강사를 바꾸지 마세요** — 카페24 미러 도장(`c24-mirror:manual`)이 함께 빠져 미러 UPDATE 가 `teacher_id` 를 되돌립니다(「옮겼는데 밤에 되돌아감」이 급여까지 끌고 갑니다). 🪤 **하니스가 네 번 헛돌았습니다** — ① 부정 검사가 **제가 적은 ⛔ 주석**을 잡음(주석 벗긴 사본으로) ② 「자리표시엔 요청이 안 나간다」를 **요청 수**로 물음(`ids` 가 비면 루프 0회라 가드를 지워도 0건 — `addSlot` 이 불렸는가로) ③ **서버 게이트에 «조건 뒤집기» 변이가 0건**이라 `if (false && isTeacher)`·«모르면 통과» 가 **둘 다 PASS** 했습니다(→ 판정을 순수 함수 `src/class-teacher-move.ts` 로 빼서 하니스가 **실제로 돌립니다**) ④ 변이 치환이 안 먹으면 **아무것도 안 돌리고 PASS** — 「치환이 일어났는가」를 따로 FAIL 로. 감시는 `test-harness/schedule_drag_persist_harness.mjs`(102종 — 저장 함수를 오려 내 **가짜 fetch 로 실제로 돌리고** 서버 게이트는 **번들해 경계값을 넣습니다**. 변이 **13종**(화면 9 · 서버 4) 전부 실제 FAIL) |
| `/admin/` 밑에 **다크 화면**을 새로 만들었더니 **어두운 카드에 «검은» 글자**가 깔림 | 🔴 **`css/adm-light-theme.css` 가 반쪽만 먹은 것입니다.** 그 파일(2026-07-23 밝은 테마 통일)은 `html, body` 의 **배경과 글자색을 `!important` 로 덮지만**, 카드·타일은 **CSS 변수**(`--card`·`--bg-card`…)를 쓰는 화면만 함께 밝아집니다. 색을 하드코딩한 화면은 ① body 는 밝게 덮이고 ② 카드는 어두운 남색 그대로 ③ 카드 안 글자는 body 에서 상속받은 `#101828`(거의 검정) → **«어두운 면 + 검은 글자»** 라는 최악의 대비가 됩니다. 2026-08-25 환불 화면에서 실제로 밟음(사장님 «배경이 너무 어두워서 글자가 잘 안 보여»). 실측 대비비: 카드 제목 **1.21** · 요약 라벨 **2.32**(읽으려면 각각 3.0·4.5 필요). ⚠️ **기존 다크 하위화면을 보고 따라 하면 그대로 밟습니다** — 그 화면들은 변수를 쓰거나 `adm-light-surfaces.js` 가 보정해 주고 있어 멀쩡해 보입니다. ✅ 새 `/admin/` 화면은 **처음부터 밝은 값**으로 쓰세요(카드 `#ffffff` · 글자 `#101828` · 보조 `#475467` · 흐림 `#667085` · 강조 앰버 `#b45309` — `adm-light-theme.css` 가 admin.html 에서 실측해 둔 값). ⛔ 다크로 만들고 그 CSS 가 알아서 해 주기를 기대하지 마세요 |
| 「색이 맞는지」 검사를 다 통과했는데 **사장님 화면에서 글자가 안 보임** | **«무슨 색인가» 와 «읽히는가» 는 다른 검사입니다.** 브라우저 검사 38종이 「실행 버튼이 실제로 빨간가」까지 재고 전부 초록불이었는데, 정작 **글자와 배경의 대비**는 한 번도 안 쟀습니다(2026-08-25). 그래서 어두운 카드에 검은 글자가 깔린 화면이 그대로 배포됐습니다. ✅ 화면 검사에는 **WCAG 대비비**를 넣으세요 — 본문 4.5:1 · 큰 글자(24px 이상, 또는 18.66px 이상 굵게) 3:1. ⚠️ 배경은 `backgroundColor` 하나로 알 수 없습니다 — 투명하면 **조상을 타고 올라가** 실제로 깔린 색을 찾아야 합니다. 🔴 **그때 «반투명»(알파 0<a<1)을 만나면 거기서 멈추지 말고 알파 합성을 하세요** — `rgba(251,191,36,0.14)` 를 그대로 배경색으로 쓰면 눈에 닿지도 않는 진한 앰버가 배경이 되어, **멀쩡한 대비를 1.16 으로 잘못 읽습니다**(2026-08-26 Caps Lock 안내 상자에서 실제로 밟았고, 합성으로 고치니 통과했습니다). 즉 **불투명한 층을 만날 때까지 층을 모아 두었다가 아래에서 위로 `c*a + bg*(1-a)`** 로 겹쳐야 합니다 — ⛔ 「측정이 실패했으니 디자인이 틀렸다」로 읽고 색을 고치러 가지 마세요. ⚠️ 그리고 반드시 **`adm-light-theme.css` 가 함께 적용된 상태**(로컬 HTTP 서버)에서 재세요 — 그 CSS 가 사고의 원인이라 빼고 재면 멀쩡하게 나옵니다. 본보기는 `test-harness/manual/refund-admin-browser.mjs` 3부(되돌려 보면 실제로 FAIL 이 납니다) |
| 표를 좁은 화면에서 굴리려고 **`min-width` 를 숫자로 박았더니 글자가 낱글자로 쪼개짐** | 🔴 **CLAUDE.md 의 「짧은 라벨에 flex 를 썼더니 낱글자로 쪼개짐」과 같은 뿌리인데, 이쪽은 표입니다.** 문서가 가로로 밀리는 것을 막으려고 표를 `overflow-x:auto` 상자에 넣는 것까지는 맞습니다. 그런데 표에 **`min-width:560px` 처럼 숫자를 박으면** 칸이 9개일 때 한 칸이 60px 이 되어 「김하나」가 «김/하/나», 「학부모 요청 · 이사」가 세로 네 줄로 쪼개집니다(2026-08-25 환불 내역 표 390px 실측). ⚠️ **가로 넘침 검사만으로는 못 잡습니다** — `document.documentElement.scrollWidth <= innerWidth` 는 «통과» 합니다(상자 안에서 굴러가니까). 문서는 멀쩡한데 표 안 글자만 못 읽는 상태라 숫자로는 정상으로 보입니다. ✅ 폭은 **`width:max-content` + `min-width:100%`** 로 «내용만큼» 넓히고 상자에서 굴리세요. 칸에는 `white-space:nowrap`, 길어질 수 있는 칸(사유·메모)만 `max-width` + `text-overflow:ellipsis` + `title` 로 자릅니다. ✅ 검사는 **줄 수를 세세요** — `칸 높이 ÷ lineHeight` 가 3 이상이면 쪼개진 것입니다(`test-harness/manual/refund-admin-browser.mjs` 2부) |
| playwright 로 폰 폭을 쟀더니 **멀쩡한 버튼이 «다른 요소가 가린다»며 안 눌림** | **`isMobile: true` 문맥의 좌표 문제입니다 — 화면 버그가 아닙니다.** 그 문맥에서는 레이아웃 뷰포트가 지정값과 어긋나(390×**844** 로 지정했는데 `innerHeight` 가 **1437**) playwright 의 «클릭 전 자동 스크롤»·«겹침 판정» 이 엉뚱한 좌표를 봅니다. 실측(2026-08-25): 확인 버튼이 `elementsFromPoint` 로는 맨 위인데 playwright 는 「`div.muted` 가 pointer events 를 가로챈다」며 30초 타임아웃 → 손으로 스크롤해 누르면 정상. ✅ **폭만 좁히면 되는 검사에는 `isMobile` 을 켜지 마세요**(`newContext({ viewport: { width: 390, height: 844 } })` 만으로 충분). ✅ `hover:none`·터치 분기를 실제로 재야 할 때만 켜고, 그때는 `locator().scrollIntoViewIfNeeded()` 를 먼저 부르세요. ⚠️ 이 함정을 모르면 **없는 버그를 쫓습니다** — 「폰에서 버튼이 안 눌린다」는 실제로 있을 수 있는 사고라 더 헷갈립니다(2장 「도크 모달이 드로어 밑에 깔림」이 진짜 그 사고였습니다) |
| 무언가를 처리하고 **«완료» 메시지를 띄웠는데 곧바로 사라짐** | **성공 뒤에 «상태를 다시 읽는» 코드가 그 메시지를 지운 것입니다.** 처리가 끝나면 화면을 최신으로 만들려고 조회를 다시 부르는데, 그 조회 함수가 처음에 «초기화»(`resetRun()` 류)를 하면서 방금 쓴 결과 칸까지 비웁니다. 사람 눈에는 «깜빡했다가 없어진» 것으로 보이고, 실행이 됐는지조차 알 수 없습니다(2026-08-25 환불 화면에서 실제로 밟았고, 브라우저 검사가 잡았습니다 — 문자열 하니스는 함수가 «있다» 고만 봅니다). ✅ 초기화 함수에 «결과는 남긴다» 인자를 두세요(`resetRun(keepResult)`). ⚠️ 그 함수를 **이벤트 리스너에 그대로 넘기지 마세요** — 첫 인자가 `Event` 라 **항상 truthy** 가 되어 이번엔 반대로 «지워야 할 때 안 지워집니다». `addEventListener('click', function () { resetRun(); })` 처럼 감싸서 부릅니다 |
| 게이트 헬퍼를 판별 유니온(`{ok:true}｜{ok:false}`)으로 만들었더니 **`if (!g.ok)` 뒤에서 필드를 못 찾음** | **이 저장소의 `tsconfig` 는 `strict: false` · `strictNullChecks: false` 입니다.** 그 상태에서는 판별 유니온 좁히기가 동작하지 않아, `type G = { ok: true; username } ｜ { ok: false; res }` 로 두면 `if (!g.ok) return g.res;` 가 **TS2339 «res 가 없다»** 로 컴파일에 실패합니다(2026-08-25 환불 게이트에서 실제로 밟음). ✅ **한 가지 모양**으로 두세요 — `type G = { ok: boolean; username: string; res: Response ｜ null }`. ⛔ 이걸 고치려고 tsconfig 의 strict 를 켜지 마세요: 파일 96개가 그 전제로 쓰였습니다 |
| 강사를 퇴사 처리했는데 **명부에는 그대로 남음** | 🔴 **재직 여부가 «두 표» 에 따로 있습니다** — `teachers.active`(0/1 — 시간표·미러·배정이 봅니다)와 `teacher_profiles.status`(활동중/비활동/퇴사 — **강사 명부 화면**이 봅니다). 둘을 잇는 코드가 **한 곳도 없었습니다.** 그래서 `teachers.active=0` 만 내리면 미러는 멈추는데 명부에는 남고, 반대로 명부에서 퇴사로 바꾸면 화면만 바뀌고 **카페24 미러는 계속 그 강사의 수업을 만듭니다**(2026-09-01 사장님 「Mariane 은 얼마전에 퇴사했는데 아직도 여기 명부에 있는데 이유가 뭘까?」 — JINETTE 도 같은 상태였습니다). ⚠️ **에러가 안 납니다** — 두 화면이 각자 «자기 표» 를 정직하게 그릴 뿐이라 어느 쪽도 고장으로 안 보입니다. ✅ 맞추는 자리는 **쓰는 쪽 한 곳**입니다 — `PATCH /api/admin/teacher-profiles/:id` 가 `status` 를 받으면 `teachers.active` 를 함께 맞추고, 퇴사·비활동이면 `c24_mirror_teachers` 에 `enabled 0` 을 넣어 미러도 끕니다(`roster_sync` 로 무엇을 했는지 돌려줍니다). ⛔ **읽는 쪽에서 «고치지» 마세요** — 조회가 값을 바꾸면 어디서 바뀌었는지 아무도 모르고 감사로그도 안 남습니다. 명부 조회는 서브쿼리로 **대조만** 해서 `roster_mismatch`·`roster_broken_link` 로 «알려줍니다». ⛔ 대조를 **LEFT JOIN** 으로 하지 마세요 — 계정 링크가 둘인 강사가 **두 줄**로 그려집니다(바로 위 항목). ⚠️ **«명부에서 안 보이기» 는 또 다른 축입니다**(`teacher_profiles.list_hidden`) — 시험용 계정처럼 «퇴사가 아닌데 안 보여야 하는» 사람이 있습니다. 축을 헷갈려 `status='퇴사'` 로 내리면 지난 수업·급여가 걸립니다(2026-09-01 실제로 밟았다가 되돌렸습니다). ✅ 감시는 `c24_mirror_harness` M절 — ⚠️ 그 검사를 「`b.hasOwnProperty('status')` 가 있는가」 같은 **문자열**로 쓰지 마세요. 같은 파일의 무관한 코드가 그 문자열을 갖고 있어 **동기화 블록을 통째로 지워도 초록불**이었습니다(실측). 판정은 **위치로** 하세요 |
| 퇴사 강사가 **드롭다운에 그대로 떠서** 잘못 이어짐 (같은 사람이 원부에 두 줄) | 🔴 **위 항목의 뒷면입니다 — 「명부 화면」이 아니라 «강사를 고르는 목록» 쪽입니다.** 원부(`teachers`)에 같은 사람이 두 줄이면(«FAR» id 22 재직 · «HT FARRAH» id 3 퇴사) 목록이 `active` 를 안 거를 때 **둘 다 떠서 잘못 고르기 쉽습니다.** 2026-08-26 실사고: 수강신청 확정에서 그렇게 골라 **스케줄 16건이 죽은 행(id 3)에 붙었고**, 5분 뒤 같은 사람이 알아채 취소하고 FAR 로 다시 만들었지만 **먼저 만든 8건은 그대로 남았습니다**(`class_schedules.notes` 에 그때 고른 이름이 박혀 있어 사후 확인이 됩니다 — 「수강신청 확정 자동생성 · **HT FARRAH** · …」). ⚠️ **에러가 안 납니다** — 목록도 저장도 정상 동작하고 틀린 것은 «누구를 골랐나» 뿐입니다. 🔴 **고칠 때 «화면이 몇 개인지» 부터 세세요** — 이 저장소의 «강사↔계정 연결» 은 **둘**입니다(`#card-teacher-links`·`adm-teacher-links.js`·`/api/admin/teachers/links` 와 **`#card-teacher-link`·`adm-tlink.js`(lazy)·`/api/admin/teacher-links`**). 둘이 **같은 표**(`teacher_account_links`)에 쓰는데 이름이 한 글자(`links`/`link`) 차이라 한쪽만 고치기 십상입니다 — 실제로 2026-09-09 에 한쪽만 고쳤다가 함정 대조가 잡았습니다. ✅ 거르는 것은 **화면 몫**입니다 — ⛔ 서버에서 `WHERE active=1` 로 지우지 마세요. 「퇴사 강사도 보기」 체크박스가 되돌릴 길인데 서버가 미리 지우면 그 체크박스가 **조용히 무동작**이 됩니다(2장 「붙이는 쪽과 읽는 쪽이 짝」). 대신 서버는 `active` 를 **뽑아서** 내려줘야 합니다 — 안 뽑으면 화면이 «거를 근거조차» 없습니다(실제로 `/api/admin/teacher-links` 가 `SELECT` 에도 `WHERE` 에도 없었습니다). ⛔ **이미 그 강사에게 이어진 계정은 «절대» 숨기지 마세요** — 숨기면 그 줄이 「연결 안 됨」으로 보여 사람이 멀쩡한 연결을 다시 만들려 듭니다. ⚠️ **`active` 가 NULL 이면 «재직» 으로 두세요** — `Number(null) === 0` 이 참이라 null 검사가 «먼저» 여야 하고, 숨기는 쪽으로 실패하면 멀쩡한 강사가 목록에서 조용히 사라져 연결 자체를 못 하게 됩니다(빠지는 쪽이 훨씬 나쁩니다). ⚠️ 그 NULL 처리가 **저장소 안에서 두 갈래**입니다 — `COALESCE(active,1)`(모르면 재직) 대 **`enroll-ops.ts` 네 곳의 `WHERE active = 1`**(모르면 제외). 지금 NULL 행이 0건이라 무해할 뿐입니다. 🪤 **브라우저 검사가 «서버 계약» 을 원리상 못 잽니다** — 서버를 스텁으로 가로채므로 `active` 를 안 뽑도록 되돌려도 **33종이 전부 통과**했습니다(실측). 서버 쪽은 **질의문을 잘라서 보는 자동 하니스**로 따로 재세요. ⛔ 그리고 이름표를 바꾸거나(`PATCH /api/admin/teachers/:id`) 행을 지워서 풀지 마세요 — 그 이름이 `no-show-truth.ts` 별칭표·`c24-mirror.ts` 퇴사자 매칭의 **판정 열쇠**라 조용히 어긋나고, 지우면 과거 스케줄이 «누구인지 모를 번호» 가 되며 카페24 「퇴사자(회색)」 판정이 **「원부에 없음(빨간 경고)」** 으로 뒤집힙니다(`AUTOINCREMENT` 라 되돌릴 수도 없습니다). 감시는 `test-harness/teacher_link_left_hidden_harness.mjs`(39종 — 두 화면의 판정을 **오려 내 실제로 돌려 같은 답인지** 대조)와 `test-harness/manual/teacher-links-left-hidden-browser.mjs`(브라우저 33종, 자동으로 안 돕니다) |
| **0 이 «정당한 값»** 인 자리에 `\|\| 기본값` 을 썼더니 **그 하나만** 조용히 뒤집힘 | 🔴 **`\|\|` 는 «없는가» 가 아니라 «falsy 인가» 를 묻습니다 — `0`·`''`·`false` 는 «있는 값» 인데도 기본값으로 떨어집니다.** 2026-09-09 실측(「강사 계정 연결」 표 `adm-tlink.js`): 정렬 키가 `var order = { unlinked: 0, ambiguous: 1, auto: 2, linked: 3 }` 에 `order[a.status] \|\| 9` 라, **첫 칸만 falsy 로 떨어져 9** 가 되어 「급한 것부터 위로 — 연결 안 됨 → … → 연결됨」이라는 **바로 그 줄의 주석과 정반대로 「연결 안 됨」이 맨 아래**로 갔습니다. ⚠️ **1・2・3 은 truthy 라 멀쩡히 동작합니다** — «절반만 맞는» 모양이라 눈으로는 «정렬이 되고 있으니 맞겠지» 로 보이고 **에러도 경고도 안 납니다.** ⚠️ 하필 그 표는 「손봐야 할 계정을 사람이 찾아 고치라고」 만든 화면이라 **제일 급한 줄이 스크롤 밖**으로 갑니다. ✅ 물어야 할 것은 «falsy 인가» 가 아니라 **«그 표에 있는 키인가»** 입니다 — `Object.prototype.hasOwnProperty.call(표, 키) ? 표[키] : 기본값`(또는 `??`. 다만 이 저장소의 화면 코드는 대부분 ES5 스타일이라 `??` 가 안 맞는 자리가 있습니다). ✅ 순위 표를 **함수 밖으로 빼고 그 함정을 주석에 이름으로** 적어 두세요 — 안 적으면 다음 사람이 «한 줄로 줄이려고» 다시 `\|\|` 로 씁니다. ⚠️ **문자열 하니스로는 못 잡습니다** — 표도 값도 다 «있고» 틀린 것은 «0 이 살아남는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 검사는 **정렬을 실제로 돌려서** 하세요(순위 함수를 오려 내 `rank('unlinked') < rank('ambiguous')` + 네 줄을 넣어 나온 순서를 대조). 🪤 그때 **자르는 범위를 «sort 문 한 줄» 로 좁히지 마세요** — 옛 falsy 모양은 순위 표를 함수 «안» 에 갖고 있어서, 좁게 자르면 변이가 `order is not defined` 로 죽습니다. 그건 «순서가 틀렸다» 가 아니라 **«검사가 못 봤다»** 이고, 실제로 **표 이름만 바꾼 변이는 그 상태에서 통과했습니다**(실측). **함수 머리부터** 잘라 변이를 «적힌 그대로» 돌리세요(DOM 은 흉내만). 🪤 그리고 **브라우저 검사의 fixture 가 두 상태뿐이면 순서를 다 못 잽니다** — 네 상태를 다 넣고, ⛔ 아이디를 «순서대로» 짓지 마세요(알파벳 2차 정렬이 우연히 답을 맞혀 헛돕니다). 감시는 `teacher_link_left_hidden_harness` ⑧절(자동 — 식이 맞나)과 `manual/teacher-links-left-hidden-browser.mjs` ⑧절(브라우저 — 그 식이 진짜로 이 표를 그리는가. 자동으로 안 돕니다, 사람이 부릅니다). 변이 5종 전부 실제 FAIL. 🪤 **그 검사 자체가 처음엔 셋이 헛돌았습니다 — 셋 다 «검사가 있는데 안 재는» 모양입니다:** ① `new Function` 의 «만들기» 만 `try` 로 감싸고 **«부를 때» 를 안 감싸면**, 이름을 바꾼 변이가 «깔끔한 FAIL» 이 아니라 **하니스를 크래시**시켜 `결과:` 줄조차 안 나옵니다(무엇이 깨졌는지 안 보입니다) ② 실체 검사를 `if (typeof rank === 'function')` **안** 에 두면, 오려내기가 깨지는 변이에서 **FAIL 이 아니라 검사가 조용히 사라집니다**(합계만 줄어듦) — 그 `if` 를 걷어내고 «못 오려 냈다» 를 따로 FAIL 로 두세요 ③ 부정 검사를 **«이름»** 으로 물으면(`\border\[…\]\s*\|\|`) 그 이름이 리팩터로 사라진 뒤 **아무것도 안 잡는 죽은 검사**가 되고 정작 가장 있음직한 재발(`TLK_ORDER[st] \|\| 9` — `\b` 가 대소문자를 가려 안 걸립니다)을 놓칩니다. **«모양»**(`[…] \|\| 숫자`)으로 물으세요 — ⚠️ 그때 숫자 폴백만 보게 좁히세요(정당한 객체 폴백·누적기 `\|\| 0` 이 거짓 FAIL 을 냅니다) |
| 카페24에서 온 것을 그대로 만드는데 **«일부러 뺀 것» 이 빨간 경고로 뜸** | 🔴 **«없다» 와 «안 만들기로 했다» 는 다른 사실인데 한 글자로 뭉치면, 늘 켜진 경고가 진짜 경고를 덮습니다.** 카페24 미러가 그랬습니다 — 퇴사한 강사의 잔재 수업과 «원부에 아직 등록 안 된 강사» 가 둘 다 `no_teacher`(⚠️ 빨강)였고, 명부에서 숨긴 시험용 계정(`mangoai1`)의 수업은 «학생 계정이 없습니다» 로 떴는데 그것은 **거짓**입니다. 📜 녹화 목록이 «정상 정리분 66%» 에 «진짜 저장 실패 76건» 을 파묻었던 것과 같은 뿌리입니다. ✅ 판정을 **성격대로 쪼개고**(`no_teacher_left`·`student_hidden` = 회색 «안 그림», `no_teacher` = 빨강 «등록 필요») 요약 숫자도 갈라 세세요. ⚠️ 퇴사 강사를 이름으로 이을 때는 **재직이 언제나 이깁니다**(동명이인). ⚠️ 숨긴 학생은 **조건절(`hiddenExcludeCond`)로 거르지 말고 «목록»(`loadHiddenStudents`)으로 받으세요** — 걸러 버리면 그 학생이 «없는 학생» 이 되어 다시 빨간 경고가 됩니다. ⚠️ 그 함수는 **fail-open** 입니다(못 읽으면 빈 집합) — 거꾸로 실패하면 시간표가 통째로 안 만들어집니다. ⚠️ **`import` 를 동적(`await import('./d1-chunk')`)으로 두지 마세요** — 하니스에서 해석이 안 되는데 fail-open try/catch 가 그것을 삼켜 **늘 빈 집합**을 돌려주고, 「숨긴 사람이 없다」가 정답인 검사만 있으면 **전부 초록불**입니다(2026-09-01 실측). 정적 import 로 두고 «제대로 찾는다» 검사를 **짝으로** 넣으세요 |
| 같은 사람인데 **계정이 두 개** (대소문자만 다름) | 🔴 **`admin_account.username` 은 `TEXT NOT NULL UNIQUE` 로 COLLATE NOCASE 가 없습니다** — SQLite 가 `mangoi_167` 과 `Mangoi_167` 을 «다른 값» 으로 봅니다. 그런데 로그인 조회가 못 찾으면 **옛 LMS 통과 인증이 그 자리에서 새 계정을 만듭니다**(입력한 글자 그대로, `legacy-teacher-auth.ts`). → **휴대폰 키보드 자동 대문자 한 번에 계정이 두 벌**이 됩니다. 2026-08-24 실측: `mangoi_167`(7/30 · HANNAH 연결됨)과 `Mangoi_167`(8/24 · 연결 안 됨)이 나란히 있었고, **정작 쓰는 쪽에 연결이 없어** 강사 화면이 「수업 없음」이었습니다. ⚠️ **출근·급여가 계정 단위**라 기록도 두 갈래로 쪼개집니다. ⚠️ 강사 계정 연결 카드에서 **둘 다 연결하지 마세요** — 급여·출근 기록이 두 갈래로 쪼개집니다. 📌 **명부가 두 줄로 보이던 것은 2026-09-01 에 구조로 고쳤습니다** — 그전에는 `teacher_profiles` 조회가 `teacher_account_links` 를 `teacher_id` 로 **LEFT JOIN** 해서 링크가 두 개면 프로필 하나가 «두 행» 으로 그려졌습니다(사장님 「왜 Len 이 두 명이나 있지?」 — 원부 18번에 `mangoi_168`·`Mangoi_168` 두 링크). 지금은 **서브쿼리로 «가장 최근에 연결한» 계정 하나만** 고르므로 링크가 몇 개든 행이 안 늘어납니다(`PICK_LOGIN_USERNAME`·`LOGIN_LINK_COUNT`, `api-admin.ts` — 목록·단건이 **같은 조각**을 씁니다. 한쪽만 조인으로 되돌리면 명부와 수정 모달이 서로 다른 계정을 보여 줍니다). ⛔ 그렇다고 중복을 **감추지는 않습니다** — 화면이 「⚠ 계정 연결 N개」라고 말합니다(감추면 아무도 정리하지 않습니다). ✅ 감시는 `teacher_status_inline_harness` ⑨절이 **진짜 SQLite 에 링크 두 개를 넣고 돌려** 한 줄인지 세고, 같은 자리에서 «옛 LEFT JOIN 은 실제로 두 줄을 만든다» 도 대조로 증명합니다. ✅ 2026-08-24 에 **대소문자를 무시**하도록 고쳤습니다(사장님 지시). 규칙은 **① 정확일치 먼저 ② 없으면 대소문자만 다른 후보**이고, 후보마다 비번을 대 봅니다(두 계정에 다른 비번이 걸려 있어도 한쪽이 잠기지 않게). ⛔ **NOCASE 하나로만 찾으면 안 됩니다** — 이미 있는 두 계정 중 아무거나 골라 「어제까지 되던 사람」의 비번이 갑자기 안 맞습니다. ⛔ **스키마를 COLLATE NOCASE 로 바꾸지 마세요** — 대소문자만 다른 행이 실재해 UNIQUE 에 걸리고 표를 다시 만들어야 합니다(운영 DB). ✅ 찾은 뒤에는 **DB 에 적힌 아이디(`acctUser`)** 로 세션·2FA·스코프·기록·응답을 전부 통일합니다 — 여기서 갈리면 대소문자가 그대로 아래로 흘러 같은 사고가 납니다. ✅ 계정을 «만드는» 두 경로(옛 LMS 자동생성 · `staff-create`)도 NOCASE 로 먼저 확인합니다. ⚠️ 그 확인은 **try/catch 로 감싸세요** — 로그인 경로라 예외가 나면 강사가 아예 못 들어옵니다(막는 쪽이 아니라 통과시키는 쪽으로 실패). ✅ `teacher_account_links` 조회도 NOCASE 여야 합니다(`api-teacher.ts`) — 안 그러면 고쳐 놓고도 화면은 「연결 안 됨」 그대로입니다. 📌 **이미 생긴 중복은 코드가 못 지웁니다** — 확인: `SELECT LOWER(username) k, COUNT(*) n, GROUP_CONCAT(username) FROM admin_account GROUP BY k HAVING n>1`. 📜 **학생 계정(`students_erp`)에도 같은 구멍이 있었습니다**(2026-08-25 수리) — `user_id` 는 `TEXT PRIMARY KEY` = BINARY 라 `jeong` 과 `Jeong` 이 **UNIQUE 에 안 걸리는데**, `/api/student/login` 만 `COLLATE NOCASE` 로 찾고 «만드는» 쪽 셋은 대소문자를 구분했습니다 → 두 벌이 생기고 로그인이 **둘 중 아무 행이나** 집습니다(출석·포인트가 갈립니다). 고친 곳은 **관리자 수동 등록**(`api-admin.ts` `/api/admin/students/create`) · **홈 회원가입**(`api-students.ts` `/api/student/register`) · **학부모–자녀 잇기**(`/api/parent/link-child` — 여기는 못 찾으면 «빈 학생 행» 을 새로 만들어 학부모를 그 빈 계정에 이어 놓아서, 화면이 영영 비어 보였습니다). ⚠️ 이 셋은 **짝**이라 한쪽만 고치면 그 경로로 그대로 두 벌이 생깁니다. ℹ️ AI 운영비서(`ai-command.ts`)는 원래부터 NOCASE 라 안 건드렸습니다. 📌 이미 생긴 중복 확인: `SELECT LOWER(user_id) k, COUNT(*) n, GROUP_CONCAT(user_id) FROM students_erp GROUP BY k HAVING n>1`. 🔴 **그런데 «로그인» 쪽은 2026-08-26 까지 반쪽이었습니다** — `/api/student/login` 은 `COLLATE NOCASE` 는 있는데 **`ORDER BY` 가 없어** 대소문자만 다른 두 행 중 **아무거나** 집었습니다(위 ⛔ 「NOCASE 하나로만」이 관리자에만 적혀 있었습니다). 그리고 그런 행이 **실재합니다** — 2026-08-26 실측: `Kim`/`kim`(=김민수) · `Lee`/`lee`(=이병엽) 네 행. 같은 날 사장님 지시(「어린이가 대소문자를 제일 헷갈려한다」)로 **정확일치 우선**을 로그인·연장결제 본인확인(`/api/student/lookup`)·비밀번호 설정(`/api/student/set-password`)·학부모 대시보드 **네 곳에 함께** 넣었습니다. ⚠️ `set-password` 는 **UPDATE 를 DB 표기로** 해야 합니다 — 입력 표기로 쓰면 «저장했다는데 새 비번으로 로그인이 안 되는» 상태가 되는데 **0건 UPDATE 는 에러를 내지 않습니다.** ⛔ **비밀번호는 대소문자를 그대로 봅니다**(2026-08-26 사장님 결정) — 학생은 `password_hash` 가 **29,520명 중 11명**뿐이라(2026-09-22 재측정 — 그 결정 당시엔 0명) 대부분 헷갈릴 것이 없고, 비번이 실제로 있는 것은 강사·관리자 49개 계정뿐인데 그게 급여·회계를 여는 열쇠라 강도를 낮추지 않습니다. 대신 `admin/login.html` 에 **Caps Lock 안내**를 띄웁니다. ✅ **폰 키보드가 아이디를 대문자로 만드는 것**은 서버로 못 막습니다 — `autocapitalize` 기본값이 `sentences` 라 첫 글자가 대문자가 되고, **자동고침은 아예 딴 낱말**로 바꿔 놓아 서버가 대소문자를 무시해도 로그인이 깨집니다. 아이디 칸에는 `autocapitalize="off" autocorrect="off" spellcheck="false"` 세 개를 **함께** 다세요. ⚠️ `index.html` 은 공동 금지구역이고 첫 화면 예산도 빠듯해서, 그 안의 칸(`vc-name-input`·`ext-uid`)과 **모달을 열 때 JS 가 만드는 `lm-uid`** 는 **defer 파일에서 밖에서 입혀 줍니다**(`js/session-guard.js` 꼬리, `pointerdown`·`focusin` 캡처. ⛔ 상주 MutationObserver 금지 — 홈을 멎게 한 전력). 감시는 `test-harness/login_username_case_harness.mjs`(58종 — 소스에서 SQL 을 오려 내 **진짜 SQLite 에 돌립니다**. ⑤절은 «스키마만으로는 안 막힌다» 는 전제까지, ⑥절은 «Kim/kim 에서 무엇을 고르는가» 를 확인합니다)와 `test-harness/manual/login-case-browser.mjs`(브라우저 12종 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 관리자 **계정을 지웠는데** 그 사람이 여전히 들어와 있고 **권한은 오히려 올라감** | 🔴 **`checkAdminSession()` 은 `admin_sessions` 만 조회하고 `admin_account` 에 그 계정이 아직 있는지 확인하지 않습니다**(`src/auth-admin.ts:468~489`). 그래서 `admin_account` 한 줄만 지우면 **살아 있는 세션 토큰이 만료일까지 그대로 통과**합니다(실측 2026-09-01: `mangoi_168` 의 세션 3개가 9/10~9/18 만료, 마지막 사용 8/26). ⚠️ 단순 잔존이 아니라 **권한 상승**입니다 — ① `getAdminActor()` 는 `admin_account` 를 «이름» 조회용으로만 읽고 행이 없으면 `name=''` 인 채 **`ok:true`** ② `getScope()` 는 `admin_scope` 행이 없으면 `autoSeedOne()` 이 도는데 계정이 없어 이름이 비면 어느 분기에도 안 걸려 **`type='none'` 을 새로 심습니다**(`INSERT OR IGNORE` — 즉 **`admin_scope` 를 먼저 지워도 세션이 살아 있으면 다시 생깁니다**) ③ `resolveRole('none', uid, '')` 는 강사 판정 근거(`scope_type='teacher'`·이름·`hq_t_*`)가 전부 사라져 **`role:'staff'`** ④ 결과적으로 **`isTeacher===false`** 라 강사 차단(`TEACHER_BLOCKED_PREFIXES`)을 안 받고 **`canEditOrg()` 는 `type==='none'` 에 `true`**(`scope.ts:181`) — 본사와 동급 쓰기 권한이 됩니다. ✅ 지울 때는 **`admin_sessions` → `admin_scope` → `admin_account` 순서로 셋 다** 지우세요(순서가 중요합니다 — 계정을 먼저 지우면 그 세션의 다음 요청이 `admin_scope` 를 다시 심습니다). ✅ `admin_login_history` 는 **남기세요**(과거 접속 기록 — 지우면 「누가 언제 들어왔나」가 사라집니다). ⛔ 대소문자만 다른 계정을 지울 때 `=` 대신 **`GLOB`** 을 쓰세요 — 어느 컬럼의 collation 에도 좌우되지 않습니다(`WHERE username GLOB 'mangoi_168'`). ✅ 지우기 전에 붙은 기록을 **세어 보세요**(`teacher_account_links`·`class_schedules.teacher_id`/`user_id`·`attendance.account_uid`·`admin_sessions`·`admin_scope`) — 0건이 아니면 사람에게 먼저 알립니다. 📄 절차 선례: `docs/작업기록/260901_mangoi_168_중복계정_링크정리.md` |
| Tailwind 클래스를 새로 썼는데 **스타일이 아무것도 안 먹음**(에러도 없음) | 🔴 **`/css/tailwind-build.css` 는 «그때 쓰인 클래스만» 뽑아 구운 정적 빌드입니다.** 원래 Play CDN(런타임 컴파일)이었던 것을 2026-07-23 에 정적으로 바꾸면서 `teacher-training.html`·`suspect-mystery.html` 두 파일만 스캔해 뽑았습니다(파일 머리 주석에 적혀 있습니다). 그래서 **그 두 파일에 없던 클래스는 CSS 에 아예 존재하지 않습니다** — `grid-cols-3`·`md:grid-cols-3`·`bg-gradient-to-br`·`lg:col-span-2`·`tracking-tight` 전부 0건(2026-08-24 실측). 클래스 이름을 아무리 봐도 오타가 아니라 **CSS 쪽에 그 규칙이 없는 것**이라 원인이 안 보입니다. ✅ 확인은 `grep -c "\.클래스명" cloudflare-deploy/public/css/tailwind-build.css` 한 줄. ✅ 새 디자인은 **그 화면 전용 CSS 를 직접 쓰는 편이 낫습니다** — 빌드를 다시 돌릴 필요가 없고 다음 사람이 빌드 절차를 몰라도 손댈 수 있습니다(2026-08-24 강사 교육 매뉴얼 v3.0 이 이 길을 택했습니다 — Tailwind 링크를 떼고 `tt-` 클래스로 전부 정의). ⚠️ 굳이 다시 빌드하려면 **설정파일로** 넘기세요(`--content` 에 쉼표로 여러 파일을 주면 일부만 스캔됩니다) 그리고 두 HTML 의 `?v=` 도 함께 올리세요 |
| 아이콘에 `style="width:16px"` 을 줬는데 **버튼이 아이콘 하나로 가득 참** | **`js/lucide-local.js` 는 `<i data-lucide="…">` 를 «갈아 끼웁니다» — 고치는 게 아닙니다.** `createElementNS` 로 새 `<svg>` 를 만들어 `replaceChild` 하는데, 옮겨 오는 것은 **`class` 뿐**입니다. 그래서 `<i>` 에 적은 인라인 `style` 은 통째로 사라지고 기본 24px 이 나옵니다(2026-08-24 실측 — 강사 매뉴얼 CSV 버튼이 아이콘만 남았습니다). ⚠️ **에러도 경고도 없습니다.** ✅ 크기는 **CSS 로** 못 박으세요(`.tt-btn svg{width:16px;height:16px;flex:0 0 auto}`). ⚠️ 이 파일에는 아이콘이 **6종(`download`·`edit-3`·`file-text`·`layout-grid`·`printer`·`sheet`)뿐**입니다 — 없는 이름을 쓰면 콘솔 경고 한 줄만 남고 **자리가 조용히 빕니다.** 쓰는 화면은 `teacher-training.html` 하나입니다 |
| 두 줄짜리를 **한 줄로 바꿨는데 높이가 그만큼 안 줄어듦** | 🔴 **`line-height` 를 안 정하면 바깥에서 상속받습니다.** 관리자 본문의 상속값이 1.5~1.7 이라, 14px 글자 한 줄이 21~24px 을 차지합니다 — 줄은 하나가 됐는데 상자 높이는 «두 줄 시절과 비슷» 하게 남습니다(2026-08-25 사이드바 시계를 한 줄로 만들다 실측: 넣기 전 45px → `line-height:1.2` 를 넣고 35px). ⚠️ **문자열 하니스·타입체크로는 절대 안 보입니다** — 마크업도 CSS 도 «맞고» 틀린 것은 «몇 px 인가» 뿐입니다. ✅ 한 줄짜리 상자에는 `line-height` 를 **반드시 못 박으세요**(1.2 안팎). ✅ `white-space:nowrap` 도 함께 — 좁은 폰에서 «09:08:» / «10» 으로 쪼개집니다. ✅ 확인은 브라우저에서 `getBoundingClientRect().height` 를 재는 것뿐입니다. ⚠️ PC 관리자 화면은 `body{zoom:1.3}` 이라 잰 값에 1.3 이 곱해져 있습니다 — «CSS 픽셀» 로 비교하려면 나눠 보세요 |
| 헤드리스로 «영상이 재생되나» 를 확인했더니 **무조건 실패** | 🔴 **이 컨테이너의 크로미움은 H.264(mp4)를 못 재생합니다** — 특허 코덱이 빠진 오픈소스 Chromium 빌드입니다(2026-08-25 실측: `canPlayType('video/mp4; codecs="avc1.42E01E"')` 가 **빈 문자열**, `webm; vp8` 은 `probably`). 그래서 멀쩡한 mp4 도 `error` 이벤트가 나고 «재생 안 됨» 으로 보입니다 — 실제로 `/promo.html` 검증에서 파일도 있고 서버도 200 을 주는데 영상 요소만 사라져 한참 헤맸습니다. ⚠️ 이걸 페이지 버그로 읽으면 **없는 버그를 쫓습니다.** ✅ 여기서 확인할 수 있는 것은 «요청이 나갔나»(CDP `Network.requestWillBeSent`)·«오류 안내가 떴나»·«좌표와 레이아웃» 까지입니다. **재생 자체는 사람이 진짜 브라우저에서** 눌러 봐야 합니다. ⛔ ffmpeg 로 webm 을 만들어 우회하지 마세요 — `/opt/pw-browsers/ffmpeg-1011` 은 `--disable-everything` 빌드라 쓸 수 있는 인코더가 사실상 없습니다(같은 날 시도: libvpx 도 필터그래프 초기화 실패. mp4 디코더가 없어 **영상에서 포스터 프레임을 뽑는 것도 안 됩니다** — 포스터는 HTML 로 그려서 크로미움 스크린샷으로 만들었습니다) |
| 손가락으로 확대했는데 **떼는 순간 100% 로 돌아감** | 🔴 **처리가 둘인데 서로를 모르는 것입니다 — 「기능이 없다」가 아니라 「됐다가 되돌려진다」입니다.** 교재 영역에는 «핀치 확대»(`js/idx-main.js:3159~3193`)와 «더블탭 → 100% 되돌리기»(`3195~3210`)가 각각 따로 붙어 있는데, 더블탭 판정이 「손가락 하나가 떨어진 일이 300ms 안에 두 번」만 봅니다. **핀치를 놓으면 손가락이 둘 떨어지므로 그 둘이 «더블탭» 으로 세입니다** → 벌리는 동안엔 실제로 커졌다가 떼는 순간 튕겨 돌아갑니다(2026-08-25 사장님 제보). ⚠️ 두 손가락이 **정확히 같은 순간** 떨어지면 신호가 하나로 묶여(`changedTouches.length===2`) 빠져나가므로 «가끔은 되는» 것처럼 보입니다 — 재현이 들쭉날쭉한 이유입니다. ✅ 되돌리기 처리를 **지우지 마세요**(그 기능도 필요합니다). «방금 핀치가 끝났다» 만 **캡처 단계**에서 기록해 두고 그 직후 500ms 안의 `pdfSetZoom(1)` **한 번만** 무시합니다(`js/idx-vc-mobilefix.js` ③). ⛔ 핀치 처리를 딴 파일에 새로 만들지 마세요 — `vcJoinRoom` 안의 것과 둘 다 돌아 배율이 두 번 적용됩니다. 📜 같은 뿌리의 형제: 게임 허브 «`pointerdown` 자동정지와 `click` 토글이 서로를 뒤집던» 건 — **틀린 것이 «순서» 뿐일 때 문자열 하니스는 전부 초록불입니다.** 감시는 `test-harness/manual/vc-textbook-mobile-browser.mjs` ② |
| 교재·칠판 **글자가 깨져 안 보임**(원본은 멀쩡한데) | 🔴 **캔버스에 «몇 개의 점으로 그릴지» 를 안 정하면 CSS 픽셀 그대로 그립니다.** 요즘 폰은 CSS 1칸을 실제 점 3개로 표시하므로(`devicePixelRatio` 3) **화면이 쓸 수 있는 점의 1/3 만 그려 놓고 3배로 늘려** 보여주던 셈입니다 — 획이 가는 한글이 이 단계에서 사라집니다. 2026-08-25 실측: `devicePixelRatio` 가 `index.html`·`idx-main.js` 전체에 **0건**이었습니다. ✅ 그리는 배율(`scale`)에 곱하고, `canvas.style.width/height` 로 **보이는 크기를 원래대로 못 박습니다**(안 하면 교재가 화면을 넘습니다). ⚠️ **펜 굵기·글자 크기도 함께 키워야 합니다** — 그 값들은 «캔버스 점» 단위라 그냥 두면 **필기가 절반 굵기**가 됩니다(`pdfDrawStroke` 를 감쌌습니다). ℹ️ 좌표는 0~1 정규화 + 입력이 `getBoundingClientRect` 기준이라 손댈 것이 없고, 필기 겹칩 캔버스는 `pdfSyncAnnoCanvas` 가 `style.width` 를 베껴 가므로 저절로 맞습니다. ⚠️ 배율은 **2로 자르세요** — 3배는 점이 9배라 구형 폰이 버겁습니다. ✅ 사진 교재는 **절반씩 나눠** 줄이세요(8배 축소를 한 번에 하면 브라우저 기본 보간이 획을 지웁니다 — 「깨진 것처럼」의 정체). 📜 사장님이 「원본은 흐리지 않았다」고 확인해 준 것이 원인을 «화면에서 줄이는 단계» 로 좁혀 줬습니다 — **이 제보를 받으면 원본 해상도부터 물어보세요** |
| **blocking 파일**을 고쳐야 하는데 첫 화면 예산이 없음 | ✅ **그 파일이 «이미 받아 둔 것» 을 담아 두는 칸이 있으면, 거기에 미리 넣어 두는 것으로 밖에서 고칠 수 있습니다.** 2026-08-25 가면(`js/idx-x6.js`, blocking)이 그랬습니다 — 얼굴인식 파일을 `vcFx._vision || (vcFx._vision = await import(CDN))` 로 캐시하므로, **defer 파일에서 그 칸을 우리 것으로 먼저 채우면** 그 줄이 앞쪽에서 끝나 CDN 을 아예 안 부릅니다(`js/idx-vc-mobilefix.js` ⑦). 함수 안에 박힌 값(모델 주소)은 그 함수를 **한 겹 감싸서** 바꿔 끼웁니다. ⚠️ **약점을 알고 쓰세요** — 그 칸 이름이 바뀌면 **조용히 헛돌고** 원래 경로로 되돌아갑니다(에러도 안 납니다). 그래서 «그 칸의 모양» 을 하니스로 못 박아야 합니다(`face_model_local_harness` ②). ✅ 실패하면 **손대지 않은 상태로 되돌리세요**(칸을 `null` 로) — 그러면 최악이어도 «고치기 전» 과 같아집니다. ⛔ 로드 순서를 바꿔(blocking → defer) 예산을 버는 방법은 **별도 PR 로만** 하세요 — `first_paint_budget_harness` 머리말이 그렇게 못 박고 있고 라이브 장애 전력이 있습니다 |
| 헤드리스로 얼굴인식(MediaPipe)을 확인했더니 **파일이 멀쩡한데 죽음** | 🔴 **`--disable-gpu` 로 띄우면 안 됩니다 — MediaPipe 는 «CPU 델리게이트» 에서도 WebGL 을 씁니다**(이미지 변환에 GL 을 쓰기 때문). 2026-08-25 실측: wasm 까지 정상 로드된 뒤 `_emscripten_glActiveTexture` 에서 `Cannot read properties of undefined (reading 'activeTexture')` 로 죽었습니다 — 이걸 파일 문제로 읽으면 **없는 버그를 쫓습니다.** ✅ 소프트웨어 GL 로 띄우세요: `--use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader`. 그러면 얼굴인식기 생성까지 실제로 확인됩니다(로컬 파일 기준 463ms). ⚠️ **빈 브라우저는 늘 «첫 방문자» 입니다** — 세로 폰에서는 「화면을 가로로 돌려주세요」 오버레이(`#vc-orientation-overlay`)가 수업 화면 전체를 덮고 아래를 `pointer-events:none` 으로 잠급니다. 그대로 재면 멀쩡한 버튼도 «다른 것이 덮는다» 로 나옵니다. 사람이 「이대로 보기」를 누르는 그 함수(`vcNukeRotationOverlays`)를 검사에서도 부르세요(관리자 `#aw-overlay` 와 같은 사정) |
| 화면에서 파일을 못 고르는데 **서버는 받아 주고 있음** | 🔴 **허용 형식이 «화면 두 곳 + 서버» 세 곳에 따로 적혀 있습니다 — 서버보다 화면이 좁으면 「폴더는 열리는데 올릴 수가 없다」가 됩니다.** 2026-08-25 실측: `<input accept>`(`index.html:8512`)와 `allowedExts`(`idx-main.js` `pdfUpload`)는 `pdf/jpg/jpeg/png` 인데 서버 `_allowedUpload`(`src/index.ts`)는 **`image/webp` 를 이미 허용**하고 있었습니다. 중국 브라우저·위챗은 그림을 기본 `.webp` 로 저장해서 **중국 강사만** 막혔습니다(파일 고르기 창에서 회색으로 죽어 보입니다). ✅ 형식을 늘릴 때는 **세 곳을 함께** 보세요. ⛔ `.heic`(아이폰)는 열지 마세요 — **브라우저가 그리지 못해** 형식만 열면 흰 화면이 됩니다. 안 되는 이유를 말해 주는 쪽이 맞습니다. ⚠️ 형식이 안 맞을 때 뜨는 확인창이 **한국어뿐**이라, 못 읽는 강사가 「예」를 누르면 교재가 아니라 «다운로드 카드» 가 학생에게 갑니다 |
| 「교사 화면이 작고 학생 화면이 크다」 | 🔴 **타일 크기는 대개 «같습니다» — 작아 보이는 것은 칸 «안에서» 검은 띠에 둘러싸여 있기 때문입니다.** 영상 맞춤 정본 `vcSmartFitVideo`(`js/idx-main.js`)는 영상과 칸의 **방향이 다르거나 비율 차가 1.35배를 넘으면** 잘림 방지로 `contain` 을 씁니다. 2026-07-14 「교사 화면도 학생처럼 꽉 차게」 예외에 **「세로폰일 때만」** 조건이 붙어 있어서, PC·태블릿의 «오른쪽 세로 컬럼» 배치에서는 예외가 안 걸렸습니다. ⚠️ **화면마다 반대입니다 — 짐작하지 말고 재세요.** 2026-08-26 실측(칸 안에서 얼굴 그림이 차지하는 비율): PC·태블릿 **교사 28.2% / 학생 42.3%**(신고와 일치) · 폰 가로 **교사 100% / 학생 26.5%**(반대) · 폰 세로 **둘 다 100%**. ✅ 지금은 「보내온 영상이 가로면 상대 타일은 꽉 채운다」로 고쳤습니다(`js/idx-vc-mobilefix.js` ⑧). ⛔ **화면 공유는 예외입니다** — 꽉 채우면 **공유한 화면의 좌우가 잘려 나갑니다.** 판별은 그 타일에 붙는 `.vc-ss-badge`(DO 가 `fromUserId` 를 실어 줍니다 — `src/video-call-room.ts:422`). 📜 그전에는 세로폰에서 공유 화면이 실제로 잘리고 있었습니다. ⛔ 내 타일(`#vc-local-box`)은 건드리지 마세요 — 가상배경이 켜지면 `contain` 이어야 턱·목이 안 잘립니다(2026-07-13). ⚠️ 맞바꾼 것: 세로로 아주 긴 칸에서는 가로 영상의 좌우가 많이 잘립니다(얼굴이 가운데면 괜찮습니다). ⚠️ **카메라 스트림으로 재면 결과가 들쭉날쭉합니다**(메타데이터 타이밍) — 칸 크기와 영상 해상도를 **고정해 넣고 판정만 읽으세요**. 감시는 `test-harness/manual/vc-textbook-mobile-browser.mjs` ⑧. 📌 **덧붙임(2026-08-26) — 「칸 크기」와 「칸 안에서 그림이 차지하는 비율」은 다른 값입니다.** 위는 «비율» 얘기고, «칸 크기» 는 화면마다 이미 달랐습니다: PC 1280x800 은 `vc-teacher-first` 의 «내 타일 62% 축소» 로 **교사가 이미 7.15배**인데 폰 가로·세로는 **정확히 1배**였습니다(2026-07-14 「정확히 반반」 지시). 사장님 지시로 그 두 곳만 **상대 : 나 = 1.6 : 1** 로 바꿨습니다(`js/idx-vc-mobilefix.js` ⑨ · 감시 ⑨절). 🔴 **커지는 것은 «상대» 가 아니라 «교사» 입니다** — 학생 화면에서는 상대(교사)가, **교사 화면에서는 자기 자신**이 커집니다(2026-08-26 사장님 추가 지시 「교사 화면에서도 교사 자신이 크게」). ⚠️ 처음엔 «상대가 주인공» 으로 만들었는데 그 근거(「자기 얼굴이 화면을 지배하면 안 된다」)는 **우리 짐작이었고 사장님 판단은 달랐습니다** — 이 표에 그렇게 적혀 있던 줄을 되돌린 것입니다. 판정은 정본 `vcIsStaffNow()` 하나로 하고 `body.mg-teacher-self` 를 붙였다 뗍니다(`window.mgSyncTeacherSelf`). 🔴 **PC 는 폰과 «구조가 다릅니다» — 폭 비율로 풀리지 않습니다.** ≥1024px 에서는 그리드가 아니라 `css/vc-refresh.css` 가 **«상대 전체화면 + 내 타일은 오른아래 작은 PIP»**(`display:block` + 둘 다 `position:absolute`)로 그립니다. 그래서 PC 에서 바꿀 것은 fr 비중이 아니라 **«누가 PIP 인가»** 하나이고, 교사 화면에서는 그 둘을 맞바꿉니다. 폭만 바꾸면 «전체화면인데 폭만 62%» 라는 어정쩡한 상태가 됩니다(실측으로 밟음 — 높이는 `aspect-ratio`·`inset` 이 정합니다). ⚠️ 그 PIP 는 `vc-refresh.css` 의 `24%` 가 아니라 **`vc-teacher-first` 의 62%(상한 210px)** 로 그려집니다((3,5,1)이 더 셉니다) — 24% 를 그대로 베끼면 82px 짜리 PIP 가 나와 학생 화면과 짝이 안 맞습니다. ⛔ **PC 용 규칙에 `@media (min-width:1024px)` 를 반드시 두세요** — 미디어쿼리 없이 두면 폰까지 닿아 위 fr 규칙과 겹쳐 **상대가 두 번 줄어듭니다**(실측: 1.6배가 아니라 **2.58배**). 그래서 하니스는 «크다» 만이 아니라 **상한(1.75)도 함께** 봅니다. ⛔ 3명 이상에는 적용하지 마세요(주인공이 정해지지 않습니다 — `data-count="2"` 일 때만). ✅ `index.html` 의 「반반」 규칙(3272·3392·3412)과 `vc-refresh.css` 의 PIP 규칙을 **고치지 말고 defer 파일에서 위에 얹으세요** — 원래 결정이 왜 그랬는지가 남고, 그 절만 지우면 되돌아갑니다. ✅ 감시는 `test-harness/manual/vc-textbook-mobile-browser.mjs` ⑨·⑨-2(두 역할 × 세 화면. 자동으로 안 돕니다, 사람이 부릅니다) 📌 **덧붙임(2026-09-07) — 그 「꽉 채운다」가 «채우기» 가 아니라 «확대» 라, 「얼굴 크게」에서만 되돌렸습니다.** 사장님 「학생이 보는 교사 얼굴이 너무 크다 · 절반으로 · 셋 중 가장 선명하게 · 최대한 가볍게」. 🔴 세로로 긴 칸에 가로 웹캠을 `cover` 로 넣으면 **세로에 맞추느라 늘어납니다** — 실측(1905x1051 · 교사 1280x720): 칸 850x938 인데 그림은 **1669x938**(원본의 1.30배 확대)이고 그중 **폭 51% 만 보입니다**(좌우가 잘려 코·입만 남습니다). ⟹ 「크다」·「잘린다」·「흐리다」가 **한 원인**이라 확대를 멈추면 셋이 같이 풀립니다 — 얼굴 **0.51배**(정확히 절반), 확대가 없어 가장 선명하고, GPU 가 늘리는 일이 없어 가장 가볍습니다(`js/idx-vc-mobilefix.js` ⑧-2). 🔴 **「기본」(video-half)은 손대지 마세요 — `index.html` 의 «학생 기본값» 입니다.** 거기서 전체 보이기를 하면 실측 459x258 = 칸의 **27.5%** 로, 바로 위에 적힌 2026-08-26 신고 수치(28.2%)가 **그대로 재현**됩니다(반경이 학생 전원입니다). 그래서 바꾼 것은 **1:1 수업의 「얼굴 크게」·「모두 보기」뿐**입니다(「모두 보기」는 실측 1905x1051 에서 칸이 **가로**라 가드에 걸려 실제로는 안 닿습니다 — 세로로 긴 화면에서만). 🔴 **그룹 수업(3인 이상)도 빼세요** — 4인 「얼굴 크게」는 스포트라이트로 **교사** 타일이 가로(834x553)라 그대로인데 **학생** 타일만(273x361) 세로여서 42.7% 로 줄어 **고치려던 것과 방향이 반대**가 됩니다(3인 「모두 보기」는 전원 37.9%). ⑨ 「교사가 더 크다」와 같은 `data-count="2"` 조건을 씁니다. ⛔ 목록에 **`video-solo` 를 넣지 마세요** — 이름과 달리 `vcScreenSet` 의 「얼굴 화면 숨기기」·「칠판만」·「교재만」이 재활용하는 «얼굴을 감추는» 클래스입니다. ⛔ **폭(px)으로 가르지 마세요** — 「교재 크게」는 `clamp(180px,18%,300px)`, 「기본」은 `clamp(260px,27%,460px)` 라 **260~300px 이 겹쳐** 원리상 구분이 안 됩니다(폭 260 으로 짰다가 하니스가 잡았습니다). 가르는 것은 **사람이 고른 크기바 모드**(`MG_WIDE_FACE_MODES`)이고, «넓게 보는 모드만» 여는 **긍정 목록**이라 모르는 모드는 저절로 옛 동작입니다. ⚠️ 「기본」에서도 줄이려면 그 목록에 한 줄을 더하면 되지만 **사람이 정할 일**입니다. ✅ 감시는 `test-harness/vc_face_fit_contain_harness.mjs`(37종 — 판정과 래퍼를 소스에서 오려 내 **가짜 화면으로 실제로 돌립니다**. 목록도 소스에서 «읽어» 화면에 실재하는 모드인지 대조합니다. 변이 8종 전부 실제 FAIL)와 브라우저 ⑧절 14종. 📌 **덧붙임(2026-09-08) — 「교사가 더 크다」(⑨)를 «교사 화면에서만» 껐습니다.** 사장님 「교사 수업에서 교사 얼굴이 너무 크게 나와. 교사 얼굴과 학생 얼굴을 같은 크기로 줄여줘. **학생 스크린에서는 현재대로 유지하고 교사 스크린에서만** 조정해줘」. ⟹ `mg-teacher-self` 가 붙은 화면에서만 폰은 `1fr 1fr`, PC 는 «전체화면 + PIP»(절대배치)를 끄고 **그리드 위아래 반반**으로 되돌립니다(실측: PC 교사 화면 7.18배 → **1배**, 폰 1.6배 → 1배). ⛔ **학생 화면(`:not(.mg-teacher-self)`)은 한 줄도 건드리지 마세요** — 거기서는 여전히 교사가 전체화면(PC 7.18배)·1.6배(폰)입니다. 둘을 함께 반반으로 만들면 2026-07-14 로 통째로 되돌아가는 것이라 지시를 벗어납니다(그 변이를 실제로 넣어 보니 브라우저 검사 2건이 FAIL 났습니다 — 그래서 «같다» 옆에 **«학생 화면은 안 바뀌었다» 를 짝으로** 두었습니다). ⚠️ PC 는 **위아래**로 나눕니다 — 얼굴 칸이 세로로 긴 컬럼이라(실측 폭 341·높이 687) 좌우로 나누면 한 칸이 170px 짜리 세로 막대가 됩니다. ⚠️ 절대배치를 풀 때 `.video-box` 규칙만으로는 부족합니다 — «62% 축소·`margin-left:auto`·`opacity:.96`» 는 `vc-teacher-first` 의 **`#vc-local-box` 규칙(3,5,1)에만** 있어서 그 셋을 끄는 줄을 따로 둬야 합니다(id 다섯 개로 이깁니다). ⚠️ **브라우저로 다시 잴 때 캐시를 두 겹 다 끄세요** — 그 검사에 `Network.setBypassServiceWorker` 가 빠져 있어 «고치기 전» 값이 나올 수 있었습니다(함께 넣었습니다). 🔴 **그런데 이것은 «칸» 을 같게 한 것이지 «얼굴» 을 같게 한 것이 아닙니다** — 내 타일은 `contain`(2026-07-13 «가상배경 켜면 턱·목이 잘린다» 결정), 상대 타일은 `cover` 라 같은 칸에서도 그려지는 크기가 다릅니다. 함정 대조 실측(PC 교사 화면 · 칸 329×334 · 카메라 1280×720): **자기 얼굴 329×185**(위아래 검은 띠가 칸의 44.6%) 대 **학생 얼굴은 꽉 참**(대신 폭의 45%가 잘림 — 칸이 210×158 가로에서 거의 정사각으로 바뀌어 25%→45%). ⟹ 「같은 크기」인데도 **눈에는 교사가 더 작아 보일 수 있습니다.** 그것까지 맞추려면 2026-07-13 결정(`contain`)을 건드려야 해서 **사람이 정할 일**입니다. ⛔ 「교사가 여전히 작다」는 제보를 받아도 그 결정을 말없이 바꾸지 마세요. ⚠️ 그리고 이 규칙에는 **크기바 모드 제한이 없습니다**(옛 규칙도 같았습니다) — 교사 화면에서는 「모두 보기」 26.56배·「얼굴 크게」 11.94배·「교재 크게」 10.38배가 **전부 1배**가 됩니다. ⚠️ ③ 줄(`#vc-local-box` 되살리기)에서 실제로 일하는 것은 **`opacity` 하나뿐**입니다(폭 62%·`margin-left:auto` 는 ②가 이미 이깁니다) — 지우면 교사 자기 타일만 조용히 흐려지고 **«면적» 만 재는 검사로는 안 잡힙니다**(변이시험에서 실제로 못 잡았고, 그래서 브라우저 ⑨-2 가 `opacity` 도 함께 잽니다). 📜 이 자리는 지시가 세 번 바뀌었습니다(07-14 반반 → 08-26 교사 크게 → 09-08 교사 화면만 같게) — 되돌릴 때는 «어느 지시로» 인지 적으세요. |
| 「번갈아」를 안 눌렀는데 **고른 친구가 아닌 목소리**가 남 | 🔴 **화면이 아니라 «서버 폴백» 입니다 — 그리고 두 겹이라 한 번 어긋나면 영구가 됩니다.** ① `/api/voice/tts` 는 Aura-2 가 실패하면 Aura-1 로 떨어지는데, **Aura-1 목록은 12명뿐**이라 대부분의 화자가 대체됩니다. 그 여자 기본 대체값이 **`asteria`** 였는데 그것이 화면의 **«Emma» 전용 목소리** → **Lily(`delia`)가 폴백하는 순간 정확히 Emma 목소리**가 됩니다(2026-08-31 사장님 제보). Noah(`aries`)는 `orion` 이라 안 겹쳐서 «Lily 만» 증상이 났습니다. ② 더 나쁜 것 — 그 폴백 음성을 **«요청 화자(delia)» 키로 R2 에 캐시**했습니다. 그래서 Aura-2 가 살아나도 **그 문장은 영원히 Emma 목소리**입니다. 문장마다 캐시가 달라 «어떤 말은 Lily, 어떤 말은 Emma» 로 섞입니다. ⚠️ **바로 아래 melotts 주석이 같은 이유로 「캐시 금지」를 못 박고 있었는데 이 한 단계만 빠져 있었습니다** — 같은 판정이 두 곳에 있으면 한쪽만 고쳐지는 그 형태입니다. ✅ 고침 — 대체표 `AURA1_SUB`(성별 유지 + **asteria 를 대체값으로 쓰지 않음**) · 폴백 음성은 **«실제로 쓴 화자» 키로만** 저장 · 캐시 세대 **v4**(오염분 무효화) · 응답에 **`X-TTS-Engine`·`X-TTS-Speaker`** 를 실어 「고른 목소리가 아니다」를 코드 추측 없이 가릅니다. ✅ 확인은 `curl -sI -X POST … /api/voice/tts` 의 그 두 헤더. ⚠️ **문자열 하니스로는 못 잡습니다** — 목록도 규칙도 다 «있고» 틀린 것은 «누구로 떨어지는가» 뿐입니다. 감시는 `test-harness/voice_fallback_harness.mjs`(70종 — 목록·대체표를 소스에서 오려 내 **실제로 돌려** 네 친구가 폴백해도 서로 안 겹치는지·성별이 유지되는지 보고, 되돌리면 실제로 FAIL 납니다). 📌 **덧붙임(2026-08-31 같은 날) — 「목소리가 계속 변해」(Noah 를 골라 뒀는데 문장마다 다른 사람이 읽음)는 위 두 겹을 고친 뒤에도 남습니다.** 고른 화자는 그대로인데 **«소리를 만들어 오는 길» 이 문장마다 달라지는** 것이고, 셋 다 조용합니다: ① Aura-2 가 «한 번» 흔들리면 그 문장만 Aura-1 이 읽습니다(Noah `aries`→`orion`) ② 요청이 한 번 실패하면 화면이 곧바로 **기기 목소리**(`speechSynthesis`)로 읽습니다 — 원어민 음성과 전혀 달라 제일 크게 들립니다 ③ 그렇게 받은 음성을 «요청 화자» 키로 화면이 캐시하면 그 문장은 **세션 내내** 그 소리로 굳습니다(서버 R2 캐시에서 고친 것과 같은 실수가 «화면 쪽에» 한 벌 더 있었습니다). ✅ 고침 — 서버는 Aura-1 로 내려가기 전에 **Aura-2 를 한 번 더**(뉴런 소진 429 는 제외 — 답이 같습니다), 화면은 **400ms 뒤 한 번 더** 물어본 뒤에만 기기 목소리로 가고, **`X-TTS-Speaker` 가 요청 화자와 다르면 캐시하지 않습니다**(캐시 적중 응답에도 그 헤더를 실어 줍니다). ⛔ **기기 목소리 폴백 자체를 없애지 마세요** — 소리가 아예 안 나는 것이 더 나쁩니다(앱 WebView·뉴런 소진 때는 그것뿐). ⚠️ 재시도를 넣으면 **늦게 온 응답이 새 발화를 덮습니다** — `_speakSeq`(`_stopSpeak` 이 올림)로 물러나게 하세요. ✅ 판정은 문자열이 아니라 **가짜 서버로 실제로 돌려서** 했습니다(첫 요청만 실패시키기·다른 화자로 대체하기·계속 실패시키기 세 가지). 실측: 한 번 실패 → 같은 화자로 **두 번째 요청**, 기기 목소리 0회 · 대체 응답(`orion`) → **캐시 0건** · 두 번 다 실패 → 기기 목소리 1회 + 안내 한 번. ⚠️ 이 저장소 기준으로 **프록시가 `mangoi.ai` 를 막아 «진짜 서버가 얼마나 자주 흔들리는지» 는 못 쟀습니다** — 위 셋 중 무엇이 사장님 화면에서 얼마나 잦았는지는 확인되지 않았습니다. 🔴 **소리를 내는 통로가 화면마다 다릅니다 — 한 곳만 고치면 조용히 갈립니다**(2026-08-31 사장님 「영어친구랑 음성코치도」). 웜업 = 자기 `fetch`(`_ttsSpeak`) · **AI 영어친구·판단력 훈련·게임 여러 종 = 공용 모듈 `js/game-tts.js`** · 음성코치 = 자기 `fetch`. ⚠️ 음성코치는 그 블록을 `speakWord`·`speakTarget` **두 벌로 복사**해 갖고 있었습니다 — `scFetchTtsUrl` 한 곳으로 모았습니다. ⚠️ **공용 모듈에 재시도를 넣을 때는 `stop()` 이 «번호를 올리게» 하세요** — 재시도가 «멈춘 뒤» 도착하면 마이크가 열린 채 AI 목소리가 재생돼 「음성인식이 AI 말을 받아 적던」 2026-07-23 사고가 그대로 되살아납니다(그 파일 `stop()` 주석이 그 사고를 적어 두고 있습니다). ⚠️ 공용 모듈을 고쳤으면 그것을 `?v=` 로 못 박아 싣는 화면들의 번호도 **함께** 올리세요(안 올리면 immutable 캐시에 옛 모듈이 남아 «어떤 화면만 안 고쳐진» 것처럼 보입니다). ℹ️ `speech-coach-cn.html`(중국어)은 화자 개념이 없어(`lang:'zh'`) 이 증상이 성립하지 않습니다 — 손대지 않았습니다. |
| 「성우 넷을 골랐는데 **전부 여자 목소리**」 — 화면은 아무 경고도 안 함 | 🔴 **바로 윗줄(Lily→Emma)의 재발이고, 이번엔 «진단 헤더» 까지 함께 거짓말했습니다.** 2026-09-14 사장님 「이거 4개 모두 여자 목소리야.」 — 사슬이 둘입니다. ① **폴백이 «요청» 키를 오염**시켰습니다: zh 갈래의 `gtts()` 안에 `putCache(gb)` 가 있는데 그 `putCache` 는 «요청 키» 를 씁니다. Azure 성우를 콕 집은 요청이 실패해 그 폴백으로 내려오면 **여성 구글 만다린이 «azure:윈시» 키에 저장**되고, 그 문장은 영영 여자 목소리가 됩니다. ⚠️ 윗줄이 「폴백을 요청 화자 키로 캐시하지 말 것」을 이미 못 박아 두었는데 **«한 단계 아래»(gtts 안)에 같은 것이 한 벌 더** 있었습니다 — 같은 판정이 두 곳이면 한쪽만 고쳐집니다. ② **캐시 적중이 «요청» 으로 엔진을 넘겨짚었습니다**: `X-TTS-Engine` 을 `azVoice ? 'r2-cache:azure' : 'r2-cache'` 로 정했는데 그건 «무엇을 달라고 했나» 이지 «무엇이 저장돼 있나» 가 아닙니다. ⟹ ①이 넣어 둔 여성 음성이 **«azure» 라고 말하며** 나갔고, 그 헤더로 «폴백인가» 를 가리던 화면이 **아무 말도 못 했습니다.** ⚠️ **[코드에서 따라간 추론 — 라이브로 못 쟀습니다]** 첫 누름은 정직하게 경고하고 두 번째부터 캐시가 그것을 덮을 것입니다 (프록시가 `mangoi.ai` 를 막아 확인할 수 없습니다). 그렇다면 **«가끔 경고가 뜬다» 로 보여 재현이 헷갈립니다.** ✅ 고침 — ⓐ `gtts` 는 **언제나 «실제로 쓴 화자» 키**(`putCacheAs(await ttsKey(speaker), gb, 'gtts')`)로만 저장 ⓑ 저장할 때 **«무엇이 만들었나» 를 `customMetadata.eng` 에 적어 두고 캐시 적중이 그것을 «읽어서»** 말함(표시가 없는 옛 캐시본은 «모름» → 화면이 정직하게 경고) ⓒ azure 키 **세대만** 올려 이미 오염된 항목을 버림(영어·일반 중국어 캐시는 그대로 삽니다). ⛔ **`v4` 같은 전역 세대를 올려 풀지 마세요** — 영어까지 전부 다시 만들게 되어 뉴런을 태웁니다. ⚠️ **문자열 하니스로는 못 잡습니다** — `putCache` 도 헤더도 다 «있고» 틀린 것은 «어느 키에 쓰는가»·«무엇을 읽어 판정하는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 검사는 **식을 오려 내 실제로 돌려** 답으로 물으세요(`hitEng` 을 넣어 «azure/빈값/gtts» 세 답을 봅니다) — «그 글자가 있는가» 로는 **`false &&` 한 번에 뚫립니다**(실측). ⚠️ 그리고 **«만들기» 만 try 로 감싸면 안 됩니다** — 밖의 값을 참조하는 변이가 «깔끔한 FAIL» 이 아니라 **하니스 크래시**가 되어 결과줄조차 안 나옵니다(실제로 밟음). ⚠️ **«넘기는가» 만 묻지 마세요** — `putCacheAs` 가 그 인자를 «무시» 해도 통과합니다(변이 실측 60/0 초록). **«받아서 쓰는가»** 를 짝으로. 🪤 곁가지로 화면에서 **성우 버튼을 차례로 누르면 눌렀던 버튼이 전부 «■ 멈추기» 인 채** 남았습니다 — 그 절이 «지금 눌린 버튼» 을 공용 `_curBtn` 이 아니라 **지역 변수**로 들고 있어 `stopNow()` 가 그것을 몰랐습니다(`stopNow` 가 `_seq` 를 올리므로 그 뒤의 `reset()` 은 영영 못 돕니다). 🪤 그리고 이 수리가 **«식 모양» 을 글자 그대로 못 박은 전제 검사 둘을 깨뜨렸습니다** — `if (hit) return new Response(hit.body…`(캐시 블록 자르기)와 `var play=function(u){`(인자를 하나 더함). 보장은 오히려 세졌는데 검사만 빨간불입니다 — **«그 캐시를 읽는 자리»·«그 이름의 함수» 로 찾고 몸통은 중괄호 짝으로** 자르세요. ⚠️ `r2.get(cacheKey)` 는 `api-games.ts` 에 **둘** 입니다(다른 라우트) — 라우트로 먼저 좁히지 않으면 엉뚱한 응답을 검사합니다(두 번째로 밟음). ⚠️ **「중괄호 짝으로 자른다」를 적어 놓고 «길이»(700자)로 자르고 있었습니다** — 함정 대조가 그 창이 블록을 지나 옆 선언까지 먹는 것을 실측했습니다. **적어 두었는가 ≠ 사실인가** 의 그 형태라, 그 자리를 실제로 `bodyAt`(중괄호 짝)으로 고쳤습니다(두 하니스 다). ⚠️ 그리고 **「받은 표시를 쓰는가」를 «글자» 로 물으면 하드코딩(`{ eng: 'azure' }`)을 못 봅니다** — 그 한 줄이면 **이 사고가 그대로 되살아나는데 검사는 73/0 초록**이었습니다(실측). `putCacheAs` 를 **오려 내 가짜 R2 로 돌려** «적힌 값이 받은 인자에서 오는가» 를 묻고, «다른 값을 넘기면 그 값이 적힌다»·«안 넘기면 아무것도 안 적는다» 를 짝으로 두세요. ⚠️ 그때 `bodyAt` 은 **선언부까지** 돌려주므로 첫 `{` 와 마지막 `}` 사이만 떼어 내야 합니다(그대로 감싸면 호출이 0건인데 «전제» 검사가 없으면 조용히 통과합니다). ⚠️ **esbuild 게이트를 파일 맨 앞에 두고 `process.exit(0)` 하지 마세요** — 정본을 «돌리는» 절만 esbuild 가 필요한데, 그 뒤 문자열·식 검사가 통째로 빠진 채 `run.mjs` 에는 **✅ 로 찍힙니다**(«확인 안 함» 이 «문제없음» 으로 위장). 필요한 절만 감싸고 나머지는 **정본 소스에서 표를 읽어** 돌리세요. 감시는 `test-harness/azure_tts_zh_harness.mjs`(78종 — 정본을 esbuild 로 실제로 돌리고 라우트는 중괄호 짝으로 자릅니다. esbuild 가 없으면 ①~③만 건너뜁니다. 변이 **18종** 전부 실제 FAIL)와 `test-harness/manual/zh-voice-sample-browser.mjs`(브라우저 12종 — **자동으로 안 돕니다, 사람이 부릅니다.** 옛 코드로 되돌리면 사장님 사진의 증상이 그대로 재현됩니다) |
| 「소리가 안 난다」를 **TTS 요청 수로 세었더니** 멀쩡한 화면이 실패로 나옴 | 🔴 **공용 TTS(`js/game-tts.js`)는 `prefetch()` 로 미리 받아 두고 `cache[…]` 에서 재생합니다 — 두 번째부터는 네트워크 요청이 «0건» 입니다.** 그래서 「눌렀는데 `/api/voice/tts` 가 안 나갔다」는 **정상 동작**이고, 그걸 실패로 읽으면 없는 버그를 쫓습니다(2026-08-26 판단력 훈련 음성 검사에서 실제로 오독). ✅ 판정은 **«이 화면이 무엇을 읽어 달라고 했는가»**(`MangoiTTS.speak` 호출)로 하고, **화자·언어**(`speaker`·`lang`)만 진짜 요청으로 확인하세요. ⚠️ 그 모듈은 **defer** 라 화면의 인라인 스크립트보다 «나중에» 옵니다 — 서버가 캐시로 즉시 응답하면 첫 화면에서 `window.MangoiTTS` 가 아직 없어 **첫 문장만 조용히 무음**이 됩니다(적어 뒀다가 로드 뒤 마저 읽는 `_pending` 방식이 본보기: `judgment.html`). ⚠️ 화자 이름은 **Aura-2·Aura-1 두 목록에 다 있는 것**으로 고르세요(`asteria` 등) — 서버가 한 단계 폴백할 때 목소리·성별이 바뀝니다. ⛔ 보기·카드가 `<button>` 인 화면에서 스피커를 `<button>` 으로 넣지 마세요 — HTML 파서가 **바깥 버튼을 먼저 닫아** 카드가 통째로 무너지는데 **에러가 안 납니다**(`<span role="button" tabindex="0">` 로). ⛔ 주석 안에 route glob 을 그대로 적지 마세요 — 별표+슬래시가 **블록주석을 거기서 닫아** 뒷부분이 코드가 되고 「api is not defined」 로 죽습니다. 감시는 `test-harness/judgment_tts_harness.mjs`(문자열 12종)와 `test-harness/manual/judgment-tts-browser.mjs`(브라우저 21종 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 웜업(`warmup.html`)을 열었는데 **AI가 먼저 말을 안 걸어옴** | **고장이 아닙니다 — 2026-08-26부터 «연령·수준 고르기» 화면을 먼저 닫아야 첫 인사가 나갑니다.** 웜업은 첫 인사부터 `/api/warmup/chat` 호출이라, 고르기 전에 시작하면 **이미 나간 첫 질문은 되돌릴 수 없습니다** (그래서 예전의 즉시실행 `(async function initGreeting(){…})()` 을 이름 있는 함수로 바꾸고 시작 버튼만 부르게 했습니다). ⛔ 「인사가 안 나온다」고 다시 즉시실행으로 되돌리지 마세요 — `warmup_age_level_harness` E절이 FAIL 냅니다. ✅ 수업 흐름에서 곧바로 대화로 보내려면 **`?setup=0`**(눈높이는 `?age=`·`?diff=` 로 함께 넘길 수 있습니다). ⚠️ **연령대는 난이도와 «독립» 인 축입니다** — 난이도(1~8)는 문장 길이·문법, 연령대(`kid`·`child`·`teen`·`adult`)는 소재·말투만 정합니다. 한 축으로 묶으면 「성인인데 유아 문장」 이나 「초등학생인데 직장 이야기」 중 하나가 반드시 생깁니다(성인 초보자 = «adult + 레벨 1»). ⚠️ 연령대 목록은 **화면(`AGE_CATALOG`)과 서버(`src/warmup-audience.ts` `WARMUP_AGE_IDS`)가 짝**입니다 — 화면에만 늘리면 서버가 모르는 값이라 조용히 기본값(child)으로 떨어집니다(위 「화면에서 골랐는데 그 값만 저장이 안 됨」과 같은 뿌리). ⚠️ 수준 이름·교재 Lv 구간의 정본은 판단력 훈련 `judgment-level.ts` 의 `BAND_SPECS` 입니다. 웜업 화면은 그것을 «같은 말» 로 베껴 두었고 어긋나면 하니스 C절이 FAIL 냅니다 |
| 중국인 강사가 「안 된다」고만 하고 이유를 못 말함 | **이 저장소 화면의 언어는 KO/EN 두 가지뿐입니다** — `js/mango-i18n.js:25` 의 `LANG_CYCLE` 에 「중국어·일본어·베트남어 제외」라고 적혀 있습니다. 그래서 «왜 안 되는지» 알려 주는 문구가 **읽을 수 없는 글자**로 뜹니다(2026-08-25 강선생님 3건이 전부 이랬습니다 — 권한 거절·형식 거절·모델 로드 실패). ✅ 화면 전체를 옮기는 일이 아니라 **«사람을 멈춰 세우는 문구» 에만 한 줄을 덧붙이세요.** 언어 설정으로 가르면 안 됩니다 — 설정 기본이 한국어라 영영 안 보입니다. **`navigator.language` 가 zh 면 «덧붙이기»** 로 하세요(기존 한국어·영어는 그대로 남깁니다). 본보기는 `js/idx-vc-mobilefix.js` 의 `zhLine()` |
| 영어 화면에 **중국어·병음**이 섞여 나옴 | 🔴 **`review_quizzes` 는 «영어 전용 표가 아닙니다»** — 중국어 교재 「다락원」이 같은 표에 들어 있습니다(2026-08-26 실측: 활성 31건 중 **15건**). 그런데 「영어 문장인가」 판정이 오래도록 **`/[a-zA-Z]/` 하나**였습니다 = 「라틴 글자가 한 자라도 있으면 영어」. **병음은 라틴 글자로 적으므로 그 검사를 그대로 통과합니다.** 그래서 학생이 다락원 퀴즈에서 틀린 병음이 Neo4j 취약문장으로 적재되고, 영어 전용인 「수업 전 AI 웜업」 첫 화면 «🎯 오늘의 연습 포인트» 에 `"cāochǎng" · "shuāngyǎnpí"` 로 되돌아 나왔습니다(2026-08-26 사장님 제보 — D1 퀴즈 28·30번, 학생 `jeong`). ⚠️ **에러가 안 납니다** — 「영어를 못 찾았다」가 아니라 「엉뚱한 걸 영어로 봤다」라 조용히 통과합니다. ✅ 정본은 **`src/english-only.ts`** 한 파일 — **`isEnglishText(s, maxLen)`**(ASCII 인쇄가능 문자만 — 성조부호·한자·한글·가나가 한 자라도 있으면 탈락. 길이 상한은 부르는 쪽이 정합니다: 웜업 80·게임 90·단어장 30)와 **`isEnglishQuestion(q)`**(문항 지문·해설·선택지·`accept` 에 한자·가나가 있거나 「중국어/일본어/한자/병음」이 있으면 그 문항을 통째로 건너뜀) 두 겹입니다. ⛔ 이 규칙을 다른 파일에 복사하지 마세요 — `warmup-graph.ts`·`index.ts` 둘 다 여기서 import 합니다. ⛔ **「두 낱말 이상만 통과」로 풀지 마세요** — 영어 교재의 정답이 한 낱말인 문항이 실제로 많습니다(BTS 2 «빨간색의 영어 단어» → `red`). 🔴 **ETL 만 고치면 화면은 안 고쳐집니다** — Neo4j 적재는 `MERGE` 라 이미 들어간 병음이 안 사라집니다. **읽을 때(`getWeakSentences`) 한 번 더 걸러야** 오늘 바로 깨끗해집니다. 그때 Cypher `LIMIT` 을 넉넉히(×4) 받아 오세요 — 요청한 5건이 전부 중국어면 거르고 나서 0건이 되어 **영어 취약문장이 있는 학생까지 빈손**이 됩니다. ✅ **게임 쪽도 2026-08-26 에 함께 고쳤습니다**(사장님 「게임 쪽도 고쳐줘」) — `src/index.ts` 의 `warmupLessonContext()`·`handleGamesVocab()`(`pushSentence` + 학생 단어장)·`handleGamesLessons()` 영어 갈래가 전부 같은 게이트를 지납니다. 🔴 **그런데 문장만 걸러서는 절반입니다 — «코스 목록» 은 따로 고쳐야 합니다.** `/api/games/lessons` 의 영어 코스 목록은 `review_quizzes` 를 통째로 훑어 만들기 때문에 **영어 게임의 코스 고르기에 중국어 교재가 한 칸 섞여 나옵니다.** 그리고 골라도 **레슨이 0개**입니다 — 그 교재에서 라틴 글자로 된 값은 전부 «한 낱말 병음» 이라 `w.length < 2` 에 걸립니다(2026-08-26 D1 실측: 활성 문항 178개 중 라틴 글자 14개, 두 낱말 이상 **0개**). ⚠️ **`count` 를 «퀴즈 건수» 로 읽지 마세요** — `cc.count = cc.keys.length` 라 «그 코스로 묶이는 distinct textbook 문자열 수» 입니다. 다락원은 문자열이 하나라 `count=1`, 같은 날 1위는 `BTS 1`(001~008 = 8)이었습니다. 📌 **저는 이 자리를 「활성 15건이라 1위 → 전원의 기본 코스가 다락원」이라고 틀리게 적었다가 trap-check 지적으로 정정했습니다.** 고쳐야 할 것은 맞지만 심각도는 그보다 낮습니다 — **심각도를 부풀린 문장이 규칙서에 박히면 다음 사람이 엉뚱한 것을 고칩니다.** ✅ 판정은 **「`zh_vocab` 에 있는 교재 = 중국어 코스」** 로 합니다(중국어 게임이 이미 그 표를 정본으로 씁니다 — `/api/games/zh-vocab`. 새 규칙을 만들지 않습니다). ⛔ **교재 «이름» 을 보고 중국어라고 짐작하지 마세요.** ⛔ 그 조회는 `try/catch` 로 감싸세요 — 표가 없을 때 영어 목록까지 멈추면 안 됩니다. ⚠️ **정확일치만 보면 안 됩니다** — 중국어 퀴즈가 「다락원 001」처럼 과 번호를 달고 들어오는 순간 «에러 없이» 필터가 통째로 헛돕니다. 원문과 **파싱된 코스명**을 둘 다 보세요. ⚠️ 그 조회에는 `active=1` 을 **일부러 안 겁니다** — 하는 일이 «빼기» 라 넓게 잡는 쪽이 안전합니다(좁게 잡으면 중국어가 영어 목록으로 샙니다). ⚠️ **길이 상한을 통일하지 마세요** — 자리마다 다릅니다(웜업 80·게임 90·단어장 30). 80으로 좁히면 «고치려던 것» 이 아니라 «되던 것» 을 깨는 변경이 됩니다. 🔗 **«레벨 폴백» 은 웜업·게임이 같은 조건이어야 합니다** — `review_quizzes` 를 레벨로 긁는 자리가 둘인데(`warmupLessonContext`·`handleGamesVocab`) **`AND (textbook IS NULL OR textbook='')` 가 게임 쪽에만 빠져 있었습니다**(2026-08-26 사장님 지시로 맞춤). 그 조건이 없으면 「그 레벨의 **아무 교재나**」가 걸리는데, `level='Lv 3'` 인 활성 퀴즈는 **전부 「다락원」** 이라 영어 게임이 순수 병음만 받는 경로가 됩니다. ⚠️ 맞춘 뒤 그 폴백은 **오늘 기준 항상 0건**입니다 — 활성 퀴즈 29건이 전부 교재가 붙어 있어 «교재 없음» 이 0건이거든요(웜업도 같은 상태). 「레벨 폴백 = 교재에 안 묶인 공용 문제은행」이라는 뜻이고 그 은행이 아직 없는 것뿐입니다. ⛔ 「그래도 뭐라도 주자」고 그 조건을 빼지 마세요 — 조용히 잘못된 교재를 주는 것보다 0건이 낫습니다(게임은 내장 기본 어휘로 폴백합니다). 감시는 `test-harness/english_only_harness.mjs`(67종 — 게이트를 컴파일해 **실제로 돌립니다**. D1 에서 실제로 나온 병음 14개를 그대로 넣어 봅니다) |
| 문제·보기 끝에 **마침표를 일괄로 찍으려** 할 때 | 🔴 **한 규칙으로 쓸어버리면 «낱말 보기» 가 고장납니다** — 단어퀴즈 보기 「어려운」·「你好」·「go to school」 에 마침표가 붙으면 그 자체로 오류입니다. 같은 `opts` 칸인데도 `choice` 는 낱말이고 `listen` 은 «들려준 문장 + 오답 문장» 이라 **문장/낱말이 칸 이름으로 갈리지 않습니다.** ✅ 정본은 `src/sentence-punct.ts` 의 `endSentence()` 하나이고 **«찍지 않는 쪽» 으로 실패하도록** 짜여 있습니다: 이미 종결부호로 끝났으면 그대로 · 마지막 글자가 로마자·숫자·한글이 아니면 그대로(**한자는 「。」를 쓰므로 손대면 안 되고**, TOEIC 빈칸 `____`·이모지도 여기서 걸립니다) · 부호를 고를 확신이 없으면 그대로. ⚠️ **한국어 «반말» 은 평서·의문 어미가 같습니다**(「고마워」 대 「뭐 해」) — 그래서 일부러 판정을 보류합니다. 넓히면 반말 의문문에 «.» 이 박힙니다. ⚠️ **부호 «자리» 도 함정입니다** — 끝에 달린 괄호 주석은 **뒤**에 찍어야 하는데(「정답: 你好 (nǐ hǎo).」) 닫는 부호를 무조건 벗겨 내면 「(nǐ hǎo.)」 가 됩니다. 통째로 따옴표에 싸인 문장만 안쪽입니다(`"Sorry"` → `"Sorry."`). ⛔ **프롬프트에 한 줄 넣는 것으로 풀지 마세요** — 문항은 문제은행이 아니라 LLM 실시간 생성이고, 지시만으로는 안 지켜진다는 것이 이 저장소의 반복 실측입니다(단어 수·문법). **절반만 찍힌 상태는 지금보다 나쁩니다.** `PUNCTUATION_PROMPT_RULE` 은 어디까지나 보조이고 정본은 결정론 함수입니다. ⚠️ 채점 문자열(`answer_text`)을 다듬어도 안전한 이유는 `rqNorm` 이 구두점을 통째로 지우기 때문입니다 — **그 전제가 깨지면 채점이 흔들립니다.** 🔴 **«문장처럼 생겼지만 문장이 아닌» 칸이 있습니다 — 시험 문제문(`mt_questions.question_text`)이 그렇습니다.** 프롬프트가 「TOEIC-style **incomplete sentence**」를 시켜서 **빈칸이 끝에 오는 미완성 문장**이 들어옵니다(실측 10건 중 **5건** — 「I like to eat」 + 보기 `apple`). 그런데 같은 칸에 완결된 의문문(「What do I like?」)도 섞여 있어 **구별할 방법이 없습니다.** 빈칸이 `____` 로 오면 함수가 걸러 내지만 **그냥 잘려서 오면 못 걸러 냅니다** → 「I like to eat.」 이 되어 문제가 깨집니다. 그래서 그 칸은 **`endSentence` 를 아예 부르지 않는 것**이 방어선입니다(2026-08-26 에 한 번 걸었다가 실측으로 되돌렸습니다). ✅ 새 자리에 쓸 때는 **「이 칸이 완결된 문장만 담는가」를 실제 데이터로 먼저 확인**하세요 — 프롬프트에 답이 적혀 있는 경우가 많습니다. 감시는 `test-harness/sentence_punct_harness.mjs`(56종 — 함수를 실제로 돌립니다) |
| 화면을 통째로 덮는 «먼저 고르는» 화면을 만들었더니 **나갈 길이 하나도 없음** | 🔴 **`position:fixed; inset:0` 오버레이는 상단바(뒤로·홈)까지 함께 덮습니다.** 웜업 설정 화면(`#wuSetup`, z-index 120)이 그랬습니다 — 상단바에 홈으로 가는 `←` 가 있었지만 그 위를 덮고 있어서 **시작 버튼을 눌러 대화에 들어가야만** 상단바가 나왔습니다(2026-08-26 사장님 「적합한 곳에 뒤로가기 화살표랑 홈버튼 만들어줘」). ⚠️ **앱(APK)·카톡 인앱은 주소창도 뒤로 버튼도 없습니다** — 브라우저 뒤로가기로 빠져나가는 것도 못 합니다. ✅ 그런 화면에는 **나가는 길을 그 화면 «안에»** 두세요. 목록이 길면 sticky 로(스크롤 상자가 그 오버레이 자신이면 sticky 가 정상 동작합니다). ⛔ 아이콘만 두고 `title` 로 설명하지 마세요(폰에는 hover 가 없습니다) — 글자를 함께 그리세요. ⛔ `data-ko`/`data-en` 로 툴팁을 달지 마세요(그 둘은 `textContent` 를 통째로 갈아끼웁니다). ✅ «뒤로» 는 **온 곳에 따라 갈 곳이 다릅니다** — 하위 단계면 그 위 단계로, 이미 시작한 뒤 다시 연 것이면 원래 화면으로, 그 외에는 앞 화면으로. ⛔ `history.back()` 하나로 풀지 마세요 — 카톡 링크로 **바로 들어온 사람은 아무 일도 안 일어나** 「버튼이 고장났다」가 됩니다(같은 오리진 referrer + `history.length>1` 일 때만 뒤로, 아니면 홈). 감시는 `test-harness/manual/warmup-setup-browser.mjs` ⑦·⑧절(브라우저 10건 — 자동으로 안 돕니다) |
| 「자꾸 튕겨요」·「영상이 안 보여요」인데 **사이트·D1·cron 은 전부 정상** | 🔴 **`X-Turn-Source` 부터 보세요 — 코드가 아니라 «시크릿» 문제일 수 있습니다.** `curl -sI https://mangoi.ai/api/turn-config | grep -i x-turn-source` 가 **`public-fallback`** 이면 모든 수업이 무료 공개 TURN(`openrelay.metered.ca`)으로 릴레이되고 있는 것입니다. `src/index.ts` 의 그 폴백 주석이 직접 경고합니다 — 「50명을 받을 수 있는 서버가 아니라서, **아무 에러 없이 «영상만 안 나오는»** 상태가 된다」. 2026-08-26 실측: **RTT 389~629ms 인데 손실은 0.7~4.2%(낮음)** — «회선이 나쁘다» 가 아니라 «경로가 멀다» 는 지문입니다. 강사 재입장률 21세션 중 13세션(**61.9%**), 최다 8~10회. ⚠️ **네 단계를 구분하세요**: `kv-cache`·`cloudflare` = 정상 · `last-known-good` = CF API 가 흔들리는 중(자격증명은 아직 진짜) · **`public-fallback` = 이것만 진짜 문제.** 🔴 **`X-Turn-Source` 만 보지 말고 `X-Turn-Detail` 을 함께 보세요.** `public-fallback` 하나가 **서로 완전히 다른 세 가지를 뭉뚱그리기** 때문입니다: ① 시크릿이 없다 ② CF API 가 거절했다(403·429·한도초과) ③ CF 에 연결조차 안 됐다. 해야 할 일이 각각 «키 등록»·«키 재발급/계정 확인»·«장애 대기» 로 다른데 글자는 똑같았습니다 — 2026-08-26 에 제가 그래서 반나절을 잘못 짚었습니다. ✅ **같은 날 사장님 지시로 이유를 헤더에 함께 싣습니다**(`src/index.ts` `handleTurnConfig`): `cache`·`ok`(정상) · `no-secrets`(키 없음) · `cf-http-401`/`403`(키 무효 → **재발급**) · `cf-http-429`(사용량 한도) · `cf-fetch-error`(CF 장애). `curl -sI https://mangoi.ai/api/turn-config | grep -i '^x-turn-'`. ⛔ **`no-secrets` 가 아니면 키를 새로 넣지 마세요** — 이미 있는 키를 덮어쓰면 「고쳤다」는 기록만 남고 원인은 그대로입니다. ⛔ 그 값에 자격증명·CF 응답 **본문**을 넣지 마세요(이 API 는 로그인 없이 누구나 부릅니다 — 상태 «코드» 까지만). ⚠️ 값은 늘어날 수 있으니 읽는 쪽은 **모르는 값이면 표시만 하고 판정하지 마세요**(`deploy.yml`·`ops/mangoi-watchdog.sh` 가 그렇게 합니다). 감시는 `test-harness/turn_detail_harness.mjs`(29건 — 함수를 오려 내 **실제로 돌립니다**). ⚠️ 다만 **`public-fallback` 이 나오려면 CF 호출 실패 «그리고» 24시간짜리 마지막 성공분(`turn:ice-servers:last-good`)이 비어 있어야** 합니다 — 즉 잠깐 흔들린 게 아니라 **최근 24시간 안에 성공이 0회**라는 뜻입니다(LKG 는 두 워커가 같은 KV 를 쓰므로 어느 쪽이든 한 번만 성공하면 채워집니다). ⚠️ **`vc_relay_force`(중국 회선 강제 릴레이)도 같이 죽습니다** — `api-mango.ts` 의 `hasCfTurn` 조건에 묶여 있어 시크릿이 없으면 켜도 안 켜집니다(무료 TURN 으로 강제 릴레이하면 더 나빠지니 맞는 판단이지만, 「대책을 넣었다」고 적힌 것이 한 번도 안 돈 상태가 됩니다). ✅ **먼저 «시크릿이 정말 없는지» 부터 눈으로 확인하세요** — Cloudflare 대시보드 > Workers & Pages > 해당 워커 > Settings > Variables and Secrets 에 이름이 뜹니다(값은 안 보입니다). 없을 때만 워커 **두 벌 모두**에 `npx wrangler secret put TURN_KEY_ID`·`TURN_KEY_API_TOKEN`(+ `--env production`). ✅ 이제 배포마다 `deploy.yml` 이 두 워커의 `X-Turn-Source` 를 요약에 찍고, 2층 감시견(`ops/mangoi-watchdog.sh`)이 15분 연속 `public-fallback` 이면 문자를 보냅니다. ⛔ 배포를 «실패» 시키지는 않습니다(TURN 없이도 수업은 느리게나마 되고, 막으면 무관한 수정까지 못 나갑니다). 감시는 `test-harness/vc_turn_quality_harness.mjs` 1부. 📌 **실사고 경과(2026-08-26)** — 07:1x 사장님 브라우저 실측 `public-fallback` → **07:40:54 배포 run #2717 러너 실측 `kv-cache`(두 워커 다)**. 사장님은 그사이 시크릿을 **넣지 않으셨습니다.** 그런데 `turn:ice-servers:v1` 을 쓰는 곳은 `src/index.ts:4213` 한 곳뿐이고 그것도 CF 발급이 성공해야만 실행되며, 그 KV 네임스페이스를 쓰는 워커는 이 워커 한 벌뿐입니다(`wrangler.toml` 62·189행 = 같은 워커의 두 환경). 🔴 **⟹ 시크릿은 «있습니다» — 같은 날 대시보드에서 «워커 두 벌 모두» 등록된 것을 눈으로 확인했습니다.** 즉 시크릿이 있는데도 최근 24시간 동안 CF 발급이 한 번도 성공하지 못한 구간이 있었고 그게 그날 아침 끝난 것입니다. **고칠 것은 «키 등록» 이 아니라 «왜 그 키로 발급이 24시간 넘게 실패했나» 입니다.** ✅ **이제는 `X-Turn-Detail` 이 그 자리에서 알려 줍니다**(위). 지난 일을 캐야 한다면 **Workers 로그**를 보세요 — 그 워커는 관찰 가능성이 켜져 있고, `console.error` 가 상태 코드를 그대로 남깁니다. ⚠️ **「시크릿이 없다」는 제 추론이었고 측정이 아니었습니다** — 측정된 것은 「그 시점에 무료 폴백을 타고 있었다」까지입니다. 위 ①②③ 중 어느 쪽이든 같은 증상을 냅니다. ⛔ 원인을 단정해 적지 마세요(「하기로 한 것」이 「했다」로 적혀 6일 뒤 재발한 전례). 관측 기록은 `docs/ENVIRONMENT.md` 표에 **줄을 덧붙이고** 윗줄은 지우지 마세요 |
| 「끊긴다」 제보를 **`vc_quality` 로 확인하려는데 기록이 거의 없음** | 🔴 **기록을 못 남긴 사람이 곧 «영상이 죽은 사람» 입니다 — 표본이 제일 필요한 쪽이 통째로 빠져 있었습니다.** 적응 루프(`js/idx-main.js`)의 `if (dSent + dLost < 25) return;` 이 `vcQualityAcc()` **앞**이라, 카메라를 껐거나 영상이 죽으면 전송 패킷이 0 이 되어 **그 자리에서 돌아갑니다.** 2026-08-26 실측: 하루 예상 ~2,900건 중 실제 **25건(약 1%)**. ⚠️ 2026-08-21 에 AAO 구간을 오디오 통계로 이어 적게 고쳤지만 그건 **절반**이었습니다 — AAO 는 오디오 손실 12% 이상에서만 켜지므로, 회선은 멀쩡한데 영상만 죽은 사람은 여전히 0건이었습니다. ✅ 지금은 그 틱을 `loss = -1` 로 넘겨 **`novideo`(영상 없던 틱 수)로 세기만** 합니다. ⛔ **손실 0% 로 적지 마세요** — 표본이 없는 것과 손실 0%는 다른 사실이고, 0 으로 적으면 **영상이 죽은 사람이 «회선이 제일 좋은 사람»** 이 되어 원인 추적을 정반대로 이끕니다. ✅ 그래서 표본이 하나도 없어도(`novideo` 만 있어도) 60초 요약은 보냅니다 — 그게 사각지대의 핵심입니다. ⚠️ `novideo` 는 **`ALTER` 로만** 붙였습니다(`attendance.host` 와 같은 방식) — `vc_quality` 의 `CREATE` 가 `api-mango.ts`·`api-admin.ts` **두 벌**이라 한쪽 모양만 바꾸면 새 DB 에서 «먼저 도는 쪽이 이기는» 사고가 납니다. 그래서 **첫 로그가 들어와야 칸이 생깁니다**(배포 직후 SQL 은 `no such column: novideo`, 배포 실패가 아닙니다). ✅ `vcQualityAcc()` 는 **`js/idx-vc-qlog.js`(defer)** 로 옮겼습니다 — 첫 화면 예산 여유가 351바이트였는데 1.5KB 를 덜어 **1,825바이트로 늘었습니다.** ⚠️ **`window.vcRoomId` 를 쓰지 마세요**(`let` 선언이라 영원히 undefined — 방 번호가 99.7% 비어 있던 사고). classic script 끼리는 전역 어휘 바인딩을 공유하므로 **bare `vcRoomId`** 로 읽습니다. 감시는 `test-harness/vc_turn_quality_harness.mjs` 2·3부(3부는 그 파일을 **실제로 실행해** payload 를 읽습니다) |
| 「고쳐야 한다」고 보고했는데 알고 보니 **이미 고쳐져 있었음** | 🔴 **재기 전에 «무엇을 재고 있는지» 부터 확인하세요 — 작업 사본이 며칠씩 묵어 있을 수 있습니다.** 2026-09-08 실측: 적응 화질 루프의 회복 문턱을 재고 「RTT 250~450 사각지대가 있습니다」라고 보고하며 `250 → 420` 을 제안했는데, 제 작업 사본이 **8/28 에 멈춰 있었고** 그 수정은 **9/1·9/2·9/5(PR #771·#772·#835)에 이미 들어가 배포까지 끝나** 있었습니다. 게다가 실제 수정은 제 제안(고정값 420)보다 나은 방식(**연결별 기준 RTT 대비**)이라, 제안대로 밀어 넣었으면 **그 상대 기준 로직을 덮어써서 되돌리는** 변경이 될 뻔했습니다. ⚠️ **하니스도 타입체크도 이걸 못 봅니다** — 묵은 사본 안에서는 전부 정상입니다. ✅ 코드를 «재기 전에» **`git fetch origin main && git log --oneline -10 origin/main`** 을 돌리세요(30초). 그 파일이 그 사이 바뀌었는지는 `git log --oneline -5 origin/main -- <파일>` 로 바로 나옵니다. ✅ 측정 결과를 보고할 때 **«어느 커밋에서 쟀는지»** 를 함께 적으세요 — 그 한 줄이 있으면 상대가 곧바로 알아챕니다. 📜 같은 줄기: 바로 위 「원인을 «찾았다» 고 보고했는데 추론이었음」·「문서에 「고쳤다」고 적혀 있는데 같은 사고가 또 남」 — 셋 다 **«지금 코드를 보라»** 로 끝납니다 |
| 원인을 «찾았다» 고 보고했는데 알고 보니 **추론이었음** | 🔴 **측정한 것과 거기서 추론한 것을 구분해 적으세요 — 안 그러면 사장님이 그 추론대로 «고치러» 가십니다.** 2026-08-26 실제로 밟았습니다: 측정된 것은 「그 시점에 무료 TURN 폴백을 타고 있었다」까지였는데, 저는 「그러니 시크릿이 없다」로 건너뛰어 **키 발급 절차까지 안내**했습니다. 시크릿은 **워커 두 벌 모두 있었습니다.** 사장님이 안 넣으셨기에 망정이지, 넣으셨다면 **이미 있는 키를 덮어쓰고 「고쳤다」는 기록만 남을 뻔했습니다**(원인은 그대로 살아 있는 채로). ✅ 보고할 때 **「잰 값」과 「그래서 내린 판단」을 줄을 나눠** 적으세요. 판단에는 «확인 방법» 을 함께 다세요(「대시보드에서 이름이 보이는지 30초면 됩니다」). ✅ 사람에게 **바꾸라고** 하기 전에 **먼저 확인할 것**을 주세요 — 「없으면 넣으세요」가 아니라 「있는지 보고, 없을 때만 넣으세요」. ⛔ 문서·주석·안내 문구에 원인을 **단정형으로** 적지 마세요(같은 뿌리: 위 「문서에 「고쳤다」고 적혀 있는데 같은 사고가 또 남」). ⚠️ **같은 단정을 여러 곳에 적어 뒀으면 한 곳만 고쳐서는 안 끝납니다** — 저는 `::error::` 문구는 고치고 **바로 위 라벨을 놓쳤고**, 배포 로그를 눈으로 읽다 겨우 찾았습니다. 문자열 하니스는 «내가 검사로 적은 곳» 밖은 못 봅니다 |
| 축이 **둘**인데 한쪽을 «임시 탐색» 분기 안에 넣어 두었더니, 그 탐색 중에는 다른 축이 **조용히 버려짐** | 🔴 **분기 안에 «둘 다» 넣으면, 그 분기가 꺼진 동안 «흔들리면 안 되는 것» 과 «흔들릴 것이 없는 것» 이 함께 멈춥니다.** 판단력 훈련이 그랬습니다 — 읽기 밴드(실력)와 나이대(소재·어투)는 독립인 축인데, 저장 코드가 둘 다 `if (!probing)`(=레벨 찾기 중이 아님) 안에 있었습니다. 밴드는 탐색 중 흔들리면 결과가 오염되니 맞지만 **소재 축은 흔들릴 것이 없습니다.** 그래서 첫 설정 카드에서 「성인 + 레벨 찾기」를 한 번에 고르면 **배치 6문항이 전부 아이 소재**로 나가고, 배치가 끝난 **뒤에야** 성인으로 바뀌었습니다(2026-08-26). ⚠️ **에러가 안 납니다** — 요청에 실어 보낸 `age_group` 을 서버가 받고도 **쓰지 않고 버리는** 것이라, 화면에는 «성인을 골랐는데 학교 이야기가 나온다» 로만 보입니다. ⚠️ **문자열 하니스가 못 잡습니다** — 그 줄은 «있고» 값도 맞습니다. 틀린 것은 **«어디에 있는가»** 뿐입니다. ✅ 검사도 위치로 하세요: 소스에서 두 지점의 `indexOf` 를 재어 «앞에 오는가» 를 판정합니다(본보기 `test-harness/judgment_first_setup_harness.mjs` F절 — 되돌리면 실제로 FAIL 납니다). ✅ 고칠 때는 **분기 밖으로 뺀 축이 그 분기 안에 남아 있지 않은지**도 함께 검사하세요(두 번 쓰기 방지) |
| 첫 화면에 «정하세요» 카드를 띄웠는데 **아무도 안 고름** | **긴 설명 상자가 위에 펼쳐져 있으면 카드가 스크롤 밖입니다.** `judgment.html` 의 「왜 판단력 훈련을 하나요?」는 기본이 펼침(`class="explain open"`)이라 폰에서 두 화면쯤 됩니다 — 390px 실측(2026-08-26): 카드 top **966px**, 화면은 844px 이라 **한 번도 안 보였습니다.** ⚠️ 「띄웠다」와 「보인다」는 다릅니다(같은 뿌리: 「열렸다≠보인다」 항목들) — `classList` 나 `innerHTML` 만 확인하면 이 사고를 못 봅니다. ✅ 반드시 **`getBoundingClientRect().top` 이 `innerHeight` 안인지** 재세요. ✅ 고침은 **그 화면일 때만 설명을 접기**(`classList.remove('open')`) — ⛔ 상자를 지우지는 마세요, 제목 줄이 그대로 토글이라 한 번 누르면 다시 펼쳐집니다 📌 **덧붙임(2026-09-15) — «sticky 시작 버튼» 이 있으면 스크롤할 «이유» 자체가 사라집니다.** 웜업 설정 화면이 그랬습니다: 📘 교재 고르기가 8단계 수준 목록 «아래» 라 실측 top **PC 1920×1040 에서 1380px · 폰 390×844 에서 1577px · 노트북 1280×720 에서 1380px** 로 전부 첫 화면 밖인데, 🚀 시작 버튼은 `position:sticky;bottom:-2px` 라 **늘 첫 화면 안**(PC t=942 · 폰 t=757)에 떠 있습니다 ⟹ 수준을 고르면 바로 아래 시작 버튼이 보이니 **아무도 아래를 안 봅니다.** 사장님이 그대로 «자유 대화» 로 시작하시고 「아직 아무것도 안되어 있는데」라고 하셨습니다 — **제가 잰 범위(로컬 서버 + 그 API 스텁)에서는 기능이 한 줄도 안 고장나 있었습니다** — 라이브 응답은 프록시 403 이라 못 쟀습니다. ⚠️ 이건 「안 고름」이 아니라 **「그런 것이 있는 줄도 모름」** 이라 더 나쁩니다(화면이 «안 골랐다» 는 사실조차 말해 주지 않습니다). ✅ 고침은 **시작 버튼 «바로 위» 에 sticky «지금 상태» 한 줄**(`#wusBookNow` — 「💬 지금은 교재 없이 자유 대화 · 고르기 ↓」, 누르면 그 자리로 `scrollIntoView`). 상태를 말해 주니 «안 골랐다» 가 눈에 보이고, 누를 곳이 첫 화면 안에 있습니다. 🔄 **2026-09-16 에 사장님 지시로 실제로 올렸습니다** — 시안 4종 중 **3안**(「📘 교재로 웜업」/「💬 자유 대화」 로 갈라 시작, 교재 칸은 «언어 바로 다음» · 기본은 접힘). ⛔ **그래도 «그냥 올리기» 는 여전히 금지입니다** — `btsListHtml()` 이 고른 수준(`_warmLevel`)으로 권을 near/rest 로 가르므로 수준보다 «앞» 에 두면 **고르는 도중 목록이 눈앞에서 재정렬됩니다.** ✅ 그래서 **«펼친 순간» 의 수준을 붙잡아 둡니다**(`_btsSortLv` · `btsSortLevel()` — `btsListHtml(sortLv)` 이 인자로 받습니다. ⛔ 그 함수 «안» 에서 `_warmLevel` 을 직접 읽지 마세요). 🔴 **그리고 «펼친 갈래»(`.on`)와 «실제로 무엇으로 시작하는가»(「지금」 배지)를 «갈라야» 합니다** — 합치면 펼치기만 하고 안 고른 사람에게 「교재로 웜업 · 지금」 이라 말하는데 그 상태로 시작하면 **자유 대화**입니다(화면이 거짓말합니다. CLAUDE.md 2장 「상태가 셋인데 저장이 둘이면」 의 형제). ⛔ «펼쳤다»(`_btsOpen`)를 **저장하지 마세요** — «아직 안 정함» 과 «사람이 골랐음» 이 같아집니다. 저장하는 것은 «고른 교재»(`_btsVol`) 하나입니다. ⛔ 두 갈래와 교재 칸은 **중국어에서 «짝» 으로** 감추세요(한쪽만 감추면 「교재로 웜업」 이 남아 없는 곳으로 데려갑니다). ⛔ 목록 안의 「교재 없이 자유 대화」 줄은 **두 갈래 카드와 중복**이라 뺐습니다(`btsListHtml(sortLv, hideFree)`) — 한 화면이 같은 말을 두 번 하면 안 됩니다. ⛔ 접힌 칸으로 **스크롤만** 시키지 마세요 — `display:none` 이라 브라우저가 통째로 무시해 «눌러도 아무 일 없는» 버튼이 됩니다(펼친 «뒤» 에 `block:'nearest'`). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무엇이 어디에 그려지고 화면이 사실을 말하는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 📌 이 변경이 기존 브라우저 하니스의 **전제 16건을 깨뜨렸습니다** — 「교재 자리는 여전히 첫 화면 밖이다」가 그 대표입니다. 그 주석이 「섹션 순서를 바꿔 해결했다면 이 줄이 먼저 빨간불이 되어 알려 줍니다」라고 **약속한 그대로** 먼저 걸렸습니다. ⛔ 느슨하게 풀지 말고 **새 경계로 옮겨 적으세요**(「고르기 전에는 접혀 있다」·「누르면 펼쳐진다」 짝). 감시는 `test-harness/manual/warmup-two-way-browser.mjs`(브라우저 63종 — **자동으로 안 돕니다, 사람이 부릅니다.** 변이 6종 전부 실제 FAIL) ⚠️ sticky 를 두 개 쌓을 때는 **`bottom` 을 앞 버튼 높이만큼 띄우세요**(실측 시작 버튼 PC 56px·폰 49px → 60px·53px) — 숫자를 검사에 베끼지 말고 **«겹치지 않는가» 로** 물으세요. ⚠️ **대화 중 ⚙️ 패널에는 그 줄이 아예 없습니다** — 거기서 교재를 바꾸는 유일한 길이 「연령·수준 다시 고르기」인데 이름이 교재를 암시하지 않아 아무도 못 찾았습니다(「연령·수준·**교재** 다시 고르기」로 고침). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «어디에 그려지는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 🪤 그 브라우저 검사에서 **회차 사이에 `localStorage` 가 넘어가 다음 폭이 «중국어» 로 시작**해 멀쩡한 절 6건이 거짓 FAIL 났고, 절이 중국어로 «끝나서» 다음 실행의 앞 절 37건까지 무너졌습니다 — **폭마다 지우고, 절이 끝나면 되돌려 놓으세요**(지울 키를 골라 적지 말 것: 옛 ① 절이 두 개만 지워서 이 사고가 났습니다). 감시는 `test-harness/manual/warmup-bts-book-browser.mjs` ⑩절(브라우저 24종 — 세 폭 × «첫 화면 안»·«겹치지 않음»·«맨 위가 그 줄»·«눌러서 그 자리로»·짝 둘. **자동으로 안 돕니다, 사람이 부릅니다.** 변이 5종 전부 실제 FAIL)과 `warmup_bts_book_harness` ⑧·⑧-a절(자동 12종 — 그 함수를 **오려 내 실제로 돌려** «무슨 글자» 로 묻고, 누르는 길은 **가짜 DOM 에 실제로 클릭을 쏴서** 봅니다). 🪤 **그 배선을 처음에 «문자열» 로 물었다가 뚫렸습니다** — `if(false && t.getAttribute('data-jump') === 'books'){` 한 글자로 버튼을 **완전히 죽여도** `'books'` 리터럴이 그 줄에 남아 **PASS 74 / FAIL 0 통과**했습니다(함정 대조 실측). 🪤 그리고 **가짜 DOM 의 초기값이 마크업과 달라도** 뚫립니다 — 마크업이 `<button … hidden>` 인데 가짜 요소를 `hidden:false` 로 시작하면 `el.hidden = false;` 를 지우는 변이(= 버튼이 **영영 안 보임**)가 그대로 통과합니다. **가짜 요소의 시작 상태를 마크업과 같게** 두세요. ⚠️ **브라우저 검사의 «위치» 검사도 같은 이유로 헛돕니다** — 감춰진 요소는 `getBoundingClientRect()` 가 전부 0이라 「첫 화면 안」·「안 겹침」이 저절로 참입니다. **«실제로 그려져 있다(높이 > 0)» 를 전제 검사로** 두세요. ⚠️ **sticky 를 더하면 그 아래 «누를 것» 을 가립니다** — 실측(폰 390 · 스크롤 11지점): 시작 버튼만 있을 때 5곳 → 이 줄을 더해 8곳이고, **바와 시작 버튼을 합치면** 최대 두 개가 동시에 가려집니다(바 «혼자» 가리는 것은 최대 1개 — 하니스가 그 숫자를 찍습니다)(스크롤로 비켜 갈 수 있는 수준 버튼이라 감수했습니다 — 되돌릴지는 사람이 정할 일). 검사는 «내가 남을 덮는가» 를 **FAIL 이 아니라 개수로 찍어** 둡니다. 🪤 **그리고 그 하니스 샌드박스에 정본이 쓰는 헬퍼(`escapeHtml`)가 없으면 «깔끔한 FAIL» 이 아니라 «크래시» 가 되어 결과줄조차 안 나옵니다** — 헬퍼도 **소스에서 오려 내 주입**하고 «부를 때까지» `try/catch` 로 감싸세요(2026-09-15 실제로 밟음). ⚠️ **파일을 고쳤다 되돌리는 변이시험과 `--fast` 를 «동시에» 돌리지 마세요** — 회귀가 변이된 파일을 읽어 «내가 깨뜨렸나» 를 한참 뒤집니다. |
| 기능이 «있는데» 아무도 못 씀 — 이름표가 사실과 다름 | 🔴 **수업 관찰 중 «채팅» 은 채팅이 아닙니다 — 서버가 귓속말로 돌립니다**(`video-call-room.ts` `handleObserverWhisper`). 참관자가 쓴 글은 방에 안 뿌려지고 강사 소켓에만 갑니다. 그런데 화면 어디에도 «귓속말» 이라는 말이 없어서 2026-08-26 사장님이 「여기 어디에 귓속말이 있어?」 → 그냥 채팅인 줄 알고 쓰신 뒤 「채팅창에 아무것도 문자 써도 안 나타나는데?」 로 이어졌습니다. ⚠️ **에러가 안 납니다** — 기능은 정상 동작했고, 틀린 것은 «이름표» 와 «에코 없음» 뿐입니다. ⚠️ 참관자 메시지는 방송을 안 하므로 **내 화면에 에코가 없습니다.** 토스트만 몇 초 떴다 사라지면 쓰는 사람은 기능이 죽은 줄 압니다 — 보낸 기록을 **채팅창에 한 줄 남기세요**(`vcAddChatSystem`, `js/idx-whisper.js` 의 `ack`). ✅ 대상은 이미 있던 1:1 칩(`vcChatTarget`·`toUserId`)을 그대로 씁니다 — 대상 없음 = 강사 전원, 이름 칩 = 그 한 사람(학생 포함). ⛔ 대상 지정 경로를 `broadcast` 로 바꾸지 마세요(학생 전원이 남에게 간 지시를 봅니다). ⛔ **학생에게 보낼 때 보낸 사람 이름을 넘기지 마세요** — 참관은 인원수·입퇴장 어디에도 안 나오는 «투명 유령» 설계라, 낯선 «관찰자» 이름이 뜨면 그 설계가 화면에서만 깨집니다(화면이 «사무실» 로 그립니다). ⛔ 참관자 글을 `/api/chat/messages` 로 저장하지 마세요 — 학생이 채팅창 「이전 대화 보기」로 그대로 읽습니다(그래서 `vcSendChat` 을 감싸 저장을 건너뜁니다). ⚠️ 라벨을 고칠 때 `textContent` 만 바꾸면 i18n 이 «채팅» 으로 되돌립니다 — `data-ko`/`data-en` 까지 함께(같은 장 「JS 로 그린 라벨」). 감시는 `whisper_delivery_harness`(48종)와 `test-harness/manual/observer-whisper-browser.mjs`(브라우저 22종 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 「칸을 만드는 곳」을 한 곳으로 믿고 막았는데 **아무것도 안 막힘** | 🔴 **경로마다 만드는 함수가 다릅니다.** 화상수업 얼굴 칸은 «명단» 경로가 `vcEnsureParticipantBox` 로 만들고, **«offer» 경로는 `vcAddRemoteVideo` 가 «자기 힘으로» 직접 만듭니다**(그 함수는 `vcEnsureParticipantBox` 를 한 번도 안 부릅니다 — 2026-09-11 실측: 호출부는 `idx-main.js` 4091·4162 두 곳뿐이고 둘 다 로스터). ⚠️ 그래서 «칸 만들기» 를 막겠다고 `vcEnsureParticipantBox` 만 감싸면 **참관자·끊긴 소켓처럼 offer 로만 알게 된 상대에게는 한 줄도 안 닿습니다** — 실제로 그렇게 짰다가 호출부를 세어 보고 고쳤습니다(하니스는 그 상태로도 초록이었습니다. 가짜 원본을 제가 만들어 주니까요). ✅ **막기 전에 «그 화면에서 그 칸을 실제로 만드는 함수» 를 `grep` 으로 세세요** — `id = 'vc-video-' + userId` 같은 **DOM 을 쓰는 줄**로 찾아야 하고(호출부 이름으로 찾으면 놓칩니다), 템플릿 리터럴(`` `vc-video-${userId}` ``)이 섞여 있어 **따옴표 모양을 하나만 찾으면 빠집니다.** ✅ 검사는 «그 함수를 감쌌는가» 가 아니라 **«칸이 생겼는가»** 로 물으세요(본보기 `observer_camera_guard_harness` J절 — 가짜 원본이 칸을 «만들었다» 고 기록하게 하고 그것을 셉니다). ⚠️ 그리고 **«안 만든다» 옆에 «진짜 사람은 그대로 만든다» 를 짝으로** 두세요 — 없으면 «전부 안 만들기» 도 통과합니다(변이시험에서 실측). |
| 화상수업 참가자를 «번호로» 콕 집어야 할 때 | 🔴 **화상방(DO)의 `userId` 는 접속마다 새로 발급되는 임시 번호입니다** — 계정 아이디(`delaware`)와 아무 상관이 없습니다(`generateUserId()`). ⚠️ 그런데 **`attendance.user_id` 가 «마침» 그 임시 번호**입니다(`mango-attendance.js` 가 `vcUserId` 를 그대로 적습니다). 계정 아이디는 `attendance.account_uid` 에 **따로** 있습니다 — 둘을 바꿔 쓰면 조용히 아무에게도 안 갑니다. ⚠️ 그리고 **재접속하면 새 번호가 발급되어 옛 행의 번호는 죽습니다.** 그래서 번호 하나만 믿으면 「보냈는데 안 온다」가 됩니다. ✅ **번호 → 이름 순으로 찾으세요**(`video-call-room.ts` 의 `/whisper`). ⛔ 이름은 **완전일치 + 후보가 정확히 하나일 때만** — 둘 이상이면 붙이지 않습니다(같은 장 「남의 이름이 뜸」과 같은 규칙). ⛔ **못 찾았다고 «강사 전원» 으로 폴백하지 마세요** — 학생에게 보내려던 글이 강사에게 가는 것이 오배달입니다. `delivered:0` 으로 정직하게 답하세요. ✅ 화면도 «갔다» 를 `ok:true` 로 판정하면 안 됩니다 — 기록은 남으므로 아무도 안 받아도 `ok:true` 입니다. **`delivered > 0`** 으로 가르세요 |
| 어두운 화면에서 «글자가 읽히나» 를 쟀는데 멀쩡한 글자가 실패로 나옴 | 🔴 **반투명 배경을 «불투명» 으로 읽은 것입니다.** 같은 장 「색이 맞는지 검사를 다 통과했는데 글자가 안 보임」의 뒷면인데, 이쪽은 **거짓 실패**입니다. 배경을 찾을 때 조상을 타고 올라가다 «색이 있는 첫 조상» 에서 멈추면, 그것이 `rgba(245,158,11,0.1)` 같은 얇은 층일 때 «밝은 주황» 이 답이 됩니다. 2026-08-26 실측: `ghost-view.html` 귓속말 상자의 초록 글자가 **1.53:1**(실패)로 나왔지만, 그 층을 아래(`rgb(20,33,59)`)와 **합성**하니 **9.73:1**(정상)이었습니다. ✅ 반투명 층을 만나면 멈추지 말고 **쌓아 두었다가 아래에서 위로 합성**하세요(`alpha`가 1인 층을 만나면 거기서 멈춤). 본보기는 `test-harness/manual/ghostview-whisper-browser.mjs` ⑥절. 🔴 **덧붙임(2026-08-26) — 층을 «못 찾는» 경우가 하나 더 있습니다: 배경이 그라데이션이면 `backgroundColor` 가 `transparent` 라 그 층이 통째로 건너뛰어집니다.** 어두운 그라데이션 화면이 **흰 바탕**으로 계산돼 «흰 바탕에 흰 글자» 가 되고, 멀쩡한 버튼이 1.18 로 나옵니다(웜업 설정 화면 `.wus-nav`·`.wu-setup` 에서 실측). ✅ `backgroundColor` 가 투명이면 **`backgroundImage` 의 첫 색**을 그 층으로 쓰세요 — 본보기 `test-harness/manual/warmup-setup-browser.mjs` ⑦절. ✅ 같은 호출에서 «줄 수» 도 함께 틀렸습니다 — `상자 높이 ÷ lineHeight` 로 세면 **padding·border 가 섞여** 한 줄짜리가 2줄로 잡힙니다(34.8÷16.8=2.07). 글자 상자만 남기고 세세요 📌 **덧붙임(2026-09-09) — 배경만이 아니라 «글자 쪽»(`opacity`)과 «배경의 어느 지점» 도 함께 봐야 합니다.** 홈 CTA 의 새 상태 줄에서 세 겹으로 헛돌았습니다: ① 새로 만든 요소를 **한 번도 안 쟀고**(라벨만 쟀습니다) ② **`opacity` 를 안 봤습니다** — 그것은 요소를 통째로 흐리게 만드는데 `color` 만 읽으면 실제보다 세게 나옵니다 ③ 그라데이션에서 **첫 stop(가장 밝은 쪽)만** 배경으로 썼습니다. 실측: 그 상태에서 «통과» 로 나온 줄이 실제로는 **4.12:1**(11px 글자 기준 미달)이었고, 셋을 고치자 그대로 FAIL 이 났습니다(`opacity` 를 빼니 5.46). ✅ `opacity` 는 **조상까지 곱해** 글자색에 알파로 넣고, 그라데이션은 **모든 stop 중 최악** 을 쓰세요.  🔴 **덧붙임(2026-09-09) — 배경이 «사진»(`url(...)`)이면 CSS 를 아무리 잘 합성해도 «원리상» 못 잽니다. 그때는 픽셀을 찍어 재세요.** 화상수업 로비가 그렇습니다 — 뒤에 배경 사진이 깔려 있고 안내 상자는 10% 틴트, `.lobby-box` 도 반투명(0.55~0.65)이라 사진이 그대로 비쳐 올라옵니다. CSS 합성으로 재니 **이미 몇 달째 배포돼 잘 보이던 초록 줄까지 «대비 2.94, 실패»** 가 나왔습니다(실측). 원인 셋 — ① 한 요소의 `background-color` 와 `background-image` 는 «둘 중 하나» 가 아니라 **«색 위에 이미지»** 인데 나란히 «후보» 로 세면 불투명한 색이 있는데도 아래가 비치는 조합이 만들어지고 ② 사진은 색을 알 방법이 없으며 ③ 그래서 최악 조합이 `body` 의 흰색까지 끌어옵니다. ⚠️ **«틀린 실패» 는 «틀린 통과» 만큼 나쁩니다** — 다음 사람이 멀쩡한 색을 고치러 갑니다. ✅ **글자를 잠깐 `color:transparent` 로 만들어 그 자리를 `Page.captureScreenshot` 으로 찍고**(= 진짜 배경) 계산된 글자색을 그 위에 합성하세요. 사진·그라데이션·반투명이 몇 겹이든 안 틀립니다(PNG 는 `node:zlib` 로 직접 풉니다 — 8bit·비인터레이스면 60줄이면 됩니다). 🔴 **그때 `clip` 은 «문서» 좌표이고 `getBoundingClientRect()` 는 «뷰포트» 좌표입니다** — `scrollIntoView` 한 뒤 `scrollX/scrollY` 를 안 더하면 **엉뚱한 자리(배경 사진)를 찍고** 「대비 1.17」이 나옵니다(실제로 밟음). ✅ 그리고 **되돌려 보면 실제로 FAIL 나는지** 확인하세요 — 옛 10% 틴트로 되돌리면 3.02·3.01 로 실제 FAIL 납니다. 본보기 `test-harness/manual/meet-room-code-browser.mjs` ④절 |
| 중국어 수업이 끝났는데 **복습퀴즈가 영어로** 나옴 | 🔴 **중국어 퀴즈가 없어서가 아닙니다 — 다락원 제1~14과가 이미 다 있습니다**(2026-08-26 실측: `review_quizzes` 활성 16건, `lang='zh'`·`source='passage'` = 사람이 만든 교재본문 기반). 닿지 못한 이유가 **셋**이고 셋 다 «에러가 안 나는» 종류였습니다. ① **언어를 «수업» 에서 안 읽습니다** — `js/idx-x8.js` 의 `st.lang` 은 `localStorage`(`mangoi_review_lang`·`mangoi_game_lang`)만 보고 기본값이 `'en'` 입니다. 즉 «학생이 예전에 게임탭에서 골라둔 값» 이지 «이 수업이 무슨 언어인가» 가 **아닙니다.** ② **같은 교재를 두 이름으로 부릅니다** — 라이브러리 `textbook_files` 는 「**다락원 중국어 마스터 3**」(583쪽)·「… 练习册」(72쪽)인데 콘텐츠(`zh_passage`·`zh_vocab`·`review_quizzes`)는 전부 「**다락원**」입니다. 서버 매칭이 `LOWER(textbook)=LOWER(?)` **정확일치**라 영영 안 맞습니다. ⚠️ `ctx()` 의 안전기본값(`다락원`/`Lv 3`)은 «교재가 비었을 때만» 걸려서, **교재를 열면 오히려 그 구제가 꺼집니다.** ③ **진도(과)를 아무도 안 적습니다** — `mangoi_current_lesson` 은 **읽는 코드만 둘**(`idx-x8.js`·`idx-main.js`)이고 **`setItem` 이 저장소 전체에 0곳**이었습니다. 그래서 `lesson_no` 는 늘 0 → «교재+과» 1순위 매칭이 한 번도 발동하지 않았습니다. 🔴 **④ 적으려 해도 읽을 데가 없습니다** — 그 교재 **583쪽이 전부 「미분류 레슨」이고 `unit_no` 도 583개 전부 NULL** 입니다(2장 「200쪽짜리 교재를 묶음 나누기로…」와 같은 상태). ⛔ **파일 이름에서 과를 짐작하지 마세요** — 단서가 한 글자도 없습니다. ✅ ①②는 **서버에서** 고쳤습니다: 정본 `src/zh-textbook.ts` 가 라이브러리 이름을 콘텐츠 표기로 잇고, `/api/review-quiz/auto` 가 **교재를 근거로 `lang` 을 zh 로 올립니다**(클라가 보낸 lang 만 믿지 않음). ⛔ 교재 «이름» 만 보고 중국어라고 짐작하지 마세요 — 판정 근거는 **`zh_passage`·`zh_vocab` 에 그 교재가 실제로 있는가**이고, 후보 목록은 그 두 표에서 **읽어 옵니다**(하드코딩 금지). ⛔ 못 이으면 «모름» 이 아니라 **«중국어 아님» 으로 실패**시킵니다 — 영어 수업을 중국어로 오인하는 쪽이 더 나쁩니다. ✅ ③은 **학생이 과를 고르게** 했습니다(2026-08-26 사장님 결정) — `js/idx-vc-mobilefix.js` ⑩절이 「📖 진도」 줄을 그립니다. ⚠️ 그 줄은 **`#rqv-body` «밖»** 에 둡니다(`renderQ()` 가 body 를 통째로 다시 그립니다 — `#rqv-live` 와 같은 이유). ⚠️ 물어보기는 **`auto_generate:0`** — 빠뜨리면 탭을 열 때마다 AI 가 퀴즈를 찍어내 `review_quizzes` 에 쌓입니다. ⚠️ 언어 자동전환은 **한 번만**(토글이라 두 번이면 도로 영어). `st.lang` 은 `idx-x8.js` 안에 갇혀 밖에서 못 읽으니 **버튼 «글자»** 로 현재 상태를 봅니다. ⚠️ 교재 표시 이름은 **「중국어 마스터」**(2026-08-26 사장님 지시) — D1 값은 안 건드리고 **읽을 때** 바꿉니다. ⚠️ `/review-quiz.html` 은 `&lang=en` **하드코딩**이라 구조적으로 영어만 나옵니다 — 중국어는 `/review-quiz-cn.html` 이라는 **다른 페이지**이고, 그리로 가는 길이 목록 **맨 아래**에만 있어 아무도 못 찾았습니다(퀴즈가 여러 개면 스크롤 밖). 이제 마지막으로 중국어를 본 학생에게는 **맨 위**에도 놓습니다. ⛔ 자동으로 넘기지는 마세요(영어 퀴즈를 풀러 온 학생을 납치합니다). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «누가 무엇을 근거로 정하나» 뿐입니다. 📌 **후속(같은 날) — 교재를 과별로 다시 올리기로 했습니다.** ⑪절(`js/idx-vc-mobilefix.js`)이 **교재를 넘기면 `mangoi_current_lesson` 을 기록**합니다(정본 훅은 `window.pdfSyncSeqIdx` — 다른 기기가 넘겨도 여기로 옵니다). ⚠️ 과를 «모르면» 0 으로 **지웁니다** — 안 지우면 앞 교재의 과가 남아 엉뚱한 퀴즈가 붙습니다. ⚠️ 학생이 손으로 고른 값은 **교재가 실제로 다른 과로 옮겨갔을 때만** 덮어씁니다. ⛔ 파일 이름에서 과를 읽지 마세요(`Slide3.JPG` 의 3). 🔴 **재업로드 폴더 이름에 함정이 있습니다** — 업로더는 교재명을 **2단계 폴더명**에서 가져오므로, 업로더 화면의 일반 팁(`다락원/마스터3/제1과`)을 그대로 따르면 **교재명이 「마스터3」로 바뀌어 중국어 연결이 통째로 끊깁니다.** 숫자 폴더(`001`)는 **유닛마다 다른 책**으로 쪼개집니다. ✅ 재업로드는 **지금 쓰는 교재 이름을 최상위 폴더**로 두고 그 안에 과 폴더만: `다락원 중국어 마스터 3/제1과/…`. ⚠️ **같은 이름으로 그냥 올리면 옛 583쪽과 섞이고**, 숨김은 교재 «이름» 단위라 숨기면 새것까지 숨습니다 — 먼저 `/api/admin/textbook-files/rebook`(dry_run 기본·본사 전용)으로 옛 묶음을 「(구본)」으로 옮겨 숨긴 뒤 올리세요(R2 파일은 안 건드리고 이름만 바꿉니다). 📄 절차서: `docs/중국어교재_과별_재업로드_절차_2026-08-26.md`. 감시는 `test-harness/zh_textbook_match_harness.mjs`(41종 — 해석 함수와 **업로더 분류기를 컴파일해 실제로 돌립니다**. D1 실측 교재 이름 그대로, 폴더 구조 3종의 결과까지)와 `test-harness/manual/review-quiz-lesson-browser.mjs`(브라우저 29종 — 자동으로 안 돕니다, 사람이 부릅니다). 📌 **덧붙임(2026-09-15) — 같은 «두 이름» 이 영어(웜업)에도 그대로 있었습니다.** 웜업 서버(`warmupLessonContext`, `src/index.ts`)도 `review_quizzes` 를 **`LOWER(textbook)=LOWER(?)` 정확일치**로 찾는데, 화면이 보내던 이름은 «BTS 1 (Welcome to school, School Stuff, My classroom)» 이고 **D1 의 실제 이름은 «BTS 1 001 (Welcome to school)» … «BTS 1 008» · «BTS 2 Korea (Shapes and colors)» · «BTS 12 Korea (Jobs, Going to work)»** 입니다(2026-09-15 실측 — BTS 1 은 과마다 **40문항**, 「Hello, I am a student.」 같은 진짜 교재 문장). ⟹ 서버의 «오늘 배울 핵심 영어 문장 예시» 코드가 **멀쩡한데 닿지 못해** 죽어 있었습니다(에러 없음 — 조회가 0행일 뿐). ✅ 고침은 **이름을 지어내지 않고 서버에게 묻는 것** — 이미 있는 학생용 API `GET /api/games/lessons?glang=en&course=…` 가 `lessons:[{seq,title,key,sentences}]` 를 주고 그 **`key` 가 곧 D1 교재 이름**이라, 그것을 `WCTX.textbook` 으로 보내면 정확일치가 걸립니다(`warmup.html` 의 `btsCourseMatch`·`btsLoadLessons`·`btsPickLesson`). ⛔ 서버는 한 줄도 안 고쳤습니다. 🔴 **코스 이름은 서버 파서(`parseEn`)가 이름 «모양» 에 따라 다르게 만듭니다** — 「BTS 1 001 (…)」은 코스 «BTS 1» 인데 「BTS 2 Korea (…)」는 **문자열 전체**가 코스명입니다. 그래서 «BTS 2» 로 물으면 안 걸리고 **응답의 `courses` 목록에서 찾아야** 합니다. ⛔ 그때 **뒤가 숫자면 다른 권**입니다(`^BTS\s*0*N(?!\d)`) — 안 그러면 «BTS 1» 이 «BTS 10»·«BTS 12» 를 물어 남의 교재 문장을 보냅니다. ⛔ D1 이름을 화면 표에 손으로 적지 마세요(교재가 늘면 조용히 어긋나고 1과 문장만 영원히 씁니다). ⛔ 목록에 없는 키를 보내지 마세요 — 서버가 못 찾는데 화면은 「문장 N개」라고 말합니다(거짓말). ⚠️ 못 받으면(통신 실패·`ok:false`·빈 목록) **«고치기 전» 으로 떨어뜨리세요** — 권 이름 그대로 보내면 예전과 똑같습니다. ⚠️ 반경은 **32권 중 3권**뿐입니다(BTS 1·2·12) — 나머지는 문장이 D1 에 없어 «주제만» 그대로입니다. 감시는 `warmup_bts_book_harness` ⑥⑦절(가짜 fetch 로 **실제로 돌림** · 변이 12종 실제 FAIL)과 `manual/warmup-bts-book-browser.mjs` ⑨절(브라우저 · 변이 5종 실제 FAIL). 📄 `docs/작업기록/260915_웜업_교재의_진짜문장_붙이기.md` |
| 부정 검사를 «그 이름이 파일에 없다» 로 썼는데 멀쩡한 코드가 FAIL | 🔴 **같은 장 「부정 검사가 자기 주석을 잡는다」의 형제입니다 — 이쪽은 «주석» 이 아니라 «정상적인 코드» 를 잡습니다.** `observer_camera_guard_harness` 의 「채팅 버튼은 숨기지 않는다」가 `!/vc-dock-chat/.test(guardCode)` 였습니다. 그래서 2026-08-26 에 그 버튼의 **이름표만 바꾸는**(숨기지 않는) 코드가 생기자 곧바로 FAIL 이 났습니다. ✅ 물어야 할 것은 «그 이름이 나오는가» 가 아니라 **«숨기는가»** 입니다 — 숨김 규칙의 «대상 목록» 만 뽑아서 판정하세요(`guardCode.match(/body\.vc-observer #[\w-]+/g)`). ✅ 같은 이유로 「이 블록 안에 X 가 없다」류는 `slice(i, i+N)` 으로 대충 자르지 마세요 — 옆 함수가 딸려 들어옵니다. 실제로 `handleObserverWhisper` 뒤의 `handleChatMessage` 안 `broadcastAll` 이 잡혀 「대상 지정이 방 전체로 샌다」는 거짓 FAIL 이 났습니다. **중괄호 짝을 맞춰** 자르세요(`whisper_delivery_harness` 의 `blockAt`). 📌 **덧붙임(2026-08-26) — 검사 범위를 «길이» 로 자르지 마세요.** `vc_turn_quality_harness` 가 「그 단계 앞 4000자 안에 `exit 0` 이 있나」였는데, 그 단계에 줄을 더하자 **보장은 그대로인데 검사만 깨졌습니다.** 「그 단계의 끝」(다음 `- name:`)까지로 자르세요. ⚠️ 그리고 **앵커를 «본문 글자» 로 잡으면 그 위 주석 블록에 먼저 걸립니다** — 실제로 905자짜리 엉뚱한 조각을 잘랐습니다. `\n      - name: <이름>` 처럼 **구조**를 앵커로 쓰세요. ⚠️ 그 앵커 자체가 `- name:` 이므로 «다음 것» 을 찾을 때 `indexOf(..., start + 1)` 로 한 칸 밀어야 합니다(안 그러면 빈 조각) |
| **중괄호 짝**으로 함수 몸통을 잘랐는데 엉뚱한 조각이 나옴 | 🔴 **TypeScript 에서는 «여는 중괄호» 가 몸통보다 «반환 타입» 에 먼저 나올 수 있습니다.** `async function sendPlainSms(…): Promise<{ ok: boolean; muted?: boolean }> {` 를 중괄호만 세어 자르면 **`{ ok: boolean; … }` 를 몸통으로 잡고** 그 안에서 게이트를 찾으니 «없다» 가 됩니다 — 2026-09-08 실측으로 **멀쩡한 코드에 7건 거짓 FAIL** 이 났습니다. ⚠️ 제네릭 인자(`Promise<…>`)와 함수 인자 목록(`(…)`)도 같은 이유로 함께 세야 합니다. ✅ 여는 중괄호를 고를 때 **괄호 깊이 0 · 꺾쇠 깊이 0** 인 것만 몸통으로 인정하세요. ✅ 그리고 **「전제: 몸통을 잘라 냈다」를 검사 한 줄로 박아 두세요** — 이번에도 그 줄이 있어서 «검사가 틀렸다» 를 «코드가 틀렸다» 로 오독하지 않았습니다(없으면 없는 버그를 쫓습니다). 📜 형제: 바로 위 줄의 「`slice(i, i+N)` 으로 대충 자르지 마세요」 — 그쪽은 «너무 많이», 이쪽은 «엉뚱한 데» 를 자릅니다 |
| 「눈높이를 맞춘다」고 만든 화면인데 **첫 문장만 눈높이를 안 봄** | 🔴 **AI 를 거치지 않는 «고정 문자열» 이 한 줄 섞여 있으면, 레벨·연령 축을 아무리 잘 만들어도 그 문장이 규칙을 어깁니다.** 웜업이 그랬습니다 — 서버 `WARMUP_LEVELS` 는 레벨 1 을 「3~5단어·현재시제」로 못 박아 두었는데 화면의 첫 인사(`fallbackGreeting`)는 레벨과 무관한 **15단어**였습니다(«Let's warm up before class» — 관용구까지. 2026-08-26 사장님 「낮은 단계는 처음 인사말도 잘 이해 못 한다」). ⚠️ **그 문장이 어렵다는 증거는 이미 같은 파일에 있었습니다** — 기계번역이 「수업 전에 따뜻하게하자」로 직역해 `curatedMeaning()` 에 손으로 의역을 박아 둔 주석(2026-08-24). ⚠️ 교재가 배정된 학생은 AI 가 인사를 만들어 난이도가 반영되고 **교재 미배정(= 대개 신입 = 낮은 단계)만 그 고정 문장**을 받습니다 — 제일 취약한 학생이 제일 어려운 인사를 받는 구조였습니다. ✅ 낮은 단계 인사는 **손으로 쓴 고정 문자열**로 둡니다(`BEGINNER_GREETINGS`, `warmup.html`). ⛔ LLM 생성으로 바꾸지 마세요 — 낮은 단계에서 단어 수를 조이면 모델이 to·is·a 를 떨어뜨립니다(같은 장 「Want play with me」). ⛔ 한국어를 영어 말풍선에 섞지 마세요 — AI 말풍선은 자동 낭독되고(`addMsg` → `speak`) 영어 TTS 가 한글을 읽으면 소리가 뭉개집니다. 도움말은 `sys` 줄로 «말풍선 밖» 에 씁니다. ⛔ **길이만 줄이지 마세요** — 초보에게 진짜 벽은 «열린 질문» 입니다. 3단어로 줄여도 «How are you?» 면 답을 스스로 만들어야 해서 그대로 막힙니다. Yes/No 나 양자택일(고를 말이 질문 안에 들어 있음)로 쓰세요 — 이 저장소의 `WARMUP_FALLBACK_QUESTIONS.low`(«Do you like pizza or chicken?»)가 이미 그 형태입니다. ✅ 감시는 `warmup_age_level_harness` G절 — «그 문자열이 있는가» 가 아니라 **서버 규칙에서 상한을 읽어 인사말의 단어를 셉니다**(되돌리면 실제로 FAIL 납니다) |
| «첫 턴» 을 「히스토리가 비었는가」로 판정했더니 **한쪽 경로의 학생이 통째로 빠짐** | 🔴 **화면이 «학생 대신» 보내는 합성 발화가 있으면 그것도 `role:'user'` 로 저장됩니다.** 웜업 교재 연동 경로는 화면이 먼저 AI 에게 인사를 시키는데(kickoff), 그 발화가 히스토리에 남아 **학생의 진짜 첫마디가 올 때 히스토리는 이미 비어 있지 않습니다.** 그래서 「히스토리가 비었으면 첫 턴」 판정은 교재 배정 학생을 **영영 못 셉니다** — 통계에 «자유 대화 학생만» 담기는데 숫자는 멀쩡해 보입니다(에러도 안 납니다). ✅ 합성 발화는 **요청에 표시해서 보내고**(`kickoff:1`) 판정은 «학생 발화 수» 로 하세요. ✅ 그 판정은 순수 함수 한 곳에 모아 하니스가 **직접 돌리게** 하세요(`warmupShouldMarkFirstReply` · `warmup_age_level_harness` H절) — 문자열 검사는 「그 줄이 있는가」만 보고 «누가 빠지는가» 는 못 봅니다. ℹ️ 웜업이 «몇 단계로 쓰는가» 를 세는 표는 `warmup_session_log` 하나이고 CREATE 정본은 `src/warmup-log.ts` 입니다(세션당 쓰기 최대 3회 — 발화마다 쓰지 않습니다) |
| 초보에게 «따라 말할 예문» 을 만들어 줄 때 | 🔴 **그 문장은 학생이 «그대로 따라 말하는» 문장입니다 — 하나만 틀려도 그걸 배웁니다.** 그래서 웜업 「대답 보기」 칩은 LLM 으로 만들지 않고 **AI 질문에서 결정론적으로 유도**합니다(정본 `src/warmup-answers.ts`). ⛔ 「그래도 뭐라도 보여 주자」고 넓히지 마세요 — 만들 수 없으면 **안 만듭니다**(같은 규칙: 강사 이름은 후보가 둘이면 안 붙인다). 열린 질문(wh-)·부정사가 섞인 질문에는 칩이 «없는 것이 정상» 입니다. ⚠️ **실제로 밟은 사고** — 「do you like **pizza**」에서 앞말을 떼려고 관사까지 떼었더니 「Are you **a student** or a teacher?」가 **「I am student.」** 라는 비문이 됐습니다(2026-08-26, 첫 시험에서 잡음). 관사는 떼지 말고, 「I like a dog.」 처럼 아무도 안 쓰는 문장이 나올 조합이면 **그 경로를 통째로 포기**하고 Yes/No 로 떨어지게 하세요. ✅ 단어 수 상한은 서버 `WARMUP_LEVELS` 와 **짝**입니다 — 하니스가 서버 문자열에서 상한을 읽어 대조합니다(`warmup_age_level_harness` I절, 함수를 **실제로 돌립니다**). ✅ 화면은 목록을 «만들지» 않고 서버가 준 것을 그리기만 합니다 — 규칙을 두 곳에 복제하면 반드시 어긋납니다. 첫 인사처럼 서버를 안 거치는 자리는 값을 화면에 두되 **하니스가 정본 함수 결과와 대조**합니다 |
| `<style>` 안 **CSS 주석**에 「」 를 썼더니 한자 폰트 검사가 FAIL | 🔴 **CLAUDE.md 2장에 이미 있는 함정인데 또 밟았습니다**(2026-08-26 웜업 대답 보기 카드 CSS). `subtitle_meaning_harness` 의 「`<style>` 안에 CJK 홑낫표가 없다」는 **`<script>` 와 HTML 주석만 벗기고 CSS 주석은 그대로 셉니다** — 화면에 안 보이는 설명 주석이 「이 화면은 983KB 한자 폰트를 받는다」로 판정됩니다. ✅ **CSS 주석에서는 `« »` 를 쓰세요**(코드·JS 주석은 그대로 둬도 됩니다 — 검사 대상이 `<style>` 안입니다). ✅ 확인은 `<style>` 블록만 뽑아 `[「」]` 를 세면 1초입니다 |
| 금액에 «배수»를 적용할 때 (수업 시간·기간 등) | 🔴 **제일 무서운 것은 «두 번 곱하기» 입니다** — 화면이 곱해서 보내고 서버가 또 곱하면 40분이 **4배**가 되어 그대로 과금 사고입니다(하니스가 그 상태를 «틀린 값»으로 못 박아 두었습니다). ✅ **곱하는 자리는 «저장하는 순간» 딱 한 곳**으로 두세요 — 정본은 `src/enroll-fee.ts` 의 `computeMonthlyFee()` 하나이고, 저장 뒤 `monthly_fee_krw` 는 «이미 곱해진 최종값» 이라 확정 단계·정기결제 예약·CSV·워드 요약 어디서도 다시 곱하지 않습니다. ✅ 곱하기 **«전» 값을 따로 남기세요**(`base_fee_krw`) — 안 남기면 「이 금액이 왜 이 금액인지」가 사라져 되돌아볼 수도, 다시 계산할 수도 없습니다. ⛔ **기준가를 못 구했다고 아무 숫자나 넣지 마세요** — 그 금액은 정기결제 자동화가 **실제로 청구**합니다. 못 구하면 `null` 로 두면 확정 단계가 예전처럼 「월 수강료가 0원입니다」 로 멈춥니다(2장 「측정할 수 없는 값을 그럴듯하게 채우고 싶을 때」와 같은 뿌리). ⛔ **사람이 적은 값을 대리점 단가로 덮어쓰지 마세요** — 협의가가 사라집니다(입력값 우선). ⚠️ **이미 «다른 배수»를 거친 금액에 또 곱하지 않도록** 경로를 갈라 보세요 — `api-pay.ts` 의 토스 결제 자동활성화가 넣는 금액은 이미 `enrollQuoteCalc`(길이배수 포함)을 지난 **실제 결제액**이라 여기에 곱하면 곧바로 과금 사고입니다. ⚠️ 「사람이 적은 값이 몇 분 기준인가」를 근거로 적어 두세요 — 이 저장소는 「수업 시간」 칸이 2026-08-26 에 처음 생겨 **그 전 등록은 전부 20분 기준**이라는 것이 근거입니다(가져오기 양식에는 지금도 그 칸이 없습니다). ✅ 사람이 **확정 «전에» 근거를 보게** 하세요(확정 패널의 「요금 근거 — 20분 기준 X원 × 30분 1.5배」) — 숫자만 보여 주면 틀려도 아무도 모릅니다. ⚠️ 분당 단가는 길이와 **무관해야** 합니다(`class-policy.ts`: 「깎아 주면 그 순간 손해」) — 하니스가 20·30·40분의 분당 단가가 같은지 잽니다. 감시는 `test-harness/enroll_fee_length_harness.mjs`(25종 — 계산 정본을 **컴파일해 실제로 돌립니다**) |
| 슬라이더·조절 기능을 붙였는데 **올려도 안 커짐**(내려도 곧 되돌아감) | 🔴 **두 가지가 겹칩니다 — «상한» 과 «되돌리는 놈».** ① `HTMLMediaElement.volume` 은 **상한이 1 = «지금 소리»** 입니다. 그래서 0~100% 슬라이더는 «작게» 만 되고 «크게» 는 **구조적으로 불가능**합니다. 2026-08-26 안드로이드(삼성) 「볼륨키를 눌러도 소리가 안 커진다」 제보에 정확히 그 슬라이더가 붙었습니다 — 화면에는 멀쩡히 보이고 값도 저장돼서 «고쳐졌다» 로 읽히는 것이 더 나쁩니다. **크게 하려면 WebAudio `GainNode`(+ 찢어짐 막을 `DynamicsCompressor`) 뿐입니다.** ② `element.volume` 은 **가만두면 유지되지 않습니다** — `idx-main.js` 에 소리 자가복구가 **둘**(`vcEnsureRemoteAudio` = 수업 중 아무 터치·키 입력마다 · `vcAudioWatchdog` = 3초마다)이고 둘 다 `volume = 1` 로 되돌립니다. 그래서 학생이 값을 내려도 화면을 한 번 만지면 튕겨 올라갑니다(폴링으로 다시 쓰면 «진동» 이 됩니다). ✅ 지금은 그 두 곳이 `vcOutVol()` 을 읽고, 증폭 중(`window.vcOutBoost`)에는 **아예 손을 뗍니다.** ⛔ 증폭 때문에 타일을 음소거할 때는 **플래그를 «음소거보다 먼저»** 켜세요 — 3초 감시견이 그 음소거를 «소리 막힘» 으로 오판해 「🔊 소리 켜기」 배너를 띄우고 보조 오디오를 헛되이 만듭니다. ⛔ 묶음은 «스트림 id» 가 아니라 **«오디오 트랙 id»** 로 — 보조 오디오(`vc-aud-*`)는 같은 트랙을 새 `MediaStream` 에 담으므로 스트림으로 세면 **같은 소리를 두 번 증폭**합니다. ⛔ `AudioContext` 가 `running` 인 것을 확인하기 전에 음소거하지 마세요(자동재생 정책에 막히면 **소리가 통째로 사라집니다**) — 실패하면 전부 «고치기 전» 으로 되돌아가야 합니다. ⚠️ **문자열 하니스·타입체크는 이걸 못 봅니다** — 함수도 값도 다 «있고» 틀린 것은 «상한» 과 «순서» 뿐이라 211건이 전부 초록이었습니다. 감시는 `test-harness/vc_output_volume_harness.mjs`(32종 — 파일을 **실제로 실행해** 저장·범위 제한까지 확인하고, 자가복구가 하드코딩 1 로 되돌아오면 FAIL) |
| 표에 **열을 하나 더했는데** 휴대폰에서 그 칸이 «맨 앞» 으로 튀어나옴 | 🔴 **`order` 의 기본값은 «DOM 순서» 가 아니라 «0» 입니다.** 관리자 수강신청 등록 표는 휴대폰에서 `<tr>` 를 카드로 눕히는데(`admin-inline-c.css` 의 `#en-multi-table tbody tr td.en-c-*`), 칸마다 `order: 1~8` 을 손으로 매겨 놨습니다. 여기에 새 `<td>` 를 넣고 규칙을 안 주면 **0 이라 학생 아이디보다 앞** 에 놓입니다. 게다가 `grid-column` 도 없으면 `span 1` 이라 12칸 중 한 칸(실측 1500px 에서 **68px**, 1150px 에서 **39px**)만 차지해 「20분」 글자가 잘리고, 12칸 합이 어긋나 **원래의 두 줄 배치까지 무너집니다**(2026-08-26 「수업 시간」 열에서 실제로 밟아 그 상태로 병합돼 있었습니다). ⚠️ 그 블록은 **미디어쿼리 밖** 이라 넓은 화면에도 적용됩니다 — 폰으로만 확인하면 못 봅니다. ✅ 새 칸에는 **`order` 와 `grid-column: span N` 을 반드시 함께** 주고, **줄마다 합이 정확히 12** 가 되게 다시 짜세요. ✅ 검사는 «규칙이 있는가» 만 보지 말고 CSS 에서 order·span 을 **실제로 읽어 줄별 합을 세게** 하세요(`enroll_class_minutes_harness` ③). ⚠️ 표 자체의 `min-width` 도 함께 올리세요(안 올리면 데스크톱에서 칸이 서로 눌립니다) |
| 「학생으로 들어왔는데 **내 얼굴이 크고 교사가 작다**」 | 🔴 **CSS 가 아니라 «역할» 입니다 — `body.mg-teacher-self` 가 붙은 것입니다.** 그 클래스는 「교사 화면에서는 교사 «자신»이 크게」(2026-08-26 지시) 규칙의 스위치이고, 붙는 조건은 `vcIsStaffNow()` 하나였습니다. 그런데 **`jeong`(사장님)은 홈 통합 로그인에서 관리자 폴백으로 들어가 학생 세션(`mangoi_logged_user`)이 없습니다** → 입장 역할 판정이 계정 역할을 못 찾고 **관리자 세션 하나만 보고 `admin` 으로 떨어집니다**(`js/idx-main.js` 의 `else if (_admUid) window.vcMyRole = 'admin'` — 묻지도 않습니다). ⚠️ **서버가 그 오판을 되돌려 주지 못합니다** — `/api/class/verify-room` 은 `role==='admin'` 이면 «privileged» 로 즉시 통과시키고 `resolved_role` 을 **안 줍니다**(`src/api-mango.ts`). 그래서 「이 예약의 학생이면 역할을 내린다」 교정(`idx-main.js`)이 **admin 에서만 안 돕니다** → 한 번 admin 으로 잡히면 **수업 내내** 뒤집힌 채로 갑니다(2026-08-26 실측 10분). 역할이 `teacher`·`student` 로 잡힌 입장은 몇 초 만에 정상으로 돌아오므로 **«다시 들어가니 괜찮던데»** 가 되어 재현이 들쭉날쭉합니다. ✅ 그래서 판정을 **«내가 스태프인가» 가 아니라 «상대 중에 교사가 있는가»** 로 옮겼습니다(`js/idx-vc-mobilefix.js` ⑨-3 `mgRemoteTeacherPresent`). ⛔ 역할 정본(`vcIsStaffNow`)이나 `idx-main.js` 를 고쳐서 풀지 마세요 — 그 값에는 화면공유·교재 넘김·장치 도우미 **권한**이 걸려 있습니다. 여기서 필요한 것은 «누가 주인공인가» 뿐입니다. ⛔ 이름 휴리스틱(「교사 …」)은 **내 타일을 내리는 데만** 쓰세요(권한을 올리면 아이디에 teacher 가 든 학생이 반 전체 교재를 넘깁니다). ⚠️ **문자열 하니스·타입체크는 이걸 못 봅니다** — 함수도 값도 다 «있고» 틀린 것은 «누구에게 걸리는가» 뿐입니다. 확인은 브라우저에서 `[document.body.classList.contains('mg-teacher-self'), window.vcMyRole]` 를 재세요. 감시는 `test-harness/manual/vc-textbook-mobile-browser.mjs` ⑨-4(자동으로 안 돕니다, 사람이 부릅니다) |
| 교재를 폴더째 올렸는데 라이브러리에 **「001」·「002」 라는 이름 없는 묶음**이 수십 개 생김 | 🔴 **업로더가 «안 도는» 게 아니라 «이름을 틀리게 붙이는» 것입니다.** `classifyFile`(`textbook-uploader.html`)은 교재명을 **2단계 폴더명**에서 가져오므로 `BTS 2/001/Slide1.JPG` 는 교재명이 **「001」** 이 됩니다. 2026-08-14 주석은 「BTS 2 001」을 약속하지만, 유닛 번호를 붙이는 그 규칙이 `textbook.indexOf(유닛) < 0` 일 때만 도는데 **교재 이름 자체가 이미 「001」** 이라 조건이 늘 거짓이어서 **한 번도 붙지 않았습니다**(2026-08-27 브라우저 실측: 001·002·034 그대로). ⚠️ **반경이 «내 PC» 보다 훨씬 넓습니다** — 공용 자료실(서버)의 묶음 이름은 파일명 앞 **[대괄호] 하나뿐**이고(`js/idx-x3.js` `_serverFilesToBooks`) 출판사는 안 실립니다 → **BTS 2 의 001 과 BTS 3 의 001 이 같은 「001」 묶음으로 합쳐집니다.** IndexedDB(내 PC)에서는 출판사로 갈라져 **멀쩡해 보여** 발견이 늦습니다. ⚠️ **문자열 하니스·타입체크는 이걸 못 잡습니다** — 규칙도 값도 다 «있고» 틀린 것은 «무슨 이름이 나오는가» 뿐이라 211건이 전부 초록이었습니다. ✅ 2026-08-27 수리: 유닛뿐인 2단계 폴더(`001`·`001. Title`·`A`)는 **상위 폴더 이름을 앞에 붙입니다**(「BTS 2 001」). ⚠️ 네 자리(`2019` = 연도)는 **일부러 유닛으로 안 봅니다**(8/14 결정) — 그 폴더는 여전히 책 이름이 됩니다. ⚠️ 「Mangoi Phonics A」처럼 이름이 함께 있는 폴더는 안 걸립니다(구분자가 없음) → 그 폴더명이 권 이름이 되는 8/14 결정 유지. 🔴 **덤으로 «고칠 틈이 없던» 것이 사고를 굳혔습니다** — 분류 결과 카드(교재명 입력칸)가 뜨고 **2초 뒤 무조건 `btn.click()`** 이었고, 바로 위 `cr.scrollIntoView` 가 smooth 라 카드가 화면에 들어오는 데만 0.5초쯤 걸립니다. 즉 **이름을 읽을 시간조차 없었습니다.** ✅ 대기를 늘리고(2026-08-27 사장님 지시 **5초**) 남은 초 표시 + **분류 카드를 만지면 멈춤**(`focusin`·`input`·`change`)으로 바꿨습니다. ⛔ 자동 저장 «자체» 를 없애지 마세요(ph241 취지) — 바꾼 것은 «언제 누르는가» 뿐입니다. ⚠️ 리스너는 `wrap.__ph241Bound` 로 **한 번만** 묶으세요(이 함수는 업로드마다 다시 돌아 리스너가 쌓입니다). ⚠️ 사람이 먼저 버튼을 누르면 타이머가 뒤늦게 또 눌러 «✅ 저장 완료» 직후에 **«⚠️ 저장할 교재가 없습니다»** 가 떴습니다 — 성공 직후의 경고는 «저장이 안 됐다» 로 읽힙니다(`!pendingGroups.length \|\| btn.disabled` 로 건너뜀). ⚠️ 403(권한 없음)일 때 «이 컴퓨터에만 저장» 안내 **바로 뒤에 «✅ 저장 완료!»** 가 또 떠서 앞말을 지웠습니다 — 머리글을 사실에 맞췄습니다(2026-08-13 「강사가 자기 PC 에만 저장하고 성공으로 읽던」 제보와 같은 뿌리). ℹ️ 업로드 API 는 **강사에게 열려 있습니다**(`/api/admin/textbook-files` 는 `TEACHER_BLOCKED_PREFIXES` 에 없음 — 막혀 있는 것은 `textbook-hidden-books` 뿐) → 「강사가 못 올린다」는 권한 문제가 아닙니다. 🔴 **«만지면 멈춤» 만으로는 반쪽입니다 — `#btn-save` 는 `#cr-groups` 의 «형제»** 라, 분류 카드를 만지지 않고 곧바로 [저장] 을 누르면 `focusin`/`input`/`change` 가 **안 걸려 타이머가 계속 돕니다.** 그러면 ① 「💾 저장 중… n/m」 진행률을 매초 「🤖 N초 뒤 자동 저장」 이 덮고 ② 저장이 끝난 뒤에도 **몇 초간 버튼이 «또 저장한다» 고 말하며 enabled** 이고(2026-08-27 실측 t+3~6초) ③ 헷갈려 한 번 더 누르면 없애려던 «⚠️ 저장할 교재가 없습니다» 가 그대로 뜹니다. ✅ **저장 핸들러 첫머리에서 타이머를 직접 끄세요**(+ tick 에서도 `btn.disabled` 면 손 뗌). ⚠️ 「타이머의 클릭만 막는 가드」는 «타이머의 글자» 를 안 막습니다 — 같은 뿌리: 2장 「«완료» 메시지를 띄웠는데 곧바로 사라짐」. 🔴 **화면 안내를 고칠 때는 «한 화면이 한 가지를 말하는지» 보세요** — 드롭존 예시만 고치고 팁 상자를 놓쳐, 드롭존은 「과 폴더는 `001` 도 됩니다」 팁 상자(8/26)는 「⚠️ 숫자 폴더는 유닛마다 다른 책으로 쪼개집니다」로 **한 화면이 정반대**를 말했습니다(사람은 위에 있는 쪽을 따르고, 하필 다락원 예시 바로 아래였습니다). 2장 「같은 단정을 여러 곳에 적어 뒀으면 한 곳만 고쳐서는 안 끝납니다」의 화면판입니다. ⚠️ 그 검사를 «그 문자열이 파일에 없다» 로 쓰지 마세요 — 팁 상자가 「이렇게 쓰지 마세요」라며 그 경로를 **일부러** 적습니다(실제로 거짓 FAIL 을 냈습니다). **«사람이 따라 하는 예시»(드롭존 블록)로 범위를 좁혀** 판정하세요. 감시는 `test-harness/textbook_uploader_naming_harness.mjs`(49종 — **분류기를 HTML 에서 오려 내 실제로 돌립니다**. 검사 범위는 길이가 아니라 **중괄호 짝**으로 자릅니다)와 `test-harness/manual/textbook-uploader-browser.mjs`(브라우저 28종 — 자동으로 안 돕니다, 사람이 부릅니다. ④-2 는 **업로드 응답을 1.5초 지연**시켜야 재현됩니다) |
| 홈이 PC·모바일 전부 «탭이 돌기만» 하고 영영 안 열림 | 서버·자산·API 가 curl 전부 200 이어도 **메인 스레드가 무한루프**면 이렇게 됩니다. 2026-08-27 실사고: `vc-observe-guard.js` 의 body-class MutationObserver 콜백이 매번 `classList.remove()` 를 불렀는데, **`remove()` 는 지울 토큰이 «없어도» class 속성을 다시 써서**(toggle 과 다름 — 스펙) 옵저버가 재발화 → 마이크로태스크 무한루프. 🔴 **`add()` 도 «이미 있는 토큰» 이면 똑같이 다시 씁니다**(2026-09-01 브라우저 실측 — 아래 「`classList` 로 body class 를 만졌더니」 줄 참고. 이 줄은 오래도록 `remove()` 만 경고하고 있었습니다). **옵저버가 감시하는 속성을 콜백 안에서 쓸 때는 `contains` 확인 뒤 «바뀔 때만»** 쓰세요. 멈춘 페이지의 원인 규명은 코드 그물질 말고 **CDP 스택 실측**: 이미 멈춘 페이지엔 디버거가 안 붙으니 새 탭에서 `Debugger.enable` → `Page.navigate` → 수 초 뒤 `Debugger.pause`. 수리 배포는 **`?v=` 범프 필수**(장애 중 접속자의 SW ASSET_CACHE 가 깨진 버전을 cache-first 로 영구 보관) — 같은 이유로 **로컬 A/B 재현도 테스트마다 포트를 바꿔야**(2번째 방문부터 SW 가 낀다) 합니다. 상세: [docs/작업기록/260827_홈전체정지_무한루프수리_및_브랜치대청소.md](docs/작업기록/260827_홈전체정지_무한루프수리_및_브랜치대청소.md) |
| 녹화를 재생하니 **영상만 나오고 소리가 없다** (상대 목소리가 아예 없거나, 중간부터 전부 무음) | 🔴 **오디오 믹스가 «녹화 시작 순간» 에 있는 트랙만 한 번 연결하던 구조였습니다**(2026-08-27 수리, PR #556). 자동녹화는 수업 화면 진입 «즉시» 시작되므로 **상대가 들어오기 전이면 상대 목소리가 영영 안 담기고**(녹화 행 「참가자 1명」이 그 지문), 수업 중 끊겼다 재연결되면 새 피어의 «새 트랙» 이 생기는데 믹서는 죽은 옛 트랙만 물고 있어 **그 시점부터 전부 무음**이 됩니다. 영상은 `collectVideos()` 가 매 프레임 DOM 을 다시 읽어 저절로 복구되니 정확히 «영상만 나오는» 녹화가 됩니다. ✅ 지금은 `mango-rec.js` 가 녹화 중 3초마다 오디오 트랙을 재스캔해 «아직 연결 안 된 트랙»(트랙 id `Set`)만 이어 붙이고, `suspended` AudioContext 도 같은 자리에서 `resume` 합니다. ⛔ 이 재스캔을 body class `MutationObserver`·상주 인터벌로 바꾸지 마세요(홈 정지 전력) — 인터벌은 녹화 동안만 살고 `stopRecording` 이 끕니다. ⛔ 죽은 옛 소스를 «끊는» 코드를 더하지 마세요 — 죽은 트랙은 무음만 내보내 무해하고, 끊기 추적이 오히려 사고 지점입니다. ⚠️ 수리 «전» 에 찍힌 무음 녹화는 소리를 되살릴 수 없습니다. 상세: [docs/작업기록/260827_녹화_저장버튼_및_오디오무음_수리.md](docs/작업기록/260827_녹화_저장버튼_및_오디오무음_수리.md) |
| 「소리를 켜면 다시 끄는 버튼이 없어짐」 · 공유받은 영상을 학생이 **못 멈춤** | 🔴 **원인이 둘이고 둘 다 «에러 없이» 조용합니다.** ① `vpAddSoundOverlay()`(`js/idx-main.js`)의 「🔊 소리 켜기」 알약이 소리를 켠 뒤 **자기 자신을 지웁니다**(`btn.remove()`) — 「켜기」는 되는데 「끄기」로 가는 길이 화면에서 사라집니다. ⛔ **«한 번 누르면 할 일이 끝난다» 고 가정한 버튼**은 대개 이 함정입니다 — 상태를 바꾸는 버튼이면 되돌릴 길을 **같은 자리에** 남기세요. ② 학생(뷰어)은 2026-08-12 Melca 피드백으로 재생 조작이 잠겨 있습니다(YouTube `controls=0` + iframe `pointer-events:none` · 파일 영상 `controls=false`) → 일시정지 버튼은 «없어진» 것이 아니라 **처음부터 없었습니다.** ✅ 2026-08-27(PR #562) `js/idx-vc-mobilefix.js` **⑫절**이 `vpAddSoundOverlay` 를 감싸 **잠긴 시청자에게만** 무대 오른쪽 아래에 아이콘 버튼 둘(`.vp-viewer-ctrl` — `⏸`/`▶` · `🔇`/`🔊`)을 얹습니다. `idx-main.js` 는 blocking 849KB 라 한 줄도 못 넣어 **defer 에서 전역을 덮는** 방식입니다(그 이름이 바뀌면 조용히 헛돕니다 — 원본이 없으면 건너뜁니다). ⛔ **구간 이동(seek)·전체화면은 넣지 마세요** — 2026-08-12 결정의 취지는 «학생이 반 전체의 재생 위치를 흔드는 것» 을 막는 데 있었고, 일시정지·음소거는 자기 화면에서만 끝나 그 취지를 안 깹니다. ⛔ **가운데 큰 「소리 켜기」 알약을 없애지 마세요** — 자동재생 정책상 «첫 사람 제스처» 를 받을 자리가 필요합니다. ⛔ **아이콘 버튼에 `data-ko`/`data-en` 을 달지 마세요** — 두 i18n 엔진이 `textContent` 를 통째로 갈아끼워 40px 상자에 문장이 들어앉습니다. 설명은 `data-ko-title`/`data-en-title`·`data-ko-aria`/`data-en-aria` 로만. ⛔ YouTube IFrame API 를 새로 싣지 마세요(중국에서 막히는 외부 의존이 늘어납니다) — `postMessage`(`enablejsapi=1`)로 충분합니다. ⚠️ 학생이 멈춰도 **강사 화면은 계속 갑니다** — `video-share` 는 주소만 보내고 재생 위치를 안 맞춥니다(원래 그렇습니다). 🔴 **처음엔 강사·관리자에게 «원래 컨트롤이 있다» 며 안 붙였는데, 2026-08-27 사장님 지시로 «모두» 에게 붙입니다** — 제보 사진에서 YouTube 자체 컨트롤바(「0:49 / 4:12」)가 **하단 독(`#vc-dock`, z-index 99993)의 「나가기」 버튼에 덮여** 있었습니다. **«버튼이 있다» 와 «손이 닿는다» 는 다릅니다** — 폰에서 「못 누르겠다」는 제보는 `document.elementsFromPoint` 로 «맨 위에 무엇이 있나» 를 재세요. ⚠️ 다만 잠기지 않은 화면에는 네이티브 컨트롤바가 실제로 깔려 있으므로 우리 버튼은 **그 위로 비켜서야** 합니다(`bottom` 14px → 54px) — 같은 자리에 두면 전체화면·설정 버튼을 덮어 「전체화면이 안 눌린다」는 새 사고가 납니다. ⚠️ **문자열 하니스로는 절대 안 잡힙니다** — 함수도 값도 다 «있고» 틀린 것은 «그 버튼이 화면에 남아 있는가» 뿐이라 회귀 하니스 217건이 전부 초록이었습니다. 감시는 `test-harness/manual/vc-shared-video-ctl-browser.mjs`(브라우저 20종 — 자동으로 안 돕니다, 사람이 부릅니다. ⑫절을 되돌리면 대부분 FAIL 납니다) |
| 「녹화 버튼을 눌러도 시작이 안 돼요」 — 「Starting…」 이 곧장 「REC OFF」 로 되돌아옴 | 🔴 **고장이 아니라 «학생 미동의» 거절일 수 있습니다 — 그리고 그 거절은 에러가 안 납니다.** 스케줄 방(`class-<id>-`)은 서버가 스케줄에서 학생 계정을 채워 동의를 확인하고, 미동의(거부 또는 동의 행 없음)면 `/api/recordings/start` 가 **일부러 HTTP 200 + `{ok:false,error:'consent_required'}`** 로 거절합니다(재시도 폭주 방지 — `api-mango.ts`, «동의 없으면 녹화 금지» 는 2026-08-12 사장님 결정). 배지 탭도 `startRecording({auto:true})` 라 실패 알림이 전부 억제 → 예외도 알림도 없이 «눌러서 시작» 으로 되돌아가고 **몇 번을 눌러도 절대 성공하지 않습니다**(2026-08-27 Teacher Shas 실사고 — class-971 학생 `ubckt02` 가 동의 팝업 거부. 같은 시각 Krystel 의 class-979 도 학생 동의 행이 아예 없어 동일 증상). ✅ 판별 지문: `recordings` 에 그 방 행이 **한 건도 없음**(시작 전 거절이라 DB 무흔적) + `SELECT recording_consent, guardian_status FROM consents WHERE user_id='<학생계정>'` 이 0 이거나 빈 값. 같은 IP 의 다른 강사가 같은 시각 정상 녹화 중이면 속도제한·네트워크는 배제됩니다. ✅ 2026-08-27 부터 배지가 「⛔ 녹화불가 · 학생 미동의」(한/영)로 사유를 **본문 글자**로 말합니다(`mango-rec.js` `consentBlocked` — 툴팁은 폰에서 안 보여서 본문에 씁니다). 탭 재시도는 그대로라 학생이 뒤늦게 동의하면 성공합니다. ⛔ 게이트를 풀거나 `consents` 를 UPDATE 로 «동의 처리» 하지 마세요 — 동의는 학생 화면에서 받는 것입니다. 감시는 `rec_restart_harness` ②-b |
| 휴대폰을 **가로로 돌려도 수업 화면이 세로 그대로** | 🔴 **CSS 가 아니라 `manifest.json` 입니다 — `"orientation": "portrait"` 가 «설치된 웹앱» 의 회전을 OS 수준에서 잠그고 있었습니다**(2026-08-27 수리). `"display": "standalone"` + 세로 고정이면 **홈 화면 아이콘으로 연 사람은 폰을 돌려도 영영 세로**입니다. 화면 CSS 로는 절대 못 이깁니다. ⚠️ **그래서 «되는 사람 / 안 되는 사람» 으로 갈립니다** — 브라우저 탭으로 열면 manifest 를 안 보므로 정상 동작합니다. 2026-08-27 헤드리스 실측: 390×844 세로 = 위아래 배치, **844×390 · 915×412 · 932×430 가로 = 전부 좌우 배치**(세로→가로 회전 시뮬레이션도 정상) → **CSS·미디어쿼리는 무죄**였습니다. ⚠️ 사장님 기기가 «설치된 웹앱» 이라는 근거는 2026-08-20 작업기록에 이미 있었습니다(시작화면 남색 = manifest `background_color`, 「홈 화면에 한 번 추가해 두면 주소를 쳐도 안드로이드가 설치된 웹앱으로 넘긴다」). ⚠️ **고쳐서 배포해도 그 폰에 즉시 반영되지 않습니다** — Android WebAPK 는 manifest 변경을 하루에 한 번꼴로 확인해 새 WebAPK 를 조용히 다시 깔기 때문에 **며칠 걸립니다.** 바로 되게 하려면 **홈 화면 아이콘을 지우고 다시 추가**하는 것이 유일하게 확실한 방법입니다. ⛔ 「회전이 안 되네」를 CSS·JS 로 풀려고 `screen.orientation.lock('landscape')` 을 새로 넣지 마세요 — 「세로로 계속하기 (교재 크게 보기)」 선택지를 통째로 없앱니다(`idx-main.js` 의 기존 시도는 `requestFullscreen` 이 먼저 성공해야만 돕니다). ✅ 폰이 애초에 회전을 안 하는 경우(기기 회전 잠금)와 가르는 방법: **다른 앱(사진 등)에서 폰을 돌려 보기** — 거기선 돌아가고 수업만 안 돌면 이 함정입니다. 감시는 `test-harness/pwa_manifest_orientation_harness.mjs`(5종 — 되돌리면 실제로 FAIL 납니다. «화면은 가로를 요구하는데 manifest 는 세로로 잠근다» 는 **모순**을 잡습니다) |
| 기능이 «있는데» 아무 일도 안 일어남 — 화면·로그 어디에도 표시가 없음 | 🔴 **`try/catch` 안의 SQL 이 «없는 컬럼» 을 참조해 죽고, catch 가 그것을 삼키고 있을 수 있습니다.** 실제 뿌리: **`students_erp` 에는 `id` 컬럼이 없습니다**(`user_id TEXT PRIMARY KEY`). 그런데 `api-mango.ts` 의 `CREATE TABLE IF NOT EXISTS students_erp` 에는 `id INTEGER PRIMARY KEY AUTOINCREMENT` 가 **있어서**, 코드가 «있겠거니» 하고 `('stu_' || id)` 를 조립해 썼습니다 — 운영 표는 그 CREATE 로 만들어지지 않았기 때문에 **새 DB 에서는 돌고 운영에서만 죽습니다.** 2026-08-27 실측: 소스 **10곳**이 이 형태였고 전부 `no such column: id`. 그중 하나가 «아이디로 오늘 수업 찾기» 여서 학생이 예약을 못 찾고 공용방으로 흘렀습니다(heyst·ubckt01 수업 불성립). ⚠️ **문자열 하니스·타입체크로는 절대 안 보입니다** — SQL 은 «있고» 호출도 «됩니다». 죽는 것은 실행뿐입니다. 🔴 **더 고약한 변종 — 서브쿼리 안에서는 «죽지 않고 오동작» 합니다.** 같은 SQL 이 `class_schedules` 를 도는 쿼리 안에 서브쿼리로 들어가면 SQLite 가 그 `id` 를 **바깥 `class_schedules.id`** 로 해석해(상관 서브쿼리) 에러 없이 통과합니다. 대신 매 행마다 `students_erp` 를 전수 스캔합니다 — 운영 D1 실측 **4,305ms / 2,348만 행**, `id` 를 빼니 **37ms / 3만 행**(116배). 「관리자 화면이 느리다」의 원인이 이런 자리일 수 있습니다. ✅ 판정은 «그 SQL 을 실제로 돌려 보는 것» 뿐입니다 — 소스에서 SQL 을 오려 내 운영과 같은 스키마의 SQLite 에 prepare 하세요(본보기 `test-harness/students_erp_no_id_column_harness.mjs` 12건, 되돌리면 FAIL 납니다). ⛔ **`CREATE TABLE` 을 고쳐서 맞추려 하지 마세요** — 운영 표는 그대로이고 새 환경만 바뀝니다. 읽는 쪽에서 `id` 를 안 쓰는 것이 정본(`user_id`·`login_id`·`rowid`). ⛔ **되살릴 때 옛 `LIMIT 1` 을 그대로 두지 마세요** — 동명이인이 실재합니다(2026-08-27 실측: 이름 「김사랑」인 계정이 **6개**). «아무나» 집으면 남의 계정에 수업이 등록됩니다. **유일할 때만** 채택하고 아니면 사람에게 되묻습니다. ⛔ **전부 되살리는 것이 정답도 아닙니다** — 되살리는 순간 «데이터를 바꾸기 시작하는» 것(학생 계정 병합·시드 자리표시 생성)은 지금 죽어 있는 편이 안전합니다. 그런 곳은 **왜 안 고쳤는지 주석으로 남기고** 하니스 `ALLOW` 에 근거와 함께 등재하세요 |
| 폰 교재 화면 오른쪽에 버튼을 띄웠는데 **보이는데 안 눌림** | 🔴 **교재 「이전/다음장」 화살표(`.pdf-nav-arrow`)가 오른쪽·왼쪽 가장자리 «세로중앙» 을 차지하고 있습니다 — 그리고 폰 가로에서는 `vc-dock.js` 가 그것을 52×88 로 «키웁니다»**(2026-07-01 사장님 지시 — 엄지로 누르기 좋게). 그래서 `right:10px` 같은 가장자리 자리에 무언가를 놓으면 겹치고, 겹친 지점의 «맨 위» 는 화살표라 **내 버튼이 안 눌립니다.** 2026-08-28 실측(크기 알약 `#mgz-zoom`): 세로 390×844 겹침 **37×64** · 가로 844×390 겹침 **35×88 = 알약 높이의 77%** · 가로 932×430 겹침 37×64. ⚠️ **`display` 만 보면 «보인다» 로 나옵니다** — 반드시 `document.elementsFromPoint` 로 «맨 위가 누구인가» 를 재세요. ⚠️ **화살표는 여러 장짜리 교재일 때만 뜹니다** — 한 장짜리로 재면 겹침이 0 으로 나와 **없는 안전을 확인**하게 됩니다. ⚠️ 🔴 **더 고약한 것: 화살표를 52×88 로 키우는 CSS 는 `vc-dock.js` 가 «독을 만들 때» 주입합니다.** 독이 서기 전(입장 후 1.5초 이내)에 재면 44×64 로 나와 **가로의 진짜 겹침을 놓칩니다**(2026-08-28 실제로 밟음 — 「가로는 문제없다」고 한 번 잘못 판단했습니다). 잴 때 `#vc-dock-style` 존재를 함께 확인하세요. ✅ 고침은 **내 것이 비켜서는 쪽**(`right:80px`). ⛔ 화살표를 옮겨서 풀지 마세요 — 그 자리는 사장님이 정한 것입니다. 감시는 `test-harness/manual/vc-textbook-mobile-browser.mjs` ⑩절(세로·가로 3종 12건 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 헤드리스로 화면을 열었는데 **페이지 스크립트가 하나도 안 돌음**(에러도 없음) | 🔴 **CDP `/json/new?<주소>` 로 «새 탭» 을 만들어 열면 이 컨테이너의 크로미움은 인라인·외부 스크립트를 실행하지 않습니다.** `--headless` 든 `--headless=new` 든, `Emulation.setScriptExecutionDisabled{value:false}` 를 줘도 같습니다. 2026-08-28 실측: `<div id=x>no</div><script>…='yes'</script>` 짜리 한 줄 페이지도 `no` 그대로였고, **예외도 콘솔 오류도 0건**이었습니다. 그래서 멀쩡한 화면을 「JS 가 죽었다」로 **오진합니다**(실제로 한참 헤맸습니다). ✅ **`/json/list` 로 «이미 있는 탭» 을 잡아 `Page.navigate` 로 여세요** — 이 저장소의 `test-harness/manual/*-browser.mjs` 가 전부 그 방식이고 정상 동작합니다(그래서 하니스는 되는데 즉석 스크립트만 안 되는 «이상한» 상태가 됩니다). ✅ 급하면 `Runtime.evaluate` 로 `eval(document.scripts[0].textContent)` 를 직접 돌려 «스크립트 자체는 멀쩡한가» 를 가를 수 있습니다. ⚠️ 이건 화면 버그가 아니라 **검사 환경 문제**입니다 — 같은 장 「FAIL 이 나면 검사 쪽을 먼저 의심하라」의 형제입니다 |
| 헤드리스 검사에서 **입력칸에 포커스가 안 잡힘** — 멀쩡한 화면이 「커서가 날아간다」로 나옴 | 🔴 **두 가지가 각각 원인이 됩니다.** ① **헤드리스 창은 «포커스가 없는 창»** 이라 `el.focus()` 가 통째로 안 먹습니다(예외도 안 납니다 — `document.activeElement` 가 계속 `BODY`). ✅ CDP `Emulation.setFocusEmulationEnabled {enabled:true}` 를 켜세요. ② **관리자 카드는 IA6 가 «한 번에 한 장»(`.ia6-hide` = `display:none`)만 보여 줍니다** — 표는 DOM 에 있어 «글자를 읽는» 검사는 전부 통과하는데 상자가 숨어 있어 «보인다·눌린다·포커스» 검사만 헛돕니다(2026-09-01 실측: `offsetParent=null` · 폭 0인데 그 옆 검사 20여 건은 초록). ✅ `jumpToMenu('card-…')` 로 그 카드를 먼저 열고, **`!!el.offsetParent` 를 검사 한 줄로 박아 두세요** — 안 박으면 다음 사람이 또 «화면 버그» 로 오진합니다. ⚠️ 둘 다 화면 버그가 아니라 **검사 환경 문제**입니다 — 같은 장 「FAIL 이 나면 검사 쪽을 먼저 의심하라」의 형제입니다 |
| 「영상 없음」·「업로드 대기」처럼 **화면 숫자가 부풀어** 정작 볼 것이 파묻힘 | 🔴 **한 문구가 «성격이 다른 여러 상태» 를 뭉뚱그리면, 화면이 거짓말을 하면서도 아무도 못 알아챕니다.** 관리자 녹화 목록의 재생 칸이 **«재생할 파일이 없다 + 녹화중이 아니다» 단 하나**로 판정해, ①이미 실패가 확정된 녹화(다시 올리는 코드가 저장소에 0곳)와 ②보관 3개월이 끝나 규정대로 지운 녹화에 **똑같이 「업로드 대기」**를 붙였습니다 — 둘 다 기다려도 영원히 안 옵니다(2026-08-28 실측 1,312건). 같은 이유로 「⚠ 영상 없음」이 정상 정리분까지 경고색으로 칠해, 목록 1,862건 중 **1,236건(66%)이 정상분**인데 정작 손봐야 할 **「저장 실패 76건」이 그 안에 파묻혔습니다**(사장님 「이거 영상 없는 이유가 뭐야?」). ⚠️ **문자열 하니스·타입체크로는 절대 안 보입니다** — 함수도 값도 다 «있고» 틀린 것은 «무슨 글자가 나오는가» 뿐이라 수리 전에도 회귀 하니스 227건이 전부 초록이었습니다. ✅ 상태마다 **사실을 따로** 말하게 하고(`저장 실패`·`보관기간 만료`·`녹화 없음`·`처리 중`), 요약 숫자도 **갈라서** 세세요(한 숫자로 합치면 그 순간 다시 파묻힙니다). ✅ «정상 정리분» 은 **기본 목록에서 감추되 지우지는 마세요** — 이 저장소가 0초 부산물에 이미 쓰는 방식입니다(`?status=deleted` 로 부르면 그대로 다 보입니다). ⛔ 화면(클라이언트)에서 거르지 마세요 — 페이지네이션 `total` 이 그대로라 「1-50 / 총 1,862건」이 계속 거짓말합니다. **서버 WHERE 에서 잘라야 숫자가 사실이 됩니다.** 🔴 **덤 — 필터 `option value` 가 DB 실제 값과 다르면 «에러 없이 늘 0건»** 입니다. `#rec-status-2` 의 「종료」가 `ended` 였는데 D1 에 들어가는 값은 `completed` 이고 `ended` 행은 **0건**이라, 고르면 조용히 아무것도 안 나왔습니다. 같은 값을 「복원」도 써서, 복원하면 배지 자리에 **영문 코드가 날것으로** 뜨고 파일이 이미 지워진 녹화까지 「완료」로 되돌려 **«완료인데 영상 없음»**(8/26 에 고친 바로 그 사고)을 재생산했습니다. ✅ 되살릴 때는 **R2 `head()` 로 실물을 «봤을 때만»** 완료로 올리고, ⚠️ 조회 자체가 실패하면 막지 말고 **통과시키는 쪽으로 실패**하세요. ✅ option value 를 고칠 때는 **서버도 옛 이름을 함께 받게** 하세요(옛 북마크·캐시). 🔴 **덤 2 — 「보관기간이 지났으니 파일도 지워졌겠지」는 «추론» 입니다.** 저는 그렇게 단정해 화면에 「3개월 보관 후 규정대로 지운 녹화입니다. 정상입니다」라고 썼는데, `src/retention.ts:50` 이 **정반대**를 명시합니다 — 그 단계는 **D1 표시만 바꾸고 R2 실물은 그대로 두며**, 고아 청소기가 그 key 를 «살아있는 파일» 로 보고 **보호**합니다(주석이 「만료된 아동 화상수업 영상이 사실상 무기한 보관된다」고 적어 두었습니다). ⚠️ 반경이 큽니다 — 「지웠습니다·정상입니다」는 **안심시키는** 문구라, 미성년자 영상이 남아 있는 상태를 «지웠다» 로 읽게 합니다. ✅ 화면 문구는 **측정한 것까지만** 말하세요(「목록에서 내렸고, 여기서는 파일을 찾지 못했습니다」). ✅ **2026-09-02 사장님 승인으로 실제 파기를 켰습니다**(`retention.ts` — D1 표시 뒤 `RECORDINGS.delete`, 배치 상한 200). ⚠️ 그래도 화면 문구를 «전부 지웠다» 로 단정하지 마세요 — **켜기 전에 목록에서 내려간 1,329행은 파일이 남아 있습니다**(그날 `expires_at` 을 90일로 소급해 그 행들은 만료가 아니게 되었으므로 지워지지 않습니다). 자세한 것은 이 표의 「보관기간 정책을 늘렸는데 옛 데이터는 옛 기준으로 지워짐」 항목. 감시는 `test-harness/recording_status_label_harness.mjs`(49종 — 판정부를 오려 내 **실제로 돌리고**, 서버 WHERE 는 **진짜 SQLite 에 돌립니다**. ⑥절은 `retention.ts` 의 그 전제가 아직 유효한지까지 대조합니다. 되돌리면 FAIL 납니다) |
| 세로 수업화면에서 두 칸의 **위아래 순서를 바꾸는 규칙을 썼는데 화면이 그대로** | 🔴 **`order` 를 정하는 규칙이 «이미» 있고, 그쪽이 id 를 두 개 씁니다.** `index.html` 17455행 `#mg-vc-right-sidebar` 블록(2026-07-28 「교재 왼쪽」 개편)이 `body.vc-in-call #vc-main-row > #vc-content-pane{order:…}` 로 두 칸의 순서를 정해 두었고, **세로에서는 다시 「얼굴=1 / 교재=2」로 되돌립니다**(2026-07-14 결정을 지키려고 일부러). ⚠️ **id 수가 먼저 비교되므로 클래스를 아무리 쌓아도 집니다** — `body.vc-in-call.내클래스 #vc-main-row:not(…)×5 > .content-pane` 은 id 1개라 id 2개짜리에게 집니다(2026-08-28 실제로 밟음). ⚠️ **문자열 하니스는 전부 초록이었습니다** — 규칙도 값도 다 «있고» 틀린 것은 «누가 이기는가» 뿐입니다. ✅ 확인은 `getComputedStyle(el).order` 를 재세요(실측 얼굴=1·교재=2 로 정반대였습니다). ✅ 고침은 **우리도 id 두 개로**(`#vc-main-row … > #vc-video-pane`). ⚠️ 덤 — 그 두 칸 사이에 빈자리를 둘 때 **`margin` 은 옆 칸이 그만큼 내놓습니다**(실측 교재 칸 −15px). 이 저장소는 `*{box-sizing:border-box}` 라 **`padding` 으로 두면 바깥 크기가 안 변해** 옆 칸이 그대로입니다. ⚠️ 그리고 **폰에서 하단 독(`#vc-dock`)은 「⋯」 뒤에 접혀 `display:none`(0px)** 입니다 — 「독이 얼마나 덮나」를 재려고 그것만 재면 늘 0 이 나옵니다. 실제로 덮는 것은 **☰ 기능 버튼(`.vc-phero-ctrl`, 53px)과 `#vc-dock-more`(⋯, 42px)** 입니다(390×844 실측 2026-08-28). 감시는 `test-harness/portrait_textbook_top_harness.mjs`(62종 — 그 절을 오려 내 **실제로 돌립니다**)와 `test-harness/manual/portrait-textbook-top-browser.mjs`(브라우저 31종 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 학생인데 **교재 업로드·라이브러리 버튼이 그대로 보임** (눌러도 아무 일 없음) | 🔴 **버튼 숨김이 통째로 죽어 있었습니다 — 작성자 `!important` 가 인라인 style 을 이깁니다.** 교재를 반 전체에 갈아치우는 조작은 2026-08-12(Melca 7·8)부터 강사·관리자 전용이고, 방어가 «둘» 입니다 — 동작 게이트(`vcCanControlTextbook`)와 버튼 숨김(`vcRenderTextbookControls`). 그런데 숨김이 `b.style.display='none'`(인라인)인데 `index.html:1963` 의 `.pdf-controls > button{display:inline-flex !important}` 가 **이겨서** 📁교재 업로드·📎파일 업로드·📚라이브러리·공유 중지가 학생에게 그대로 보였습니다. 누르면 게이트가 거절하므로 「보이는데 안 눌리는 버튼」이 되고, 거절 토스트는 **4초에 한 번**이라 연타하면 두 번째부터 아무 반응이 없어 «고장» 으로 읽힙니다(2026-08-28 사장님 제보 「라이브러리랑 업로드가 안 되는 거야?」). ⚠️ **문자열 하니스가 못 잡았습니다** — 숨기는 «대상» 도 «부르는 시점» 도 다 맞았고 틀린 것은 «어떻게 숨기는가» 하나뿐이라 `vc_textbook_permission_harness` 가 그 상태에서도 전부 초록이었습니다. ✅ 판정은 코드가 아니라 브라우저 **`getComputedStyle(el).display`** 로 하세요 — 인라인엔 `display:none` 이 들어가 있는데 계산값이 `flex` 입니다(CDP `CSS.getMatchedStylesForNode` 로 이기는 규칙을 찾았습니다). ✅ 고침은 **`el.style.setProperty('display','none','important')`** + 되돌릴 때 `removeProperty('display')` (`style.display=''` 로는 안 지워집니다). 같은 파일 7555행에 선례가 있습니다. ⛔ `index.html` 의 그 `!important` 규칙을 지워서 풀지 마세요 — 교재도구 바 버튼 전체의 모양이 걸립니다. ⚠️ **판정 자체는 이 함정과 무관합니다** — 학생에게 막는 것은 «반 전체에 영향을 주는 조작» 뿐이고, 방 종류(공용방 `mangoi-class` 대 `class-…`)를 보는 게이트는 **어디에도 없습니다.** ⚠️ 「내가 학생이야?」는 `admin_account` 에 그 아이디가 있는지로 가르세요 — 학생표에만 있고 `password_hash` 가 비면 `/api/student/login` 이 **비번 없이 통과**시켜 `role='student'` 가 됩니다(2026-08-28 실측: `jeong` 은 `admin_account` 0건·`students_erp` 1건). 📌 형제 함정: 같은 표의 「관리자 카드를 `style.display='none'` 로 숨겼는데 PC 에서 그대로 보임」·「CSS 를 JS 로 덮었는데 안 먹음」. 감시는 `vc_textbook_permission_harness` ①②(되돌리면 실제로 FAIL 납니다) |
| 학생이 «메모» 로 적은 것을 그대로 글쓰기 칸에 넣을 때 | 🔴 **그 칸의 글은 그대로 영어 AI 첨삭으로 갑니다 — 한글이 한 글자만 섞여도 첨삭이 통째로 헛돕니다.** AI 영작 첨삭의 그래픽 오가나이저(2026-08-30)가 그 자리입니다: 6하원칙 답을 모아 서론·본론·결론 뼈대를 만들어 `#text` 에 넣는데, 아이들은 대개 **한글로** 적습니다. ✅ **영어로 적은 답만 문장에 합치고, 한글 답은 «메모» 로만 옆에 남기세요**(`goLineFor` — 판정은 `/[가-힣ㄱ-ㅎㅏ-ㅣ]/` 한 줄). ⚠️ 시작 문장을 **무조건** 앞에 붙이면 겹칩니다 — 「I lost 」 + 「I lost my watch」 = **「I lost I lost my watch」**(실제로 밟음). 답이 이미 그 시작 문장으로 시작하거나·첫 낱말이 겹치거나·이미 완결된 문장이면 안 붙입니다. 🔴 **덧붙임(2026-08-31) — 「같은 낱말」만 보면 반쪽입니다.** 시작 문장이 전치사로 끝나는데 답도 **«다른» 전치사로 시작**하면 그대로 겹칩니다: 「I was at 」 + 「in my house」 = **「I was at in my house.」**(사장님 화면에 그대로 찍혔습니다. `at the park`·`my house` 는 멀쩡해서 «되는 줄» 알기 쉽습니다). ✅ 둘 다 전치사면 **학생이 적은 쪽을 살리고 시작 문장의 전치사를 뗍니다**(`GO_PREP`). ⚠️ 그리고 **답을 넣는 자리(질문)를 여러 벌 만들 때는 «문단 배치» 를 질문 자신이 들고 있게** 하세요 — 목록을 딴 곳에 또 적으면(예전 `GO_PARTS.keys`) 세트를 하나 더 만드는 순간 어긋납니다. ⚠️ **문자열 하니스로는 안 보입니다** — 함수도 값도 다 «있고» 틀린 것은 «무슨 글자가 들어가는가» 뿐입니다. 뼈대 생성기를 소스에서 **오려 내 실제로 돌리세요**(`test-harness/ai_write_brainstorm_harness.mjs`, 되돌리면 FAIL). ⛔ 질문·뼈대를 LLM 으로 만들지 마세요 — 초보가 그대로 따라 쓸 문장이라 결정론이어야 합니다(같은 뿌리: 「초보에게 따라 말할 예문을 만들어 줄 때」). ⚠️ 덤으로 **AI 가 «학생이 쓰지도 않은 단어» 를 고쳐 준 것처럼 지어냅니다** — 어휘 업그레이드는 `from` 이 원문에 실제로 있을 때만 통과시키고 `to` 에 한글·숫자가 섞이면 버립니다(`api-ai.ts`). 🔴 **덧붙임(2026-08-31) — 「겹치는가」를 «부분문자열» 로 보면 «주어가 통째로 사라집니다».** 시작 문장 `I ` + 답 `in my house` 에서 `indexOf('i')===0` 이 참이라 시작 문장을 버리고 **「in my house.」**(주어 없음)가 됩니다 — `invited my friend`·`is very kind`·`ice cream` 도 같은 함정이었습니다. **낱말 경계**로 보세요(`low === st || low.indexOf(st + ' ') === 0`). ⛔ 시작 문장을 **관사로 끝내지 마세요** — `I want to have a ` + `an elephant` = 「a an elephant」. 관사를 빼고(`I want to have `) **관사는 학생이 쓰게** 두되 입력칸 예시(`ph`)로 안내합니다(관사를 코드로 고르려 하면 답마다 달라 반드시 하나는 깨집니다). ⛔ 학생이 소문자로 적은 답을 그대로 쓰면 **문장 첫 글자가 소문자로 남습니다** — 대문자 보정 한 줄. ✅ 이 셋은 눈으로 못 찾습니다 — 생성기를 오려 내 **모든 시작 문장 × 답 24종 = 13,344 조합을 돌려** 기계 검사해 찾았습니다(같은 낱말 반복·전치사 겹침·`a`+모음·대문자·마침표·한글 섞임). 수리 전 결함 5건 → 수리 후 0건 |
| 화면에 쓸 **이미지·파일을 밖에서 받아와야** 할 때 | 🔴 **이 컨테이너의 조직 프록시가 대부분의 외부 호스트를 403(정책 거부)으로 막습니다.** 2026-08-31 실측: AI 이미지 생성물 CDN 을 `curl` 도, 헤드리스 크로미움의 `fetch` 도 똑같이 못 받았습니다(둘 다 확인해야 «내 코드 문제» 로 오진하지 않습니다). 상태는 `curl -sS "$HTTPS_PROXY/__agentproxy/status"` 의 `recentRelayFailures` 로 확인합니다. ⛔ 파일 호스트를 이것저것 찔러 보는 우회는 분류기가 막습니다(그게 맞습니다). ✅ **CI 러너는 인터넷이 열려 있습니다** — 그 브랜치에서만 도는 임시 워크플로를 만들어 러너가 받아 변환·커밋하게 하고, **그 파일만** 작업 브랜치로 가져오세요(`git checkout <ci커밋> -- <경로>`). 임시 워크플로는 **main 에 올리지 말고 브랜치째 지웁니다.** ⚠️ 러너에는 `cwebp`·ImageMagick 이 apt 로 들어갑니다(로컬에는 cwebp·PIL·쓸 만한 ffmpeg 가 **없습니다** — 로컬에서 굳이 변환해야 하면 크로미움 캔버스 `toDataURL('image/webp')` 가 유일한 길이고, 원격 URL 을 직접 그리면 캔버스가 tainted 되니 같은 오리진으로 서빙한 뒤 그리세요). ⛔ 받은 주소를 코드에 그대로 쓰지 마세요 — 남의 도메인에 실서비스가 묶이고(중국 회선) 주소가 만료되면 그림이 사라집니다. ⛔ base64 로 대화에 실어 나르지 마세요(20장이면 2MB, 파일로 되돌리려면 한 번 더 흐릅니다) |
| 목록(그림·글감 등)에 **항목만 늘리고 파일을 안 넣음** | 🔴 **그 항목에 걸린 학생은 «빈 카드» 를 봅니다 — 에러도 안 납니다.** `PIC_SCENES` 는 `/img/write-scenes/<img>.webp` 를 그리는데, 파일이 없으면 `onload` 가 안 와서 사진만 안 뜨고 화면 코드는 멀쩡합니다. ✅ 감시는 «목록에 있는가» 가 아니라 **«실물 파일이 있는가»**(`existsSync`)로 하세요 — `ai_write_brainstorm_harness` [A-2]절(되돌리면 실제로 FAIL). 크기 상한(400KB)·이름 중복·Unicode 13+ 이모지도 같은 자리에서 봅니다. ⚠️ 같은 목록의 `starter` 는 **자리마다 뜻이 다릅니다** — 그림은 «완성된 한 문장»(브레인스토밍 서론 첫 줄로 그대로 들어감), 글감은 «이어 쓰는 조각 + 끝에 공백»(첫 질문의 시작 문장). 바꿔 쓰면 뼈대가 깨집니다 |
| 이미 있는 화면에 «고르는 곳» 을 하나 더 만들 때 | 🔴 **같은 성격의 목록이 셋이 되면 학생은 «무엇이 다른지» 를 모릅니다.** AI 영작 첨삭이 그랬습니다 — 오늘의 그림 12 · 오늘의 글감 15 · 브레인스토밍 주제 8 = **35개**가 한 화면에 있는데 **서로 이어지지 않아**(그림을 골라도 브레인스토밍은 그대로) 사장님이 「1번과 2번이 같은 내용이냐」고 물으셨습니다. ✅ **목록은 셋이어도 «가는 길» 은 하나로** — 어느 것을 눌러도 같은 다음 단계로 들어가게 잇고, 「지금 주제: 🖼 … (오늘의 그림)」처럼 **어디서 온 것인지** 화면이 말하게 하세요. ⛔ 새 목록을 또 만들지 마세요 — 이미 있는 `PIC_SCENES`·`TOPICS` 를 그대로 주제로 씁니다(데이터 두 벌 금지). ⛔ 그렇다고 **예전 동작을 없애지도 마세요** — 「시작 문장 한 줄만 넣기」를 쓰던 학생이 있으므로 「질문 없이 바로 쓰기」로 남깁니다. ⚠️ 성격이 다른 소재에는 **질문도 달라야** 합니다(그림=지금 보이는 장면이라 «언제» 를 안 묻고, 글감=사건이 아니라 «이야기 하나» 라 언제·어디서·누구를 안 묻습니다). ⚠️ 이 어긋남은 **문자열 하니스로 안 보입니다** — 셋 다 «있고» 각각 잘 동작합니다. 틀린 것은 «이어져 있는가» 뿐입니다(배선을 검사하세요: `ai_write_brainstorm_harness` 🔗절) |
| 전체화면 인쇄 오버레이를 만들었는데 **인쇄 버튼 글자가 가려짐** | 🔴 **홈/EN 칩(`#mangoi-global-bar` z=99999)과 사이드 드로어(`#mg-drawer` 100000)가 학생 화면 어디에나 떠 있습니다.** `z-index:99600` 짜리 오버레이를 만들면 그 둘이 위에 남아 오른쪽 위 버튼을 덮습니다(2026-08-30 `ai-write.html` 한 장 출력에서 실측 — 동화책 오버레이도 같은 값이었습니다). ⚠️ **「버튼이 있다」와 「손이 닿는다」는 다릅니다** — `display`·`classList` 로는 멀쩡해 보입니다. `document.elementsFromPoint` 로 «맨 위가 그 버튼인가» 를 재세요. ✅ 고침은 **오버레이를 올리는 쪽**(100002). ⛔ 그 칩을 숨겨서 풀지 마세요 — 닫을 때 되살려야 해서 상태가 하나 더 늡니다. ✅ 인쇄 규칙(`body.<열림클래스> > *:not(#오버레이){display:none!important}`)도 함께 두세요 |
| 배치를 CSS `order` 로 뒤집었는데 **그것을 설명하는 그림·아이콘은 그대로** | 🔴 **좌우를 바꾸면 «그 배치를 그린 아이콘» 이 그 순간부터 조용히 거짓말을 합니다.** 2026-07-28 에 화상수업을 「교재 왼쪽 / 얼굴 오른쪽」 으로 뒤집었는데(`#mg-vc-right-sidebar` 의 `order`), 얼굴 크기바의 그림 4칸(`index.html` 의 `.vsb-i`)은 **채운 칸(얼굴)을 계속 왼쪽에** 그리고 있었습니다 — **넉 달 동안 아무도 못 봤습니다**(2026-08-28 사장님 지적). 에러도 안 나고, 그 하니스의 라벨 검사도 전부 초록이었습니다. ⚠️ **비율도 함께 거짓말했습니다** — 그림은 «이름이 약속하는» 25/50/75 인데 실제 CSS 는 `clamp(180px,18%,300px)`·`27%`·`45%` 입니다(1920px 에서는 상한에 걸려 16/24/45). ✅ 감시는 «그 글자가 있는가» 가 아니라 **«CSS 와 그림이 서로 같은 말을 하는가»** 로 하세요 — `vc_sizebar_labels_harness` ④가 `index.html` 에서 clamp 퍼센트를 **읽어** `<rect>` 폭 비율과 ±1.5%p 로 대조하고, 채운 칸이 오른쪽인지까지 봅니다(되돌리면 실제로 FAIL 납니다). ⛔ 25/50/75 로 되돌리지 마세요. ✅ 얼굴 칸의 사람(머리+어깨)은 **라벨이 KO/EN 뿐이라 못 읽는 강사**를 위한 것입니다(중국인 강사) — 지우지 마세요. ⚠️ 사람은 **칸 폭에 비례해** 그리세요 — 어깨 높이를 고정했더니 좁은 칸(18% = 실제 3.4px)에서 «세로로 늘어난 알약» 이 됐습니다. **문자열 하니스는 «사람이 있다» 까지만 보고 «사람으로 보이는가» 는 못 봅니다** — 브라우저에 그려서 눈으로 잡았습니다 |
| 「화면 한가운데에 있는가」를 `innerWidth` 로 쟀더니 **정중앙인데 FAIL** | 🔴 **`position:fixed` 요소의 기준은 «초기 컨테이닝 블록» 이라 «세로 스크롤바 폭» 만큼 좁습니다.** `left:50%;transform:translate(-50%,-50%)` 로 정확히 가운데 놓은 상자를 `innerWidth/2` 와 비교하면 **스크롤바의 절반(7.5px)만큼 어긋나** 멀쩡한 화면이 실패로 나옵니다(2026-08-31 실측: 1280 창에서 상자 중심 632.5 = (1280-15)/2). ⚠️ **`document.documentElement.clientWidth` 로 바꿔도 안 됩니다** — 헤드리스에서는 그 값이 1280 으로 나와 여전히 어긋났습니다(오버레이 스크롤바). ✅ **같은 조건의 «자» 를 잠깐 놓아 재세요** — `position:fixed;left:0;right:0;top:0;bottom:0` 인 빈 div 를 붙여 `getBoundingClientRect()` 로 재고 곧바로 치웁니다. 그 값이 그 상자가 실제로 쓰는 기준입니다. 본보기: `test-harness/manual/substitute-notice-pilot-browser.mjs` ②절 |
| 화면 **우상단**에 새 안내 카드를 띄웠더니 밝기·포인트·EN·관리자 칩이 **안 눌림** | 🔴 **홈 첫 화면의 우상단은 빈자리가 아닙니다 — 칩 한 줄(`#ph50-chip-row`: 🌤 밝기·⭐ 포인트·🌐 EN·🔐 관리자)이 바로 거기에 있습니다.** `position:fixed; top:16px; right:16px` 로 띄우면 그 줄을 통째로 덮어 «보이는데 안 눌리는» 상태가 됩니다(2026-08-30 실측: PC 1280 은 밝기·포인트, 폰 390 은 EN·관리자 칩이 배너 밑에 깔렸습니다 — 옛 도메인 안내 배너). ⚠️ **`display`·`classList` 만 보면 «떴다» 로 나옵니다** — 판정은 `document.elementsFromPoint` 로 «맨 위가 누구인가» 를 재세요(내 상자 «안» 을 훑어 button·a·input 이 밑에 깔리는지). ✅ 비켜서는 쪽은 **나중에 온 카드**입니다 — 칩 줄을 옮기거나 z-index 를 낮추지 마세요. ⚠️ 그 줄은 JS(`ph50MoveChips`)가 **나중에** 만듭니다 — 지금 재고, 잠시 뒤 한 번 더 재고, `resize` 때 다시 재세요(⛔ 상주 MutationObserver·setInterval 금지 — 홈을 통째로 멎게 한 전력). ⚠️ 다시 잴 때는 **`top` 을 먼저 원위치로 되돌리고** 재세요(이미 밀린 위치를 다시 재면 계속 내려갑니다). ⛔ 「52px 고정」처럼 숫자를 박지 마세요 — 칩 줄 높이가 바뀌면 그대로 재발합니다. 감시는 `test-harness/manual/substitute-notice-pilot-browser.mjs` ②절 📌 **덧붙임(2026-08-31) — 그 배너(옛 도메인 안내)는 사장님 지시로 «화면 정중앙 · 약 3배 크게» 로 옮겼습니다.** 그래서 이 항목이 서술하는 회피 로직(`place`/`TOP_SEL`·1200ms 재측정·`resize`)은 그 파일에 더 이상 없습니다 — **일반론으로는 그대로 유효하니 우상단에 새 카드를 띄운다면 되살리세요.** ⚠️ 가운데로 옮기면 반대 함정이 생깁니다 — 폰 «가로»(세로 390px)에서는 상자가 화면을 거의 다 채워 **A.i 상담사 위젯(2147483000)·➕ FAB(2147483200) «밑» 에 깔립니다**(그 둘은 이 배너보다 위여야 하므로 z-index 로 못 이깁니다). ✅ `@media (max-height:520px)` 로 **상자를 줄여** 비켜서세요. ✅ 검사도 «내가 남을 덮는가» 만 보지 말고 **«남이 나를 덮는가»(모서리 24px 안쪽을 훑어 맨 위가 나인지)** 를 함께 보세요 — `test-harness/manual/substitute-notice-pilot-browser.mjs` ②절(PC·폰 세로·폰 가로 세 폭) |
| `class_schedules.day_of_week` 를 **단일 값**으로 읽었더니 그 수업만 「그 날짜에 안 열린다」고 거절 | 🔴 **그 칸은 숫자 하나가 아닙니다 — `4` 말고도 `Thu`·`목`·`목요일`·`1,3,5`·`Mon,Thu` 가 실제로 들어 있습니다.** 그러려고 `api-admin.ts` 에 `admDowMatches()`(+`ADM_DOW_MAP`, 한글·영문·나열 전부)가 따로 있습니다. 새 코드에서 `Number(day_of_week)` 나 «앞 세 글자» 로만 읽으면 「오늘 수업」 표에는 그 수업이 뜨는데 **내 기능만 조용히 «해당 없음»** 이 됩니다 — 2026-08-30 대체강사 배정에서 실제로 밟았습니다(표에는 🔄 버튼이 뜨는데 누르면 «이 수업은 그 날짜에 열리지 않습니다»). ⚠️ **에러가 안 납니다** — 한 화면은 «있다», 다른 화면은 «없다» 고 말할 뿐이라 「같은 배정을 두 API 가 서로 다르게 확인」(이 표의 그 항목)과 같은 뿌리입니다. ✅ 새로 읽는 곳을 만들면 **`admDowMatches` 와 같은 폭**으로 받으세요. ⛔ `enroll-ops.ts` 처럼 «다른 도메인을 import 하지 않는» 파일에서는 import 하지 말고 **같은 판정을 옆에 복제**하고(`NOT_PLACEHOLDER` 와 같은 방식), 두 함수를 **실제로 돌려 답이 같은지** 하니스가 대조하게 하세요(`substitute_assign_harness` ①절 — 되돌리면 13건 FAIL). 🔴 **덧붙임(2026-08-30) — 한 곳만 고치면 반쪽입니다.** 「그 날짜에 열리나」(`subScheduleDow`)만 넓히고 정작 **«그 강사가 그 시간에 비었나»** 를 보는 네 곳(`enrollConflicts`·`busyTimesForTeacher`·`teachersFreeAt`·긴 수업 하루 정원)을 그대로 두었더니, `'목'`·`'1,3,5'` 반복수업을 가진 강사가 후보에 **🟢(그 시간 가능)** 으로 나오고 충돌검사도 통과해 **이중배정**이 됩니다(에러 없음). ✅ 판정은 **`enrollDowList()` 한 곳**으로 모으고, 하니스 ⑦절이 «네 곳이 전부 그것을 쓰는가» 를 봅니다. ⚠️ 나열은 **여러 날**을 돌려줘야 합니다 — 한 날만 돌려주면 나머지 요일의 충돌을 조용히 놓칩니다 |
| 「이거 다 됐는지 확인해 줘」 — **커밋 해시로 판정하면 «다 안 됐다»** 가 나옴 | 🔴 **이 저장소는 squash 병합을 쓰는 경우가 있어, 병합된 커밋의 해시는 main 어디에도 없습니다.** 그래서 보고서·문서에 적힌 해시로 `git merge-base --is-ancestor <해시> origin/main` 을 돌리면 **이미 배포돼 잘 돌고 있는 것까지 전부 「main 에 없음」**으로 나옵니다 — 2026-08-30 실측: 8/28 누적 보고서의 25건을 그렇게 재니 **보고서가 «병합됨» 이라고 적어 둔 PR #341(`7017fb1`)까지** 「없음」이었습니다. 그대로 믿으면 「2주 작업이 통째로 안 나갔다」는 **틀린 보고**가 되고, 사장님은 **이미 잘 되는 기능을 다시 «적용» 하러** 가십니다. ✅ 판정은 **「그 커밋이 추가한 줄이 지금 main 에 실제로 있는가」**로 하세요 — 파일별로 추가줄을 뽑아 `git show origin/main:<파일>` 에 있는지 세면 깨끗하게 갈립니다(실측: 미반영 `496c53c` **13/238**·`1ea2d77` **0/3** 대 반영 `f23125e` **24/24**·`7017fb1` **139/144**). ⚠️ **낮은 일치율을 곧바로 「안 들어갔다」로 읽지 마세요** — 요일 체크박스(`ff2b190`)는 **1/5** 였는데 main 에는 «그 뒤 더 개선된 형태»로 들어가 있었습니다. 숫자로 후보를 좁히고 **그 부분은 눈으로** 확인합니다. 📜 같은 뿌리: 이 표의 「문서에 「고쳤다」고 적혀 있는데 같은 사고가 또 남」 — 그쪽은 «적어 둔 것 ≠ 사실», 이쪽은 «해시가 없는 것 ≠ 안 한 것» 이고 둘 다 **지금 코드를 보라**로 끝납니다. 📄 `docs/작업기록/260830_8월28일보고서_미반영3건_PR누락_마무리.md` |
| 관리자 API 를 만들었는데 **지사·대리점이 그대로 씁니다** | 🔴 **`src/index.ts` 의 스코프 차단(`forbidden_scope`)·강사 차단(`TEACHER_BLOCKED_PREFIXES`)은 «`/api/admin/` 접두사에만» 걸립니다.** `/api/pay/enroll/admin/*` 처럼 그 밖에 있는 관리자 API 는 **미들웨어를 아예 안 지나므로** 핸들러가 스스로 막지 않으면 지사·대리점·지사본사 계정이 그대로 실행합니다(2026-08-30 대체강사 배정에서 실제로 그 상태였습니다 — 전국 강사 명부를 받아 남의 학생 수업의 담당 강사를 바꿀 수 있었습니다). ⛔ **`canEditOrg()` 로 막지 마세요** — 그 함수는 `'none'`(내부직원·**교사**)에도 true 라 강사를 못 막습니다(이 표의 「본사 전용으로 막았는데 강사가 그대로 실행됨」). ✅ 두 가드를 **따로** 두세요 — 강사는 `getAdminActor().isTeacher`, 조직 계정은 **`isOrgScopedRole(actor.role)`**(`src/auth-admin.ts`). 둘 다 **DB 를 만지기 «전»** 에. ✅ 화면도 함께 감추세요 — 서버만 있으면 «눌러도 안 되는 버튼» 이 남고 화면만 있으면 URL 로 뚫립니다. ⚠️ 화면 판정의 정본은 서버가 확인해 주는 **`window.__ADM_ME.role`**(`js/adm-identity.js`)이고, **모를 때는 막지 않습니다**(본사 화면이 잠깐 비는 것보다 낫고 서버가 최종 판정). 신원이 늦게 오므로 **`mangoi:identity`** 로 다시 그리되 ⚠️ 그 이벤트는 **`document`** 에서 발행됩니다. ✅ **차단보다 «스코프로 자르기» 를 먼저 검토하세요**(2026-08-30 사장님 지시로 그렇게 바꿨습니다) — 판정은 **그 화면이 목록을 자를 때 쓰는 바로 그 함수**(`scopeStudentCond`)를 써야 합니다. 더 좁으면 «화면엔 있는데 눌러도 안 되는 버튼», 더 넓으면 남의 자료를 바꿉니다. ⛔ 조건을 못 구했으면 **막는 쪽으로 실패**하세요. 감시는 `substitute_assign_harness` ②-2·②-3 과 `manual/substitute-notice-pilot-browser.mjs`(다섯 역할로 각각 열어 봅니다) |
| 하니스가 잡아 주는데 **함수에 인자를 하나 더했더니** 관련 없어 보이는 검사가 FAIL | 🔴 **일부 하니스는 함수를 소스에서 «오려 내 실제로 실행» 하는데, 그때 시그니처를 «고정 인자 수» 로 재작성합니다.** `payroll_minutes_source_harness` 는 `calcPayrollOne` 을 떼어 `async function calcPayrollOne(env, teacherId, year, month)` 로 바꿔 돌립니다. 그래서 다섯 번째 인자를 더하면 그 인자는 **선언되지 않은 이름**이 되고, 그것을 읽는 줄이 하필 `try/catch` 안이면 **ReferenceError 가 조용히 삼켜져** 그 경로만 죽습니다 — 2026-08-30 실측: 카페24가 보낸 «분(minutes)» 을 쓰는 갈래가 통째로 안 돌아 급여가 20분 기준으로 계산됐습니다(3,000₱ 차이). ⚠️ 타입체크는 통과합니다(진짜 TS 에서는 정상 인자입니다). ⚠️ 같은 변경이 `payroll_cafe24_bridge_harness` 의 «이름으로 찾는다» 정규식도 함께 깼습니다 — 한 줄을 못 박은 검사라 변수 이름만 바꿔도 FAIL 입니다. ✅ **성능 때문에 인자를 더하지 마세요** — 이 함수들은 급여 계산이라, 잃을 것이 «중복 SELECT 29번» 보다 훨씬 큽니다. 정말 필요하면 하니스의 재작성 규칙까지 함께 고치고, 되돌려 FAIL 이 나는지 확인하세요. ✅ 새 파라미터를 꼭 넣어야 하면 `typeof x !== 'undefined'` 로 읽으세요(선언되지 않은 이름에도 안 던집니다) |
| 「보냈습니다」라고 하는데 **아무 데도 안 갔음** (케어·알림 버튼) | 🔴 **`ok` 와 «실제로 나갔는가» 는 다른 사실입니다.** 「이탈 위험」 카드의 [💬 카톡]·[📱 문자](`arrCare` → `POST /api/admin/retention/care`)는 `retention_care_log` 에 줄 하나만 남기고 `status='queued'` 로 응답했는데, **그 큐를 읽어 보내는 코드가 저장소에 없습니다.** 화면은 `if (!d.ok) throw` 만 보고 **✅ 발송됨** 을 띄웠습니다 — 즉 몇 달간 «보냈다고 말하지만 아무 데도 안 가는» 버튼이었습니다(2026-08-30 발견·수리). ✅ 서버는 solapi(`sendPlainSms`)로 **실제로 보내고**, 못 보내면 `status='failed'` 와 이유를 돌려줍니다. ✅ 화면 판정은 `ok` 가 아니라 **`status`** 로 가르세요(`sent`=✅ · `queued`/`mock`/`logged`=ℹ️ · `failed`=❌). ⚠️ **자유 문구는 알림톡으로 못 보냅니다** — 알림톡은 사전 승인 템플릿만 나갑니다. 그래서 [카톡] 버튼도 **문자(LMS)로 나가고, 화면이 그렇게 적습니다.** ⛔ «전화» 처럼 사람이 하고 나서 적는 것은 `sent` 로 쓰지 마세요(`logged`). ⛔ **카톡·문자 «일괄» 은 열지 마세요** — 이제 진짜로 나가므로 한 번에 학부모 전원에게 가고 되돌릴 수 없습니다(`arrBulkAction` 이 그 둘을 막습니다). ℹ️ 「장기 결석생」 화면의 퀵케어는 이 경로가 아니라 `sms:`·`tel:` **링크**라 «보냈다» 고 말하지 않습니다 — 그쪽은 그대로 두세요. ⚠️ 같은 함정이 여기저기 있습니다 — 「발송」 버튼을 만나면 **그 큐를 실제로 비우는 코드가 있는지** 부터 찾으세요 |
| `public/` 에 새 `.html` 화면을 만들었더니 관련 없어 보이는 하니스가 FAIL | **두 가지가 «전 화면» 을 훑습니다.** ① `hanzi_font_harness` — `font-family` 맨 앞이 `MangoiHanSC` 가 아니거나 `/css/mangoi-han.css` 링크가 없으면 FAIL(한자 글꼴 통일). ② `admin_site_structure_sync_harness` ⑦ — `public/**.html` 중 **구성표(`admin/site-structure-map.html`)에 없는 화면**이 있으면 FAIL. ✅ 새 화면을 만들면 그 구성표에 `<li class="leaf">` 한 줄을 넣고, **그 갈래의 개수(`.lane-head .n`)와 묶음 개수(`.group-t .gn`)도 함께 올리세요** — 숫자가 안 맞아도 FAIL 입니다(2026-08-30 실측: 40→41 · 7→8). ℹ️ 확장자가 있는 `.html` 은 `src/index.ts` 를 안 건드려도 서빙됩니다(확장자 없는 주소만 손으로 이어 줍니다) |
| 참관(Ghost)으로 열어 놓고 **새로고침했더니 참관자가 수업에 «실제로» 등장** | 🔴 **참관 화면도 주소에 `?room=` 을 남기고, 그 파라미터는 «실제 참가자 자동 입장» 에 쓰입니다.** `index.html` 의 「수업 중 새로고침 = 그 수업으로 되돌아오기」 블록(`vc-refresh-stay-in-class`)이 `history.replaceState` 로 `?room=<방>` 을 붙이는데, 그 블록의 `currentRoom()` 은 «수업 관찰 중» 이라는 제목까지 벗겨 내므로 **참관 중에도 붙습니다.** 한편 다른 블록은 `?room=` 을 보면 700ms 뒤 `mangoiJoinClass(room)` 을 부르고, 저장된 아이디·비밀번호가 있으면 그대로 `vcJoinRoom()` 까지 갑니다 — **참관하려던 사람이 카메라를 켜고 학생 화면에 등장합니다.** ⚠️ 에러가 안 납니다(두 블록 각자는 «맞는» 일을 합니다). ⚠️ **문자열 하니스로는 안 보입니다** — 2026-08-31 브라우저 검사에서 되돌리기 주소를 재다가 «`&room=probe-room` 이 어디서 붙었지?» 로 우연히 잡혔습니다. ✅ 고침은 **참관 중에만 `history.replaceState` 를 감싸 `room` 을 지우는 것**(`js/vc-observe-guard.js` ⑨절) — `index.html` 은 공동 금지구역이고 그 블록의 `mark()` 는 닫힌 스코프라 `window.__vcRejoinMark` 를 덮어도 내부 호출은 그대로입니다. ⛔ 상주 `setInterval` 로 지우지 마세요(홈을 멎게 한 전력). ⛔ 참관 화면에서 «다시 열기» 를 만들 때 **지금 주소를 물려받지 마세요** — 그 사이 붙은 `?room=` 이 딸려 갑니다. 필요한 파라미터만 손으로 조립하세요. 감시는 `observer_camera_guard_harness` G⑫~G⑭ 와 `test-harness/manual/observe-audio-whisper-browser.mjs` ③-4 |
| 내 코드를 고쳤을 뿐인데 **남의 버튼이 죽음**(내 검사는 전부 초록) | 🔴 **주석 시작부터 함수 끝까지를 «문자열 위치» 로 잘라 바꾸면, 바로 뒤에 붙어 있던 «남의 함수» 까지 함께 먹습니다.** 2026-08-30 실측: 화면 판정(`subAllowed`)을 고치는 편집이 같은 파일의 `tcInviteLink`(다른 PR 이 그날 넣은 「🔗 초대 링크」)를 **통째로 삭제**했습니다. ⚠️ **타입체크·내 하니스 122종·브라우저 75종이 전부 초록이었습니다** — 검사는 «내가 만든 것» 만 보기 때문입니다. ✅ 잡아 준 것은 **`page_contract_harness`**(「admin.html:42716 `tcInviteLink()` 정의 없음 — 누르면 아무 일도 안 일어납니다」) — 화면의 `onclick` 이 가리키는 함수가 실제로 있는지 세는 검사라, 이런 «남의 배선이 끊긴» 사고를 잡습니다. ✅ 고칠 때는 `git show origin/main:<파일>` 에서 그 블록을 **그대로 오려 되살리세요.** ✅ 그리고 커밋 전에 **`git diff` 의 삭제줄(`^-`)이 전부 내 것인지** 눈으로 확인하세요 — 이것이 가장 값싼 방어입니다. ⚠️ 되살린 뒤에는 **`?v=` 를 한 번 더 올려야** 합니다(원장에 «되살리기 전» 내용이 그 번호로 이미 기록됩니다). 배포된 적 없는 그 중간 번호는 원장에서 빼세요 — 남겨 두면 «나갔던 버전» 으로 읽힙니다 |
| 오래 열려 있던 PR 을 **「CI 초록불이니까」** 하고 그대로 병합하려 할 때 | 🔴 **그 체크런은 «PR 을 열던 시점의 main» 기준입니다 — 그 사이 main 이 움직였으면 아무것도 보장하지 않습니다.** 2026-09-01 실측: 8/31 01:45 에 열린 PR #609(야간배치 계측)를 22시간 뒤 병합하려는데 CI 는 `success` 였습니다. 그런데 병합 «전» 에 현재 main 을 브랜치에 병합해 다시 돌리자 **FAIL 1** — 그 사이 main 에 야간 작업이 하나 늘어(`c24-mirror`) **작업 17개에 표시 16개**가 됐고, 즉 **그 PR 이 만들려던 감시에 사각지대가 생긴 채로 나갈 뻔했습니다.** ⚠️ GitHub 의 «This branch has no conflicts» 는 **파일이 겹치는가** 만 보지 «뜻이 아직 맞는가» 는 안 봅니다 — 이번 건도 `src/index.ts` 는 **충돌 없이 자동 병합**됐습니다. ✅ **하루 이상 묵은 PR 은 병합 전에 로컬에서 `git merge origin/main` 하고 tsc·`run.mjs --fast` 를 다시 돌리세요.** 그 결과가 «지금» 의 판정입니다. ✅ 그리고 새 커밋을 push 한 뒤에는 **체크런이 새 sha 로 다시 돌았는지** 확인하세요 — 옛 sha 의 초록불이 그대로 보입니다(판정은 `head_sha` 로). ⚠️ 이때 「CI 체크런 0건」 함정을 만나기 쉽습니다 — 이번에는 **main 병합 푸시가 안 통하고 «닫았다 다시 열기» 가 통했습니다**(2026-08-26 PR #542·#545 실측과 정반대. 그 줄에 함께 적어 뒀습니다). ⛔ 「순수 추가라 안전하다」도 근거가 못 됩니다 — 이번 결함은 **추가한 줄이 모자랐던 것**이라 삭제 0줄이어도 났습니다 |
| 아바타 얼굴을 새로 바꿨는데 **화면엔 옛날 얼굴 그대로** | 🔴 **고장이 아니라 «폴백» 입니다 — 그 캐릭터의 그림이 `/img` 에 없다는 뜻입니다.** 2026-08-31 부터 웜업·AI영어친구는 고를 수 있는 친구가 **넷**(`emma`·`jake`·`lily`·`noah`)이고, 그중 **Lily·Noah** 는 «초록 배경 영상» 이 아니라 **«입모양 정지 이미지 3장(투명 WebP)»** 입니다(`/img/lily-{closed,mid,wide}.webp` · `/img/noah-*.webp` — 640×800, 장당 44~67KB(2026-08-31 실측). PNG 는 같은 그림이 600KB 라 필리핀 회선을 생각해 WebP 로 넣었습니다). 그 파일을 못 받으면(404) **성인 얼굴(`emma`·`jake`)로 조용히 되돌아갑니다** — 얼굴 자리가 «빈 카드» 로 남는 것이 제일 나쁘기 때문입니다. ⚠️ **에러가 안 납니다** — 화면은 멀쩡히 얼굴이 보이므로 「안 바뀌었네」로만 읽힙니다. 🔴 **2026-08-31 실사고 — 표만 고치고 그림 파일을 커밋에 안 담아, 며칠간 조용히 폴백이 돌았습니다**(「목소리는 바뀌었는데 얼굴이 안 바뀌었어」가 세 번 반복). ✅ 확인은 개발자도구 네트워크에서 `lily-closed.webp` 가 **404 인지** 보는 것이 제일 빠릅니다. ✅ 지금은 자동 하니스가 «표가 가리키는 그림이 저장소에 실제로 있는가»(+300KB 이하)를 검사하므로 같은 누락은 CI 가 잡습니다 — ⛔ fallback 이 있다고 그 검사를 빼지 마세요(폴백은 «런타임 사고» 대비, 그 검사는 «커밋 누락» 대비로 다른 방어입니다). ⛔ 성인 얼굴(`emma`·`jake`)을 «이제 안 쓰니» 지우지 마세요 — 폴백이자, `playClip()` 의 립싱크 클립(`teacher-say-*`)이 **Emma 얼굴과 짝**입니다. 🔴 **이름·얼굴·목소리가 세 파일에 흩어져 있습니다** — `warmup.html` 의 `VOICE_MODES` · `ai-friend.html` 의 `PEOPLE` · `mango-avatar.js` 의 `CHARACTERS`. 한쪽만 고치면 「Lily 를 골랐는데 Emma 얼굴·목소리」가 되고 **에러는 안 납니다**(감시가 세 곳을 대조합니다)(그래서 이미지 캐릭터에서는 `playClip` 이 거절합니다). ⛔ 화면 HTML 의 `<video id="tavatar-video">` 안에 `<source>` 를 **되살리지 마세요** — 캐릭터는 JS 가 항상 장착하므로, 적어 두면 JS 가 돌기 전에 브라우저가 `teacher-avatar.webm` **942KB 를 먼저 받기 시작합니다**(실측: 206 부분요청 1건). ⚠️ 왜 영상을 버렸나 — 2026-07-29 v5 사고(「남자 아바타가 한 번 움직이고 멈춘다」)의 뿌리가 **영상 seek** 이었는데 이미지는 seek 이 아예 없어 그 사고 유형이 구조적으로 사라집니다. ⚠️ 투명 그림은 **«겹쳐 그리면» 앞 입모양이 유령처럼 남습니다** — `keyFrame()` 이 매번 `clearRect` 로 지우고 그립니다. ⚠️ 카드 비율은 선언값(`aspect`)으로 시작해 **'closed' 장이 로드된 뒤 `naturalWidth/Height` 로 다시 맞춥니다** — 다른 비율 파일로 갈아 끼워도 따라옵니다. ⛔ 그 «따라오는가» 검사를 **«선언값과 다른가» 로 쓰지 마세요** — 그림을 카드 비율(4:5)에 맞춰 만들면 선언값과 실측이 같아져 **멀쩡한 코드가 FAIL 합니다**(2026-08-31 실제로 밟음). «캔버스 비율 == 그 그림의 `naturalWidth/naturalHeight`» 로 물으세요. ⚠️ 입모양 3장은 **한 장씩 따로 자르면 안 됩니다** — 입이 바뀔 때마다 얼굴이 흔들립니다. 세 장의 알파 경계 **합집합**으로 같은 좌표계를 잡아 자르세요(실측으로 Lily l=119/119/120·Noah t=74/70/69 처럼 거의 같아야 «같은 세트» 입니다). ⛔ 용량을 줄이려고 **팔레트 양자화(256색)를 쓰지 마세요** — 피부에 심한 얼룩이 생깁니다(실측 600KB→85KB 지만 못 씁니다). WebP q=86 이면 600KB→46KB 이고 원본과 구별이 안 됩니다. ⚠️ **문자열 하니스로는 «그려졌는가» 를 못 봅니다** — 감시는 `test-harness/avatar_image_frames_harness.mjs`(70종 — 캐릭터 표를 **실제로 평가해** 폴백이 실재하는지·그림 파일이 저장소에 있는지까지 봅니다)와 `test-harness/manual/avatar-image-frames-browser.mjs`(브라우저 61종 — **자동으로 안 돕니다, 사람이 부릅니다.** 폴백 절을 위해 진짜 그림을 잠깐 `.bak` 으로 치웠다 되돌립니다 — 중간에 죽어도 `git status` 에 보이고 다음 실행이 먼저 복구합니다) |
| 카페24에 있는 수업을 **망고아이 시간표에 그대로 만들려** 할 때 | 🔴 **카페24에는 «실제로 없는 수업» 이 섞여 있습니다 — 그대로 만들면 학생·강사가 있지도 않은 수업을 기다립니다.** 2026-08-31 사장님 확인: 허윤아 8/31 17:00 에 실제로는 **Zee 한 명뿐인데** 카페24에는 네 건(`c24-511741` Kes 30분·`511742` Sid 20분·`511743` Zee 30분·`511744` Zee 20분)이 있었습니다. 번호가 연속이고 30/20분이 짝을 이루는 것으로 보아 **강사 변경 잔재**로 보입니다(카페24에 「스케줄변경중」이라는 **이름의 강사**가 실제로 있습니다) — ⚠️ 이건 추론이고 측정이 아닙니다. ⚠️ **`attendance` 로는 못 가립니다** — `importCafe24Attendance` 가 `class_state` 를 **`present`/`scheduled` 두 값으로 뭉개서** 취소·변경 표식이 그 단계에서 사라집니다(창 안 362행이 전부 `scheduled`). ✅ 그래서 그림자 성적표(`GET /api/admin/reports/c24-mirror`)가 **카페24 원본 `class_state` 를 그대로** 싣습니다(`by_state`). 값이 한 가지뿐이면 상태로는 못 가른다는 뜻이고, 그때는 카페24에 «취소를 어떻게 표시하는지» 물어야 합니다. ⛔ **그 값의 «뜻» 을 추측해서 판정에 쓰지 마세요.** 🔒 **2단계(실제 생성)는 «켠 강사만» 입니다**(2026-08-31 사장님 승인, 첫 강사 **Ana** = 원부 7 · 카페24 182). 정본은 `applyMirror()`(`src/c24-mirror.ts`) 하나 — ⛔ **`dry_run` 이 기본값**이고, 손대는 행은 `source='c24-mirror'` **하나뿐**이며 `UPDATE` 의 `WHERE` 에도 `source` 를 겁니다(사람이 손대 도장 `c24-mirror:manual` 이 찍혔으면 0행이 되어 «사람 손이 이긴다» 가 경합에서도 지켜집니다). ⛔ 지우지 않고 `status='cancelled'` 로만 내립니다. 🔴 **카페24 조회가 0건이면 «취소» 단계를 통째로 건너뜁니다** — 조회 실패와 구분할 수 없어 그대로 두면 한 번의 장애로 전부 내려갑니다(건너뛴 «이유» 를 결과에 적습니다). ⛔ `only_teacher_id` 로 화이트리스트를 건너뛸 수 없습니다. 🕐 **자동 실행은 2026-08-31 에 들어갔습니다**(사장님 지시) — 정본 `runMirrorSweep()`. ⛔ **새 cron 은 못 만듭니다**(계정 한도 5/5) → 기존 트리거에 얹었습니다: **15분 감시견 = 좁은 창(오늘~+2일)** · **`0 18 * * *` = 넓은 창(오늘~+14일)**. ⚠️ 두 갈래인 이유는 **카페24가 «당일에» 예약을 채우기 때문**입니다 — 2026-09-01 08:45 첫 자동 실행 실측: 9/1 **12건** 대 9/2 1건 대 9/3 0건. 하루 한 번만 돌면 **그날 수업을 통째로 놓칩니다.** ⛔ 「시(hour)」로 가르지 마세요(15분 트리거 때문에 하루 네 번 + 정각 동시 2회). 🔴 **18:00 UTC 정각에는 두 트리거가 «동시에» 웁니다** — 두 호출이 각각 «아직 없네» 로 읽고 나란히 INSERT 하면 같은 수업이 두 벌 생기는데 **서로 다른 워커 호출이라 코드로는 못 막습니다.** 부분 유니크 인덱스 `idx_c24_mirror_class`(`WHERE source='c24-mirror'`)로 **DB 가 보증**합니다 — ⛔ `notes` 형식(`c24:<class_id>`)을 바꾸면 그 보증이 통째로 풀립니다. ✅ **끄는 스위치는 `mode='off'` 하나** — 그때는 카페24를 부르지도 않습니다. ✅ 「정말 도는가」는 성적표의 **`last_runs`**(= `c24_mirror_config` 의 `last_run:*`)로 봅니다. ⛔ `runMirrorSweep` 이 예외를 던지게 고치지 마세요 — 부르는 쪽이 **사이트 감시견**이라 함께 죽습니다. ✅ 켜지 않은 강사는 **보기 전용 겹쳐 그리기**로 봅니다 — 관리자 › 강사 스케줄 캘린더가 카페24 수업을 **파란 점선 카드**로 겹쳐 그립니다(`js/adm-q6.js` `ph54C24Card`). ⛔ 그 카드에 **`draggable`·`data-idx`·`data-block` 을 달지 마세요** — 진짜 수업 배열(`ph54State.records`)의 **인덱스**로 드래그·삭제가 도는데 섞이면 **엉뚱한 수업에 PATCH** 가 나갑니다. ⛔ 이미 망고아이에 행이 있는 판정(`already`·`update`·`conflict`·`manual_locked`·`diverged`)은 겹쳐 그리지 마세요(같은 수업이 두 번 보입니다). ⛔ 강사를 못 이은 것을 아무 칸에나 놓지 마세요(건수만 알립니다). ⚠️ 겹쳐 그리기는 «보이는 것» 일 뿐 **수업이 열리는 것이 아닙니다**(켠 강사만 진짜 행이 생깁니다) — 결정론적 방 번호(`class-{예약id}-{YYYYMMDD}`)를 만들려면 `class_schedules` 에 진짜 행이 있어야 합니다. 감시는 `test-harness/c24_mirror_harness.mjs` J절(가짜 D1 을 물려 `applyMirror` 를 **실제로 돌리고 나간 SQL 을 셉니다**)·K절(자동 실행 — «off 면 카페24를 안 부른다»·«죽어도 안 던진다» 를 돌려서 확인)·I절과 `test-harness/manual/c24-overlay-browser.mjs`(브라우저 17종 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 화면이 **일부러 가려 놓은 것**인데 «글자가 잘 안 나온다»=고장 으로 읽힘 | 🔴 **가리는 기능에는 «왜 가려졌는지 + 되돌리는 길» 을 그 자리에 함께 그려야 합니다.** AI 영어친구·웜업의 자막 3단계 중 «완전 끄기» 에는 안내(`.sub-veil` = 「🔊 듣고 맞혀 보세요 (탭하면 보여요)」)가 있었는데 **«가리기»(blur) 에만 아무 표시가 없었습니다** — 흐린 글자만 남으니 렌더링 사고와 구별이 안 됩니다(2026-08-31 사장님 제보 「글자가 잘 나오지 않아」. 브라우저 실측: `.ai-text` 가 `filter: blur(6px)` 이고 `.sub-veil{display:none}`, 탭하면 정상 해제). ⚠️ **에러도 로그도 안 납니다** — 기능은 정상 동작 중이고 틀린 것은 «화면이 아무 말도 안 한다» 하나뿐이라 회귀 하니스 231건이 전부 초록이었습니다. ✅ 안내는 **가려지는 요소 «밖»** 에 두세요 — 안에 두면 안내까지 함께 흐려져 없는 것과 같습니다. ⛔ 안내를 `display:flex` 로 감싸지 마세요(짧은 문장이 낱글자로 쪼개집니다). ⛔ 그렇다고 가리기 기능을 빼지 마세요 — 듣기 훈련의 실제 자리입니다. ✅ 감시는 `subtitle_meaning_harness` ②절이 «가리기에도 안내가 있는가»·«그 안내가 흐림 대상이 아닌가» 를 함께 봅니다(되돌리면 실제로 FAIL 납니다) |
| 「기초 레벨인데 AI 문장이 너무 길고 어렵다」 | 🔴 **눈높이를 프롬프트 «한 줄» 에 맡기면, 같은 프롬프트의 다른 규칙들이 그 한 줄을 이깁니다.** AI 영어친구가 그랬습니다 — 레벨 지시가 `A1 = very short simple sentences; C1 = natural and fluent` 한 줄뿐이라 **A2·B1·B2 는 설명이 아예 없고 어느 레벨에도 숫자 상한이 없었는데**, 길이를 늘리는 규칙 여섯 개(1~3문장+질문 · 칭찬 · 재미있는 사실 · 오늘의 단어 · 최근 틀린 단어 · 한국어 팁)는 **레벨과 무관하게 전부** 걸렸습니다. ⚠️ **에러가 안 납니다** — 답변은 멀쩡한 영어라 화면·로그로는 티가 안 나고, 수리 전에도 회귀 하니스 231건이 전부 초록이었습니다. ✅ **확인은 DB 에 쌓인 결과를 세어 보는 것뿐입니다**(2026-08-31 운영 D1 `ai_friend_chats` 실측: B1 290건 평균 31.1단어 · A2 13건 17.5단어 · **A1 10건 28.5단어(최대 45)** — 즉 **A1 이 A2 보다 길었습니다**). ⛔ 프롬프트 문구만 세게 써서 풀지 마세요 — 이 저장소의 반복 실측이 「지시만으로는 안 지켜진다」입니다(단어 수·문법·구두점 전례). ✅ 정본은 `src/ai-friend-level.ts` — ① 레벨마다 «한 문장 N단어·전체 M문장» 을 숫자로 못 박아 프롬프트에 주입하고 ② 기초(A1·A2)에서는 길이를 늘리는 규칙을 끄고 ③ 만든 뒤 **실제로 세어 보고** ④ 넘치면 한 번 더 뽑고 ⑤ 그래도 넘치면 문장 «수» 만 줄입니다. ⛔ 문장 «안» 의 낱말은 자르지 마세요(아이가 그대로 따라 읽습니다) — 되묻는 질문도 버리지 마세요(대화가 그 자리에서 끊깁니다). ⚠️ 숫자는 웜업 `WARMUP_LEVELS`(`src/index.ts`)와 **짝** 입니다(A1 = 웜업 레벨 1 = 3~5단어, 2026-08-31 사장님 지시) — 두 화면이 다른 기준을 쓰면 «화면마다 답이 다른» 사고가 시작됩니다. 🔴 **덤 — 화면이 레벨을 «기억하지 않고» 있었습니다**: `let currentLevel = 'B1'` 에 저장 코드가 없어 아이가 A1 을 눌러도 새로고침하면 B1 으로 돌아갔고, 그래서 답변 313건 중 **290건(93%)이 B1** 으로 저장돼 있었습니다(속도·목소리·자막·주제는 전부 기억하는데 레벨만 빠져 있었습니다). 게다가 **화면 기본 B1 · 서버 기본 A2** 로 서로 달랐습니다 — 지금은 둘 다 `AI_FRIEND_DEFAULT_LEVEL` 하나를 봅니다. ⚠️ `students_erp.level` 로 자동 판정할 수는 **없습니다**(29,438명 중 채워진 사람 0명 — 2026-08-31 실측). 감시는 `test-harness/ai_friend_level_harness.mjs`(75종 — 규격 모듈을 **실제로 돌리고**, 실측된 진짜 A1 답변 5건을 넣어 «넘친다» 고 잡는지·줄인 뒤 질문이 남는지까지 봅니다. 배선 검사는 길이가 아니라 **중괄호 짝**으로 `chat-friend` 핸들러만 잘라 냅니다 — 같은 파일 영작첨삭 쪽의 똑같이 생긴 멀쩡한 줄을 잡았던 전례) | 🔴 **덧붙임(2026-08-31 같은 날 실사고) — 길이를 조이면 모델은 «문법 낱말» 부터 버립니다.** 수리 뒤 A1(S1) 화면에 「Great try! **You have dog?**」·「Nice sentence! **Dog is healthy?**」·「Great try! **Dog is big?**」 가 나왔습니다 — `Do`·`Is`·`a`·`the` 가 통째로 빠진 전보문입니다. ⚠️ **길이 검사는 셋 다 `ok` 로 통과시켰습니다**(단어만 세니까요). ⚠️ 그리고 «올바른» 문장은 「Do you have a dog?」**5단어** ·「Is the dog big?」**4단어** 로 상한 «안» 입니다 — **길이가 원인이 아니라 «문법이 우선이라고 안 적은 것» 이 원인**이었습니다. 📜 이 저장소에 **이미 적혀 있던 함정**입니다(판단력 훈련의 「선택지 5단어 이하」가 `to` 를 떨어뜨린 건) — 그 줄을 읽고도 같은 실수를 했습니다. ✅ 그래서 규격이 이제 «문법이 이긴다» 를 **코드로** 들고 있습니다: ① 규칙 첫머리가 `GRAMMAR COMES FIRST` 이고 **올바른 예·틀린 예를 함께** 보여 주며 ② 판정 상한을 «목표» 가 아니라 **`hardMaxWordsPerSentence`(목표+2)** 로 두어 «문법을 지키다 한두 낱말 넘는 것」을 봐주고 ③ **`aiFriendBrokenQuestions()`** 가 전보문 의문문을 결정론으로 잡아 다시 뽑게 합니다(조동사·의문사로 시작하는가 · 조동사 뒤에 한정사·대명사·고유명사가 오는가). ④ 다시 뽑은 답은 **`score`**(깨진 문법 100 · 길이 10 · 문장 수 1)가 낮을 때만 받습니다 — 길이만 비교하면 **전보문이 이깁니다.** ⛔ 이 검사를 상급(`plain` 아님)으로 넓히지 마세요 — 「So you like dogs?」 같은 구어체를 잡습니다. ⛔ 코드가 문장을 «고쳐 쓰게» 하지 마세요 — 아이가 그대로 따라 읽을 문장을 지어내는 것이 됩니다(틀렸다고만 말하고 모델에게 다시 시킵니다). ⚠️ **관사만 빠진 것(「Do you have dog?」)은 못 잡습니다** — 품사 분석이 필요하고, 명사 목록을 손으로 박으면 대부분 놓치면서 엉뚱한 것만 잡습니다. 그쪽은 프롬프트 예시가 맡습니다. ✅ 감시는 **전수**입니다 — 올바른 의문문 2,215종 × 3칸(6,645회) **거짓 경보 0** · 깨진 전보문 90종 × 3칸(270회) **놓침 0**, 그리고 사장님 화면에 실제로 나온 3건을 그대로 넣어 «길이만으로는 안 걸렸다» 까지 함께 못 박습니다.
| **단계를 늘려 달라**는 말에 «맨 아래에 한 칸 끼워 넣어» 눈금을 밀 때 | 🔴 **눈금을 밀면 «저장된 값의 뜻» 이 말없이 바뀌고, 같은 이름을 두 화면이 서로 다른 숫자로 부르게 됩니다.** 2026-08-31 「웜업과 친구하기 레벨을 8개 단계로」 지시에 웜업 여덟 칸을 한 칸씩 밀어 맨 아래에 Pre-A1 을 끼웠다가 되돌렸습니다. ⚠️ **웜업은 이미 여덟 칸이었습니다** — 다섯 칸이던 것은 AI 영어친구뿐인데, 사장님 화면 사진이 그 화면이라 «둘 다 다섯» 으로 읽었습니다. 「늘려 달라」를 받으면 **지금 몇 칸인지부터 세어 보세요.** ⚠️ 밀면 ① `localStorage['mangoi_warmup_level']` 이 그대로인 채 학생 전원이 한 단계 쉬운 대화를 받고(에러 없음) ② 「중급(Lv 18-21)」을 판단력 훈련은 12~15단어, 웜업은 9~12단어로 부르게 되고 ③ `WARMUP_CHIP_WORD_CAP`(`src/warmup-answers.ts`)·자동 한국어 뜻(`_warmLevel <= 2`)·kickoff 힌트(`under 7 words`)처럼 **«그 숫자» 를 박아 둔 자리가 전부 한 칸씩 어긋납니다.** ✅ 정본은 `judgment-level.ts` 의 `BAND_SPECS`(이름·교재 Lv 구간)이고, `warmup_age_level_harness` C절이 대조합니다 — 실제로 그 절이 잡았습니다. ✅ **기초를 쉽게 하는 길은 «길이» 가 아니라 «열린 질문을 없애는 것» 입니다** — 실측이 그것을 뒷받침합니다(A1 은 이미 3~5단어 규격인데 실제 답변은 28.5단어였습니다. 모자랐던 것은 더 낮은 칸이 아니라 **지키게 만드는 장치**였습니다). 그래서 1단계는 Yes/No 만, 2단계는 «고를 말이 질문 안에 있는» 양자택일까지로 못 박습니다. ✅ 칸 이름을 새로 보여 달라는 요청(「CEFR 이름 함께」)은 **칸을 «더하는»** 것으로 푸세요 — `LEVEL_CATALOG.cefr`·`AI_FRIEND_CEFR` 가 그 예이고, ⛔ 기존 `ko`·`lv` 를 갈아 끼우면 안 됩니다(실제로 갈아 끼웠다가 FAIL 로 잡혔습니다). ✅ 옛 키를 이어받을 때는 **«같은 CEFR 이름을 단 칸»** 으로 보내세요(A1→S1·B1→S4) — 다른 칸으로 보내면 학생이 고른 난이도가 조용히 바뀝니다. 감시는 `ai_friend_level_harness`(120종 — 두 화면의 CEFR 표와 웜업 일곱 칸의 단어 수를 대조)와 `test-harness/manual/ai-friend-8levels-browser.mjs`(브라우저 27종 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 「이런 문장을 잡아라」 판정을 만들 때 — **구조가 같은 문장은 낱말을 모르면 절대 못 가릅니다** | 🔴 **`I'm Louie.` 와 `I'm Taiwanese.` 는 문장 구조가 «완전히 같습니다».** 그래서 «이름인가» 를 구조로 가르려는 시도는 반드시 한쪽으로 무너집니다 — 좁히면 진짜 사고를 놓치고, 넓히면 멀쩡한 답을 버립니다. 2026-09-01 에 **하루에 두 번 다 밟았습니다**: 8/31 판은 자기소개 열 가지 중 «두 가지» 만 잡았고(사장님 「계속 루이라고 말해」), 그것을 「`I'm` + 대문자면 이름」으로 넓혔더니 함정 대조가 정상 문장 98종에서 **거짓경보 71건**을 실측했습니다(`I'm Taiwanese.`·`I'm Catholic.`·`I'm Mangoi's friend!`·`I'm SLEEPY.`·`I'm Seoul born!`). ⛔ **허용목록을 키워서 풀지 마세요** — 국적·종교·형용사는 끝이 없고, 키우면 진짜 사람 이름(Grace·May·Joy·Summer)이 샙니다. ⛔ 접미사 휴리스틱(`-ese`·`-ian`·`-an` 이면 국적)도 안 됩니다 — Ryan·Nathan·Ethan·Logan·Jordan·Megan·Susan 이 전부 걸립니다. ✅ **«구조로 가를 수 있는 자리» 와 «없는 자리» 를 갈라** 앞은 그대로 잡고, 뒤는 **«뒷받침» 이 있을 때만** 잡으세요(정본 `wrongSelfName` — ⓐ 아는 이름 ⓑ 학생이 방금 그것을 물었다 ⓒ 인사말 모양 ⓓ 동격 `, your …`). 넷 다 아니면 «모르는 것» 으로 두고 넘어갑니다 — **지어내지 않는 쪽으로 실패**합니다. ⚠️ 거짓경보의 대가를 먼저 재세요 — 이 자리는 잡히면 AI 를 한 번 더 불러 «답을 갈아치웁니다». 학생은 자기가 물은 것과 상관없는 «이름 정정» 을 받습니다. ⚠️ **한국어 신호를 빠뜨리지 마세요** — 이 화면 학생은 「이름이 뭐야?」로 묻습니다. 영어만 보면 ⓑ가 거의 안 켜집니다. 🔴 **그때 제 하니스는 초록이었습니다** — `GOOD` 목록의 국적이 넷뿐이었고 **전부 제 허용목록에 이미 든 값**이라, «제가 아는 것만» 검사하고 있었습니다. 감시는 `friend_name_guard_harness`(47종) |
| 파이썬·스크립트로 소스를 **생성**했는데 정규식이 조용히 헛돌 때 | 🔴 **`\b` 가 «실제 백스페이스 문자(U+0008)» 로 들어갑니다 — 눈으로는 안 보입니다.** 파이썬에서 `"\\b"`(백슬래시 2개)는 파일에 `\b` 한 글자가 아니라 **제어문자**를 씁니다. 정규식은 `/\x08(?:hi|hello)\x08/` 가 되어 **영원히 아무것도 안 맞고 에러도 안 납니다.** 2026-09-01 실측: 인사말·절끝 판정 4곳이 통째로 헛돌아, 고쳤다고 생각한 검출이 그대로 놓쳤습니다. ⚠️ 편집기·`grep` 으로 파일을 읽으면 `\b` 처럼 보입니다. ✅ 확인은 **`cat -A`**(또는 `grep -P '\x08'`)로 바이트를 보세요 — `^H` 로 드러납니다. ✅ 정규식이 든 코드를 생성할 때는 파이썬 **raw 문자열**(`r'''…'''`)을 쓰거나, 생성 뒤 `s.count('\x08')` 을 한 줄 확인하세요. ✅ 더 나은 길은 **생성하지 말고 파일 도구로 직접 쓰는 것**입니다 📌 **덧붙임(2026-09-09) — JS «템플릿 리터럴» 안의 정규식도 같은 병입니다.** CDP 로 페이지에 넣을 코드를 `` `...` `` 안에 쓸 때 `/rgba?\(([^)]+)\)/` 를 그대로 두면 **`\(` 가 `(` 로 풀려** `/rgba?(([^)]+))/` 가 됩니다 — 괄호가 «그룹» 이 되어 `m[1]` 이 `"(110, 231, 183"` 이 되고 `parseFloat` 가 **`NaN`**, 그 값이 `returnByValue` 로 **`null`** 이 되어 돌아옵니다. **에러가 안 납니다.** 템플릿 안에서는 **`\\(`** 로 쓰세요. ✅ 판별은 결과에 `null`·`NaN` 이 섞였는지 보는 것 — 실측에서 `rgb(null,231,183)` 이 찍혀 잡았습니다 🔴 **덧붙임(2026-09-16) — 한글을 `\uXXXX` 로 «손으로» 적지 마세요. 조용히 «다른 낱말» 이 되고, 하필 그것이 사장님께 보일 문구입니다.** 학생 상세 연락처 토스트를 그렇게 넣었다가 실측: 「번호를 **지우습니다**」(← 지웠습니다) · 「«**번 칸**»」(← 빈 칸) · 「«원래 **바 있던** 칸»」(← 비어 있던) · 「**지우게다**」(← 지우겠다) · 「칸이 **바 츠로**」(← 빈 채로) · 「PII **열램**」·「**구먹**이지만」·「보지도 **모한**」. ⚠️ **어디에서도 안 잡힙니다** — `node --check` 통과 · 하니스 84/0 통과 · 한글이라 오타로도 안 보입니다(음절이 «있는 글자» 라서). 저는 `git diff` 를 눈으로 읽다 겨우 찾았습니다. ✅ 한글은 **파일 편집 도구로 «글자 그대로»** 쓰세요(escape 산수를 아예 하지 않는 것이 유일한 방어). 스크립트로 넣어야 하면 파이썬 **`u'…'` 리터럴에 한글을 직접** 적으세요. ✅ 그리고 **커밋 전에 `git diff | grep '^+'` 로 새로 들어간 한글을 한 번 읽으세요** — 사람에게 보이는 문구(i18n 사전·토스트·버튼)는 특히 |
| 영어 화면인데 **한국어가 섞여 나옴** — 화면을 뒤져도 원인이 없을 때 | 🔴 **화면이 붙인 것이 아니라 «모델이 스스로 덧붙인» 것일 수 있습니다.** 2026-09-01 사장님 제보 — AI 영어친구가 `Wow! What's your favorite kind of candy? «너의 가장 좋아하는 캔디는?»` 를 내보냈습니다. 실측: `ai-friend.html:924` 는 서버가 준 글자를 `escapeHtml` 해서 **그대로** 그리고, `chat-friend` 프롬프트에서 한국어가 허용된 자리는 **문법 팁 하나뿐**이며 «번역해 주라» 는 지시는 **0건**입니다. ⚠️ **두 가지가 함께 망가집니다** — ① 「뜻」 버튼·자막 3단계의 «영어를 먼저 읽어 보게 한다» 는 설계가 무너집니다(한국어가 옆에 있으면 아무도 영어를 안 읽습니다) ② 그 화면 `speakText` 는 괄호를 안 걸러 내서 **영어 TTS 가 한글을 그대로 읽습니다.** ✅ 정본 `src/reply-korean.ts` — 프롬프트에 금지를 적고, 그것만 믿지 않고 **만든 뒤 확인해서 뗍니다**(반복 실측 「지시만으로는 안 지켜진다」). 선례가 바로 옆 줄입니다 — 2026-07-27 `aiFriendStripHanzi`. ⛔ 문장을 **고쳐 쓰지 마세요** — 떼기만 하면 원래 영어가 그대로 남습니다. ⛔ 문법 팁까지 떼지 마세요(2026-08-07 에 일부러 넣은 기능). ⚠️ 떼고 나서 영어가 하나도 안 남으면 **원문 그대로** — 빈 말풍선이 더 나쁩니다. 🔴 **⛔ 이 제거를 웜업에 적용하지 마세요** — `warmupSystem` 의 `[언어]` 규칙이 「한국어가 꼭 필요하면 **괄호 안에만** 짧게」를 **일부러** 허용하고 그 화면 TTS 는 괄호를 안 읽습니다. 같은 화면처럼 보여도 설계가 다릅니다. `reply_korean_gloss_harness` ⑥절이 그 «안 한 일» 과 «그 허용 규칙이 아직 살아 있는가» 를 함께 못 박습니다 |
| 정상 동작하는 버튼인데 「**하나하나 눌러봐야** 알 수 있다」는 제보 | 🔴 **화면이 «지금 상태» 를 말해 주지 않으면, 멀쩡한 기능이 «못 쓰는 기능» 이 됩니다.** 강사 명부 👁 수업관찰(`tpGhostObserve`)이 그랬습니다 — **누른 뒤에야** 그 강사의 수업을 찾는 구조라, 수업 중인 강사를 고르려면 「눌러본다 → 「지금 진행 중인 수업이 없습니다」 → 확인 → 다음 강사」를 **강사 수(30명 안팎)만큼** 반복해야 했습니다(2026-08-31 사장님 제보). 그 경고창은 **고장이 아니라 정상 동작**이라 코드를 아무리 봐도 이상한 곳이 없습니다. ✅ 지금은 명부 **상태 칸 뒤 「지금」 칸**이 미리 말해 줍니다(`js/adm-core.js` 의 `_TP_LIVE` 절 — 표를 그린 뒤 **표당 한 번만** 조회. ⛔ 강사 수만큼 부르지 마세요). 🔴 **세 상태를 한 색으로 합치지 마세요** — ① `classes/today` 의 **`join_open` 은 «들어갈 수 있는 시간대»**(시작 10분 전~종료 15분 후, 레벨테스트 30분)이지 **«실제로 접속해 있다» 가 아닙니다** → `/api/active-rooms` 를 겹쳐 봐야 🟢(방이 실제로 열림)이고, 안 겹치면 🟡(시간대만) 입니다 ② **`c24-*`(카페24) 수업은 망고아이 방이 없어 참관이 «구조적으로» 불가능**합니다 → ⚪ 로 갈라 «왜 안 되는지» 를 툴팁으로 적습니다. 합치면 이 표의 「배지는 있는데 눌러도 안 되는 버튼」이 그대로 재현됩니다. ⛔ 「수업 중만 보기」에서 ⚪ 를 **거르지 마세요** — 명부에서 사라지면 그 강사가 «한가하다» 로 읽힙니다. ⛔ 강사↔수업은 **이름 완전일치만**(번호가 세 벌이라 남의 수업이 «수업 중» 으로 뜹니다 — 같은 표의 「남의 이름이 뜸」). ⚠️ 45초 갱신은 **표를 다시 그리지 말고 칸만 칠하세요**(행을 갈아치우면 조준한 버튼이 마우스 아래에서 움직입니다 — 2026-08-12 방 목록 사고). ⚠️ 403(강사·지사)이면 **«모른다» 고 적고 다시 묻지 마세요** — 조용히 «수업 없음» 으로 그리면 거짓말이 됩니다. ℹ️ 「전체를 한 표로」는 **따로 있습니다**(🗼 `/admin/monitor-wall.html` 수업 관제탑 — 순회 참관까지). 쓰는 사람이 달라 **둘 다 남깁니다.** ⛔ 한쪽을 «중복» 이라고 지우지 마세요. ⚠️ **문자열 하니스가 이걸 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무슨 글자가·무슨 색으로·눌리는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 감시는 `test-harness/manual/teacher-live-now-browser.mjs`(브라우저 23종 — **자동으로 안 돕니다, 사람이 부릅니다**) |
| 「왜 3명이 나와?」를 고쳤는데 **연결이 계속 쌓임** — 화면엔 안 보이는데 회선만 느려짐 | 🔴 **원격 트랙의 `readyState` 는 상대가 사라져도 계속 `'live'` 입니다**(`ended` 는 트랙을 실제로 끝낼 때만). 그래서 «살아 있나» 를 `track.readyState === 'live'` 로 판정하면 **«한 번 붙었다가 신호가 끊긴» 상대를 영원히 «정상» 으로 봅니다.** 2026-09-01 실측: `class-1070-20260901` 은 출석이 **학생1·강사1 두 명뿐**인데 학생 브라우저의 `vcPeerConnections` 가 **26분에 걸쳐 1→2→…→9 로 단조 증가**했습니다(8개가 유령). 그날 저녁 **7개 방 중 5개**가 같은 모양이었습니다. ⚠️ **화면에는 안 보입니다** — 타일은 `vcRemovePeer` 가 지웠고 남은 것은 «연결» 뿐입니다. 증상은 **학생 업링크 포화**로만 나타나서 (같은 영상을 최대 9벌로 업로드) 「그 학생 인터넷이 나쁘다」로 오진합니다 — 실제로 그렇게 보고했다가 정정했습니다. ⚠️ 청소 경로 셋이 전부 못 잡습니다: ① `user-left` 가 안 오면 `vcRemovePeer` 는 아예 안 불립니다 ② `vcSweepGhostTiles()` 는 `data-vc-ghost="1"` 표시가 붙은 타일만 치우는데 그 표시는 `user-left` 가 붙입니다 ③ `idx-vc-dupghost.js` 의 `hasLiveTrack` 이 위 이유로 늘 true 입니다. ✅ **믿을 수 있는 신호는 «패킷이 실제로 오는가» 하나뿐입니다** — `receiver.getStats()` 의 `packetsReceived` 델타. 정본은 `js/idx-vc-qlog.js` 의 `vcPeerNoMedia(id)`(오디오·영상 **둘 다** 조용한 시간(초))이고, 청소기가 60초 이상일 때 «죽은 연결» 로 봅니다. ⛔ **한쪽만 조용한 것으로 판정하지 마세요** — 카메라만 끈 사람(오디오는 흐름)·마이크만 끈 사람(영상은 흐름)을 죽은 것으로 만듭니다. «둘 중 작은 값» 이어야 합니다(변이시험에서 «마지막에 본 값» 을 쓰는 실수가 실제로 안 잡혀 검사를 보강했습니다). ⛔ 임계값을 짧게(30초 미만) 잡지 마세요 — 잠깐 끊긴 사람을 지웁니다. ⛔ 그 값만으로 지우지도 마세요 — 기존 **비대칭 확인**(내 이름이거나, 같은 이름의 다른 타일이 실제로 수신 중)을 그대로 통과해야 합니다. 혼자 있는 상대를 지우면 수업이 끊깁니다. ⚠️ `vcPeerNoMedia` 가 아직 없으면(defer 로드 전·수업 밖) 0 을 돌려 «옛 동작 그대로» 여야 합니다. 감시는 `test-harness/vc_quality_blindspot_harness.mjs` ⑩절(가짜 수신 통계로 **실제로 돌립니다** — 되돌리면 FAIL 납니다) |
| 「끊긴다·흐리다」를 몇 달째 못 고침 — **재는 쪽에 그 사람이 통째로 빠져 있음** | 🔴 **«누구의» 지표를 모으는 기능은 «아이디를 어느 키에서 가져오는가» 부터 확인하세요.** `vc_quality` 는 파일 머리말에 「강사 회선이 대체로 어떤가를 보는 용도」라고 적혀 있는데, 2026-09-01 실측 **742건이 전부 학생(8명)이고 강사는 0건**이었습니다 — 관리자 메뉴 「📶 강사 회선품질」 화면이 **개설 이래 강사 데이터 0건**이었습니다. 원인은 로그가 `getCurrentUser()`(=`mangoi_logged_user`, **학생 전용 키**)로만 uid 를 만들고, **강사는 쿠키 세션**이라 늘 null 이며(2장 「로그인했는데 또 로그인하래요」와 같은 뿌리), 서버가 `if (!b.uid) return json({ok:true})` 로 **조용히 버린** 것입니다. ⚠️ **에러도 경고도 없고 화면은 «아직 데이터가 없습니다»** 라 「수업이 적어서 그렇겠지」로 읽힙니다. ✅ uid 는 **학생 키 → `mangoi_admin_session` → 화면 이름표** 순으로 떨어지게 하세요(⛔ 강사에게 `mangoi_logged_user` 를 만들어 주는 방식은 금지 — 학생 전용 기능이 통째로 열립니다. 로그에 적을 «이름» 만 가져옵니다). 🔴 **같은 사고의 절반이 하나 더 있습니다 — «받는 쪽» 을 아무도 안 쟀습니다.** 적응 루프도 로그도 `sender.getStats()` 만 보는데, 「화면이 흐리다·소리가 끊긴다」는 전부 **«내가 받는 것»** 이야기입니다. ✅ `receiver.getStats()` 의 `inbound-rtp` 로 **freezeCount(영상 멈춘 횟수)·concealedSamples(끊겨서 메워진 소리 비율)** 를 함께 남기세요 — 후자가 「소리 끊김」의 직접 지표입니다. ⛔ 표본이 없을 때 **0 으로 적지 마세요**(`-1`=모름). 0 으로 적으면 «영상이 죽은 사람» 이 «회선이 제일 좋은 사람» 이 됩니다. ⚠️ **평균만 남기지 마세요** — 실측 1분 평균 1.9% 안에 20~27% 스파이크가 들어 있었고, 그래서 어느 화면에서도 «양호» 였습니다(p95 를 함께). ⚠️ 수신 계측 타이머는 **수업 중에만** 살게 하세요(첫 호출에서 켜고 `vc-in-call` 이 없으면 스스로 끔) — ⛔ 상주 `setInterval` 도 body class `MutationObserver` 도 금지(홈 전체를 멎게 한 전력). 감시는 `test-harness/vc_quality_blindspot_harness.mjs`(38종 — 가짜 브라우저에서 `idx-vc-qlog.js` 를 **통째로 돌려** 강사 uid 가 실리는지 확인합니다). 📌 **덧붙임(2026-09-03) — «어떤 길로 갔는가» 도 0칸이었습니다.** Farrah↔Karl 이 RTT 1,100~1,700ms 였다가 2분 뒤 60~85ms 로 떨어졌는데 **경로가 바뀐 것인지 회선이 풀린 것인지** 가릴 칸이 없어 추측으로 끝났습니다(`X-Turn-Source` 는 «발급» 이지 «실제 사용» 이 아닙니다). ✅ 이제 `vcqPathProbe` 가 선택된 candidate-pair 로 **`path`(relay/direct/mixed)·`relay_ticks`·`path_ticks`·`turn`(TURN 서버 host:port proto)** 을 남기고 관리자 「📶 강사 회선품질」에 「🛰경로」 칸이 있습니다. ⛔ **«모름» 을 `direct` 로 적지 마세요** — getStats 없음·연결 전은 `path_ticks 0` 이고 화면은 «—» 입니다(0/0 을 «직접» 으로 읽으면 이 칸을 만든 이유가 사라집니다). 상대만 relay 면 서버 주소는 «모름»(그건 상대의 TURN) — 지어내지 않습니다. 감시는 같은 하니스 ⑬절(변이 3종 실제 FAIL). 📌 **같은 날 후속(2층 2·3·4)** — 기준 RTT 300ms↑ 회선은 첫 연결부터 한 단계 아래서 시작(`vcqStartStep`, `window.vcCreatePeer` 를 밖에서 감싸 `pc.__qStep` 을 심음 — idx-main.js 0줄) · 같은 계정 둘째 탭 토스트(⛔ 끊지 않음, 가족 공용 계정) · 회선 경고·음성전용 안내에 지연·손실·이유 한 줄. ⛔ SFU 는 **1:1 수업에 안 닿습니다** — 9/2 C 2단계 설계는 «참관자가 있을 때만» 이라 Farrah 류 사고는 D안(전면 이전)이어야 하고 시크릿·실측이 먼저입니다(사람이 정할 일). 감시는 같은 하니스 ⑭절 |
| 화질·지연 문턱을 **더 세게** 조였는데 하니스가 빨간불 | 🔴 **검사가 «옛 숫자·옛 식» 을 글자 그대로 못 박아 두면, 보장을 강화한 수리에 FAIL 이 납니다.** 2026-09-01 실측: `vc_stability_checklist` 가 「상향 조건: **3틱** 연속」을 `/__qGood.*>=\s*3/` 로, `vc_latency_tuning` 이 `tuneReceiveLatency(pc, step === 0 && lossPct < …)` 를 **식 전체**로 못 박고 있어서, 진동을 3틱→8틱으로 늘리고 지연 조절에 «지속 확인» 을 더한 수리가 두 곳에서 빨간불이 났습니다(보장은 오히려 세졌는데). ✅ 물어야 할 것은 «몇 틱인가» 가 아니라 **«진동 방지 장치가 있는가»** 입니다 — 숫자를 **읽어서 하한과 비교**하고(`Number(m[1]) >= 3`), 식은 **필요한 조각이 들어 있는가**(`step === 0` · `lossPct` · `rtt` · `__qGood`)로 보세요. ✅ 그리고 문턱을 바꾸는 검사에는 **«되돌려 보면 실제로 나빠지는가»** 를 짝으로 넣으세요 — 실측 손실 패턴을 넣어 돌리니 옛 문턱은 진동 **38회**, 지금은 **4회** 였습니다. 그 대조가 없으면 검사가 헛돌아도 알 수 없습니다. 📜 같은 줄기: 이 표의 「하니스가 «객체 모양» 을 정규식으로 못 박아 두어」·「검사 범위를 «길이» 로 자르지 마세요」 |
| 화질이 **한번 흐려지면 수업 끝까지 안 돌아옴** — 손실은 낮은데 | 🔴 **회복 문턱이 «절대값» 이면 기준 RTT 가 높은 회선에서는 영영 안 맞습니다.** 적응 루프(`js/idx-main.js` `vcAdaptiveQuality`)가 «RTT 450 초과면 내리고, 250 미만이어야 올린다» 였는데, 중국 강사 회선은 RTT 가 **늘 360ms 안팎**입니다(2026-09-02 `class-849` D1 `vc_quality` 실측: 19분 내내 360~435ms, 손실 1% 미만). **[추론 — 단계 값은 로그에 없음]** 스파이크 한 번에 내려간 화질이 «250 미만» 을 한 번도 못 만나 바닥(해상도 1/4·5fps)에 굳었고, RTT 는 양쪽이 같은 값이라 교사·학생이 동시에 떨어졌다고 봅니다(잰 것: 19:30~19:35 양쪽 영상 표본 소멸 — 사장님 「흐려지거나 검은색」). ⚠️ **화면 크기는 원인이 아닙니다** — 타일 크기를 인코더에 넘기는 코드가 없습니다. 큰 타일은 320px 짜리 영상을 4배로 늘려 «흐림을 드러낼» 뿐입니다. ⛔ 그래서 「연결 나쁘면 자동으로 작게」는 대역폭을 한 바이트도 안 줄입니다(P2P 라 받는 쪽 크기가 보내는 쪽에 안 갑니다) — 흐림을 감추고 수업 중 화면만 움직입니다. ✅ 2026-09-02 수리: 문턱을 **그 연결의 기준 RTT(최소값, 위로는 틱당 2%) 대비**로 잽니다 — 내림 `max(450, 기준+200)` · 올림 `max(250, 기준+100)`, 기준 상한 500. 기준 150 미만 회선은 옛 숫자 그대로입니다. ⛔ 손실 문턱(6%·1.5%)은 안 건드렸습니다 — «나쁜» 신호는 손실이고 RTT 는 «막힌» 신호라 증가분이 맞습니다. ⚠️ 이런 것은 **문자열 하니스로 못 잡습니다** — 숫자도 조건도 «있고» 틀린 것은 «그 회선에서 만족될 수 있는가» 뿐입니다. 판정 블록을 오려 내 **가짜 시계로 실제 회선 패턴을 돌리세요**(`vc_quality_blindspot_harness` ④-2 — 옛 절대값으로 되돌리면 «영영 바닥» 이 실제로 재현됩니다). ⚠️ 단계 값은 `vc_quality` 에 안 남습니다 — 확정은 브라우저 콘솔 `[vc-adapt]` 줄로. 📌 **덧붙임(같은 날, 함정 대조 검사 지적)** — 손실 없이 RTT 만 애매(rttUp~rttDown)한 틱이 «8틱 연속 양호» 를 **지우면** 28초마다 흔들리는 회선은 여전히 영영 못 올라옵니다(옛 코드도 그랬습니다). 그런 틱은 진행을 **멈추기만** 하고, 손실이 있는 틱만 지웁니다. 그리고 「감시: ④-2」라고 적어 놓고 상한·드리프트는 못 잡고 있었습니다 — «감시한다» 는 주석은 그 변이를 실제로 돌려 본 뒤에만 적으세요. 🔕 **그 「흐린 이유」 배지는 2026-09-15 사장님 지시로 껐습니다** — 「"저화질로 받는 중" 글자 안나오게 해줘」. 얼굴 타일 «위에» 얹히는 글자라 **회선이 나쁠수록 오래 떠, 정작 얼굴이 제일 안 보일 때 가장 크게 가립니다**(사장님 화면에 «받는 중»·«보내는 중» 이 동시에). ⛔ **판정·로그는 한 줄도 안 바꿨습니다** — «왜 흐린지» 는 관리자 「📶 강사 회선품질」에서 그대로 읽습니다. ✅ 되살리기는 콘솔에서 `window.__vcLowQBadge = true` 한 줄(게이트는 `vcLowQBadgeOn()`). ⛔ 그 게이트를 지워서 되살리지 마세요. 감시는 `vc_quality_blindspot_harness` ⑪이 «기본은 안 그린다 ↔ 스위치를 켜면 옛 판정이 그대로»를 **짝으로** 못 박습니다(한쪽만 두면 «전부 끄기»·«전부 켜기» 가 통과). 📜 아래는 «그때 무슨 판정이었나» 로 남깁니다(스위치를 켜면 그대로 돕니다) — 보내는 쪽 단계 3 이상 · 받는 쪽 `frameWidth` 가 «그 상대에게서 본 최대 폭» 의 1/2.5 이하면 그 타일에(⛔ 절대값 430 으로 두면 폰(640 송신)은 1단계부터 걸립니다 — 실제로 그렇게 짰다가 잡혔습니다) 「저화질로 보내는/받는 중」. 받는 쪽 배지는 **모든 화면**에 뜹니다 — 문구가 상대를 탓하지 않아서이고, 탓하는 말(「이 학생 인터넷이 불안정」)은 여전히 강사에게만입니다. ⛔ 그 배지로 타일 크기를 바꾸지 마세요. 📌 **덧붙임(2026-09-08) — 같은 사각지대가 «손실 축» 에 그대로 남아 있었습니다.** 위에 「⛔ 손실 문턱은 안 건드렸습니다」라고 적어 둔 바로 그 자리입니다 — 내려감은 `> 6%`, 회복은 `< 1.5%` 라 **그 «사이»(1.5~6%)에 꾸준히 머무는 연결은 어느 쪽에도 안 걸립니다.** 세 번째 분기가 `__qGood` 을 매 틱 지워서 8틱을 영영 못 모았고, 스파이크 한 번에 바닥으로 내려간 뒤 **RTT 가 100ms 로 아주 좋아도** 수업 끝까지 그대로였습니다. [잰 것 — 2026-09-08, 판정 블록을 실제로 돌림] RTT 100ms 고정 · 3틱 스파이크 뒤 손실만 바꿔 3분: **1.4% → 단계 0(완전복구) · 1.5%부터 고착**(2·3·4·5·6% 전부 같음). 1.5% 에서 칼같이 갈립니다. ✅ 고침은 **카운터를 둘로 나누는 것** — `__qMid`(«나쁘지 않은» 틱)로 `LOSS_MID_STEP`(=2, ×0.35·해상도 1/2)까지만 «부분 회복». ⛔ **단계 0 까지 열지 마세요** — 손실 4% 짜리 회선을 «정상» 으로 보는 것이라 그 손실을 우리가 만듭니다. ⛔ **`__qGood`(완전 회복) 하나로 겸하지 마세요** — 그러면 28초마다 2% 손실이 오는 회선도 단계 0 까지 올라가 2026-09-01 진동 방어가 깨집니다(변이시험에서 실제로 재현됨). ⚠️ 스파이크형(class-1015 의 20~27%)은 이 분기에 **안 옵니다** — 6% 를 넘어 첫 분기에서 `__qBadAt` 이 찍힙니다. ⚠️ 그때 **하니스 두 곳이 함께 깨집니다** — ④ 의 되돌림 시험이 «`pc.__qGood >= 8 && Date.now() - …`» 라는 **식 모양을 글자 그대로** 못 박아 두어, 홀드 조건을 `held` 변수로 빼는 무해한 정리에 «보장은 세졌는데 검사만» 빨간불이 났습니다. **뜻으로 묻도록** 고치세요. 감시는 `vc_quality_blindspot_harness` ④-3(30종). 🔴 **그 수리를 하면서 «검사가 아무것도 안 재고 있던» 것을 두 개 찾았습니다 — 둘 다 초록불이었습니다.** ㉠ **④ 「진동이 사라졌나」가 `Date` 를 안 넘겨 «진짜 벽시계» 로 돌고 있었습니다.** 300틱이 1ms 안에 다 도니 첫 스파이크에서 `__qBadAt` 이 찍힌 뒤 홀드(30초)가 끝까지 참이 안 되어 **회복 경로가 통째로 얼었고**, 그래서 진동 횟수가 옛 코드든 새 코드든 **언제나 4회**로 나왔습니다 — «같다» 가 아니라 «안 잰다» 입니다(실측: 벽시계 옛 4·새 4 / 가짜시계 옛 16·새 16·옛문턱 변이 **38**). 그 위에서 보정된 문턱 `flips <= 12` 는 근거가 없었습니다. ⛔ `Date` 인자를 다시 빼지 마세요. ㉡ **④-3 이 «끝 단계» 만 보고 fixture 가 전부 `__qMid = 0` 에서 시작해**, 새로 넣은 가드 셋(문턱·둘다애매 리셋·회복 후 리셋)을 무력화해도 **PASS 214 / FAIL 0** 이었습니다 — 「손실·RTT 가 둘 다 애매하면 부분 회복도 안 한다」는 **이름의 검사가 있는데도** 리셋 코드가 한 번도 «실행되지 않아» 통과했습니다. ✅ 가드를 재려면 **그 가드가 «실제로 일하는» 상태를 만들어 주세요** — 지울 값이 있고(`__qMid = MID_TICKS`), 홀드가 이미 지났고(`__qBadAt` 을 아주 과거로), 시작 단계가 바닥인 상태. 그렇게 넷을 넣으니 세 변이가 **소스를 실제로 고쳐 돌렸을 때 FAIL 6 · 2 · 2** 로 잡힙니다. ⚖️ **맞바꿈을 숫자로 재고 고르세요** — 부분 회복 문턱을 8틱(32초)으로 두면 class-1015 계열에서 진동이 **16 → 27회(+69%)** 로 늘어 2026-09-01 결정을 되돌립니다. **15틱(60초)이면 그 계열이 옛 코드와 «정확히 같은» 16회**이면서 꾸준한 손실 회선은 그대로 단계 2 까지 회복합니다(스파이크 주기 28·40·60초에서도 옛 코드와 동일). ⛔ 그 값을 8 로 되돌리지 마세요 |
| 「이 강사는 시간표가 있으니 카페24에서 잘 넘어온 것」 | 🔴 **아닙니다 — `class_schedules` 에 행이 있다고 «이관» 이 아닙니다.** 2026-08-31 에 제가 그렇게 잘못 보고했다가 사장님이 정정해 주셨습니다. 그 7명의 수업 52건은 전부 `source='adm-enroll:78~85'` 이고 `created_at` 이 **8/27~9/1** 이었습니다 — 카페24에서 온 것이 아니라 **파일럿 테스트로 손으로 넣은 수업**입니다. 실제로 카페24에서 이관된 수업은 **0건**이고 대상은 23명이었습니다(재직 29명 중 7명만 «있는» 것처럼 보였습니다). ✅ 이 표를 볼 때는 **`source` 와 `created_at` 을 함께** 보세요 — `lms_import_w26`·`type_seed_*` 는 옛 자리표시(전부 cancelled), `adm-enroll:<신청id>` 는 수강신청 확정이 만든 것, `enroll:<주문번호>` 는 결제가 만든 것입니다. ⚠️ 「몇 건 있나」만 세면 **셋이 구분되지 않습니다.** 📄 미러 설계: `docs/작업기록/260831_카페24_시간표미러_1단계_그림자.md` |
| 강사의 **로그인 아이디**를 찾을 때 | 🔴 **정본은 `teacher_profiles.notes` 의 「아이디:…」 입니다** — 별도 칸이 아니라 메모 첫머리에 `아이디:mangoi_149 / 프랜차이즈:… / 평점:…` 형태로 들어 있습니다(2026-08-31 실측: 활동중 프로필 대부분에 있음). 계정↔원부 연결은 `teacher_account_links`, 계정 자체는 `admin_account` 라 **세 곳을 대조**해야 «있다/없다»를 말할 수 있습니다. 🔴 **현직인데 계정이 «아예 없는» 강사가 실재합니다** — 2026-08-31 실측: JANICE `mangoi_026`(8월 22건 수업 중) · JENNY `mangoi_148`(79건) · RICA `mangoi_007`(10건) 이 `admin_account`·`admin_login_history` 어디에도 없었습니다. 시간표를 넣어 줘도 **본인 화면에는 안 보입니다.** ✅ 계정은 우리가 만들 필요가 없습니다 — 그 아이디로 **한 번 로그인하면 옛 LMS 통과 인증이 그 자리에서 만듭니다**(`legacy-teacher-auth.ts`). ⚠️ **프로필 메모의 아이디와 실제 쓰는 계정이 다를 수 있습니다** — 강선생님은 메모엔 `mangoi_cn2` 인데 실제로는 `hq_t_kang` 으로 연결돼 잘 쓰고 계십니다. 그러니 «계정이 없다» 가 아니라 **«프로필에 적힌 아이디로는 없다»** 까지만 단정하세요. ⛔ 카페24 강사번호로 사람을 찾지 마세요(원부 번호와 다른 체계 — 이 표의 「남의 이름이 뜸」 항목) |
| 화면 «위쪽 한쪽 구석» 에 고정 버튼을 하나 더 붙였는데 **반대쪽 구석의 것이 제목 글자를 덮음** | 🔴 **가운데정렬은 «한쪽만 비우면» 반대쪽으로 밀립니다.** 2026-08-31 음성 코칭 화면에 [🏠 홈] 을 왼쪽 위 ← 옆에 붙이고 `.brand{padding-left}` 만 키웠더니, 가운데정렬된 제목이 그만큼 오른쪽으로 밀려 **우상단 ☰ 가 제목 글자 위에 얹혔습니다**(390·430px 실측 — 붙이기 «전» 에는 8px 여유로 안 겹쳤습니다). ✅ 예약은 **양쪽을 함께** 하세요(왼쪽 클러스터·오른쪽 클러스터를 각각 재서 좌우 padding 으로). ⛔ 그 폭을 숫자로 박지 마세요 — 이 화면은 `back-nav.js` 가 `innerWidth<=480` 으로, 화면 CSS 는 640px 으로 기준이 달라 **480~640px 구간에서만 조용히 어긋납니다.** 버튼을 «재서» 그 값을 CSS 변수로 넘기세요. 🔴 **그다음에 밟는 것 — `100vw` 는 세로 스크롤바 폭까지 포함합니다.** 남은 폭에 제목을 맞추려고 `calc((100vw - 예약폭)/N)` 을 썼더니 320px 창에서 계산은 147px 인데 **제목 상자는 138px** 이라 두 줄로 쪼개졌습니다(그리고 두 줄이 되면 «폰에서 스크롤 없이 한 화면» 이 깨집니다). ✅ `clientWidth`(스크롤바 뺀 실제 배치 폭)를 **재서** 넘기세요. ⚠️ **문자열 하니스·타입체크로는 둘 다 안 보입니다** — 마크업도 CSS 도 «맞고» 틀린 것은 «몇 px 이고 몇 줄인가» 뿐입니다. 확인은 브라우저에서 `getBoundingClientRect()`·`Range.getClientRects()`(줄 수)·`elementsFromPoint`(맨 위가 누구인가) 세 가지. ⛔ 그 자리 맞추기를 상주 `MutationObserver`·`setInterval` 로 지키지 마세요(홈을 통째로 멎게 한 전례) — 처음 몇 번과 `resize` 때만 재면 됩니다. 📌 **문서 흐름 «안»(sticky 헤더)에 항목을 더할 때는 «변경 전 높이» 와 대조하세요** — 항목이 하나 늘어 한 줄에 안 들어가면 헤더가 두 줄로 커지고, sticky 라 **그만큼 본문이 영구히 가려집니다**(2026-09-01 `teacher-training.html` 실측: 768px 에서 64 → **112px**. 16px 이 부족했던 것이라 그 폭에서만 라벨을 접고 간격을 줄여 되돌렸습니다 — 브랜드·도구는 안 건드렸습니다). ⚠️ 그리고 `justify-content:space-between` 인 헤더에 항목을 더하면 **가운데 항목이 밀립니다** — `flex-start` + 오른쪽 묶음에 `margin-left:auto` 로 바꾸세요 |
| 「이 화면에 뒤로가기·홈 버튼이 있나」를 **파일에서 세려** 할 때 | 🔴 **정적 grep 은 양쪽으로 틀립니다.** ① `back-nav.js` 의 ← 는 **조건부 주입**입니다 — 홈 화면에서는 안 넣고, 「이미 보이는 ← 가 있으면」 건너뜁니다(`hasVisibleBack`). 즉 «스크립트가 있으니 버튼도 있다» 가 아닙니다. ② 공용 [🏠 홈][🌐 EN] 바는 `mango-i18n.js` 가 넣지만 `<meta name="mangoi-no-global-bar">` 로 꺼져 있을 수 있습니다(그 화면은 홈이 없는 것입니다). ③ 🔴 **학생 게임 16개는 «iframe 자식» 입니다** — `student-games.html` 이 `_hubIframeSrc` 로 감싸 열고(`speaking-quiz`·`suspect-mystery`·`battle-3d` 도 같은 목록), 홈 첫화면도 `#game-suite-frame` 으로 같은 방식입니다. 그래서 파일만 보면 **「나가는 문이 하나도 없다」로 나오는데 거짓입니다** — 실측하면 게임을 연 순간 좌상단이 **「← 게임 선택」** 으로 바뀌고 폰·PC 둘 다 눌립니다(2026-09-01. 게임 안에서 홈까지는 2탭). ✅ 판정은 **브라우저에 그려서** 보이는 `a`·`button` 을 훑고 **`elementsFromPoint` 로 «맨 위인가»** 까지 재세요 — 「있다」·「보인다」·「눌린다」는 다 다릅니다. ⚠️ 오버레이가 있는 화면(warmup 설정)은 **아래 화면의 버튼이 «가려짐» 으로 잡히는 것이 정상**입니다(오버레이가 자기 나가는 문을 갖고 있으면 됩니다). ⚠️ ← 를 JS 로 만들 때는 `history.length`(가능하면 `document.referrer` 까지) 를 함께 보세요 — `history.back()` 하나면 카톡 링크로 들어온 사람에게 **무반응**입니다. ✅ **그 판정을 새로 쓰지 마세요 — 정본은 `js/back-nav.js` 가 내보내는 `window.mangoiGoBack` 입니다**(2026-09-01 신설). 화면 헤더에 이미 ← 가 있으면 그것에 붙이세요: `onclick="if(window.mangoiGoBack){event.preventDefault();window.mangoiGoBack();}"` + **`href="/"` 를 남겨** 폴백으로 씁니다(그 파일은 `?v=` 없이 실려 캐시에 옛 사본이 남을 수 있고, 홈 화면에서는 맨 위에서 return 하므로 함수가 없습니다 → 그때도 «홈으로는 갑니다». `href="#"` 로 두면 무반응 = 「버튼이 고장났다」가 됩니다). ⚠️ 헤더에 보이는 ← 가 있으면 back-nav.js 는 자기 동그란 ← 를 **안 넣습니다**(`hasVisibleBack`) — 그래서 화살표가 둘이 되지 않습니다. ✅ **2026-09-01 에 복제본 5개를 전부 정본으로 모았습니다**(`micro-quiz`·`judgment`·`student-games`·`game-learning-science`·`warmup`) — 이제 이 저장소에 뒤로 판정은 **한 곳**뿐입니다. 그러면서 정본이 인자 둘을 받습니다: **`mangoiGoBack(폴백주소, {sameOriginOnly:true})`**. ① 폴백은 화면마다 다릅니다 — 게임 안 학습화면(`game-learning-science`)은 홈이 아니라 **게임 허브**(`/student-games.html`)로 가야 합니다. 갈아치울 때 «그 화면이 원래 어디로 갔는지» 를 반드시 보존하세요. ② `sameOriginOnly` 는 웜업의 정책입니다 — 「앞 화면이 우리 사이트일 때만 되돌아가고, 밖에서 왔으면 홈」. 정책이 다른 화면을 «같게» 만들지 말고 **인자로** 넘기세요. 🔴 폴백 인자를 받을 때 **«문자열일 때만» 인정하세요** — 이 파일 안에 `b.onclick = goBack` 자리가 있어 **첫 인자로 Event 가 들어옵니다**(리스너 첫 인자는 항상 truthy — 이 저장소가 이미 밟은 함정). 그대로 쓰면 `location.href = <Event>` 가 됩니다. ⚠️ `<button>` 은 `href` 폴백이 없으니 `else{location.href='…'}` 안전망을 남기되 **거기에 판정을 다시 적지 마세요**(그 순간 복제가 되살아납니다) |
| 🔴 **로컬 서버로 화면을 «고친 뒤 다시» 잴 때** | **브라우저가 편집 전 사본을 캐시에서 줍니다.** 2026-09-01 실측: `speech-coach-cn.html` 에 버튼을 넣고 쟀는데 `#sc-home-btn` 이 **«없다»** 고 나왔고, 파일·서버 응답에는 멀쩡히 있었습니다(`curl` 로 확인). 주소에 `?_nc=<시각>` 을 붙이자 즉시 통과했습니다. ⚠️ 이걸 모르면 **멀쩡한 코드를 의심**하며 헤맵니다(같은 장 「FAIL 이 나면 검사 쪽을 먼저 의심하라」의 형제입니다). ✅ CDP `Page.navigate` 헬퍼에 캐시 우회를 **박아 두세요**. ℹ️ 배포 화면 쪽 캐시는 별건입니다 — 거기는 `?v=` 원장과 시크릿 창으로 가립니다 |
| 스크립트로 **설명 주석을 «그 줄 앞»** 에 끼워 넣었더니 화면이 통째로 깨짐 | 🔴 **`onclick=`·`aria-label=` 이 있는 줄은 «태그 도중» 일 수 있습니다.** 그 앞에 주석을 넣으면 `<button` 과 `>` **사이**에 들어가 그 태그가 깨집니다. 2026-09-01 실측: `judgment.html`·`game-learning-science.html` 의 뒤로 버튼이 `<button type="button" class="tn-btn" …` 다음 줄에 `onclick=` 을 두고 있어서, 그 줄 앞에 주석을 넣자 **버튼이 DOM 에서 사라졌고**(그래서 `back-nav.js` 가 «보이는 ← 가 없다» 고 판단해 자기 동그란 ← 를 하나 더 넣어 **화살표가 둘**이 됐습니다). ⚠️ **에러가 안 납니다** — HTML 파서는 조용히 복구하고, 타입체크·문자열 하니스는 통과합니다. ✅ 넣을 자리는 **위로 올라가 `<button`·`<a` 가 시작하는 줄**을 찾아 그 앞입니다. ✅ 검사는 정규식 한 줄로 됩니다 — `<(?:button\|a)\b[^>]*<!--` 가 하나라도 잡히면 태그 «안» 에 들어간 것입니다. ✅ 그리고 **브라우저로 재세요** — 이번에도 「그 버튼이 DOM 에 있나 · 화살표가 둘이 아닌가」 검사가 잡았습니다(제가 저지른 결함이고 문자열 검사로는 못 봤습니다) |
| `docs/INDEX.md` 충돌을 **「양쪽 항목을 다 남기기」로 풀었더니 «지운 줄» 이 되살아남** | 🔴 **그 규칙은 «둘 다 추가한» 경우의 것입니다 — 상대가 «지운» 경우에는 반대로 작동합니다.** 2026-09-01 실측: `main` 의 직전 커밋(#694)이 INDEX 목차의 **중복 한 줄을 지우는** 커밋이었는데, 제 브랜치에서 충돌을 «양쪽 다 남기기» 로 풀자 **그 줄이 그대로 되살아났습니다**(충돌 표시의 «상대» 쪽이 «없음» 이라 «내 쪽만» 남긴 것이 곧 부활이었습니다). ⚠️ 하니스도 CI 도 못 잡습니다 — 목차 줄이 두 개인 것은 문법 오류가 아닙니다. ✅ 풀고 나서 **`git show origin/main:docs/INDEX.md` 와 대조**하세요 — 같은 문서를 가리키는 줄이 둘이면 **`main` 에 남아 있는 판을 남기고** 나머지를 지웁니다(둘 중 어느 것이 최신인지는 길이·내용으로 눈대중하지 말고 그 대조로 정합니다). ✅ 더 값싼 예방: **`git log --oneline -3 origin/main` 으로 «상대가 무엇을 했는지» 를 먼저 보세요.** 「중복 정리」·「삭제」·「되돌리기」 커밋이 보이면 자동 해결을 쓰지 마세요 |
| 새 주기 작업(cron)을 추가하려는데 | ⛔ **못 넣습니다 — Cloudflare 계정 cron 한도가 5개인데 이미 5개를 다 쓰고 있습니다**(`wrangler.toml` 157행: `["0 18 * * *", "0 0 * * *", "0 1 * * *", "0 10 * * *", "*/15 * * * *"]`). 여섯 번째를 등록하면 배포가 `code 10072` 로 거절됩니다. ✅ 새 주기 작업은 **기존 cron 안에서 함수를 부르는 방식**으로 얹으세요 — 밤 3시 야간 동기화(`0 18 * * *` = KST 03:00)나 15분 감시견(`*/15 * * * *`)이 자리입니다. ⚠️ 그때 **`event.cron` 정확일치로 가르세요** — 시(hour) 비교로 가르면 15분 감시견이 같은 시간대에 네 번 참이 되어 이중 실행됩니다(이 표의 「야간 작업이 하루에 여러 번」 항목). ℹ️ 2026-08-31 카페24 미러가 이 한도 때문에 「아침 6시 cron 신설」을 포기하고 «15분 좁은 창 + 야간 넓은 창» 으로 설계했는데, 결과적으로 그쪽이 더 나았습니다(카페24 예약이 당일에도 채워지므로 하루 한 번으로는 놓칩니다) |
| 관리자 화면에서 글자에 색을 줬는데 **화면에서는 다른 색**으로 나옴 (배지·칩) | 🔴 **글자색을 «인라인 `!important`» 로 덮는 페인터가 «셋» 입니다** — `js/adm-s12.js`(`fixWithin` → `darken()`) · `js/adm-s13.js`(`fixTextOnDark` → `#f8fafc`/`#101828`) · `js/adm-light-surfaces.js`(`fixText` → AA 4.5 로 보정). **인라인 !important 라 CSS 로는 못 이깁니다** — 파일 맨 끝에 조상 id 를 붙여도 집니다. 2026-09-01 실측(강사 상태 배지): 퇴사 `#991b1b` 가 `adm-s12` 의 darken 으로 `rgb(38,76,115)` 이 되고, 그 셋을 그냥 두면 **연분홍 위 흰 글자 = 대비 2.2**(안 읽힘)가 됐습니다. ✅ 색이 «구분 정보» 인 요소는 **세 곳 모두**에 등재하세요 — `adm-light-surfaces` 는 `SKIP_SEL`, `adm-s12` 는 `KEEP_SEL`, `adm-s13` 은 `TX_KEEP`(⚠️ 그 파일의 `EXCLUDE` 는 «배경 칠하기»용이라 `fixTextOnDark` 는 보지 않습니다 — 이름이 비슷해 헷갈립니다). 본보기: `.tp-st-badge`. ⛔ 한 곳만 등재하면 나머지 둘이 그대로 덮습니다(값이 바뀌므로 «고쳐진 줄» 알기 쉽습니다). ⚠️ 그리고 인라인 배경은 **`background-color:`** 로 쓰세요 — `background:#f…` 로 쓰면 `admin-inline-c.css` 의 옛 다크 규칙에 걸려 **배경이 통째로 투명**해집니다(실측 computed `rgba(0,0,0,0)` — 세 상태가 화면에서 구분되지 않았습니다). ✅ 판정은 코드가 아니라 브라우저 `getComputedStyle` 이고, 누가 썼는지는 `CSSStyleDeclaration.prototype.setProperty` 를 후킹해 **스택을 찍으면** 한 번에 나옵니다(선택자를 눈으로 좇으면 못 찾습니다). ⚠️ 문자열 하니스는 이걸 못 봅니다 — 수리 전에도 `--fast` 246건이 전부 초록이었습니다. 감시는 `teacher_status_inline_harness` ⑥절과 `manual/teacher-status-inline-browser.mjs` |
| 저장 뒤 «되돌리기» 버튼을 뒀는데 **눌러도 아무 일도 안 일어남**(에러 없음) | 🔴 **성공 뒤에 목록을 다시 읽으면, 방금 바꾼 행이 그 목록에서 빠지면서 «행 캐시» 에서도 사라집니다.** 되돌리기 콜백이 그 캐시를 보고 있으면 `if (!t) return;` 에서 조용히 끝납니다. 2026-09-01 실측(강사 명부 「🙈 명부에서 숨기기」): 숨긴 뒤 `loadTeacherProfiles()` 가 `window._tpRowById = {}` 로 비우고 **응답에 온 행만** 다시 채우는데, 숨긴 행은 기본 조회에서 빠지므로 5초 토스트의 「되돌리기」가 **항상 무동작**이었습니다. 「🙈 안보임」 필터에서 되살릴 때도 대칭으로 같습니다(그 조회는 숨긴 행«만» 줍니다). ✅ 되돌리기에 필요한 것(이름·되돌릴 값)은 **닫힘(closure)으로 넘기세요** — 캐시는 «있으면 쓰고 없으면 없는 대로». ⚠️ **하니스가 이걸 못 잡습니다** — 목록 스텁이 «무엇을 숨기든 같은 목록» 을 돌려주면 그 행이 안 사라져 되돌리기가 되는 것처럼 보입니다. 스텁을 **PATCH 를 반영하는 «상태 있는» 것**으로 두고, 되돌리기를 **실제로 눌러** 보세요(되돌리면 3건 FAIL 나는 것까지 확인). 📜 같은 뿌리: 이 표의 「«완료» 메시지를 띄웠는데 곧바로 사라짐」 — 둘 다 «성공 뒤에 도는 재조회» 가 범인입니다 |
| 「뜻」 버튼이 **직역**을 보여 줌 — 「Good job!」 → 「훌륭한 직업!」 | 🔴 **그 화면이 `/api/translate` 를 «모드 없이» 부른 것입니다.** 기본 경로는 번역모델 **m2m100 직역**이고, 학생이 읽는 «뜻» 은 **`mode:'learn'`**(언어모델 의역)이어야 합니다. 2026-08-24 에 웜업(`warmup.html`)에서 「Let's warm up before class」→「수업 전에 따뜻하게하자」 제보로 이미 고쳤는데 **`ai-friend.html` 만 빠져 있었습니다** — 같은 판정이 두 곳이면 한쪽만 고쳐진다는 그 형태입니다(2026-08-31 사장님 제보: 「Good job! 🦊 Do you have a pet animal?」 → **「훌륭한 직업! 당신은 애완 동물이 있습니까?」**). ⚠️ **에러가 안 납니다** — 번역은 «성공» 이고 틀린 것은 «무슨 한국어가 나오는가» 뿐이라 회귀 하니스 237건이 전부 초록이었습니다. ✅ 학생이 읽는 뜻 카드는 **전부 `mode:'learn'`** 으로 부르세요(`ai-friend`·`warmup`·`speech-coach`·`student-game-shooter`·`english-mastery-suite`). ⛔ **모드만 켜고 끝내지 마세요 — 그건 «확률» 입니다.** 이 저장소의 반복 실측이 「지시만으로는 안 지켜진다」(단어 수·문법·구두점 전례)라, 제일 자주 나오고 제일 크게 틀리는 **말머리 칭찬 상투구는 결정론으로** 못 박습니다 — 정본 **`src/learn-phrase-ko.ts`**(`peelLearnLead`)가 「Good job!」·「Great try!」·「Nice answer!」 66종을 떼어 내 정해진 한국어로 바꾸고 **나머지만** 모델에 넘깁니다. ⛔ 문장 «안» 의 낱말을 이 표로 갈아 끼우지 마세요(「My dad has a good job.」 의 job 은 진짜 직업입니다) — 떼는 것은 **말머리 + 종결부호가 붙은 것만**입니다(「Good job on your sentence」 는 안 뗍니다). 🔴 **프롬프트를 고치면 KV 캐시 접두사를 반드시 올리세요**(`trl1:`→`trl2:`) — 안 올리면 「훌륭한 직업!」이 **180일** 그대로 나옵니다. 🔴 **그리고 «말머리를 떼는 순간 캐시 조건이 뒤집힙니다»** — 옛 코드는 실패하면 `out = t`(원문)라서 `out !== t` 가 거짓이 되어 **저절로 캐시를 비켜 갔는데**, 말머리를 떼면 실패해도 「잘했어요! Do you have a pet animal?」처럼 `t` 와 «달라져서» 그 반쪽짜리가 180일 굳습니다(2026-08-31 수리 중 실제로 만들었다가 trap-check 가 잡았습니다 — 같은 모양의 전례가 이 표의 「폴백 음성을 «요청 화자» 키로 캐시」 항목). ✅ 「번역이 실제로 나왔는가」를 따로 들고 그때만 캐시하세요. ⏱ **배치 호출(한 번에 여러 문장)도 이 모드를 씁니다** — 서버가 `TRANSLATE_CONCURRENCY`(4)로 **동시에** 번역합니다(2026-09-01). ⛔ 그 상한을 크게 올리지 마세요(뉴런 소진 429 가 나면 그 문장은 원문으로 떨어져 게임에서 빠집니다). ⛔ 화면 쪽 `texts` 개수(50)도 올리지 마세요 — 문장마다 모델을 한 번씩 부릅니다. ✅ **고정 인사말**처럼 손으로 다듬은 의역이 필요하면 `curatedLearnMeaning()`(같은 파일)에 넣으세요 — ⛔ 화면 쪽에 정규식을 다시 만들지 마세요(2026-09-01 에 `warmup.html` 의 같은 판정을 지우고 서버 한 곳으로 모았습니다). ℹ️ 다만 «그린 쪽이 방금 자기가 쓴 한국어를 미리 넣어 두는 것»(`warmup.html` 의 `_koCache`)은 중복이 아닙니다 — 판정이 아니라 요청 자체를 없애는 빠른 길입니다. ✅ 프롬프트에 「~습니까」·「당신」 금지를 함께 넣으세요(아이가 읽는 카드입니다). 감시는 `test-harness/learn_translate_meaning_harness.mjs`(49종 — 표를 **실제로 돌려** 66종 전수로 확인하고, 화면 다섯 곳의 `mode:'learn'` 배선도 봅니다. 되돌리면 실제로 FAIL 납니다) |
| 기능은 멀쩡한데 **화면에서 찾아갈 길이 없음** — 코드에는 입구가 여럿 보이는데 전부 죽어 있음 | 🔴 **«코드에 있다» 와 «화면에 있다» 는 다릅니다. 코드만 읽고 경로를 답하면 틀립니다** — 2026-09-01 에 제가 두 번 틀렸습니다. 사장님 「원격수리하는 곳이 사이트에서 어디야?」에 「그리드 메뉴 ▸ 도움말·기타 ▸ 💻 PC원격지원」이라고 답했는데, 브라우저에 그려 보니 그 길이 죽어 있었습니다. 실측: ① 그리드 메뉴 타일 → `index.html:14675` 의 **`fix-v19` 가 `#grid-menu`·`#cat14-modal`·`#v11-modal` 을 `display:none !important` 로 통째로 감춥니다**(계산값 확인) ② 카테고리 모달 카드 → `showCatV11` 이 모달을 만들고 **`document.body` 에 안 붙여서** `a[href="/remote.html"]` 가 **0개**(즉 제가 「404 카드를 고치자」고 한 그 카드는 **화면에 아예 안 나타나는 죽은 코드**였습니다 — 고쳤어도 화면은 그대로였을 것입니다) ③ 전체메뉴 오버레이 → 보이는 항목 71개 중 원격 **0건**. ⟹ 살아 있던 길은 **「검색창에 「원격」 을 정확히 치기」 하나뿐**이었고, 그건 **컴퓨터가 안 되는 학생에게 검색을 요구하는 구조**였습니다. ⚠️ **회귀 하니스·타입체크가 전부 초록입니다** — 함수도 값도 다 «있고» 틀린 것은 «보이는가» 뿐입니다. ✅ 판정은 **로컬 서버 + 크로미움으로 그려서** — `getComputedStyle(el).display`·`visibility` 를 재고, 눌리는지는 `document.elementFromPoint` 로 «맨 위가 누구인가» 를 보세요(「있다」·「보인다」·「눌린다」는 다 다릅니다). ✅ 새 입구를 만들 때는 **사람이 실제로 여는 자리**에 두세요 — 「문제가 확인된 그 순간」이 가장 정확합니다(`precheck.html` 진단 실패 줄 · 홈 ➕ `#mg-fab-wrap` 처럼 이미 «문제 생겼을 때 누르는 묶음»). ⛔ 죽은 코드를 고쳐 놓고 «고쳤습니다» 라고 적지 마세요. ✅ 주소 하나로도 열리게 두면(`/?menu=remote`) 카톡으로 안내할 수 있고 타일 `href` 폴백도 됩니다(⛔ `href="#"` 로 두면 JS 가 죽었을 때 「눌러도 아무 일도 없음」). 📄 `docs/작업기록/260902_원격도움받기_입구와_접속코드_양방향전달.md` |
| **원격으로 학생 기기에 들어가 고치게** 하려 할 때 | 🔴 **브라우저에는 «남의 기기를 조작하는» API 가 없습니다 — 보안상 일부러 없어서 어느 웹사이트도 못 합니다.** TeamViewer·AnyDesk 도 전부 네이티브 프로그램을 깔아야 동작합니다. ⟹ 우리가 할 수 있는 것은 **접속 코드를 날라다 주는 것**뿐이고, 실제 조작은 Quick Assist·AnyDesk 가 합니다(정본 `src/api-mango.ts` 의 `/api/class/remote-support/*` 네 경로 · 화면 `js/idx-remote-support.js`·`js/adm-remote-pin.js`). ⛔ 우리 서버가 학생 화면을 제어하는 코드를 붙이지 마세요. 🔴 **방향이 둘이고 서로 정반대입니다** — PC(Quick Assist·Chrome 원격)는 **직원이 코드를 만들고 학생이 입력**, 폰(AnyDesk)은 **학생 기기가 번호를 갖고 직원이 접속**. 한 방향만 만들면 폰이 통째로 빠집니다. ⛔ **학생 쪽 조회를 PIN 으로 열지 마세요** — 6자리로 열면 `claim` 의 시도 5회 제한을 우회해 번호를 찍어 볼 수 있습니다(claim 이 32자 세션 토큰을 발급하고 그 뒤로는 그것만 씁니다). ⛔ 코드는 **숫자 4~12자리만** 받으세요(그 값이 상대 화면에 그대로 그려집니다 — Quick Assist 6 · AnyDesk 9~10 · Chrome 원격 12 를 덮습니다). ⛔ 학생이 PIN 을 넣기 «전» 에 직원이 보낼 수 있게 두지 마세요 — 보낼 곳이 없는데 «보냈다» 고 답하면 직원이 기다리기만 합니다(서버 `not_claimed` + 화면 잠금 둘 다). ⚠️ **화면공유(`getDisplayMedia`)로 «보기» 를 대신 만들려 하지 마세요** — `js/idx-main.js:952` 가 직접 「컴퓨터의 크롬이나 엣지에서 사용해 주세요」라고 말하듯 **모바일 브라우저는 지원하지 않고**, PC 에서는 위 방식이 **조작까지** 되므로 «보기» 는 열등합니다(A 가 되는 곳에선 열등하고 A 가 안 되는 곳에선 불가능 — 2026-09-01 에 사장님이 그 안을 고르셨지만 재 보고 안 만들었습니다). ⚠️ **아이폰은 «조작» 이 안 됩니다** — 애플이 서드파티에 그 권한을 주지 않습니다. **이건 플랫폼 사실이고 이 저장소에서 잰 것이 아닙니다** — 실제 기기로 확인하세요. ⚠️ 수업 «중» 에는 눈에 띄는 자리에 두지 마세요 — 지금부터 프로그램을 깔고 코드를 주고받는 일이라 20분 수업이 끝납니다(설정 시트 안의 조용한 한 줄로). 감시는 `test-harness/manual/remote-code-relay-browser.mjs`(브라우저 38종 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 브라우저 하니스로 **«가려졌나»·«눌리나»** 를 잴 때 | 🔴 **화면 «가운데 몇 점» 을 찍으면 헛돕니다** — 실제로 겹치는 것들(수업 독은 아래 가운데, A.i 위젯·➕ 는 아래 모서리)이 그 표본을 비켜 갑니다. 2026-09-01 실측: 모달 `z-index` 를 옛 값(11500)으로 되돌려도 **32건이 전부 초록**이었습니다. ✅ ① **그 상자가 가진 조작들**(`button, a, input`)을 하나하나 훑어 그 자리의 `elementFromPoint` 가 상자 «안» 인지 보고 ② 그것만으로도 부족하니 **쌓임 순서 자체**를 재세요 — 지금 화면에 떠 있는 fixed/sticky 중 내 `z-index` 보다 위인 것이 있으면 FAIL(⛔ 단 `#mg-fab-wrap`·`#vc-reconnect-banner` 는 **일부러 위**라 예외). 그렇게 고치니 되돌리기에서 **실제로 4건 FAIL** 이 났습니다. 🪤 **관리자 화면 검사에는 함정이 둘 더 있습니다** — ① **`admin.html` 은 자기 `fetch` 래퍼를 «나중에» 씌웁니다.** 스텁을 `Page.addScriptToEvaluateOnNewDocument` 로 먼저 깔면 그 래퍼에 덮여 무력화되고, 화면은 404 를 «성공 아님» 으로 **올바르게** 처리하므로 **멀쩡한 코드가 FAIL 로 보입니다**(실측). 스텁은 **로드가 끝난 뒤** 거세요. ② **IA6 는 카드를 «한 번에 한 장» 만 보여 줍니다**(`.ia6-hide` = `display:none`) — `jumpToMenu('card-…')` 로 그 카드를 먼저 열지 않으면 글자는 읽히는데 «보인다·눌린다·포커스» 만 헛돕니다(`!!el.offsetParent` 를 검사 한 줄로 박아 두세요). ✅ 그리고 **되돌려 실제로 FAIL 이 나는지 반드시 확인**하세요 — 이 세 가지 모두 그 확인에서만 드러났습니다 |
| 검사에서 «본보기 값» 이 «정책에 따라 바뀌는 값» 일 때 | 🔴 **정책이 바뀌면 그 검사가 «양쪽 방향으로» 틀립니다 — 그리고 조용한 쪽이 더 위험합니다.** 배포 보류 창 검사 두 곳이 «창 밖의 본보기» 로 **수요일**을 쓰고 있었습니다(`deploy_class_window_harness` 의 live-class 짝 시험 · `deploy_class_guard_harness` 의 「창 밖이라 실제로 물어본다」 CLI 시험). 2026-09-02 에 수요일이 창에 들어오자 각각 **3건·4건 FAIL** — 여기까지는 좋습니다. ⚠️ **문제는 반대 방향입니다** — 그 자리에 「이 시각이 정말 창 밖인가」를 묻는 줄이 없으면, 창이 넓어졌을 때 «보류된다» 가 **창 때문인지 실접속 때문인지 구별하지 못한 채 조용히 초록**이 됩니다. 그러면 요일 축소의 짝인 실접속 판정이 죽어도 아무도 모릅니다. ✅ 본보기는 **그 정책이 건드릴 수 없는 값**으로 고르세요(여기서는 실측상 수업 0건인 **토요일**). ✅ 그래도 **「그 본보기가 실제로 그 조건을 만족하는가」 한 줄을 짝으로** 두세요 — 정책이 또 바뀌면 그 줄이 «먼저» 빨간불이 되어 아래 검사가 헛도는 것을 막습니다(`isClassWindow(t) === false`). ⛔ 본보기를 정책 값으로 되돌리지 마세요(주석으로 못 박을 것). ✅ 확인은 **변이시험**으로 — 고친 뒤 정책을 되돌려 보면 이제 «정책» 검사만 FAIL 나고(「수요일은 배포한다」·「요일이 정확히 두 개다」) 본보기 때문에 나던 곁가지 FAIL 7건은 사라집니다. **검사가 느슨해진 것이 아니라 정확해진 것**입니다. 📜 같은 줄기: 이 표의 「하니스가 «객체 모양» 을 정규식으로 못 박아 두어」·「화질·지연 문턱을 더 세게 조였는데 하니스가 빨간불」 — 그 둘은 «옛 값을 못 박아 강화한 수리에 FAIL», 이쪽은 «본보기가 정책에 딸려 다녀 양방향으로 틀림» 입니다. 📄 `docs/작업기록/260902_뒤로홈버튼_INDEX중복정리_배포창_요일세번바뀜.md` |
| 카페24 미러가 **«실제로 없는 수업»** 을 강사 화면에 만듦 (「LMS에 없는 수업이 있어요」) | 🔴 **카페24는 강사를 바꿀 때 옛 예약을 지우지 않고 남깁니다 — 미러는 카페24가 정본이라 그것을 그대로 만듭니다.** 2026-09-01 실사고: Zee 를 켠 날(09:13 등록 → 09:15 자동실행) **9/1 17:40 허윤아**(카페24 `511745`)가 강사 화면에 떴고, 강사가 헛기다린 뒤 **학생 노쇼까지 찍혔습니다**(Teacher Zee 제보). ⚠️ **켜기 전 «채점» 이 못 잡았습니다** — 「겹침 0·유령 패턴 0」이었는데 그 자리는 다른 수업과 **시간이 안 겹칩니다**(허윤아 9/1 = 17:00 Far + 17:40 Zee + 21:30 Zee). ⛔ **「같은 학생·같은 날 2건이면 잔재」로 막지 마세요** — 허윤아는 **진짜로 하루 두 번**(17:00·21:30) 수업합니다. 그렇게 막으면 멀쩡한 수업이 사라집니다. ✅ 실제로 가르는 신호는 **«그 자리가 되풀이되는가»** 하나뿐입니다 — `attendance` 의 `c24-*` 행을 **(학생·강사·시각)** 으로 세어 **1건뿐이면 잔재**로 봅니다(2026-09-01 실측: 허윤아·Zee 21:30 **4건**=진짜 · 17:40 **1건**=유령 · 한채아·Belle 15:10 2건=진짜). 정본은 `planMirror` 의 **`suspect_dup`**(`src/c24-mirror.ts`), 근거는 `loadSlotHistory`. ⚠️ **입구 조건(그날 그 학생에게 2건 이상)을 빼면 «새로 생긴 주간 수업» 이 전멸합니다** — 변이시험으로 확인했습니다. ⚠️ 이력이 **비면 판정을 통째로 건너뜁니다** — 「0건이었다」와 「안 봤다」는 다릅니다(같은 방어가 미러의 «취소 단계» 에도 있습니다). ⛔ 그래서 그 조회에 **`swallowErrors` 를 쓰지 마세요** — 청크 하나가 실패했는데 나머지를 이어 붙이면 Map 이 «비지는 않았지만 불완전» 해져 `size > 0` 게이트를 **통과**하고, 그 학생들의 수업이 통째로 차단됩니다. 한 청크라도 실패하면 **빈 Map** 을 돌려주세요. 🔴 **한 번짜리 수업(보강)은 «하루» 가 아니라 «그 자리가 두 번째로 잡힐 때까지» 보류됩니다** — 야간 동기화가 돌면 그 수업 «자신» 이 이력 1건이 되지만 임계값이 `< 2` 라 여전히 걸립니다. 즉 「정규수업이 있는 날에 한 번만 추가되는 보강」은 미러가 만들지 않고 `suspect_dup` 으로 띄우니 **사람이 보고 손으로 넣으면 됩니다.** ⚠️ 이것이 이 판정이 치르는 진짜 값입니다 — 「안전한 방향의 실패」로 뭉뚱그려 적었다가 trap-check 가 «사실이 아니다» 로 잡았습니다. ⚠️ 판정은 **화이트리스트 «뒤»** 에 둡니다 — 앞에 두면 안 켠 강사까지 걸려 `not_whitelisted` 집계가 흐트러집니다(대신 그쪽은 detail 에 «켜기 전 확인» 한 줄만 붙입니다). ⚠️ **완전하지 않습니다 — 그렇게 적어 둡니다.** 카페24가 «같은 자리» 에 잔재를 둘 이상 남기면 이력이 2건이 되어 그냥 통과합니다(허윤아·Far 17:00 이 그 모양 — 8/27 에 실제로 있었던 것은 17:20 입니다). 🔴 **2026-09-02 에 실제로 한 번 통과했고, 그 뒤가 더 나빴습니다** — 최검·Len 9/2 18:20(이력 7/8+오늘 = 2건)이 만들어진 뒤 진짜 수업(Win, 이력 11건)이 같은 자리라 `update`(강사 변경)로 Win 에게 넘어갔는데, **다음 감시견이 Len 예약을 다시 읽고 Len 으로 되돌렸습니다 — 15분마다 강사가 번갈아 바뀌었습니다**(운영 D1 실측: 행 1230 `teacher_id` 17:06 Win → 17:15 Len → 17:30 Win. 이경록 16:50 Kaye↔Len 도 같은 모양). 옛 코드는 «행을 아직 안 가진 쪽» 이 매 회차 update 를 얻는 구조라 읽는 순서와 무관하게 뒤집힙니다. 에러는 없고 강사 화면만 계속 바뀝니다. ✅ 같은 날 수리 — **같은 학생·날·시각에 강사가 둘 이상이면 «이력이 많은 쪽» 만 진짜**(`planMirror` 1-b «같은 자리 경합»), 진 쪽은 이미 있는 행을 **길이만 달라도** 덮어쓰지 않습니다(applyMirror 의 UPDATE 가 `teacher_id` 까지 통째로 쓰기 때문 — trap-check 가 잡음). 동률(신입 학생, 둘 다 1회)은 둘 다 보류이고 **저절로 풀리지 않습니다**(야간 동기화가 둘을 똑같이 셉니다) — 카페24에서 하나를 지워야 합니다. 그 행의 `notes` 는 잔재 번호(`c24:512222`)로 남을 수 있고, 카페24에서 잔재를 지우면 취소 단계가 내리고 진짜 번호로 다시 만듭니다. 근본 해결은 **«카페24가 취소·변경을 어떻게 표시하는가» 를 확인해 그것으로 거르는 것**이고, 그 답을 찾으려고 성적표에 **`prop_keys`**(:Class 속성 이름)를 함께 싣습니다. ⛔ 그 값으로 판정하지 마세요(뜻을 확인한 뒤가 순서입니다). ✅ **이미 만들어진 유령은 관리자 화면의 «수업 삭제» 로 내리세요** — `status='cancelled'` **와 함께** `source='c24-mirror:manual'` 도장이 찍혀야 미러가 다시 안 만듭니다. ⛔ `status` 만 바꾸면 **15분 뒤 되살아납니다**(`planMirror` 가 취소된 행을 «없는 것» 으로 봅니다). ⛔ 노쇼 기록은 지우지 마세요(학생이 못 본 것은 사실입니다 — 이 표의 「노쇼 알림이 떴는데」 항목). ✅ 되돌리는 길은 `c24_mirror_config.dup_guard='off'` 한 줄(기본 켬). 감시는 `test-harness/c24_mirror_harness.mjs` **O절**(48종 — `planMirror` 와 `loadSlotHistory` 를 **둘 다 실제로 돌립니다**. O-12 14종은 «같은 자리 경합» 을 순서를 바꿔 두 번 돌려 같은 답인지·행이 어느 쪽으로 있어도 덮어쓰지 않는지·길이만 다른 잔재까지 봅니다(변이: 경합 판정 끄기 5건 · 덮어쓰기 되살리기 3건 실제 FAIL). 이력은 진짜 SQLite 를 D1 모양으로 감싸 **정본 함수에 물립니다** — ⚠️ 처음엔 SQL 을 «베껴» 돌렸는데 그러면 정본을 한 번도 실행하지 않아, 학생↔강사 인자를 뒤바꾸거나 `COUNT(*)` 를 `1` 로 바꿔도 **전부 초록이었습니다**(trap-check 가 잡음). 변이시험: 판정을 끄면 O-1·O-6, 입구 조건을 빼면 O-3, 인자를 뒤바꾸면 O-8 5건, `swallowErrors` 를 되살리면 O-8b 가 실제로 FAIL 납니다) |
| 「이미 고친 사고」인데 **한 화면만 그대로 재발** (저장·다운로드) | 🔴 **고침을 «엔드포인트 안» 에만 넣고 «부르는 곳» 을 다 안 옮기면, 옮겨 간 화면만 낫고 나머지는 그대로입니다.** 2026-08-15 에 「갤럭시에서 녹화 저장이 «다운로드에 실패했습니다» 만 반복한다」를 진단해 `/api/recording/play?…&dl=1` 을 만들었습니다 — `dl=1` 이면 **Range 를 무시하고 200 전체 본문**을 주고 `Content-Disposition: attachment` 를 붙입니다(안드로이드 DownloadManager 는 저장 요청에 `Range: bytes=0-` 를 끼워 넣고서 **206 이 오면 사유 없이 실패**합니다). 그런데 옮겨 간 것은 강사 화면(`flow.js`)뿐이고 **관리자 녹화 목록(`adm-core.js`)은 옛 `/api/recordings/blob/<키>` + `<a download>` 에 그대로 남아** 2026-09-01 사장님 「카카오에 저장도 안돼」로 다시 올라왔습니다(2026-09-01 수리, PR #682). ⚠️ **에러가 안 납니다** — PC 브라우저는 206 도 잘 저장하므로 «되는 사람 / 안 되는 사람» 으로 갈리고, 고친 쪽 화면으로 확인하면 멀쩡해 보입니다. ⚠️ **문자열 하니스도 못 잡았습니다** — 함수도 값도 다 «있고» 틀린 것은 «어느 통로를 부르는가» 뿐이라 수리 전에도 `--fast` 256건이 전부 초록이었습니다. ✅ 「고쳤다」를 적기 전에 **그 통로를 «부르는 곳» 을 전부 세세요** — 옛 경로를 `grep` 하는 데 30초면 됩니다. ✅ URL 은 **화면이 조립하지 말고 서버가 만들어 내려주세요**(`/api/recordings` 의 `dl_url`) — 화면은 «그 파일이 정말 있는가» 를 모르므로 «눌러도 404 나는 버튼» 을 만듭니다. 서버는 `play` 의 404 조건(키 해석·`status`·`storage`·만료)을 **그대로** 한 번 더 보고, 못 풀면 아예 안 줍니다. ✅ 교사·관리자는 **쿠키로만** 인증되는데 카톡 인앱 브라우저·안드로이드 WebView 는 저장을 **쿠키 없는** 다운로드 관리자에 위임합니다 — 녹화 1건 전용 단기 서명(`&sig=`, `auth-token.ts signRecDlSig`)을 함께 실으세요(2026-08-13 «휴대폰 저장 안 됨»). ⛔ 그 서명을 **인증 없는 경로에서 발급하지 마세요** — `/api/recordings` 목록이 관리자 전용(`isAdminOnlyApi`)이라는 전제 위에 서 있습니다. ⛔ **카톡으로 «파일» 을 보내려 하지 마세요** — 녹화는 webm 이고 21분이 195MB 입니다. 카카오톡·아이폰·갤러리는 mp4 를 전제로 해서 첨부 목록에 안 뜨거나 «지원하지 않는 형식» 이 됩니다. **링크를 보내세요**(`share_url` + 🔗 링크 버튼 — 휴대폰은 `navigator.share` 공유 시트, PC 는 클립보드 복사). ⛔ `window.open` 으로 열지 마세요(인앱 브라우저는 예외도 안 던지고 null 만 돌려줍니다). ⚠️ 그 링크는 **6시간 만료**입니다 — 화면이 **만료 시각을 사람에게 말해야** 합니다(감추면 「보냈는데 안 열린대요」가 됩니다). ⚠️ 미성년자 수업 영상이라 유효기간을 늘리는 것은 노출 창을 넓히는 **정책 판단**이고 사람이 정할 일입니다. 감시는 `test-harness/recording_download_link_harness.mjs`(51종 — 서버의 «내려줄 수 있는가» 판정을 소스에서 **오려 내 실제로 돌리고**, `play` 의 404 조건과 «서로 같은 말을 하는가» 를 두 파일로 대조합니다. 되돌리면 실제로 FAIL 납니다) |
| 「투명 유령」로 만든 것이 **화면에만** 나타남 (참관자·숨김 대상) | 🔴 **«방송을 안 한다» 와 «이름이 안 붙는다» 는 다른 이야기입니다.** 수업 관찰이 그랬습니다 — `handleJoinObserve` 가 `joined:false` 로 붙이고 방송을 하나도 안 해서 인원수·입퇴장·로스터(`existing-users`·`user-joined`) 어디에도 안 나오는데, **참관자는 영상을 받으려고 recvonly offer 를 직접 보냅니다.** 그 offer 를 중계하는 `handleOffer` 가 `this.usernameOf(userId) || '참가자'` 로 **이름을 지어내고 있었고**(`usernameOf` 는 joined 인 사람만 이름을 줍니다 → 참관자는 늘 null → 늘 «참가자»), 받는 화면이 그 이름으로 피어를 만들어 **강사 화면에 이름표가 「참가자」인 검은 얼굴 칸**이 생겼습니다(2026-09-01 사장님 「수업 관찰 할 때 참가자가 안 보이게 할 수 있어?」). ⚠️ **에러가 안 납니다** — 인원수는 맞고 얼굴 칸만 하나 더 있어서 «화면 버그» 로도 «참관 기능» 으로도 안 보입니다. ⚠️ **문자열 하니스가 못 잡습니다** — 유령 설계 검사(«방송을 안 하는가»)는 전부 초록이었습니다. 틀린 것은 «그 사람 이름이 어디로 새는가» 뿐입니다. ✅ **«숨긴다» 를 만들 때는 «그 사람이 나가는 통로» 를 전부 세세요** — 방송만이 아니라 offer·answer·ice 중계, 채팅, 귓속말, 로그 응답까지. 이름을 «지어내는» 폴백(`|| '참가자'`)이 있으면 거기가 바로 새는 자리입니다. ⛔ **다른 이름으로 바꿔서 풀지 마세요** — 참관자는 이름이 «있으면» 안 됩니다. ✅ 서버가 `fromObserver:true` 로 **표시해 주고**, 화면은 «서버가 참관자라고 말한 id» 에 대해서만 칸을 막습니다(`js/vc-observe-guard.js` ⑩절). ⛔ **«이름이 참가자인 칸을 지운다» 로 풀지 마세요** — 이름이 아직 안 붙은 진짜 학생 칸까지 지웁니다. ⛔ `vcRemovePeer(id,'left')` 로 지우지 마세요(학생 화면에 「수업이 끝났어요」 — 이 표의 그 항목). 감시는 `observer_camera_guard_harness` H절(14종 — ⑩절을 **오려 내 실제로 돌립니다**. «참관자는 안 생긴다» 만 넣지 말고 **«진짜 학생은 생긴다» 를 짝으로** 넣으세요 — 가드가 전부를 막아도 초록이 됩니다). ⚠️ 그 검사를 「`fromObserver` 라는 글자가 있는가」로 쓰지 마세요 — 변이시험에서 `const fromObserver = …` **선언만 남아도 통과**했습니다. **«보내는 묶음 안에» 있는가**로 물으세요 |
| 녹화 목록의 「교사」 칸에 **학생 계정**이 찍혀 있음 · 참가자를 이름으로 알고 싶을 때 | 🔴 **`recordings.teacher_name` 은 «교사» 가 아니라 «방을 먼저 켠 사람» 입니다.** `/api/recordings/start` 가 요청 본문의 `teacher_name` 을 그대로 적는데 그 요청은 먼저 들어온 사람의 브라우저가 보냅니다 — 그래서 그 칸에 `heyst`·`cys01`·`mby1` 같은 학생 계정이 그대로 올라옵니다(2026-09-01 사장님 「학생도 알아야 해서」). ⛔ **`participant_names` 를 학생 이름으로 쓰지 마세요** — 그 배열에는 교사 표시이름과 «접속할 때마다 새로 생기는 임시 번호»(`z6nn4uhuwt95py0f4o6hvm`)가 섞여 있습니다(D1 실측: 최근 15건 중 9건). ⛔ 이름으로 명부를 뒤지지도 마세요(동명이인 — 「김사랑」 계정이 6개). ✅ 근거는 둘뿐입니다 — ① 방 번호 `class-<예약id>-<날짜>` → `class_schedules` 의 `user_id`·`student_name`(결정론적) ② 녹화에 적힌 계정(`participant_ids` ∪ `consented_user_ids`) 중 **`students_erp` 에 실제로 있는 것**(「있으면 학생」이라 추측이 없고 임시번호·강사 계정이 저절로 걸러집니다). 정본은 `src/recording-students.ts` 하나이고 매칭은 `user_id` **완전일치**입니다(⛔ NOCASE 로 넓히지 말 것 — `Kim`/`kim` 처럼 대소문자만 다른 행이 실재해 «둘 중 아무나» 집습니다). ✅ 못 찾으면 **«—» 와 «왜 비었는지»** 를 함께 적으세요(빈칸은 고장으로 읽힙니다). ✅ 이 조회는 실패해도 **던지지 않습니다** — 학생 칸만 비고 목록은 그대로 떠야 합니다. ⚠️ 표에 칸을 하나 늘리면 **thead `<th>` 수 · 한 줄이 그리는 `<td>` 수 · 빈 줄 `colspan`** 셋을 함께 고쳐야 합니다(하나만 빠지면 «녹화 기록 없음» 줄만 폭이 다르거나 칸이 통째로 밀립니다). ⚠️ 화면에 이름을 보여 주면 **검색도 함께 넓히세요** — 보이는 이름으로 찾았는데 0건이면 「검색했는데 아무것도 없다」가 됩니다. 감시는 `test-harness/recording_student_column_harness.mjs`(40종 — 판정 모듈을 컴파일해 가짜 D1 로 **실제로 돌리고** 화면 셀도 오려 내 실행)와 `test-harness/manual/recording-student-column-browser.mjs`(브라우저 11종 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 녹화 목록의 「교사」 칸에 **아이디가 그대로** 나옴 (「교사 이름」에 계정이 뜸) | 🔴 **바로 위 항목의 뒷면입니다 — 그 칸을 채우던 두 값이 «둘 다» 교사가 아닙니다.** 화면이 `teacher_name || teacher_id` 를 그렸는데 앞의 것은 «방을 먼저 켠 사람» 이고(위 항목), **뒤의 것은 화상방(DO)이 접속마다 새로 발급하는 임시 번호**입니다 — 2026-09-04 실측 2,122행 중 **2,105행(99.2%)이 `u_` 로 시작**합니다(`u_iyeuu18a2v`). 그래서 사장님 화면에 `jye46712`(=학생 김선우 계정)·`jeong`·`heyst`·`교사 Mangoi_168` 이 「교사」 로 떴습니다. ⚠️ **앞으로 더 나빠지는 자리였습니다** — 2026-09-02 부터 강사는 자동 녹화를 안 하므로(`isStaffSkipRecording`) 그 칸은 «거의 항상 학생» 이 됩니다. ✅ 정본은 **`src/recording-teacher.ts`** 하나이고 근거가 셋뿐입니다 — ① 방 번호가 `class-<예약id>-<날짜>` 면 `class_schedules.teacher_id` → 이름 `teachers.name` · 아이디 `teacher_account_links.username`(실측 `class-*` 255건 중 **253건**이 이것으로 풀립니다) ② `teacher_name` 이 `교사 <값>` 이고 그 `<값>` 이 **강사 계정과 완전일치** ③ `<값>` 이 **원부 이름과 완전일치 + 후보가 유일**. ⛔ **`recordings.teacher_id` 를 아이디 칸에 그리지 마세요** — 계정이 아닙니다(정본은 그 칸을 한 번도 읽지 않고, 하니스가 그것을 못 박습니다). ⛔ **학생 계정을 교사 칸에 옮겨 적지 마세요**(`students_erp.user_id` 완전일치로 가릅니다) — 그것이 이 사고 자체입니다. ⛔ 부분일치 금지·후보가 둘 이상이면 붙이지 말 것(강사 번호가 세 벌이라 조용히 남의 이름이 붙고 **에러가 안 납니다** — 위 「남의 이름이 뜸」). ⚠️ 대소문자는 **정확일치 먼저**, 없을 때만 «대소문자만 다른 후보» 를 보되 **유일할 때만** — `Mangoi_168`(링크)과 `mangoi_168`(표시이름)이 **둘 다 실재**합니다. ✅ **이름은 찾았는데 계정 연결이 없는 강사가 실재합니다** — 그때 이름을 아이디 자리에 옮겨 적지 말고 «—» 와 이유를 적으세요. ✅ 근거가 없으면 «—» 와 «왜 비었는지 + 누가 켰는지» 를 함께 말합니다(빈칸은 고장으로 읽힙니다). ⚠️ 그러면 공용방 녹화(실측 1,587건)는 교사가 «—» 로 남는데 **그게 사실입니다** — 예약이 없어 알 방법이 없습니다. ⚠️ 아이디 칸에 `font-family` 를 적지 마세요 — 한자 글꼴 통일 가드(`hanzi_font_harness`)가 「맨 앞이 MangoiHanSC 가 아니다」로 FAIL 냅니다(실제로 밟음). 🪤 **이 변경이 기존 하니스 네 곳을 깨뜨렸습니다** — 「colspan 13」·「th 13」·「LIKE 5개」·반환문(`return { ...row, students };`)을 **글자 그대로** 못 박아 둔 검사들이라, 칸을 늘리고 검색을 넓히자 **보장은 오히려 세졌는데 검사만** 빨간불이 났습니다. 물어야 할 것은 «몇 개인가» 가 아니라 **«thead 칸 수 = 한 줄 `<td>` 수 = colspan 셋이 서로 같은가»**·«LIKE 자리 수 = 바인드 수»·«그 반환값에 들어 있는가» 입니다. ⚠️ 그때 colspan 을 파일 전체에서 찾으면 **다른 표의 「로딩 중」 줄**(`colspan="8"`)이 먼저 걸립니다 — `renderRecordingsTable` 안에서만 찾으세요. ⚠️ **문자열 하니스로는 이 사고를 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무엇을 교사라고 부르는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 감시는 `test-harness/recording_teacher_column_harness.mjs`(54종 — 판정 모듈을 컴파일해 가짜 D1 로 **실제로 돌리고** 화면 셀도 오려 내 실행. 변이 4종(옛 코드 되돌리기·학생 가드 제거·대소문자 아무거나·colspan 되돌리기) 전부 실제 FAIL) 📌 **덧붙임(같은 날) — 「학생」 칸도 같은 병을 앓고 있어 둘로 갈랐습니다**(사장님 «학생도 학생아이디 목록을 만들어줘»). ⚠️ 여기엔 함정이 하나 더 있습니다 — **서버(`recording-students.ts`)가 이름을 못 찾으면 «계정을 그대로 `name` 에» 넣습니다.** 칸이 하나였을 때는 「계정이라도 보여 준다」가 맞았지만, 갈린 뒤 그대로 그리면 **고치려던 사고가 학생 쪽에 그대로 남습니다** (실측: 정우영의 실사용 계정 `jeong` 은 카페24에 이름이 없어 명부에 「jeong」 으로 찍힙니다). ✅ 화면에서 **`name === uid` 면 «이름 모름»** 으로 보고 «—» 와 이유를 적고, 아이디는 옆 칸에 그대로 둡니다(잃는 정보가 없습니다). ⛔ 서버 폴백 자체를 없애지는 마세요 — 그 값을 쓰는 다른 검사·소비처가 있습니다. ⚠️ **두 칸은 «같은 사람·같은 차례» 로 그려야 합니다**(3명까지 + «외 N명»). 한쪽만 자르면 이름과 아이디가 어긋나 **남의 계정처럼** 읽힙니다 — 그런데 그 검사를 «앞 몇 명이 있나» 로 쓰면 한쪽을 2명으로 잘라도 **통과합니다**(변이시험에서 실제로 놓쳤습니다). **사람 수를 세서** 비교하세요. 📄 `docs/작업기록/260904_녹화목록_교사이름과아이디_두칸분리.md` |
| 「무엇을 가리나」를 **요소 상자**로 쟀더니 두 방향으로 다 틀림 | 🔴 **`elementFromPoint`·`getBoundingClientRect` 는 «글자 자리» 가 아니라 «상자» 를 줍니다 — 가운데정렬 글자는 상자가 폭 전체라도 글자는 가운데에만 있습니다.** 2026-09-01 실측(`student-game-space-monster` 390px): 문장 상자는 `x=12..378` 인데 **실제 글자는 `x=137..253`**, 버튼은 `x=10..44` 라 한 자도 안 덮는데 상자로 재면 «덮었다» 가 됩니다 → 쓸데없이 비켜섭니다. ⚠️ **반대 방향이 더 나쁩니다** — 상자의 «왼쪽 한 줄만» 찍어 판정하면 **오른쪽 글자를 통째로 놓칩니다**: escape-school·escape-voice·escape-zombie 의 「성공 0」, grammar-pizza·wordfighter 의 제목·점수를 덮고 있었는데 **13/13 초록**이었습니다(5개 게임이 그 상태로 통과). 🔴 게다가 «조상이 화면 대부분을 덮으면 시작 오버레이» 라는 편법 분류가 `DIV#game`(플레이 영역)을 오버레이로 읽어 **틀린 이유로 맞는 답**을 냈습니다 — 그래서 검사가 초록인 채 아무도 안 봅니다. ✅ **글자는 텍스트 노드마다 `Range.getClientRects()`** 로 재고, **누를 수 있는 것은 상자 그대로** 세세요(상자가 곧 탭 표적입니다). ✅ 덮는 것이 있어 비켜설 때는 **상한을 실측으로 정하세요** — 120px 로 «대충» 두었더니 grammar-pizza(왼쪽 위를 y=132 까지 씀)가 상한에 걸려 **포기하고 「🏷️ 성분 ON」 버튼을 덮은 채 멈췄습니다**(글자보다 탭 표적을 덮는 것이 최악인데 거기로 떨어짐). ⛔ 그 «비켜서기» 를 상주 `MutationObserver`·`setInterval` 로 따라다니게 하지 마세요(홈을 멎게 한 전례) — 한 번 재서 몇 번만 되풀이합니다. ⚠️ **문자열 하니스·타입체크는 이걸 못 봅니다** — 함수도 값도 다 «있고» 틀린 것은 «몇 px 이고 무엇을 덮는가» 뿐입니다. ✅ 그리고 **되돌려 보면 실제로 FAIL 나는지** 확인하세요 (변이시험 3종으로 15·2·3건이 났습니다). 감시는 `test-harness/manual/game-standalone-exit-browser.mjs`(브라우저 45종 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 녹화를 재생하는데 **빨리감기·탐색이 안 되고 총 길이가 `--:--`** | 🔴 **파일이 «생방송용 WebM» 입니다 — 고장이 아니라 형식입니다.** 녹화는 `mango-rec.js` 가 `video/webm;codecs=vp8,opus` 로 찍고 MediaRecorder 조각을 **손대지 않고 그대로** R2 에 올립니다(`recordings-r2.ts`). MediaRecorder 의 WebM 은 «언제 끝날지 모르는 스트림» 이라 파일 안에 **총 길이(Duration)도 탐색 색인(Cues)도 없습니다** — 저장소 전체에 EBML 헤더 보정 코드가 **0곳**입니다(2026-09-01 실측). ⚠️ 그래서 막대를 끌어도 그 지점으로 못 가고, 플레이어에 따라 **195MB 를 처음부터 다 받아야** 겨우 움직여 «느리다» 로 느껴집니다. ⚠️ **관리자 목록의 「21:06」을 근거로 «파일에 길이가 있다» 고 읽지 마세요** — 그건 D1 `recordings.duration_ms` 이지 파일 안 값이 아닙니다. 판별은 휴대폰·플레이어가 총 길이를 `--:--` 로 보여 주는가입니다. ⛔ 이걸 «재생 화면 버그» 로 읽고 플레이어를 고치러 가지 마세요. ℹ️ 근본 수리는 **녹화 형식(mp4)** 이나 **변환 서비스**가 걸린 별건이고 사람이 정할 일입니다 — ⚠️ 그때 **이 컨테이너에서 mp4 지원 여부를 재지 마세요**: 여기 크로미움은 H.264 가 빠져 있어 `MediaRecorder.isTypeSupported('video/mp4;codecs=avc1…')` 가 거짓으로 나옵니다(2장 별도 함정). 강사 실제 브라우저에서 재야 합니다. ⚠️ 그리고 mp4 로 바꿔도 **fragmented mp4 라 총 길이가 없을 «수» 있습니다** — 실제 파일 하나로 확인하기 전에는 «이러면 풀린다» 고 적지 마세요 |
| 「나가기」를 눌렀는데 **평가도 복습퀴즈도 안 뜨고 그냥 나가짐** | 🔴 **화면에 보이는 「나가기」와 종료 방아쇠가 찾는 버튼이 «다른 버튼» 입니다.** `index.html` 의 수업 종료 흐름(평가 ⭐ → 복습퀴즈 🧠)은 `endOfClassFlow()`(4309행) 하나가 소유하고 방아쇠가 **둘뿐**입니다 — ⓐ 나가기 버튼 클릭(4325행 `isExitBtnEl`) ⓑ 강사 퇴장 후 **10초**(4408행). 그런데 ⓐ가 찾는 것은 `a#vc-exit-btn-v34, a#vc-exit-btn-v33, #vc-exit-btn-v32, button.ctrl-btn.danger, a.ctrl-btn.danger` 인데, 화면에 실제로 보이는 빨간 「나가기」는 **하단 독이 그리는 `button#vc-dock-leave.leave`**(`js/vc-dock.js` `mk('leave',…)`)라 **한 셀렉터에도 안 걸렸습니다.** 게다가 같은 파일 106행이 옛 버튼(`#vc-exit-btn-v34`)을 `display:none !important` 로 **숨깁니다.** ⟹ 독이 켜진 화면에서 나가기를 누르면 곧바로 `vcLeaveRoom()` 이 돌아 **평가·복습퀴즈가 통째로 건너뛰어졌습니다.** ⚠️ **에러가 안 납니다** — 버튼은 정상 동작하고 단계만 사라집니다. ⚠️ 그래서 **사실상 ⓑ로만 뜨고 있었습니다** — 2026-09-02 D1 실측: `class_ratings` 에 평가가 남은 수업은 전부 강사→학생 퇴장 간격이 10초를 넘었고(12초·3분36초·4분20초·5분19초), 학생이 먼저 나간 `class-849-20260902`(**3초**)만 한 줄도 없었습니다. 사장님은 그날 「중국어 복습퀴즈도 설문조사도 안 나온다」고 제보하셨는데 **둘은 별개 원인이 아니라 흐름이 평가 → 퀴즈 «직렬» 이라 앞에서 끊긴 하나**였습니다. ✅ 고침은 **독 버튼에 그 셀렉터가 알아보는 표식(`ctrl-btn danger`)을 함께 다는 것**입니다 — 판정·순서·중복방지는 index.html 의 기존 코드 그대로 둡니다(`index.html` 은 공동 금지구역이라 손대지 않습니다). ⛔ **표식을 달면 «색» 이 바뀝니다** — `css/vc-refresh.css:29` 의 `body.vc-in-call .ctrl-btn.danger{…!important}` 가 이겨 연빨강(.18)이 진빨강(.96)+흰글자가 됩니다(브라우저 실측). 독 CSS 에서 `!important` 로 되돌려 두었으니 그 줄을 지우지 마세요. ✅ **크기는 안 바뀝니다** — `#vc-dock button`(1,0,1)이 `.ctrl-btn`(0,1,0)을 이깁니다(실측 PC 70×62 · 폰 38×44 동일). 🔴 **터치는 따로 한 줄이 더 필요했습니다 — 안 하면 «폰에서만» 그대로 깨집니다.** 학생 방아쇠 배열은 `['pointerdown','touchstart','mousedown','click']` 라 **`touchend` 를 안 잡는데**, v29(14907)·v34(15161)는 `touchend` 를 캡처하고 v29 는 **`title` 의 「나가기」로도** 매칭합니다(독 버튼 title 이 「수업에서 나가기」). v35 에는 touchend 가 있지만 학생이면 양보하므로 **학생의 touchend 를 아무도 안 삼킵니다** → 헤드리스 터치 실측: `touchstart` 에서 평가 모달이 떴다가 `touchend` 에서 `/?_e=…` 로 **홈으로 튕겨 지워집니다.** ⟹ **학생 29,000명 대부분이 폰이므로 지금 상태로는 목적이 절반만 달성됩니다.** ✅ **2026-09-02 사장님 승인으로 그 배열에 `'touchend'` 를 더했습니다**(`index.html:4330`) — 헤드리스 터치 실측으로 touchend 뒤에도 평가 모달이 남는 것을 확인했습니다. ⛔ 그 낱말을 빼면 폰 학생이 다시 통째로 빠집니다(`class_end_flow_harness` ③절이 «옛 패치가 잡는 이벤트를 학생 방아쇠가 전부 덮는가» 로 대조해 «사람 결정 대기» 를 출력합니다). ✅ **강사도 안전합니다** — 학생은 평가 방아쇠(4325)가, 강사·관찰자·관리자는 **v35(4601)** 가 먼저 삼켜 `__mangoLeaveClassSPA()` → `vcLeaveRoom()` 으로 갑니다. 셋 다 v29(14907)·v32(15050)·v34(15161)의 `location.replace` 보다 **먼저 등록**돼 있어 홈으로 안 튕깁니다(캡처 리스너는 «등록 순서» 가 곧 실행 순서입니다). ⚠️ **문자열 하니스·타입체크로는 절대 안 보입니다** — 함수도 값도 다 «있고» 틀린 것은 «셀렉터가 서로 안 맞는다» 하나뿐이라 수리 전에도 `--fast` 260건이 전부 초록이었습니다. ✅ 감시는 `test-harness/class_end_flow_harness.mjs`(index.html 의 셀렉터를 **읽어 파싱해** 독 버튼이 실제로 매치되는지 대조 — 어느 쪽을 바꿔도 어긋나면 FAIL. 변이 4종 전부 실제 FAIL)와 `test-harness/manual/class-end-flow-browser.mjs`(브라우저 18종 — **자동으로 안 돕니다, 사람이 부릅니다**) |
| 「**나가기가 되지 않아**」 — 눌러도 아무 일도 안 일어남(모달도 없고 화면 그대로) | 🔴 **종료 흐름의 «재진입 방지» 가드가 한 번 굳으면 나가기가 «영구히» 삼켜집니다 — 그 값을 되돌리는 코드가 저장소 어디에도 없습니다.** `index.html:4341` 의 학생 방아쇠는 `STATE.triggered` 가 참이면 `preventDefault` + `stopPropagation` + `stopImmediatePropagation` 으로 나가기 이벤트를 **통째로 먹습니다**(캡처 단계라 뒤의 v35·독 `onclick` 까지 전부 못 옵니다). 🔬 **브라우저 실측(2026-09-15)**: 나가기 → 평가 ⭐ 제출 → 복습퀴즈 「예」 → **`goReviewQuiz()` 가 복습퀴즈 «탭» 으로 보내고 수업 화면에 그대로 남습니다**(방을 안 나갑니다) → 다시 나가기 → **무반응**(`vc-in-call` 이 계속 true). 몇 번을 눌러도 같습니다. 같은 모양이 하나 더 있습니다 — 평가를 이미 한 세션에서 `vcReviewQuizPrompt` 가 모달도 안 띄우고 `return true`(`STATE.done`)를 주면 `fallbackExit` 이 안 불려 **조용히 아무 일도 안 일어납니다**(사장님 사진: 참여자 1명 · 20:31 · 모달 없음). ⚠️ **에러가 안 납니다** — 화면은 정상이고 버튼도 멀쩡해 보입니다. ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «그 클릭이 어디까지 가는가» 뿐입니다. ✅ 고침은 **defer 파일에서 «삼켜졌는지» 를 재는 안전망**(`js/vc-dock.js`) — 그 가드는 IIFE 지역 변수라 밖에서 못 풀고 `index.html` 은 **공동 금지구역이자 첫 화면 예산이 빠듯합니다**(⛔ 그 바이트를 여기 베껴 적지 마세요 — 낡으면 다음 사람이 그 값을 믿습니다. 정본은 「첫 화면 예산이 FAIL」 줄). **나가기에서 «손을 뗀 것»(`pointerup`)은 그 방아쇠가 «안 잡는» 이벤트라 버튼까지 정상 전파됩니다**(그쪽은 `pointerdown`·`touchstart`·`touchend`·`mousedown`·`click` 만 잡습니다). 700ms 뒤에도 수업 화면 그대로고 모달이 하나도 없으면 직접 종료합니다. ⛔ **평가·복습퀴즈를 건너뛰게 만들지 마세요** — 그 모달이 **하나라도** 떠 있으면 손을 뗍니다(둘 다 클릭과 «같은 틱» 에 동기로 붙습니다. 실측: 첫 나가기 600ms 뒤 평가 모달 존재 true). ⛔ **`ctrl-btn danger` 표식을 빼서 풀지 마세요** — 그 순간 평가·복습퀴즈가 통째로 사라집니다(바로 윗줄 2026-09-02 수리를 되돌리는 것). 🔴 **그런데 `pointerup` 을 그냥 달면 «쓸어넘기기» 가 나가기가 됩니다 — 고치려던 것보다 나쁜 사고입니다.** `pointerup` 은 «그 요소에서 시작하지 않은» 포인터에도 옵니다. 교재를 쓸어넘기다 손가락이 독까지 내려와 나가기 «위에서» 떨어지면 그것도 pointerup 이고, 브라우저는 그때 `click` 을 **안** 내므로 **예전에는 아무 일도 안 일어났습니다.** ✅ 가르는 법: **창 «버블» 리스너에는 «나가기 버튼에서 시작한 pointerdown» 이 안 옵니다**(바로 그 방아쇠가 캡처에서 삼키니까 — 이 안전망이 필요한 이유와 같은 사실입니다). ⟹ **그 `pointerup` 의 짝이 되는 down 을 «우리가 봤고» 그것이 나가기가 아니었으면**(같은 `pointerId`) 쓸어넘기기로 보고 손을 뗍니다. 🔴 **그 판정을 «시간 창» 으로 쓰지 마세요 — 제가 처음에 «2초 안에 딴 데서 시작했으면 스와이프» 로 짰는데, «2초보다 느리게 끄는 드래그» 가 그대로 빠져나가 수업이 끊깁니다**(함정 대조가 실제로 잡았고, 되돌려 보면 브라우저 검사 ③-9 가 FAIL 합니다). ⚠️ **한 번 쓴 기록은 반드시 지우세요**(`pointerup`·`pointercancel`) — 마우스는 `pointerId` 가 늘 같아서, 안 지우면 예전 기록이 남아 «나가기에서 시작한 것» 까지 스와이프로 오판해 안전망이 통째로 죽습니다. ⛔ **`mouseup` 은 받지 마세요** — 터치 뒤 브라우저가 «흉내내서» 보내는 mouse 이벤트는 «놓은 자리»(= 나가기 버튼)에서 나는데 그 짝이 되는 `mousedown` 은 캡처에서 삼켜져 **쓸어넘긴 것을 나가기로 오해**합니다(Pointer 이벤트가 없는 아주 옛 브라우저에서는 안전망이 없을 뿐 = «고치기 전» 과 같습니다 — 그쪽으로 실패하는 것이 맞는 방향). ℹ️ 터치는 `pointerup` 이 «시작한 요소» 로 갑니다(암묵 포인터 캡처) — 교재에서 시작한 스와이프는 이 버튼 리스너가 **애초에 안 불립니다**. 위 판정은 마우스 쪽 방어입니다. ⚠️ 이 가드를 빼면 브라우저 검사 ③-7 이 **실제로 FAIL** 합니다(변이 실측) — ⛔ 지우지 마세요. ⚠️ 검사는 「나간다」 옆에 **「엉뚱하게는 안 나간다」를 짝으로** 두세요(딴 좌표에서 `mousePressed` → 버튼 위에서 `mouseReleased`). ⚠️ **키보드 Enter 로 누르면 `pointerup` 이 없어 여기 안 걸립니다** — 그 경로는 여전히 삼켜집니다(알고 둔 한계). ⚠️ 이 안전망이 도는 것 자체가 «가드가 굳었다» 는 뜻이라 `console.warn` 을 남깁니다 — 조용히 고치지 않습니다. 🟡 **근본 수리**(흐름이 끝나면 가드를 푸는 것)는 `index.html` 수정이라 **사람이 정할 일**입니다. 🪤 그 브라우저 검사를 만들 때 **앞 회차의 `sessionStorage`(«이미 평가함» 표시)가 남아 복습퀴즈 모달이 미리 떠 있었고, 그 덮개가 클릭을 가로채 검사가 통째로 헛돌았습니다**(버튼에 이벤트 0건). 회차마다 `sessionStorage.clear()` 하고 **「시작 시점에 모달이 없다」를 전제 검사로** 두세요. ⚠️ 그리고 **`.click()` 은 `pointerup` 을 안 만듭니다** — 실제 좌표로(`Input.dispatchMouseEvent`) 눌러야 이 안전망이 검사됩니다. 감시는 `test-harness/manual/class-end-flow-browser.mjs` **③절**(브라우저 **10종** — 자동으로 안 돕니다, 사람이 부릅니다. 「나간다」·「쓸어넘기기로는 안 나간다」·**「느린 드래그로도 안 나간다」**·「그러고도 진짜 나가기는 된다」를 짝으로. 리스너를 지우거나 옛 시간 창으로 되돌리면 실제로 FAIL) |
| **로컬 서버로 화면을 고치고 다시 쟀는데 «고치기 전» 값이 나옴**(변이시험이 통과해 버림) | 🔴 **서비스워커(`public/sw.js`)가 자산을 cache-first 로 줍니다 — `Network.setCacheDisabled` 로는 안 꺼집니다.** 그것은 HTTP 캐시만 끄고 SW 는 별개입니다. 2026-09-02 실측: 나가기 버튼 고침을 되돌려도 브라우저는 계속 새 사본으로 동작해 **변이시험이 «통과»** 했습니다 — 즉 「고쳐졌다」를 확인한 그 검사가 실은 아무것도 안 보고 있었습니다. ⚠️ **이 방향의 거짓말이 제일 위험합니다** — 「안 고쳐졌다」는 다시 보게 되지만 「고쳐졌다」는 아무도 다시 안 봅니다. ✅ CDP `Network.setBypassServiceWorker {bypass:true}` 를 **함께** 켜세요. ✅ 그리고 **되돌려 보면 실제로 FAIL 이 나는지** 반드시 확인하세요 — 그것이 검사가 헛도는지 아는 유일한 신호입니다. ℹ️ 포트를 바꾸는 방법(새 오리진)도 되지만 매번 바꿔야 해서 잊기 쉽습니다. 📜 같은 줄기: 이 표의 「하니스가 자기가 새로 만든 상수를 잡아 통과」·「가짜 DB 로 하니스를 돌렸는데 검사가 헛돌며 통과」 |
| 수업 중에 배포가 나가 **여러 방이 동시에 끊김** (「인터넷 오류가 심해 대화가 안 됐어요」) | 🔴 **배포하면 화상수업 Durable Object(VideoCallRoom)가 재시작되어 진행 중인 «모든» 수업의 WebSocket 이 끊깁니다.** 2026-09-01 실측: 19:33~22:06 에만 운영 워커가 **22번** 재배포됐고, 21:43:57 배포 **8초 뒤** `class-1070` 강사, **37초 뒤** `class-1078` 강사(Krystel — 김선우 학생 수업)가 동시에 끊겼습니다. `class-996` 은 Teacher Kaye 가 10분 사이 **네 번** 끊겨 녹화가 4:03 / 3:18 / 0:18 로 토막났습니다. ⚠️ **에러가 안 납니다** — 수업은 계속 «되는» 것처럼 보이고 사람에게는 「인터넷이 나쁘다」로만 읽힙니다. ⚠️ 그리고 DO 가 죽으면 `user-left` 를 방송할 상대가 이미 없어 **상대 화면에 옛 타일이 «유령 참가자» 로 남습니다** (유령 청소는 «살아 있는 옛 소켓» 을 `state.getWebSockets()` 에서 찾아 닫는 방식이라, DO 자체가 재시작되면 찾을 소켓이 없습니다). 그날 녹화 참가자 목록에 1:1 수업인데 `class-1078` **3명**, `class-1086` **5명**, `class-988`·`996` 3명이 잡혔고, 유령 타일은 고착 워치독이 진짜 사람으로 오판해 강제 재연결을 겁니다(이 표 「왜 3명이 나와?」). ⚠️ 이 «배포 → DO 재시작 → 유령» 사슬은 **코드를 읽고 세운 추론**입니다 — 배포 시각과 끊긴 시각이 30초 안팎으로 겹친 것은 실측이지만 로그로 직접 잇지는 못했습니다(완료형으로 적지 말 것). ✅ 2026-09-01 부터 **배포 게이트가 수업 시간대에는 배포를 보류**합니다 — 창은 **화·목 12:00~16:00 KST**(2026-09-16 사장님 지시 「스케줄이 바뀌어서 이제는 오후 12시부터 16시까지 막아줘」 — 요일은 그대로 두고 시각만 옮겼습니다) — 판정 정본은 `.github/scripts/class-window.mjs` **하나**이고 `deploy.yml` 은 그것을 부르기만 합니다(⛔ 판정을 워크플로에 다시 적지 마세요). 보류돼도 실행은 초록불이고 게이트(tsc·회귀 하니스·?v=)는 그대로 돌며, 보류된 커밋은 **몰아 배포**(`deploy.yml` 의 `schedule`, 창 밖에 여러 번)가 내보냅니다. ⛔ 횟수를 여기 적지 마세요 — 2026-09-11 에 「네 번」이, 2026-09-16 에 「여섯 번」이 한꺼번에 거짓이 됐습니다. ⛔ **그 시각을 안내 문구에 베껴 적지 마세요** — 2026-09-01 에 크론만 옮기고 주석이 남아 「언제 나가는지」를 거짓으로 말했습니다(#721). 정본은 `schedule` 목록 하나입니다. 🔴 **몰아 배포는 «제 시각에» 오지 않습니다 — 실측 지연이 «가장 짧게 잡아 +56분, 길게는 +286분» 입니다.** 관측된 회차(2026-09-01~02, 3건): 예정 16:30Z → 19:27:38Z **+177분**(run #2926) · 17:05Z → 19:38:57Z **+153분**(#2977) · 20:47Z → 22:50:46Z **+124분**(#2978). **누락된 것을 아직 못 봤고(예정 발화를 전수로 세지는 않았습니다) 매번 한 시간 이상 늦게 옵니다.** ⛔ **예정 시각에서 30분~1시간 안 왔다고 「안 나갔다」로 판단하지 마세요** — 2026-09-02 에 제가 그렇게 두 번 잘못 보고했습니다. **3시간은 기다려 보고 말하세요.** ⚠️ 표본이 6개뿐이라 「항상 이만큼」이 아니라 「지금까지 6번이 그랬다」입니다(아래 재실측 포함). ⚠️ 그래도 GitHub 문서상 **아예 누락**될 수 있습니다 — 하나에만 기대면 「나갔어야 할 게 조용히 안 나가는」 상태가 그대로 재현됩니다. 🔴 **그 지연이 «마지막 기회» 회차를 무력화합니다** — 📜 옛 창(14:00~23:00) 시절 실측: 12:35 예정에 위 지연을 더하면 **14:39~17:21** 이라 화·목이면 창이 열린 뒤라 **스스로 보류됐습니다** — 2026-09-10 에 실제로 **17:20 KST 에 도착해 보류**됐습니다. 즉 창이 있는 날에 그 회차가 헛돕니다. ⛔ 「크론이 고장났다」로 읽지 마세요(보류는 설계대로입니다). ✅ 고치려면 그 회차를 더 앞으로 옮기는 쪽인데 창 밖 수업(월 13:00~13:20)과 다시 가까워집니다 — **사람이 정할 일**로 남겼습니다(2026-09-03 미결). 그래서 창 밖에 **여섯 번** 흩어 두고(⛔ 시각은 여기 적지 않습니다 — 정본은 `deploy.yml` 의 `schedule` 목록 하나), 하니스가 «3개 이상 · 서로 90분 이상 · 창 열리기 30분 전 마지막 기회» 를 못 박습니다. 🔴 **«창 밖» 이 «수업이 없다» 는 뜻이 아닙니다** — 창은 화·목만 막으므로 크론 시각은 창 밖에 실재하는 수업·접속과도 떨어져 있어야 합니다. **2026-09-02 에 이것을 하루에 두 번 밟았습니다**: ① 마지막 크론 **13:23** 이 **월 13:00~13:20 수업**(장지웅 `adm-enroll:85`)이 끝난 **3분 뒤** ② 첫 크론 **23:17** 이 **야간 접속 23:20** 이 시작되기 **3분 전**. ⚠️ ②는 **`deploy.yml` 주석이 그 시간대를 이미 적어 두고 있었는데 제가 만든 검사 표가 그것을 빠뜨린 것**입니다 — 「새 수업이 생기면 손으로 더해야 한다」는 주석이 그 «이미 아는 누락» 을 덮고 있었습니다. ✅ 지금은 하니스가 «창 밖 수업 표»(`OUT_WINDOW_CLASSES` — 예약표 2건 + 야간 실접속 2건)와 **앞뒤 15분**을 검사하고, **표가 비면 FAIL**(비우면 검사가 통째로 사라지는데 초록이었습니다) · **자정을 넘는 구간은 두 줄로** 쪼개게 강제합니다(한 줄로 넣으면 13분 전인 크론도 통과했습니다). ⚠️ 그래도 그 표는 D1 을 읽지 않고 **그때 잰 것**이라 완전하지 않습니다. 🔴 **「N분 전에 끝난다」로 안심하지 마세요** — 같은 줄에 적힌 **+286분까지 가는 지연**이 그것을 무효로 만듭니다. 📜 옛 12:35 회차는 «지연되면 13:00 수업 «안»으로 들어오는» 값이라, 위험을 없앤 것이 아니라 **종류를 맞바꾼 것**이었습니다(월요일은 창이 안 막습니다). ⚠️ 이 교훈은 2026-09-16 에 그대로 다시 쓰였습니다 — 아래 덧붙임의 「창이 닫힌 직후」 참고. 🔴 **수·금·월에도 수업이 있지만 이 게이트는 안 막습니다** — 실측(2026-09-02, 앞으로 잡힌 수업)은 목 45 · **수 40**(전부 `c24-mirror`) · 화 30 · 금 7 · 월 5 건이고 토·일만 0건입니다. 「다른 날은 수업이 없다」가 아니라 **「그날은 «창» 으로 막지 않는다」가 결정**입니다(⚠️ 아래 «실접속 판정» 은 CI 토큰에 D1 읽기 권한이 없어 매번 `http-403` 으로 통과하므로 **지금은 그 구멍을 메우지 않습니다** — 깨우는 것은 사람이 정할 일). 📜 **요일 결정 경위(2026-09-02 하루 동안 세 번)**: 1차 「화·목만 14~23시」 → 배포 한 번이 수 17:35 에 나가자 「수요일도 넣어줘」(#761, 화·수·목) → 같은 날 저녁 사장님 「**수요일은 배포 보류 안 해도 돼 — 화요일과 목요일만 14시부터 23시까지**」로 되돌림(#761 revert). 정본은 `class-window.mjs` 의 `days:[2,4]` 두 줄이고, 이 표·`deploy.yml` 주석·하니스 두 개가 같은 말을 해야 합니다(한쪽만 고치면 «수요일에 보류되는지» 를 아무도 모릅니다). ⛔ 이 줄을 «수업이 없다» 로 바꿔 적지 마세요 — 수요일에 수업이 끊긴 이유를 아무도 못 찾게 됩니다. 급하면 `force_now` 수동 실행이나 커밋 메시지의 `[deploy-now]` 로 우회합니다. 🔴 **창의 «끝» 을 예약표(`class_schedules` 최대 22:40)로 잡으면 틀립니다** — 접속을 분 단위로 세면 23:30–23:50 · **00:00–00:20(22건)** · 00:40–01:10 수업이 실재합니다. 처음에 23:30 을 끝으로 잡았다가 몰아 배포(23:35)가 **수업 한복판**에 떨어질 뻔했습니다(고치려던 사고를 그대로 재현할 뻔함). ⛔ **몰아 배포 크론은 반드시 그 창 «밖» 이어야 합니다** — 창 안이면 보류를 풀러 오는 그 실행마저 스스로 보류해 **배포가 영영 안 나갑니다.** ⚠️ 창을 자정 넘게 넓히면 `start <= m < end` 로는 자정 뒤가 통째로 빠집니다(되감기 코드는 남겨 뒀습니다 — 2026-09-01 에 실제로 밟아 00:00~00:20 수업 22건을 놓칠 뻔했습니다). ⚠️ 요일은 **KST 기준**으로 세세요(UTC 로 재면 하루가 밀립니다). ✅ **로컬 `deploy.ps1` 도 같은 판정을 «불러서» 씁니다**(`[0c]` 게이트 — `--exit-on-hold` 로 부르고 종료코드 **2 = 보류**면 파일 하나 안 건드리고 중단. 우회는 **`-ForceNow`**). ⛔ **`-SkipSmoke` 로는 안 꺼집니다** — 막는 것이 「느린 배포」가 아니라 「지금 수업 중인 학생·강사의 연결」이라서입니다(`0b` 되감김 게이트와 같은 판단). ⛔ 판정을 PowerShell 로 복제하지 마세요 — 정본은 `class-window.mjs` 하나이고, ps1 이 스스로 시각을 재면 하니스가 FAIL 냅니다. ⚠️ 종료코드 계약이 갈립니다 — **플래그가 없으면(=CI) 보류여도 항상 0** 입니다. 거기서 죽으면 뒤의 배포 게이트가 통째로 안 돕니다. 감시는 `test-harness/deploy_class_window_harness.mjs`(110종 — 판정 모듈을 **실제로 돌리고**, 종료코드는 **node 를 띄워서** 확인하며, 크론 시각을 그 판정에 넣어 자기잠금을 막습니다. 요일 검사를 빼면 5건, 크론을 창 안으로 옮기면 2건, 한 시간대로 몰면 1건, 하나로 줄이면 1건, 안내 문구에 시각을 베끼면 1건, ps1 의 게이트를 빼면 2건이 실제로 FAIL 납니다) 🔴 **덧붙임(2026-09-02·2026-09-16 갱신) — 지금 실제로 막히는 것은 «화·목 12:00~16:00» 하나뿐입니다.** 같은 파일에 «실접속 판정»(reason `live-class` — 요일·시각과 무관하게 지금 방에 사람이 있으면 보류)이 들어 있지만 **CI 토큰에 D1 읽기 권한이 없어 매번 `http-403` 으로 통과합니다**(실측: 배포 run 33597528051 로그의 「⚠️ 실접속 확인 실패(http-403)」). ⛔ 이것을 «수·금·월도 보호된다» 로 바꿔 적지 마세요 — 지금은 사실이 아닙니다. ⚠️ **그것을 깨우는 것은 사람이 정할 일입니다** — 2026-09-02 사장님 지시가 「배포 멈추는 것은 화·목 14~23시만(그때가 테스트 수업이라 수업의 질을 위해)」이라, 깨우면 수·금·월에도 막히기 시작해 그 지시를 벗어납니다. ⛔ 「고장났으니 고쳐야지」 하고 D1 권한을 추가하지 마세요. ✅ 정책을 바꾸기로 했을 때만: Cloudflare API 토큰에 `Account · D1 · Read` 를 더하면 다음 배포부터 그 403 줄이 사라집니다. 판정 근거는 **`attendance.last_seen_at`** — 화상방 브라우저가 30초마다 보내는 `/api/speaking-time` 이 **서버에 도착한 시각**이라 위조도 과다계상도 안 됩니다(최근 3분 + `left_at` 이 비어 있을 때). ⛔ **카페24 예약(`class_schedules`)이나 카페24가 밀어넣은 `attendance` 씨앗으로 판정하지 마세요** — 그건 «오늘 수업이 잡혀 있다» 이지 «지금 사람이 들어와 있다» 가 아니고, 그 수업은 우리 DO 를 안 거쳐 **배포해도 안 끊깁니다.** 🔢 30일 실측: 전체 4,666행 중 **3,747행이 씨앗**이라, 씨앗을 함께 세면 13시 46·23시 51·0시 34 로 보이지만 실접속은 **10·4·13** 건뿐입니다(옛 «01:20 까지» 창이 그래서 넓었습니다). ⛔ 창을 다시 넓히려고 접속을 셀 때는 반드시 `WHERE last_seen_at > 0` 을 거세요. ⚠️ 그 조회가 실패하면 **막지 않습니다**(「수업 없음」이 아니라 「모름」 — 고장 난 감시견이 모든 배포를 영구히 막는 쪽이 더 나쁩니다). ⛔ 그 통과를 조용히 하지는 않습니다. ⚠️ 못 막는 것은 «곧 시작될 예정인데 아직 아무도 안 들어온» 구간뿐입니다(배포에 2분 남짓). ⛔ **그 둘은 짝입니다** — 실접속 판정을 끄면 수·금·월이 «창도 없고 실접속 판정도 없는» 상태가 됩니다. 📚 **근거는 Cloudflare 문서입니다** — 「Code updates disconnect all WebSockets. Deploying a new version restarts every Durable Object, which disconnects any existing connections.」([Durable Object Lifecycle](https://developers.cloudflare.com/durable-objects/concepts/durable-object-lifecycle/) · 2026-09-01 확인). ⚠️ **「무중단 재연결」(`VC_STICKY_UID='on'`)이 이 경우엔 «안» 걸립니다** — 그 장치는 «같은 clientId 의 옛 소켓이 서버에 아직 살아 있을 때» userId 를 물려받는 것인데(`src/video-call-room.ts` sticky 절), DO 가 통째로 재시작되면 옛 소켓이 하나도 없습니다 → 새 userId → 화면이 `room-joined` 에서 «정체성이 바뀌었다» 로 판정해 `vcCleanupAllPeers()` 로 PeerConnection 을 전부 버리고 다시 협상합니다. ⚠️ 끊기는 «시간» 은 재보지 않았습니다. ✅ 살아남는 것: 교재 공유(`pdfState`)·동영상 공유(`videoState`)·잠금 4종 — `state.storage` 에 있어 재입장 때 다시 내려옵니다. ⛔ 사라지는 것: 칠판 판서·교재 필기 **재전송 버퍼**(`wbOps`·`pdfAnnoOps`, 메모리) — 이미 그린 사람 화면엔 남지만 **그 뒤 새로고침하거나 늦게 들어오는 사람에게는 판서가 안 갑니다**. ℹ️ 학생 화면에 「수업이 끝났어요」는 **안 뜹니다** — `vcCleanupAllPeers` 는 타일을 직접 지우지 `vcRemovePeer(id,'left')` 를 부르지 않습니다(1-4 「`left` 만 종료」). 감시는 `test-harness/deploy_class_guard_harness.mjs`(38종 — 판정 SQL 을 **진짜 SQLite 에 돌려** «카페24 씨앗은 안 세고 실접속만 센다» 를 확인하고, D1 조회를 **가짜 fetch 로 실제로 실행**해 403·타임아웃·이상한 응답에 «막지 않는지» 를 봅니다). 📌 **덧붙임(2026-09-11) — 「수업 끝나고 바로 배포되게 해줘」는 이 schedule 로는 «구조적으로» 못 합니다.** 재 보니 막는 것이 둘입니다 — ① **[2026-09-16 에 이 제약이 풀렸습니다]** 옛 창이 끝나는 23:00 직후에는 크론을 놓을 자리가 없었습니다 — 22:40~23:00 수업과 야간 실접속 23:20~ 사이가 **20분**뿐인데 `CLASS_MARGIN_MIN` 이 앞뒤 15분씩(=30분)을 요구해 교집합이 비었습니다 — 변이로 확인: 23:10 크론은 **2건**, 23:05 는 **1건** FAIL 이고, 규칙이 허용하는 가장 이른 시각은 **01:35**(경계를 1분씩 훑어 확인 — 01:34 FAIL · 01:35 PASS. 그것도 90분 간격 규칙에 걸려 02:05 를 «옮겨야» 놓입니다). ② **지연이 «가장 짧게 잡아도» +56분**입니다(아래 재실측). ⟹ **«바로» 를 원하면 사람이 눌러야 합니다** — 창이 닫힌 뒤(지금은 16:00)는 이미 창 밖이라 Actions → Run workflow 를 그냥 누르면 되고 `force_now` 도 필요 없습니다(로컬 `deploy.ps1` 도 같습니다). ⛔ **클로드는 그 버튼을 못 누릅니다** — `workflow_dispatch` 가 403 `Resource not accessible by integration`(2026-09-11 재확인). ⚠️ **보류된 run 을 «재실행»(rerun)해 우회하는 길도 같은 403 입니다** — 2026-09-22 에 창이 닫힌 직후(16:12 KST) 성공한 run 의 rerun 을 시도해 실측했습니다. 즉 클로드가 배포를 «지금» 내보내는 길은 **main 에 push 되는 것**(= PR 병합) 하나뿐입니다. ⛔ 그러니 「23시에 자동으로 나가게 해 뒀습니다」라고 적지 마세요. 📊 **지연 재실측(2026-09-10)** — ⚠️ **도착 시각은 «잰 것» 이지만 «어느 회차인가» 는 «추론» 입니다**(API 응답에 그 값이 없어, 그날 도착이 가능했던 크론 셋 00:11Z·03:35Z·17:05Z 과 회차 셋을 «순서대로» 맞춘 것입니다 — ⛔ 완료형으로 읽지 마세요). 19:28:35Z 도착(#3153) → 17:05Z 면 **+144분**(앞 크론으로 보면 +953분이라 성립 안 함 — 이 짝은 사실상 확실) · 08:20:37Z(#3152) → **+285분** 또는 +489분 · 04:31:17Z(#3148) → **+56분** 또는 +260분. ⟹ **어느 짝으로 읽어도 «가장 짧게 잡아 +56분»** 이고, 순서대로 맞추면 +260·+285·+144 라 위 「124~177분」과 크게 어긋나지 않습니다(표본 6개). 🔴 그날 한 회차가 **17:20 KST(목요일 = 창 «안»)에 도착해 스스로 보류**됐고(이것은 짝짓기와 무관하게 «잰 것» 입니다), 그 뒤 02:05 회차가 **04:28 KST** 에 나가기까지 15:07 에 병합한 것이 **13시간** 묶여 있었습니다(위 「마지막 기회 회차가 무력화된다」가 실제로 일어난 기록). ✅ 그래서 «당기는» 대신 **기회를 늘렸습니다**(네 번 → 여섯 번 — 새 시각도 창 밖·수업에서 15분 이상·서로 90분 이상입니다. ⛔ **시각·횟수를 여기 베끼지 마세요** — 정본은 `deploy.yml` 의 `schedule` 목록 하나이고, 2026-09-11 에 넷 → 여섯으로 늘리면서 여러 곳에 박아 둔 「네 번」이 한꺼번에 거짓이 됐습니다). ⚠️ 이걸로도 «바로» 는 안 됩니다 — 나쁜 회차의 손해를 줄일 뿐입니다. ⛔ **첫 회차를 더 앞으로 당기지 마세요** — 야간 실접속이 01:20 까지 있어 여유가 45분 → 15분으로 줄어드는데, 그 16분을 지켜 줄 «실접속 판정» 이 지금 403 으로 잠들어 있습니다. | 📌 **덧붙임(2026-09-16) — 창을 «화·목 12:00~16:00» 으로 옮겼습니다**(사장님 「스케줄이 바뀌어서」). 🔴 **보호 범위가 줄었습니다 — 늘어난 것이 아닙니다.** [잰 것 — 2026-09-16, 최근 30일 `attendance` 실접속(`last_seen_at > 0` · `c24-*` 제외) **770건**] 새 창이 막는 화·목 실접속은 **95건**이고 옛 창(14~23시)은 **358건**이었습니다 — 하필 지난 30일 피크가 **21시**(화·목 142건)라 그 시간대가 통째로 창 밖이 됩니다(화·목 17시 이후만 **300건**). ⚠️ 30일 롤링이라 합계는 날마다 바뀝니다 — 숫자를 인용할 때 «잰 날짜» 를 함께 적으세요. ⚠️ 그런데도 이 값이 맞는 이유는 **테스트 수업 시각이 옮겨졌기** 때문이고, 그 숫자는 «옮기기 전» 을 잰 것이라 앞으로를 예측하지 못합니다. ⛔ 그러니 「21시가 피크니 되돌리자」로 읽지 마세요 — 되돌리려면 **지금 테스트 수업이 실제로 몇 시인지** 를 다시 재고 사장님께 확인해야 합니다. 🔴 **창을 옮기면 몰아 배포 크론이 창 «안» 으로 들어올 수 있습니다** — 이번에 12:35 회차가 정확히 그렇게 되어(= 보류를 풀러 가는 실행이 스스로 보류 = 배포가 영영 안 나감) 창이 «닫힌 뒤» 로 옮겼습니다. ✅ 확인은 한 줄입니다 — 모든 크론을 `isClassWindow()` 에 넣어 돌려 보세요(하니스 ④가 같은 일을 합니다). ⛔ **«창이 닫힌 직후에 두면 지연에 강하다» 로 풀지 마세요 — 그렇게 했다가 되돌렸습니다.** 자기잠금에서 안전한 것과 «수업을 안 끊는다» 는 다른 말입니다. 16:20 에 두면 지연(+56~285분)이 착지시키는 곳이 **17~21시 = 실측 피크**이고, 게다가 16:16·16:27 에 실접속이 있어 앞뒤 15분 규칙도 4분·7분 차이로 어깁니다. ⟹ 옛 회차는 「지연되면 스스로 보류」(손해 = 배포가 늦음)인데 그 자리는 「지연되면 피크에 그대로 나감」(손해 = 수업이 끊김)이라 **위험이 나쁜 쪽으로 맞바뀝니다.** ✅ 그래서 그 여섯 번째 회차를 **없애고 다섯으로** 두었습니다(09:11 이 «창 열리기 전 마지막 기회» 를 그대로 맡습니다). ⚠️ 대가 — 창이 닫히는 16:00 부터 다음 기회(새벽)까지 **10시간 공백**입니다. ⚠️ 그리고 **새로 잰 접속을 `OUT_WINDOW_CLASSES` 에 넣지 않으면 하니스가 이 위반을 못 잡습니다** — 16시 항목이 표에 없어 16:20 이 초록이었습니다(2026-09-02 에 두 번 밟은 것과 같은 모양). ⚠️ **하니스에 박아 둔 «실사고 시각»(2026-09-01 화 21:38~21:46)은 이제 «보류되지 않는다» 로 뒤집어 두었습니다** — 지우지 마세요. 21시대에 수업이 다시 잡히는 날 그 줄이 가장 먼저 눈에 띄어야 합니다.
| 보관기간 정책을 늘렸는데 **옛 데이터는 옛 기준으로 지워짐** | 🔴 **`expires_at` 은 «만들 때» 계산해 박는 값이라, 나중에 정책을 바꿔도 소급되지 않습니다.** 녹화 보관기간을 2026-08-06 에 1개월 → 3개월로 늘렸는데, **8/7 이전 녹화 1,483건에는 여전히 30일 값이 박혀 있었습니다**(8/8 이후 615건만 90일). 그래서 2026-09-02 에 사장님 지시로 실제 파기를 켜려다 dryRun 에서 잡았습니다 — 그대로 켰으면 **6~7월 수업 녹화 1,313건 10.46GB 가 첫날 밤에 한꺼번에 사라졌습니다.** 3개월 규칙을 소급하면 그날 지울 것은 **0건**이었으니, 사장님 결정과 정확히 반대되는 결과였습니다. ⚠️ **에러도 경고도 없습니다** — 코드는 「만료된 것을 지운다」를 정직하게 수행할 뿐이고, 틀린 것은 «만료가 언제인가» 뿐입니다. ✅ 확인은 한 줄입니다 — `SELECT ROUND((expires_at-started_at)/86400000.0) AS 보관일수, COUNT(*) FROM recordings GROUP BY 1` (값이 두 종류로 갈리면 그 사이에 정책이 바뀐 것입니다). ✅ 소급이 필요하면 `UPDATE ... SET expires_at = started_at + <새 기간>` 으로 **먼저 고친 뒤에** 파기를 켜세요(순서를 뒤집으면 그날 밤에 지워집니다). ⛔ **되돌릴 수 없는 삭제를 켤 때는 반드시 dryRun 으로 「몇 건·몇 GB」를 먼저 세고 사람에게 보고**하세요 — `retention.ts` 주석이 「사장님 승인 + dryRun 선행」을 요구하는 이유입니다. 📌 파기 구현의 계약: **D1 표시가 먼저, R2 삭제가 나중**(반대면 「완료인데 영상 없음」 사고 재현) · 조회 조건에 `status` 를 넣지 말 것(삭제 실패분이 영영 재시도되지 않습니다) · **성공했을 때만 `file_url` 을 비울 것**(그 칸이 «아직 파일이 있다»는 유일한 표시이자 고아 청소기의 보호 목록입니다) · 한 번에 다 지우지 말 것(배치 상한). 📌 **덧붙임(같은 날) — 보관기간을 3 → 6개월로 늘렸습니다**(사장님 결정). **동의 화면 문구(`js/mango-consent.js`)를 먼저** 6개월로 바꾸고 기간을 올렸습니다(정본 `api-mango.ts` 의 `RETENTION_MS`). 🔴 **그렇다고 «6개월 안내를 본 사람만 180일» 이 되지는 않습니다** — `mangoConsentEnsure()` 는 동의 행이 있으면 그 자리에서 돌아가고 `CONSENT_VERSION` 을 **비교하지 않습니다**(저장만 하고 읽는 곳이 0곳). 그래서 3개월로 안내받은 학생의 «앞으로 찍힐» 녹화에도 180일이 붙습니다. 즉 「동의 범위 안」은 **이미 쌓인 데이터에 대해서만** 참입니다 — 그 틈을 닫으려면 학생 29,000명에게 동의를 다시 받아야 해서 **사람이 정할 문제**입니다(2026-09-02 미결). ⛔ 이 자리를 「순서를 지켰으니 동의 범위 안」으로 요약하지 마세요(제가 실제로 그렇게 적었다가 trap-check 가 잡았습니다). ⛔ **기존 2,098건은 소급하지 않습니다** — 「3개월」로 안내받고 동의한 것이라, 더 오래 갖고 있는 것이 곧 동의 범위를 넘는 일입니다. ⚠️ 그래서 한동안 **3개월짜리와 6개월짜리가 섞여 있습니다** → 관리자 화면 문구에서 «3개월」이라는 **숫자를 뺐습니다**(한 숫자로 말하면 어느 쪽이든 거짓이 됩니다). ⚠️ 6월분 423건은 3개월 기준이라 예정대로 9/3부터 순차 파기됩니다. 🪤 **`file_url` 은 R2 키만 담는 칸이 아닙니다** — `DEBUG:`+업로드 로그·`FATAL:`+예외·외부 녹화의 `https://…` 도 들어갑니다. **R2 `delete()` 는 없는 키에도 예외를 안 던지고 성공**하므로 그대로 넘기면 「지웠다」로 세고 그 칸을 비워 **저장 실패의 원자료가 영구히 사라집니다**(2026-08-28 에 「저장 실패 76건」을 살려 낸 그 화면의 원자료입니다). 조회를 **`file_url LIKE 'rec/%'`** 로 좁히세요. 🪤 그리고 **「남은 건수」를 `LIMIT+1` 로 재지 마세요** — 언제나 «1건 남음» 이 되고 dryRun 건수도 상한에서 잘려 「몇 건인지 먼저 세라」가 성립하지 않습니다(`COUNT(*)` 로 따로). 🔴 **파기를 켜면 그 실행 경로의 권한도 다시 봐야 합니다** — `/api/retention/run` 은 `/api/admin/` 접두사가 아니라서 스코프 차단·강사 차단이 **안 걸립니다**(전에는 되돌릴 수 있는 표시뿐이라 문제가 아니었습니다). `isTeacher`·`isOrgScopedRole` 두 가드를 따로 두세요(⛔ `canEditOrg()` 는 교사에 true 라 못 막습니다). 감시는 `test-harness/retention_purge_harness.mjs`(33건 — 가짜 D1·R2 로 **실제로 돌려** 순서·재시도·dryRun·키판정·게이트를 확인. ⚠️ 가짜 D1 이 `LIKE`·`LIMIT` 을 실제로 흉내내야 합니다 — 안 그러면 그 절이 헛돕니다. 변이 6종 전부 실제 FAIL) |
| 「녹화를 서버가 하게 만들자」 · 「SFU 로 옮기면 녹화가 튼튼해진다」 | 🔴 **Cloudflare 는 WebRTC 방송을 «녹화하지 않습니다».** 문서가 세 곳에서 명시합니다 — 「WebRTC broadcasts cannot currently be recorded, thus no storage is consumed」(Stream 요금) · 「**Recording and live HLS playback are not yet supported**」(Stream WebRTC 제한) · 「WebRTC is not currently eligible for recording」. 즉 **Stream 라이브 입력 + WHIP** 으로 브라우저가 올릴 수는 있어도 녹화는 안 됩니다(Stream 이 녹화하는 것은 RTMP/SRT 인제스트뿐인데 브라우저는 그 둘을 못 씁니다). **Realtime SFU 에도 녹화 기능이 없습니다** — WebSocket 어댑터가 PCM 오디오·JPEG 프레임을 흘려 줄 뿐이고 그걸 영상으로 굽는 것은 우리 몫인데 **Worker 에서는 불가능**합니다(Containers 는 별건). 합성 녹화가 실제로 있는 것은 **RealtimeKit**(가상 봇이 회의에 들어와 녹화해 R2 에 올림)이고, 그건 화상수업 스택 전체를 그 SDK 로 갈아 끼우는 일입니다. ⚠️ 2026-09-02 에 제가 제안서에 「SFU 로 옮기면 서버가 녹화한다」고 적었다가 구현 단계에서 문서를 읽고 정정했습니다 — **그럴듯해서 아무도 안 의심합니다.** ✅ 지금 녹화는 **브라우저가 굽습니다**(`js/mango-rec.js` — 캔버스 합성 + WebAudio 믹스 + `MediaRecorder` + R2 multipart). 그 전제를 바꾸려면 위 넷 중 하나를 골라야 하고 전부 «별건 프로젝트» 입니다. ✅ **요금은 걸림돌이 아닙니다**(SFU egress $0.05/GB · 월 1,000GB 무료 · 인그레스 무료 · **TURN 과 합산해 한 줄** 청구) — 막는 것은 기능이 없다는 사실입니다. 📄 근거·계산: `docs/작업기록/260902_SFU검토_A_B_C_참관정원_녹화유실_자격증명경계.md` · 켜는 절차는 `docs/ENVIRONMENT.md` 「Realtime SFU」 |
| 캘린더 «전체 강사» 보기에서 **같은 시각 카드가 한 자리에 쌓임**(맨 위 한 장만 보임) | 🔴 **배치 규칙이 «없는» 것입니다 — 카드가 전부 `left:3px;right:3px` 로 요일 칸 «전체 폭» 에 절대배치됩니다**(`js/adm-q6.js` `ph54EventCard` + `admin-inline-c.css .ph54-ev`). 강사 25명이 한 요일에 모이면 20:00 에 6명의 수업이 정확히 같은 자리에 쌓입니다(2026-09-02 사장님 캡처). ⚠️ **에러가 안 나고 하니스도 전부 초록**입니다 — 함수도 값도 다 «있고» 틀린 것은 «어디에 그려졌나» 뿐입니다. ✅ 2026-09-02 수리(사장님이 샘플 4종 → B → B-1+B-3 을 골라서): 전체 보기는 **펼친 요일 하나만 «그날 수업 있는 강사» 열로 쪼개고** 나머지 요일은 40px 띠(밀도 = 그라데이션 «한 장» — div 수십 개로 만들지 말 것), 같은 강사·같은 시각만 **군집 단위**로 폭을 나눠 둘 다 보이게(⛔ 열 전체를 나누면 한 곳만 겹쳐도 그 열 카드가 전부 반쪽), 수업 사이 30분 이상은 «빈 N분» 점선. 정본은 `ph54LayoutItems` 하나. ⛔ `ph54C24Card` 의 인자를 늘리지 마세요 — `c24_mirror_harness` I절이 `function ph54C24Card(s){` 모양을 오려 내 검사합니다(자리는 `s.__lay` 로 넣고 곧 지웁니다). ⛔ 그 함수 주석에 «data-idx»·«draggable» 이라는 낱말을 적지 마세요 — 부정 검사가 자기 주석을 잡습니다(실제로 밟음). ⚠️ 드롭은 요일·시각만 바꿉니다 — 다른 강사 열에 놓아도 강사는 그대로(PATCH 계약). ⚠️ 브라우저로 잴 때 **관리자 화면은 zoom 1.3** 이라 px 상수 비교가 어긋나고, 20분 카드는 **최소 높이 22px(=26분)** 라 «시간상 끝» 보다 아래까지 내려옵니다 — 앞 카드와의 상대 위치로 재세요. 감시는 `test-harness/teacher_calendar_lanes_harness.mjs`(55종 — 배치 함수를 오려 내 **실제로 돌립니다**)와 `test-harness/manual/teacher-calendar-accordion-browser.mjs`(브라우저 22종 — 같은 열 카드 교차 0건. 자동으로 안 돕니다, 사람이 부릅니다). 샘플·결정 경로: `docs/작업기록/260902_강사캘린더_겹침수리_요일접기_강사별열.md` |
| 여러 문항·여러 단계짜리 화면에서 **잠깐 나갔다 오면 처음부터** (전화·앱 전환) | 🔴 **진행 상태가 그 화면의 JS 변수에만 있는 것입니다 — 폰은 전화·앱 전환만으로도 탭을 버립니다.** 2026-09-21 사장님 제보: AI 자동 진단 24문항 중 **14문항(약 7분)을 풀고 전화를 받고 돌아오니 처음부터**였습니다. `level-test-ai.html` 의 `var questions=[], answers={}, idx=0` 이 전부라 페이지가 다시 로드되면 초깃값이었습니다. ⚠️ **에러가 안 납니다** — 화면도 코드도 정상이고 틀린 것은 «남는가» 하나뿐이라, 그 화면을 물고 있던 하니스 3종이 수리 전에도 전부 초록이었습니다. ✅ 답을 **«고르는 즉시»**(넘어가기 «전» 에) `localStorage` 에 적습니다 — 그래야 마지막 한 문항이 안 날아갑니다. ⛔ **문항 «번호»(idx)로 저장하지 마세요** — 문제은행이 바뀌면 남의 답이 엉뚱한 문항에 붙는데 **에러가 안 납니다.** 문항 «id» 로 저장하고 되살릴 때 지금 받은 문항에 없는 id 는 버리세요. 🔴 **답이 `0`(A번)일 수 있습니다** — «안 푼 문항 찾기»·«푼 개수 세기» 를 falsy 로 판정하면 **A번을 고른 문항이 «안 푼 것»** 이 됩니다(변이시험으로 확인). `== null` 로 보세요. ⛔ **저장본은 «결과가 확인된 뒤에만» 지우세요** — 제출 «전» 에 지우면 채점이 실패했을 때 24문항이 통째로 날아갑니다(하필 다시 채점해야 할 때). 그때 **`fetch` 는 HTTP 500 에 reject 하지 않습니다** — `.catch` 는 «네트워크가 끊긴» 경우만 잡으므로 오류 응답이 그대로 «결과» 로 흘러 «—» 만 찍힌 채 «끝» 으로 보입니다. **«성공이라고 말했는가»(`d.ok === true` + 실제 값)로 가르세요.** ⛔ `sessionStorage` 는 안 됩니다(탭이 버려지면 같이 사라집니다 — 고치려던 그 경우). ⛔ `beforeunload` 로 막으려 하지 마세요 — 전화는 막을 수 없고 폰에서는 안 뜹니다. **막는 것이 아니라 남게** 하는 것이 답입니다. ✅ 그리고 **버튼과 안내를 함께** 두세요 — 자동 저장만 해 두면 학생은 «나가면 날아간다» 고 여겨 전화도 못 받습니다(사장님이 요청한 것이 «중간 멈춤» 버튼이었습니다). ⚠️ 보관기간은 정하되 **소리 없이 지우지 말고 «언제 저장한 것인지» 화면에 적어** 사람이 고르게 하세요(여기서는 24시간). ⚠️ `localStorage` 는 시크릿 창·저장 차단·용량 초과에서 **던집니다** — 전부 try/catch. 🔴 **그런데 «던지지 않게» 만 하고 끝내면 안 됩니다 — «저장됐다» 고 말하기 «전» 에 «저장됐는지» 를 확인하세요.** `catch(e){}` 로 삼키면 화면이 「여기까지 저장했어요」라고 단정하고, 학생은 **안심하고 나가서 처음부터 다시** 풉니다 — **고치기 «전» 에는 화면이 아무 약속도 안 했으므로 거짓 약속은 그때보다 나쁩니다.** 저장 함수가 **true/false 를 돌려주고** 부르는 쪽이 그 값을 보고 화면 글자를 정하게 하세요(같은 표의 「상한·검증을 새로 걸 때 화면이 그 실패를 뭐라고 말하는지」 ②와 같은 뿌리 — 2026-09-21 에 채점 경로에는 그 규칙을 걸고 **저장 경로만 빠뜨렸다가** 함정 대조가 잡았습니다). ⚠️ **`setItem` 이 던지지 «않고» 아무것도 안 하는 환경도 있습니다** — 쓴 뒤 **다시 읽어** 확인하세요. ⛔ 미리 겁주지는 마세요 — 실제로 실패한 뒤에 한 번만 약속을 거두고, 그때 «할 일»(지금 끝까지 푸는 것이 안전)을 함께 주세요. 감시는 `test-harness/leveltest_resume_harness.mjs`(49종 — 저장·복구 함수를 오려 내 **실제로 돌립니다**. 변이 10종 전부 실제 FAIL)와 `test-harness/manual/leveltest-resume-browser.mjs`(브라우저 40종 — 제보를 reload 로 그대로 재현하고, ⑩절이 `setItem` 이 던지는 문맥에서 **화면 글자를 직접 읽습니다**. 자동으로 안 돕니다, 사람이 부릅니다). 📄 `docs/작업기록/260921_레벨테스트_중간멈춤_이어서하기.md` |
| «실패» 로 찍는 분기가 **무엇을 얼마나 잃었는지는 안 적을** 때 | 🔴 **그 값이 비면 다음 판정이 «없던 일» 로 뒤집히고, 화면이 그것을 통째로 감춥니다.** `recordings-r2.ts` 의 `upload/complete` 실패 분기가 `status`·`storage` **만** 적고 `ended_at`·`duration_ms`·`size_bytes` 를 안 썼습니다 — 2026-09-02 D1 실측: `upload_failed` **73건이 전부 `duration_ms` 0** 이었고 그중 여럿은 `size_bytes` 가 수 MB(611KB·1.4MB·1.7MB·4.5MB — 조각은 올라갔는데 마무리만 실패). 그러면 ㉠ 화면이 «몇 분짜리 수업을 잃었는지» 를 말할 수 없고 ㉡ `duration_ms` 가 0 이라 나중에 오는 `/api/recordings/stop` 의 `nothingRecorded` 판정이 **«없던 일(aborted)»** 쪽으로 기울며, 목록은 «aborted + size 0» 을 통째로 감추므로 **진짜 잃어버린 수업이 «정상 정리분» 에 파묻힙니다**(이 표의 「정상 정리분에 진짜 실패가 파묻힌다」와 같은 뿌리). ✅ 실패로 찍을 때도 **«무엇을 얼마나»** 는 남기세요. ⚠️ 단 **덮어쓰지 마세요** — `COALESCE`·`MAX` 로. 이 경로는 `beforeunload` 비콘으로도 오고 늦게 도착한 중복 요청이 0 으로 지우면 같은 사고가 다음 번에 다시 납니다. ✅ «없던 일» 판정은 **클라이언트가 보낸 값만 보면 안 됩니다** — 탭을 닫고 나가면 `onstop` 이 안 돌아 0·0 으로 오는데, 그 사이 조각 업로드가 이미 `size_bytes` 를 적어 뒀을 수 있습니다. **DB 값도 함께**(둘 중 큰 쪽) 보세요. ⚠️ 문자열 하니스는 못 잡습니다 — 조건도 값도 «있고» 틀린 것은 «무엇이 적히는가» 뿐입니다. **소스에서 그 UPDATE 문을 오려 내 진짜 SQLite 에 돌리세요**(본보기 `recording_stop_truth_harness` ⑧절 — 되돌리면 실제로 8건 FAIL). 🪤 그때 하니스를 «크래시» 시키지 마세요 — 되돌리면 바인드 개수가 안 맞아 `run()` 이 던지는데, 감싸지 않으면 스택트레이스만 남아 무엇이 깨졌는지 안 보입니다(`try/catch` 로 **깔끔한 FAIL** 로). |
| 작업하는 사이 **다른 세션이 같은 일을 먼저 병합**함 | 🔴 **2026-09-02 하루에 세 번 밟았습니다** — 「수업 중 배포 금지」를 만드는 동안 #708(시간대 게이트) · #730(화·목 창) · #742(크론 옮김)가 차례로 먼저 병합됐고, 매번 제 PR 이 `mergeable_state: dirty` 가 됐습니다. ⚠️ **그대로 병합하면 상대의 새 작업을 되돌립니다** — 제 브랜치에는 옛 크론 시각(23:17·02:41…)이 들어 있어서, 충돌을 대충 풀었으면 새 값(02:05·05:47…)을 덮어썼을 것입니다. ✅ **병합 전에 `git log --oneline HEAD..origin/main` 으로 «상대가 무엇을 했는지» 를 먼저 읽으세요** — 이 표의 「INDEX 충돌을 «양쪽 다 남기기» 로 풀었더니 지운 줄이 되살아남」의 형제입니다(그쪽은 «상대가 지운 경우», 이쪽은 «상대가 같은 것을 더 낫게 고친 경우»). ⛔ **「양쪽 다 남기기」가 틀리는 경우가 있습니다** — `CLAUDE.md` 한 줄을 둘이 각자 고치면 «같은 줄의 두 판» 이라 다 남기면 한 문단이 두 번 실립니다. **상대 줄을 정본으로 두고 내 고유 부분만 덧붙이세요.** ⚠️ 그때 **상대 줄에 남아 있는 «전제» 가 내 변경으로 깨지지 않는지** 보세요 — #730 의 「그날 끊겨도 감수한다」가 제 실접속 판정 때문에 반쪽만 맞게 되어 함께 고쳤습니다(안 고치면 규칙서가 자기모순입니다). ✅ 겹치는 부분이 크면 **PR 을 «main 에 아직 없는 것만» 으로 다시 쓰는 편**이 낫습니다(실제로 그렇게 해 파일 6개 → 4개로 줄었습니다). ⚠️ **CI 초록불은 «그 PR 을 열던 시점의 main» 기준입니다**(이 표의 「오래 열려 있던 PR 을 «CI 초록불이니까» 하고 병합하려 할 때」). 📌 **덧붙임(2026-09-09) — 병합 전 diff 를 `origin/main..HEAD` 로 보지 마세요.** 로컬 `origin/main` ref 가 낡아 있으면 **남의 커밋이 «내 변경» 으로 딸려 보입니다** — 실측: 43파일·4,077삭제로 보였는데 `git fetch origin main` 뒤 **`git diff $(git merge-base origin/main HEAD)..HEAD`** 로 다시 재니 **9파일 / +1,103** 이었습니다. ⚠️ 그 상태로 「내가 남의 것을 지웠나」를 의심하면 시간을 통째로 씁니다 — **fetch 먼저, 기준은 merge-base**. 📄 `docs/작업기록/260902_수업중_배포금지_실접속판정과_잠들어있음_정정.md` · `docs/작업기록/260908_화상수업_사무실모드_옆자리목소리.md` §7 |
| 하니스가 **초록불인데 그 기능이 라이브에서 한 번도 안 돌았음** | 🔴 **하니스는 «코드 계약» 을 재지 «라이브에서 실제로 돌았는가» 를 재지 않습니다.** 2026-09-02 실측: 배포 보류 게이트의 «실접속 판정» 이 전용 하니스 42종 전부 초록이었는데, 실제 배포 로그는 **`⚠️ 실접속 확인 실패(http-403)`** 였습니다 — CI 토큰에 D1 읽기 권한이 없어 **매번 통과**하고 있었습니다(그 기능이 라이브에서 한 번도 안 돈 것입니다). ⚠️ 하니스가 잘못한 것이 아닙니다 — 가짜 fetch 로 «판정이 맞는가» 를 정확히 재고 있었고, 틀린 것은 «그 판정에 닿기 전에 죽는다» 는 **환경** 쪽입니다. ✅ **바깥 자원(D1·R2·외부 API·시크릿)에 기대는 기능은 «배포 로그» 로 한 번 확인**하세요 — 그 자리에서 갈립니다(`mcp__github__get_job_logs` 로 그 단계 줄을 보면 됩니다). ✅ 그래서 그런 기능은 **«실패했다» 를 반드시 말하게** 만드세요 — 조용히 통과하면 영영 모릅니다(이 표의 「측정할 수 없는 값을 그럴듯하게 채우고 싶을 때」·「보냈습니다라고 하는데 아무 데도 안 갔음」과 같은 뿌리). 이번에도 그 한 줄이 있어서 찾았습니다. ⛔ 그리고 **「하니스가 통과하니 동작한다」로 문서에 적지 마세요** — 이 저장소가 네 번 사고를 낸 「하기로 한 것이 «했다» 로 적힌다」 그대로입니다. 실제로 `class-window.mjs`·`CLAUDE.md` 두 곳이 그렇게 적혀 있어 PR #752 로 정정했습니다. ⚠️ ⛔ 다만 **«고장났으니 고쳐야지» 하고 그 권한을 열지는 마세요** — 그 403 은 «켜면 정책이 바뀌는» 자리라 일부러 잠들어 있습니다(같은 표의 배포 보류 줄) |
| 영어만 받는 필터에 **불릿·머리기호가 붙은 줄**을 넘길 때 | 🔴 **`isEnglishText`(`src/english-only.ts`)는 ASCII 인쇄가능 문자만 통과시킵니다 — `•`(U+2022) 한 글자에 그 줄이 통째로 «영어가 아님» 이 됩니다.** 2026-09-02 교재 OCR 실측: 같은 페이지를 Llama 3.2 Vision 은 **36낱말**, Llama 4 Scout 은 **8낱말**로 읽어 «모델 실력 차이» 로 보였는데, 실은 Scout 이 **원문 그대로 살려 읽은** 「• Do you listen to some tracks from other countries?…」가 필터에서 떨어진 것이었습니다(Vision 은 불릿을 빼고 읽어서 통과했을 뿐 — **더 잘 읽는 쪽이 더 나쁘게 채점됐습니다**). ⚠️ 하필 그것이 **높은 레벨 교재에서 제일 원하는 문장**(웜업·AI영어친구가 쓸 열린 질문)입니다 — 잃는 것이 «장식» 이 아니라 «본문» 입니다. ⚠️ **에러가 안 납니다** — 줄 수가 조용히 줄 뿐이라 «이 엔진은 원래 이 정도» 로 읽힙니다. ✅ 고침은 **줄머리 표시를 벗기고 나서 판정**하기(`stripLeadMark`, `src/textbook-ocr.ts`). ⛔ **`isEnglishText` 자체를 넓히지 마세요** — 웜업·게임·단어장이 함께 쓰는 정본이고 그 화면들의 전제가 바뀝니다. 쓰는 자리에서 벗겨 넣으세요. ⛔ `-`·`*` 는 **뒤에 공백이 있을 때만** 벗기세요 — 「-5 degrees」·「A-B-C song」이 깨집니다. ⛔ 줄 «안» 의 문자는 건드리지 마세요(줄머리만). ✅ 검사는 «그 함수가 있는가» 가 아니라 **실제 데이터를 넣어 돌려서** — 사장님 화면에 나온 그 두 장을 그대로 넣었고, 벗기기를 끄면 실제로 FAIL 납니다(`test-harness/textbook_ocr_probe_harness.mjs`). 📌 같은 뿌리로 **길이 상한도 자리마다 다릅니다**(웜업 80·게임 90·단어장 30) — 통일하지 마세요 |
| 같은 기능에 **방향이 반대인 제보**가 차례로 올 때 | 🔴 **한쪽만 보고 고치면 다른 쪽이 그대로 재발합니다 — 그리고 두 번째 제보는 «내가 첫 번째를 고친 결과» 입니다.** AI 영어친구가 그랬습니다: 2026-07-29 「영화 주제로 들어왔는데 동물 얘기를 물어봤어요」 → 프롬프트에 `Stay on THIS topic` · `every question must be about it` · `Do NOT switch` **금지 세 겹**을 넣었고, 2026-09-03 벨잉글리시 원장님이 정확히 그 결과를 보고했습니다 — 「대화가 매끄럽게 이어지지 않고 **정해진 문장 안에서만 한다**, 그래서 금방 흥미가 떨어진다」. 같은 시기 8/31 「A1인데 문장이 너무 길다」로 길이까지 조여 놓아, 기본 레벨에서 «칭찬 1문장 + 질문 1문장» 을 빼면 **내용이 1문장**이었습니다. ⚠️ **에러가 안 납니다** — 프롬프트도 값도 «맞고», 틀린 것은 «두 요구 사이 어디에 서 있는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 고칠 때는 **두 제보를 같은 자리에 함께 적어 두고**, 둘 다 지키는 문장을 찾으세요 — 여기서는 「AI 가 «스스로» 갈아타지는 않지만, **학생이 꺼낸 얘기는 따라간다**」(`FOLLOW THE STUDENT` … `Only steer back when the chat stalls`). ✅ 그리고 **하니스가 양방향으로** 못 박게 하세요(`ai_friend_reply_quality_harness` — 「따라가는가」·「스스로 갈아타지 않는가」·「옛 감옥 문구로 되돌아가지 않았는가」 셋. 되돌리면 실제로 3건 FAIL). ⛔ **옛 프롬프트 문구를 «글자 그대로» 못 박은 검사는 이때 반드시 깨집니다** — 보장이 세졌는데 검사만 깨지는 그 함정입니다(이 표의 「하니스가 «객체 모양» 을 정규식으로」 항목). 문구가 아니라 **뜻**으로 물으세요. ⛔ 그리고 **세 화면에 같은 수리를 복사하지 마세요** — 같은 날 웜업·음성코칭도 봤는데 웜업은 주제가 원래 부드러웠고(「이어지도록 물어봐」), **음성 코칭에는 LLM 이 아예 없었습니다**(문장은 고정 세트, 채점은 Azure 음소 분석, 피드백은 하드코딩 `if/else`). 「AI 가 딱딱하다」 제보를 받으면 **그 화면에 LLM 이 실제로 있는지부터** 확인하세요 — 없으면 고칠 곳은 프롬프트가 아니라 «손으로 쓴 문구» 입니다. 📄 `docs/작업기록/260903_AI대화_세화면_정해진문장_수리.md` |
| 한글이 든 화면을 **PDF 로 뽑아야** 할 때 | ⚠️ **이 컨테이너에는 한글 폰트가 0개입니다**(2026-09-03 실측 `fc-list` 59개 중 CJK 0). 그대로 `--print-to-pdf` 하면 글자가 전부 □□□ 로 나옵니다. ✅ **그런데 `fonts.googleapis.com`·`fonts.gstatic.com` 은 프록시가 열어 줍니다**(같은 날 실측 **200**) — 그래서 «내려받아 base64 로 심는» 길이 있습니다: `css2?family=…&text=<쓸 글자만>` 으로 받고, 응답 CSS 의 `url(...)` 을 `data:font/woff2;base64,…` 로 바꿔 `<style>` 에 넣은 뒤 크로미움으로 인쇄합니다(이모지는 `Noto Color Emoji` 를 같은 방법으로). ⚠️ 2장의 「구글 폰트가 프록시에 막혀 안 받아집니다」는 **헤드리스 브라우저가 페이지에서 부르는 경우** 이야기이고, **curl·python 으로 받는 것은 됩니다** — 둘을 헷갈리지 마세요. 🪤 `text=` 파라미터 함정 둘: ① **굵기를 여러 개 한 번에 못 받습니다**(`:wght@400;500;700;900` + `text=` → **400**). 굵기마다 따로 요청하세요. ② 폰트 이름에 `+` 를 직접 넣으면 urlencode 가 `%2B` 로 만들어 **400** 입니다 — `Noto Sans KR` 처럼 **공백으로** 넘기세요. ⚠️ A4 한 장에 안 들어갈 때 **글자를 균일 축소하지 마세요** — 1,486px 를 1,039px 에 맞추려면 30% 축소라 13px 가 9px 이 됩니다. **인쇄용 2단 배치**(`@media print{ .sheet{display:grid; grid-template-columns:1fr 1fr} }`)로 접으면 글자 크기를 지킨 채 997px 가 됩니다. ✅ 넘치는 양은 **재서** 정하세요 — `@media print` 를 `@media all` 로 바꾼 사본을 A4 인쇄 폭(210mm − 좌우여백 ≈ 703px)으로 열어 `.sheet` 높이를 `getBoundingClientRect()` 로 잽니다. ⚠️ 스크린샷으로 «잘렸나» 를 판정하지 마세요 — `--window-size` 높이에서 잘린 것을 «내용이 잘렸다» 로 오독합니다(실제로 밟았습니다). |
| AI 학습도구를 **새로 만들거나** 도구 화면의 «레벨 키» 를 바꿀 때 | 🔴 **도구 8종은 서로 몰랐습니다 — 2026-09-03 D1 실측: 최근 30일 AI 도구를 쓴 학생 60명 중 2개 이상 도구를 쓴 학생 7명.** 그래서 «오늘 할 순서» 는 `src/today-plan.ts`(정본) 한 곳이 정하고 `/today.html` 이 그리며, 도구 화면은 `js/today-bar.js` 한 줄로 «돌아가기» 만 얹습니다. ✅ 새 도구를 만들면 **`TOOLS`·`HOME_WEEK` 에 등록**하고 그 화면에 `today-bar.js` 를 실으세요 — 안 실으면 그 도구에서 흐름이 끊깁니다(`today_plan_harness` ⑤가 8종을 못 박습니다). ✅ 레벨은 한 눈금입니다 — 밴드 n = 웜업 `mangoi_warmup_level` `'n'` = AI 친구 `mangoi_aifriend_level` `'Sn'`. **키 이름이나 값 모양을 바꾸면 `levelKeys` 도 함께** 바꾸세요(안 그러면 «비어 있을 때만 심기» 가 조용히 헛돕니다). ⛔ 도구 화면이 서버 레벨을 «직접 읽어 덮는» 방식으로 통일하지 마세요 — 학생이 고른 값이 사라집니다. «했나» 는 서버가 «오늘 그 도구 기록이 있는가» 로만 봅니다 — **분 단위를 지어내지 마세요.** ⚠️ 카페24 예약 씨앗(`attendance.room_id LIKE 'c24-%'`, `joined_at` = 예약 시각)은 «오늘 학원 수업이 있다» 로만 쓰고 «실접속» 으로 쓰지 마세요. 🪤 브라우저 검사에서 `addInitScript` 로 `localStorage.clear()` 를 하면 **reload 마다 다시 지워져** 「학생이 고른 값을 안 덮는다」 검사가 거짓 FAIL 이 납니다(실제로 밟음 — sessionStorage 표시로 한 번만 지울 것) |
| 안내·경고가 **한 번도 안 뜸** — 「조건이 안 맞았나 보다」로 넘길 때 | 🔴 **조건 미달이 아니라 «판정이 그 틱을 통째로 건너뛰는» 경우가 있습니다 — 그리고 하필 «제일 나쁜 틱» 이 그렇습니다.** 2026-09-02 사장님이 class-849 수업 19분 내내 「인터넷 연결이 불안정합니다」 토스트를 한 번도 못 보셨습니다. `vcNetSelfWatch`(`js/idx-vc-qlog.js`) 첫 줄이 **`if (loss === -1) return;`** 이었는데, `loss === -1` 은 «영상 표본이 없던 4초» 라는 뜻이지 **«RTT 를 모른다» 가 아닙니다** — `idx-main.js:5060` 은 그 틱에도 `vcQualityAcc(-1, rtt)` 로 **측정된 RTT 를 그대로 넘깁니다.** 실측: RTT 가 제일 높았던 두 창이 **19:30:44 RTT 440(novideo 13/15)** · **19:34:37 RTT 435(novideo 11/15)** 로, 틱의 대부분이 바로 그 건너뛰는 틱이었습니다. ⇒ **«무엇을 모르는가» 를 축마다 갈라서 보류하세요** — 손실을 모르는 것과 RTT 를 모르는 것은 다른 사실입니다. 📜 같은 뿌리가 이 표에 이미 있습니다(「끊긴다」 제보를 `vc_quality` 로 확인하려는데 기록이 거의 없음 — 그때도 «표본이 제일 필요한 쪽이 통째로 빠져» 있었습니다). 🔴 **둘째 함정이 더 큽니다 — 지연(RTT) 문턱을 «절대값» 으로 두면 안 됩니다.** 그 토스트는 `rtt >= 400` 이었는데 중국 강사 회선은 **평소가 360~440ms** 라(같은 수업 실측), 떴더라도 「공유기 가까이 가거나 유선으로 연결하세요」라는 **사람이 고칠 수 없는 것을 시키는 틀린 안내**가 됩니다. 거꾸로 기준이 130ms 인 국내 학생에게는 400 이 느슨해 진짜 막힘을 놓칩니다. ✅ 정본 방식은 **«이 연결의 기준 RTT» 대비**입니다(#771 이 화질 회복 문턱에 먼저 쓴 것) — 기준 = 그 연결에서 본 **최소 RTT**(위로는 틱당 2% 씩만 따라감), 상한 500, 문턱 = `max(옛 절대값, 기준 + 200)`. **⛔ 손실 문턱은 절대값 그대로 두세요** — 손실은 «나쁜» 신호라 절대값이 맞고, RTT 는 «막힌» 신호라 기준 대비 증가분이 맞습니다. ⛔ 문턱을 기준 대비로 바꾸면서 **«되던 것» 을 깨지 마세요** — 기준이 낮은 국내 회선은 옛 숫자가 그대로 남게 `max()` 로 바닥을 두세요. ⚠️ **문자열 하니스·타입체크는 둘 다 못 봅니다** — 함수도 값도 다 «있고» 틀린 것은 «어느 틱을 보는가» 와 «무엇과 비교하는가» 뿐이라 수리 전에도 `--fast` 가 **돌 수 있었던 하니스 254건 전부 초록**이었습니다(⚠️ 이 컨테이너는 `node_modules` 가 없어 12건이 환경 사유로 FAIL 합니다 — 「전부 초록」은 «돌 수 있었던 것만» 이라는 뜻입니다). ✅ 검사는 함수를 **실제로 돌려서** 하고, **«뜬다» 와 «함부로 안 뜬다» 를 짝으로** 넣으세요(한쪽만 두면 반대로 무너집니다). 🔴 **셋째 — 기준값을 «매 틱» 올리면 계속 나쁜 회선이 «자기 나쁜 값» 을 평소로 학습해 스스로 정상이 됩니다.** 실측(기준 130ms 회선이 410ms 에 계속 머무는 경우): **16틱(약 64초)** 만에 문턱이 410 위로 올라가 **토스트가 사실상 1회만 뜨고 맙니다**(3분 쿨다운이 끝날 무렵엔 이미 «정상» 이라 두 번째가 없습니다) — 옛 절대값 때는 3분마다 반복해서 알렸으니 **«되던 것» 을 깨는 것**입니다. ✅ 기준은 **«나쁘지 않은 틱» 에서만 위로** 따라가게 하세요(내려가는 쪽은 언제나). ⚠️ **여기가 #771(화질 회복)과 갈리는 자리입니다** — 그쪽은 지연이 높은 회선도 언젠가 화질을 올려야 하니 계속 따라가는 것이 맞지만, 이쪽은 «네 평소보다 나쁘다» 를 사람에게 말하는 것이라 평소는 **좋았던 때에서만** 배워야 합니다. ⚠️ 그렇게 하면 «나쁨» 상태가 다음 수업으로 넘어갈 수 있으니 **수업 종료 정리 목록에 그 상태도 함께** 넣으세요(`__vcNetSelf`). 감시는 `test-harness/vc_quality_blindspot_harness.mjs` ⑧절(검사 **10건**) — 옛 `return` 되살리면 1건, 옛 절대값 400 으로 되돌리면 1건, 기준을 나쁜 틱에도 올리면 1건, 종료 정리에서 빼면 1건이 **실제로 FAIL** 납니다 |
| 「수업이 흐리고 소리가 끊긴다」 — 회선을 의심하기 «전» 에 볼 것 | 🔴 **그 사람 브라우저가 수업 중에 «녹화» 를 함께 하고 있는지 보세요 — 실시간 영상과 «같은» 업로드·CPU 를 나눠 씁니다.** 2026-09-02 실측: 자동 녹화에 **역할 검사가 없어** 데모방·참관자만 빼고 **수업에 들어온 모든 사람이 각자** 찍고 있었습니다. D1 `recordings` 대조(class-* 방): `class-1008` 교사 224.5MB/24.3분 **+** 학생 265MB/27.6분 · `class-988` 교사 235.2MB/26.0분 **+** 학생 210MB/22.5분 · `class-967` 교사 191.9MB/23.1분 **+** 학생 218.8MB/23.1분 · `class-1070` 교사 166.7MB/26.7분 **+** 학생 246.3MB/26.6분 — **두 파일은 내용이 같습니다**(둘 다 화면 전체를 합성해 찍습니다). 그 한 벌이 강사 노트북에서 **1920×1080 @15fps 소프트웨어 VP8 인코딩**(WebRTC 영상 인코더와 **같은 CPU**)과 **약 200MB/24분 ≈ 1.1Mbps 업로드**(R2 multipart **5MB 조각** = 36초마다 몰아 쏨)를 수업 내내 했습니다. 학생에게 보내는 실시간 영상 상한이 1.2Mbps 이니 **녹화가 강사 업로드를 거의 두 배로** 만듭니다. ⚠️ **«그것 때문이다» 를 직접 재지는 못했습니다** — 상관이지 증명이 아닙니다(같은 저녁 실측 `rx_freeze` 140~165회). 단정해서 적지 마세요. ✅ 2026-09-02 사장님 결정: **강사는 «자동» 녹화를 하지 않고**(정본 `isStaffSkipRecording`, `js/mango-rec.js`) 학생만 찍되 **1280×720·1.2Mbps** 로 낮춥니다. ⛔ **막지는 마세요** — 배지를 누르면 강사도 손으로 시작할 수 있어야 합니다(안전판). 🔴 **판정은 «모르면 찍는다» 쪽으로 실패해야 합니다** — 막는 쪽으로 실패하면 그 수업 녹화가 통째로 사라집니다(학부모 약속·미성년자 기록). `vcIsTeacherRole()` 이 역할을 모를 때 false 를 주는 것이 그 방향입니다. ⚠️ 정본 `vcIsTeacherRole` 에는 **이미 이름 휴리스틱이 있습니다** — 로그인이 아예 없을 때만 표시이름의 교사/강사/선생님/teacher/tutor 를 봅니다. 즉 비로그인으로 들어온 사람의 이름에 그 낱말이 있으면 그 사람은 자동 녹화를 안 합니다. ⛔ 그것을 «로그인한 사람» 에게까지 넓히지 마세요 — 아이디에 teacher 가 든 학생의 녹화가 사라집니다. 🔴 **화면이 «왜 안 찍는지» 를 본문 글자로 말해야 합니다** — 배지가 「녹화 꺼짐 · 눌러서 시작」이라고만 하면 걷어낸 부하를 화면이 **다시 켜라고 권유**하는 꼴입니다(함정 대조가 잡았습니다). ⛔ 「학생이 녹화 중」이라고 단정하지 마세요 — 강사 기기에서 확인할 수 없습니다. 말할 수 있는 건 «이 기기는 안 찍는다» 와 «왜» 까지입니다. ⚠️ **비트레이트를 내리면 스냅샷 상수를 함께 보세요** — 첫 5MiB 파트가 생기기까지가 ~17초 → **약 35초**로 늘어, `SNAP_EVERY_MS` 30초 그대로면 스냅샷이 9초 한 번뿐이라 최악 손실이 ~7초 → ~22초가 됩니다(12초로 줄임). ⚠️ `recordings.teacher_name` 이 이제 **항상 학생**이 됩니다(그 칸은 «방을 먼저 켠 사람» 이었는데 이제 찍는 사람이 학생뿐) — 관리자 목록 「교사」 칸과 강사의 «녹화 보기» 조회가 그 칸에 기대고 있었다면 확인이 필요합니다. ⚠️ 「⛔ 녹화불가 · 학생 미동의」 안내는 «찍으려는 기기» 에만 뜨므로 이제 강사가 못 봅니다. ⚠️ **강사끼리만 있는 방은 자동 녹화가 0개가 됩니다**(사장님 시험 수업 `class-849` 등) — 아는 채로 택한 맞바꿈입니다. ⚠️ 칸 폭·높이는 `VID_W = width*0.3` 처럼 비례하지만 **글꼴·탭바·REC 배지·미니플레이어는 고정 픽셀**입니다 — 1280×720 까지는 산술로 겹치지 않는 것을 확인했고, 더 낮출 거면 그 값들부터 다시 재세요. ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «누가 찍는가» 뿐입니다. 감시는 `test-harness/rec_teacher_skip_harness.mjs`(25종 — 가짜 DOM·가짜 시계로 `mango-rec.js` 를 **실제로 돌려** «강사는 안 부른다» 와 **«학생은 부른다» 를 짝으로** 봅니다. 짝이 없으면 «전부 안 찍기» 도 통과합니다. 변이 3종 전부 실제 FAIL). 📌 **덧붙임(2026-09-06, 사장님 「학생 자동녹화 진행하지만 최대한 가볍게」) — 남은 학생 쪽 부하를 더 줄였습니다: 1280×720 은 그대로 두고 «그리는 fps» 를 낮췄습니다.** 🔴 `draw()` 가 `requestAnimationFrame` 으로 도니 **화면 주사율(보통 60fps)로 그리는데 `captureStream(15)` 는 그중 15장만 가져갑니다** — 그린 것의 **3/4 를 그리자마자 버리고 있었습니다.** 그 CPU 는 WebRTC 실시간 영상 인코더와 **같은 것**을 나눠 씁니다. ✅ `REC_FPS = 10` 하나로 «그리기 가드 + `captureStream`» 을 함께 정하고(숫자를 두 곳에 적으면 다시 어긋납니다), `getContext('2d', { alpha: false })`, `videoBitsPerSecond` 1.2 → **0.9Mbps**. ⟹ 그리기 **60 → 10회/초(약 1/6)** · 업로드 **25% 감소** · **교재 글자 화질은 한 픽셀도 안 바뀝니다**(프레임당 비트는 80kb → 90kb 로 오히려 늘어납니다). ⛔ **해상도를 낮춰서 풀지 마세요** — `CONTENT_W` 가 896 → 672 로 줄어 교재 글자를 잃고, 위에 적힌 «고정 픽셀» 요소들과 겹칩니다. ⛔ **rAF 구조를 타이머로 바꾸지 마세요** — 2026-08-05 실장애(탭이 숨으면 rAF 가 멎어 캔버스가 얼어붙고 녹화가 빈 껍데기)의 방어가 rAF + keepAlive 짝입니다. 넣은 것은 «그릴 차례가 아니면 건너뛰기» 뿐입니다. ⚠️ **건너뛸 때도 `composeTickAt` 을 갱신하고 rAF 를 다시 걸어야 합니다** — 안 갱신하면 keepAlive(700ms)가 «rAF 가 멎었다» 로 오판해 **오히려 두 번 그리고**(가볍게 하려다 무겁게), 안 걸면 그 자리에서 그리기가 **영영 멎습니다**(화면은 멀쩡한데 녹화만 얼어붙습니다). ⚠️ **비트레이트를 내렸으니 스냅샷을 또 보세요** — 첫 5MiB 파트까지가 35초 → **47초**(영상만 세는 보수적 값)이고 `SNAP_FIRST_MS` 9초 + `SNAP_EVERY_MS` 12초면 4회가 덮습니다. 🪤 **그리고 이 검사는 처음에 헛돌았습니다** — 가드 문턱을 하니스에 «손으로 다시 적어» 두어, 소스에서 문턱을 `0` 으로 바꿔(=고치기 전과 동일) 놓아도 **초록불이었습니다**(변이시험에서 실측). **소스에서 오려 낸 식 자체를 `new Function` 으로 평가**하도록 고쳤고, 그제서야 변이 4종(가드 제거·문턱 0·`captureStream` 에 숫자 다시 적기·건너뛸 때 rAF 안 걸기)이 **전부 실제 FAIL** 났습니다. 🔴 **덧붙임(2026-09-08) — 그 「강사는 자동 녹화 안 함」이 «날마다 갈립니다».** 사장님 「왜 자꾸 동시에 두번씩 녹화가 되는 거지?」 실측: 같은 방에 **강사 기기 1벌 + 학생 기기 1벌**이 남습니다(`class-1924` 14:00:15 「교사 Teacher - Farrah」 + 14:01:58 「ysyt01」 · `class-2069` 12:23:41 「교사 Teacher Kaye」 + 12:33:48 「mby1」). 날짜별로는 9/4 강사 녹화 **0건**인데 9/8 **4건**. ⚠️ **왜 안 걸렸는지는 못 쟀습니다** — 후보 셋(강사가 배지를 손으로 눌렀다 · 그 브라우저가 옛 파일을 캐시로 물고 있다 · 역할 판정이 학생으로 떨어졌다)이고, `recordings` 표에 **자동/수동 구분 칸이 없어** D1 만으로는 못 가릅니다. ✅ 가르는 방법은 하나 — **강사 화면 배지가 「📵 녹화 안 함 · 회선 보호」로 뜨는지** 보면 「사람이 눌렀다」와 「코드가 못 걸렸다」가 갈립니다. ✅ 그래서 «왜» 를 묻지 않고 **서버가 두 번째 시작을 거절**합니다(정본 `src/recording-dup-guard.ts`). ⛔ 그 게이트는 **막는 쪽으로 실패하면 안 됩니다** — 잃을 것이 «수업 녹화 그 자체» 라 **0벌이 두 벌보다 나쁩니다**(조회 실패·모르는 시각·미래 시각·예외 전부 «찍는 쪽» 으로). ⚠️ 「살아 있는가」는 **`recording_parts.created_at`** 으로 잽니다 — `recordings` 에 그 시각 칸이 없고, 새 칸을 지연 ALTER 로 붙이면 없는 DB 에서 조회가 통째로 죽습니다. ⚠️ 창은 **3분** — 짧으면 느린 회선의 «살아 있는» 녹화를 죽은 것으로 봐 두 벌이 다시 생기고, 길면 유령이 오래 남습니다. 막힌 기기가 60초마다 다시 시도하므로 상대가 죽어도 **최악 3분** 뒤 이어받습니다. ⚠️ **문자열 하니스로는 이 사고를 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «누가 찍는가» 뿐입니다. 감시는 `test-harness/recording_dup_guard_harness.mjs`(53종 — 정본을 실제로 돌리고 변이 9종이 각각 그 검사를 FAIL 시킵니다). ⛔ **덤으로 바뀐 「미동의 거절도 60초마다 자동 재시도」는 «그대로 둡니다»**(2026-09-09 사장님 「미동의 자동 재시도는 그대로 둬줘」). 그 재시도는 «다른 기기가 찍는 중» 으로 막힌 기기가 상대가 죽으면 이어받게 하려고 넣은 것인데, 같은 자리를 지나는 미동의 거절에도 함께 걸립니다 — 학생이 뒤늦게 동의하면 그때부터 녹화가 시작됩니다(그전에는 강사가 배지를 눌러야만 했습니다). ⚠️ 대가는 **요청 수**입니다 — 40분 수업이면 기기당 40회이고, 학원 NAT 뒤에 막힌 기기가 여럿이면 시간당 상한(`REC_START_MAX_PER_IP_HOUR` 300)에 닿을 수 있습니다. ⛔ PR #858 본문·커밋 메시지에 「지시 범위 밖 변경이라 되돌릴 수 있습니다」라고 적혀 있지만 **그 뒤에 사장님이 유지를 결정했습니다** — 그 문장을 근거로 되돌리지 마세요. ⛔ 그리고 «미동의만» 골라 빼려고 `if (!isRecording) autoRecStarted = false;`(mango-rec.js) 를 조건부로 바꾸지 마세요 — 그 한 줄이 동시녹화 이어받기의 전부라, 빼는 순간 상대 기기가 죽어도 그 방 녹화가 통째로 사라집니다 |
| 한 칸에 «척도가 두 개» 인 점수를 화면에 그릴 때 | 🔴 **`student_evaluations.score_overall` 은 «만점이 하나가 아닙니다» — 강사 1분 수업일지(`/api/eval/create`·`bulk-create`)는 1~5, AI 수업 리포트(`/api/eval/ai-lesson-report`)는 0~100 을 «같은 칸» 에 적습니다**(2026-09-04 운영 D1 실측: 1~5 행 3건 · 0~100 행 2건). 그래서 `report.html` 이 「/10」으로 못 박아 **두 갈래 어느 쪽도 아닌 숫자**를 학부모에게 몇 달간 보여줬습니다. ⛔ 「/10 이 틀렸으니 /5 로」 도 안 됩니다 — 그 순간 0~100 행이 **「88 / 5점」** 이 되어 새 거짓말이 됩니다. 🔴 **덤으로 목록이 통째로 안 그려집니다** — 별점을 `'☆'.repeat(5 - Math.round(88))` 로 그리면 **`RangeError: Invalid count value: -83` 을 던져**(실측) 그 학생의 목록 전체가 「불러오지 못했습니다」가 됩니다. 0~100 행이 **하나만 섞여도** 그렇습니다. ⚠️ **행으로 두 갈래를 가를 수 없습니다** — `ai_lesson_reports.evaluation_id` 로 조인하는 것이 원리상 맞지만 실측에서 **2건 모두 비어 있어**(적재하는 코드가 없습니다) 성립하지 않습니다. ✅ 그래서 **값에서 척도를 읽고**(`> 5` 면 100점 만점) 만점을 **화면이 그대로 말하게** 하세요(`evalMax()` — `eval.html`·`report.html` 둘 다). ⛔ 임의의 숫자로 «환산» 하지 마세요(2장 「측정할 수 없는 값을 그럴듯하게 채우고 싶을 때」와 같은 뿌리). ⚠️ 두 화면이 각자 그리므로 판정이 **복제**돼 있습니다 — 한쪽만 고치면 같은 수업일지가 화면마다 다른 만점을 말합니다. ⚠️ **문자열 하니스로는 못 잡습니다** — 숫자도 함수도 다 «있고» 틀린 것은 «만점이 몇인가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 감시는 `test-harness/eval_report_naming_harness.mjs` ⑤⑥절(두 파일에서 함수를 **오려 내 실제로 돌려** 같은 답을 내는지 보고, 88·84 를 넣어 별점이 안 던지는지까지. 변이 6종 전부 실제 FAIL). 🔴 **«고쳤다» 로 읽지 마세요 — 같은 코드가 여러 화면에 흩어져 있습니다**(2026-09-04 함정 대조가 여섯 자리를 더 찾아냈습니다. 「고쳤다」를 적기 전에 옛 식을 `grep` 하는 데 30초면 됩니다). ✅ 고친 곳: `eval.html`(목록·낱장·세부 막대) · `report.html` · **`parent.html`**(학부모 대시보드도 「/10」이었습니다 — 고친 `report.html` 보다 학부모가 더 자주 여는 화면입니다) · **`js/adm-r6.js`**(관리자 평가서 표가 그 RangeError 로 통째로 안 그려지고 있었습니다). 🟡 **아직 남은 자리 셋** — `public/teacher.html` 2곳(강사 배지 「⭐ 88/5」) · `public/monthly-report.html` 1곳(추이 그래프가 5점 만점 전제) · **`src/api-admin.ts` 1곳**(AI 월간 리포트 «프롬프트» 에 「종합 88/5점」이 실려 모델에 들어가고 그 글이 학부모에게 나갑니다). 그 셋은 하니스 ⑦절이 **이름을 찍어 출력**합니다(⛔ FAIL 로 만들지 않습니다 — 무관한 PR 이 전부 빨간불이 됩니다. 선례 `popup_open_return_harness`). 📌 **성적표 「평균 점수」** 는 `buildMonthlyReportData`(`src/api-reports.ts`)가 **두 척도를 섞어 평균**을 내서 5 와 88 이 46.5 가 됩니다 — 서버를 고치는 것이 정본이고, 지금은 **화면이 «섞였는지» 만 보고 «—» 로 가립니다**(`avgLabel()`). ⛔ 만점도 단서도 없이 그 숫자를 그대로 보여주지 마세요 |
| `data-ko`/`data-en` 을 달았더니 **그린 내용이 통째로 사라짐**(「불러오는 중…」에서 안 넘어감) | 🔴 **화면 전체가 그려지는 «그릇» 에 달았기 때문입니다 — 이 표의 「아이콘 버튼에 달았더니」와 같은 뿌리인데, 이쪽은 «내용» 이 통째로 날아갑니다.** i18n 엔진은 `[data-ko],[data-en]` 인 요소마다 **`textContent` 를 갈아끼웁니다.** 그래서 `eval.html` 의 `#eval-root` 처럼 «렌더 결과를 담는 상자» 에 달면, 그려 넣은 목록·낱장이 로딩 문구로 **되돌아갑니다**(2026-09-04 실측 — 화면이 「📥 …불러오는 중…」에서 영영 안 넘어갔습니다). ⚠️ **에러가 안 납니다** — 화면은 «아직 로딩 중» 처럼 보여 서버·API 를 뒤지게 됩니다. ⚠️ **문자열 하니스로는 절대 안 잡힙니다** — 속성도 문구도 다 «맞고» 틀린 것은 «어느 요소에 달았는가» 뿐이라 그때도 `--fast` 가 전부 초록이었습니다(브라우저 검사가 잡았습니다). ✅ **안쪽 `<span>` 에 다세요** — 그리는 순간 `innerHTML` 로 통째로 교체되어 충돌이 없습니다. ✅ 판정은 브라우저에서 «그 글자가 실제로 나오는가» 로 하세요. 감시는 `test-harness/eval_report_naming_harness.mjs` ⑦절(«그릇에 안 달았는가» + «안쪽 span 에 달았는가» 짝)과 `test-harness/manual/eval-report-naming-browser.mjs`(브라우저 24종 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 화면 이름을 바꿀 때 — 옛 이름으로 «찾던» 사람 | 🔴 **이름을 바꾸면서 검색어까지 갈아치우면, 그 말로 찾던 사람이 못 찾습니다.** 2026-09-04 「평가서」→「오늘 수업일지」·「리포트」→「월간 성적표」로 바꿀 때(사장님 「이거 같은 거야 다른 거야」), 홈 검색(`js/idx-ai-home.js`)의 **`label` 만 새 이름으로 바꾸고 `kws` 는 «옛 말 + 새 말» 을 모두** 받게 했습니다. ✅ 이름은 **여러 곳이 같은 말을 해야** 합니다 — 전체메뉴 타일(`js/idx-allmenu.js`) · 화면 `<title>`·머리글 · 홈 검색 label · 사이트 구성표(`admin/site-structure-map.html`). 한 곳만 고치면 에러 없이 어긋나고, 그때가 그 제보가 다시 올라오는 때입니다. ⛔ 타일 라벨에 `data-ko`/`data-en` 을 달 때는 **글자만 담은 span** 에만 다세요(`<a>`·아이콘에 달면 두 엔진이 `textContent` 를 갈아끼워 **사진이 사라집니다** — 이 표의 「아이콘 버튼에 달았더니」). ⛔ **i18n 사전(`js/i18n-sweep.js`)을 고쳐서 풀지 마세요** — **42개 화면이 함께 싣고 있어** `?v=` 를 42곳 올려야 하고 무관한 화면이 통째로 PR 에 딸려 옵니다. 바뀐 문구에 `data-ko`/`data-en` 을 직접 다는 쪽이 낫습니다(사전 없이 됩니다). ⚠️ 그러면 그 사전의 옛 항목은 **아무것도 안 잡는 죽은 항목**이 됩니다 — 해롭진 않지만 그 파일을 만질 때 함께 정리하세요. ⚠️ 홈 드로어(`index.html`)와 `js/mg-sidebar.js` 에 **같은 라벨이 한 벌 더** 있습니다 — 앞은 공동 금지구역이라, 한쪽만 고치면 **홈과 나머지 25개 화면이 서로 다른 말**을 합니다. 셋을 한꺼번에 하거나 아예 두세요 |
| 색은 «맞게 칠했는데» 사람이 「그냥 글자로 보인다」고 함 (배지·칩) | 🔴 **`backgroundColor` 만 재고 «정상» 이라 하면 안 됩니다 — 옅은 파스텔 바탕은 «칠해져 있어도» 상자로 안 읽힙니다.** 2026-09-03 사장님 「교재 상태가 배지가 아니라 그냥 글자로 보여」 실측: 배경은 이미 `rgb(220,252,231)`, 크기 49×27, `border-radius` 99px 로 **정상이었고** 빠진 것은 **윤곽선** 하나였습니다. ⚠️ 화면이 밝은 흰 카드 위라 «연두 위 초록 글자» 가 배경 없는 글자와 구별이 안 됩니다. ✅ 고침은 **1px 테두리**(`border:1px solid <같은 계열 진한 색>`) — ⛔ **바탕을 진하게 하지 마세요**: 대비가 떨어지고(6.49 → 4점대) 상태마다 다시 색을 고르게 되어 팔레트가 갈라집니다. ⚠️ 테두리를 더할 때는 **`box-sizing:border-box` 와 «투명 테두리» 를 기본값으로 함께** 두세요 — 안 그러면 테두리가 있는 배지만 2px 커져 표 칸이 흔들립니다. ✅ 판정은 코드가 아니라 브라우저 `getComputedStyle` 의 **`backgroundColor` + `borderColor` + 크기**를 «함께» 재세요(하나만 재면 이번처럼 «정상» 이라 답하게 됩니다). ⚠️ 문자열 하니스는 못 봅니다 — 클래스도 색도 다 «있고» 틀린 것은 «상자로 보이는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 📄 `docs/작업기록/260902_강사명부_색과버튼_퇴사강사_파란불.md` |
| 「본인 토큰이면 통과」 게이트가 **로그인한 사람을 전부 403** 으로 막음 — 비로그인만 통과 | 🔴 **함수도 조건도 «있고» 틀린 것은 «무엇과 비교하는가» 뿐입니다 — 그런 사고는 문자열 하니스가 못 봅니다.** 출석 소프트 인증 `_attnSoftAuthOk`(`src/api-mango.ts`)는 「토큰이 있으면 토큰 uid 가 claimedUid 와 같아야 통과」인데, 출석 호출부 둘(join·checkin)이 **`b.user_id`** 를 넘겼습니다. 그런데 `mango-attendance.js` 의 `user_id` 는 계정이 아니라 **기기 식별자(`u_`+난수) 또는 DO 임시번호**이고(그 파일의 `accountUid` 주석에 그렇게 적혀 있습니다), 계정은 `account_uid` 로 «따로» 실립니다. 로그인해서 `mango_token` 을 실은 학생은 토큰 uid(`jye46712`) ≠ `user_id`(`u_j4cs5c65bp`) → **항상 403 `uid_mismatch`**. 통과한 것은 «토큰 없는 요청(비로그인·토큰 없는 계정)» 과 «교사(관리자 세션)» 뿐이었습니다(2026-07-19 도입 → 2026-09-03 발견). [잰 것 — 2026-09-03 운영 D1] `attendance.account_uid` 가 남은 계정 전 기간 **4개**(관리자 외 셋은 «계정은 있는데 토큰은 없던» 로그인으로 보임 — 추론) · 최근 7일 로그인 학생 11명 중 출석에 계정이 남은 사람 **관리자 1명** · 그날 학생 출석 22행 중 19행이 이름·host·계정 전부 빈 행(`/api/gaze-score` 의 «없으면 만든다» 폴백). ⚠️ **같은 이름이 두 표에서 뜻이 다릅니다** — 출석의 `user_id` 는 기기, 동의(`consents`)의 `user_id` 는 계정(`mango-consent.js` 가 `getCurrentUser` 의 uid 를 그 칸에 싣습니다). 그래서 동의 호출부의 `b.user_id` 비교는 맞고 출석만 틀렸습니다. ⚠️ **「라이브에서 검증했다」가 이 사고를 못 막았습니다** — 7/19 검증(`docs/보안_PII_감사.md`)은 `user_id` 칸에 계정을 넣어 보낸 것으로 보이고[추론 — 그 문서에 payload 가 없음], 실제 클라이언트는 그렇게 안 보냅니다. **검증은 «실제 클라이언트가 보내는 모양» 으로** 하세요. ✅ 고침: 출석 호출부는 `b.account_uid` 를 넘기고, 계정 칸이 비어 있으면(옛 클라·비로그인) 통과 — `account_uid` 칸에는 아무것도 안 적히므로 «계정 칸 위조» 는 성립하지 않고 7/19 의 «자격증명 없는 요청은 통과» 취지도 그대로입니다. ⚠️ 그래도 `user_id` 를 계정으로 읽는 곳이 실재해(학부모 대시보드·게임·리포트의 `attendance WHERE user_id = ?`) 토큰 보유자가 계정 칸을 비우고 `user_id` 에 남의 계정을 넣는 것은 막지 않습니다 — **익명 요청이 원래부터 할 수 있던 일**이라 7/19 가 의도적으로 남긴 한계와 같은 수준입니다. ⛔ 완전 강제로 바꾸지 마세요(결석률 100% 버그 방어). ✅ 감시는 `test-harness/attendance_soft_auth_harness.mjs`(42종 — 헬퍼 본문과 **호출부의 인자식**을 소스에서 오려 내 가짜 요청으로 **실제로 돌립니다**. 고치기 전 소스로 돌리면 9건 실제 FAIL — `ATTN_SRC_FILE=` 로 재현). 📄 `docs/작업기록/260904_출석_소프트인증_기기식별자_비교_403수리.md` |
| 결재·상태 «정책» 을 고쳤는데 **이미 만들어진 건은 그대로 막혀 있음** | 🔴 **결재 단계 역할은 «그 건에» 박힙니다** — `approval_steps.role` 은 만들 때 `stagesFor()` 로 정해져 저장되고, 나중에 정책을 바꿔도 **옛 건은 따라오지 않습니다.** 2026-09-04 실측: 8/30 에 올라온 긴급 건이 `role='exec'` 인데 `EXEC_USERNAMES=['admin']`(`approval-policy.ts`)이라 경영진이 **기안자 본인뿐**이고, 「본인이 올린 결재는 본인이 승인할 수 없습니다」(`api-approval.ts`)와 맞물려 **기다려도 처리될 수 없는 상태**로 5일을 서 있었습니다. 화면은 그동안 「대기 중」이라고만 했습니다. ⚠️ 하필 **9/2 에 긴급의 단계가 `'any'`(본사 누구나)로 이미 바뀌어 있어서** 코드만 보면 「안 막히는데?」로 읽힙니다 — **새 건은 안 막히고 옛 건만 남습니다**(`git log -S` 로 언제 바뀌었는지 보세요). ⛔ 옛 건의 역할을 «지금 정책» 으로 다시 판정해 풀지 마세요 — **역사를 덮어써** 왜 멈췄는지가 사라집니다. ⛔ D1 `UPDATE` 로 role 을 고치는 것도 사람이 결정할 일입니다(1-1). ✅ 화면이 **사실을 말하게** 하세요 — 서버 `approverCounts()` 가 «지금 이 건을 결재할 수 있는 사람 수»를 세어 `blocked` 로 내려주고 `work.html` 이 「이대로는 처리되지 않습니다」+막힌 단계 이름+이유를 그립니다. ⛔ 그 판정을 **화면에서 하지 마세요** — 화면은 계정 목록도 결재 규칙도 몰라 「승인할 사람이 없습니다」라는 틀린 말을 하게 됩니다. ⛔ 건마다 `approversFor()` 를 부르지 마세요(목록 15건이면 D1 조회 15회 — 계정 조회는 1회). ⚠️ **`exec` 단계는 지금도 인사·급여와 큰 금액 지출에 걸립니다** — 경영진이 `admin` 하나뿐인 한 사장님이 그런 건을 올리면 같은 자리에서 또 멈춥니다(경영진 계정 추가는 «사람이 정할 일»). ⚠️ ETag 에 hq 계정 수를 넣으세요 — 안 넣으면 경영진을 추가해도 **304 로 옛 화면이 최대 1시간** 남습니다. ⚠️ 「며칠째」는 **화면이** 세세요(서버가 담으면 응답이 매초 달라져 304 가 영영 안 나옵니다 — 지연 판정과 같은 이유). 감시는 `test-harness/manual/approval-track-browser.mjs`(브라우저 27종 — **자동으로 안 돕니다, 사람이 부릅니다.** 「막힘이 뜬다」만이 아니라 **「안 막힌 건·남의 건·결재함에는 안 뜬다」를 짝으로** 셉니다. 변이 4종 전부 실제 FAIL) |
| 「N건 중 M건만 봤다」처럼 **두 수를 비교해 알려 주는** 안내를 만들 때 | 🔴 **두 수의 «모집단» 이 같은지부터 보세요 — 다르면 두 방향으로 다 거짓이 됩니다.** 2026-09-04 결재 맨 위 요약이 그랬습니다: 「진행 중 N건 가운데 최근 M건만 살펴봤다」에서 N 은 **진행 중만**(`my_open`), M 은 화면에 있는 **모든 상태 15건**(`mine_shown`)이었습니다 ⟹ ⓐ 최근 15건이 전부 승인·반려면 M=15 라 진행 중 12건이 창 밖에 있어도 **아무 말도 안 하고** ⓑ 그 15건 중 진행 중이 6건뿐이면 **없는 누락을 알립니다.** ⚠️ **에러가 안 납니다** — 두 숫자 다 «맞는 값» 이고 틀린 것은 «비교해도 되는가» 뿐입니다. ✅ 화면에 보이는 쪽도 **같은 조건으로 걸러서** 세세요(`my_open_shown`). 🔴 **그리고 그 안내를 «경고 상자 안» 에 두지 마세요** — `if (stuck.length)` 안에 있으면 창 안에 문제가 없을 때 상자가 통째로 안 그려져 **정작 알려야 할 때 사라집니다.** ✅ 검사는 **두 값이 «다른» 상황을 만들어서** 하세요 — 시험 데이터에서 같게 두면 「화면 수로 세기」 변이가 그대로 통과합니다(실측) |
| **여러 벌 뽑은 것 중 «가장 깨끗한 것»** 을 코드로 고를 때 | 🔴 **«잡음이 없는가» 만으로 고르면 «아무 말도 안 하는 것» 이 언제나 이깁니다.** 2026-09-05 실측: 안내 영상 나레이션 한 줄을 복제 목소리로 3벌 뽑아 「떨어진 덩어리가 없는 것」으로 골랐더니 **원본 4.180초짜리 줄에서 2.344초 «잘린» take** 를 뽑았습니다 — 말이 중간에 끊겨 있으니 당연히 덩어리가 0개였고, 같은 점수에서 «짧은 쪽» 이 이겼습니다. ⚠️ **에러가 안 납니다** — 뒤 단계가 그 길이에 맞춰 장면을 만들어 주니 영상은 «정상으로» 완성됩니다. ✅ 고르는 규칙에는 «깨끗한가» 옆에 **«내용이 다 들어 있는가»** 를 함께 두세요 — 원본 길이의 **0.75~1.25배** 밖은 후보에서 빼고, 같은 점수면 **원본 길이에 가까운 쪽**으로 가릅니다. ⚠️ 같은 함정의 반대편도 있습니다 — 8.380초(원본 5.907초)짜리 후보는 **되풀이**입니다. 위아래 «둘 다» 막으세요 |
| 소리 문제를 **숫자로만** 판단하고 「정상 범위」라고 적을 때 | 🔴 **범위 «안» 의 숫자여도 사람 귀에는 잡음입니다.** 2026-09-05 실사고: 문장 끝 «툭» 을 −25 dB 문턱으로 잘라내고 남은 **−19.0 dB** 창을 「정상 말소리 범위」라고 적고 넘어갔는데, 사장님이 바로 그 자리를 **「9초때에 삡 소리가 나」** 로 제보하셨습니다. ⚠️ 이 컨테이너에는 **오디오 디코더가 없어** 제가 들을 수 없습니다(2장 「헤드리스로 «영상이 재생되나»」의 소리 판입니다) — 그러니 잰 값은 «어디가 의심스러운가» 를 좁히는 데까지이고 **«괜찮다» 를 말할 수는 없습니다.** ✅ 보고할 때 「잰 것」과 「그래서 내린 판단」을 **줄을 나눠** 적고, 소리는 **「사람이 들어야 확정」** 이라고 남기세요. ✅ 의심스러운 창이 남으면 **문턱을 조여** 없애는 쪽이 낫습니다(−25 → −33 dB) — 잘라내서 잃는 것은 «없어도 되는 꼬리» 뿐입니다. ⛔ 반대로 문장 «안» 의 덩어리는 잘라낼 수 없습니다 — **다시 녹음**해야 합니다 |
| 정적자산(영상·큰 파일)을 갈아 끼울 때 | 🔴 **Cloudflare Workers 정적자산은 «파일 하나 25 MiB» 가 상한입니다**(무료·유료 같음). 넘으면 배포가 막히는데, 그것을 **다음 사람이 무관한 PR 에서 밟습니다.** 2026-09-05 실측: 안내 영상 긴 판이 **24.98 MiB** 로 여유가 **22 KiB** 였습니다 — 장면 한 장만 늘려도 그 자리에서 배포가 죽습니다. ⚠️ 그 확인이 작업기록에서 빠져 있었고, 그래서 다음 판을 만들 때까지 아무도 몰랐습니다. ✅ 감시를 **자동 하니스에** 두세요(`today_plan_harness` ⑨절이 `statSync` 로 재고 남은 여유를 KiB 로 출력합니다 — 되돌리면 실제로 FAIL). ✅ 여유가 없으면 **품질을 한 단계 낮추세요**(crf 20 → 21 로 24.98 → 22.61 MiB). ⚠️ 영상을 갈아 끼우면 **길이가 바뀝니다** — 화면이 말하는 길이 다섯 곳(`promo.html`·`today.html`·`js/today-page.js`·`js/idx-about.js`·`admin.html`)을 함께 고쳐야 합니다. ⛔ 브라우저 검사에는 그 숫자를 **못 박지 마세요** — `promo.html` 프리셋을 **읽어서** 기대값을 만듭니다(못 박아 뒀다가 6건이 깨진 전례) |
| 로그인 벽을 «맛보기» 로 열 때 | 🔴 **게이트를 여는 것과 «개인정보가 실릴 자리가 없는 갈래» 를 따로 내는 것은 다른 일입니다.** 2026-09-05 사장님 「처음 들어온 사람이 로그인 안 하면 볼 수 없으니 하루쯤 맛보기로」 — `/api/student/today` 를 게스트에게 열면 **학생 이름·수업 시각**이 그대로 나갑니다(그 게이트가 있는 이유). 대신 **`?sample=1`** 갈래를 **인증 게이트 «앞»** 에 두고 그 안에서는 **D1 을 한 줄도 안 읽습니다** — 개인정보가 실릴 자리가 아예 없습니다. ✅ 이미 등록된 경로의 **쿼리 파라미터**라 `src/index.ts`(공동 금지구역)를 한 줄도 안 건드립니다(같은 표의 「②가 금지구역이라 못 고칠 때」의 형제). ⛔ **값을 그럴듯하게 채우지 마세요** — 교재 이름을 지어 넣으면 화면이 «네 교재는 …» 이라고 말하게 되고(`SAMPLE_TEXTBOOK = null`), 수업을 넣으면 **「오늘 19시 수업」이라고 말했는데 그 시각에 아무도 안 옵니다**(`classes: []`). 이름도 `null` 입니다. ✅ 화면이 **«맛보기» 라고 직접 말하게** 하세요 — 안 말하면 그 계획을 «내 계획» 으로 읽습니다. ⛔ **맛보기 레벨을 기기에 심지 마세요** — 이 저장소의 레벨 심기는 «비어 있을 때만» 이라, 한 번 심으면 나중에 **진짜 레벨이 와도 안 덮입니다.** ⛔ 0 인 값(연속일·포인트)을 그리지 마세요 — 사실이지만 그 화면에서는 «너는 아무것도 안 했다» 로 읽힙니다. ✅ 창(하루)은 **저장이 막힌 기기에서 «열어 주는» 쪽으로** 실패해야 합니다 — 못 보여주면 고치려던 그 벽이 그대로입니다. ✅ 감시는 문자열이 아니라 **위치와 실행으로** — 「갈래가 게이트보다 앞인가」(`indexOf` 대조) · 「그 갈래 안에 `env.DB` 가 없는가」 · 「정본을 그 값으로 실제로 돌리면 «집에서 하는 날» 계획이 나오는가」 · **「본 화면의 게이트가 그대로 살아 있는가」**(짝으로 두지 않으면 «전부 열기» 도 통과합니다) |
| 화면을 «맛보기» 로 열었더니 **없던 데이터가 리포트에 생김** | 🔴 **로그인을 열면 «그 사람이 남기는 행» 도 함께 열립니다 — 그 표를 «학생 구분 없이» 읽는 곳이 있으면 거기부터 오염됩니다.** 2026-09-05 판단력 훈련을 맛보기로 열 때: 게스트가 문제를 풀면 `judgment_events` 에 `student_uid='guest_xxxxxxx'` 행이 쌓이는데, `decision-graph.ts` 의 Neo4j ETL 이 그 표를 **WHERE 없이 통째로** 읽어 `Student` 노드와 취약 스킬 집계를 만듭니다 ⟹ **«있지도 않은 학생» 이 선생님 리포트에 올라옵니다.** ⚠️ **화면만 고치고 끝냈으면 못 봤을 자리입니다** — 에러가 안 나고, 다음 ETL 이 돌 때까지 아무 일도 안 일어납니다. 🔴 **그리고 «한 곳» 으로 끝나지 않습니다** — 2026-09-05 실측: `judgment_events`·`judgment_analysis` 를 학생 구분 없이 읽는 자리가 **넷**이었고 처음엔 ETL 하나만 막았습니다(함정 대조가 셋을 더 찾음). 나머지 셋: **밴드별 정답률 통계**(그 코드 주석이 「이 수치가 이 기능의 **유일한 성공 지표**」라고 적어 둔 자리 — 대충 눌러 본 사람이 섞이면 **실제 학생의 난이도를 잘못 내립니다**) · **야간 성장 스냅샷 cron**(게스트마다 행 생성) · **엔벨로프 외부 내보내기**(상황문과 «학생이 쓴 이유 원문» 이 밖으로 나갑니다). ✅ 맛보기·게스트를 여는 변경에서는 **그 사람이 쓰는 표를 읽는 곳을 전부 세세요**(`grep 'FROM <표>'`) — 사람별 조회(`WHERE student_uid = ?`)는 안전하지만 **집계·ETL·내보내기가 위험합니다**. ✅ 감시는 **목록으로** 두세요(새 집계가 생기면 한 줄만 더하면 같은 검사를 받습니다) — 한 곳씩 못 박으면 그 하나만 지켜집니다. ⛔ 패턴은 `guest%` 로(밑줄 없는 bare `guest` 도 화면이 만듭니다). ✅ 감시는 「그 표를 읽는 ETL 이 게스트를 빼는가」로 못 박으세요(변이로 실제 FAIL 확인) |
| 화면 위에 상자를 하나 «더» 얹을 때 (배너·안내줄) | 🔴 **그 아래에 «고르는 카드» 가 있으면 선택지가 스크롤 밖으로 밀립니다 — 그리고 큰 폰에서는 재현되지 않습니다.** 2026-09-05 판단력 훈련 맛보기 배너 실측: **137px** 배너에서 첫 설정 카드의 선택지가 **360×640 폰에서 y=739(화면 640)** 로 화면 밖이었고, **390×844 에서는 멀쩡**했습니다(그 폭만 보면 «문제 없음» 이라 답하게 됩니다). CLAUDE.md 의 「첫 화면에 «정하세요» 카드를 띄웠는데 아무도 안 고름」과 같은 뿌리인데, 그쪽은 «펼쳐진 설명 상자» 이고 이쪽은 «내가 새로 얹은 상자» 입니다. ✅ 상자를 얹으면 **가장 작은 폰(360×640)에서 «그 아래 첫 조작» 의 top 을 재세요** — `getBoundingClientRect().top < innerHeight`. ✅ 화면마다 필요한 만큼만 그리세요(여기서는 설정 화면에서만 «한 줄» 로 줄이고, 긴 설명은 «풀기 시작한 뒤» 에 붙였습니다 — 그 문구가 그때 뜻이 생기기도 합니다). 🪤 **상자를 조이다 이번엔 버튼이 «못 누르는» 크기가 됩니다** — 실제로 로그인 버튼이 **27px** 이 됐습니다(보이는데 누르기 어려움). 버튼 높이는 **상자 자신의 여백에서 갚으세요**(padding·margin 을 줄여 총높이 유지). ⚠️ 「보인다」·「눌린다」·「첫 화면 안에 있다」는 **전부 다른 값**입니다 — 셋 다 재세요 |
| 「이 코드가 저 코드보다 앞인가」를 **이름의 위치**로 잴 때 | 🔴 **그 이름이 앞뒤 어디에 있어도 통과합니다.** 2026-09-05 실측: 「상한을 세는 자리가 LLM 생성보다 앞인가」를 `body.indexOf('SAMPLE_SCENARIO_CAP') < body.indexOf('generatePersonalizedScenario')` 로 썼는데, 가드를 통째로 «생성 뒤» 로 옮기고 앞에 그 이름만 한 번 남겨 두는 변이가 **그대로 통과**했습니다. ✅ 물어야 할 것은 «이름이 어디 있나» 가 아니라 **«막고 돌아가는 return 이 어디 있나»**(`indexOf("error: 'sample_limit'")`)·**«세는 쓰기가 어디 있나»**(`indexOf('kvS.put')`) 입니다 — 그 둘은 옮기면 뜻이 바뀝니다. 📜 형제: 같은 표의 「게이트가 채운 «그 변수» 를 쓰는가」·「하니스가 자기가 새로 만든 상수를 잡아 통과」 |
| 「두 곳이 같은 판정을 쓰는가」를 **호출 수**로 셀 때 | 🔴 **«함수 선언» 줄이 함께 잡혀, 호출 하나를 지워도 통과합니다.** 2026-09-05 실측: `isSampleLimit(d)` 를 세니 **3건**이었는데 그중 하나가 `function isSampleLimit(d){` 선언이었습니다 → 두 실패 경로 중 하나에서 판정을 지워도 `>= 2` 를 만족해 초록불이었습니다. ✅ **호출 모양으로** 세세요(`if(isSampleLimit(d))`) — 선언은 안 걸립니다. ✅ 그리고 「선언은 정확히 1개」를 **짝으로** 두세요(복제가 되살아나는 것을 함께 막습니다). 📜 형제: 같은 표의 「«renderIntro(p)» 로 찾으면 «선언» 이 잡혀 호출을 통째로 지워도 통과」 — **같은 실수를 이 저장소에서 두 번째 밟았습니다** |
| «없는 것» 을 세는 칸(「금액 없음」·「첨부 없음」)을 만들 때 | 🔴 **그 칸이 «아예 존재하지 않는 종류» 까지 세고 있지 않은지 보세요.** 2026-09-04 실측: 결재 요약의 「금액 없음 N건」이 금액 칸을 받지도 않는 종류(긴급·휴가·문서)까지 세어, **같은 데이터에서 요약 6건 대 지출 정리 0건**이 나왔습니다(제 주석은 「같은 규칙」이라고 **틀리게** 적혀 있었습니다). 「빠뜨린 것」이 아니라 **원래 없는 것**을 세면 사람이 채우러 갔다가 채울 칸이 없습니다. ✅ 종류 목록은 **정본에서 만드세요**(`TYPES.filter(t => t.wantsCategory)`) — 손으로 적으면 종류가 늘 때 조용히 어긋납니다. ⛔ 그 목록을 SQL 에 넣을 때 **`map(() => '?')` 로 자리표시자를 만들지 마세요**(D1 바인드 100개 한도 규칙 — `d1_bind_limit_harness` 가 FAIL 냅니다). **콤마 문자열 한 개**를 바인딩해 `instr(?, ',' || 칸 || ',') > 0` 으로 씁니다. ✅ 검사는 **같은 데이터를 두 화면에 넣어 답이 같은지** 대조하세요 — 「그 조건이 있는가」로는 못 봅니다 |
| «되돌리는 기록»(취소·반품·환불 전표)을 원본과 **같은 모양으로** 만들 때 | 🔴 **그 순간 그 자신이 «완전한 지출 행» 이 되어 합계에 들어갑니다.** 2026-09-05 결재 취소 결재가 그랬습니다 — 원본의 `req_type`·`amount`·`currency`·`category` 를 그대로 복사해 만들었더니, 정본을 돌려 실측한 결과 ⓐ 취소를 **올린 순간** 그 돈이 **두 번**(승인 500,000 + 대기 500,000) 세어지고 ⓑ 취소가 **승인돼도 총액이 한 푼도 안 줄었습니다**(원본이 빠진 자리를 취소 결재가 그대로 채움). ⚠️ **제 주석은 정반대를 적어 두었습니다** — 「금액의 부호를 뒤집지 않는다 — **원본이 합계에서 빠지므로**」. 그 전제가 「취소 결재 자신은 안 세어진다」인데 실제로는 세어집니다. ⚠️ **에러가 안 납니다** — 숫자만 틀립니다. ✅ 금액을 **지우지는 마세요** — 결재자가 «얼마짜리를 없애는지» 봐야 합니다. 대신 **«이 행이 지출인가» 를 묻는 정본 함수**를 두고(`isSpendRow` — `reverses_id` 가 있으면 false) 합계·항목·달별 집계가 **전부 그것을 지나게** 하세요. ⚠️ SQL 쪽도 짝입니다 — 맨 위 요약(`WHERE status='approved'`)·중복 감지·이번 달 합계에 **`AND reverses_id IS NULL`** 을 함께 걸어야 합니다(안 걸면 「같은 금액을 또 올렸다」는 거짓 경고까지 붙습니다). ✅ 검사는 **원본과 되돌림을 «짝» 으로** 넣어 «대기 중일 때»와 «승인 뒤» 둘 다 세세요 — 한쪽만 넣으면 그 절이 통째로 헛돕니다 |
| 목록 응답이 **본문을 잘라** 주는데 그 값을 다시 «폼에 채울» 때 | 🔴 **내용이 말없이 바뀌어 다시 올라갑니다.** 결재 목록은 `rowOf(…, brief)` 로 본문을 **300자**로 줄여 주는데, 「이 내용으로 다시 올리기」가 그 목록에서 찾아 채웠습니다 — 서버는 4,000자까지 받고 화면은 「채웠습니다」라고만 합니다. 「수정」을 대신하는 자리라 **그게 제일 나쁩니다**(2026-09-05 함정 대조가 잡음). ⚠️ **검사가 구조적으로 못 잡습니다** — fixture 본문이 짧으면 잘릴 일이 없어 언제나 통과합니다. ✅ 서버가 **«잘렸다» 를 말해 주고**(`body_truncated`) 화면이 **원문을 받아 온 뒤** 채우게 하세요. ⛔ **못 받아 오면 아무것도 채우지 마세요** — 잘린 채로 채우고 「채웠습니다」라고 하는 쪽이 더 나쁩니다. ⚠️ 그 단건 조회는 목록·첨부와 **같은 판정**(`canView`)을 써야 합니다 — 거기만 느슨하면 새 구멍입니다 |
| 브라우저 검사에서 「**남의 것에는 안 뜬다**」를 볼 때 | 🔴 **그 카드가 «내 카드와 같은 경로» 로 그려지지 않으면 검사가 통째로 헛돕니다.** 2026-09-05 실측: 남의 건 fixture 를 `inbox`(결재함)에만 뒀더니 그 카드는 `decidable=true` 경로로 그려져 **`.mineacts` 가 애초에 안 생겼고**, `isMine` 을 **`return true` 로 되돌려도 36건 전부 통과**했습니다(`!decidable && isMine` 조합이 한 번도 안 걸림). ✅ 남의 건을 **내 카드와 같은 경로**(여기서는 `urgent`·문서함 결과)에도 하나 넣고, **「그 경로로 그려졌는가」를 전제 검사로 박아 두세요** — 안 박으면 다음 사람이 fixture 를 옮길 때 또 조용히 헛돕니다. 📜 같은 줄기: 이 표의 「「남의 것은 안 샌다」만 보면 아무것도 안 세는 코드가 통과」·「검사가 헛돌며 통과」 |
| 하니스가 「기준선 이탈 — SKIP 이 늘었다」고 하는데 **내 변경과 무관해 보임** | ⚠️ **되돌려서 한 번 더 재 보세요 — 불안정한(flaky) 하니스가 있습니다.** 2026-09-04 실측: `--fast` 첫 실행이 `SKIP 20 → 21` 로 나와 제 변경을 의심했는데, `git stash` 로 되돌려 두 번 더 돌리니 **양쪽 다 20** 이었습니다(그때만 `azure_pronunciation_harness` 가 SKIP). ✅ 판정은 **변경 전/후 SKIP 목록을 파일로 저장해 `comm -13` 으로 대조**하세요 — 숫자만 보면 «어느 검사가 빠졌는지» 를 못 봅니다. ⛔ 그렇다고 경고를 무시하지는 마세요 — 검사가 조용히 빠지는 일이 이 저장소에 실제로 있었습니다. ⚠️ 기준선(`test-harness/baseline.json`)은 `{"fast":{"pass":140,"skip":20}}` 로 **PASS 쪽이 한참 옛 값**이라(실제 277) 사실상 SKIP 만 감시하고 있습니다 |
| 「배포됐나」를 **워크플로 초록불로 판정**할 때 | 🔴 **Workers 정적자산은 «바뀐 것만» 올라갑니다** — `?v=` 를 안 올렸거나 파일이 그대로면 `🌀 Found **0** new or modified static assets` 가 되고, **아무것도 안 나갔는데 배포는 성공**입니다(그 반대도 있습니다 — 병합만 하고 배포 run 이 «cancelled» 로 정리되는 경우). ✅ 판정은 러너 로그의 **그 줄과 바로 아래 파일 목록**(`+ /js/idx-main.js` 처럼 이름이 찍힙니다)으로 하세요. ⚠️ 이 컨테이너는 프록시가 `mangoi.ai`·`test.mangoi.co.kr`·`*.workers.dev` 를 **전부 403**(정책 거부)으로 막아 파일을 직접 못 받으므로, **그 로그가 «실제로 올라갔나» 의 가장 강한 근거**입니다(상태 확인: `curl -sS "$HTTPS_PROXY/__agentproxy/status"` 의 `recentRelayFailures`). ⛔ 정책 거부는 우회하지 말고 그대로 보고하세요. ✅ 근거는 **세 겹**으로 — ① `git show origin/main:<파일>` 로 «병합된 커밋에 그 코드가 있나» ② 위 업로드 목록 ③ 헬스체크·`BUILD_STAMP`·`X-Turn-Source`. ⚠️ 그래도 «학생 브라우저에서 보인다» 는 아닙니다 — 사람이 F12 콘솔에서 `[...document.scripts].find(s=>s.src.includes('idx-main')).src` 로 `?v=` 를 한 번 봐야 끝입니다(4-1-1 「CI 통과 ≠ 화면이 맞다」). 📄 `docs/작업기록/260906_지터버퍼_배포와_반영확인_재측정예약.md` |
| 「효과는 배포 «뒤에» 재겠다」고 미룰 때 | 🔴 **배포 «전» 기록에 «갈라 볼 축» 이 없으면 뒤에 가서 아무것도 못 가릅니다.** 2026-09-09 지터버퍼 재측정 실측: 겨냥한 구간(RTT 300~450) conceal **7.82% → 1.06%**, 같은 사람 8명 중 7명 개선 — 그런데 **그 사람들의 RTT 도 절반**(유세영 588→312 · Farrah 494→235)이었고, 배포 전 626분 중 **585분은 `path` 칸이 «모름»**(9/3 부터 기록)이라 «길이 바뀐 것» 인지 «버퍼 덕» 인지 **원리상 가를 수 없었습니다.** 밴드 평균만 보고 「내려갔다」고 적었다면 이 표가 가장 나쁘다고 못 박은 «전제가 틀렸는데 맞았다고 결론» 이 됩니다. ✅ 효과 측정을 예약할 때 **「무엇이 함께 바뀌면 판정이 무효인가」(여기서는 RTT·path)를 먼저 적고, 그 칸이 배포 전부터 쌓이고 있는지 확인**하세요 — 안 쌓이고 있으면 판정 기준을 «밴드 평균» 이 아니라 **«같은 사람 · 같은 경로»** 로 미리 낮춰 두고, 실제 비교 창을 «배포 후 첫 주 대 둘째 주» 로 옮기세요. ⚠️ 덤이 있습니다 — 가르려고 함께 뽑은 축(`path`·`turn`)이 **본래 질문보다 큰 신호**를 냈습니다: relay-tcp 4명 conceal **10.46%** 대 direct 9명 **0.61%**, relay-tcp 의 conceal/aloss **19배**(= 버퍼가 겨냥한 «늦어서 버린 것» 모양)인데 그 인구는 RTT 206 이라 **버퍼 게이트(`rb >= 300`)에 안 걸립니다** — 게이트가 «먼 사람» 이 아니라 «우회로 탄 사람» 을 봐야 할 가능성. ⛔ 그렇다고 그 자리에서 게이트를 바꾸지 마세요 — 대가(+300ms 지연)가 걸린 결정이라 사람이 정할 일이고, 표본도 34분·4명입니다. 📄 `docs/작업기록/260906_지터버퍼_배포와_반영확인_재측정예약.md` §8 |
| 제약이 거부될 때 «원래 인자로 다시 부르는» 폴백을 둘 때 (`getDisplayMedia`·`getUserMedia`) | 🔴 **«사람이 취소를 누른 것» 까지 재시도하면 선택 창이 «두 번» 뜹니다.** 2026-09-06 화면 공유에 fps 상한을 걸며 「제약을 거부하는 브라우저에서는 원래 인자로 한 번 더」라는 폴백을 뒀는데, 그 자리로 오는 오류에는 `OverconstrainedError`(제약 거부)만이 아니라 **`NotAllowedError`·`AbortError`(사람이 «취소» 를 누름)** 도 옵니다 — 가르지 않으면 취소할 때마다 공유 선택 창이 다시 떠서 「창이 안 닫힌다」가 됩니다(넣기 «전» 에 잡았습니다). ✅ **재시도는 «제약이 원인일 때» 만** 하고 취소 계열은 그대로 던지세요. ⚠️ 검사에 «거부되면 재시도한다» 만 넣으면 이 결함이 **그대로 통과합니다** — **«취소는 재시도 «안» 한다» 를 짝으로** 넣으세요(변이시험에서 실제로 3건 FAIL). ✅ 그리고 그 상한은 **`idx-main.js` 가 아니라 defer 에서** 겁니다(첫 화면 여유 116바이트) — 약점은 «그 API 이름이 바뀌면 조용히 헛돈다» 이고, `screen_share_fps_harness` 가 «호출처가 여전히 그 이름인가» 로 못 박습니다. ⛔ 화면 공유의 **해상도는 조이지 마세요** — 2026-08-11 에 `contentHint='detail'` 로 «글자 선명함이 먼저» 라고 일부러 정한 자리입니다(카메라는 정반대인 'motion'). ⛔ **PC 카메라 fps 도 내리지 마세요**(2026-09-06 사장님 결정 — 회선이 나쁘면 적응 루프가 이미 내리므로 기준값을 내리면 «좋은 회선만» 손해입니다). 📌 **덧붙임(2026-09-08) — «껐다 켜는 플래그» 로 가르면 그 판정이 아예 없는 것과 같습니다.** 웜업에 `response_format` 을 켜며 `try { … } catch (e) { if (!rfOn) throw e; rfOn = false; 다시부르기 }` 로 썼는데, **`!rfOn` 은 «이미 껐나» 를 물을 뿐 «제약 탓인가» 가 아닙니다** — 플래그가 매 요청 true 로 시작하므로 **첫 호출의 429(뉴런 소진)·타임아웃·5xx 가 전부 그 분기로 들어옵니다.** 결과 셋: ⓐ 무관한 일시 장애 한 번에 플래그가 꺼져 **그 요청의 남은 경로가 JSON 모드를 잃고**(= 고치려던 회귀가 되살아남) ⓑ 로그가 「response_format rejected」라는 **틀린 진단**을 남기고 ⓒ 429 에 쓸모없는 재시도가 붙습니다(같은 표 목소리 폴백의 「429 는 제외 — 답이 같습니다」와 같은 자리). ✅ **오류 «내용» 을 보는 판정을 따로 두세요**(`isRfRejection` — 429·5xx·timeout·abort 를 **먼저 거르고** 그 뒤에 제약 낱말을 봅니다). 🪤 **그 검사를 «막는 메시지 목록» 으로만 채우면 헛돕니다** — 「429 Too Many Requests」는 제약 낱말이 없어 **먼저 거르는 줄을 통째로 지워도** 저절로 false 라 초록입니다(실측). **두 낱말이 겹치는 메시지**(「400 Bad Request — rate limit exceeded」·「timed out while validating response_format」)를 반드시 섞으세요 — 그때만 «어느 쪽이 이기는가» 가 검사됩니다. 🪤 그리고 **상한 상수를 «선언» 으로만 검사하지 마세요** — `const MAX = 320` 은 그대로 두고 **호출부만 `max_tokens: 200` 으로 되돌리는** 변이가 통과합니다(실측). 「호출 옵션이 그 상수를 쓰는가 + 숫자를 다시 적지 않았는가」로 물으세요 |
| 응답에 «누가/무엇» 을 함께 내려주려 할 때 — 그 칸이 «이름» 인 줄 알았는데 **학생 로그인 아이디** | 🔴 **«이름 칸» 이라는 이름을 믿지 말고 «그 값이 어디서 왔는지» 를 따라가세요.** 2026-09-08 동시녹화 게이트에서 실제로 밟았습니다 — 거절 응답에 「누가 찍고 있나」로 `recordings.teacher_name` 을 실었는데, 그 칸은 화면의 **아이디 입력칸**(`index.html` 의 `#vc-name-input`, `autocomplete="username"`)에서 옵니다. 실측으로 그 자리에 `ysyt01`·`mby1`(학생 계정)이 그대로 찍혀 있었습니다. ⚠️ 하필 `/api/recordings/start` 는 **무인증**이고 방 번호가 `class-{예약id}-{YYYYMMDD}` 로 **열거 가능**해서, «녹화가 도는 모든 수업» 에서 그 방 학생의 아이디를 아무나 받아 가는 통로가 됩니다(이 서비스에서 아이디는 곧 비밀번호 — 같은 표의 「공개 API 에 학생 아이디를 실어도 되나」 항목). ⚠️ **에러가 안 납니다** — 기능은 정상 동작하고 새는 것은 값 하나뿐이라 타입체크·하니스가 전부 초록이었습니다(함정 대조가 잡았습니다). ✅ 물어야 할 것은 「이 값이 필요한가」가 아니라 **「이 값이 어느 입력칸에서 오는가」** 입니다 — 서버가 만든 값(예약표의 이름)과 **클라이언트가 보낸 값**은 다릅니다. ✅ 대개는 **그냥 빼면 됩니다** — 사람에게는 「다른 기기에서 녹화 중」 하나로 충분했고, 화면은 이름 없이도 그립니다. ✅ 정본 타입에 **⛔ 주석을 그 칸 옆에** 달아 두세요(다음 사람이 다시 실을 자리입니다). 감시는 `recording_dup_guard_harness` C-5·D-11(응답 객체를 **중괄호 짝으로 잘라** 이름이 없는지 봅니다 — ⚠️ 범위를 넓게 잡으면 SQL 의 `AS teacher_name` 별칭에 걸려 거짓 FAIL 이 납니다) |
| `CREATE TABLE` 이 **여러 벌**이라 DEFAULT 가 환경마다 갈림 | 🔴 **`INSERT` 에서 칸을 빼고 DEFAULT 에 기대면, 그 표를 만드는 코드가 둘 이상일 때 조용히 갈립니다.** 2026-09-08 실측: `recordings.status` 는 `schema.sql` 에 `DEFAULT 'recording'` 이 있는데 **런타임 `CREATE TABLE IF NOT EXISTS recordings` 세 곳**(`api-mango.ts` 2 · `index.ts` 1)에는 **없습니다.** 운영 DB 는 앞쪽으로 만들어져 멀쩡하지만(D1 `sqlite_master` 로 확인), 그렇지 않은 환경에서는 `status` 가 NULL 이라 ① 파트 업로드가 통째로 거절되고(`recordings-r2.ts` 의 `status !== 'recording'`) ② 그 칸을 보는 게이트가 **영원히 0행** 을 봐서 «넣었는데 한 번도 안 도는» 상태가 됩니다. ⚠️ **에러가 안 납니다** — 조회가 0행일 뿐입니다. ✅ **`INSERT` 에 값을 명시하세요** — 운영 DEFAULT 와 같은 값이면 기존 동작은 안 바뀌고, 어느 환경에서든 확실해집니다. ⛔ 런타임 `CREATE TABLE` 쪽을 고쳐서 맞추려 하지 마세요 — 이미 있는 표는 그대로이고 새 환경만 바뀝니다(같은 표의 「`students_erp` 에 `id` 컬럼이 없습니다」와 같은 뿌리). ✅ 확인은 `SELECT sql FROM sqlite_master WHERE name='<표>'` 한 줄 — **추측하지 말고 운영 DB 의 실제 정의를 보세요** |
| 모델 출력을 **JSON 으로 받게 바꿨더니** 잘린 응답이 «생 JSON» 으로 학생 화면·TTS 에 그대로 나감 | 🔴 **잘림은 «드문 예외» 가 아니라 «가장 흔한 실패 모양» 입니다 — 그리고 잘리면 정의상 닫는 중괄호가 없습니다.** 2026-09-08 웜업 교정 카드에서 실제로 밟았습니다: 파서가 `const e = body.lastIndexOf('}'); if (s >= 0 && e > s) {…}` 라, 닫는 중괄호가 없으면 그 블록을 **통째로 건너뛰고 원문을 그대로** 돌려줬습니다. ⚠️ **하류 안전망도 안 잡습니다** — 무너진출력 게이트(`replyRejectReason`)에 그 덩어리를 넣어 봐도 «차단 사유 없음» 이라 말풍선에 그려지고 TTS 가 읽고 KV 히스토리에 저장됩니다. ⚠️ **원인은 «예산» 입니다** — `max_tokens` 는 그대로인데 그 안에 JSON 껍데기 + 교정(토큰이 비싼 한국어 설명 포함)이 함께 들어가면서 본문 예산이 줄어, **바꾸기 «전» 보다 잘림이 잦아집니다.** ✅ **본문을 JSON 첫 키로 두세요** — 잘려도 그것만은 이미 끝나 있습니다. ✅ 회수는 «닫는 중괄호가 있을 때만» 이 아니라 **여는 중괄호부터 끝까지** 로 하세요(`e > s ? slice(s, e+1) : slice(s)`). ✅ 그러고도 못 읽으면 **빈 문자열**로 두어 기존 안전문구가 받게 하고, **마지막 안전망**으로 «JSON 흔적(`{"`·`"reply":`)이 남아 있으면 내보내지 않기» 를 두세요. ⛔ 그 안전망을 «맨 앞이 중괄호일 때만» 으로 좁히지 마세요 — 모델이 앞에 한마디 붙이면(`Here you go: {…`) 그대로 새어 나갑니다. ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무엇이 돌아오는가» 뿐입니다. 검사에 **잘린 입력을 실제로 넣어** 보세요(끝이 잘린 것 · 본문 자체가 잘린 것 · 앞에 말이 붙은 것 셋). ⛔ 그리고 「중괄호를 **절대** 안 내보낸다」처럼 **완료형으로 적지 마세요** — 저는 닫는 중괄호가 있는 입력 하나만 시험하고 그렇게 적었고, 그 문장 때문에 아무도 다시 안 볼 뻔했습니다 🔴 **덧붙임(2026-09-08, 같은 날 저녁 실사고) — 그 파서는 «문자열이 온다» 는 전제 위에 서 있었는데 그 전제가 틀렸습니다.** **Workers AI 는 `response_format: {type:'json_object'}` 를 켜면 `response` 를 «이미 파싱된 객체» 로 주기도 합니다.** 그러면 `String(raw)` 가 **`"[object Object]"`** 가 되고, 그 글자에는 **중괄호가 없어서** 위에 적은 안전망 셋(코드펜스 벗기기·잘림 회수·「JSON 흔적이 남으면 안 내보내기」)을 **전부 비켜 갑니다** — 평문으로 판정되어 학생 말풍선·TTS·KV 히스토리로 그대로 나갔습니다(사장님 「이렇게 잘 못나와object 이 뭐야??」 — 세 턴 연속). ⚠️ **JSON 모드를 켠 바로 그 변경이 스스로 만든 회귀**라 «켜기 전» 에는 절대 재현되지 않습니다. ✅ **저장소에 답이 이미 있었습니다** — `api-diary.ts` 가 「JSON 모드면 response 가 이미 객체이거나 JSON 문자열. **둘 다 대응**」이라 주석까지 달아 두었고 `api-approval.ts` 의 `parseLooseJson` 첫 줄이 `if (raw && typeof raw === 'object') return raw;` 입니다. **새 옵션을 켤 때는 그 옵션을 이미 쓰는 자리들이 «결과를 어떻게 읽는지» 부터 보세요** — 저는 「여덟 곳에서 쓴다」고 세어 놓고 «어떻게 읽는지» 는 안 봤습니다. ✅ 고침은 **객체면 그 자리에서 꺼내고**(`typeof raw === 'object'` → `raw.reply`), 모양이 다르면 `String()` 이 아니라 **`JSON.stringify()`** 로 글자로 되돌려 기존 문자열 경로에 태우는 것입니다. ✅ 그리고 **마지막 안전망을 하나 더** 두세요 — `[object …]` 가 남아 있으면 내보내지 않습니다(배열·중첩이 굳은 변종까지 덮습니다). 🔴 **그 안전망은 «입력 글자» 가 아니라 «돌려줄 reply» 에 거세요** — 객체 분기가 그보다 «앞» 이라, 입력에 걸면 **객체 경로가 통째로 비켜 가** `{ reply: "[object Object]" }` 가 그대로 학생에게 나갑니다. 제가 실제로 그렇게 짜 놓고 이 자리에 «덮습니다» 라고 **완료형으로 적었고**, 정본을 돌려 본 함정 대조가 잡았습니다(바로 이 행 끝에 「완료형으로 적지 마세요」라고 제가 적어 둔 그것을 그대로 다시 어겼습니다). ℹ️ reply 에 걸면 덤으로 «답장은 멀쩡한데 `fix.why_ko` 에 그 글자가 있어 답장까지 버리는» 일도 없어집니다. ⚠️ 검사 목록에 **`{ reply: '[object Object]' }` 를 반드시 넣으세요** — 그것이 빠져 있으면 「어떤 모양으로 와도」라는 검사 **이름이 실제 보장보다 넓어집니다**(그 값 하나가 없어서 통과하고 있었습니다). ⚠️ **호출부도 같은 병의 두 번째 입구입니다** — `r.response || r.result` 처럼 «응답 객체» 를 통째로 넘기면 파서가 아무리 튼튼해도 같은 자리로 옵니다. 저장소 다섯 곳이 쓰는 **`r?.response ?? r?.result?.response`** 로 맞추세요. ⚠️ **문자열만 넣어 보는 검사로는 원리상 못 잡습니다** — 검사에 **객체를 실제로 넣으세요**(정상 객체 · `reply` 없는 객체 · 배열 · `reply` 가 숫자인 것). ⛔ 그때 «객체를 막는가» 만 두면 **«전부 빈 문자열» 도 통과합니다** — **«정상 객체에서 reply 를 꺼내는가» 를 짝으로** 두세요. ✅ 확인은 **실사고 당시 코드로 되돌려 보는 것** — 그러면 검사가 화면에 찍힌 그 글자(`"[object Object]"`)를 그대로 뱉으며 4건 FAIL 합니다(변이 4종 전부 실제 FAIL 확인). 감시는 `test-harness/warmup_correction_harness.mjs` B-7·B-7b·B-7c·B-7d·A-10 |
| WebAudio 로 마이크를 가공한 뒤 **«되돌릴» 때** | 🔴 **가공 트랙에는 장치 id 가 없습니다 — 그것을 `deviceId:{exact:…}` 로 넘기면 `OverconstrainedError` 가 나고, 되돌리기가 통째로 실패해 «무음 트랙» 이 그대로 남습니다**(= 껐는데 소리가 아예 안 나감. 켜져 있는 것보다 나쁩니다). 2026-09-08 사무실 모드(`js/idx-vc-officemode.js`)에서 실제로 밟았습니다 — `currentMicId()` 가 «지금 트랙» 에서 읽었는데 그때 그 트랙은 `MediaStreamDestination` 이 만든 가공 트랙이었습니다. ⚠️ **에러가 화면에 안 뜹니다** — `console.warn` 한 줄만 남고 사람에게는 「사무실 모드를 껐더니 학생이 내 말을 못 듣는다」로만 보입니다. ✅ **켤 때 «진짜» 장치 id 를 기억해 두고**(`srcMicId`) 되돌릴 때 그것을 쓰세요. ✅ 되돌리기는 «절대 실패하면 안 되는» 경로라 **2단 폴백**을 두세요 — 기억한 장치로 잡아 보고, 안 되면 장치 지정 없이 한 번 더(⛔ 장치 지정을 «먼저» 포기하진 마세요. 교사가 고른 마이크가 아닌 것으로 바뀌는 것도 사고입니다). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무엇을 읽는가» 뿐이라 그때도 `--fast` 가 전부 초록이었습니다. 브라우저에서 **가짜 마이크**(`--use-fake-device-for-media-stream`)로 «실제로 켜고 꺼» 봐야 드러납니다. ⚠️ 그 검사에 **「켜졌는가」 전제를 짝으로** 두세요 — 켜기가 실패하면 끄기가 no-op 이 되어 **무엇을 되돌려도 통과합니다**(변이시험에서 실제로 그 상태였습니다). 🪤 곁가지 — **`#vc-dock-settings` id 가 «둘» 입니다**(`vc-dock.js:603` 설정 팝오버 · `848` 설정 «버튼»). `querySelector('#vc-dock-settings …')` 가 문서 순서상 먼저 나오는 쪽을 집어 검사가 회차마다 들쭉날쭉했습니다 — 그 안의 것을 찾을 때는 id 말고 **그 요소 고유의 속성**으로 찾으세요. 📌 **덧붙임(같은 날 함정 대조) — 그 브라우저 검사 34종이 «전부 통과» 하면서 결함 넷을 더 놓치고 있었습니다**(「보인다·눌린다·켜진다」만 재고 «언제 걸리는가»·«무엇을 읽는가» 를 안 봤습니다). 🔴 ① **입장은 `showView('view-videocall-call')` 가 «먼저», `body.vc-in-call` 이 «나중» 입니다**(`idx-main.js:2869~2870` · 관찰자 3830~3831도 같음) — 그래서 showView 를 감싸 «수업이면 건다» 를 `inCall()` 로 판정하면 그 순간 아직 false 라 **한 번도 안 걸립니다**(퇴장은 반대로 class remove 가 먼저라 «끄기» 는 정상 동작해, 켜기만 조용히 죽습니다). ✅ 전환 «대상»(인자)으로 가르거나, 폴링이 «입장할 때까지» 기다리게 하세요. 🔴 ② **«사람이 껐다» 와 «수업이 끝났다» 를 안 가르면 설정이 다음 수업으로 안 넘어갑니다** — 퇴장 훅이 부르는 그 함수가 저장값까지 지웁니다(실측: 나가기 전 `'1'` → 나간 뒤 `'0'`). ⚠️ ①과 ②는 **서로를 가립니다**(저장값을 안 읽고 + 남지도 않아 어느 쪽도 증상으로 안 보입니다). 🔴 ③ **`vcSwitchMic` 은 «직접» 불리는 경로가 둘입니다** — `index.html` 의 `<select onchange=…>` 와 강사→학생 「장치 도우미」 원격 전환. 그 함수는 `vcLocalStream` 의 오디오 트랙을 **전부 stop·remove** 하므로 가공 트랙이 날아가는데, 감싸지 않으면 상태 함수가 계속 «켜짐» 이라 **「켜졌다고 말하는데 아무 일도 안 하는」** 상태 + 타이머·AudioContext·두 번째 마이크 캡처가 수업 내내 남습니다. 🔴 ④ **마이크 자가치유(`vcHealLocalMic`)가 원리상 못 돕니다** — 그것은 「살아 있는 트랙이 있으면 그만둔다」인데 **WebAudio 가공 트랙은 원본 마이크가 죽어도 계속 `live`** 입니다(실측). USB 를 뽑으면 교사가 조용히 무음이 되고 안내도 안 나옵니다 → 원본 트랙의 `ended` 를 듣고 **스스로 손을 떼세요**(저장값은 지킨 채). 🪤 **그리고 제 검사가 «측정하지 않은 전제» 위에 서 있었습니다** — 「가공 트랙에는 `deviceId` 가 없다」로 «진짜 마이크인가» 를 판정했는데 **재 보니 있습니다**(`deviceId="WebAudio-<uuid>"` · `label="MediaStreamAudioDestinationNode"`). 그래서 한 검사는 **늘 통과**하고 다른 검사는 **정상 코드를 «모순» 으로 오판**했습니다. ✅ 판별은 그 두 신호로 하고, **「판별식이 두 상태를 실제로 가르는가」를 전제 검사로** 두세요(안 두면 그 위의 검사가 통째로 헛돕니다). ⚠️ 부정 검사(«이 이름이 없어야 한다»)는 **주석을 벗겨 낸 사본**으로 판정하세요 — 죽은 키 이름을 «왜 이렇게 했는지» 설명하려고 주석에 남기면 검사가 자기 주석을 잡습니다(여기서 실제로 밟았습니다). 감시는 `test-harness/manual/vc-office-mode-browser.mjs`(브라우저 **45종** — 자동으로 안 돕니다, 사람이 부릅니다. 변이 **Ⓐ~Ⓓ 4종 전부 실제 FAIL**). ⚠️ **못 잰 것**: 원본 마이크 `ended` 감시는 검사가 없습니다(`track.stop()` 은 표준상 `ended` 를 쏘지 않아 헤드리스에서 재현이 안 됩니다) · 실제 소음 억제 효과·문턱 값의 적절함(오디오 디코더가 없어 사람이 들어야 확정) · 피어가 실제로 붙은 상태의 `replaceTrack` 반영. ✅ **선생님 + 관리자 로그인**(2026-09-08 「선생님만 쓰게 막아줘」 → **2026-09-09 「사장님 계정도 보이게 넓혀줘」**) — 독 설정은 전원에게 열려 있으므로 설정을 **열 때마다** 물어 학생 화면에서는 그 줄을 감춥니다. 🔴 **축이 «둘» 입니다 — 합치지 마세요**: «보이기»(`canSee` = `vcIsStaffNow()` **또는** `mangoi_admin_session.uid`)와 «자동 켜기»(`isStaff` = `vcIsStaffNow()` 만). 지시가 둘이었고 범위가 다릅니다(「보이게」 대 「**교사**한테 항상 켜지는」). 합치면 **공용 PC 에 남은 관리자 세션으로 «학생으로 입장» 한 아이의 마이크**에까지 게이트가 자동으로 걸리는데, `idx-main.js:2622` 주석이 바로 그 confirm 을 「공용 PC 에 남은 관리자 세션으로 학생이 승격되는 길을 한 겹 막는다」고 못 박아 둔 방어선입니다(2026-09-09 함정 대조가 잡음). ⛔ 반대로 «누르는 것»(`enable`)까지 `isStaff` 로 좁히지도 마세요 — 사장님 화면에 **보이는데 눌러도 안 되는 버튼**이 남습니다. **enable 은 `canSee`, 자동은 `isStaff`** 입니다. ⛔ «강사가 아니면 학생»(`!vcIsStaffNow()`)으로 판정하지 마세요 — 그 판정은 **역할이 아직 확정되지 않은 강사를 학생으로 오판**합니다(같은 파일 `vcIsStudentNow` 주석의 8/10 사고). 여기서는 그 오판이 «교사가 자기 기능을 못 쓰는» 쪽이라 두 가지로 견딥니다 — 설정을 다시 열면 보이고(열 때마다 재판정), 수업 진입 자동 적용은 «교사가 될 때까지» 기다립니다. ⛔ **역할 정본을 고쳐서 풀지 마세요**(화면공유·교재 넘김·장치 도우미 권한이 함께 걸려 있습니다) — 여기서는 «묻기만» 합니다. ⛔ **«거절»(권한 없음)일 때 저장값을 지우지 마세요** — 역할이 늦게 온 강사의 설정이 그 한 번의 오판으로 사라져 «다음 수업에도 안 켜지는» 상태가 굳습니다. ⚠️ 그것과 **«켜다가 실패»(마이크·AudioContext)는 다른 사실**입니다 — 그쪽은 「아직 안 정함」(키 삭제)으로 되돌립니다. ✅ 그리고 **자동으로 켜졌을 때는 저장값을 «쓰지» 않습니다**(`enable(byUser)`) — 쓰면 첫 수업 한 번에 전 교사가 «사람이 켰음» 으로 굳어 나중에 기본값을 되돌릴 수 없습니다. ⛔ `el.hidden` 으로 감추지 마세요 — `.sg-row{display:flex}` 가 브라우저 기본 `[hidden]{display:none}` 을 이깁니다. ⚠️ 검사에 **「선생님에게는 보인다」를 짝으로** 두세요 — 없으면 «전부 감추기» 도 통과합니다(변이시험에서 실제로 확인). ℹ️ 이것은 보안 게이트가 아닙니다 — 사무실 모드는 «자기 마이크» 만 가공하므로 학생이 콘솔로 불러도 남에게 영향이 없습니다(화면을 어지럽히지 않는 것이 목적) |
| 「**남자 목소리**로 해줘」인데 그 언어의 서버 TTS 에 **화자가 하나뿐**일 때 | 🔴 **먼저 «그 언어가 화자를 가리는가» 부터 재세요 — 못 내는 소리를 약속하면 화면이 거짓말을 합니다.** [잰 것 — 2026-09-14] `src/api-games.ts` 의 `/api/voice/tts` 는 `lang` 이 zh 면 **`gtts(text,'zh-CN')` 로만** 갑니다. 그 함수에 **화자 인자가 없습니다** = 구글 만다린 «한 목소리»(여성)이고 `speaker` 는 캐시 키에만 쓰입니다(폴백 MeloTTS zh 도 화자 개념이 없고 «앙캉캉캉» 잡음 전력이 있습니다). ⟹ **서버로는 못 냅니다.** 📜 그래서 낮에는 «기기 목소리(`speechSynthesis`)가 유일한 길» 로 짜고 「없으면 그 사실을 말한다」를 하니스에 못 박았는데, **사장님 PC 에 그 음성이 없어 안내만 뜨고 여자 목소리가 났습니다** — 저녁에 「너가 알아서 중국어 말하는 남자목소리 넣어줘, 아무나」로 지시가 바뀌었습니다. ✅ **길은 둘이고 짝입니다** — ⓐ 기기에 진짜 남자 음성이 있으면 그것을 **서버보다 «먼저»**(⚠️ 뒤에 두면 서버가 먼저 읽습니다 — 검사도 «위치» 로 물을 것) ⓑ 없으면 **받은 오디오를 브라우저에서 «음높이만» 내려** 남자로 만듭니다(정본 `_zhDeepen`, `warmup.html`). ⚙️ ⓑ는 **두 단계이고 한 단계만 하면 둘 다 틀립니다** — ① `OfflineAudioContext` 에서 `playbackRate = P` 로 다시 그려 «음높이 P 배·길이 1/P 배» 를 만들고 ② 재생할 때 **`preservesPitch=true`** 로 `playbackRate = S/P` 를 주어 «길이만» 되돌립니다(`S` = 사람이 고른 배속). 🔴 **«`preservesPitch=false` 한 줄» 로 풀지 마세요** — 이 화면은 들어올 때 항상 2단계(**0.65**)에서 시작하므로 `0.65 × P` 가 되어 **«귀신 목소리»** 이고, 속도 슬라이더가 그대로 **음높이 슬라이더**가 됩니다(올리면 여자로 되돌아갑니다). ⛔ ②를 빼면 «느리고 낮은» 소리입니다(고치려던 것의 절반). 📐 [잰 것 — 크로미움에서 220Hz 사인파를 실제로 통과] `P=0.84` → 184.4Hz(비율 0.838) · **`P=0.78` → 171.2Hz(0.778)**, 재생 길이는 원본을 그 배속으로 들은 것과 **1ms 오차**로 같았습니다. 리샘플이라 **포먼트도 함께** 내려가 성별 전환에 유리합니다. ⛔ `0.7` 미만으로 내리지 마세요(«괴물 목소리» + 보정 배속 `S/P` 가 커져 time-stretch 왜곡도 커집니다). ⚠️ **소리는 사람이 들어야 확정입니다** — 이 환경에는 오디오 디코더가 없습니다(잰 것은 «음높이 비율·길이» 까지). ⛔ 그 갈래를 «그 언어 전체» 로 넓히지 마세요 — 여자 선생님은 서버 목소리 그대로가 맞습니다(짝 검사가 없으면 그 변이가 통과합니다). ⛔ **「기기에 없어서 여자 목소리로 읽어 드려요」 안내를 되살리지 마세요** — 이제 «항상» 남자 목소리라 거짓말이 됩니다(그 부정 검사를 짝으로 두세요). ⚠️ 굽기는 **비동기**라 그 사이 다음 말이 오면 옛 문장이 겹쳐 재생됩니다 — `_speakSeq` 로 스스로 물러나게 하고, **굽기가 실패하면 원본 그대로** 재생하세요(소리가 아예 안 나는 것이 최악). 같은 문장을 다시 굽지 않게 캐시하되 ⚠️ 그 검사를 「그 이름이 있는가」로 쓰면 **«저장하는 줄» 만 남겨도 통과합니다** — **«읽고 그 자리에서 돌아가는가»** 로 물으세요(실측으로 밟음). 🔴 **브라우저 음성(ⓐ)에는 성별 칸이 없습니다 — 이름뿐입니다.** Windows 는 `Kangkang`·`Yunxi`(남)·`Huihui`(여), Apple 은 `Li-mu`(남)·`Tingting`(여)인데 **안드로이드 구글 음성은 「Chinese (China)」** 라 **판별이 원리상 안 됩니다**(그래서 ⓑ가 필요합니다). ⛔ 「그 언어 음성이면 남자겠지」로 넓히지 마세요 — 기본 음성은 대개 여자라 **얼굴과 어긋납니다.** ✅ ⓐ의 «굵기» 는 **`u.pitch`** 로만 됩니다(0.85 안팎). ⛔ **전부 낮추지 마세요** — 영어 폴백 목소리까지 함께 바뀝니다(짝으로 검사). ⚠️ **`\b` 는 CJK 에서 안 먹습니다** — 「男」 같은 글자는 낱말 경계 밖에 따로 두세요(`/(\b(kangkang|…)\b|男)/i`). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «어느 목소리가 나는가» 뿐입니다. ✅ 판정을 **오려 내 실제로 돌리고**(⚠️ 한 줄짜리 함수는 정규식 «첫 `}`» 가 `try` 에서 끊깁니다 — **중괄호 짝으로** 자를 것), 「남자면 굵게 한다」 옆에 **「여자·영어는 안 건드린다」·「굵게 만든 판만 배속을 나눈다」를 짝으로** 두세요. 🚤 **그리고 「판정 함수가 옳은가」만 검사하면 아무것도 안 지켜집니다** — 실측: `if(!_zhMaleWanted())` 를 **`if(true)` 로 한 글자** 바꿔 굽기를 통째로 끄자(= 다시 여자 목소리) 그 하니스가 **126건 전부 초록**이었습니다. ✅ **그 판정을 «실제로 부르고 그 결과로 굽는가» 를 짝으로** 두세요 — `play` 를 오려 내 **가짜 부품으로 돌려** «어디로 가는가» 를 답으로 보고, `_speakSeq` 를 함수 호출로 바꿔 넣어 **«굽는 사이 번호가 바뀌는» 상황까지** 재현하세요(그래야 «성공 분기의 가드만» 지운 변이가 잡힙니다 — 글자로 물으면 실패 분기에 남은 것을 보고 통과합니다). ⚠️ 그 갈래는 **«캐시 단축» 보다도 앞**이어야 합니다(뒤로 옮기면 한 번 캐시된 문장은 영영 기기 목소리를 안 씁니다). 감시는 `warmup_zh_lang_harness` 목소리 갈래 절(변이 **20종** 전부 실제 FAIL — «조건 뒤집기» 포함)과 `manual/warmup-zh-friend-browser.mjs`(브라우저 — 220Hz 를 실제로 통과시켜 음높이·길이를 잽니다. 자동으로 안 돕니다, 사람이 부릅니다) |
| 이름표가 **그 언어에서만 기본값으로** 떨어짐 (「Mango」·「Unknown」) | 🔴 **«글자를 잘라내는» 방식은 그 언어에 눈이 없습니다 — 그리고 에러가 안 납니다.** 2026-09-14 실사고: 웜업 말풍선 이름이 `label.replace(/^[^A-Za-z]+/, '') || 'Mango'` 였는데, 영어 친구 라벨 `'👩 Emma'` 는 `Emma` 로 잘 되지만 **중국어 선생님 라벨 `'👨‍🏫 룽 선생님'` 에는 영문자가 한 글자도 없어** 정규식이 **전부 지우고** 폴백으로 떨어졌습니다 ⟹ 사장님 화면에 **「Mango」**. ⚠️ **룽만의 문제가 아니었습니다** — 메이도 같은 상태였는데 아무도 못 봤습니다(실측: 두 이름 다 `Mango`). ⚠️ 하필 그 자리는 **AI 가 자기를 부르는 이름과 짝**이라(서버 `AI_FRIEND_NAMES`), 갈리면 「이름이 왜 두 개죠?」가 됩니다. ✅ 그 언어의 이름을 **표에 따로 담고**(`zhName`) 읽는 쪽이 그것을 «먼저» 보게 하세요. ⛔ 정규식을 «CJK 도 남기게» 넓혀서 풀지 마세요 — 이모지·역할 표시(👩‍🏫)가 함께 남아 말풍선에 그대로 찍힙니다. ⛔ 그 값을 화면에만 적지 마세요 — **서버 표와 «같은 글자»** 여야 합니다(하니스가 두 표를 대조). ⚠️ **문자열 하니스로는 못 잡습니다** — 표도 값도 다 «있고» 틀린 것은 «무슨 글자가 찍히는가» 뿐입니다. ✅ 검사는 그 함수를 **오려 내 실제로 돌려** 답을 보고, **「영어 이름은 예전 그대로다」를 짝으로** 두세요(짝이 없으면 «전부 한자 이름» 이나 «전부 기본값» 도 통과합니다). ⚠️ 그리고 **「표에 그 칸이 있다」만 묻지 마세요** — 읽는 줄을 지워도 통과합니다(실측: 변이 Ⓛ). 감시는 `ai_friend_name_harness` ⑥절 |
| 한 화면에서 잘 도는 기능을 **다른 화면에도 «똑같이»** 붙일 때 | 🔴 **«판정» 은 한 벌로 두고 «프롬프트» 만 화면별로 두세요.** 2026-09-09 사장님 「A.i 친구도 똑같이 만들어줘」 — 웜업 교정 카드를 AI 영어친구에 붙였습니다. 그때 AI 친구는 답변 «본문 끝» 에 `(💡 ~가 더 자연스러워요)` 한국어 팁 한 줄을 붙이는 옛 방식이었는데, **«고친 문장» 이 없어서** 학생이 무엇을 어떻게 고칠지 알 수 없었습니다(실측: "I go to school yesterday" → 「어제에 가는 것이 더 자연스러워요」 · 같은 화면의 "I did breakfast yesterday" 는 팁조차 없었습니다). ✅ `parseWarmupOutput`·`verifyWarmupFix`·`decideWarmupFixShow`·`isRfRejection` 은 **그대로 재사용**하고 프롬프트만 `AI_FRIEND_CORRECTION_RULE` 로 따로 뒀습니다(정본 `src/warmup-correction.ts` — ⛔ 파일 이름이 warmup 이라고 웜업 전용으로 읽지 마세요). ⚠️ **fix 스키마는 «한 문자열» 이어야 합니다** — 화면마다 따로 적으면 그 순간 한쪽만 조용히 어긋나고 «파서·검증 한 벌이 두 화면을 받는다» 는 전제가 깨집니다(`fixJsonLine()` 하나가 두 규칙을 만듭니다). ⚠️ **그 대가로 영어 프롬프트에 한국어가 섞입니다**(실측 11줄 중 5줄 — 줄을 더하면 이 숫자와 소스 주석을 **함께** 고칠 것) — 「이 규칙은 영어다」라고 적지 마세요. 새어 나온 한국어는 하류 `stripAddedKorean` 이 받습니다(막지는 못합니다). ⛔ **옛 방식을 프롬프트에 남겨 두지 마세요** — 같은 말이 카드와 본문에 두 번 나옵니다. 화면에서도 `hasFix` 로 팁 칩을 억제하되 ⚠️ **«카드가 없을 때는 팁이 그대로 뜬다» 를 짝으로** 검사하세요(안 그러면 «전부 지우기» 도 통과합니다). ⚠️ **재시도가 여럿이면 «전부» 같은 파서를 지나야 합니다** — 한 곳만 빠져도 그 경로가 생 JSON 을 학생 화면·TTS 로 내보냅니다(하니스가 «모델 호출 수 == 파서 호출 수» 로 못 박습니다). ⚠️ 교정은 **«그 답장을 채택했을 때만» 확정**하세요(`commitFix`) — 재시도 답장이 거절될 수 있는데 파싱하자마자 덮으면 화면의 답장은 옛것인데 카드만 새 답장의 것이 됩니다. ⚠️ **옛 기능을 프롬프트에서 뺐으면 그것을 «일부러 넣었다» 고 적어 둔 다른 파일도 함께 고치세요**(`src/reply-korean.ts` 머리말이 그대로 남아 있었습니다 — 「같은 단정을 여러 곳에 적어 뒀으면 한 곳만 고쳐서는 안 끝납니다」). 🪤 이 변경은 **옛 문구·옛 인자 모양을 글자 그대로 못 박은 검사 둘을 깨뜨렸습니다**(「`NEVER translate your own English into Korean`」·「`temperature: 0.95`」) — 보장은 오히려 세졌는데 검사만 빨간불이라 **뜻으로 묻도록** 고쳤습니다. ⚠️ 그때 인자를 정규식으로 세면 `messages.concat([...])` 의 괄호에서 끊겨 **NaN** 이 됩니다 — **괄호 짝으로** 자르세요. 감시는 `test-harness/ai_friend_correction_harness.mjs`(53종 — 정본을 실제로 돌리고 두 규칙의 fix 스키마를 «만들어서» 대조. 변이 11종 전부 실제 FAIL)와 `test-harness/manual/ai-friend-fix-card-browser.mjs`(브라우저 18종 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 「몇 번째 턴인가」로 무언가를 «가끔만» 보여 줄 때 | 🔴 **그 값이 «단조 증가» 하는지, 그리고 «1일 때 어떻게 되는지» 를 실제로 돌려 보세요.** ① `history.length` 로 세면 그 조회가 `LIMIT 20` 이라 곧 상한에 붙고, `turnCount - lastShownTurn` 이 영영 0 이 되어 **그 기능이 통째로 멎습니다**(에러도 없고 화면도 멀쩡합니다). `SELECT COUNT(*)` 처럼 단조 증가하는 값으로 세세요. ⚠️ 웜업(`src/index.ts`)에는 그 모양이 아직 남아 있습니다 — 21턴 넘는 세션에서 교정이 멎습니다(공동 금지구역이라 안 고쳤습니다, **사람이 정할 일**). ② 🔴 **«N턴에 한 번» 규칙의 «초깃값 0» 이 «첫 번째» 를 원리상 막습니다** — `turnCount - (lastShownTurn \|\| 0) < 2` 는 turn 1 에서 `1-0=1` 로 걸려, **학생의 맨 첫 마디는 절대 못 고쳐 주고 있었습니다**(2026-09-09 사장님 「I ate pizza yesterday 인데 아바타가 수정해주지 않았어」로 발견 — 정본을 돌려 실측). ✅ **「한 번도 안 보여줬으면 간격이 성립하지 않는다」**(`shownBefore`)로 고치세요. ⚠️ 그 고침을 **간격을 애초에 안 보는 갈래(major)로 검사하면 그 줄이 한 번도 안 검사됩니다** — 변이시험에서만 드러났습니다. «턴 수가 1 에 고정된 채 같은 작은 실수를 두 번째로 할 때» 를 따로 넣으세요(그게 그 줄이 실제로 닿는 유일한 경우). ✅ 검사는 **경계값 1 을 넣어** 돌리세요(제 하니스는 turn 5 로만 시험해 이것을 한 번도 안 쟀습니다). ⛔ 그리고 **동작을 바꾸면 이 표의 그 문장도 함께 고치세요** — 저는 하루 전 이 자리에 「turnNo=1 은 막힌다」고 적고 다음 날 그 동작을 바꿨습니다. ⚠️ 그 COUNT 조회가 «계속» 실패하면 turnNo 가 1 에 고정됩니다 — 지금은 major 만 뜨고 «작은 실수는 두 번째부터» 쪽이 멎으므로 **조회 실패를 로그로 남기세요.** ③ 🔴 **«얼마나 중요한가» 를 모델에게 맡기면 게이트가 통째로 무력해집니다** — 모델은 «뜻이 통하는» 시제 오류를 `minor` 로 줍니다. 그러면 「작은 실수는 두 번째부터」가 걸려 **학생의 첫 실수가 그대로 지나갑니다**(에러 없음). ✅ 뜻이 달라지는 종류(시제·동사꼴·주어동사 수일치·의문문 어순)는 **우리가 목록으로 들고 `major` 로 확정**하세요(`MEANING_CHANGING_TAGS`, `src/warmup-correction.ts`) — 「지시만으로는 안 지켜진다」. ⛔ 그 목록에 관사·복수·낱말 고르기를 넣지 마세요 — 그 순간 **매 턴 교정**이 되어 2026-09-03 원장님 제보(「정해진 문장 안에서만 한다」)와 같은 뿌리로 되돌아갑니다. ⛔ 그리고 **간격 규칙을 «중요한 것» 에까지 걸지 마세요** — 과거형을 세 번 연달아 틀려도 한 번만 고쳐 주게 되고, 학생 화면에는 「어떤 건 고쳐 주고 어떤 건 안 고쳐 준다」로 보입니다. ✅ 검사는 «매번 고친다» 와 **«사소한 것은 여전히 매 턴이 아니다» 를 짝으로** 두세요 — 앞만 두면 «전부 매 턴 고치기» 도 통과합니다. ⚠️ 그리고 **«기록을 지우는 기능» 과 «메모» 가 짝인지** 보세요 — 「대화 초기화」가 D1 을 지워 턴 수를 0 으로 되돌리는데 KV 메모(`lastShownTurn`, TTL 6h)는 남아, 지운 뒤 **최대 6시간 동안 조용히 억제**됐습니다. ⚠️ KV **읽기** 실패도 로그를 남기세요 — 쓰기만 남기면, 읽기가 계속 실패할 때 memo 가 늘 null 이라 「연달아 안 하기」가 통째로 풀려 **매 턴** 뜨는데 화면은 멀쩡해 보입니다 |
| 채팅 화면 **왼쪽 가장자리**에 카드·상자를 붙였는데 글자가 가려짐 | 🔴 **홈·학습 화면 왼쪽에는 «메뉴» 손잡이(`#mg-drawer-tab`)가 세로 가운데에 떠 있습니다 — x 2~46px.** `position:absolute; right:-46px; width:44px`(`js/mg-sidebar.js`)라 드로어가 닫혀 있어도 그 띠를 차지합니다. 그래서 폰에서 카드를 `margin-left:0` 으로 두면 제목과 첫 라벨이 덮입니다(2026-09-09 교정 카드 실측: 카드 left=14px → 「내가 말한 것」의 «내» 가 가려짐). ⚠️ **AI 말풍선은 x=50 에서 시작해 4px 차이로 안전합니다** — 그래서 «말풍선은 멀쩡한데 내 카드만» 가려져 원인이 안 보입니다. ✅ 새 상자는 **말풍선과 같은 줄**(왼쪽 36px)에 맞추세요. ⚠️ **`display` 검사로는 절대 안 보입니다** — 「보인다」와 「가려지지 않았다」는 다른 값입니다. `document.elementFromPoint` 로 **«맨 위가 누구인가»** 를 재세요. 🪤 검사를 «내가 남을 덮는가» 만 두지 마세요 — 여기서 실제로 밟았습니다. **«남이 나를 덮는가»** 를 짝으로 두세요(카드 «안» 의 라벨·버튼을 하나하나 훑어 그 좌표의 맨 위가 카드 «밖» 요소인지). ⚠️ 브라우저 검사 18종이 전부 초록인 상태에서 **스크린샷을 눈으로 보고서야** 찾았습니다. 📌 같은 화면에 **큰 화면용 확대 블록**(`@media (min-width:760px)`)이 따로 있으면 새 상자도 거기 넣으세요 — 안 넣으면 말풍선만 28px 로 커지고 카드는 15px 로 남아 «카드만 작다» 가 됩니다(같은 날 실측). ⚠️ 그리고 **검사에서 말풍선을 붙일 때는 «실제 전송 경로» 를 그대로 재현**하세요 — `sendMsg` 는 `.empty-state` 를 «먼저 지우고» 붙이는데 그 단계를 건너뛰면 언어 전환 핸들러의 `renderEmpty()` 가 채팅을 통째로 다시 그려 **「🌐 를 누르면 카드가 없어진다」는 거짓 실패**가 납니다(실제로 밟아 화면 버그로 오인할 뻔했습니다). 그 전제를 **검사 한 줄로 박아 두세요** |
| 사장님이 준 **음성 파일**을 «목소리 샘플» 로 보고 AI 복제에 쓸 때 | 🔴 **그 파일이 «이미 완성된 나레이션» 일 수 있습니다 — «무슨 말인지» 부터 받아쓰기로 확인하세요.** 2026-09-05 에 사장님이 주신 mp3(29.4초)를 «목소리 샘플» 로 보고 Higgsfield 로 복제해 AI 가 대본을 다시 읽게 했는데, 2026-09-09 에 받아쓰기(러너 + faster-whisper)로 확인하니 **그 파일이 바로 그 대본 7줄을 읽은 완성 나레이션**이었습니다(끝에 대본에 없는 「자, 이제 우리 한번 재밌게 시작해봐요」까지 들어 있었습니다). ⟹ 사람 목소리가 있는데 그 목소리의 «복제본» 을 넣은 것이라 「목소리가 이상해」가 나왔고, **나흘 동안 아무도 몰랐습니다.** ⚠️ 그때 적은 진단(「샘플이 3~15 kHz 잡음으로 꽉 찬 녹음이라 복제가 그 성질을 베꼈다」)은 원인의 **절반만** 짚은 것이었습니다 — 진짜 원인은 «복제를 한 것» 자체입니다. ✅ **길이·비트레이트만 보고 성격을 정하지 마세요**(29.4초·320kbps 모노는 샘플로도 나레이션으로도 그럴듯합니다). ✅ 이 컨테이너에는 오디오 디코더가 없어 들을 수 없으니 **러너에서 받아쓰기**로 재세요(임시 워크플로 + faster-whisper large-v3 — 29초에 약 70초. 그 워크플로는 임시 브랜치 `video13` 에 있고 main 에는 없습니다). ✅ 그리고 **md5 로 «지난번 파일과 같은지» 부터** 대조하세요 — 같으면 «다시 복제» 는 결과가 같아 무의미하고, 그 사실 자체가 «내가 성격을 잘못 봤다» 는 신호입니다. ✅ 완성 나레이션이면 **자르지 말고 통째로** 쓰고 화면(슬라이드)만 «무음 한가운데» 에서 넘기세요 — 줄마다 잘라 이어붙이던 구조가 사라져 «툭»·«삡»·이음매 사고가 원천적으로 없어집니다(전환 시각은 손으로 적지 말고 `silencedetect` 로 **실측**). ⛔ 그 소리에 잡음 제거를 «들어 보지 않고» 걸지 마세요 — 세게 걸면 «물속 소리» 가 됩니다(2026-09-05 실측). 🔴 **그리고 영상을 «같은 이름으로» 갈아 끼웠으면 `?v=` 를 올리세요** — `src/index.ts` 의 미디어 `Cache-Control` 이 **7일**(`max-age=604800`)이라 **최근 7일 안에 그 영상을 본 사람에게는 옛 판이 그대로 재생됩니다.** ⚠️ 하필 그 조건에 제일 확실하게 해당하는 사람이 **「이상하다」고 제보한 그 사람**입니다 — 확인하러 열면 옛 소리가 나서 「안 고쳐졌네」가 됩니다. 2026-09-09 에 붙이지 않고 배포했다가 함정 대조가 잡았습니다(그 파일은 같은 이름으로 **네 번째** 교체였습니다). ✅ 부르는 곳(여기서는 `promo.html` 프리셋 `src`)에 `?v=2` 를 붙이면 판정식(`_mVersioned`)이 참이 되어 주소가 갈립니다. ⛔ 그 순간 **1년 immutable** 이 되므로 다음에 또 바꾸면 **반드시 번호를 올려야** 합니다(안 올리면 1년입니다). ⚠️ 내용이 안 바뀐 영상에는 붙이지 마세요 — 주소가 갈려 전원이 24MB 를 다시 받습니다. ✅ 감시는 `today_plan_harness` ⑨(«짧은 판 주소에 ?v= 가 있는가» + 파일을 찾을 때는 쿼리를 뗀다). 📄 `docs/작업기록/260909_안내영상_사장님목소리로_교체.md` |
| 「누가 결재하고 누가 확인하나」를 바꿀 때 (결재선·결재권자) | 🔴 **단계의 «누가»(`approval_steps.role`)는 «그 건에» 박힙니다 — 규칙을 바꿔도 이미 올라온 건은 옛 역할 그대로입니다**(이 표의 「결재·상태 «정책» 을 고쳤는데 이미 만들어진 건은 그대로 막혀 있음」). 📜 2026-09-09 사장님 「결재는 장지웅 부장이 하고 확인만 정우영 대표가 한다」로 돈 나가는 분류(물품·지출)의 1단계를 `staff`(본사 아무나) → **`mgr`(지정 결재권자 `MONEY_APPROVERS`)** 로 바꿨습니다(그전에는 필리핀 건이 대표님 화면에도 떠서 ₱2,500·₱4,600·₱2,800 세 건을 대표님이 혼자 찍고 계셨습니다). ⛔ **`EXEC_USERNAMES` 에서 사람을 빼는 방식으로 결재선을 좁히지 마세요** — 인사·급여 **열람**(`visibility:'exec'`)까지 함께 닫혀 「대표님이 올린 건을 아무도 결재 못 하는」 8/30 사고(5일 방치)가 되살아납니다. ⛔ **명단이 비면 막지 말고 옛 동작(본사 아무나)으로 떨어뜨리세요** — 「명단을 못 읽어 아무도 결재 못 함」이 훨씬 나쁩니다. ⚠️ **«누를 수 있는 사람»(`canDecideStage`)과 «알릴 사람»(`isPrimaryApprover`)은 다른 질문입니다** — 경영진은 결재권자가 자리를 비웠을 때 **대신** 누를 수 있어야 하지만 알림까지 가면 「결재는 장 부장이 한다」가 화면에서만 참이 됩니다(그 행에 `by_proxy` 로 「대신 결재」라고 적습니다). 🔴 **그리고 «알림 대상» 으로 좁히면 «결재권자 본인이 올린 건» 의 알림이 0명이 됩니다** — `approversFor` 가 기안자를 건너뛰기 때문입니다. 아무도 안 나오면 `canDecideStage` 로 한 번 더 훑는 폴백을 **짝으로** 두세요(2026-09-09 함정 대조가 잡음 — 에러 없이 조용했고 2일 뒤 승격 cron 까지 아무도 몰랐을 상태였습니다). 🔴 **전결(`straightThrough`)을 그대로 두면 2단계가 «건너뜀» 으로 닫혀 대표 차례가 아예 안 열립니다** — 결재권자가 경영진 명단에도 있기 때문입니다. 돈 나가는 분류에서만 끕니다(`allowsStraightThrough`). 🔴 **그것만으로는 부족합니다 — 같은 사람이 1단계(mgr)에 이어 2단계(exec)까지 «연달아» 누를 수 있습니다**(decide 에는 「본인이 올린 건은 본인이 승인 못 함」만 있었습니다). 그러면 대표님께 «결재» 가 아니라 «확인» 으로 옵니다. 2026-09-09 사장님 「1번 막아주고」로 `sameDeciderBlocked` 를 넣었습니다 — **대가는 «둘 중 하나가 자리를 비우면 큰돈 결재가 멈춘다»** 이고 아는 채로 고른 것입니다. 🔴 **여기 「지금 명단으로는 교착이 없다」고 적었던 것은 사실이 아니었습니다**(2026-09-09 함정 대조가 정본을 돌려 잡음) — 「본인이 올린 건은 본인이 승인 못 함」과 맞물려 **경영진이 «올린» 큰돈 건은 2단계 결재자가 0명**이 됩니다(남이 올린 건은 정상). 이 변경 «전» 에는 되던 흐름이라 **이 규칙이 만든 교착**입니다(실측: 물품·지출 4건은 전부 필리핀 매니저의 소액이라 걸린 건 0건). ✅ 화면이 그 사실을 말하게 했습니다 — **`approverCounts` 가 이 판정을 함께 세어** 「이대로는 처리되지 않습니다」 배너를 띄웁니다. ⛔ 그 줄을 빼면 아무도 못 누르는 건이 조용히 「대기 중」으로만 보입니다(8/30 긴급 건 5일 방치와 같은 모양). ✅ **2026-09-09 사장님 결정 — 그대로 둡니다**(「내가 직접 큰 돈을 올릴 일이 없어. 이거 아주 예외적인 경우야」). 그때는 배너를 보고 취소한 뒤 다른 사람이 올립니다. ⛔ 그러니 「경영진이 올린 건은 예외」로 **고치지 마세요** — «아직 안 정한 것» 이 아니라 사람이 보고 고른 것이고, 돈 규칙을 느슨하게 하는 일이라 다시 열려면 지시가 한 번 더 있어야 합니다. 🪤 그리고 그 전제를 «못 박는다» 던 하니스 한 줄(`EXEC.filter(...).length >= 1`)은 **기안자 제외를 모형에 안 넣어 언제나 참**이었습니다 — 지금은 «기안자별로 1·2단계 후보를 실제로 세어» 봅니다. ⛔ **반려까지 막지 마세요**(돈이 안 나가는 방향). ⛔ **앞 단계 조회가 실패하면 빈 배열로 떨어뜨리지 마세요** — 「앞 단계에 아무도 없다」가 되어 게이트가 통째로 풀립니다. `null` 로 넘겨 **막는 쪽으로** 실패시키세요. ✅ **막으면 화면이 그 사실을 «먼저» 말해야 합니다** — 서버가 결재함 행에 `same_decider` 를 붙이고 화면이 승인 버튼을 감추며 **묶음 승인에서도 뺍니다**(안 빼면 조용히 건수만 줄어 「승인했습니다」가 거짓이 됩니다 — 이 표의 「상한·검증을 새로 걸 때 화면이 그 실패를 뭐라고 말하는지」). ✅ **「확인」(`needsExecAck`)은 결재가 아닙니다** — 이미 확정돼 필리핀 통보까지 끝난 건에 «봤다» 도장만 찍고 **아무것도 막지 않습니다**(안 눌러도 업무가 흘러가는 것이 설계 의도). ⛔ 분류를 넓히지 마세요(문서·휴가까지 넣으면 「확인할 것 30건」이 되어 돈 건이 파묻힙니다 — 녹화 목록에서 이미 밟은 함정). ✅ **결재권자에게는 확인이 안 뜹니다**(`isApprover` — 2026-09-09 「2번은 대표만 보이게」). ⛔ 그 판정에 **계정 이름을 못 박지 마세요** — 「결재권자인가」로 물으면 명단이 바뀌어도 따라옵니다. ⚠️ **새 칸(`exec_ack_at`)을 만들면 ETag 서명에도 넣으세요** — 안 넣으면 확인해도 최대 1시간 동안 304 로 옛 화면이 옵니다. ⚠️ **문자열 하니스로는 이 종류를 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «누가 누구를 어떤 순서로» 뿐입니다. 🪤 **배선 검사도 「부르는가」로 쓰면 헛돕니다** — 그 게이트의 인자 `decision` 을 `'rejected'` 리터럴로 못 박아 **한 번도 안 막게** 만들어도 143건이 전부 초록이었습니다(부르고·403 이고·순서도 앞이라서). **그 호출의 인자 묶음을 괄호 짝으로 오려 내 «게이트가 채운 그 변수를 쓰는가»** 로 물으세요. ⚠️ 같은 이유로 「반려 버튼은 남긴다」를 «파일 어딘가에 그 글자가 있는가» 로 쓰면 반려까지 감추는 변이가 통과하고, 가드를 «하나» 로 가정하면 두 번째 가드를 넣는 변이가 통과합니다(둘 다 실측) — **가드의 범위를 «전부» 구해 그 밖인지** 보세요. ⚠️ 그리고 **개수로 못 박지 마세요**(`isApprover` 넘기는 곳이 2개인가) — 안 넘기고 새 호출부를 더해도 2가 유지되어 통과하고, 정당한 3번째가 생기면 거짓 FAIL 입니다. **`needsExecAck(` 호출 수 == `isApprover:` 수** 로 짝지으세요. ⚠️ 그 인자를 오려 낼 때 TS 는 **인자 목록·반환 타입 안에도 `{`** 가 있으므로 괄호·꺾쇠 깊이가 0인 것만 몸통으로 보세요(실제로 밟아 `approverCounts` 대신 타입을 잘랐습니다). 🔴 **게이트의 «근거» 가 best-effort 로 기록되면 조용히 풀립니다** — 단계 도장(`approval_steps`)은 `safe(…, false)` 로 쓰이는데 게이트는 그것만 읽었습니다. 한 번 실패하면 앞 단계 도장이 비어 같은 사람이 그대로 2단계를 승인합니다(에러도 로그도 없고 `lookup_failed` 로도 안 잡힙니다). **하드 UPDATE 로 남는 `approval_requests.decided_by` 를 근거에 함께** 넣으세요. 감시는 `test-harness/approval_money_approver_harness.mjs`(158종 — 정본을 **실제로 돌리고** 라우트를 중괄호 짝으로 잘라 배선을 봅니다. 변이 16종 전부 실제 FAIL) 📌 **덧붙임(2026-09-10) — 규칙은 맞는데 «화면» 이 그 지시를 무너뜨리고 있었습니다.** 사장님 「₱5,000 이상은 둘 다 떠야 승인, 미만은 장 부장님만 결재하고 저는 확인」으로 다시 보니, `stagesFor`·`needsExecAck` 는 이미 그대로였는데 **소액 건이 대표님 결재함에 «승인/반려 버튼과 함께» 뜨고 있었습니다** — `canDecideStage('mgr')` 이 경영진에게 «부재중 대신 결재» 를 열어 두기 때문입니다(위 ⚠️ 줄). 게다가 **「N건 한 번에 승인」이 그 건들을 쓸어 담았습니다** — 한 번 누르면 결재권자 몫까지 함께 승인됩니다. ✅ 그래서 `by_proxy` 단계는 **접어 두고**(「대신 결재하기」를 눌러야 승인·반려가 펼쳐짐) `cleanOnes` 에서 뺐습니다(`public/work.html`). ⛔ **목록에서 통째로 빼지 마세요** — 결재권자가 휴가·출장이면 그 건이 그대로 멈춥니다(8/30 긴급 건 5일 방치). 사장님이 «접기» 를 고르신 이유입니다. ⚠️ 펼침은 `PROXY_OPEN` 에 남깁니다 — 이 화면은 **60초마다 repaint** 해서, 안 남기면 펼쳐 놓은 것이 눈앞에서 접힙니다. ⚠️ **탭 제목·알림음 건수(`inbox.length`)는 그대로 소액 건을 셉니다** — 일부러 둔 것입니다(안 세면 결재권자가 자리를 비운 사이 멈춘 건을 대표님이 영영 모릅니다). 🪤 이 변경이 **하니스 하나를 «보장은 그대로인데» 깨뜨렸습니다** — `approval_policy_harness` 가 `cleanOnes` 를 「앞 400자 안에 그 글자가 있나」로 재고 있어 주석 한 문단에 밀려났습니다(이 표의 「검사 범위를 «길이» 로 자르지 마세요」). **중괄호 짝**으로 몸통을 자르게 고쳤습니다. 🪤 그리고 그 검사는 **주석을 벗겨 내야 합니다** — 변이시험에서 가드를 `// (변이) if (r.by_proxy) …` 로 막았더니 그 글자가 주석에 남아 **141건이 전부 초록**이었습니다. 🔴 **접힘 안내는 «그다음이 무엇인가» 를 말해야 합니다** — 큰돈(₱5,000 이상) 건도 **1단계는 결재권자**라 똑같이 접힙니다. 그 건은 대표님이 **2단계를 직접 결재**하셔야 하고 「확인」은 영영 안 뜨는데(`needsExecAck` 는 `decided_by === me` 면 false), 한 문장으로 뭉치면 **하필 사장님 지시의 그 부류에 「확인만 하시면 됩니다」라고 정반대 말**을 합니다(2026-09-10 함정 대조가 브라우저로 잡음). ⛔ 금액 기준을 화면에 베끼지 마세요 — 서버가 이미 실어 보낸 **«남은 단계»(`steps`·`stage_total`)** 로만 가릅니다. ⚠️ 뒤에 단계가 남았는데 역할을 모르면 **지어내지 말고** 「이 뒤에 결재 단계가 더 있습니다」로 두세요. 🪤 그리고 **fixture 에 «by_proxy × 2단계» 조합이 없으면 이 결함은 원리상 검사에 안 닿습니다**(둘 다 «접히기» 때문에 갈리는 것은 «무슨 글자가 나오는가» 뿐). 🪤 **묶음 가드를 «그 글자가 있는가» 로 검사하면 조건 뒤집기로 뚫립니다** — `if (r.by_proxy && false)` 로 바꿔도 141/141 초록이었습니다(실측). **`cleanOnes` 몸통을 중괄호 짝으로 잘라 `new Function` 으로 실제로 돌려** «그 행이 빠지는가» 를 보고, **«내 차례인 건은 남는가» 를 짝으로** 두세요(없으면 «전부 빼기» 도 통과). ⚠️ 문자열 하니스로는 «무엇이 그려지는가» 를 못 봅니다 — 감시는 `test-harness/manual/approval-proxy-fold-browser.mjs`(브라우저 23종 — **자동으로 안 돕니다, 사람이 부릅니다.** 「접힌다」 옆에 **「내 차례인 건은 안 접힌다」·「펼치면 나온다」·「2단계 건에는 «확인만» 이라고 안 한다」를 짝으로** 둡니다. 변이 **7종** 전부 실제 FAIL) |
| 「배포 후 며칠 뒤에 다시 재서 보고해줘」를 받았을 때 | 🔴 **재기 «전» 에 «그게 나가긴 했나» 부터 확인하세요 — 이 저장소에서 두 번 밟았습니다.** 2026-09-06 작업기록에 「재려고 보니 배포가 안 돼 있었습니다」가 이미 적혀 있는데, 2026-09-09 재측정에서 **같은 일이 또** 났습니다: 커밋 4건이 작업 브랜치에만 있고 **PR 조차 없었습니다**(`gh` 대신 MCP `list_pull_requests` 로 확인). 그 상태로 숫자만 재서 「효과가 있었다/없었다」를 적으면 **아예 돌지도 않은 코드를 판정**하는 것이고, 이 파일이 네 번 사고를 낸 「하기로 한 것이 «했다» 로 적힌다」와 같은 자리입니다. ⛔ **커밋 해시로 판정하지 마세요**(squash 병합이라 해시가 main 에 없습니다 — 같은 표의 그 항목). ✅ 확인은 셋 — ① `git log --oneline origin/main --grep`·**`git show origin/main:<파일>` 로 «추가한 줄이 지금 main 에 있는가»** ② `?v=` 번호가 올라가 있는가 ③ 배포 run 로그의 업로드 파일 목록. ✅ 그리고 **예약(Routine)의 프롬프트 첫 줄에 그 확인을 «먼저 하라» 고 박아 두세요** — 며칠 뒤 그 예약이 깨어날 때 사람은 「배포됐겠지」로 시작합니다. 📄 `docs/작업기록/260909_UDP직결_전환_재측정과_병합보류.md` §2 |
| 고치려던 원인이 **저절로 사라졌을** 때 | 🔴 **«고치지 않는 것» 이 답일 수 있고, 그 판단을 예약에 «규칙으로» 적어 두지 않으면 다음에 깨어난 사람이 그냥 병합합니다.** 2026-09-09 실측: 소리 끊김(`rx_conceal`)을 겨냥해 지터버퍼 문턱을 relay 기준으로 바꿔 뒀는데, 재 보니 **9/8 에 처음으로 UDP 직결이 붙어**(TURN `3478 udp` ×10 — 그전 전부 tcp) 겨냥한 인구 자체가 사라졌습니다: relay-tcp 34분 → **0분**, direct 0분 → **140분**, conceal **10.59% → 0.88%**(같은 회선·같은 사람 12.56% → 0.05%). 그 기간 TURN·ICE 커밋은 **0건**이라 우리가 고친 것이 아닙니다(원인은 회선 쪽으로 «추정» — 재지 않았습니다). ⚠️ 그때 병합하면 **없어진 문제에 대가(+300ms 지연)만 남습니다.** ✅ **보류하고 지켜보되, 「무엇이 다시 나타나면 병합한다」를 숫자로** 적어 두세요(여기서는 「relay-tcp 가 다시 30분 이상 · conceal ≥ 5% · conceal/aloss ≥ 3배」). ⛔ 「일단 넣어 두면 손해 없다」로 넘기지 마세요 — 그 문턱은 **켜지면 지연이 늘어** 겨냥 밖 사람에게는 손해입니다. ✅ 그리고 **«저절로 나아진 것» 도 기록으로 남기세요** — 안 적으면 다음에 같은 증상이 올 때 「전에 고쳤는데 왜 또?」로 엉뚱한 곳을 뒤집니다 📌 **덧붙임(2026-09-21) — 2차 재측정: 규칙 3조건 중 «1개» 만 충족 → 계속 보류, 그리고 «그 신호» 의 정체가 바뀌었습니다.** [잰 것 — 9/7~9/20 `vc_quality`] relay-tcp 는 w1(9/7~13) **34분·conceal 10.46%** → w2(9/14~20) **14분·4.46%** (규칙 30분↑·5%↑·3배↑ 대비 **14분 ✗ · 4.46% ✗ · 10.9배 ✓**). 🔴 **더 중요한 것 — w1 의 그 34분은 전부 «2026-09-07 하루» 이고 방이 회의방·공용방(`meet-…`·`mangoi-class`), 사람이 jeong(사장님)·MAIMAI·student·mgr_karl 로 «예약 수업 0분 · 학생 0명» 입니다.** ⟹ 「우회로 탄 사람이 아프다」는 1차 신호는 **우리가 시험한 방**에서 나온 것이었고, 그 위에 세운 «게이트를 relay 기준으로» 제안의 근거가 그만큼 약합니다(예약 수업에서 그 길이 처음 잡힌 것은 w2 의 **강사** Krystel 9/18 9분이고 conceal 3.64% 로 문턱 아래 — ⚠️ `vc_quality` 는 **uid 별 행**이라 그 수업 «학생 쪽» 이 relay-tcp 였는지는 모릅니다. ⛔ 「학생이 탔다」로 읽지 마세요). ⚠️ **w2 에는 `rb >= 300` 인 사람이 0명**(8분 이상 14명의 최소 RTT 최댓값 189ms)이라 버퍼의 «먼 사람» 갈래는 그 주 한 번도 안 켜졌습니다. ⚠️ 같은 사람·같은 경로(direct) 10명 중 9명 conceal 하락이지만 **RTT 도 함께 내려가**(jeong 372→222) 1차와 같은 교란이 그대로입니다 — **판정 불가**. 🔴 **새 함정: conceal 이 높다고 다 버퍼 대상이 아닙니다** — Teacher Win 은 conceal 5.06% 인데 aloss 6.52%(**0.78배**)라 «늦어서 버린» 것이 아니라 **진짜 손실**이고 기다려도 안 살아납니다. ⛔ 「conceal 이 높으니 버퍼」로 읽지 말고 **반드시 conceal/aloss 비를 함께** 보세요. ✅ 「언제까지 보류하나」는 **사람이 정할 일**입니다 📄 `docs/작업기록/260906_지터버퍼_배포와_반영확인_재측정예약.md` §9 |
| 예약(Routine)으로 「며칠 뒤에 확인」을 걸 때 | 🔴 **`create_new_session_on_fire: true` 로 만든 예약은 «MCP 연결이 없는» 새 세션에서 깨어납니다 — D1 을 못 읽어 그 확인이 조용히 헛돕니다**(에러도 안 나고 「확인했습니다」만 남습니다). 이 저장소의 기존 예약이 전부 **세션 바인딩**인 이유가 이것입니다. ✅ 재측정·모니터링처럼 **도구가 필요한 예약은 지금 세션에 묶으세요**(`create_trigger` 의 기본값 = 이 세션). ⚠️ **남의 세션에 묶인 예약은 프롬프트를 못 고칩니다** — `update_trigger` 가 「editing the prompt of a routine whose fires deliver into a session that is not your own is not available」로 거절합니다(이름·주기·on/off 는 됩니다). 그래서 **프롬프트에 판정 규칙·숫자·문서 경로를 처음부터 다 넣어** 두세요. ⚠️ `create_trigger` 가 실패한 것처럼 보여도 **실제로는 만들어져 있을 수 있습니다** — 다시 만들기 전에 `list_triggers` 로 확인하세요(2026-09-09 에 중복으로 만들어 하나를 지웠습니다) |
| AI 가 답을 못 만들었을 때 **«명랑한 고정 문구»로 자리를 메울** 때 | 🔴 **그 문구는 «학생이 무슨 말을 했든 똑같이» 나오므로, 화면에는 «AI 가 딴소리를 한다» 로 보입니다.** 2026-09-09 사장님 「아직도 아바타가 내 말을 이해못하고 이상한 주제로 질문해」 — AI 영어친구에서 공룡 얘기 중에 「오늘 뭐 먹었어?」가 나왔습니다. 그 두 문장은 LLM 답이 아니라 `src/api-ai.ts` 의 **«모델 3개가 다 실패했을 때» 폴백**이었습니다. ⚠️ **에러가 안 납니다** — 서버가 `ok:true` 로 주니 화면의 「⚠️ 잠시 응답이 지연되고 있어요」 경로로 **안 갑니다**. 그대로 Lily 말풍선으로 그려지고 **TTS 가 소리내어 읽기까지** 했습니다. 🔴 **더 나쁜 것 — 그 폴백이 대화 기록(`ai_friend_chats`)에 «AI 가 한 말» 로 저장됐습니다.** **[잰 것]** 그 세 턴은 정상답 → 폴백 → 폴백이라, 폴백이 기록에 들어간 «뒤» 모델이 실제로 답한 턴은 **없습니다**. **[판단 — 예방]** 다음 «정상» 턴이 오면 모델이 그 고정 문구를 자기 직전 발언으로 보게 되고, 그 문구는 주제와 무관하므로(예: 음식) 대화를 끌 수 있습니다. ⛔ 이것을 «끌려갔다» 는 **관측**으로 적지 마세요. ✅ 폴백은 **«답» 인 척하지 말고 밝히세요**(`ai_unavailable`) — 화면이 그것을 보고 안내로 그리고, **소리로 읽지 않고, 교정 카드·따라말하기도 안 붙입니다**(둘 다 «AI 가 한 말» 이라는 전제 위에 있습니다). ✅ **기록에 남기지 마세요.** 단 **학생 발화는 그대로 남기세요** — 학생이 말한 것은 사실이고, 답이 없는 턴으로 남는 것이 「없던 일」로 지우는 것보다 정직합니다. 🔴 **«왜 실패했나» 를 Workers 로그로 찾지 마세요 — 20건 중 19건이 버려집니다.** `wrangler.toml` 이 **`head_sampling_rate = 0.05`**(요청의 5%만 기록)라, 2026-09-09 사장님 세션 3건은 **한 줄도 안 남았습니다**(대시보드 실측: 목록이 22:41 → 22:32 로 건너뜀). ⛔ 저는 그 설정을 확인하지 않고 「로그가 정본」이라 안내해 사장님이 대시보드를 한참 뒤지게 했습니다. ⛔ 그 비율을 올려서 풀지 마세요(`wrangler.toml` 은 공동 금지구역). ✅ **실패한 그 순간에 사유를 D1 에 남기세요** — 정본 `src/ai-failure-log.ts` 의 `recordAiFailure`, 표 `ai_failure_log`(마지막 예외 문장·모델·JSON모드 여부·평문/빈응답 횟수). 읽는 것은 SELECT 한 줄입니다. ⛔ **학생 발화·AI 답변은 한 글자도 싣지 마세요**(진단용 표이지 개인정보를 담는 곳이 아닙니다). ⛔ 기록을 `try` 밖에 두지 마세요 — 기록이 실패하면 대화가 죽습니다. 배포 시각과 겹친다고 **원인을 단정하지 마세요** (실측 표본이 배포 뒤 3턴뿐이었습니다). ⚠️ **빈도를 세는 방법이 이 수리로 바뀌었습니다** — 폴백을 기록에 안 남기게 했으므로 **이 수리 «이전» 기간에 한해서만** `ai_friend_chats` 를 `LIKE` 로 셀 수 있습니다(그렇게 잰 값: 9/9 35건 중 2건 · 9/1 80건·9/2 53건·9/3 10건은 **0건**). ⛔ **그 뒤로는 D1 이 0건이어도 «안 난다» 는 뜻이 아닙니다** — 유일한 근거는 Workers 로그의 `[chat-friend] all models failed …` 한 줄이고, **사람이 보러 가야 보입니다.** ⛔ 폴백 문구를 «주제에 맞게» 지어내지 마세요(2장 「측정할 수 없는 값을 그럴듯하게 채우고 싶을 때」와 같은 뿌리 — 모르면 모른다고 말합니다). ⚠️ **문자열 하니스가 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «그 문장이 답인 척하는가» 뿐이라 수리 전에도 `--fast` 304건이 전부 초록이었습니다. 🪤 그리고 이 수리는 **기존 검사 하나를 깨뜨렸습니다** — `ai_friend_correction_harness` A-7 이 `fix: showFix, repeat: offerRepeat` 라는 **식 모양을 글자 그대로** 못 박아 두어, 폴백일 때 카드를 끄는 (보장이 «더» 센) 수리에 빨간불이 났습니다. **뜻으로** 물으세요(«그 변수를 싣는가»). 🪤 **그리고 «표시를 켜는 곳» 만 검사하면 뚫립니다** — 폴백 블록 «뒤» 에 `usedFallback = false;` **한 줄**만 더해도 응답 칸·저장 가드·화면 분기가 **전부 되돌아가는데** 문자열 위치 검사 22종이 **그대로 초록**이었습니다(함정 대조 실측). ✅ **«그 표시를 끄는 곳이 선언 한 줄뿐인가» 를 짝으로** 두고, 응답 조립식은 **오려 내 실제로 평가**하세요. 🪤 **안내를 «말풍선»(기존 `appendMsg`)으로 그리지도 마세요** — 본문이 `.ai-text` 안에 들어가 자막 «가리기/끄기» 모드에서 **흐려지거나 사라지고**(하필 서버가 +1P 로 권하는 모드라 그 학생에게만 안 닿습니다) 🔊·뜻 버튼이 붙어 **영어 TTS 가 한국어를 읽습니다.** 전용 알림(`appendNotice`)으로 그리세요. 감시는 `test-harness/ai_friend_fallback_honest_harness.mjs`(31종 — 서버·화면 양쪽을 중괄호 짝으로 잘라 «폴백일 때만 켜지는가»·«정상 답은 여전히 그려지는가» 를 **짝으로** 봅니다. 변이 **10종** 전부 실제 FAIL) |
| 버튼이 **늘 같은 실패 문구 하나**만 말함 (「알림 받기」·「연결하기」류) | 🔴 **실패가 여러 단계인데 `catch` 하나로 받으면, 「틀린 말은 아니지만 아무것도 알려 주지 않는」 문장이 됩니다 — 다음에 할 일이 정해지지 않습니다.** 2026-09-10 사장님 「알람받기 안 안켜진다고 나오지?」: 결재함 웹푸시가 네 단계(키 발급 → SW 등록 → 구독 → 서버 저장)를 거치는데 `\.catch(function(){ return false; })` 로 통째로 받아 전부 「알림을 켜지 못했습니다」였습니다. ⟹ 서버에 키가 없는 것인지, 이 브라우저가 거절한 것인지, 마지막 저장만 안 된 것인지가 **전부 같은 글자**입니다. ✅ **`{ ok, why }`** 를 돌려주고 화면이 `why` 를 그대로 말하게 하세요. ⛔ **`''`(성공)/사유문자열 로 돌려주지 마세요** — 호출부의 `if (!ok)` 가 **성공에서 참**이 되어 정반대로 동작합니다(호출부가 둘 이상이면 반드시 밟습니다). ⛔ **연결 실패를 «키 없음» 으로 읽지 마세요** — 「서버에 키가 없다」고 말해 **관리자를 엉뚱한 곳(시크릿 등록)으로 보냅니다.** 끊긴 것과 없는 것은 다른 사실이고, 이미 있는 키를 덮어쓰면 «고쳤다» 는 기록만 남습니다(2026-08-26 TURN 실사고와 같은 모양). 🔴 **그런데 `fetch` 는 404·500·502 에도 «reject 하지 않습니다»** — `.catch` 갈래로 안 오므로 본문이 JSON 이 아니면 그대로 «키 없음» 으로 떨어집니다. 제가 이 규칙을 적은 바로 그 커밋에서 **그 자리에서 어겼고 함정 대조가 잡았습니다.** **`r.ok` 를 따로 보세요.** ⚠️ **문구표의 «기본값» 을 실제 사유 중 하나로 두지 마세요** — 나중에 단계를 하나 더 넣고 표를 잊으면 **틀린 문구가 조용히** 나갑니다. 모르면 「알 수 없는 단계」라고 말하게 두세요. ✅ 진짜 예외는 **콘솔에도** 남기세요(토스트는 몇 초 뒤 사라집니다). ⚠️ **그 옆에 «조용히 재시도하는» 코드가 있는지 보세요** — 여기서는 `repaint()` 마다 도는 되살리기가 있었는데 실패하면 표시가 false 로 돌아와 **화면을 그릴 때마다** 발급요청·SW등록·POST 가 새로 나갔습니다(가드 한 줄로 막되 ⛔ **버튼은 그 가드에 걸리면 안 됩니다** — 사람이 누르면 다시 시도해야 합니다). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무슨 글자가 나오는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 검사는 **실패를 하나씩 만들어 실제로 눌러** 보세요. ⛔ 「막힌다」만 넣지 말고 **«성공하면 성공이라고 말한다» 를 짝으로** 두세요(앞만 보면 «전부 실패로 만들기» 도 통과합니다). 🪤 그 검사에서 **스텁을 회차마다 쌓지 마세요** — `addInitScript` 가 누적되면 2회차부터 최상위 `const` 재선언으로 죽고 **1회차 스텁이 계속 살아** 모든 회차가 같은 답을 냅니다(실제로 6건 중 5건이 거짓 FAIL 이었습니다). 회차마다 **새 컨텍스트** + 스텁은 **IIFE**. ⚠️ 그리고 그 가드가 **«저절로 낫는 실패»(끊김)까지 막지 않는지** 보세요 — 오프라인에서 연 사람이 회선이 돌아와도 영영 안 붙습니다(하필 그 기능이 겨냥한 사용자입니다). ✅ 검사에는 «문구» 만이 아니라 **«토스트가 실제로 보이는가»(show 클래스) · «누르기 전에는 그 일이 없었는가» · «다시 그려도 요청이 안 나가는가»** 를 함께 두세요(앞뒤를 안 재면 버튼이 아무 일도 안 해도 통과합니다). 감시는 `test-harness/manual/approval-push-reason-browser.mjs`(브라우저 42종 — 자동으로 안 돕니다, 사람이 부릅니다. 변이 4종 전부 실제 FAIL: 문구 되돌리기 6 · HTTP 가드 제거 1 · 재시도 가드 제거 5 · 끊김 예외 제거 1). 📄 `docs/작업기록/260910_결재함_알림받기_실패사유를_말하게.md` |
| 목록·드롭다운이 «찾아 놓고» 한 줄도 안 그림 — `.show` 는 붙었는데 화면이 빔 | 🔴 **옛 «정리» 규칙이 같은 클래스를 쓰는 새 화면을 통째로 먹고 있을 수 있습니다.** 2026-09-09 관리자 가로 통합검색이 그랬습니다 — 2026-05-25 ph89「우측 메인 영역의 옛 메뉴 검색 카드 제거」가 `@media (min-width:1024px){ .menu-search-wrap, .menu-search-input, .menu-search-dropdown, .menu-search-clear, .menu-search-item{display:none!important} }` 인데, 2026-06-24 에 되살린 **히어로 검색창 드롭다운이 같은 클래스**(`menu-search-dropdown`)를 달고 있었습니다. 작성자 `!important` 라 `.hero .hero-search-dropdown.show{display:block}`(important 없음)을 **언제나 이깁니다**(인라인 `display:none` 도 함께 이기므로 CSS 한 곳으로 둘 다 풀립니다). [잰 것] 「녹화」 입력 시 **1600px → display none·높이 0 · 900px → block·높이 482**, 그런데 **DOM 안 후보는 12개** — 즉 «찾아 놓고 안 그렸습니다». ⚠️ **PC 에서만 재현됩니다**(미디어쿼리) — 폰으로 확인하면 멀쩡해 보입니다. ⚠️ `.show` 가 붙어 있어 `classList` 로는 «열렸다» 로 보입니다 — 「열렸다」·「보인다」는 다릅니다. **`getComputedStyle(el).display` 와 `getBoundingClientRect().height` 를 재세요.** ⛔ 옛 규칙을 지워서 풀지 마세요(그 정리가 통째로 풀립니다). ⛔ 새 화면에서 그 클래스를 떼는 것도 대개 안 됩니다 — 항목 모양(`display:flex`·padding·hover)이 그 클래스에 걸려 있습니다. ✅ **파일 맨 끝 꼬리 블록에서 «내 것만» `!important` 로 되살리세요.** ⚠️ 그때 상자만 되살리고 **항목(`.menu-search-item`)을 빠뜨리면 «빈 상자»** 가 뜹니다 — 항목의 display 와 **실제 높이**를 따로 재세요. 📜 **형제 — «찾았다» 와 «화면에 보인다» 가 다른 자리가 하나 더 있습니다**: 관리자 사이드바는 두 벌(옛 9그룹 `data-ia6-legacy` = `display:none` · 그 위 IA6)인데 검색이 둘 다 훑어서, 옛 쪽에만 있는 말은 «찾았다»고 판단해 그룹까지 열어 놓고 **화면엔 0건**이었습니다(「결재」 실측: 찾은 항목 1 · 보이는 것 0). ✅ 감춰진 벌은 **훑지 마세요**(`hasAttribute('data-ia6-legacy')` 로 건너뜀 — IA6 가 아직 안 돌았으면 그 속성이 없어 저절로 예전처럼 훑습니다). ✅ 그리고 **남는 것이 없으면 «없다»고 말하게** 하세요 — 항목이 전부 사라지고 아무 말도 없으면 사람에게는 «검색이 고장났다» 로 읽힙니다. ⚠️ 사이드바 검색은 **`textContent` 만 보지 마세요** — 항목에 `data-ko`/`data-en` 이 함께 달려 있어(IA6) 한국어 화면에서 「recording」·「payroll」이 **0건**이었습니다(EN 스태프는 자기 메뉴를 영어로 못 찾습니다). 손자 줄의 영문 이름은 속성이 아니라 `adm-r25.js` 가 물려 준 **`__gc` 객체**에만 있습니다. ⚠️ **`.ph85-sub` 가 아닌 항목을 빠뜨리지 마세요** — 결재함은 `<a id="ia6-appr">` 라 필터 루프 밖이었고, 「결재」로 치면 **화면에 보이는데도** 0건이었습니다(숨김 CSS `.ph85-shide` 목록에도 그 id 를 넣어야 합니다). ⚠️ **문자열 하니스로는 넷 다 못 잡습니다** — 함수도 값도 규칙도 다 «있고» 틀린 것은 «누가 이기는가 / 무엇이 그려지는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 감시는 `test-harness/manual/admin-search-browser.mjs`(브라우저 36종 — **자동으로 안 돕니다, 사람이 부릅니다.** 변이 6종 전부 실제 FAIL) |
| 대비(글자가 읽히는가)를 쟀는데 **«페인터가 구제해 준 값»** 을 재고 있음 | 🔴 **`el.style.color` 를 «내가 준 색» 으로 쓰면 안 됩니다 — 페인터가 «그 자리를» 인라인 `!important` 로 덮어씁니다.** 이 저장소의 글자색 보정기 셋(`adm-s12`·`adm-s13`·`js/adm-light-surfaces.js`)은 대비가 낮을 때만 도는데, **덮는 곳이 인라인 style 그 자체**라 나중에 읽으면 언제나 보정된 값이 나옵니다. 2026-09-09 실측: 사이드바 안내 상자를 만들며 `color:#e2e8f0`(밝은 회색)을 줬는데 «지금 화면 대비» 는 **8.17 통과**였습니다 — 그런데 그것은 페인터가 `rgb(45,72,108)` 로 바꿔 준 값이었고, **내가 준 색만으로는 대비 1.08**(합성 배경 `rgb(237,240,243)`)이었습니다. ⚠️ 그래서 **페인터가 돌기 전 몇 초 동안은 그대로 안 보입니다**(실측: 로드 직후 `getPropertyPriority('color')` 가 `''` → 잠시 뒤 `'important'`). 「가끔 안 보인다」 제보의 모양입니다. ✅ 검사는 **«지금 화면 대비» 와 «내가 준 색만으로의 대비» 를 짝으로** 재고, 뒤엣것은 **소스 파일에서 그 색을 읽어** 쓰세요(화면에서 읽으면 위 이유로 헛돕니다). ✅ 그리고 **처음부터 그 화면 톤에 맞는 색**을 주세요 — 관리자 화면은 `adm-light-theme.css` 로 **밝은 테마**라 사이드바도 밝고 옆 항목이 `rgb(52,64,84)` 를 씁니다. 페인터는 «구제» 이지 «설계» 가 아닙니다. 🪤 **덤 — hex 색을 `[\d.]+` 로 파싱하지 마세요.** `#e2e8f0` 에서 숫자만 뽑으면 **`2,8,0` = 거의 검정**이 되어 밝은 바탕에서 대비가 높게 나와 **거짓 통과**합니다(실제로 그 상태로 통과했습니다). hex 와 `rgb()/rgba()` 를 갈라서 읽으세요. ⚠️ 반투명 배경은 **불투명한 층을 만날 때까지 모아 아래에서 위로 합성**해야 합니다(2장 「어두운 화면에서 «글자가 읽히나»」 항목과 같은 자리). 📜 형제: 「관리자 카드 안 **글자**에 색을 줬는데 화면에는 흰빛·회청색으로 나옴」 — 그쪽은 «색이 왜 바뀌나», 이쪽은 «그 바뀐 색을 재고 안심하는» 검사 판입니다 |
| 「권한이 없습니다」는 나오는데 **어느 계정인지** 를 안 말함 | 🔴 **그러면 «내 계정이 뭔지» 를 화면이 아니라 사람이 추측하게 됩니다 — 2026-09-10 사장님이 그 한 줄이 없어 반나절을 잃으셨습니다.** 관리자 주소를 여셨는데 「강사 권한으로는 볼 수 없는 정보입니다」만 나와 서버 설정을 의심하셨는데, 실제로는 그 브라우저가 **강선생님(`hq_t_kang`) 세션**을 들고 있었습니다 — **세션 쿠키는 브라우저당 한 개**라 앞서 한 «강선생님 로그인» 이 관리자 세션을 덮은 것입니다(D1 실측: 08:49 `admin` → 09:04 `hq_t_kang`). ⚠️ **에러가 안 납니다** — 게이트는 정상 동작이고 틀린 것은 «말해 주지 않는다» 하나뿐입니다. ✅ 거절 본문에 **«지금 로그인한 계정»** 을 함께 실으세요. 정본은 `src/forbidden-teacher.ts` 의 `forbiddenTeacherBody(actor, 사유?)` 하나입니다. ⛔ **한 곳만 고치지 마세요** — 2026-09-11 실측(주석 벗긴 사본 전수)으로 같은 문구가 **서버 44곳**(=응답을 만드는 자리 **42** + 순수 게이트 **2**)에 흩어져 있었고, 더 나쁘게 **화면 다섯**(`admin/refunds.html`·`manager.html`·`textbook-uploader.html`·`js/adm-bulkbook.js`·`js/monitor-wall.js`)은 서버 문구를 **안 쓰고 자기 문구를 그립니다.** 서버만 고치면 그 다섯에서 그대로 재발합니다. ✅ 그 다섯은 서버가 완성해 준 **`who_line` 한 문장을 «붙이기만»** 합니다 — ⛔ 문장을 화면에 베껴 적지 마세요(정본이 여섯 벌이 됩니다). ⛔ **`error` 코드(`forbidden_teacher`)는 바꾸지 마세요** — 그 다섯이 코드로 가릅니다. ⛔ **계정을 못 읽었으면 지어내지 마세요** — 사유만 두고 계정 줄을 뺍니다(빈 괄호가 더 헷갈립니다). ⚠️ **기본 문구를 「볼 수 없는 정보입니다」로 두지 마세요** — `detail` 없이 부르는 자리에 **삭제·저장·스위치 켜기가 섞여 있어**(교재 묶음 삭제·강사 계정 연결 저장·자동결제 스위치 …) **막은 것과 다른 말**을 하게 됩니다. 「보다/쓰다」를 안 가리는 말로 두고, 아는 자리는 `detail` 로 그 말을 넘기세요. ⛔ **막힌 대상·볼 수 있는 사람**을 함께 싣지 마세요 — 그건 그 사람이 원래 못 보는 정보입니다(싣는 것은 **부르는 사람 자신의 계정**뿐이고 `/api/admin/me` 가 이미 줍니다). ⚠️ **순수 게이트 함수**(`textbookPurgeGate`·`teacherMoveDenyReason`)는 코드만 돌려줍니다 — 계정은 **응답을 만드는 호출부**가 붙이세요(게이트에 계정을 넘기면 그 함수들의 하니스가 잡고 있는 입력 모양이 깨집니다). 🔴 **덤 — 그 문구가 «닿지도 않는» 죽은 줄일 수 있습니다.** `manager.html` 의 `reqErrOf` 가 `if (st === 403 …)` 를 **먼저** 보고 돌아가서, 서버가 403 으로 주는 `forbidden_teacher` 갈래가 **한 번도 안 걸렸고** 강사에게 「다른 지사·대리점 요청입니다」라는 엉뚱한 말이 나왔습니다. **코드로 먼저 가르고, 모르는 것만 상태값으로** 떨어뜨리세요. ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무슨 글자가 나오는가»·«어느 줄이 이기는가» 뿐입니다. 감시는 `test-harness/forbidden_teacher_who_harness.mjs`(55종 — 정본을 컴파일해 **실제로 돌리고**, 화면 문구 함수(`reqErrOf`·`failText`)도 **오려 내 돌려** 나온 글자를 봅니다. 변이 8종 전부 실제 FAIL). 🪤 이 변경은 **기존 하니스 11개를 깨뜨립니다** — `json({ ok: false, error: 'forbidden_teacher' }, 403)` 라는 **객체 «모양» 을 글자 그대로** 못 박아 둔 검사들이라 **보장은 오히려 세졌는데 검사만** 빨간불이 납니다. 뜻으로 묻도록 고치세요. ⚠️ 그중 **코드를 오려 내 실행하는** 하니스(`enroll_admin_hq_gate`·`textbook_purge_book`)는 `forbiddenTeacherBody is not defined` 로 **죽습니다** — 그건 「막혔다」가 아니니 **정본을 컴파일해 주입**하세요. 📄 `docs/작업기록/260911_강사차단_문구에_계정이름.md` |
| 회귀를 «FAIL 목록으로» 대조했는데 **깨진 하니스가 통째로 빠짐** | 🔴 **`run.mjs --fast` 의 출력은 하니스마다 형식이 다릅니다 — 한 가지 모양으로 뽑아 `comm` 하면 «양쪽에서» 빠져 «새 FAIL 0건» 이 됩니다.** 2026-09-09 실측: 제가 `grep -oE '[a-z0-9_]+_harness — PASS N / FAIL M'` 로 뽑아 대조하고 「새로 생긴 FAIL 0건」이라고 **틀리게 보고**했는데, 정작 제 변경이 깨뜨린 `sidebar_three_level_harness` 는 그 형식으로 요약을 안 찍어 **양쪽 목록에 아예 없었습니다**(CI 게이트 ②가 빨간불인 채였습니다). 이어서 «개별 `❌` 줄» 로 바꿔 봤더니 이번에는 **`run.mjs` 가 하위 하니스의 `❌` 를 그대로 안 흘려 0건 대 0건** — 또 헛돌았습니다. ⚠️ CLAUDE.md 는 「숫자만 비교하지 말고 FAIL **목록**을 `comm` 으로 대조하라」고 이미 적어 두었는데, **그 «목록» 을 어떻게 뽑느냐가 함정**입니다. ✅ **가장 확실한 것은 «내가 건드린 파일을 읽는 하니스» 를 찾아 하나씩 돌려 «종료코드» 를 대조하는 것**입니다 — `grep -rl "<바꾼 파일들>" test-harness/*_harness.mjs` 로 목록을 만들고 각각 `node <파일>; echo $?` 를 before/after 로 `diff` 합니다(2026-09-09 에 87개를 그렇게 대조해 «차이 0건» 을 확정했습니다). ⚠️ 그때 **«그 하니스가 실패하면 정말 종료코드 1 을 내는지» 를 한 번 확인**하세요 — 이 저장소에는 «실패해도 exit 0» 이라 `--fast` 합계가 한 자리도 안 움직인 전례가 있습니다(2장 그 항목). 일부러 깨뜨려 `exit=1` 이 나오는지 보면 10초입니다. ⚠️ **`git stash -u` 로 before 를 재는 동안에는 아무 파일도 편집하지 마세요** — 그 stash 가 편집을 함께 치웠다가 `pop` 에서 충돌하거나 조용히 사라집니다(2장 「`git checkout -- .` 로 내 편집이 날아감」과 같은 뿌리). ⛔ 그리고 「하니스가 전부 초록이니 회귀 없음」을 **완료형으로 적지 마세요** — 위 두 번 다 그렇게 적을 뻔했습니다 📌 **덧붙임(2026-09-11) — 또 밟았습니다.** `--fast` 요약을 `"[a-z_]+_harness — PASS n / FAIL m"` 로 뽑아 `diff` 하고 「하니스별 완전 동일」이라고 적었는데, **그 모양으로 찍히는 줄은 305개 중 «한 개»** 였습니다(양쪽 다 1줄이라 `diff` 가 조용히 0). ✅ `--fast` 가 하니스마다 찍는 실제 모양은 **`▶ <이름>.mjs … ✅`(통과)** 와 **`▶ <이름>.mjs … ⚠  실제 확인 필요`(실패)** 입니다. 🔴 **실패 글자는 `❌` 가 아닙니다** — 2026-09-11 실측(`▶` 319줄 = ✅ **305** · ⚠ **14** · ❌ **0** · ⏭ **0**). 그래서 «`[✅❌⏭]` 로 뽑기» 는 **실패한 14개를 통째로 빠뜨립니다** — 고치려던 그 사고가 그대로 재현됩니다(함정 대조가 잡았습니다). ⚠️ **E2E 로 건너뛴 20개는 `▶` 줄이 «아예 없습니다»**(맨 끝에 `- 이름.mjs` 로만 나열) — 그래서 `▶` 줄 수(319) = PASS + FAIL 이고 합계(339)와 다릅니다. ✅ **뽑은 줄 수를 먼저 세어 보세요**(`wc -l`) — 맨 끝 요약줄의 `PASS + FAIL` 과 안 맞으면 그 추출이 헛돈 것입니다. ✅ 그리고 **«이름만» 뽑아 `comm` 하세요**(`grep '▶' | grep '⚠' | awk '{print $2}'`) — 그래야 «양쪽에 다 빨간 것»(환경 사유)과 «내가 새로 깨뜨린 것» 이 갈립니다 |
| 카드·타일이 넓은 화면에서 **가운데 좁은 띠**로만 그려짐 | 🔴 **폭을 `width:min(1100px,100%)` 로 못 박으면 화면이 아무리 넓어도 그 값 그대로입니다** — 게임 허브가 그랬습니다(2026-09-21 브라우저 실측: **1920·3840·7680px 어디서나 그리드 1100px·3열**, 7680 에서는 화면의 **14%**). 카드가 «작다» 가 아니라 **열이 안 늘어나는** 것이라 코드만 보면 정상으로 읽힙니다. ⚠️ **제보에는 두 원인이 섞여 옵니다** — 그 스크린샷에서 카드 한 장이 88px 인데 코드는 352px 이었습니다(정확히 1/4 = **브라우저 확대 25%**. 브라우저 메뉴 글자만 그대로인 것이 그 증거이고 `Ctrl`+`0` 으로 돌아옵니다). **«작다» 는 확대, «가운데 띠» 는 코드** — 갈라서 보세요. ✅ 고침은 **폭 정본을 한 곳**(`--hub-w:min(2200px,100%)`)에 두고 그리드와 그 위 컨트롤 패널이 **같은 변수**를 쓰게 하기(그 패널 주석이 「그리드와 같은 폭」이라고 약속하는데 숫자가 두 곳에 적혀 있었습니다). `minmax` 를 함께 올리면(280→300px) **카드 크기는 그대로 두고 열 수만** 늘릴 수 있습니다(실측 352 → 357px · 3열 → 5열). 🪤 **그때 «머리글의 좌우 padding» 이 폭을 어긋나게 합니다** — `align-items:center` 인 flex 열에서 패널이 머리글의 자식이면 `100%` 가 «머리글 내용폭» 이라 패널만 그만큼 좁아집니다(실측 1841 대 1873). 여백은 머리글이 아니라 **글자 쪽에** 주세요. ⚠️ **문자열 하니스로는 못 잡습니다** — 값도 규칙도 다 «있고» 틀린 것은 «몇 px 이고 몇 열인가» 뿐이라 수리 전에도 `--fast` 286건이 전부 초록이었습니다. 확인은 브라우저에서 `getBoundingClientRect().width` 와 `getComputedStyle(grid).gridTemplateColumns` 를 여러 폭에서 재는 것뿐입니다 |
| 「카드를 **가장 처음에** 보이게 해줘」 — 위에 쌓인 것을 내릴 때 | 🔴 **내리기 «전» 에 그 블록이 왜 위에 있었는지 보세요 — 설명은 내려도 되지만 «버튼» 은 다릅니다.** 게임 허브는 카드 위에 463px(폰 688px)이 쌓여 있었는데, 그중 퀘스트 안내 바에는 「레벨테스트로 한 번에 열기」·「전체 열기」 **버튼**이 있고 그 자리 주석이 「잠긴 카드를 눌러야만 보이면 **존재를 모르는 학생이 생긴다**」라고 적어 두고 있었습니다. 카드가 21장이라 그 아래로 내리면 폰에서 **11행을 지나야** 나옵니다. ✅ 폭에 따라 값이 다르면 **마크업이 아니라 `order` 로** 가르세요 — 넓은 화면은 카드 위(62px 이라 첫 화면을 안 밀어냄), 폰만 카드 아래(228px). ⚠️ **`order` 를 쓰면 뒤따르는 형제의 차례도 함께 적어야 합니다** — 기본값 0 이라 안 적은 형제가 앞으로 옵니다(새로 넣는 형제도 마찬가지라 그 자리에 주석을 남기세요). ⚠️ 그리고 **`position:absolute` 인 줄을 흐름 안으로 되돌릴 때**(폰에서 제목 글자를 덮어서) `left`·`right` 가 무시되므로 `align-items:center` 를 받아 **가운데로 옮겨집니다** — 뒤로가기 ←가 좌상단에서 상단 중앙으로 말없이 이사합니다. `width:100%` 를 함께 주세요. ✅ 확인은 `#hub-menu` 자식들의 `getBoundingClientRect().top` 을 폭별로 재는 것 — 「옮겼다」와 「첫 화면에 보인다」는 다릅니다 |

| 역할·권한으로 감추는 class 를 붙였는데 **화면에는 그대로** (class 는 붙어 있음) | 🔴 **원인이 «둘» 이고 둘 다 조용합니다 — 한쪽만 고치면 반쪽입니다.** 관리자 사이드바 역할 감춤(`.ia6-role-hide`)이 그랬습니다. **① 이기는 규칙** — `adm-ia6.js` 가 `document.head` 에 주입하는 `#ph85-sidebar .ia6-role-hide{display:none!important}`(1,1,0)와 `admin-inline-c.css` 의 겨루는 규칙 **셋**(`#ph85-sidebar .ph85-group{display:block!important}` · `#ph85-sidebar .ph85-sub{display:block!important}` · `#ph85-sidebar .ph85-sub{display:flex!important}`)이 **특이성 동률**(1,1,0)인데, ⛔ 행 번호로 적지 마세요 — 그 파일은 계속 자랍니다(이 표의 「행 번호로 가리키지 마세요」와 같은 자리). 게다가 그 블록의 자기 주석이 「어떤 역할로 hide 해도 ph85 사이드바는 강제 visible」이라 **역할 감춤을 이기려고 만들어진 규칙**입니다. 그 CSS 는 `<head>` 가 아니라 **`<body>` 안**에서 링크되어 언제나 «뒤» 라 **언제나 이깁니다.** ⟹ class 는 붙는데 computed display 가 `flex` — **역할 감춤이 한 번도 안 먹고 있었습니다**(2026-09-09 실측). ✅ 클래스를 하나 더 붙여 (1,2,0)으로 올려 이기세요(`.ph85-sub.ia6-role-hide`). ⛔ 그 `flex` 규칙을 지워서 풀지 마세요 — 항목 정렬이 통째로 걸립니다. **② 판정이 그 항목을 아예 안 봅니다** — `applyRoleFilter` 는 «가리키는 카드가 전부 감춰졌나» 로 판정하는데, 딴 페이지로 가는 항목(`href`)은 카드가 없어 `known=0` → **언제나 «남긴다»** 입니다. 그래서 서버가 403 으로 막는 화면(환불·영업 실적·수업 관제탑)이 강사·지사 사이드바에 그대로 떴습니다. ⚠️ 그 사실은 `adm-ia6.js` 주석 **두 곳**에 이미 적혀 있었습니다 — 「적어 둔 것 ≠ 고친 것」 그대로입니다. ✅ 항목마다 **금지목록**(`hideFrom`)을 들려 판정하세요. ⛔ **허용목록으로 뒤집지 마세요** — 역할이 하나 늘면 그 사람 메뉴가 «조용히» 사라집니다(빠지는 쪽이 훨씬 나쁩니다). ✅ 역할을 «모르면»(신원 미도착·조회 실패) **아무것도 감추지 않습니다** — 정본은 `window.__ADM_ME.role`(`js/adm-identity.js`)이고 그 사이 눌러도 서버가 거절합니다. ⚠️ 신원은 **늦게** 옵니다 — `mangoi:identity` 를 듣지 않으면 영영 안 감춰집니다. 그 이벤트는 `window` 가 아니라 **`document`** 에서 발행됩니다(bubbles:false). ⚠️ 역할 어휘가 **두 벌**입니다 — 카드 쪽 `_applyMenuVisibility` 는 `hq_exec`·`hq_mgr`·`hq_teacher`, 서버 `resolveRole()` 은 `teacher`·`hq`·`staff`·`franchise`·`branch`·`agency`. **섞지 마세요.** ✅ 금지목록 값의 근거는 **«그 화면이 부르는 API 를 서버가 실제로 막는가»** 입니다(`TEACHER_BLOCKED_PREFIXES`·`isAgencyAllowedApi`·핸들러 게이트를 열어 확인). 추측으로 늘리지 마세요. ⛔ **서버 가드가 없는 화면을 «메뉴만» 감춰서 덮지 마세요.** ⚠️ 이유를 「화면만 감추기는 금지」로 적지 마세요 — CLAUDE.md 원문은 「**서버 403 과 화면 감추기 둘 다** 필요합니다」 = «불충분» 이지 «금지» 가 아닙니다. 진짜 이유는 둘입니다: ⓐ **서버 가드가 없는 것이 진짜 문제**인데 메뉴를 감추면 그 문제가 눈에서 사라지고 ⓑ 지금 그 화면을 쓰고 있을 조직 계정이 **이유도 모른 채 못 쓰게** 됩니다(빠지는 쪽). 📌 **「수강 운영」(`/enroll-ops.html`)이 바로 그 경우라 2026-09-09 에는 «일부러 안 감췄고», 2026-09-10 사장님 「수강 운영 게이트 달아줘」로 서버가 본사 전용이 된 «뒤에» 화면도 함께 감췄습니다**(`enrollAdminHqOnly` ↔ `hideFrom` — 짝). ⛔ 순서를 뒤집지 마세요 — 화면부터 감추면 그 구멍이 눈에서 사라집니다. 📄 `docs/작업기록/260909_역할별_사이드바_숨김.md`·`260910_수강운영_본사전용_게이트.md`. ⚠️ **문자열 하니스로는 둘 다 못 잡습니다** — 함수도 값도 규칙도 다 «있고» 틀린 것은 «누가 이기는가 / 누가 무엇을 보는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 감시는 `test-harness/manual/admin-sidebar-role-hide-browser.mjs`(브라우저 58종 — **자동으로 안 돕니다, 사람이 부릅니다.** 변이 9종 전부 실제 FAIL). ⚠️ **역할은 `window.__ADM_ME` 만 보세요** — `admIdentity()` 로 떨어뜨리면 `localStorage['admin_session']` 의 «지난 방문 값» 을 믿게 되어, 공용 PC 에서 앞사람이 강사였을 때 **본사 사람의 메뉴가 사라집니다**(빠지는 쪽). 🪤 **`adm-ia6.js` 의 `GROUPS` 안에는 «상수 이름» 을 쓰지 마세요 — 순수 리터럴이어야 합니다.** 하니스 셋(`today_menu_split`·`admin_site_structure_sync`·`admin_sidebar_ia6_click`)이 그 블록만 오려 내 **`eval`** 하므로 블록 «밖» 의 이름은 정의되지 않아 **GROUPS 가 통째로 `null`** 이 됩니다(2026-09-09 `ORG_ROLES` 로 실제로 밟음) |
| 조직 계정을 **`getAdminActor().role` 로** 막았는데 D1 이 한 번 흔들리면 **그대로 통과** | 🔴 **그 함수는 스코프 조회 실패를 «삼키고» 조직 계정을 `staff`(본사 동급)로 돌려줍니다.** `getAdminActor()` 는 스코프를 «못 구해도» `scopeType='none'` 으로 떨어지고, `resolveRole('none', …)` 은 이름도 비어 있으면 어느 분기에도 안 걸려 **`staff`** 를 돌려줍니다. ⚠️ **그 자리를 `auth-admin.ts` 의 `try/catch` 로만 알지 마세요** — `getScope()` 안의 D1 접근이 전부 **`s_safe`**(`scope.ts`)로 감싸여 있어 **던지지 않고 조용히 null** 을 돌려주고, 그러면 `autoSeedOne()` 이 이름으로 추측하며 이름마저 못 읽으면 `'none'` 입니다. ⛔ 그 `try/catch` 를 걷어내는 것으로는 «고쳤다» 가 아닙니다. ⟹ `if (isOrgScopedRole(actor.role)) return 403` «하나만» 둔 게이트는 **모르는 순간 열립니다** — CLAUDE.md 「가드에 필요한 값을 `safe(…, null)` 로 조회하면 fail-open」의 형제이고, 이쪽은 **함수가 대신 삼켜 줘서 호출부에서는 안 보입니다.** ⚠️ **에러도 로그도 안 납니다** — 화면은 정상 동작하고 지사 계정 하나가 본사 화면을 받습니다. 📊 [잰 것 — 2026-09-10] 계정 49개 중 **4개(`agency_sc002`·`capitown`·`cfo`·`ops_lead`)는 `admin_scope` 행이 아예 없어** 매번 `autoSeedOne()` 의 «이름으로 추측» 을 탑니다(전부 정확히 추측됨 — 즉 평소에는 잘 돌고 **흔들릴 때만 샙니다**). ✅ 되돌릴 수 없는 조작(문자 발송·수업 강사 변경·급여 배율)을 지키는 게이트는 **근거(`admin_scope.scope_type`)를 «삼키지 않고» 한 번 더 읽고, 못 읽으면 막는 쪽으로 실패**시키세요(본보기 `enrollAdminHqOnly`, `src/enroll-ops.ts`). ⛔ 그 재조회를 «행이 없으면 통과» 로 두지 마세요 — 바로 앞 `getAdminActor()` 가 `getScope()`→`autoSeedOne()` 으로 **행을 심어 놓으므로**, 그러고도 없다면 심기까지 실패한 것입니다. ⚠️ 판정을 복제하지 말고 **정본 `isOrgScopedRole()`** 에 물으세요(검사도 그 정본을 **읽어서** 쓰세요 — 손으로 베끼면 정본이 좁아질 때 게이트는 뚫리는데 검사만 초록입니다). 🪤 **재조회로도 «못» 막는 변종이 있습니다** — `autoSeedOne()` 이 그렇게 만들어진 `'none'` 을 **`INSERT OR IGNORE` 로 영구히 심고**, 그 뒤로는 재조회가 그 값을 «정상적으로» 읽어 통과시킵니다. 즉 막히는 것은 «못 읽는 순간» 이지 «잘못 심긴 값» 이 아닙니다(정본 수리는 `autoSeedOne` 이 «이름을 못 읽었으면 안 쓰게» 하는 쪽 — 반경이 있어 **사람이 정할 일**, 2026-09-10 미결). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «모를 때 어느 쪽으로 실패하는가» 뿐입니다. ✅ 검사는 게이트를 **오려 내 실제로 돌리고**, 「던지면 막는다」 옆에 **「본사는 통과한다」를 짝으로** 두세요(짝이 없으면 «전부 막기» 도 통과합니다 — 실측). 🪤 **같은 fail-open 이 `subScopeDenied`(대체강사)에도 남아 있습니다** — 그쪽은 `actor.role` 만 보고 `isOrgScopedRole` 이 거짓이면 **조건절 검사 자체를 건너뜁니다**. 아직 안 고쳤습니다(사람이 정할 일 — 2026-09-10). ✅ 게이트의 **«위치»** 도 재세요(첫 라우트보다 앞인가) — «있는가» 만 보면 호출을 라우트 아래로 옮겨도 초록입니다. 감시는 `test-harness/enroll_admin_hq_gate_harness.mjs`(35종 — 변이 9종 전부 실제 FAIL) |
| 「하니스를 하나씩 돌려 **종료코드**로 회귀를 봤는데 CI 만 빨간불」 | 🔴 **이 저장소에는 «FAIL 을 찍고도 `exit 0`» 인 하니스가 실재합니다** — 그러면 종료코드 대조는 **원리상 아무것도 못 봅니다.** 2026-09-09 실측: 바뀐 파일을 읽는 하니스 76개를 돌려 «종료코드 전부 0» 이라 보고했는데, CI 는 그중 **3건이 FAIL**(`today_menu_split` 은 «PASS 49 / FAIL 2» 를 찍고도 exit 0). ⚠️ `--fast` 도 못 잡았습니다 — `node_modules` 가 없는 컨테이너에서는 그 셋이 아예 안 돌아 로컬 PASS 296 대 CI PASS 310 이었습니다. ✅ 대조는 **출력의 `❌` 개수**로 하세요(종료코드 아님). ✅ 그리고 반드시 **기준선과 짝으로** — `git worktree add /tmp/base origin/main` 으로 `origin/main` 사본을 만들어 **같은 목록을 같은 방식으로** 돌린 뒤 `join` 으로 «내 쪽에서 늘어난 것» 만 보세요. 그래야 «환경 사유로 원래 빨간 것»(이 컨테이너 기준 15건)과 «내가 깨뜨린 것» 이 갈립니다. ⛔ `git stash` 로 기준선을 만들지 마세요 — 커밋 안 한 편집이 함께 치워졌다가 충돌하거나 조용히 사라집니다(이 표의 `git checkout -- .` 항목과 같은 뿌리). ⚠️ 그리고 **「종료코드가 0 이니 회귀 없음」을 완료형으로 적지 마세요** — 저는 그렇게 적었고 CI 가 정정해 줬습니다 |
| 「지금 진행 중인 수업」에 **수업이 아닌 방이 섞여** 뜸 · 「⚠ 혼자 대기중」이 몇 시간째 켜져 있음 | 🔴 **화면이 고장난 것이 아니라 «사실» 을 말한 것입니다 — 그런데 그 사실을 잘못된 이름으로 부릅니다.** `/api/active-rooms` 는 KV 목록을 그냥 뿌리지 않고 **방마다 DO 에 `/status` 를 물어 인원 0이면 그 자리에서 KV 키를 지웁니다** (`handleActiveRooms`) — 그러니 «찌꺼기가 남았나» 부터 의심하지 마세요. 목록에 떴다 = **그 순간 소켓이 실제로 붙어 있었다** 입니다. ⚠️ 문제는 그 카드 제목이 「수업」인데 그 API 는 **방 종류를 가리지 않는다**는 것입니다 — 회의방(`meet-…`)은 예약이 없어 «수업» 칸이 «—» 이고 **끝나는 시각이 없어** 탭을 닫기 전에는 사라지지 않습니다(죽은 소켓 청소는 «브라우저가 죽었을 때» 만 돕니다 — 탭이 살아 있으면 25초마다 ping 이 가서 «정상 접속» 이 맞습니다). ⟹ 「⚠ 혼자 대기중」이 **몇 시간이고** 켜져 있고, 그 표시를 만든 목적(급히 봐야 할 줄)이 오히려 죽습니다(2026-09-10 `meet-1234` 09:05~ 실측). 🔴 **그때 그 경고는 «노란색» 이 아니었습니다** — 관리자 카드 안에서는 `[id^="card-"] :is(p,span,div,…)` 의 `#101828 !important` 가 **인라인 앰버를 검정으로 눌러** 두 표시가 같은 색이었습니다(함정 대조 브라우저 실측 `rgb(16,24,40)`). ⟹ **그 카드에서 색으로 뜻을 지고 가려면 «클래스 + 조상 id 꼬리»(1,1,0) 뿐입니다** — 인라인 `style="color:…"` 는 한 글자도 안 먹습니다(선례 `.tr-st-badge` v=63 · 이번 `#card-admin-ghost .gh-alone` v=75). ⛔ 앰버 `#fbbf24` 로 되돌리지 마세요(흰 카드 위 대비 **1.67**) — `#b45309`(5.02)입니다. ⚠️ 🗼 관제탑은 자기 CSS 를 써서 그쪽 배지는 실제로 노란색이 맞습니다 — **화면마다 다릅니다.** ✅ 2026-09-10 수리 — 판정 정본은 **`_ghIsMeetRoom`(`js/adm-s1.js`) 한 곳**이고 회의방을 **접힌 «회의방 N개 · 수업 아님» 구역**으로 내립니다. ⛔ **줄을 지우지 마세요** — 참관·직접입장은 회의방에서도 필요합니다(감추면 「기능이 없어졌다」가 됩니다). 노란 경고만 회색 «혼자» 로 낮추고 **사실은 그대로 적습니다.** ⛔ **개수를 한 숫자로 합치지 마세요**(«화상방 N개») — 회의방이 수업 건수로 읽힙니다. ⛔ **서버(`handleActiveRooms`)에서 거르지 마세요** — `src/index.ts` 는 금지구역이고, 서버가 미리 지우면 「회의방도 보고 싶다」를 되돌릴 길이 사라집니다. ⛔ **`meet-` 을 소문자로만 판정하지 마세요** — `MEET-1234` 는 `idFromName` 이 대소문자를 구분해 **다른 방**인데 그것도 회의방으로 보여야 합니다(`/^meet-/i`). ⛔ 반대로 `class-`·`c24-`·`demo-`·`room-`·`mangoi-class` 는 한 글자도 건드리지 마세요 — **진짜 수업이 접힌 구역으로 숨는 쪽이 훨씬 나쁩니다.** ⚠️ **「N분째 혼자」는 지금 원리상 못 그립니다** — DO 의 `/status` 응답에 **입장 시각이 아예 없습니다**(`roomId`·`userCount`·`users`·교재상태뿐). 실으려면 `video-call-room.ts`(공동 금지구역) 수정이라 **별건·별도 승인**입니다. ⛔ 「혼자 N시간이면 자동으로 닫기」로 풀지 마세요 — 오판하면 **수업 중에 끊깁니다.** ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무엇이 어느 구역에 그려지는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 감시는 `test-harness/meet_room_split_harness.mjs`(21종 — `node:vm` 에 **가짜 DOM·가짜 fetch** 를 깔고 `adm-s1.js` 를 **통째로 실행해** 나온 HTML 을 `<details>` 안팎으로 갈라 보고, 판정은 소스에서 오려 내 실제로 돌립니다. ⚠️ **짝으로** 물으세요 — 「회의방엔 노란불이 없다」만 두면 «전부 없애기» 도 통과합니다. 변이 4종 전부 실제 FAIL). ✅ **🗼 관제탑도 같은 날 함께 갈랐습니다**(`js/monitor-wall.js` — `isMeetRoom`·`splitRooms`, 칩도 «수업방/회의방» 으로 갈라 세고 「⚠ 혼자 대기」 칩은 **수업방만** 셉니다). ⚠️ 그 화면은 **HTML 12KB · HTML+JS 52KB 상한**(`monitor_wall_harness` ① — 2026-09-13 D안으로 50→52, 09-14 사장님 확정)이 있어 주석을 길게 달 수 없습니다 — 지금 여유 ~0.4KB. ⚠️ `list.map(rowHtml)` 로 부르지 마세요 — 둘째 인자가 **«순번»** 이라 둘째 줄부터 전부 회의방이 됩니다(하니스 ⑬이 **주석을 벗겨 낸 사본**으로 봅니다 — 그 경고 주석 자신이 걸렸던 자리입니다). 📌 곁가지 별건 둘: **퇴장 기록 뒤에도 하트비트가 `last_seen_at` 을 갱신**하고, `/api/attendance/leave` 가 (방, **기기id**)로 «가장 최근 미종료 행» 을 닫아 **같은 기기의 옛 유령 행과 세션 통계까지 덮어씁니다** — 그래서 «그 접속이 몇 시간 살아 있었나» 를 그 두 칸으로 판정하면 틀립니다. 📄 `docs/작업기록/260910_관리자_지금진행중인수업_meet1234_혼자대기중_진단.md` |
| 기능이 «안 도는» 것이 아니라 **닿을 사람이 없는** 것 (알림·문자·푸시) | 🔴 **«감지» 와 «발송» 을 갈라서 세지 않으면 «기능이 없다» 로 오진합니다.** 2026-09-10 실측: 수업 30분 전 리마인더 cron 은 **정상 동작 중**이었고(`lesson_reminder_log` 826행 · 마지막 당일 14:00) 최근 7일 **671건의 수업을 정확히 찾아냈는데 문자는 0건**이었습니다 — `students_erp` 29,485행의 `phone`·`student_phone`·`parent_phone`·`kakao_id` 가 **전부 0건**이라 보낼 곳이 없었기 때문입니다. ⚠️ **에러도 경고도 안 납니다** — 그 자리는 `no_phone` 으로 «정상 종료» 하고 로그만 남습니다. ✅ 이런 제보를 받으면 **①감지 건수 ②발송 건수 ③받는 사람 번호 보유율** 을 각각 세세요. 셋이 갈리는 자리가 원인입니다. 🔴 **그리고 «번호를 D1 에 넣으면 된다» 로 끝내지 마세요** — **2026-09-15 «전» 에는** `students_erp` 의 번호 세 칸이 카페24 UPSERT 의 SET 목록에 있어 **매일 밤 03:00 KST 에 무조건 덮였습니다**(DELETE 보존 목록에도 없었습니다). 실제로 8월에 문자가 나갔던 체험계정 `lt15`·`lt16`·`lt18` 이 그렇게 번호를 잃었습니다 — 그 세 건이 «우리 화면에서 받은 번호는 나간다» 는 증거이자 «그 표에 두면 사라진다» 는 증거였습니다. ✅ **2026-09-15 에 그 뿌리를 막았습니다**(`cafe24-sync.ts`) — UPSERT 가 `COALESCE(NULLIF(TRIM(excluded.x), ''), students_erp.x)` 로 **«카페24가 값을 줄 때만»** 덮고, **DELETE 보존 목록에도 세 칸이 들어갔습니다.** 📊 **[잰 것 — 2026-09-16, 운영 D1] 배포돼 돌았고 아무 번호도 안 잃었습니다 — 다만 «이 수리가 살렸다» 는 아직 증명이 아닙니다.** 03:00 cron 이 `cafe24-sync` 를 **371초** 돌았고(`corpcard_meta['nightly:0 18 * * *:run']` 에 `last_ok`), 센티넬 행이 **29,496 → 29,508** 로 «되돌아왔습니다» — DELETE 가 그 행들을 먼저 비우고 INSERT 가 되채우는 구조라 루프가 중간에 끊겼으면 이 숫자가 **만 단위로 줄어듭니다.** ⟹ 「ALTER 실패 → DELETE 가 `no such column` 으로 죽고 동기화가 조용히 멈춘다」는 **최악 경로는 일어나지 않았습니다**(번호 개수도 전후 동일 3·4·0). 🔴 **그런데 번호가 든 유일한 센티넬 행 `jeong` 은 증거가 못 됩니다** — `password_hash`·`level` 이 차 있어 **옛 다섯 칸 조건에서도 이미 보존되던 행**입니다(⟹ 새 세 줄이 «새로» 남긴 행은 아직 **0건**). 옛 코드에서 그 번호를 지웠을 것은 UPSERT 의 «무조건 덮기» 인데 그건 **카페24 payload 에 `jeong` 이 있을 때만**이고, 그 소속은 못 쟀습니다(이름은 동기화 직후 override 가 되입히고 `updated_at` 은 전 행이 같은 상수라 가릴 칸이 없습니다). ✅ **증명하려면 «번호가 있으면서 다섯 칸이 전부 빈 센티넬 행»** 이 필요합니다 — 그런 행이 처음 생기는 날이 첫 시험입니다. ⚠️ **그 둘은 짝입니다** — DELETE 가 행을 먼저 지우면 UPSERT 가 «새 INSERT» 로 떨어져 COALESCE 가 **한 번도 발동하지 않습니다**(카페24 학생 전원이 그 경우라 한쪽만 고치면 효과가 0입니다). ⚠️ 보존 조건은 **칸마다 한 줄씩**이라 «한 줄만» 지우는 변이를 잡으려면 fixture 에 **번호가 한 칸만 찬 학생**이 있어야 합니다 — 실제 데이터가 그 모양입니다(`createTrialStudent` 는 `phone` 하나만, 자가가입은 `parent_phone`+`phone` 만 넣습니다). 세 칸이 다 찬 행만 시험하면 **한 줄씩 지워도 전부 초록입니다**(실측으로 밟았습니다). ⛔ **그래도 «번호를 그 표에만» 두지 마세요** — 카페24가 번호를 채우기 시작하면 그때부터는 카페24 값이 이깁니다(정본이 그쪽이라 맞는 동작이고, 그게 곧 «우리가 넣은 번호가 덮이는» 경우입니다). ⚠️ 그리고 그 COALESCE 는 **«카페24가 번호를 지운 것»과 «카페24에 원래 없는 것»을 구별하지 못합니다** — 번호가 바뀌어 카페24에서 빈 값이 오면 **옛 번호가 남아 틀린 번호로 학부모에게 갑니다**(「틀린 번호가 빈칸보다 나쁩니다」). ⚠️ DELETE 보존을 넓힌 대가도 있습니다: 카페24를 떠난 학생이라도 번호가 있으면 행이 남아 **`status` 가 옛 값에 굳습니다** — `status` 로 세는 명부·정산·리포트가 그 사람을 계속 «재학» 으로 셉니다(오늘 반경 0건이지만 번호를 채울수록 늘어납니다). ✅ 우리가 받은 번호는 **`student_erp_override`**(동기화가 안 건드리는 표)에 두고, 읽는 정본 **`phonesForStudent`**(`notify-contacts.ts`)가 **그것을 먼저** 보고 없으면 명부로 떨어집니다. ⛔ 같은 판정을 리마인더·알림 코드에 복제하지 마세요. ⚠️ **fail-open 방향이 축마다 다릅니다** — 번호 «조회» 실패는 통과(명부로 떨어짐 = 예전과 같음), 번호 «저장» 실패는 **반드시 사람에게 말하기**(조용히 넘기면 「넣었으니 가겠지」로 믿는데 안 갑니다 — 이 표의 「보냈습니다라고 하는데 아무 데도 안 갔음」과 같은 뿌리). 🪤 **입력칸을 새로 만들 때 «이미 등록된 것» 을 잊지 마세요** — 등록 화면의 칸은 «앞으로 등록되는» 학생에게만 걸립니다. 그것만 만들면 **지금 수업 중인 학생에게는 영영 못 씁니다**(테스트 명단 9명이 정확히 그 경우였습니다). 목록 쪽에 고치는 길을 «짝으로» 두세요. ⚠️ 그때 목록이 그리는 값은 **«발송이 실제로 읽는» 값**이어야 합니다 — 신청서에 적힌 값을 그리면 둘이 갈렸을 때 화면이 옛 값을 말합니다. ⚠️ 번호가 없을 때 «—» 로 두지 말고 **«문자 안 감»** 이라고 적으세요(빈칸은 «고장» 이나 «모름» 으로 읽힙니다). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무슨 답이 나오는가» 뿐이라 수리 전에도 `--fast` 314건이 전부 초록이었습니다. 감시는 `test-harness/enroll_parent_phone_harness.mjs`(44종 — 정본을 번들해 **실제로 돌리고** 저장 SQL 은 **진짜 SQLite** 에 실행. 변이 14종 전부 실제 FAIL) 📌 **덧붙임(2026-09-15) — 그 정본을 «안 보는» 경로가 남아 있었습니다.** 수강 만료 안내(`runEnrollExpirySweep`, `enroll-ops.ts`)가 `students_erp` 를 **직접 읽어** 번호를 정하고 있어서, **화면에서 번호를 받은 학생에게는 한 건도 안 나갔습니다**(2026-09-15 실측: 그날 23건 시도 · 번호를 찾은 학생 **1명** — 그 1명은 명부에 번호가 남아 있던 직접 등록 계정이고, `student_erp_override` 로 받은 번호는 이 경로에 한 번도 닿지 않았습니다). ⛔ **「번호를 저장하는 길을 만들었다」를 「모든 문자가 그 번호를 본다」로 읽지 마세요** — `phonesForStudent` 를 부르는 곳을 `grep` 으로 세는 데 30초면 됩니다. ⚠️ 이 sweep 은 **하루 한 번(KST 10:00)** 돌고 `exp7`·`exp3` 두 날만 발화하므로, 그날 번호가 없으면 그 기회는 돌아오지 않습니다. ✅ 고칠 때 **「override 로 나간다」 옆에 「명부에만 있어도 예전처럼 나간다」·「아무 데도 없으면 안 보낸다」를 짝으로** 검사하세요(앞만 보면 «전부 막기» 도 통과합니다). 🪤 그 검사는 함수를 **오려 내 실제로 돌려야** 합니다 — 「그 함수를 부르는가」로 물으면 **부르고 결과를 안 쓰는 변이가 통과합니다**(실측). ⚠️ 타입 표기를 걷어낼 때 **배열(`as any[]`)을 «먼저»** 지우세요 — ` as any` 를 먼저 지우면 `(x as any[])` 가 `(x[])` 가 되어 그 절이 통째로 «실행 실패» 로 죽습니다(실제로 밟았습니다). ⚠️ **폴백 기준을 «비었나» 로 묻지 마세요** — `normPhone` 은 9자리도 통과시키는데 발송 게이트는 «10자리 미만 skip» 이라, 9자리 override 하나가 명부의 멀쩡한 11자리를 가로막고 그대로 skip 됩니다(= 이 수리가 «되던 것» 을 깨는 유일한 방향). **하류 게이트와 «같은» 기준**(`phone.length < 10`)으로 물으세요. 🔴 **그리고 「배선 누락이 닫혔다」로 읽지 마세요 — 닫힌 것은 «두 곳» 뿐입니다.** 정본을 안 보는 학부모 문자 경로가 **5곳** 남아 있습니다: `api-lessons.ts`(수업일지 → `/api/eval/create` — 강사 화면이 **매 일지마다** 부르는, 가장 자주 도는 경로) · `api-pay.ts`(자동결제 사전안내 — **돈이 걸린 경로**) · `api-admin.ts`(AI 초안 승인) · `enroll-activate.ts`(등록 확정 안내 · 화면 기본값 OFF) · `api-games.ts`(microlearn). ✅ 하니스 I절이 그 **이름을 찍어 출력**합니다(FAIL 아님 — 선례 `popup_open_return_harness`). 고치면 «✅ 이제 정본을 봅니다» 로 바뀌니 목록에서 빼세요. ⚠️ **그 «찍어 주는» 검사는 주석을 벗긴 사본으로 판정하세요** — 「왜 아직 안 고쳤나」를 적은 설명 주석이 잡혀 «이미 고쳐졌다» 고 거짓으로 찍힙니다(2026-09-15 `api-pay.ts` 에서 실제로 밟았습니다). «import 가 있는가» 가 아니라 **«부르는가»** 로 물으세요. ⚠️ **실패가 여전히 조용합니다** — `src/index.ts` 가 `sent > 0 || !ok` 일 때만 로그해서 **번호를 못 찾아 전부 skip 된 날은 로그가 0줄**입니다(그래서 이 누락이 몇 달 안 보였습니다). `skipped > 0` 도 남기는 것이 맞지만 그 파일은 **공동 금지구역**이라 사람이 정할 일입니다. 감시는 같은 하니스 **I절**(12종). 🪤 **그 절은 처음에 «시나리오가 배타적이라» 원리상 헛돌았습니다** — «override 만»·«명부만» 둘로는 「누가 이기는가」를 못 봐서, **정본을 폴백으로 내리는 변이(= 이 수리를 통째로 되돌리기)·학생 번호 폴백 삭제·학부모↔학생 우선순위 뒤집기** 셋이 **전부 통과**했습니다(함정 대조 실측 PASS 56 / FAIL 0). ✅ **«둘 다 있는» 시나리오를 짝으로** 두세요(override+명부 · 학부모+학생). 🪤 그리고 **가짜 D1 이 «표 이름» 만 보면 `WHERE user_id = ?` 를 지우는 변이(= 남의 번호로 문자가 나갑니다)가 통과합니다** — 질의문을 보고 «좁히지 않으면 «남의 행» 을 주게» 만드세요(2026-09-15 에 CLAUDE.md 「학생 목록 예약칸」 항목에 들어간 그 규칙의 형제). 이제 변이 **7종**(Ⓗ정본을 폴백으로·Ⓐ학생폴백 삭제·Ⓑ우선순위 뒤집기·Ⓒ`WHERE` 제거·Ⓓ호출 제거·Ⓔ결과 안 씀·Ⓕ9자리 코너 되살리기) **전부 실제 FAIL** 📌 **그리고 「테스트로 등록한 학생에게 문자가 안 온다」 제보를 받으면 «수업 날짜» 부터 보세요** — 리마인더는 **시작 15~45분 전**에만 나갑니다(`lesson-reminder.ts`). 2026-09-15 실측: 직접 등록한 학생 4명 중 예약이 있는 사람은 `yahee` 하나였고 그 수업이 **이틀 뒤**라 «한 번도 발송 대상이 아니었습니다» — 고장이 아닙니다. ⚠️ 등록·배정 시점에 나가는 문자는 **없습니다**(`parent_notify_queue` 에 8/24 이후 2건이 `queued` 로 남아 있고 그 큐를 비우는 코드가 없습니다 — 별건). ✅ 바로 시험하려면 **오늘 날짜로 «지금부터 40분 뒤»** 수업을 한 건 넣으세요(15분 cron 이라 다음 회차에 잡힙니다). ⛔ 15분보다 가깝게 잡으면 창 밖이라 안 나갑니다. ℹ️ 직접 만든 계정은 카페24에 그 `user_id` 가 없어 **UPSERT 가 아예 안 닿습니다** — 그래서 명부 번호가 밤 동기화를 견딥니다(2026-09-14 등록분이 9/15 03:00 뒤에도 남은 것을 실측). ⚠️ **그것을 「카페24 학생 번호는 밤에 지워진다」의 근거로 쓰지 마세요** — 그쪽도 2026-09-15 에 막혔습니다(#995). 지금 카페24 학생 번호가 0건인 이유는 «덮여서» 가 아니라 **원본에 값이 없어서**입니다 📌 **덧붙임(2026-09-16) — 「번호를 지웠는데 문자가 계속 간다」: 학생 쪽 폴백이 «세 겹» 입니다.** `phonesForStudent`(`notify-contacts.ts`)가 **`ovStudent || student_phone || phone`** 순으로 읽는데, 관리자 학생 상세의 연락처 저장(`PATCH /api/admin/student/:uid/contact`)은 `allowed` 목록에 **`phone` 칸이 없어** 그 칸을 한 번도 안 건드립니다. 게다가 **카페24 야간 동기화가 학생 번호를 `student_phone` «과» `phone` 두 칸에 넣습니다**(`cafe24-sync.ts` 의 `sPhone, sPhone`) ⟹ override 를 지우고 `student_phone` 을 NULL 로 만들어도 **`phone` 이 남아 문자가 갑니다**(그 칸이 채워진 학생에게만 — 기록상 「phone 9개」, 오늘 값은 `SELECT SUM(phone IS NOT NULL AND TRIM(phone)<>'') FROM students_erp` 로). ⚠️ **학부모 쪽은 폴백이 둘**(`ovParent || parent_phone`)이라 지우기가 그대로 먹습니다 — **학생·학부모가 비대칭**입니다. ⛔ 그러니 화면이 **「이제 문자가 가지 않습니다」라고 단정하면 거짓**입니다(제가 그렇게 적었고 함정 대조가 잡았습니다) — 「남은 번호가 있으면 그 번호로 갑니다」까지만 말하세요. ⛔ `phone` 을 그 `allowed` 에 넣어 풀지 마세요 — 카페24 정본 칸이라 그날 밤 다시 채워집니다(정본을 카페24에서 고치는 별건). ⚠️ 그리고 **override 의 «지우기» 는 tombstone 이 아닙니다** — 칸을 NULL 로 만들 뿐이고 `getOverridePhones` 는 NULL 을 «지정 없음» 으로 읽어 명부로 떨어지므로, 카페24가 그 번호를 주기 시작하면 되살아납니다 |
| 회선이 나빠 영상을 껐는데 받는 쪽이 **«검은 화면»** 이 됨 — 마지막 장면이 안 남음 | 🔴 **`track.enabled = false` 는 «안 보내는» 것이 아니라 «검은 프레임을 보내는» 것입니다.** [잰 것 — 2026-09-10, Chromium 1194 WebRTC 루프백, 30ms 간격 픽셀 측정] `enabled=false` → 받는 쪽 `<video>` 가 **49ms 만에 밝기 0**(min·max 전부 0)이 되고, 원격 트랙은 `muted=false`·`live` 그대로라 «정상 도착» 으로 보입니다. 게다가 그 검은 프레임에 **약 10 kbps** 를 계속 씁니다 — 대역폭을 되찾으려고 끈 것인데 완전히 회수되지 않습니다. ✅ **`sender.getParameters()` → `encodings[0].active = false` → `setParameters()`** 로 끄면 받는 쪽이 **«마지막 장면에서 멈추고»**(7초 뒤까지 픽셀 동일) **보내는 바이트가 정확히 0**, **재협상 0회**, 복구는 **96ms** 입니다. ⛔ **`replaceTrack(null)` 로 풀지 마세요** — 같은 «멈춤» 을 주지만 `sender.track` 이 비어서, 이 저장소에서 영상 sender 를 `s.track && s.track.kind==='video'` 로 찾는 코드 **10곳 넘게가 조용히 헛돕니다**(화면공유·가상배경·카메라 자가치유·장치교체·사무실모드). 특히 화면공유 시작은 sender 를 못 찾으면 **addTrack + 재협상** 으로 빠지는데, 그 자리 주석이 「대역폭 위기 중에 연결을 다시 맺는 것은 최악」이라고 못 박아 둔 바로 그 동작입니다. ⛔ **`enabled=false` 를 «함께» 쓰지 마세요** — 검은 프레임 한 장이 먼저 나가면 그 «검정» 에서 얼어붙습니다. ✅ **알림을 «끄기 전» 에 보내세요** — `cam-state` 가 늦으면 상대 감시견(`vcRemoteBlackWatch`)이 7초 뒤부터 최대 5회 재협상을 겁니다(수신 트랙이 `muted` 가 되는 데 실측 1.0초). ✅ `active=false` 는 **`getParameters`→수정→`setParameters` 왕복에도 살아남습니다** — 4초마다 도는 `applyStep` 과 가상배경 교체(`vcSwapVideoTrack`)를 그대로 재현해 확인했습니다(둘 다 멈춤 유지·바이트 0). 그래서 **AAO 중에 배경을 바꿔도** 그 새 화면이 복구 때 정확히 나갑니다. ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무엇이 보이는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 화면은 **얼굴을 덮지 말고** 위쪽 띠 + 흑백 + **«N초 전»** 경과 시간으로 — 멈춘 그림이 «지금» 으로 오인되면 이 파일이 가장 나쁘다고 못 박은 방향입니다. ⛔ **`reason==='user'`(사람이 일부러 끔)에는 절대 적용하지 마세요** — 껐는데 얼굴이 남으면 프라이버시 사고입니다. ⛔ **보여 줄 마지막 장면이 없으면**(한 프레임도 안 온 상대) «멈춤» 이라 말하지 마세요 — 검은 바탕에 그렇게 적으면 거짓말입니다. ⛔ **화면공유 중에는 손대지 마세요** — 그때 sender 가 쥔 것은 «화면» 이라 끄면 교재가 사라집니다. 🔴 **«손대지 않는다» 가드를 «양방향» 에 걸지 마세요 — 되돌릴 길이 사라집니다.** 화면공유 중에는 영상을 끄면 안 되니 함수 첫 줄에 `if (공유중) return;` 을 뒀는데, 그것이 **«켜기» 까지 막았습니다.** `active=true` 로 되돌리는 코드는 저장소에 그 함수 한 곳뿐이고 부르는 곳도 한 곳뿐이라, **회복 시점에 공유 중이면 그 뒤로 아무도 되살리지 않아 영상이 수업 내내 안 돌아옵니다.** 게다가 회복 알림(`cam-state camOn:true`)은 나가므로 받는 쪽은 **띠와 흑백을 걷어내** «아무 안내 없는 멈춘 그림» 이 됩니다 — 이 수리가 막으려던 바로 그 상태입니다. ✅ **«건너뛰기» 가 아니라 «상태를 지정»** 하세요(`var want = 공유중 ? true : !!on;`) — 한 줄로 ⓐ 공유 중 끄기 금지 ⓑ 공유 중에도 켜기 ⓒ AAO 중에 공유가 «시작» 되면 되살리기 ⓓ 공유가 «끝나면» 다시 멈추기가 전부 성립하고, 4초 타이머가 저절로 따라잡습니다. ⚠️ 그때 **«실제로 보내는가» 가 뒤집히면 상대에게 다시 알리세요** — 안 그러면 살아 움직이는 교재 위에 «N초 전 모습» 이 얹힙니다. ⚠️ 이 결함을 **하니스가 못 잡고 있었습니다** — 「공유 중엔 안 끈다」만 묻고 **「공유 중에도 켠다」를 짝으로 안 물었기** 때문입니다. 같은 이유로 「AAO 중이면 다시 건다」만 있고 **「AAO 가 아니면 안 건드린다」가 없어**, «모든 수업에서 4초마다 전원의 영상을 끄는» 변이가 **46/46 초록**으로 통과했습니다(함정 대조가 실제로 넣어 확인). 🔴 **덧붙임(2026-09-11) — 얼굴을 덮던 상자를 «띠» 로 바꿨더니 그 띠가 이번엔 «누를 것» 을 덮었습니다.** 브라우저에 그려서 픽셀을 재니 **⭐ 칭찬 버튼과 💬 개별채팅 버튼이 통째로 띠 색**이었습니다(390px 폰 실측: 별버튼 중앙 픽셀 `rgb(121,53,15)` = 띠의 갈색. 띠는 `top:0` 전폭 z-index 9, 버튼은 `top:6/8px` z-index 8). 띠가 `pointer-events:none` 이라 **«눌리기는 하는데 안 보이는»** 상태였고, 하필 그 버튼이 **1P=1원 포인트를 주는 자리**입니다. ⚠️ `elementFromPoint` 는 `pointer-events:none` 을 건너뛰므로 **«맨 위가 누구인가» 만 보면 «정상» 이라 답합니다** — 이 사고는 «칠해진 색» 으로만 드러납니다. ✅ **띠를 옮기지 말고 그 밑의 것을 내리세요** — 띠 높이를 재서 `--aao-h` 로 넘기고 `.video-box.vc-aao-on` 에 걸린 CSS 가 모서리 조각을 그만큼 내립니다. ⛔ 클래스를 «버튼» 이 아니라 **«타일» 에** 붙이세요 — `vcRefreshPraiseUI` 가 버튼을 다시 그려도 살아남습니다. ⛔ `transform` 으로 내리지 마세요 — `.vc-star-btn:hover{transform:scale(1.1)}`·`.vc-star-toast.show{transform:translateY(0)}` 가 이미 쓰고 있어 서로 지웁니다. `top:calc(base + var(--aao-h))` 로. ⚠️ `!important` 가 필요합니다(`#vc-local-box .vc-star-btn{top:6px}` 이 특이성으로 이깁니다). 🔴 **그리고 «위쪽 모서리» 는 한 칸이 아니라 «세로로 쌓인 칸» 입니다 — 하나만 내리면 그 밑칸에 올라탑니다.** ⭐6 · ⇱분리6 · 포인트바구니6 · 🖥공유배지6 · 💬8 · 📶회선경고8 · 🎛40(장치 도우미) · +1P토스트48 · vpb-fly48 — **아홉**이 한 줄로 쌓여 있어서, ⭐·💬 만 내렸더니 **💬 가 🎛 위에 얹혀** «가린 것을 옮겨 또 가리는» 상태가 됐습니다(실측으로 잡음). ✅ 검사는 «덮였나» 만이 아니라 **«누를 수 있는 조각끼리 서로 겹치지 않는가»** 를 짝으로 두세요 — 그것이 그 두 번째 결함을 잡은 검사입니다. 🔴 **그 짝으로도 못 보는 것이 둘 더 있었습니다 — 그래서 «픽셀» 이 최종 심판입니다.** ⇱ **분리 버튼**(`z-index:5`)은 «겹침» 검사가 «눌리는 조각» 끼리만 봐서 안 걸렸고 — 게다가 검사가 타일을 «손으로» 만드느라 `vcAddDetachButton` 을 안 불러 **0 of 0 으로 통과**하고 있었습니다. 📶 **회선 경고**(`.vc-netlow-hint`, `z-index:9` · `pointer-events:none`)는 띠와 **z-index 가 같아** 「아래 깔렸나」에도, `pointer-events:none` 이라 「겹치나」에도 안 걸렸습니다 — 하필 **AAO 와 같은 조건**(회선이 나쁠 때)에 뜨는 문구입니다. ✅ 그래서 최종 검사는 **«띠와 겹친 조각의 가운데가 띠 색으로 칠해졌는가»** 하나로 물으세요 — z-index·DOM 순서·pointer-events 와 무관하게 «보이는가» 만 봅니다. ⚠️ 그때 **띠 색을 «띠 가운데 픽셀» 로 재지 마세요** — 거기는 글자가 지나가 회차마다 값이 바뀝니다 (실측으로 `rgb(111,49,14)` 와 `rgb(156,111,84)` 가 번갈아 나와 변이시험이 되다 말다 했습니다). **글자를 잠깐 `color:transparent` 로 만들고 찍으면** 그 자리가 합성이 끝난 진짜 배경입니다. ⚠️ 그리고 검사가 타일을 손으로 만들면 **실제 경로가 붙이는 조각이 빠집니다** — `vcAddDetachButton`·`vcNetPeerMark` 처럼 «그 화면이 실제로 부르는 함수» 를 함께 부르고, **«그 조각이 실재하는가» 를 전제 검사로** 두세요(없으면 0 of 0 으로 조용히 통과합니다). ⚠️ 역할에 따라 붙는 것이 다릅니다 — ⇱ 분리는 **학생 화면**(칭찬 UI 가 없을 때), 📶 경고는 **강사 화면** 전용입니다. 한 시나리오로는 둘 다 못 봅니다. ⛔ 숫자를 검사에 베껴 적지 마세요 — **base top 을 소스에서 읽어** 비켜서기 CSS 와 대조합니다(`aao_freeze_harness` E-10). ⚠️ 자동 하니스는 **배치를 못 잽니다** — 가짜 DOM 이라 «가려졌나» 가 원리상 안 보입니다. 그래서 «어느 값이 넘어가는가» 만 자동으로 두고, **«무슨 색으로 칠해졌나» 는 브라우저**가 잽니다(`test-harness/manual/vc-aao-freeze-browser.mjs` 46종 — 자동으로 안 돕니다, 사람이 부릅니다). 감시는 `test-harness/aao_freeze_harness.mjs`(74종 — 가짜 peer·가짜 DOM 으로 **실제로 돌리고** 변이 14종 전부 실제 FAIL. ⚠️ base top 을 소스에서 읽을 때 **`idx-vc-qlog.js` 자신도 읽어야** 합니다 — 빠뜨려 📶 경고가 통째로 새어 나갔습니다. 결함이 있던 판으로 되돌리면 B-10~B-12 가 실제로 FAIL). 📄 `docs/작업기록/260910_음성전용_검은화면_대신_화면멈춤.md` 📌 **덧붙임(2026-09-15) — 그 «멈춤» 은 «대개» 되는 것이지 «언제나» 가 아닙니다.** `encodings.active=false` 로 끄면 보통 `<video>` 가 마지막 프레임에서 멈추지만, **그 전에 트랙이 죽거나(회선 붕괴·재협상) 첫 프레임이 한 장도 안 왔으면 `videoWidth` 가 0** 이고 그러면 게이트가 **옛 전면 덮개**(`.vc-camoff-hint`, 불투명 82%)로 떨어져 **얼굴이 통째로 사라집니다.** 사장님 화면(2026-09-15 `class-848` 05:54)이 그 상태였습니다 — 교사 자리가 「연결이 약해 지금은 음성만 전송 중이에요」 글자만 남고 새까맸습니다. ⟹ 사장님 「음성만 나올 땐 교사 얼굴이 멈춤 상태라도 나오게. 검게 하지 말고 **반드시 마지막 모습이 계속**」. ✅ 고침은 **영상이 살아 있는 동안 4초 틱마다 한 장을 캔버스로 떠 두는 것**(`__vcAaoStill`, uid별 JPEG dataURL 폭 320 — 실측 1.5KB) — 검어지면 그 그림을 `z-index:2`(이름표 3·띠 9 «아래» 라 그 둘을 안 가림)로 깝니다. **한 번이라도 얼굴이 온 상대라면 그 뒤로 무엇이 끊겨도 마지막 모습이 남습니다.** 🔎 **«어떻게 맞출지»(`object-fit`)는 내가 정하지 말고 «그 영상» 에게서 베끼세요** — 정본 `vcSmartFitVideo`(`js/idx-main.js`)가 타일마다 따로 정합니다(내 타일 가상배경 `contain`·화면공유 `contain`·폰 세로 상대 타일 `cover`). 한쪽을 박아 두면 **멈추는 순간 그림이 확 커지거나 잘려** «방금 보던 그 화면» 이 아니게 됩니다(함정 대조가 잡음). ✅ 그 함수는 `videoWidth` 가 0 이면 첫 줄에서 돌아가므로 **검어진 뒤에도 인라인 값이 «살아 있던 마지막 판정» 그대로** 남아 있습니다. ⚠️ 검사는 «contain 이다» 만 묻지 말고 **짝으로**(cover 이던 타일은 cover) 두세요 — 한쪽만 두면 «전부 cover»(옛 코드)도 «전부 contain»(엉터리 수리)도 통과합니다(변이 2종 실측). 🔒 **사람이 «일부러» 끈 카메라(`cam-state 'user'`)의 그림은 그 자리에서 버리세요** — 껐는데 얼굴이 남으면 프라이버시 사고입니다(위 ⛔ 와 같은 줄기). ⛔ **멈춤 중인 타일은 다시 뜨지 마세요** — 떠 두는 것은 «멈추기 직전» 의 모습이어야 하는데, 뜨면 «멈춘 그림» 을 떠서 덮어씁니다. ⛔ **살아 있는 영상 위에는 깔지 마세요**(`videoWidth>0`) — `<video>` 자신이 이미 그 장면을 붙잡고 있어 덧그릴 이유가 없습니다. ⛔ **새 타이머를 만들지 마세요** — 이미 있는 4초 틱(`vcqRxStart`→`vcAaoTick`)에 얹습니다(홈이 멎은 전력 2회). 나간 상대의 그림은 버립니다(메모리·프라이버시). ⚠️ **한 프레임도 안 온 상대는 원리상 못 채웁니다** — 그때는 옛 전면 안내가 «사실» 이라 그대로 둡니다(검은 바탕에 「영상 멈춤」은 거짓말). ⚠️ **가짜 DOM 으로는 «tainted canvas» 를 못 잽니다** — `toDataURL` 이 진짜 `<video>` 에서 던지지 않는지는 브라우저가 `captureStream()` 으로 확인합니다(실측: 1,519자 JPEG · 영상을 죽인 뒤 **187×302 로 실제로 그려짐**). 🪤 그 브라우저 검사에서 **SETUP 이 `videoWidth` 를 «가짜 getter»(`Object.defineProperty`)로 고정**해 두어 진짜 영상을 물려도 값이 안 바뀌고 **새 절이 통째로 헛돌았습니다** — 걷고(`delete v.videoWidth`) 재세요. ⚠️ 그리고 SETUP 이 이미 AAO 를 걸어 둔 «멈춤 중» 상태에서는 일부러 안 뜨므로 **먼저 해제하고** 재야 합니다. 감시는 `aao_freeze_harness` **Ⓕ절**(가짜 canvas 로 실제로 돌림 — 변이 6종 전부 실제 FAIL이고 «원본에서는 조용한가» 를 **전제 검사로** 둡니다)과 `manual/vc-aao-freeze-browser.mjs` **⑪절**(브라우저 12종 + ⑥절 짝 1 — 자동으로 안 돕니다, 사람이 부릅니다) |
| 상태를 «다시 거는» 코드를 넣었는데 **한쪽 방향만** 재적용함 (끄기만·켜기는 한 번) | 🔴 **그 한 번이 실패하면 되돌릴 사람이 아무도 없습니다 — 그리고 조용합니다.** 2026-09-15 실측: 음성전용(AAO) 의 4초 타이머 `vcAaoTick` 이 «끄기»(`A.active`)만 다시 걸고 «켜기» 는 `vcAAOApply` 의 복구 갈래에서 **딱 한 번** `vcAAOVideo(1)` 이었습니다. 그런데 `active=true` 로 되돌리는 코드는 저장소에 `vcAAOVideo` **한 곳뿐**이고 부르는 곳도 한 곳뿐이라, 그 `setParameters` 가 한 번 거절되면(**같은 4초 주기의 `applyStep` 이 같은 sender 에 `getParameters→setParameters` 를 걸어 스냅샷이 어긋납니다 — 화질 회복 `__qGood>=8` 과 AAO 복구 `good>=8` 이 «둘 다 약 32초 조용함» 이라 같은 틱에 함께 발화할 개연성이 높습니다**) **영상이 수업 끝까지 안 돌아옵니다.** ⚠️ **에러도 화면 표시도 없습니다** — `.catch(function(){})` 가 삼키고, 소리는 멀쩡해서 «수업은 되는데 얼굴만 안 돌아온다» 로만 보입니다. ✅ 재적용은 **«상태를 지정» 하는 양방향**으로 (`var want = (A && A.active) ? 0 : 1;`) — 바뀔 때만 실제로 쓰므로(`cur === want` 조기반환) 평소 비용은 0입니다. ⛔ **실패를 삼키지 마세요** — 그 거절 한 줄이 원인을 사후에 가릴 유일한 근거입니다. ⚠️ **대칭으로 만들 때 «남이 일부러 꺼 둔 것» 을 되살리지 않는지 보세요** — `idx-vc-sfu.js` 의 `cutMeshVideo()` 가 **같은 `vcPeerConnections`** 의 영상 sender 에 `encodings[].active=false` 를 겁니다. 가드(`vcAaoSfuCut`) 없이 넣으면 4초마다 되살려 **영상이 두 갈래(mesh+SFU)로 나갑니다.** ✅ 그리고 **«켰다» 를 «복구 성공» 으로 판단하지 마세요** — `outbound-rtp.framesSent` 가 실제로 느는지 보고 안 늘면 말합니다(`vcAaoVerify`). ⛔ 그 자리에서 **재협상·`restartIce` 를 걸지 마세요**(회선이 방금 나빴던 자리입니다 — 되살리는 일은 4초 재적용이 합니다). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «다시 거는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 검사는 «켠다» 옆에 **«AAO 중엔 안 켠다»·«SFU 가 끊어 뒀으면 안 켠다»·«프레임이 늘면 경보 안 한다»·«카메라 끈 사람에겐 경보 안 한다» 를 짝으로** 두세요. 감시는 `test-harness/aao_freeze_harness.mjs` C-21~C-29(126종 — 2026-09-16 에 main 의 «마지막 얼굴» 검사와 합쳐짐 — 가짜 DOM 으로 그 절을 **실제로 돌립니다**. 변이 9종을 **진짜 소스에 넣어** 전부 실제 FAIL 확인 — 그중 «거절을 도로 삼키기» 는 함정 대조가 «무방비» 로 잡아 준 것이다). 🔴 **그런데 가짜 sender 로는 «실제로 멈추고 다시 나가는가» 를 원리상 못 봅니다** — `setParameters` 가 객체를 바꿔치기만 하므로 «active 를 되돌렸다» 까지가 전부이고, 정작 이 수리의 목적인 «프레임이 다시 흐른다» 는 한 번도 재지 않습니다. ✅ 그래서 **진짜 `RTCPeerConnection` 둘을 로컬로 붙여** 받는 쪽 `framesDecoded` 와 **화면 픽셀**로 재는 검사를 따로 둡니다 — `test-harness/manual/vc-aao-realpc-browser.mjs`(29종 — **자동으로 안 돕니다, 사람이 부릅니다**: `PW_DIR=/tmp/pw node test-harness/manual/vc-aao-realpc-browser.mjs`. 받는 쪽을 **다른 탭**으로 옮긴 시나리오도 함께 봅니다). ⚠️ **프라이버시는 픽셀로만 증명됩니다** — 사람이 카메라를 끈 것은 «트랙»(`track.enabled=false`)이고 AAO 는 «인코딩»(`encodings.active`)이라 축이 다른데, 그 전제를 «추론» 으로 두면 `s.track.enabled = true` 한 줄을 더하는 그럴듯한 변이가 그대로 통과합니다. 실측: 그 변이를 넣으니 **받는 화면이 «검정»(luma<6)에서 «밝음»(>8)으로 뒤집혀** 끈 카메라의 얼굴이 실제로 나갔습니다(F-2·F-3 이 잡습니다). ⛔ **그 luma 절대값을 문서에 적지 마세요** — 가짜 카메라가 «움직이는 무늬» 라 회차마다 달라집니다(같은 변이가 49~55 로 나옵니다). ⚠️ 되돌려 보면 실제로 FAIL 납니다 — 옛 «한쪽짜리 재적용» 으로 되돌리면 **9건**(D-2 가 «증가 0» 으로 실사고 증상 그대로), «바뀔 때만» 가드 둘을 빼면 **2건**(B-1·B-2). 🔴 **그리고 «이름이 음성전용인데 소리도 끊는» 변이가 한동안 무방비였습니다** — 영상 검사만 촘촘히 두면 오디오 인코딩까지 함께 끄는 한 줄이 **29종 중 한 건도 안 걸립니다**(실측 27/0 통과). ✅ 받는 쪽 `inbound-rtp(kind:"audio")` 의 `packetsReceived` 가 그 구간에도 느는지를 **짝(브라우저 C-4)으로** 두세요. ⚠️ **그 브라우저 검사만으로는 부족합니다** — `manual/` 이라 게이트가 물어 가지 않습니다. 자동으로 막는 것은 `aao_freeze_harness` **B-4b·B-4c**(가짜 오디오 sender 를 «안정된 객체» 로 두고 «아예 안 건드렸는가» 를 셉니다)뿐입니다. ⛔ `makePc` 의 오디오 sender 를 «매번 새 객체» 로 되돌리지 마세요 — 그 순간 이 검사가 원리상 무력해집니다. ⛔ 그 변이시험은 **저장소 파일을 고쳤다 되돌리는 방식으로 하지 마세요** — `QLOG_SRC=<고친 사본>` 으로 가리킬 수 있게 해 두었습니다(git 은 «방금 쓴 것» 을 모르고, `public/` 안에 백업을 두면 `deploy.ps1` 이 통째로 올립니다). 📄 `docs/작업기록/260915_음성전용_회복뒤_영상이_안돌아오던_비대칭재적용.md` |
| 실수를 막으려고 **«잠금»**·«확인 한 번 더» 를 걸 때 | 🔴 **«저장이 안 되던 것» 을 고치면 그 자리에 «실수로 한 번 하면 그대로 나간다» 는 반대 위험이 생깁니다 — 고침과 같은 날 함께 보세요.** 2026-09-11 주간 스케줄이 그랬습니다: 드래그가 18일간 아무것도 저장하지 않아 실수로 끌어도 무해했는데, 그 저장을 고친 순간부터 한 번 끌면 수업 시각·담당 강사가 바로 바뀝니다(학생 홈 「수업 입장」 카드·리마인더가 따라갑니다). 사장님 지시로 **기본을 «잠김»(보기 전용)** 으로 두고 **저장 뒤 15초 되돌리기**를 붙였습니다. ⛔ **잠금을 «드래그 시작» 에만 걸지 마세요** — 같은 일을 하는 입구가 셋이면(드래그·우클릭 「연기/변경」·「이동」 모달) 나머지 둘이 그대로 나갑니다. **최종 방어선은 공용 저장 함수 한 곳**(`persistSlotMove`)이고 드래그 차단은 «끌리지도 않게» 하는 편의입니다. ⚠️ 그 판정을 **mousedown 에 두지 마세요** — 카드를 눌러 상세를 여는 평범한 클릭마다 경고가 뜹니다. 실제로 끌기 시작하는 자리(8px 판정)에서만 막고, 그때 `moved=true` 를 세워 뒤따르는 click 이 삼켜지게 하세요(안 그러면 잠금 경고와 상세 모달이 함께 뜹니다). ⛔ **되돌리기를 «화면만» 되돌리는 방식으로 만들지 마세요** — 그것이 바로 이 사고의 뿌리입니다. 서버에 원래 값을 다시 보내고 끝나면 서버에서 다시 읽습니다. ⛔ **되돌리기가 잠금에 걸리면 안 됩니다** — 15초 사이에 자동 잠금이 오면 «되돌릴 수 없는» 상태가 되는데 그건 이 기능을 만든 이유와 정반대입니다(`opts.undo` 예외). ⛔ **«반쯤 되돌린» 상태를 만들지 마세요** — 강사를 바꿨는데 원래 강사를 모르면 되돌리기를 **내놓지 않습니다**(시간만 원래대로 + 강사는 새 사람이 더 나쁩니다). ⛔ 되돌리기 버튼을 기존 토스트에 넣지 마세요 — `.dnd-toast` 는 `pointer-events:none` 이라 **보이는데 안 눌립니다**. ⛔ **상주 `setInterval` 금지** — 초읽기는 «편집 중» 에만 살고 잠그면 스스로 멈춥니다. ⚠️ 잠금 버튼 라벨은 **그릴 때 `data-ko`/`data-en` 도 함께** 갱신하세요(🌐 를 누르면 옛 상태로 되돌아갑니다). ✅ **검사는 «막힌다» 와 «켜면 된다» 를 반드시 짝으로** — 앞만 두면 «전부 막기» 도 통과하는데, 여기서 «전부 막기» 는 사장님이 수업을 아예 못 옮기는 상태라 **고치려던 것보다 나쁩니다.** 🪤 **«변이 N종 전부 FAIL» 이라고 적기 전에 «원본이 그 검사를 통과하는가» 를 전제 검사로 두세요** — 제 검사 하나가 원본에서도 실패해서(자동 잠금 뒤 타이머를 «0개» 로 못 박았는데 실제로는 잠금·되돌리기 **둘**) **변이 8종이 전부 «검출» 로 보였습니다.** 그 줄이 없었으면 «전부 FAIL» 이 아무것도 증명하지 못한 채 남았습니다. 🪤 그리고 **«이미 다른 겹이 막고 있는» 곳을 변이 대상으로 고르면 그 변이는 원리상 실패하지 않습니다** — 되돌리기 재제안은 `prev=null` 이 이미 막고 있어서 `!opts.undo` 만 지우는 변이가 그대로 통과했습니다(두 줄을 함께 되돌려야 실제로 틀려집니다). 🪤 검사 러너가 **숫자를 «그때 값» 으로 담으면** 되돌리기를 누른 «뒤» 를 못 잽니다(살아 있는 게터로 주세요). 감시는 `test-harness/schedule_drag_persist_harness.mjs` ⑩~⑬(187종 — 잠금 모듈을 **소스에서 오려 내 가짜 DOM·가짜 시계로 실제로 돌립니다**)와 `test-harness/manual/weekly-schedule-lock-browser.mjs`(브라우저 30종 — **자동으로 안 돕니다, 사람이 부릅니다.** 진짜 마우스로 끌어 보고 `elementFromPoint` 로 «맨 위인가» 를 잽니다. 변이 실제 FAIL 5건) |
| 「수업에서 나가면 다시 못 들어와요」 — 강사가 종료 뒤 재입장 못 함 | 🔴 **같은 수업에 «문» 이 둘인데 서로 다른 답을 합니다.** 강사 포털(`api-teacher.ts`)은 2026-08-07 마이마이 요청 ⑤⑪ 로 **그날 00:00~23:59 하루 종일** 열려 있는데(`enter_from_ts`/`enter_until_ts`), 홈 화면은 `vcJoinMyClass()` 의 **`status === 'ended'` 하드블록**이 **역할을 안 가리고** 막았습니다(= 종료+15분). 그리고 수업에서 「나가기」를 누르면 **홈으로 떨어지므로**(`index.html` 의 `location.replace('/?_e=…')`) 강사가 다시 들어가려는 «그 순간» 만나는 것은 늘 **닫힌 쪽**이었습니다. ⚠️ **방 자체에는 시간 잠금이 한 줄도 없습니다**(`video-call-room.ts` 실측 0건) — 잠그는 것은 «화면이 방 번호를 찾아 주느냐» 뿐입니다. 📊 [잰 것 — 2026-09-11 `class-2332-20260911`(15:00~15:20, 창은 15:35:00 에 닫힘)] 15:18:31 강사 입장 → 15:25:20 재입장 → **15:34:04 재입장 → 15:35:28 퇴장 → 그 뒤 입장 기록 0건.** 제보의 “I entered at 3:35pm but I can't log in again” 이 정확히 이 지점입니다. 그 수업에 `class_no_show` 가 **2건**(둘 다 `notified_push=1`·`waited_min=0`) 찍혔고, `teacher_no_show` 로 확정되면 **수업료가 0원**입니다. ✅ 정본은 **`src/class-entry-window.ts`** 하나 — 강사·관리자는 하루 종일(포털과 **같은 답**), 학생은 시작−10분 ~ `max(종료+15분, min(종료+60분, 다음 수업 시작−5분))`. ⛔ **`close_at_ts` 를 늘려서 풀지 마세요** — 상태 라벨(진행중/완료)·카운트다운·「지금 진행 중인 수업」이 전부 거기 걸려 있어, 늘리면 끝난 수업이 한 시간 동안 «진행중» 으로 떠 관제탑·오늘수업이 통째로 거짓말합니다. «수업 시간인가»(`join_open`)와 «문을 열어 줄 것인가»(`can_enter`)를 **따로** 내려주세요 — `api-teacher.ts` 가 2026-08-07 에 이미 쓴 선례입니다. ⛔ **바닥(종료+15분)을 없애지 마세요** — 실측 활성 1,263회차 중 뒤에 수업이 있는 998건에서 **간격이 0분 이하인 것이 287건(28.8%)** 이라, 바닥이 없으면 그 회차의 창이 «다음 수업−5분» = 종료보다 **앞** 으로 계산돼 **지금보다 짧아집니다.** ⛔ **「다음 수업 −5분」을 «강사를 막아 준다» 로 읽지 마세요** — 강사 창은 하루 종일이라 그 값이 강사를 제한하지 않습니다. 강사가 옛 방에 남아 다음 수업 학생을 혼자 두는 것은 **창으로 못 막습니다**(사람이 두 방에 동시에 못 있는 문제). 그리고 **그 겹침은 지금도 이미 있습니다** — 간격 0분인 283회차에서는 현재의 15분 유예가 이미 다음 수업 시작 후 15분까지 옛 방을 열어 둡니다(결석 감지 창은 시작+10~40분). 답은 「다음 수업이 곧 시작됩니다」 안내로 강사를 다음 방으로 끌어오는 것이고 **별건**입니다. ⚠️ **막는 문구를 한국어 전용으로 두지 마세요** — 옛 코드가 `alert('오늘 수업은 이미 종료되었어요.')` 였고, 필리핀·중국인 강사에게는 「무언가 떴는데 읽을 수 없다」 = **“can't log in”** 으로 보입니다(그 줄만 한/영 병기가 빠져 있었습니다). ✅ 문구는 **서버가 만들어 보내세요** — `js/idx-main.js` 는 blocking 이라 한국어 문자열을 새로 넣을 자리가 없습니다(그 수리 때 여유가 **70바이트**였고, 한 줄짜리 포인터 주석 88바이트를 넣자 그 자리에서 예산 FAIL 이 났습니다 — **주석도 못 넣습니다**). ⚠️ **급여는 안 부풉니다** — 수업료는 실제 접속 시간이 아니라 예약된 길이로 계산합니다(`api-admin.ts` 의 `duration_min ?? 30`). 늘어나는 것은 출석 기록뿐입니다. ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무슨 답이 나오는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 🪤 이 수리는 **기존 가드 하나를 «보장은 그대로인데» 깨뜨렸습니다** — `teacher_page_problem_260807_harness` 의 「학생 입장 창은 건드리지 않았다」가 「`api-mango.ts` 에 `enter_from_ts` 가 **없다**」였습니다. 그건 «검사가 헛돈 것» 이 아니라 **그 검사가 지키던 전제를 사람이 바꾼 것**이라 **그냥 느슨하게 풀면 안 됩니다** — 「학생이 **여는** 시각은 그대로다(OPEN_BEFORE 10분)」·「학생 창은 하루 종일이 아니다」·「바닥이 있다」 **셋으로 옮겨 적고** 왜 옛 단정을 버렸는지 날짜와 함께 남기세요. 🔴 **처음 수리는 «수업이 하나뿐인 강사» 에게만 닿았습니다**(함정 대조가 잡음) — 뒤에 수업이 하나라도 남으면 서버 `current` 가 그 «다음 수업»(`status='early'`)으로 채워지고, 선택 목록 조건이 `sessions.length > 1 && !current` 라 목록이 안 뜬 채 `status==='early'` 분기가 먼저 잡혀 **카운트다운(닫기만 가능)** 으로 빠집니다 — 끝난 수업으로 갈 길이 없습니다. ✅ `(!current \|\| current.status === 'early')` 로 고쳤습니다. ⛔ 되돌리지 마세요. ⚠️ **「수업이 하나면 되니까 된다」로 시험하지 마세요** — 제보자(MAIMAI)가 마침 그날 수업이 1건이라 제 시뮬레이션이 통과했습니다. **뒤에 수업이 남은 경우를 짝으로** 넣으세요. ⚠️ 그리고 바닥 15분의 «지금» 정본은 `api-mango.ts` 의 `LATE_AFTER` 입니다 — 둘을 각각 15 리터럴로 못 박으면 한쪽을 20분으로 바꿔도 둘 다 초록입니다(A-23 이 대조). 감시는 `test-harness/class_entry_window_harness.mjs`(49종 — 정본을 **타입 제거로 실제 실행**해 경계값을 넣고, 「막힌다」 옆에 **「막히지 않으면 입장까지 간다」를 짝으로** 둡니다. 변이 **7종** 전부 실제 FAIL — «조건 뒤집기»(`canEnterNow` → `return true`)를 포함해서. ⚠️ 변이 대응 번호를 **눈대중으로 적지 마세요**, 제가 그랬다가 4/5 가 틀렸습니다). 📄 `docs/작업기록/260911_수업_재입장_시간창.md` |
| 아바타 «입모양 3장» 을 생성 모델로 만들 때 | 🔴 **생성 모델은 «입만» 바꿔 주지 않습니다 — 같은 프롬프트로 입모양만 다르게 세 장을 뽑으면 얼굴 크기·눈썹·볼이 함께 달라집니다.** 그대로 넣으면 `js/mango-avatar.js` v8 이 적어 둔 **Emma 사고**(사장님 「입이 너무 빨리 움직여」 → 실제 원인은 «한 번 바뀔 때 얼굴째 움직인다»)가 그대로 재현됩니다. ✅ **한 장을 기준으로 나머지를 정렬한 뒤 «입 부위만 오려 얹으세요»** — 입 밖이 픽셀 단위로 같아집니다(2026-09-14 메이 실측: 정렬은 `scale 1.0 · dx 0 · dy 0` 로 «이미 맞았고», 다른 것은 «얼굴 안» 이었습니다). ⛔ **«달라진 곳» 을 전부 합성하지 마세요** — 볼·눈썹까지 딸려 와 얼굴 전체를 갈아 끼우게 됩니다(처음에 그렇게 짰다가 입 부위가 896×1120 중 (188,268)–(656,797) = 얼굴 전체로 나와 알았습니다). 가르는 기준은 **«가장 센 변화»**(입 안의 어두운 구멍 + 이). ⛔ **입만 오리지 마세요** — «턱은 닫힌 채 입만 벌어진» 얼굴이 됩니다. 아래쪽(턱)을 넉넉히 포함하세요. ✅ 다 만들면 **픽셀로 재서 숫자를 남기세요**(세로 10칸의 «바뀐 픽셀 %»): 메이 **0.33~1.32%, 입 부근 3칸만** · Lily 2.4~3.4% · **Emma(사고) 11.4~18.3% · 10칸 전부**. ⛔ 세 장을 «각각 따로» 다시 뽑아 넣으면 그 순간 이 숫자가 무너집니다. ⚠️ **이 환경에서는 이미지를 못 받습니다**(프록시 403) — 임시 브랜치의 CI 워크플로가 러너에서 받아 변환·커밋하게 하고 **미리보기 JPEG 를 함께 커밋해 눈으로 보세요**(「투명 픽셀 80%」 같은 숫자로는 멀쩡해 보입니다 — 2026-08-31 Lily 건과 같은 교훈). ⛔ 임시 워크플로는 main 에 올리지 마세요. ⚠️ 그리고 **푸시 프록시가 브랜치 «삭제» 를 거부합니다**(`Everything up-to-date` 로 되돌아옴) — 지울 수 없으면 **그 브랜치에서 워크플로와 산출물을 지워** 더는 안 돌게 하고 사람에게 알리세요. 🔴 **덤 — `js/mango-avatar.js` 는 `defer` 라 화면의 `setCharacter` 요청이 «버려집니다».** 화면 인라인 스크립트가 먼저 도는데 그때 `window.MangoAvatar` 가 아직 없어서, **저장된 친구를 골라 둬도 부팅 때는 Emma 가 뜨고 `teacher-avatar` 942KB 를 받습니다**(2026-09-14 실측 — `origin/main` 에서 저장값을 lily 로 두고 열면 lily 그림 **0건** · teacher-avatar **2건**). 화면이 `window.__mgAvatarWant` 에 남기고 모듈이 부팅 때 이어받습니다. ⚠️ 그 전역을 **안 세우는 화면의 동작은 바뀌지 않아야** 합니다 — 짝으로 검사하세요. ⚠️ **그 고침을 넣으면 «그림을 받았는가» 를 «요청 수» 로 세던 검사가 거짓 FAIL 납니다**(부팅이 이미 받아 두고 브라우저가 캐시에서 줍니다) — 물어야 할 것은 «몇 건 받았나» 가 아니라 **«그 파일이 닿고 그려지는가»** 입니다. 감시는 `test-harness/avatar_image_frames_harness.mjs` 와 `test-harness/manual/warmup-zh-friend-browser.mjs`(브라우저 25종 — 「입만 다른가」를 픽셀로 잽니다. 자동으로 안 돕니다, 사람이 부릅니다) |
| 이미 열린 마이크의 **소리 설정(AGC·에코제거)을 나중에 바꾸려** 할 때 | 🔴 **크로미움은 `applyConstraints` 로 그것을 «조용히 무시합니다» — 에러도 안 나고 `getSettings()` 가 옛 값 그대로입니다.** [잰 것 — 2026-09-11] `autoGainControl:true` 로 연 트랙에 `applyConstraints({autoGainControl:false})` → **예외 없음 · 적용 후에도 `true`**. `getCapabilities()` 에는 그 이름이 버젓이 있습니다(그래서 «되겠지» 하고 짜게 됩니다). ⟹ 오디오 처리 체인은 **트랙을 만들 때 정해지고 그 뒤로는 못 바꿉니다** — «열고 나서 고치기» 는 원리상 불가능하고 **«열 때 정하기»** 만 됩니다. ✅ 그래서 사무실 모드는 `getUserMedia` 를 **밖에서 감싸** 처음부터 AGC 를 끄고 엽니다(`hookGum`, `js/idx-vc-officemode.js`) — 그 덕에 마이크를 두 번 열지 않습니다(교사 입장 지연의 실제 원인이었습니다). ⛔ **«에러가 안 났다» 를 성공으로 읽지 마세요** — 반드시 `getSettings()` 로 **다시 확인**하고, 안 바뀌었으면 예전 경로(새로 열기)로 떨어지세요. 그 확인이 없으면 AGC 가 켜진 채로 «사무실 모드가 켜진 척» 하는데 **소리는 정상이라 아무도 못 알아챕니다.** 🔴 **그렇게 트랙을 «빌려 쓰게» 되면 `swapTrack` 류가 새 함정이 됩니다** — 되돌릴 때 `old` 와 `next` 가 **같은 트랙**이 되는데, 그 함수가 «옛 것을 stop» 하므로 **자기 자신을 죽이고 죽은 것을 다시 넣어 교사가 무음**이 됩니다(이 수리가 실제로 만든 결함이고 브라우저 검사 ⑧ 「실패해도 마이크가 살아 있다」가 잡았습니다). `if (old === next) return true;` 한 줄이 그 자리입니다. ⚠️ **브라우저 검사에서 «학생은 AGC 를 안 끈다» 를 재려면 페이지를 새로 여세요** — 크로미움은 **같은 장치를 겹쳐 열면 앞서 연 트랙의 오디오 설정을 그대로 줍니다**(장치를 다 놓고 800ms 기다려도 같았습니다). 그대로 재면 멀쩡한 코드가 FAIL 이라 다음 사람이 없는 버그를 쫓습니다. ⚠️ 실패 주입도 다시 보세요 — 「켜기가 실패하면 되돌아가는가」를 **`getUserMedia` 거부로만** 만들면, 빌려 쓰게 된 뒤로는 그 경로를 안 지나 켜기가 «성공» 해 버려 그 절이 통째로 거짓 FAIL 입니다(실측 3건). **경로와 무관한 자리**(AudioContext)를 함께 막으세요. 🔴 **덧붙임(같은 날 함정 대조) — «끄기» 쪽에 같은 실수를 그대로 했습니다.** 되돌릴 때도 `applyConstraints` 로 AGC 를 켜 준다고 «가정» 하고 `getSettings` 확인을 안 했습니다(켤 때는 했는데 — **같은 판정이 두 곳인데 한쪽만 고쳐진** 그 형태). 되돌리기 방향도 똑같이 무시되므로(실측: AGC 끈 트랙에 `{autoGainControl:true}` → 예외 없음 · 여전히 `false`) **껐는데도 그 세션 내내 AGC 가 꺼진 채**였고, 그건 이 파일이 「학생에게 안 거는 이유」로 적어 둔 바로 그 해악(조용히 말하면 소리가 작아짐)을 교사에게 일으킵니다. ✅ 되돌린 뒤에도 **`getSettings` 로 확인**하고 안 돌아왔으면 예전처럼 새로 여세요(끄기는 사람이 누르는 순간이라 속도가 안 걸립니다 — 고치려던 것은 «입장» 입니다). 🔴 **그때 새로 열기 «전» 에 빌린 트랙을 놓으세요** — 같은 장치가 아직 열려 있으면 크로미움이 **그 트랙의 설정을 새 트랙에도 그대로 줍니다**(실측: 놓기 전 `false` · 놓고 나면 `true`. ⚠️ 그래서 「새로 열면 되겠지」도 그냥은 안 됩니다). ⛔ 놓아도 무음이 되지 않습니다(아직 가공 트랙을 내보내는 중이고 바로 갈아끼웁니다). 🔴 **되돌리는 «동안» 은 그 훅이 손을 떼야 합니다**(`restoring`) — 켜기 실패 경로는 저장값이 아직 «켜짐» 이라, 되돌리려고 새로 여는 마이크의 AGC 를 훅이 **또 끕니다.** ⚠️ 그 가드는 **이 환경에서 «결과» 로 안 드러납니다**(지우면 훅이 실제로 덮는 것을 계측으로 확인했는데도 fake device 가 AGC 를 켜서 내줍니다) — 검사가 못 지키니 지우지 마세요. 🪤 곁가지로 **자동 적용 폴링이 `autoFailed` 를 «진입할 때만» 보고 있었습니다** — 사람이 눌러 실패한 직후(저장값은 `forget` 으로 «아직 안 정함» = 기본 켜짐) 이미 도는 폴링이 곧바로 다시 켜서, 「이 페이지에서는 자동으로 다시 시도하지 않는다」는 약속이 깨지고 되돌려 놓은 마이크가 다시 가공 트랙이 됐습니다. 감시는 `test-harness/manual/vc-office-mode-browser.mjs` ⑱절(마이크 열림 횟수를 **실제로 세고**, 「안 연다」 옆에 **「그런데도 켜졌다」를 짝으로** 둡니다 — 짝이 없으면 «아무것도 안 하기» 도 통과합니다. 그리고 **「끈 뒤 AGC 가 실제로 돌아왔는가」**·**「켜기가 «실패» 해도 돌아왔는가」**(가드가 일하는 유일한 경로)를 함께 봅니다. 변이 5종 실제 FAIL). ⚠️ 그 ⓙ 절을 만들 때 **재기 전에 기다리지 마세요** — 저장값이 «켜짐» 이라 자동 적용이 먼저 켜 버리면 `vcSetOfficeMode(true)` 가 「이미 켜짐」으로 true 를 돌려줘 그 절이 통째로 헛돕니다(실측). |
| 이미 있는 화면에 **다른 언어**를 붙일 때 (영어 화면에 중국어 등) | 🔴 **«영어만 걸러 내는» 게이트가 그 언어에는 «원리상 눈이 없습니다» — 에러도 안 나고 무엇이 와도 통과합니다.** 2026-09-13 웜업에 중국어를 붙이며 실측: 무너진 출력 검사(`src/reply-sanity.ts`)의 `words()` 가 `[a-z가-힣0-9' ]` **밖을 전부 지워서** 순수 중국어 문장은 `w` 가 «빈 배열» 이 되고, 길이 조건(12·25·40)에 한 번도 안 걸려 **한 글자를 수십 번 되풀이한 죽도 그대로 학생 화면에 나갑니다.** ✅ 그 언어의 «글자» 로 같은 질문을 한 번 더 하세요(⑤절). ⚠️ 임계값은 영어보다 **훨씬 느슨하게** — 중국어는 的·了 가 원래 자주 나오고, **멀쩡한 문장을 버리면 대화가 끊겨 깨진 문장 하나보다 나쁩니다**(이 파일이 그 파일 머리말에 못 박은 방향). ⛔ 「맞는 문장인가」(성조·병음·번체자)를 여기서 묻지 마세요 — 그건 «무너졌는가» 가 아니고 알 방법도 없습니다. 🪤 **같은 병이 «세는 것» 에도 있습니다** — 낱말 수 상한(`WARMUP_WORD_CAP`)은 띄어쓰기가 없는 언어에서 성립하지 않습니다. 한 덩어리가 «1낱말» 로 세어져 상한이 아무 일도 안 하거나, 반대로 통째로 버립니다 → 그 언어에서는 **꺼세요**(`sanityCap = 0`). 🔴 **덤 — «안전망·기본값» 이 먼저 샙니다.** 사람이 보는 본문은 언어를 타는데, ⓐ 모델이 실패했을 때만 오는 «준비된 질문»(`WARMUP_FALLBACK_QUESTIONS`) ⓑ 낮은 단계에 **조건 없이** 붙는 «막혔을 때» 보기(`WARMUP_STUCK_CHIPS` — 유도 결과가 0개여도 붙습니다) ⓒ 화면이 쓰는 고정 인사말 — 셋 다 **영어가 그대로 나갑니다.** 「본문만 번역하면 되겠지」로 끝내지 마세요. ⚠️ **캐시 키에 언어를 넣으세요** — 교재 소재 캐시(`sig`)에 빠져 있어 **같은 세션에서 영어 소재가 중국어 대화로 셀** 뻔했습니다(에러 없음). ⛔ **«보기 칩» 에 병음·한국어 뜻을 붙이지 마세요** — 누르면 **그 글자가 그대로 학생 발화로 전송**됩니다(`pickAnswer`). ℹ️ AI **말풍선** 안의 괄호는 괜찮습니다 — `_speechText()` 가 낭독에서 걸러 냅니다. ⛔ **답을 «유도» 하는 규칙을 새 언어로 지어내지 마세요**(「만들 수 없으면 안 만듭니다」) — 중국어는 탈출구 두 개만 줍니다. ⛔ **새 언어 재료를 공동 금지구역(`src/index.ts`)에 적지 마세요** — 새 파일(`src/warmup-zh.ts`)에 두고 거기서는 «부르기만» 합니다. ⚠️ 단 **교정처럼 «출력 계약(JSON 스키마)과 한 몸» 인 규칙은 원본 옆에** 두세요(`warmup-correction.ts`) — 스키마를 두 파일에 적으면 「파서·검증 한 벌이 두 화면을 받는다」는 전제가 깨집니다. ⚠️ **기본값은 «모르면 옛 언어»** 로 실패하게 하고, 서버 힌트로 기본을 바꿀 때는 **저장하지 마세요** — 저장하면 «학생이 고른 것» 이 되어 나중에 기본을 되돌릴 수 없습니다(이 표의 「기본값을 뒤집으면 실패 처리의 뜻까지 뒤집힌다」와 같은 자리). ⚠️ **CSS 주석에 「」 를 쓰지 마세요** — 한자 폰트 검사 두 개(`subtitle_meaning`·`admin_lazyload`)가 `<style>` 안 홑낫표를 «장식 문자» 로 셉니다(`« »` 로). ⚠️ **그 언어 글자를 화면에 넣으면 「그 화면은 한자가 없다」류 부정 검사의 전제가 바뀝니다** — 「그 낱말이 있는가」로 묻던 검사를 **「문자범위 정규식을 복제했는가」** 처럼 «뜻» 으로 옮겨 적고 왜 바꿨는지 날짜와 함께 남기세요(느슨하게 푸는 것과 다릅니다). ✅ 검사는 **「그 언어에서 막힌다」 옆에 「옛 언어는 그대로다」를 짝으로** 두세요 — 짝이 없으면 «전부 새 언어로 만들기» 도 통과합니다. 🔴 **그리고 «배선했다» 와 «닿는다» 는 다릅니다 — 이 작업에서 실제로 밟았습니다.** 새 언어용 규칙·축·배선을 다 만들어 놓고도 **검증기(`verifyWarmupFix`)가 `isEnglishText` 로 재서 중국어 교정이 한 건도 통과하지 못했습니다**(한자가 게이트에서 100% 탈락하고 그 아래 `normEn` 이 한 번 더 빈 문자열을 만들어 **두 겹**으로 막혔습니다). 그런데 규칙도 상수도 호출도 다 «있어서» 문자열 검사 네 건이 전부 초록이었고, **교정 카드가 «그냥 안 뜨는» 것으로만** 보였습니다. ⚠️ 게다가 그 규칙은 출력을 평문 → JSON 으로 **바꾸므로**, 버려질 결과를 위해 본문 토큰 예산을 계속 씁니다(이 표의 「JSON 으로 받게 바꿨더니」와 겹칩니다). ✅ **끝에서 끝까지 «실제로 돌려» 답을 보는 검사**를 한 절이라도 두세요 — 「그 함수를 부르는가」로는 원리상 못 봅니다. 🪤 **그리고 «뜻으로» 고친 검사는 되돌려 FAIL 이 나는지 반드시 확인하세요** — 「영어 정본이 있는가 + 중국어 정본이 있는가」 짝으로 바꿨더니 **영·중을 뒤바꾼 변이(`ctxLang === 'zh' ? 영어 : 중국어`)가 둘 다 통과**했습니다(옛 «식 모양» 검사는 잡던 것입니다). 삼항식을 **오려 내 평가해** «zh 일 때 무엇이 나오는가» 로 물으세요 — ⚠️ 그때 상수 이름을 그대로 평가하면 `… is not defined` 로 죽어 **조용히 catch 로 빠지므로**, 이름을 표식으로 바꿔 넣고 **「식을 찾아 평가했다」를 전제 검사로** 두세요. 🪤 **지연 ALTER 는 «실패하면 그 표 기록이 통째로 죽는지» 보세요** — `_schemaReady` 를 catch 뒤에도 세우면서 INSERT 가 새 칸을 요구하면 `no such column` 이 바깥 catch 에 삼켜져 **분모 전체가 사라집니다**. **칸이 없으면 그 칸 없이라도 적으세요.** ⚠️ 그 검사를 「그 플래그 이름이 있는가」로 쓰면 **`if (true)` 로 바꿔도 통과합니다**(실측) — «그 INSERT 를 감싸는 조건인가»로 물으세요. 감시는 `test-harness/warmup_zh_lang_harness.mjs`(53종 — 판정을 esbuild 로 번들해 **실제로 돌리고** 기대값은 전부 정본 소스에서 «읽어» 옵니다. 변이 13종 전부 실제 FAIL). 📄 `docs/작업기록/260913_웜업_중국어대화_언어축.md` 📌 **덧붙임(2026-09-14) — «AI 가 자기를 뭐라고 부르는가» 도 그 언어에 눈이 없습니다.** 새 언어 친구를 화면에만 늘리면 **정본 표(`src/ai-friends.ts` 의 `AI_FRIEND_NAMES`)에 그 칸이 없어** `resolveFriendName` 이 «모르는 값 → 기본값(Mango)» 으로 떨어뜨립니다 — **에러가 안 나고** AI 가 중국어 수업에서 「我是Mango」 라고 말할 뿐입니다(2026-09-14 사장님 제보 「你好，我是Emma！」는 그 «화면이 아직 영어 친구를 보내던» 판입니다). ⚠️ 그 표의 값은 «그 언어의 글자» 여야 합니다 — `'Mei'` 라고 적으면 중국어 문장 한가운데에 로마자가 섞여 읽힙니다. ✅ **화면 첫 인사와 짝으로** 검사하세요(화면은 「我是美美老师」 라고 인사해 놓고 AI 는 다른 이름을 대는 2026-08-31 Lily 건과 같은 모양). 🔴 **그리고 «다른 이름으로 소개했나» 가드(`wrongSelfName`)가 그 언어에서는 원리상 한 번도 안 걸립니다** — 패턴 셋이 전부 영어 문형(`I'm X`·`my name is X`)이고, 문장을 가르는 `[.!?]` 도 **중국어 종결부호(。！？)를 모릅니다.** ⛔ 그렇다고 「我是」 뒤의 글자를 «이름» 으로 뽑지 마세요 — 「我是老师」(나는 선생이야)·「我是韩国人」 이 「我是Emma」 와 **문장 구조가 완전히 같습니다**(그 파일이 영어에서 이미 두 번 밟은 함정). ✅ **«우리가 아는 이름» 일 때만** 잡으세요. ⚠️ 그때 **긴 이름을 먼저** 견주세요(짧은 이름이 긴 이름의 앞가리면 «기대한 이름» 을 «남» 으로 읽습니다). ✅ 검사는 「잡는다」 옆에 **「평범한 그 언어 문장은 안 잡는다」와 「영어 판정은 그대로다」를 짝으로** 두세요 — 앞만 두면 «전부 잡기» 도 통과합니다. 🪤 **그런데 그 짝만으로는 «이름을 견주는 방법» 이 통째로 안 검사됩니다 — 2026-09-14 함정 대조 실측.** 잡는 반례를 «전부 정확한 대소문자» 로만 적고, 지금 표에 **서로 앞가리인 이름 쌍이 하나도 없으면**(`Mango`·`Emma`·`Jake`·`Lily`·`Noah`·`美美老师`) ⓐ 대소문자 무시를 지워도 ⓑ «긴 이름 먼저» 정렬을 지워도 ⓒ 「기대한 이름이면 `break`」를 `continue` 로 바꿔도 **세 변이가 전부 통과합니다** — 셋 다 «그래야 한다» 고 주석에 적혀 있는데 그것을 재는 검사가 한 줄도 없던 것입니다. ✅ **반례를 표에서 «자동 생성»** 하세요(⛔ 하니스에 이름을 손으로 적지 마세요 — 친구가 늘면 그대로 어긋납니다): 대소문자는 표의 ASCII 이름마다 `我是<소문자>`·`我是<대문자>`, 앞가리는 **«앞으로 생길» 이름 둘**(기대 이름의 앞토막과 기대 이름을 품는 더 긴 이름)을 표에 **잠깐 넣었다 `finally` 로 되돌려** 봅니다(정본이 표를 «부를 때마다» 읽으므로 그대로 걸립니다). ⚠️ **되돌렸는지도 검사 한 줄로** 두세요 — 안 되돌리면 그 뒤 검사가 조용히 오염됩니다. 감시는 `ai_friend_name_harness` ⑥ · `friend_name_guard_harness` ⑧·⑧-2(변이 **9종** 전부 실제 FAIL). 🟡 **아직 안 한 것**: 첫 턴에 넣는 합성 인사(`src/index.ts` 의 `` `Hi! I'm ${ctxFriend}.` ``)는 **언어를 안 가려** 중국어 세션에도 영어 문장이 들어갑니다 — 공동 금지구역이라 사람이 정할 일입니다. |
| **고르는 칸**(친구·목소리·모드)을 어떤 조건에서 «없앨» 때 | 🔴 **버튼을 «하나만 남겨» 보여 주면 사람에게는 여전히 «고를 수 있는 것» 으로 보입니다.** 2026-09-14 실사고: 중국어 웜업에서 친구 넷을 감추고 «메이» 버튼 하나만 남겼더니 사장님이 화면을 보시고 「중국어는 목소리와 얼굴 선택없이 무조건 메이 한 교사만」으로 다시 지시하셨습니다. ✅ **칸을 통째로 감추고** «왜 못 고르는지» 한 줄을 그 자리에 두세요(감추기만 하면 「왜 없지?」를 혼자 추측합니다). ⛔ **`[hidden]` 만 믿지 마세요** — 그 상자에 `display:grid`·`flex` 를 정해 뒀으면 작성자 CSS 가 UA 기본 `[hidden]{display:none}` 을 이깁니다. `[hidden]{display:none!important}` 를 함께 박으세요(이 표의 「`el.hidden = true` 인데 그대로 보임」). 🔴 **판정을 «좁은 가드» 로 두지 마세요** — 그 화면은 `if(_voiceMode==='mix') return isZh() ? …` 로 «번갈아일 때만» 막고 있어서, 옛 저장값이 `lily` 면 **중국어인데 Lily 얼굴**이 나왔습니다(에러 없음). **「그 조건이면 무조건 X」 한 줄을 함수 첫머리에** 두고 그것을 정본으로 삼으세요 — 그러면 어느 경로로 들어와도 한 번에 막힙니다. 🔴 **저장 칸을 함께 없애세요** — 고를 수 없는데 저장하면 그 값이 «학생이 고른 것» 으로 굳어, 나중에 선택지를 늘릴 때 기본값을 되돌릴 수 없습니다(이 표의 「기본 켜짐을 저장값으로 구현하지 말 것」). 🔴 **그 조건이 «버튼 말고 다른 경로로도» 켜지는지 보세요** — 서버 힌트로 켜지는 주 경로(`probeSuggestLang`)가 상태만 바꾸고 `renderSetup()` 을 불렀는데, 그 칸은 **정적 HTML** 이라 다시 안 그려져 **영어 친구가 그대로 남았습니다**(함정 대조가 잡음). ⚠️ 그 자리에서 «고르기 함수»(`setVoiceMode`)를 부르면 **저장이 일어나** 위 규칙이 깨집니다 — **«화면·얼굴만 맞추는» 함수를 따로 떼세요**(`applyVoiceFace`). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무엇이 보이는가·무슨 답이 나오는가» 뿐입니다. ✅ 검사는 판정 함수를 **오려 내 실제로 돌리고**, 「그 조건에서 막힌다」 옆에 **「원래 조건은 그대로다」를 짝으로** 두세요(짝이 없으면 «전부 감추기» 도 통과). 🪤 배선은 **「파일 어딘가에 그 글자가 있는가」로 묻지 마세요** — `if(false){ sync(); }` 로 감싸도 통과합니다(실측 101/0). **중괄호 짝으로 그 함수 몸통을 잘라** 그 안에서 물으세요. 🪤 가짜 DOM 으로 돌리는 검사는 **`try/catch` 로 감싸세요** — 없는 메서드를 부르는 변이가 «깔끔한 FAIL» 이 아니라 **하니스 크래시**가 되어 결과줄조차 안 나옵니다. 감시는 `test-harness/warmup_zh_lang_harness.mjs` ⑯(변이 12종 전부 실제 FAIL)과 `manual/warmup-zh-friend-browser.mjs`. 📌 **덧붙임(같은 날 오후) — 같은 자리에서 지시가 또 바뀌어 그 칸을 «되살렸습니다».** 「남자 교사도 한명더 추가해줘」로 중국어 선생님이 둘이 되었기 때문입니다(하루에 세 번: 버튼 하나만 → 칸 삭제 → 칸 복원). 🔴 되살리는 순간 **옛 경계를 못 박은 검사 셋이 한꺼번에 빨간불**이 됩니다(「중국어면 언제나 메이」·「중국어 버튼이 없다」·「중국어 저장 칸을 두지 않는다」). ⛔ 그것을 «검사가 헛돈다» 로 읽고 지우지 마세요 — **그 검사가 지키던 전제를 사람이 바꾼 것**이라, **새 경계로 옮겨 적고** 왜 옛 단정을 버렸는지 날짜와 함께 남기세요(여기서는 「그 언어의 선생님만 말한다」·「두 상자가 서로 섞이지 않는다」·「두 저장 칸이 서로를 안 지운다」). ✅ 상자는 **둘로 나누세요**(`#voiceBtns` 영어 · `#voiceBtnsZh` 중국어) — 한 상자에 섞으면 중국어에서 Emma 를 고를 수 있게 되고 서버가 영어 프롬프트를 돌립니다. ✅ 저장 칸도 **언어별로** — 한 칸이면 중국어에서 고를 때 영어 선택이 지워집니다. ⚠️ 버튼 on 표시는 **두 묶음 모두** 훑으세요(한쪽만 보면 언어를 바꾼 뒤 옛 묶음에 on 이 남습니다). ⚠️ 그리고 «그 언어의 사람만» 을 판정할 때 **표의 표시(`zh:true`)로** 물으세요 — 이름을 손으로 적으면 선생님이 늘 때 그 사람만 조용히 빠집니다. 📄 `docs/작업기록/260914_중국어교사_아바타_메이.md` §9·§11 |
| 첫 화면이 무거워 **blocking 을 defer 로 옮겨** 빠르게 하려 할 때 | 🔴 **«조작 가능» 시간은 거의 안 줄어듭니다 — defer 도 `DOMContentLoaded` 전에 실행되기 때문입니다.** 옮기면 HTML 파싱은 안 막지만 **그 코드를 파싱·실행하는 일은 그대로** 일어납니다. [잰 것 — 2026-09-11, 진짜 Chromium · RTT 220ms · 5Mbps · CPU 1/4 · 2회 평균] 안전한 8개(126KB)를 defer 로 옮겨도 **조작 가능 8,625ms → 8,622ms(3ms)** 로 회차 편차(±120ms) 안이었습니다. 첫 글자(FCP)만 1,352 → 1,260ms. ⟹ **되돌렸습니다** — 공동 금지구역 `index.html` 수정에 라이브 장애 전력까지 있는데 이득이 측정되지 않았습니다. ⚠️ 병목은 «바이트» 가 아니라 **«실행»** 입니다(brotli 529KB 는 5Mbps 에서 0.85초인데 전체는 8.6초). 그래서 진짜로 줄이려면 **첫 화면에서 아예 실행하지 않는 것**(수업 전용 코드를 입장할 때 동적 로드)이어야 하고, 그건 반경이 훨씬 큰 별건입니다. 🪤 **A/B 를 잴 때 한쪽만 응답을 가로채면 «없는 개선» 이 나옵니다** — 처음에 defer 판만 `route.fulfill` 로 HTML 을 바꿔 주고 기준선은 그냥 서빙했더니 **1.3초 개선**으로 나왔습니다(압축·캐시 동작이 달라진 것). 두 쪽 다 **실제 파일로 서빙**해서 재니 3ms 였습니다. ✅ 옮길 후보를 고를 때는 **«남는 blocking 이 그 전역을 로드 시점에 부르는가»** 를 기계로 확인하세요 — `idx-main.js` 가 `vcSwitchTab`·`getLang`·`vcSendChat` 등을 그렇게 부르고 있어 `idx-x3`·`idx-vc-textbook`·`idx-vc-chat`·`idx-vc-seg-lessonchange` 는 **옮기면 안 됩니다**. ℹ️ `onclick=` 속성이 부르는 것은 defer 여도 안전합니다(클릭 시점엔 이미 정의돼 있습니다). ⚠️ 그리고 **`vcScreenSet` 은 기준선에서도 `undefined`** 입니다 — 옮긴 뒤 그것을 보고 «내가 죽였다» 고 오해하지 마세요(조건부로 정의됩니다) |
| 홈에서는 되는데 **다른 화면 사이드바에는 그 메뉴가 아예 없음** | 🔴 **왼쪽 메뉴 정본이 «두 벌» 입니다** — 홈은 `index.html` 안에 박힌 `#mg-drawer`(접이식 `<details>` 6개), 그 밖 **25개 화면**은 `js/mg-sidebar.js`(평면 목록). **홈은 공용 파일을 애초에 싣지 않습니다**(2026-09-13 실측: `<script src>` 25곳, `index.html` 0건) — 그래서 둘이 겹치지 않고 **한쪽만 고쳐도 에러가 안 납니다.** ⚠️ 공용 쪽 첫머리의 `if (document.getElementById('mg-drawer')) return;` 은 «혹시 실수로 실어도» 를 막는 **안전망이지 지금 도는 경로가 아닙니다** — 그것을 믿고 홈에 이 파일을 싣지 마세요(모양이 다른 메뉴가 겹칩니다). 📜 2026-09-12 실측: 그렇게 **11칸**이 벌어져 있었습니다(홈 28 · 공용 18 · 겹침 17). AI 학습 도구가 통째로 빠져 있어 **`ai-write.html` 에서 사이드바를 열어도 「AI 글쓰기」가 없었고**, 새 도구 11개를 쓰려면 학생이 홈으로 나갔다 다시 들어와야 했습니다. ⚠️ **에러도 경고도 안 납니다** — 두 메뉴가 각자 자기 목록을 정직하게 그릴 뿐이라 어느 쪽도 «고장» 으로 안 보입니다. 🔴 **`mgGo(코드)` 가 두 곳에서 «다른 일» 을 합니다** — 공용은 `location.href = URLS[코드]`(주소 이동)인데 **홈은 `document.querySelector('.gm-card[data-go=…]').click()`(홈 안의 카드를 대신 클릭)** 입니다(`index.html:4835`). 그래서 «주소가 같은가» 로 대조할 수 있는 것은 홈이 `location.href` 를 **직접 적은** 항목뿐이고, 나머지는 «라벨이 짝이 맞는가» 로 물어야 합니다. ✅ 새 화면·새 메뉴를 만들면 **두 곳 다** 등록하세요 — 공용은 `URLS`(주소) · `ITEMS`(라벨·별점) · `GROUPS`(어느 묶음) **세 군데**이고, `GROUPS` 등록을 잊으면 맨 아래 「기타」로 흘러갑니다(`buildNav` 의 `rest` 폴백 — 조용히 사라지지는 않습니다). ⛔ **홈에서만 도는 함수를 부르는 항목은 옮길 수 없습니다** — 「교사 소개」(`gridActions.teachers`) · 「녹화 보기」(`MangoFlow.playRecording`) · 「체험수업」(`openTrialSignup`)이 그렇고, 그 함수를 담은 `js/idx-grid-menu.js`·`js/idx-payment-modal.js` 는 홈 밖 25개 화면에 **실리지 않습니다**. 길을 내려면 홈에 `?menu=` 파라미터를 새로 만들어야 하는데 그건 `index.html`(공동 금지구역 · 첫 화면 예산)입니다 — **사람이 정할 일**. ⛔ **그렇다고 주소 없이 목록에 넣지 마세요** — `mgGo` 가 `URLS[go] || '/'` 라 «보이는데 눌러도 홈으로 가는» 버튼이 됩니다. ✅ **`?v=` 는 안 올려도 됩니다** — 25개 화면이 이 파일을 **버전 없이** 부르고, `src/index.ts` 의 `_versioned` 판정이 거짓이라 `no-cache`(매번 재검증)로 나갑니다. 그래서 `asset_version_harness` 의 원장에도 없습니다. ⚠️ 대신 **`?v=` 를 새로 붙이지 마세요** — 붙이는 순간 1년 immutable 이 되어 25개 HTML 을 매번 함께 올려야 합니다. ⚠️ **모양(펼침/접힘)은 일부러 다릅니다** — `mg-sidebar.js` 42행이 2026-07-27 직원 피드백 #5 로 「접기(accordion)로 만들지 않았다 — 접으면 모든 항목이 클릭 1회씩 더 든다」고 못 박아 두었습니다. ⛔ «통일» 을 이유로 그 결정을 지우지 마세요(사장님 확인이 필요한 별건). ⚠️ 항목이 늘어도 **스크롤은 이미 됩니다**(`.mg-drawer-nav{overflow-y:auto}`) — 2026-09-12 실측 25개·내용 1495px·칸 813px, 전부 한 줄이고 스크롤하면 25개 모두 손이 닿습니다. ⚠️ **문자열 하니스로는 못 잡습니다** — 표도 함수도 다 «있고» 틀린 것은 «무엇이 그려지는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 감시는 `test-harness/sidebar_menu_parity_harness.mjs`(45종 — 표와 `buildNav()` 를 **오려 내 실제로 돌리고** 기대값은 홈 드로어에서 **읽어** 옵니다. ⛔ 주소·라벨을 하니스에 손으로 적지 마세요. ④절은 «홈에서만 도는 함수를 부르는 것» 을 FAIL 대신 **이름을 찍어** 출력합니다 — 선례 `popup_open_return_harness`. 변이 6종 전부 실제 FAIL). 🪤 **그 하니스는 처음에 구멍이 셋이었고 «변이 4종 전부 FAIL» 이라고 적은 제 문장이 거짓이었습니다**(2026-09-13 함정 대조 → 실측 재현). ㉠ ②절이 「**GROUPS 등록 누락**」이라는 «이름» 을 달고 실제로는 「ITEMS 에 있는가」만 쟀습니다 — `buildNav` 의 **«기타» 안전망**(그룹에도 PINNED 에도 없는 항목을 맨 아래에 다시 그려 줍니다)이 있어서 **GROUPS 에서 빼도 `nav` 에는 남습니다**(실측 PASS 22 · exit 0). ✅ «선언됐는가»(`GROUPS` 값을 봅니다)와 ««기타» 머리글보다 위에 그려졌는가» 를 **짝으로** 물으세요. ㉡ ①절이 «ITEMS 를 돌며 홈에서 짝을 찾다 못 찾으면 `continue`» 라, **한국어 라벨이 어긋난 항목이 FAIL 이 아니라 검사에서 조용히 사라졌습니다**(PASS 22→**21** — 합계만 줄어듭니다). ㉢ 그래서 **ITEMS 에서 항목을 통째로 지워도 초록불**이었습니다(`refund` 실측). ✅ ㉡㉢의 뿌리는 **묻는 방향**입니다 — «공용 → 홈» 으로 물으면 «공용에서 빠진 것» 을 원리상 못 봅니다. **«홈에 갈 곳이 있는 메뉴» 쪽에서 «공용에도 같은 이름으로 있는가»** 로 물으면 라벨 어긋남과 누락이 한 번에 드러납니다(대조 항목 11 → 22개). ⛔ 그 방향을 되돌리지 마세요. ⚠️ 그리고 «몇 개 대조했는가» 의 바닥(`checked >= 7`)을 **상수로** 두면 지금처럼 대조 대상이 11개일 때 **4개까지 어긋나도 통과**합니다 — «홈에서 읽은 수와 같은가» 로 물으세요. ⚠️ 그 하니스를 만들 때 ④를 «홈에서 주소를 못 읽었다» 로 판정했더니 공용에 **이미 있는** 항목까지 15개나 「못 넣음」으로 찍혔습니다(`mgGo` 가 위처럼 다른 일을 해서) — **틀린 진단을 출력하는 검사**였고, 「공용 목록에 그 라벨이 있는가」로 고쳐 3개만 남았습니다. 📄 `docs/작업기록/260912_사이드바_두벌_빠진메뉴_채우기.md` |
| 화면 한 조각을 다시 그렸더니 **엉뚱한 자리(다른 버튼)의 글자가 바뀜** | 🔴 **다른 파일이 그 조각의 «클래스 이름» 을 읽어 자기 글자로 쓰고 있을 수 있습니다 — 그 계약은 코드 어디에도 «선언» 돼 있지 않습니다.** 2026-09-14 실측: 홈 «내 수업» 카드(`js/idx-my-schedule.js`)를 세로 목록으로 다시 그리며 머리글 「강사 2명」에 `.ncc-sub` 를 달았더니, **「수업 입장」 버튼 아래 줄이 「강사 2명」** 이 됐습니다 — `js/idx-cta-status.js` 의 `classSub()` 가 「다음 수업을 다시 조회하지 않고 카드가 **그려 놓은 `.ncc-sub` 첫 요소의 글자**를 읽는다」는 설계라서입니다(그 파일 머리말에 적혀 있지만 카드 쪽에는 한 줄도 없었습니다). ⚠️ **에러가 안 납니다** — 두 파일 다 «정상 동작» 이고 틀린 것은 «어느 글자가 어디로 가는가» 뿐이라 타입체크·하니스 전부 초록이었고, 배포 뒤 사장님 캡처로 알았습니다. ✅ 화면 조각을 갈아 끼우기 전에 **그 조각의 id·클래스를 «읽는» 파일을 `grep` 으로 세세요**(`querySelector('.ncc-sub')` 같은 줄 — 30초). ✅ 계약은 **양쪽에** 적으세요 — 읽는 쪽 머리말만으로는 그리는 쪽을 고치는 사람이 못 봅니다. ✅ 검사는 읽는 쪽 함수를 **오려 내 새 카드 위에서 실제로 돌려** «무슨 글자가 나오는가» 로 물으세요(`my_schedule_rows_harness` ⑦절 — 되돌리면 3건 FAIL). ⛔ 읽는 쪽을 «더 똑똑하게» 고쳐서 풀지 마세요 — 그 파일의 `?v=` 까지 올려야 하고, 계약을 두 곳에서 각자 해석하게 됩니다 |
| 사이드바 메뉴를 눌렀는데 **홈으로만 가고 아무것도 안 열림** (「보이는데 안 되는」 버튼) | 🔴 **바로 윗줄의 뒷면입니다 — «주소가 없는» 것이 아니라 «주소가 홈» 인 경우입니다.** 공용 사이드바는 **주소로만** 옮겨 가는데(`mgGo` → `location.href = URLS[go] || '/'`), 홈에서만 도는 **함수**를 부르는 메뉴는 줄 주소가 없어 주소표에 `'/'` 라고 적히기 쉽습니다. 그러면 홈 밖 25개 화면에서 눌러도 홈으로 **«가기만»** 하고 그 기능은 아무도 열어 주지 않습니다. ⚠️ **에러도 경고도 안 납니다** — 이동은 «성공» 이고 틀린 것은 «도착해서 아무 일도 안 일어난다» 뿐입니다. 📜 2026-09-14 실사고: 「🏠 전체메뉴」(`'all-menu':'/'`)가 그 상태로, 사장님 **「이거 왜 전체 메뉴 누르면 홈화면으로만 가? 전체메뉴는 안보여」**. 🪤 **검사 셋이 나란히 초록불이었습니다** — ①「홈과 공용이 같은 주소인가」는 **양쪽 다 `/`** 라 «일치», ③「갈 곳 없는 버튼이 없는가」는 `URLS[go]` 가 **truthy**, ④「옮길 주소가 없는 메뉴」는 «갈 곳이 있다»며 **건너뜀**. ⟹ «주소가 없다»·«이름이 어긋났다» 에 이은 **세 번째 결함 종류**라 셋 중 어느 쪽도 원리상 못 봅니다. ✅ 고침은 **주소를 하나 내 주는 것** — 홈이 이미 쓰는 `?menu=` 방식으로 defer 파일에 «받는 절» 을 두고(`?menu=all-menu` → `js/idx-allmenu.js`. 선례: `?menu=leveltest`·`?menu=remote`·`?menu=aitools`) 주소표를 `'/?menu=all-menu'` 로 바꿉니다. ⛔ **`index.html` 의 `?menu=` 핸들러에 갈래를 더하지 마세요** — 그 스크립트는 **blocking** 이라 첫 화면 예산을 씁니다(여유는 「첫 화면 예산이 FAIL」 줄의 **정본 숫자** — ⛔ 여기에 바이트를 **베껴 적지 마세요**. 그 복제가 곧 낡습니다: 2026-09-20 에 이 자리가 「여유 16바이트」인 채로 발견됐는데 실제로는 10,970B 였습니다). ⚠️ 여유가 얼마든 `index.html` 은 **공동 금지구역**이라 그 한 글자도 사람 승인입니다. 실제 코드는 **defer 파일**에 둡니다. ⚠️ 그 defer 파일을 고치면 **그것을 `?v=` 로 부르는 HTML 의 번호를 반드시 함께** 올리세요(안 올리면 immutable 캐시에 옛 파일이 최대 1년 남습니다). `13`→`14` 처럼 **글자 수가 같으면 첫 화면 예산은 0바이트**입니다(2026-09-14 실측 1,060,176 → 1,060,176). `index.html` 은 공동 금지구역이라 **그 한 글자도 사람 승인**입니다. ⛔ **`menu` 만** 지우세요 — `pathname` 으로 통째로 갈아치우면 `index.html` 이 수업으로 되돌아올 때 쓰는 `?room=` 과 다른 쿼리·해시까지 조용히 잃습니다. ⛔ **상주 `setInterval`·body class `MutationObserver` 를 두지 마세요**(홈을 통째로 멎게 한 전력) — defer 는 DOM 이 이미 준비돼 있어 기다릴 필요가 없습니다. ⛔ **줄을 빼서 «정리» 하지 마세요** — 기능을 없애는 쪽입니다. ⛔ **25개 화면에 홈 전용 파일을 새로 싣는 것**도 답이 아닙니다(화면마다 40KB 배경·별 필드가 따라붙습니다). 🟡 **같은 모양이 하나 더 있습니다 — 「망고아이란?」(`'about':'/'`)**. 홈에서는 `openAboutMangoi()` 가 열리는데 공용에서는 홈으로 가기만 합니다. 값은 같고 역시 0바이트지만 **사람이 정할 일**로 남겼습니다(하니스가 이름을 찍어 줍니다). ⚠️ **문자열 하니스로는 못 잡습니다** — 표도 주소도 다 «있고» 틀린 것은 «도착해서 무엇이 열리는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 감시는 `sidebar_menu_parity_harness` **⑤절**(«주소가 `/` 뿐인 항목» → FAIL. `HOME_ONLY_ALLOW` 에 **이름과 «왜 아직 그대로인가»** 를 적은 것만 넘어갑니다 — ⛔ 그 목록을 «조용히 넘기는 자리» 로 쓰지 마세요)와 **⑥절**(그 뒷면 — «`?menu=X` 로 **보내 놓고 받는 절이 없는가**». 받는 쪽만 지우면 ③·⑤는 「갈 곳이 있다」며 그대로 통과하므로 **짝**으로 둡니다)이고, «그 주소로 들어가면 실제로 무엇이 그려지는가» 는 `test-harness/manual/allmenu-url-browser.mjs`(브라우저 11종 — **자동으로 안 돕니다, 사람이 부릅니다**)가 잽니다. ⚠️ 그 브라우저 검사는 「열렸다」·「보인다」·「눌린다」를 **따로** 재고, ④절이 **제보의 그 동작 그대로**(`ai-write.html` 에서 눌러 홈으로 와서 오버레이가 열리는지) 합니다. ⚠️ **「파라미터가 없으면 안 열린다」를 짝으로** 두세요 — 없으면 «언제나 열기» 도 통과합니다. 🪤 **그 ⑥절만으로는 «조건 뒤집기» 를 못 잡습니다 — 제가 그 자리에서 그 함정을 밟았습니다.** 받는 절을 **통째로 지우면** ⑥이 잡지만, `if (false && …)` 로 막거나 `===` 를 **`!==` 로 뒤집으면** `'all-menu'` 라는 글자가 그 줄에 그대로 남아 **③·⑤·⑥이 전부 통과합니다**(2026-09-14 함정 대조 → 실측 재현: 두 변이 다 **PASS 38 / exit 0**). 🔴 하필 `!==` 뒤집기는 **«파라미터가 없는 평범한 홈 방문마다 오버레이가 저절로 뜨는»** 회귀라 학생 29,000명 전원의 첫 화면이 걸립니다. ✅ 그래서 **⑦절**이 그 절을 **오려 내 가짜 `location`·`history` 로 실제로 돌립니다** — «열린다» 옆에 **«파라미터가 없으면 «안» 연다» 를 짝으로**(없으면 «언제나 열기» 도 통과) · «오려 냈다» 를 **전제 검사 한 줄**로(앵커가 어긋나면 빈 문자열을 보고 조용히 통과) · 부르는 자리까지 `try` 로(변이가 문법을 깨면 크래시가 아니라 **깔끔한 FAIL**). ⚠️ **「변이 N종 전부 FAIL」이라고 적기 전에 그 N 에 «조건 뒤집기» 가 들어 있는지 세어 보세요** — 저는 «삭제» 만 넣고 그렇게 적었고, 그 문장이 세 곳에 박혀 있었습니다. 변이 **6종** 전부 실제 FAIL(조건 항상거짓 2 · **조건 뒤집기 5** · 받는 절 삭제 2 · 주소를 `pathname` 으로 갈아치우기 1 · 공용 주소 되돌리기 1 · ALLOW 죽은 항목 2. 브라우저는 받는 절 끄기 5 · 공용 주소 되돌리기 1 «제보 그대로 재현»). 📄 `docs/작업기록/260914_전체메뉴_홈으로만_가던_죽은버튼.md` |
| 관리자 「학생 등록」으로 만든 학생만 **상세 카드가 전부 «—»** · 수정·수강 연장이 «조용히» 안 먹음 | 🔴 **`students_erp` 를 찾는 조건이 `user_id`(PRIMARY KEY) 를 «안 보고» `student_id OR login_id OR username` 만 봤습니다** — `api-mango.ts` 의 상세 조회(`/full`)·수정 preRow·수정 UPDATE·수강 연장 SELECT·UPDATE **다섯 곳**이 같은 조건이었습니다. ⚠️ **왜 오래 몰랐나** — 카페24 동기화 행 29,494건은 `student_id = login_id = user_id` 라 우연히 걸립니다. 관리자 「학생 등록」(`students/create`)이 만든 행만 그 두 칸이 NULL 이고 `username` 은 한글 이름이라 **어느 조건에도 안 걸립니다**(2026-09-14 `yahee` 실측 — admin_manual 3행 전부). ⚠️ **에러가 안 납니다** — 조회는 «없는 학생», UPDATE 는 «0행 갱신» 으로 정상 종료라, 화면에는 「수업을 넣었는데 카드가 비었다」로만 보입니다. ✅ 정본은 **`ERP_BY_UID`**(`api-mango.ts` 머리) 한 조각이고 바인드도 `erpUidBinds(uid)` 로 넘깁니다 — ⛔ 새 자리에 조건을 다시 적지 마세요(한 곳만 고쳐지는 사고). ✅ 등록 INSERT 도 `student_id`·`login_id` 를 `user_id` 로 함께 채웁니다(카페24 행과 같은 모양 — 두 겹). ⛔ 이미 있는 3행을 D1 UPDATE 로 «맞추지» 마세요 — 이 다섯 곳은 읽는 쪽이 `user_id` 를 보므로 필요 없고, 1-1 입니다(⚠️ `ai-command.ts` 의 AI 운영비서는 `login_id` 만 봐서 그 3행을 여전히 못 찾습니다 — 별건). ✅ 지연 ALTER 목록에도 그 두 칸을 넣었습니다 — 새 INSERT 가 쓰는 칸이 없는 DB 에서는 등록이 `no such column` 으로 죽습니다(「보존 조건이 읽는 칸은 지연 ALTER 로 생깁니다」). ⚠️ **문자열 하니스로는 못 잡습니다** — SQL 도 호출도 다 «있고» 틀린 것은 «이 모양의 행을 찾는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 감시는 `test-harness/student_erp_lookup_harness.mjs`(26종 — 조각·INSERT 를 소스에서 오려 내 **진짜 SQLite** 에 돌리고, INSERT 의 `.bind(...)` 인자 목록도 **소스에서 평가해** 넣습니다. 「찾는다」 옆에 「없는 아이디는 안 찾는다」·「**옛 조건은 실제로 못 찾는다**(대조)」를 짝으로. 변이 6종 전부 실제 FAIL, 무해한 리팩터 2종은 PASS). 🪤 그 하니스가 첫 판엔 셋이 헛돌았습니다 — 변이에서 **크래시**(`find(...).end_date`) · 연장 UPDATE 의 «줄바꿈 자리» 와 INSERT 의 «컬럼 순서» 를 글자 그대로 못 박아 무해한 리팩터에 거짓 FAIL · INSERT 바인드를 «내가 적은 값» 으로 돌려 **순서가 뒤바뀐 진짜 버그(student_id 에 이름)를 못 잡음**(함정 대조가 셋 다 잡았습니다). 되돌려 보면 «깔끔한 FAIL» 이 나는지, 무해한 변형은 통과하는지까지 확인하세요. ℹ️ 그 카드의 `수강 종료·주당 수업·결제 유형` 은 조회를 고쳐도 «—» 입니다 — 값 자체가 NULL 이고 이 화면의 수업 「등록」은 `class_schedules` 에만 씁니다(예약표에서 계산해 그릴지는 사람이 정할 일). 📌 **2026-09-15 에 정해졌습니다** — 그 칸들은 **그대로 두고** 「예약」 칸을 새로 달았습니다(아래 줄). 📄 `docs/작업기록/260914_학생상세_카드_전부빈칸_user_id누락.md` |
| 수업을 넣었는데 **학생 목록·상세 카드가 «—»** (수강 시작일·종료일·수업회수·결제타입) | 🔴 **고장이 아니라 두 표 사이에 배선이 «아예 없는» 것입니다.** 관리자 「수업 예약 등록」은 `class_schedules` 에만 쓰는데, 그 네 칸은 **`students_erp` 의 칸이고 카페24가 정본**입니다(`created_at = 1751500000000` 센티넬) — D1 에 채워도 **매일 밤 03:00 동기화가 덮습니다.** 📊 [잰 것 — 2026-09-15] `lee` 활성 2건 · `jeong` 활성 5건인데 두 화면 다 «—» 였고, 같은 이름 4계정 중 `unistudy` 만 날짜가 보이는 것도 «카페24에 그 값이 있어서» 였습니다. ⚠️ **에러가 안 납니다** — 두 화면이 각자 «자기 표» 를 정직하게 그릴 뿐입니다. ✅ 그래서 그 칸들을 **채우지 않고** 「예약」 칸을 새로 달았습니다(정본 `src/student-schedule-summary.ts` — 반복은 «주 N회», 날짜 지정은 «단건 M회». 목록 `erp-list` 와 상세 `student/:uid/full` 이 **같은 정본**). ⛔ **「수강 종료일」에 «마지막 예약일» 을 넣지 마세요** — 뜻이 다릅니다(원래는 «결제한 수강권이 끝나는 날», 예약 마지막 날은 수업을 더 잡을 때마다 **저절로 늘어납니다** = 거짓). ⛔ 「수업회수(주)」도 마찬가지 — 일회성(`one_off`)은 «주당 회수» 가 아닙니다(실측 `lee` 의 2건이 그렇습니다 — ⚠️ `jeong` 은 반복 4 + 일회성 1이라 «전부 one_off» 가 아닙니다). ⛔ **라벨을 화면에서 조립하지 마세요** — 그리는 화면이 둘이라 한쪽만 고쳐집니다. 서버가 `label_ko`·`label_en` 을 만들어 보내고 화면은 «고르기만» 합니다. ⛔ **학생마다 조회하지 마세요**(명부 최대 2,000명 · 바인드 100개 한도) — 활성 예약 전체를 한 번에 읽어 메모리에서 묶습니다(실측 1,518행/492명). ⚠️ **`schedule_kind` 가 비면 «반복» 으로 보세요** — 표 DEFAULT 가 `'recurring'` 이고 읽는 쪽도 `schedule_kind || 'recurring'`(`api-mango.ts` 두 곳)이라 저장소 관례가 그쪽입니다(반대로 두면 **반복 수업이 「단건」으로** 찍힙니다. 지금 INSERT 8곳이 전부 값을 명시해 실피해는 0이지만 방향은 맞춰 두세요). ⚠️ **「📅 스케줄」 탭과는 축이 셋 다릅니다**(상태 · 자리표시 제외 · 동명 계정 통합) — 두 숫자를 «같아야 하는 것» 으로 읽지 마세요(정본 머리말에 적어 뒀습니다). ⚠️ 실패하면 **그 칸만 «—»** 이고 명부는 그대로 떠야 합니다(`erp-list` 는 어떤 에러든 삼켜 빈 배열을 주는 곳). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무슨 답이 나오는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 감시는 `test-harness/student_sched_summary_harness.mjs`(32종 — 정본을 esbuild 로 **실제로 돌립니다.** 「센다」 옆에 **「안 세는 것도 안 센다」를 짝으로** 둡니다. 변이 **12종** 전부 실제 FAIL이고 «이름만 바꾸는 무해한 리팩터» 1종은 통과합니다). 🔴 **그 하니스가 처음엔 «질의문» 을 한 줄도 안 봐서 변이 3종이 조용히 통과했습니다**(함정 대조 실측) — 가짜 DB 가 `prepare: () => ({…})` 로 **SQL 을 무시**하고 조건절은 «선언 텍스트» 만 읽어서, ⓐ `WHERE` 통째 삭제(취소된 예약·자리표시까지 셈) ⓑ 자리표시 제외만 제거 ⓒ **`user_id = ?` 제거(= 남의 예약이 그 학생 카드에 뜸)** 가 전부 초록이었습니다. ✅ **가짜 `prepare(sql)` 이 받은 SQL 을 적어 두고 «그 조건절을 실제로 쓰는가»·«그 학생으로 좁히는가» 를 짝으로** 물으세요(CLAUDE.md 「가짜 DB 는 질의문을 보고 답을 바꾸세요」의 형제 — 그쪽은 «답», 이쪽은 «질의문» 입니다). 🪤 **표에 칸을 더하면 열 수를 «19» 로 못 박은 검사가 깨집니다** — 보장은 그대로인데 검사만 빨간불입니다. 「몇 개인가」가 아니라 **「col·th·td·colspan 네 곳이 서로 같은가」** 로 물으세요(⚠️ td 를 셀 때 `<td` 만 세면 안 됩니다 — 헬퍼(`_ct`·`_schedTd`)가 한 칸을 통째로 돌려줍니다. ⛔ 헬퍼 «이름» 을 검사에 적지 마세요). 🪤 **검사 범위를 «길이» 로 자르지 마세요** — `cut(…, 420)` 로 둔 두 창이 실측으로 **이미 옆 코드를 먹고** 있었고(여유 111자) 그 함수가 조금만 자라면 정작 볼 부분이 창 밖으로 나가 **거짓 통과**합니다(중괄호 짝으로). 🪤 **변수 «이름» 을 글자 그대로 못 박지 마세요** — 이름만 바꾸는 무해한 리팩터에 거짓 FAIL 이 납니다(이름을 **소스에서 읽어** 쓰세요: `const (\w+) = await load…`). 🪤 그리고 **부정 검사를 «파일 전체» 에 걸지 마세요** — 무관한 기능(`adm-core.js` 의 딴 코드 `'주 ' + classes_per_week`)이 걸려 **거짓 FAIL** 이 났습니다(그 칸을 그리는 «그 자리» 만 잘라서 볼 것). 🪤 **fail-open 을 재는 검사는 «부를 때» 를 `try` 로 감싸세요** — 안 감싸면 그 변이가 «깔끔한 FAIL» 이 아니라 **하니스를 크래시**시켜 ❌ 가 **0건**으로 세어집니다(= «검출 못 함» 이 «통과» 로 위장 — 실측). ⚠️ 그 네 칸은 **여전히 카페24가 정본**이라 계속 «—» 입니다 — 채우려면 카페24 쪽에 값이 들어가야 합니다(사람이 정할 일). 📄 `docs/작업기록/260915_학생목록_예약칸.md` |
| 수업이 **2~3분마다 끊기는데 회선은 멀쩡함** (「오늘 5번이나 끊겼어」) | 🔴 **회선도 배포도 아니고 «서버가 살아 있는 학생을 죽었다고 보고 끊어내는» 것일 수 있습니다.** 📊 [잰 것 — 2026-09-15] 그날 수업 **3건 전원**이 20~30분에 **8~9번** 재접속했고(`class-1926` 8 · `class-1901` 9 · `class-2070` 8), 간격이 **123·135·137·137·137·160초**로 몰렸습니다. 같은 시각 회선은 멀쩡했습니다 — 학생 RTT **57ms**·손실 **0%**·영상멈춤 0, 강사도 손실 0%(`vc_quality`). 그리고 **9/1부터 계속**이었습니다(인당 5~11회). ⚠️ **에러가 안 납니다** — 화면은 재연결로 복구되고 출석표에도 한 행만 남아, 사람 눈에만 «끊김» 으로 보입니다. ✅ **세는 법은 `vc_roster` 입니다** — `PRIMARY KEY(room_id, peer_id)` 이고 peer_id 는 접속마다 새로 발급되므로 **행 개수 = 접속 횟수**입니다: `SELECT room_id, name, COUNT(*), datetime(MIN(updated_at)/1000,'unixepoch','+9 hours') FROM vc_roster WHERE updated_at > (strftime('%s','now')-86400)*1000 GROUP BY room_id, name HAVING COUNT(*)>1`. ⛔ `attendance` 로 세지 마세요 — 재접속해도 행이 안 늘고 `last_seen_at` 만 갱신돼 **정상으로 보입니다**. ⛔ **회선부터 의심하지 마세요** — 간격이 «규칙적» 이면 회선이 아니라 타이머입니다(회선 장애는 불규칙합니다). 🔴 **137초의 정체** — `video-call-room.ts` 의 죽은 소켓 청소(`LIVENESS_STALE_MS` 120초 + `LIVENESS_ALARM_MS` 45초)가 정확히 그 구간입니다. 그 판정의 정본은 **`getAutoResponseTimestamp()`** 인데, ping 이 자동응답으로 처리되면 `webSocketMessage` 가 **안 불려** 나머지 두 근거(`lastSeen`·`att.seenAt`)가 갱신되지 않습니다 ⟹ 그 정본이 비면 남는 바닥값이 «입장 시각» 뿐이라 **입장 120초 뒤 끊고, 재접속 후 또 120초 뒤 끊는** 순환이 됩니다. ⚠️ **이 마지막 한 줄은 «추론» 이고 측정이 아닙니다** — 확정하려면 Workers 로그의 `[VideoChat][liveness] … auto=… → 정리` 에서 `auto=없음` 인지 보세요(`head_sampling_rate = 0.05` 라 20건 중 19건은 안 남습니다). ✅ 그래서 **끊지 않는 쪽으로 실패**하게 고쳤습니다(2026-09-15) — ping 근거를 «한 번도» 못 잡은 소켓은 `LIVENESS_NOPING_STALE_MS`(600초)까지 기다립니다. ⛔ **그 상한을 «무한» 으로 두지 마세요** — 죽은 소켓이 수업 내내 남아 2026-08-20 「왜 3명이 나와?」 유령 사고가 되살아납니다. ⛔ **`LIVENESS_STALE_MS` 를 늘려서 풀지 마세요** — 그러면 ping 근거가 «있는» 진짜 유령까지 함께 늦게 치워집니다(고치려는 것은 «근거가 없을 때» 뿐입니다). ⛔ **클라이언트 ping 문자열을 바꿔 자동응답을 비켜 가게 하는 방식도 안 됩니다** — `idx-main.js` 는 blocking 이라 첫 화면 예산이 없고, 그러면 DO 가 25초마다 깨어나 hibernation 설계가 통째로 뒤집힙니다. ✅ 판정은 **순수 함수 `VideoCallRoom.livenessVerdict()` 한 곳**이고 `alarm()` 은 «부르기만» 합니다 — ⛔ 조건을 `alarm()` 안에 도로 복제하지 마세요(문자열 검사로는 `if (false && …)` 한 글자를 못 잡습니다). ✅ 끊을 때 **세 근거를 각각**(`auto=`·`mem=`·`att=`) 로그에 남깁니다 — 합친 값(`silent=`)만으로는 «왜 끊었나» 를 사후에 가를 수 없습니다. 📌 **배포는 무죄였습니다(그날 기준)** — 14:10·14:18 두 건은 보류 게이트에 걸려 `Deploy … Worker` 가 **skipped** 였고, 그 수업(`class-1926`)은 **배포 0건인데 8번 끊겼습니다.** ⚠️ **다만 «창 밖» 수업은 무방비입니다** — 그때 보류 창이 화·목 14:00~23:00 뿐이라 그날 13:03 배포가 `class-1901`(12:54~13:23) **한복판**에 나갔고 학생이 **21초 뒤 재접속**했습니다. ✅ **[2026-09-16] 바로 이 구멍은 메워졌습니다** — 창이 «화·목 12:00~16:00» 으로 바뀌어(사장님 「스케줄이 바뀌어서」) 정본을 그 시각으로 돌려 보면 **2026-09-15 13:03 KST(화) → 보류**입니다(옛 창에서는 «배포» 였습니다). ⚠️ 그렇다고 «창 밖이 안전해졌다» 는 뜻은 아닙니다 — 막는 폭 자체는 줄었고(475행 덧붙임), 그날 그 수업이 걸린 13시대가 마침 새 창 안으로 들어온 것입니다. 창을 넓힐지·실접속 판정(지금 403 으로 잠들어 있음)을 깨울지는 **사람이 정할 일**입니다. ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무슨 답이 나오는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 감시는 `test-harness/vc_ghost_liveness_harness.mjs`(51종 — 판정 함수를 **오려 내 실제로 돌려** 경계값을 넣습니다. 「근거가 없으면 안 끊는다」 옆에 **「근거가 있으면 예전대로 끊는다」·「상한을 넘기면 끊는다」를 짝으로** 둡니다 — 짝이 없으면 «전부 살려 두기» 도 통과합니다. 변이 **8종**(순수 함수 5 + **배선 3**) 전부 실제 FAIL). 🔴 **처음 판은 「판정 함수가 옳은가」만 물어 배선 변이 셋이 전부 초록이었습니다** — 유예 갈래 통째 삭제 · `if (verdict === 'grace')` → `if (false)` · **`autoAt = 0`**(= 모든 소켓이 유예를 받아 유령 보호가 통째로 풀림, 고치려던 것과 **반대 방향**). ✅ `alarm()` 몸통을 중괄호 짝으로, 판정 호출 인자를 괄호 짝으로 오려 내 **«무엇을 넘기는가»**(3번째 인자가 `autoSeenOf()` 가 채운 그 변수인가 — ⛔ 이름은 하니스에 적지 말고 소스에서 읽을 것)로 물으세요. 📄 `docs/작업기록/260915_수업_2분마다_끊김_liveness오판.md` |
| 관리자 화면이 **작업할 때마다 로딩이 무겁다** (같은 페이지인데 매번 통째로 다시 받음) | 🔴 **`htmlEtag304()`(src/index.ts, 공동 금지구역)의 ETag 검증자가 `env.BUILD_STAMP`(사이트 전체 배포 시각) «하나» 였습니다.** 그런데 BUILD_STAMP 는 «그 파일이 바뀌었는가» 가 아니라 «사이트 어딘가가 배포됐는가» 입니다 — 이 저장소는 하루에도 여러 번 배포되므로, `admin.html` 을 한 글자도 안 고친 배포에도 그 화면(1.3MB) 전체가 다음 방문에서 매번 통째로 다시 내려가고 있었습니다(2026-09-15 사장님 「작업을 할 때마다 너무 로딩이 심해」). ⚠️ **에러가 안 납니다** — 배포도 헬스체크도 정상이고, 틀린 것은 «어느 파일이 바뀌었는지 못 가린다» 뿐입니다. ✅ 고침 — Cloudflare Assets 는 워커 «안» 에서는 HTML 에도 **파일별** ETag 를 실어 줍니다(2026-07-22 주석이 이미 확인해 둔 사실 — CF 가 밖으로 나갈 때 떼는 것뿐). `htmlEtag304()` 이 그 `headers.get('ETag')` 값(native)을 우선 기준으로 쓰고, `BUILD_STAMP` 는 native 를 못 구했을 때만 쓰는 폴백입니다 — 같은 내용이면 배포가 몇 번이든 304 가 나갑니다. ✅ **호출부 6곳(teacher·manager·work·정적자산·SPA 폴백)은 한 글자도 안 건드렸습니다** — 전부 이미 `new Headers(xResp.headers)` 로 넘기고 있어서, 함수 안에서 읽기만 하면 됐습니다. ⛔ **본문을 매 요청마다 직접 해시하지 마세요** — `index.html` 은 29,000명이 여는 경로라 CPU 낭비입니다. ⛔ **stamp 와 native ETag 를 이어 붙여 하나의 문자열로 비교하지 마세요** — stamp 부분이 배포마다 바뀌므로 합친 문자열도 매번 달라져 원래 문제가 그대로 남습니다. ⚠️ 이 함수에는 이미 `build_stamp_harness.mjs` 가 있는데 그건 «BUILD_STAMP 를 검증자로 쓰는가·2줄이 같은가» 만 문자열로 봅니다(304 실제 응답은 안 돌립니다). 새로 만든 `test-harness/html_etag_native_harness.mjs` 가 함수를 중괄호 짝으로 오려 내 `new Function` 으로 **실제 실행**합니다(「배포가 갈려도 파일이 같으면 304」·「파일이 바뀌면 거짓 304 없음」·「native 없으면 예전 stamp 동작 보존」을 짝으로, 변이시험으로 `basis = stamp` 되돌리면 실제로 무너지는지까지 — 디스크 파일은 안 건드리고 메모리 문자열만 변형). 🪤 **내가 추가한 주석이 `build_stamp_harness.mjs` 의 `{0,2000}` 길이 창을 넘겨 「304 를 돌려주는 경로가 살아 있다」가 한때 거짓 FAIL 났습니다** — 하니스를 안 건드리고 내 주석을 줄여 해결(가장 안전한 수정 — 기존 하니스를 건드리지 않는 쪽을 선호). 📄 `docs/작업기록/260915_관리자화면_로딩_HTML_ETag_파일내용기준.md` |
| 학생 상세 화면에서 **「가입일」이 늘 «—»** (명부 표에는 뜨는데) | 🔴 **같은 라벨을 화면마다 다른 필드로 읽고 있었습니다.** `admin/student.html`(학생 상세 카드)은 「가입일」에 `erp.signup_date` 를 읽는데, 관리자 학생 **명부 표**는 같은 라벨을 이미 **`created_at`** 으로 정해 두고 있습니다(`js/adm-core.js` 의 `data-sort-key="created_at" data-ko="가입일"` + 정렬 기본값을 정하며 남긴 주석 「실제 표에 있는 열(가입일 = created_at)로 명시한다」). `signup_date` 는 명부 표에서는 **다른 이름**(「수강 시작일」)으로 따로 있는 카페24의 값 — 즉 「가입일」이 아니라 카페24 «수강 시작일» 입니다. ⚠️ **에러가 안 납니다** — 카페24 원본에 `signup_date` 가 없는 학생은 명부에는 가입일이 뜨는데(`created_at` 을 보므로) 상세 카드만 늘 «—» 였습니다(2026-09-15 사장님 제보 「왼쪽에 가입일이 비어있어」). ✅ 고침은 `erp.signup_date || fmtDay(erp.created_at)` — 두 자리(요약 카드·KPI 카드) 모두. ⛔ **`signup_date` 자체는 지우지 마세요** — «수강 시작일» 이라는 별개의 사실이고 다른 화면이 그 뜻으로 쓸 수 있습니다. ⚠️ **`created_at` 이 항상 «진짜 가입일» 은 아닙니다** — 카페24 동기화 행은 `created_at = CAFE24_STUDENT_SENTINEL`(고정 상수, 실제 생성 시각이 아니라 «이 행은 카페24가 정본» 이라는 표식)이라 그 학생들은 명부에도 상세에도 **똑같은 상수 날짜**가 뜹니다 — 의미는 약하지만 이 저장소가 이미 그렇게 정해 둔 기존 관례라 두 화면을 맞추는 쪽을 택했습니다. 관리자 수동 등록(`admin_manual`, `signup_date = today()` 로 생성)이나 자가등록 학생만 `created_at` 이 진짜 등록 시각입니다. ⚠️ **문자열 하니스로는 못 잡습니다** — 필드도 값도 다 «있고» 틀린 것은 «어느 필드를 읽는가» 뿐입니다. 감시는 `test-harness/student_contact_org_override_harness.mjs` ①절(폴백 식을 소스에서 오려 내 실제로 평가 — signup_date 있음/없음/둘 다 없음 세 경우를 짝으로) |
| 관리자 상세 화면에서 고친 값이 **다음 날 밤이면 원래대로 돌아옴** — 「학생 이름·전화번호」 말고 **다른 칸에서도** | 🔴 **`students_erp` 의 카페24 UPSERT SET 목록에 있는 칸은 «전부» 같은 사고를 냅니다 — 이름·전화번호는 이미 고쳤지만 그 목록의 다른 칸은 하나씩 새로 밟게 됩니다.** 2026-09-15 실사고: 학생 상세 화면의 「가맹점」(`franchise`)·「소속」(`shop_name`) 편집칸이 그랬습니다 — `PATCH …/contact` 가 `students_erp.franchise` 를 직접 UPDATE 해서 **그날은 저장이 되는데**, `cafe24-sync.ts` 의 UPSERT 가 `franchise = excluded.franchise` 로 카페24 값(대부분 비어 있음)을 그대로 덮어(카페24 쪽이 비어도 NULL 로 덮습니다) **오늘 밤 03:00 KST 에 지워집니다.** ⚠️ 에러가 안 납니다 — 저장은 「성공」했다고 응답하고, 다음날 다시 열어야 사라진 것을 압니다. ✅ 정본은 **`student_erp_override` 표 + 「지정 후 재적용」 패턴**(정본 `src/student-override.ts`) — 새 칸을 지킬 때마다 ① 지연 ALTER 로 override 표에 그 칸 추가 ② `set원본필드Field(env, uid, field, value, by)` 형태로 **한 번에 한 필드만** 받는 setter 신설(전화번호가 겪은 교차 오염 — 아래 항목 — 을 구조로 차단) ③ `applyStudentErpOverrides()` 에 그 칸 전용 **독립 UPDATE 문**을 추가(korean_name 과 «따로» — 한 SET 절에 합치면 한쪽만 지정된 행에서 지정 안 한 칸까지 override 의 빈 값으로 덮어씁니다) ④ 쓰는 쪽(`PATCH …/contact`)이 그 필드가 손에 닿았는지 추적해 필드마다 setter 를 따로 부르고 실패하면 응답에 `_override_warning` 을 실어 화면이 말하게 함 ⑤ 읽는 쪽(`GET …/full`)이 override 값을 **PII 마스킹 전에** 덮어 보여줌(짧은 창·재적용 실패에도 방금 저장한 값이 보이게). ⛔ **UPDATE 문을 한 SET 절에 합치지 마세요** — 위 ③의 이유. ⛔ **setter 시그니처를 두 필드 이상 받게 만들지 마세요** — 다음 항목의 사고가 그대로 재현됩니다. ⚠️ **새 칸을 지킬 때마다 이 다섯 단계를 전부 밟으세요** — 하나라도 빠지면(특히 ③) 「저장은 되는데 오늘 밤 지워지는」 상태가 그 칸에서 재현됩니다. 감시는 `test-harness/student_contact_org_override_harness.mjs`(34종 — 재적용 SQL 을 소스에서 오려 내 **진짜 SQLite 에 돌려** 독립 UPDATE 인지 확인. franchise 재적용 블록을 지우면 실제로 FAIL) |
| 조회 실패·행 없음을 **`unknown` 으로 떨어뜨리려** 했는데 «0» 으로 통과함 | 🔴 **`Number(null)` 은 `0` 이고 `Number.isFinite(0)` 은 참입니다 — 「못 물어봤다」가 「0건이다」로 조용히 바뀝니다.** 2026-09-21 실측: `resolveStudentTrack`(`src/student-track.ts`)이 `const n = Number(r && r.n); if (!Number.isFinite(n)) return unknown;` 였는데, 행이 없으면(`r === null`) `Number(null) === 0` 이라 유한수로 통과해 **`ai_only`** 가 나왔습니다 — 그 파일 자신이 바로 위 주석에 「⚠️ 조회가 실패하면 'unknown' 이다 — 'ai_only' 로 떨어뜨리지 않는다」고 적어 둔 **정반대 방향**입니다. ⚠️ **에러가 안 납니다** — 값도 조건도 다 «있고» 틀린 것은 «없는 것을 0으로 읽는다» 뿐입니다. ✅ **행 자체를 먼저 물으세요**(`if (!r || r.n == null) return unknown;`) — `Number()` 로 뭉뚱그린 뒤에는 「0건」과 「모름」이 같은 값이 됩니다. ⚠️ 같은 함정이 `Number('')`(=0)·`Number([])`(=0)·`Number(false)`(=0)에도 있습니다 — 『`|| 기본값`』 항목의 «coercion» 판입니다. ✅ 검사는 **가짜 DB 가 `first()` 로 `null` 을 돌려주는 시나리오를 짝으로** 두세요 — 「행이 있으면 센다」만 두면 이 결함이 원리상 안 보입니다(실제로 그 짝 하나가 잡았습니다). 감시는 `test-harness/leveltest_track_split_harness.mjs` ①절(변이 Ⓐ 로 되돌리면 실제로 FAIL) |
| 「지정 값을 저장」하는 setter 에 **두 필드를 한 번에** 넘기고 그중 하나만 지우려 할 때 | 🔴 **`clear` 플래그가 «필드별» 이 아니라 «호출 전체」 라면, 한쪽을 지우려는 요청이 다른 쪽의 멀쩡한 값까지 지웁니다.** `setOverridePhones({parent:'', student:'0103', clear:true})` 처럼 한 호출에 두 필드를 담으면, `clear:true` 가 **호출 전체**에 걸려 `student` 쪽도 «넘겨졌다» 는 이유만으로 같은 clear 취급을 받습니다 — student 값이 9자리 미만의 «가비지(오타 등, 마스킹 아님)」 값이면 `normPhone()` 이 그것을 `''`로 정규화하면서 **멀쩡히 저장돼 있던 학생 전화번호가 조용히 NULL 로 지워집니다**(2026-09-15 trap-check 로 발견, 진짜 SQLite 로 재현). ✅ 고침은 **필드마다 setter 를 따로 부르는 것**(`api-admin.ts` 의 기존 두 호출부가 이미 이 방식) — 한 호출의 payload 에 두 필드를 담지 않습니다. ✅ **더 튼튼한 고침은 시그니처 자체를 한 필드만 받게 만드는 것**입니다 — `setOverrideOrgField(env, uid, field, value, by)` 처럼 `field: 'a' | 'b'` 하나 + `value: string` 하나만 받으면, 호출자가 규율(«절대 합쳐 부르지 않기»)을 지킬 필요 자체가 없어집니다(같은 날 franchise·shop_name 에 이 설계를 적용해 원천 차단 — 위 항목). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 호출도 «있고» 틀린 것은 «한쪽을 지울 때 다른 쪽도 같이 지워지는가» 뿐입니다. 검사는 **진짜 SQLite 로 「한쪽 비움 + 다른쪽 가비지」 시나리오를 재현**하고, 옛 방식(합쳐서 호출)이 실제로 지우는 것과 새 방식(따로 호출)이 살아남는 것을 **짝으로** 대조하세요. 감시는 `test-harness/student_contact_phone_override_harness.mjs` ④-5절 |
| 「교재를 올렸으니 **AI 웜업에 붙여 달라**」를 받았을 때 | 🔴 **웜업이 문장을 가져오는 곳은 «교재 이미지»(`textbook_files`)가 아니라 «문장»(`review_quizzes`) 입니다 — 다른 표입니다.** 2026-09-15 실측: Mai 가 SIU BASIC 29권 **438장**을 올렸는데 `review_quizzes` 의 SIU BASIC 문항은 **0건**이었습니다(전체도 active **29건 · 14교재** 뿐 — BTS 1 001~008 각 1 · BTS 2/12 · 다락원 15 · 「SIU BOOKS」 1). ⟹ **이미지를 아무리 올려도 웜업은 그 교재 문장을 한 줄도 못 받습니다.** ⚠️ 에러가 안 납니다 — 고르면 AI 가 «주제로» 대화하고 문장 예시만 없습니다(BTS 34권 중 31권이 이미 그 상태입니다). ✅ 그래서 「붙여 달라」는 **두 갈래로 갈라 먼저 말하세요** — ⓐ 목록에 넣기(주제만 · 오늘 가능 · 비용 0) ⓑ 문장 만들기(교재 OCR 또는 사람 입력 · 비용·별건). ⛔ ⓐ만 해 놓고 「웜업에 넣었습니다」로 보고하지 마세요. 🔴 **그리고 시리즈를 한 목록에 섞지 마세요** — BTS 는 «학교 첫날·모양과 색깔»(어린이), SIU BASIC 은 «About you·Money and Shopping»(청소년~성인), SIU ADVANCE 는 «Job interview·Politics» 입니다. 합치면 **7살 학생 목록에 「Job interview」가 올라옵니다.** ✅ 2026-09-15 사장님 결정 — **단계로 가릅니다**(`SIU_BAND_MIN=5`: 1~4단계는 BTS 를 위에, 5~8단계는 SIU 를 위에). ⛔ **«안 보이게» 가 아니라 «접어 둔다»** 입니다 — 어른이 쉬운 교재를, 아이가 어려운 교재를 고르는 일이 실제로 있습니다. ⛔ **권마다 난이도를 매기지 마세요** — SIU BASIC 은 전부 같은 한 레벨이고 그것을 가를 자료가 없습니다(지어내는 것이 됩니다). `SIU_BAND_MIN` 한 줄로 «이 단계 이상에서 먼저 보인다» 만 정합니다. ⛔ **없는 Lv 를 지어내지 마세요**(`WCTX.level=''`) — 서버가 엉뚱한 레벨로 문장을 찾습니다. ✅ **`WCTX.textbook` 은 D1 이름 «그대로»** 보내세요 — 서버 `warmupLessonContext` 가 `LOWER(textbook)=LOWER(?)` **정확일치**라, 그래야 나중에 그 교재의 복습퀴즈가 생기는 날 **화면을 한 줄도 안 고치고** 저절로 붙습니다. ⚠️ 그래서 표의 `book` 칸에는 «공백 두 칸» 같은 D1 의 지저분함도 **그대로** 적어야 합니다(SIU 008·011·026 이 그렇습니다). **D1 을 고치면 그 표도 함께** 고치세요 — 한쪽만 고치면 조용히 안 붙습니다. ⚠️ **서버에 없는 번호를 목록에 만들지 마세요**(SIU 012 가 안 올라왔습니다) — «골랐는데 아무것도 없는» 교재가 됩니다. 🔴 **id 를 `parseInt` 로 읽지 마세요** — BTS 는 숫자(5)인데 SIU 는 문자열(`'siu015'`)이라 **NaN → 0 → «자유 대화» 로 조용히 떨어집니다**(정본 `bookIdOf`). 🪤 **이 변경이 기존 브라우저 검사를 깨뜨립니다** — 「교재 버튼 **33개**(자유대화1+32권)」처럼 **개수를 못 박은** 검사라 62개가 되자 **보장은 오히려 세졌는데 검사만** 빨간불이었습니다. 「몇 개인가」가 아니라 **「표에 있는 교재가 하나도 안 빠졌는가」** 로 묻고 기대값은 **소스에서 읽으세요**(⛔ 표를 못 읽으면 `0+0+1` 을 기대해 조용히 통과하니 «전제» 검사를 짝으로). ⚠️ **하니스마다 헬퍼가 다릅니다** — 자동 하니스의 `no(...)` 를 브라우저 하니스에 그대로 쓰면 `no is not defined` 로 **결과줄조차 안 나옵니다**(실측). ⚠️ **「SIU 가 위에 온다」만 두지 마세요** — «전부 SIU 로 바꾸기» 도 통과합니다. **「낮은 단계에서는 BTS 가 위」를 짝으로.** ⚠️ 그리고 **이름만으로 묻지 마세요** — `bookTitleOf` 는 갈래와 무관하게 «SIU BASIC …» 을 돌려주므로 **SIU 판정을 죽여도 통과했습니다**(실측 93/0). 📗 표시까지 짝으로 물어야 잡힙니다. 감시는 `test-harness/warmup_bts_book_harness.mjs` ⑨절(110종 — 표·`bookIdOf`·`btsBookOf`·목록·`applyBtsBook` 을 **오려 내 실제로 돌립니다**. 변이 **8종** 실제 FAIL: parseInt 되돌리기 6 · 조건 항상거짓 2 · **조건 항상참 3** · D1 이름 안 보내기 1 · 없는 Lv 지어내기 1 · 과 조회 가드 제거 1 · 표 비우기 2 · 없는 012 넣기 1. ⚠️ `String()` 비교를 `===` 로 되돌리는 것은 **등가 변형**이라 안 깨집니다 — `bookIdOf` 가 이미 정규화하기 때문이고, 그것을 «변이 N종» 에 넣어 세지 마세요)와 `manual/warmup-bts-book-browser.mjs` ⑪절(94종 — **자동으로 안 돕니다, 사람이 부릅니다.** 단계를 실제로 눌러 목록이 다시 그려지는지 봅니다. 변이 3종 실제 FAIL) 📌 **덧붙임(2026-09-15) — 「그 교재 문장이 없다」를 `review_quizzes` 하나로 판정하지 마세요.** 바로 위에 제가 「SIU 문장 0건 → OCR 이나 사람 입력이 필요」라고 적었는데, 저장소에 **`public/js/speech-data-siu-basic.js` 의 `SIU_BASIC_SENTENCES`(30과 × 100문장 = 3,000문장)** 가 이미 있었습니다(쓰는 곳은 발음 코칭 `speech-coach.html` 하나). 「`review_quizzes` 에 0건」은 맞았지만 **「문장이 없다」는 틀렸습니다.** 📜 같은 뿌리가 이 표에 이미 있습니다 — 「그 기능이 없어요」 제보를 받았을 때(이미 있는데 딴 화면에 있음). ⛔ **그렇다고 번호로 이어 붙이지 마세요** — [잰 것 — 2026-09-15] D1 29권 대 정본 30과에서 **이름이 맞는 것 22권**이고, 008(「and」 빠짐)·013(`&` 대 `and`)·027(`the`)은 글자만 다르지만 **022·023·024·025 는 정본에 아예 없고** 정본 쪽 4과(Law and Order·Money·Plans·Relationships)는 D1 에 없습니다 ⟹ **번호로 이으면 남의 과 문장이 붙습니다**(2장 「남의 이름이 뜸」과 같은 뿌리). ✅ 잇는다면 **이름 완전일치로만**, 안 맞으면 «모름». ⚠️ 그리고 그 3,000문장이 «교재 원문» 인지는 그때 **안 쟀습니다**(→ 2026-09-16 저녁에 쟀습니다. 이 행 맨 끝을 보세요 — **교재 원문이 아니라 310개짜리 공용 더미**였습니다). 📌 같은 날 D1 `textbook_files` 의 SIU 이름 셋을 고쳤습니다(44행 — 008·011·026). ⛔ 그때 **`SIU_BOOKS` 표를 함께** 고치세요(한쪽만 고치면 조용히 안 붙습니다). ⚠️ 008 은 «겹공백» 이 아니라 **낱말 하나(`and`)가 빠진 것**이었습니다 — 겹공백만 줄이면 `Celebrations Special Days` 라는 없는 제목이 됩니다. ⛔ 이름을 고친 뒤 Mai 가 **옛 폴더명 그대로 다시 올리면 같은 책이 두 묶음**이 됩니다. 🔴 **그 경고가 18시간 만에 그대로 일어났습니다** — 2026-09-16 12:44 KST 에 Mai 가 `SIU BASIC 026 - Should  you`(겹공백) 폴더를 다시 올려, 고친 이름과 옛 이름이 **각각 15장씩 두 묶음**이 됐습니다(페이지마다 `size_bytes` 가 같은 **byte 동일 중복**). ⟹ **D1 이름을 고쳤으면 «올리는 사람의 폴더도 바뀌었는지» 를 다음 업로드에서 확인**하세요 — 문자로 부탁하는 것만으로는 안 지켜집니다. ✅ **2026-09-16 16시 Mai 가 로컬 폴더명을 고치고 빠졌던 012 를 올렸습니다**(15장 — 그래서 SIU BASIC 이 001~030 으로 채워졌고 `SIU_BOOKS` 에 넣었습니다). ⚠️ **그래도 «서버에 이미 생긴 두 묶음» 은 저절로 안 사라집니다** — 폴더명을 고쳐도 옛 이름으로 올라간 15장은 D1 에 그대로 남습니다. ✅ 정리는 **지우지 말고 숨기세요**(`textbook_hidden_books` 에 그 이름 한 줄 — `/api/textbook-files?group=1` 이 그것을 걸러 수업 화면 라이브러리에서 사라집니다. ⚠️ 파일은 그대로라 이미 열려 있는 수업(`?book=`)은 계속 동작하고, 되돌리기는 `DELETE FROM textbook_hidden_books WHERE book = ?` 한 줄입니다). ⛔ `textbook_files` 행을 지워서 풀지 마세요 — 되돌릴 수 없습니다. 📌 **덧붙임(2026-09-16) — SIU ADVANCE 에서 «번호로 잇기» 의 대가를 실측했습니다.** D1 `textbook_files` ADVANCE 20권 대 저장소 정본 `public/js/speech-data-siu-advance.js`(`SIU_ADVANCE_SENTENCES` 20과 × 100문장 = 2,000문장)를 견주니 **번호 일치 4/20 · 이름 일치 6/20** 이었습니다 — D1 008 「Politics and government」 자리에 정본 8과 「Business Meeting」이, D1 004 「Environment」 자리에 「Presentation」이 옵니다(BASIC 의 22/29 보다 훨씬 나쁩니다). ⟹ **번호로 이으면 20권 중 16권에 남의 과 문장이 붙습니다.** ⛔ 이 표의 ⛔ 는 그대로입니다 — **이름 완전일치로만**, 안 맞으면 «모름». ℹ️ ADVANCE 도 `review_quizzes` 에는 **0건**이라 «주제만» 붙습니다(⚠️ 여기 「문장 붙이기는 사람이 정할 일」이라고 적혀 있던 것은 **이 행 맨 끝에서 닫혔습니다** — 2026-09-16 저녁에 재 보니 ADVANCE 문장도 **210개짜리 공용 더미**라 붙일 값이 없습니다). 🔴 **덧붙임(2026-09-16) — ADVANCE 는 «넣어도 한 문장도 안 쓰입니다». 그리고 그 이유는 «낱말 수» 가 아니라 «글자 수» 입니다.** 웜업이 예시 문장을 거르는 진짜 게이트는 `warmupLessonContext` 안의 **`isEnglishText(s)` — maxLen 을 안 넘겨 기본 80자**입니다(`src/english-only.ts`). [잰 것 — 2026-09-16, 정본 게이트를 오려 내 두 파일에 실제로 돌림] `SIU_ADVANCE_SENTENCES` 2,000문장은 **글자 수 최소 118 · 중앙 202 · 최대 255** 라 **게이트 통과 0건(0.0%)** 입니다 ⟹ **`SIU_ADV_BAND_MIN`(=7)·학생 단계와 무관하게 100% 버려집니다.** ⛔ **그러니 「이름 완전일치로 6권은 이을 수 있다」를 «쓸 수 있다» 로 읽지 마세요** — 그대로 D1 에 INSERT 하면(사장님 승인 + 학생 복습퀴즈에 SIU 가 생기는 부작용을 치르고도) **웜업 문장은 0개**이고 에러도 안 납니다. ⛔ **`WARMUP_WORD_CAP` 으로 예시 문장을 재지 마세요** — 그것은 **AI 답변** 길이 안전망(`replyTooLongFor`)이고 교재 예시 문장은 시스템 프롬프트에 **그대로 주입**되어 그 상수를 한 번도 안 지납니다(제가 그 둘을 견주어 「길이는 문제가 아니다」라는 **정반대 결론**을 한 번 냈다가 함정 대조로 잡았습니다 — 하루에 두 번 뒤집힌 자리라 **«무엇을 재서 그렇게 말하는가»** 를 함께 적습니다). ✅ **반대로 `SIU_BASIC_SENTENCES`(30과 × 100 = 3,000문장)는 게이트를 100% 통과합니다** — 글자 수 최소 45 · 중앙 60 · **최대 77** 로 「He gave his grandmother a tablet and explained how to use it.」 류입니다. ⚠️ **다만 80자까지 여유가 3자뿐**이라 문장을 손보거나 접두사가 붙으면 **조용히 탈락합니다.** 📊 **[D1 실측 — 2026-09-16, SELECT 만]** BASIC 은 이제 **30권**입니다(`012 What if` 가 올라왔습니다 — 「012 는 0장」이라고 적힌 옛 줄은 지금 사실이 아닙니다). 정본 30과와 견주면 **번호+이름 둘 다 맞음 24 · 이름 완전일치 25 / 30**이고, **못 잇는 것은 022·023·024·025·027 다섯 권**입니다(⚠️ 「022·023·024·025 네 권」으로 적힌 옛 줄은 **027 `Weather and the Seasons` ↔ 정본 `Weather and Seasons`** 를 빠뜨린 것입니다). 정본에만 있는 과도 다섯입니다(`Law and Order`·`Money`·`Plans`·`Relationships`·`Weather and Seasons`). ⚠️ **그래도 «붙이는 길» 이 D1 뿐입니다** — 그 표는 **학생 복습퀴즈와 같은 표**라 넣는 순간 학생 복습퀴즈 목록에도 SIU 가 생깁니다. ⛔ 그 부작용을 말하지 않고 넣지 마세요(1-1 — D1 쓰기는 사람 승인 뒤). 🔴 **덧붙임(2026-09-16 저녁) — 쟀습니다. «교재 원문이 아닙니다» ⟹ «붙이지 마세요».** [잰 것 — 정본 파일을 실제로 파싱하고 셸(`sort -u`)로 교차검증] `SIU_BASIC_SENTENCES` 는 30과 × 100 = **3,000문장인데 서로 다른 문장이 310개**뿐입니다. 한 과 «안» 에는 중복이 0인데 **그 310개가 전부 3과 이상에 나타납니다**(한 문장이 나오는 과 수 최소 3 · 중앙 10 · 최대 17 · **한 과에만 나오는 문장 0개**). 과끼리 문장이 **21~44%(평균 32%) 겹칩니다.** ⟹ 과마다 «그 과의 문장» 이 있는 것이 아니라 **310개짜리 공용 더미를 30과에 섞어 나눠 둔 것**입니다. 📊 그래서 **주제와 안 맞습니다** — 과 제목·주제의 낱말이 그 과 문장에 나오는 비율이 **0~8%**(낱말경계 매칭 기준 — 부분문자열로 재면 0~9%)이고, 「Weather and Seasons」의 첫 다섯 문장은 «선물 소포 · 다른 관점 · 단골 할인 · 조별과제 · 어머니날 편지» 로 **날씨 얘기가 하나도 없으며**(⚠️ 넷째에 `climate` 라는 낱말이 들어가지만 «조별과제» 문장입니다 — 그 과 100문장 중 날씨 낱말이 든 것은 **그 한 개**) 「Money」와 **33문장을 같이 씁니다.** 🔬 무엇으로 된 더미인지도 쟀습니다 — 310개 중 **4할 안팎이 수여동사형**(«주다/보내다/써 주다» + 사람 + 사물. 제 정규식으로 129개, 함정 대조는 132개 — 방법에 따라 갈립니다). 즉 **주제가 아니라 문형으로 묶인 문법 드릴 더미**이고, 그것이 곧 «주제 적중률이 0에 가까운 이유» 입니다. ⚠️ **ADVANCE 도 같은 모양입니다**(2,000 → **210개**) — 즉 위 「번호 일치 4/20 · 이름 일치 6/20」은 애초에 이을 값이 없던 것입니다. ⛔ **그러니 «그 더미로는» `review_quizzes` INSERT 도, «학생 화면을 안 건드리는 새 모듈» 도 하지 마세요**(⚠️ «SIU 를 `review_quizzes` 에 영영 넣지 마라» 는 뜻이 아닙니다 — 막는 것은 **이 310개짜리 더미**입니다) — 붙이면 AI 가 그 교재와 **무관한 문장을 «오늘 배울 문장» 이라고 학생에게 말합니다**(2장 「측정할 수 없는 값을 그럴듯하게 채우고 싶을 때」와 같은 뿌리 — 빈 것보다 나쁩니다). ✅ 진짜 교재 문장을 넣는 길은 **올라온 453장을 OCR 하는 것** 하나뿐이고 비용이 드는 별건입니다. 🔴 **그리고 그 어긋남은 발음 코칭(`speech-coach.html`)에 «이미 살아 있습니다»** — 처음에 저는 「거기는 따라 읽을 문장이면 되니 교재 원문일 필요가 없다」고 적었는데 **사실이 아니었습니다**(함정 대조가 잡음): 그 화면은 드롭다운에 `키 + ' · ' + title`(805행), 머리글 `#unit-topic` 에 `topic + ' · ' + N + '문장'`(823행)을 그려 **「SIU Basic 27 · Weather and Seasons」·「Climate / Seasons / Weather Activities · 100문장」을 학생에게 그대로 보여 준 뒤** 날씨와 무관한 문장을 읽힙니다. ⛔ **그래도 코스를 빼지는 마세요** — 따라 읽을 문장 자체는 멀쩡하고, 빼면 그 화면이 통째로 빕니다(2장 「그 기능이 없어요」의 반대 방향). ✅ **라벨(과 제목·주제)을 손볼지는 사람이 정할 일**입니다. ⛔ 이 결과를 근거로 그 데이터 파일을 지우지 마세요. ✅ **위 «숨김» 의 효과를 다시 쟀습니다** — 자료실에 보이는 SIU BASIC **30묶음·453장**으로, 겹공백만 빠지고 정본 이름은 그대로입니다. ⚠️ **그 방법이 «겹공백만» 거는 이유**는 숨김 판정이 `Set.has()` **정확일치**이고 묶음 이름을 만드는 `BOOK_EXPR`(`substr(name,2,instr(name,']')-2)`)에 **TRIM 도 공백축약도 없어서**입니다 — 두 이름이 27자·26자로 갈립니다. ⛔ 그러니 **이름이 «똑같은» 중복에는 이 방법이 안 먹습니다**(그때는 올린 쪽 폴더를 고쳐 다시 올리는 길뿐). 🪤 **그리고 제가 이 측정을 하면서 「그 숨김이 git 어디에도 없다」고 단정할 뻔했습니다** — 로컬 `main` 이 뒤처져 있어 그 작업(PR #1010)이 안 보였을 뿐입니다. 이 표의 「고쳐야 한다고 보고했는데 알고 보니 이미 고쳐져 있었음」 그대로이니, **«없다» 를 말하기 전에 `git fetch origin main` 부터** 하세요. |
| 그림·영상을 **낱말에 붙일 때** — 「이 그림이 그 낱말을 보여 주는가」를 무엇으로 판정하나 | 🔴 **«그 낱말이 들어 있는 문장의 그림» 은 근거가 아닙니다.** 그림은 문장 «전체» 를 보고 만들어 대개 그 문장의 «다른» 낱말을 그립니다 — 2026-09-21 사장님 제보: 「nice(좋은·멋진)」 카드에 **「Your backpack looks nice.」 의 가방 사진**. [잰 것 — 2026-09-21 빌드 입력 전수] 옛 기준이 그림을 붙인 **39,762줄 가운데 38,475줄(96.8%)이 그 낱말과 무관**했고, 같은 선택 함수의 «그림 있는 쪽 먼저» 때문에 **약 5,400줄(13%)은 남의 교재 문장을 예문으로** 보여 줬습니다(BTS 1 `student` ← SIU ADVANCE 감사 문장. ⚠️ 그 줄 수는 동점 정렬에 따라 갈리므로 «약» 으로 적으세요). ⚠️ **에러가 안 납니다** — 그림도 문장도 «있고» 틀린 것은 «그 낱말을 보여 주는가» 뿐이라 수리 전에도 하니스가 전부 초록이었습니다. ✅ 근거는 **«그 그림을 만들 때 쓴 설명»** 하나뿐입니다(변형형까지 맞춰 봅니다 — scissors/looking/boxes). 🔴 **그런데 그 «설명» 이 설명이 아닐 수 있고, 모양이 «셋» 입니다 — 하나씩 막을 때마다 수리가 그대로 실패할 뻔했습니다.** ① **문장을 그대로 넣은 틀**(`Natural candid photograph. A person'm fine too. … Your backpack looks nice. …`) → 설명이 자기 문장을 **네 낱말 이상** 그대로 담으면 버립니다. ② **그 틀이 «I → A person · my → their» 로 바꿔 놓은 판** → 낱말 창이 어긋나 **48개(그림 줄 909개)가 «진짜 설명» 으로 빠져나갔고**, 하필 `happy·favorite·hungry` 처럼 **`nice` 와 같은 부류**였습니다. ⛔ 창을 세 낱말로 좁혀서 풀지 마세요(멀쩡한 설명까지 버립니다 — 실측 97 → 38). **바꿔 놓은 것을 되돌려 놓고**(«a person» → «I», 소유격은 한 자리로) 견주세요. ③ **「Yes,」만 떼거나 「We → People」 로 바꾼 판** → **자기 문장에 없는 낱말을 둘도 못 보태는 설명은 설명이 아닙니다**(실측 분포가 0~1개 아니면 8개 이상으로 갈립니다). 🧱 **그리고 «거의 모든 설명에 들어 있는 낱말»(껍데기)을 근거로 삼지 마세요** — `natural·candid·photograph·clear·subjects·soft·text …` 가 그것이고, 안 빼면 **`clear`·`natural`·`subjects` 가 «모든» 그림에 붙습니다**(실측으로 실제로 그 셋이 붙어 있었습니다). ⛔ 껍데기 목록을 손으로 적지 마세요 — 틀이 바뀌면 조용히 낡습니다. **있는 설명에서 80% 규칙으로 세어** 정하세요. 🔴 **그 껍데기를 «두 번» 세지 마세요** — 남은 설명만으로 다시 세면 모집단이 달라져 껍데기가 어긋나고(실측: `clear`·`subjects` 가 다시 근거가 됨) 판정이 조용히 헐거워집니다. 그래서 입구를 **`pictureEvidence()` 하나**로 묶었습니다(설명·껍데기·판정이 짝을 잃을 수 없게). ⟹ 진짜 설명은 **그림 1,511개 중 26개**(+클립 45개)뿐이고, **그 그림들은 «낱말 그림» 이 아니라 «문장 삽화» 입니다** — 화면 문구도 그렇게 말해야 합니다. ✅ 그래서 세 단계로 가르세요: ① 🖼 **낱말 그림** = 진짜 설명이 그 낱말을 가리킬 때만 ② 🏞 **예문 상황 그림** = 근거가 없으면 **그 낱말의 교재 예문 «자체»의 그림**만(「낱말 뜻 그림은 아니에요」라고 말합니다) ③ 📄 **그림 없음** = 그것도 없으면 **안 붙입니다.** ⛔ **「같은 교재의 다른 문장」에서 빌려 오지 마세요** — 실제로 넣었다가 `nice` 에 가방이 **그대로 다시** 붙어 뺐습니다. 억지로 붙인 그림은 없는 것보다 나쁩니다(아이가 그 뜻으로 외웁니다). ⛔ **남의 교재 문장은 payload 에 아예 싣지 마세요** — 안 실으면 수준이 안 맞는 문장이 샐 자리가 없어집니다(덤으로 최대 payload 337,652 → 110,189바이트). ⛔ **「그림 연결 N개」처럼 «붙인 개수» 로 세지 마세요** — 그것이 이 사고를 몇 달 감췄습니다. **낱말 그림 / 상황 그림만 / 그림 없음** 셋으로 갈라 세고 화면도 그렇게 말합니다(실측 형태 **95 / 4,157 / 294**, 줄 **1,287 / 32,001 / 6,622**). ⛔ **없는 뜻을 지어내지 마세요** — 추상어 「설명」은 학생 화면들이 이미 쓰는 `/api/translate` **`mode:'learn'`** 에 **예문째로** 묻습니다(낱말만 번역하면 「Good job!」 → 「훌륭한 직업!」 류). ✅ 판정은 **모듈 하나**(`scripts/scene-picture-evidence.mjs`)에 두고 **빌드와 하니스가 같은 것을 돌리게** 하세요. 🪤 **하니스가 「97% 가 verified picture」라고 «단정» 하고 있었습니다** — 실제로 잰 것은 «그림을 붙였나» 뿐이었습니다. 「몇 %가 그림을 가졌나」가 아니라 **「그림이라고 말한 것마다 근거가 있는가」** 로 물으세요. 🔴 **그렇게 고쳐도 «게이트를 항상 참으로 여는» 변이를 못 잡습니다** — 이미 만들어진 payload 를 같은 모듈로 **되짚기만** 하면 `ok(depicts(…))` 는 더 쉽게 통과하고 짝 검사는 다른 항으로 저절로 참이 되어 **300,747건이 전부 초록**이었습니다(함정 대조 실측). ✅ **빌드가 고른 것을 하니스가 «다시 골라» 맞춰 보세요** — 「붙인다」 옆에 **「안 붙일 때는 정말 근거가 없다」를 짝으로**. 그러면 모듈 변이 6종·빌드 변이 1종이 **전부 실제 FAIL** 입니다. ⚠️ 그리고 **「낱말 그림이라고 말한다」만 검사하면 «전부 낱말 그림» 도 통과합니다** — 세 상태를 **짝으로** 두세요. 🪤 **화면 검사 100건이 CI 에서 한 번도 안 돌고 있었습니다** — `run.mjs` 와 `ci-gates.sh` 는 **`*_harness.mjs` 만** 모읍니다. `test-harness/` 루트에 있으면 «자동으로 도는 것» 처럼 보이니 **이름을 그 규칙에 맞추세요**(`scene_curriculum_ui_harness.mjs`). 감시는 `test-harness/scene_curriculum_harness.mjs`(341,133건)와 `test-harness/scene_curriculum_ui_harness.mjs`(100건 — 변이 **7종** 전부 실제 FAIL). ⚠️ 그 변이시험에서 **둘이 처음엔 안 잡혔습니다** — fixture 에 «bookExample 과 그림 문장이 «둘 다» 있고 다른» 줄이 없어서 「예문을 그림 문장 우선으로 되돌리기」가 **등가 변이**가 됐습니다. **두 값이 다른 시나리오를 반드시 넣으세요.** 📄 `docs/작업기록/260921_낱말그림_뜻과_안맞는_연결_기준교체.md`  📌 **덧붙임(2026-09-21 오후) — 사장님 「그림이 반드시 붙게 해줘. 모든 단어에 반드시」.** 그 자리를 «근거가 없으면 안 붙인다» 로만 두면 학생이 보는 것은 **빈 상자 + 「그림이 없어 붙이지 않았어요」** 입니다(실측 39,910줄 가운데 **6,622줄**). ⛔ 그렇다고 위 ⛔ 를 풀지 마세요 — 「같은 교재의 다른 문장 사진 빌려 오기」는 그대로 금지입니다(「nice」 ← 가방). ✅ 두 갈래로 채웁니다: ⓐ 그 교재 **«안»** 에서 **«그림이 있는 예문» 을 먼저** 고릅니다 — 그림만 빌려 오는 것이 아니라 **예문 자체를 그 문장으로 옮겨** 그림과 예문이 언제나 짝입니다(⚠️ `sourceIndex` 도 함께 옮기세요 — 안 옮기면 연습 구간이 예문과 어긋납니다). ⓑ 그래도 사진이 없으면 **우리가 그리는 «낱말 그림카드»**(그림문자 + 낱말, `#cq-wordcard`). ⟹ **그림 없는 줄 0**(🖼 1,287 · 🏞 33,452 · 🎨 5,171, 그중 4,751줄이 뜻에 맞는 그림문자). ⛔ 갈래만 맞춘 그림문자(`exact:false`)를 «이 낱말의 그림» 이라고 말하지 마세요 — 화면이 「낱말 갈래를 나타내는 기본 카드」라고 적습니다. 🔤 그림문자는 **단일 코드포인트 + Unicode ≤12** 만 쓰세요(ZWJ 조합 👩‍🏫 류도 금지 — 옛 Win10 에서 쪼개집니다). 정본은 `js/scene-curriculum.js` 의 `PICTO_TABLE/MORE/LAST` **한 곳**이고 빌드가 `createRequire` 로 그것을 읽습니다(⛔ 빌드에 표를 복제하지 마세요). 🪤 **카드를 «`!s.image`» 로 그리지 마세요** — 갈래 정본이 둘이 되어 `picKind` 를 옛 `'none'` 으로 되돌려도 **하니스가 전부 초록**이었습니다(실측). **`picKind(item)==='card'`** 로 물으세요. 🔴 **그리고 퀴즈 화면에 새 요소를 넣을 때는 «답이 새는가» 를 보세요** — 카드가 낱말을 적는데 예문은 빈칸이라 그대로 두면 **그 빈칸이 뜻을 잃습니다**(넣자마자 밟았고 브라우저 실측으로 잡았습니다 — 낭독기 이름(`aria-label`)도 같이 막으세요). 가리되 **맞히면 보이게** 짝으로 두세요(영영 가리면 반대 사고입니다). ⚠️ 검사는 「카드가 뜬다」 옆에 **「사진이 있는 줄에는 안 덮는다」를 짝으로** 두세요 — 짝이 없으면 «전부 카드» 도 통과합니다. ⚠️ **사진 로드 실패에도 카드로 채웁니다** — 회선이 나쁠수록 그림이 필요한 학생입니다. ℹ️ 남은 5,171줄이 «사진» 이 되려면 그 문장들의 그림을 새로 생성해야 하고 **비용이 드는 별건**입니다(사람이 정할 일). 감시는 `scene_curriculum_harness`(425,844종)·`scene_curriculum_ui_harness`(134종) — 변이 **10종** 전부 실제 FAIL(picKind 되돌리기 · 카드 안 그리기 · 그림문자 표 비우기 · 갈래를 exact 로 · 옛 예문 고르기 · 같은 교재 다른 문장 빌리기 · 남의 교재 빌리기 · 퀴즈에 답 적기 · 맞혀도 영영 가리기 · 카드 안내를 한국어로 고정). 📄 `docs/작업기록/260921_모든낱말에_그림_낱말그림카드.md` 📌 **덧붙임(2026-09-21 저녁) — «근거가 무엇인가» 를 고쳐도, 그 근거를 «누가 세는가» 가 어긋나면 검사가 통째로 헛돕니다.** 🔴 두 가지가 더 새고 있었습니다. ⓐ **틀의 꼬리가 prompt 를 타고 들어왔습니다** — `Natural **light**, realistic **setting**. No **writing**, logos, **subtitles** …` 의 그 넷은 «껍데기 80%» 에 못 미쳐 안 걸리는데 **교재에 실제로 있는 낱말**이라, 클립 549편 «전부» 가 그 넷을 보여 준다고 판정했습니다. ✅ 클립이 «설명»(`visual`·`action`)을 가졌으면 **prompt 는 보지 마세요**(안에 든 것은 같은 설명 + 틀뿐입니다. 실측 light 549→34 · setting 549→2 · subtitles 549→0. ⚠️ **「판정이 안 바뀐다」를 뭉뚱그려 적지 마세요** — 그대로인 것은 «낱말 사진» 근거 판정(1,401건 중 1,398건 통과)이고, 클립 그림을 근거로 삼던 **🖼 줄은 7,240 → 7,210 으로 30줄 줄어듭니다**(그 30줄이 바로 「light·setting 을 보여 준다」던 거짓 근거라 줄어드는 것이 목적입니다)). ⓑ **`variants()` 가 «있지도 않은 어미» 를 잘라 기능어를 만들어 냅니다** — `thing→the` · `ones→on` · `toes→to` · `used→us`(실측: `variants('thing')` 에 `the` 가 들어 있습니다). 그 기능어는 설명마다 있으니 **「thing」이 클립 그림 549개 중 426개(77.6%) · 「ones」가 312개(56.8%)** 에 붙었습니다. ✅ **기능어(`stopwords.json`)도 근거에서 빼세요** — 교재가 스스로 «뜻이 없다» 고 정해 둔 낱말이라 그것이 설명에 있다고 아무것도 알려 주지 않습니다(그 둘이 0개·3개로 떨어지고 교재 낱말 중 최대 비율이 77.6%(thing) → **26.0%**(hand 143개). 낱말 사진 판정은 1,398건 그대로, 🖼 줄은 7,211 → 7,210). ⛔ `variants()` 에서 그 넷을 손으로 빼지 마세요 — 어미 규칙이 바뀌면 조용히 낡습니다. 🪤 **그리고 그 둘을 잡겠다고 만든 새 검사가 처음엔 원리상 헛돌았습니다** — 하니스가 정본에 «안 묻고» 자기가 토큰을 다시 걸러 세는 바람에, 모듈에서 기능어 거르기를 **통째로 지워도 486,407건이 전부 초록**이었습니다(실측). ✅ 반드시 **정본 `depicts()` 에 물어서** 세세요. ✅ 검사는 «결과» 로 물으세요 — **«어느 교재 낱말도 클립 그림의 절반을 넘게 가리키지 않는다»**(실측 최대 26.0%). 그 한 줄이 위 둘을 «각각» 잡습니다(ⓐ 되돌리면 `light 549/549 = 100%` · ⓑ 되돌리면 `thing 426/549 = 77.6%`). ⛔ 문턱을 올려서 풀거나 낱말을 손으로 빼지 마세요. ⚠️ **«그래도 가리키는 낱말이 있다» 를 짝으로** 두세요 — 없으면 «근거를 통째로 죽이기» 도 통과합니다. ⚠️ 그리고 근거 판정을 고치면 **배포된 payload 가 낡습니다** — 반드시 다시 빌드하세요. 변이 3종 전부 실제 FAIL(기능어 거르기 제거 · clipRaw 되돌리기 · 껍데기 게이트 끄기). ⛔ **여기 적은 숫자는 «클립 549편» 기준입니다** — 클립을 더 받으면 비율이 통째로 달라지므로 인용하기 전에 다시 재세요(그 전에 44편 기준으로 적어 둔 95.5%·20.5% 가 클립이 늘자마자 거짓이 됐습니다).📌 **덧붙임(2026-09-21 밤) — 새 묶음을 «다음 번호» 로 매길 때 «이미 임자가 있는 자리» 를 뺏지 않는지 보세요.** 🔴 클립 505편을 받아 `6001..6505` 로 매겼더니 **6007 이 겹쳤습니다** — 그 번호는 `clip-plan` 의 5007(「The dog has four legs.」)이 `reuseClip` 으로 쓰던 «네 다리 강아지 재촬영» 매체였고, 등록이 그 매체를 사무실 영상으로 덮었습니다. ⚠️ **에러가 한 줄도 안 납니다** — 빌드도 하니스도 통과하고, 바뀌는 것은 「그 그림이 dog 를 보여 주는가」 하나뿐이라 근거 게이트가 **정직하게 «아니오»** 로 답해 `bts-03·08·18·24` 의 「dog」가 사진에서 그림문자 카드로 내려갔습니다(실측 카드 줄 14 → 25). ✅ **제가 알아챈 유일한 신호는 manifest 의 `cardRows` 가 늘어난 것**이었습니다 — 빌드 산출물을 갈아 끼울 때는 **manifest 를 «이전과 대조»** 하고, 늘거나 준 칸이 있으면 **그 낱말을 이름으로 찍어** 보세요(«개수만» 보면 어느 낱말이 내려갔는지 모릅니다). ✅ 고침은 묶음을 **`7001..7505` 로 옮기는 것**(천 단위로 묶음을 나눕니다 — 5xxx 1차 · 6007 재촬영 · 7xxx 2차). ⛔ **겹친 한 편만 비켜 두는 것으로 때우지 마세요** — 다음 묶음이 같은 자리에서 또 겹칩니다. 🪤 **그리고 감시를 «임자 없는 자리만 예약» 으로 짜면 스스로 무력해집니다** — 뺏기는 순간 임자가 생겨 예약 목록에서 빠지기 때문입니다(제가 실제로 그렇게 짰고 변이가 안 잡혔습니다). **«reuseClip 이 가리키는 자리를 «다른 묶음» 의 클립이 자기 번호로 쓰고 있지 않은가»** 로 물으세요 — 같은 묶음 안의 정당한 재사용(5014 → 5028)은 그대로 통과합니다. ⚠️ 그 둘은 **구조가 똑같아**(둘 다 「reuseClip 이 임자 있는 번호를 가리킨다」) 번호 묶음 말고는 가를 방법이 없습니다 — 그러니 **새 묶음은 반드시 «쓰지 않은 천 단위» 에서 시작**하세요. 감시는 `scene_curriculum_harness`(「클립 번호가 겹치지 않는다」·「reuseClip 자리를 다른 묶음이 뺏지 않았다」 — 변이 2종 전부 실제 FAIL). 🔴 **그런데 그 검사는 «이미 뺏긴 뒤» 만 봅니다 — «같은 천 단위 안에서» 뺏으면 정당한 재사용과 구조가 같아 원리상 못 잡습니다**(2026-09-21 함정 대조가 합성 입력으로 실측: 5007→5100 을 같은 5xxx 클립이 차지하면 «안 잡힘»). ⟹ **규약을 사람 기억에 맡기지 말고 기계가 강제하게 하세요** — 「**임자 없는 «전용 자리» 는 클립 번호가 하나도 없는 천 단위에 둔다**」 한 줄이면, 새 묶음이 그 천 단위로 들어오는 «순간» 빨간불이라 **뺏긴 뒤가 아니라 뺏기 전에** 걸립니다(정당한 재사용 5014→5028 은 «임자가 있어» 저절로 빠지므로 거짓 FAIL 이 안 납니다). ⛔ 전용 자리를 클립이 쓰는 천 단위 «안» 에 두지 마세요 — 그 자리를 지켜 줄 방법이 사라집니다. ⚠️ 그리고 **`reuseClip` 행이 0이 되면 이 검사들이 조용히 공회전합니다** — 「reuseClip 을 쓰는 클립이 있다」를 **전제 검사 한 줄로** 두세요. 변이 **3종** 추가로 실제 FAIL(같은 천 단위 뺏기 · 새 묶음이 전용 천 단위로 들어옴 · reuseClip 전부 제거). 📌 **덧붙임(2026-09-21 3차) — 「사진은 두고 «낱말 뜻 그림은 아니에요» 라고 말하면 된다」는 틀렸습니다.** 사장님 「아직도 nice에 가방이 보여 .. 전혀 상관관계가 없는데 서로다른 단어와 실사 이미지가 이렇게 되면 문제야」 — **아이는 그 글자보다 사진을 먼저 봅니다.** ⛔ 안내 문구로 덮지 마세요. 🔴 원인은 «고르는 차례» 였습니다 — ①교재 안 근거 있는 사진 → ②**근거 «없는» 예문 사진** → ③그 낱말만 그린 사진 인데, **③도 근거가 검증된 사진인데 ② 뒤에** 있었습니다(⟹ 그 낱말을 실제로 그린 사진이 있는데도 엉뚱한 사진이 이깁니다. 실측 **12,246줄**). ✅ **근거가 검증된 갈래를 «전부 먼저» 보고, 하나도 없으면 사진을 «아예 안 붙입니다»**(그 자리는 🎨 낱말 그림카드). ⛔ **화면 판정만 고치지 마세요** — 근거 없는 줄이 가리키는 장면의 **사진 «주소» 를 payload 에서 빼세요**(「안 실으면 샐 자리가 없습니다」). ⚠️ 한 장면을 «사진이 필요한 낱말» 과 «근거 없는 낱말» 이 함께 쓰면(실측 **6,116줄**) 주소가 남습니다 — 근거 없는 줄만 **사진 없는 쌍둥이 장면**(`<scene>~`)으로 옮기세요. ⛔ 그때 **원본 `scenes` 에서 지우지 마세요**(여러 교재가 함께 쓰는 원본이라 남의 교재가 깨집니다) — 사본으로. **[잰 것]** 🖼 **7,210 → 19,456줄(48.7%)** · 🏞 **32,679 → 0줄** · 🎨 **21 → 20,454줄**(뜻에 맞는 그림문자 **6,377**) · 남은 사진 주소 **0건** · 교재 한 권 최대 115,690 → **207,950바이트**(상한 350,000). 카드 그림문자 적중률 **BTS 41% · SIU BASIC 60% · SIU ADVANCE 21%**(ADVANCE 가 낮은 것은 `nevertheless` 류 추상 어휘라 **어떤 사진으로도 못 고치는** 종류입니다 — 「적중률이 낮으니 사진을 되살리자」로 읽지 마세요). 🪤 **변이시험 runner 가 «소스만» 되돌리고 빌드 산출물을 그대로 두면 다음 검사가 «변이가 만든 payload» 를 읽습니다** — 멀쩡한 코드가 빨간불이 나고 새로 넣은 검사를 의심하게 됩니다(실제로 밟았습니다). **`restore()` 가 다시 빌드까지** 하게 하세요. 🪤 **「표를 만들어 두고 loader 목록에서 빠뜨리기」는 원리상 조용합니다** — 표도 값도 다 «있고» 틀린 것은 «실렸는가» 뿐이라 두 하니스가 통과했습니다. **표를 소스에서 읽어** 그 낱말이 전부 `exact` 인지 보세요(⛔ 낱말을 하니스에 베껴 적지 마세요 — 표가 늘면 저절로 함께 검사됩니다). 🪤 **「치환 앵커 0곳」으로 한 번도 안 돈 변이를 «통과» 로 세지 마세요** — runner 가 그것을 출력하게 하고, 그 줄은 «변이 N종 전부 FAIL» 에서 빼세요. 감시는 `scene_curriculum_harness`(516,305종)·`scene_curriculum_ui_harness`(139종) — 변이 **8종** 전부 실제 FAIL(고르는 차례 되돌리기 · 근거 없는 사진을 «낱말 그림» 으로 세기 · 쌍둥이 분리 제거 · 화면에 옛 갈래 되살리기 · `showImage` 갈래 게이트 제거 · 그림문자 표를 loader 에서 빼기 · 전부 카드 · 근거 게이트 무력화). 📄 `docs/작업기록/260921_낱말그림_근거없는사진_안붙이기.md` 📌 **덧붙임(2026-09-22) — 「그 낱말을 담았는가」와 「그 교재 예문의 뜻인가」는 다릅니다. 앞만 물으면 게이트가 «정직하게» 통과시킵니다.** 사장님 「실사이미지들과 단어들이 연관성이 없어」 — `name` 에 글자 없는 빈 명패, `english` 에 그냥 교실, `clearly` 에 창밖. 프롬프트를 **낱말만 보고 썼더니** 모델이 «가장 흔한 뜻» 을 그렸고 근거 게이트는 그 낱말이 설명에 «글자로» 있으니 통과시켰습니다 — **에러도 없고 하니스도 전부 초록**입니다. [잰 것 — 2026-09-22] 표 3,229개를 교재 예문과 전수 대조 → **400개(12.4%)가 어긋남**: 다의어(`treats` 간식→**치료한다** · `nails` 못→**손톱** · `theme` 주제→**놀이공원** · `stock` 재고→주식) · 고유명사를 일반명사로(`tiger`·`woods` = Tiger Woods) · 품사 뒤바뀜(`showcase`·`breed`) · 추상어를 글자대로(`reflection`) · **«없는 것» 을 그림**(`name` 빈 명패 — 뜻이 정반대). ✅ 399개를 «예문 장면» 으로 다시 썼습니다(`clear` 는 틀에서 오는 껍데기라 구조적으로 불가). 🔴 **그런데 그 전수 대조가 «사장님이 보기로 든 낱말» 을 놓쳤습니다 — 400 은 «바닥» 이지 «전부» 가 아닙니다.** `name`(13125)은 제가 «형태» 로 문서에 적어 놓고도 **400 안에 없었고**, 그래서 399 를 고친 뒤에도 **빈 명패 그대로**였습니다 — 사장님이 시키는 대로 그 낱말을 열면 **신고한 화면이 그대로** 나옵니다(2026-09-22 뒤늦게 손으로 찾아 401번째로 고쳤습니다). ⛔ **「전수로 다 고쳤다」고 적지 마세요** — 한 번 놓친 판정은 또 놓칩니다. ✅ 기계로 훑히는 형태 하나(«빈·없는» 모양 87건)는 눈으로 전수 확인했고 뜻이 뒤집힌 것은 `name` 하나였습니다(나머지는 `bare foot` 류라 게이트로 걸면 86건이 거짓 FAIL). 나머지 네 형태는 훑을 기계가 없습니다. ⛔ **이것을 막는 기계 검사는 만들 수 없습니다** — 두 신호를 재 보았고 둘 다 게이트가 못 됩니다: 「«빈·없는» 모양인가」는 옛 3/400 대 새 1/400 로 **판별력이 없고**(표 전체 15건 중 `bare foot`·`bare arm` 이 대부분), 「프롬프트가 예문의 내용어를 공유하는가」는 옛 11/400 → 새 326/400 로 판별은 센데 **표 전체로는 2,216/3,243(68%)이 안 겹쳐** 전수 게이트로 걸면 거짓 FAIL 이 2천 건입니다(「environmental」 사진이 예문과 낱말을 안 나눠 가져도 그 낱말을 잘 그립니다). ⟹ 지키는 것은 **절차**입니다 — 프롬프트를 쓰기 전에 **그 낱말이 교재에서 쓰인 문장을 읽으세요**(**`typeof w.ex === 'number' ? b.examples[w.ex] : b.scenes[w.scene].text`** — ⛔ **뒤엣것만 읽지 마세요**: 예문은 `examples` 배열에 한 번만 담고 낱말 줄에는 번호만 싣습니다. 실측 사진 줄 39,885개 중 **39,143개**가 `w.ex` 쪽이라 뒤엣것만 보면 거의 다 «예문 없음» 으로 보입니다). 🔴 **그리고 사진을 갈아 끼우면 «그 사진에 빌려 붙어 있던» 낱말이 소리 없이 떨어집니다.** 399장을 바꾸자 낱말 **14개**(`dog's`·`touches`·`skateboard`·`shovel`·`joke`·`entertaining`·`treats`·`prizes`·`theme`·`electronics`·`canopy`·`north`·`historic`·`nails`)가 근거를 잃고 그림문자 카드로 내려갔습니다(카드 25 → **39**줄) — 전부 «자기 전용 사진이 없고 남의 사진에 rank-1 로 붙던» 낱말이라, 옛 프롬프트가 **우연히** 그 낱말을 담고 있던 것입니다. ⚠️ **기존 검사는 원리상 못 봅니다** — 「근거가 있는데 카드면 FAIL」만 보는데 여기서는 근거가 **통째로 사라졌습니다**. 잡아 준 것은 **manifest 대조** 하나뿐이었습니다(CLAUDE.md 의 그 줄이 실제로 일했습니다). ✅ 이제 `scene_curriculum_harness` 가 **「카드 줄이 늘지 않았다」(≤25)와 「사진 붙은 낱말이 줄지 않았다」(≥4,543 — 2026-09-23 에 **≥4,536** 으로 내림: 한 글자 «낱말» p·e·j·r·k·x·c 7개가 «낱말이 아니라서» 빠짐. 「P.E.」가 「e」 카드로 나오던 사고. 정본 `vocabWords()`)를 짝으로** 봅니다 — 짝이 없으면 «사진을 빼고 카드도 안 그리기» 가 통과합니다. 변이 실측: 14장을 치우면 **39 ≤ 25 · 4529 ≥ 4543 으로 각각 실제 FAIL**(종료코드 1). ⚠️ 그 숫자는 «상한·하한» 이지 «정답» 이 아닙니다 — 교재 문장을 더해 새 낱말이 들어오면 정당하게 카드가 늘 수 있고, 그때는 사진을 만들거나 숫자를 올리며 «왜» 를 적으세요. 🪤 **제 검증이 두 번 헛돌았습니다** — ⓐ `pictureEvidence` 를 **`clips:[]` 로** 불러 빌드와 «다른 껍데기» 로 판정(빌드는 `assets.concat(wordAssets)` 와 `clips` 를 함께 넣습니다 — 검증은 **빌드와 같은 인자**로) ⓑ 「표의 모든 항목이 통과한다」만 물어 **rank-1 로 빌려 붙는 낱말이 그 검사 범위 밖**이었습니다. ⚠️ 그 rank-1 이 **1,347개**입니다(사진 붙은 낱말 4,543 중 자기 전용은 3,196) — **그 1,347개의 «예문 뜻» 은 아직 아무도 안 봤습니다.** ⛔ 「전수로 고쳤다」고 적지 마세요. 📄 `docs/작업기록/260922_낱말사진_예문뜻으로_재생성.md` 📌 **덧붙임(2026-09-23) — 전용 사진을 «만들어» 채울 때 셋을 밟았습니다.** 🔴 ① **근거 게이트는 «아이 화면에 써도 되는가» 를 원리상 못 봅니다** — 묻는 것이 «그 설명이 그 낱말을 담았는가» 하나라서입니다. 575장을 만들어 접촉시트로 전수 확인하니 `showers` 한 장이 **수건·속옷 차림의 사람 셋**이었습니다(뜻은 정확했고 게이트도 정당하게 통과). ⟹ ⛔ **전수 눈 확인을 «게이트가 통과했으니» 로 빼지 마세요** — 사람이 아예 없는 문장으로 다시 써서 게이트를 **다시** 지나게 하세요. ⚠️ 그리고 사진을 갈아 끼우면 `build-scene-curriculum.mjs` 와 `build-word-pictures.mjs` 를 **둘 다** 다시 돌리세요 — payload 가 사진의 **바이트 수**를 들고 있어 한 장만 바꿔도 하니스가 「크기가 실제 파일과 같다」로 **크래시**합니다(실제로 밟았습니다). ⚠️ ② 제공자 실패는 **두 종류이고 대응이 다릅니다** — [잰 것 — 2026-09-23, 575건] 평범한 `failed` **116건(20.2%)** 은 **같은 프롬프트로 재제출하면 대개** 되고, `nsfw` **4건** 은 **다시 써야** 하며 **다시 쓴 프롬프트는 반드시 게이트를 다시 지나야** 합니다(안 지나면 그 사진은 표에만 남고 화면에는 한 번도 안 붙습니다). ⛔ 왜 `nsfw` 였는지는 제공자가 사유를 안 줘 **못 쟀습니다** — 추론을 사실로 적지 마세요. 🔴 ③ **«아직 안 쓰이는 새 사진 장수» 를 산술로 계산하지 마세요** — `만든 장수 − imageAssets 증가` 는 **«밀려나 안 쓰이게 된 옛 장면»** 을 빼먹습니다. 실측: 새로 쓰임 **457** · 밀려남 **6** ⟹ 순증 451 이라 `575 − 451 = 124` 가 아니라 **118** 입니다(작업기록·INDEX 두 곳에 124 로 적었다가 2026-09-23 정정 — 「잰 것」과 「거기서 계산한 것」을 줄을 나눠 적으세요). ⚠️ 그리고 **«남은 개수» 는 «같은 낱말인가» 를 무엇으로 보느냐로 갈립니다** — 같은 payload 에서 양방향 `variants()` **61** · 낱말→계획말 62 · 계획말→낱말 63 · 거친 어간 163. **줄어든 폭(−457)은 네 정의에서 모두 같으니** 성과는 «줄어든 폭» 으로 말하고 «남은 개수» 에는 **재는 방법**을 함께 적으세요. 📄 `docs/작업기록/260923_빌려쓰는낱말_전용사진_2차575장.md` |
| `pkill -f "<패턴>"` 를 썼더니 **그 명령 자체가 죽음**(뒤 명령이 통째로 안 돎) | 🔴 **`pkill -f` 는 «내 셸의 명령줄» 도 훑습니다 — 패턴이 그 줄에 들어 있으면 자기 자신을 죽입니다.** 2026-09-22 실측: 임시 서버를 치우려고 `pkill -f "http.server 8931"` 을 «파일을 쓰는 같은 명령» 맨 앞에 뒀더니 그 뒤 heredoc 이 한 글자도 안 써졌고 종료코드가 **144**(=128+16) 였습니다. ⚠️ **에러 메시지가 없습니다** — 다음 명령이 `MODULE_NOT_FOUND` 로 죽어서야 «파일이 없다» 를 알았고, 그때 의심하게 되는 것은 heredoc 문법이지 pkill 이 아닙니다. ✅ 패턴에 **대괄호 한 쌍**을 넣으세요(`pgrep -f 'http[.]server 8931' | xargs -r kill`) — 정규식으로는 같은 것을 잡지만 내 명령줄에 남는 글자는 `http[.]server` 라 스스로는 안 걸립니다. ✅ 또는 **포트로** 잡으세요(`fuser -k <포트>/tcp`). ⛔ 죽이는 명령과 «파일을 쓰는 명령» 을 한 줄에 잇지 마세요 — 죽으면 뒤엣것이 통째로 사라집니다. |
| 검사는 **문턱만** 보는데 보고서에는 **그 «값»** 을 적을 때 | 🔴 **그 숫자는 «잰 것» 이 아니라 «지어낸 것» 이 됩니다 — 그리고 아무도 다시 안 봅니다.** 2026-09-22 실측: 대비 검사를 `ok(..., cr >= 4.5)` 로 두고 값은 **실패할 때만** 찍게 해 놓고, 작업기록·목차·PR 세 곳에 **「대비 5.87:1」** 이라고 적었습니다. 함정 대조가 실제로 재니 **5.46** 이었습니다(같은 검사가 3.08 짜리 다크 모드는 **아예 안 보고** 있었습니다 — «통과했다» 가 «괜찮다» 가 아닙니다). ⚠️ 이 종류는 검사가 **초록불인 채로** 거짓 숫자를 낳으므로 하니스로는 원리상 안 잡힙니다. ✅ **성공해도 값을 찍게** 하세요(`console.log` 한 줄) — 적은 숫자가 사실인지 다음 사람이 바로 봅니다. ✅ 그리고 보고서에 숫자를 적기 «전» 에 **그 값을 찍는 줄을 실제로 돌려** 확인하세요. ⛔ 「4.5 를 넘었다」를 「5.87 이다」로 옮겨 적지 마세요 — 이 파일이 네 번 사고를 낸 「하기로 한 것이 «했다» 로 적힌다」의 «숫자» 판입니다. 📜 형제: 이 표의 「측정할 수 없는 값을 그럴듯하게 채우고 싶을 때」·「원인을 «찾았다» 고 보고했는데 알고 보니 추론이었음」 |
| 세션이 끊긴 뒤 **직전 턴에 내가 보고한 숫자·상태를 근거로** 이어서 작업할 때 | 🔴 **커밋하지 않은 작업트리는 컨테이너가 새로 뜨면 통째로 사라지는데, 대화에 남은 내 요약은 «했다» 로 읽힙니다.** 2026-09-16 실측: 앞 세션에서 안내문구를 고치고 「첫 화면 예산이 16 → 43바이트로 늘었다」까지 보고했는데, 새 컨테이너에서 재니 **16바이트**(= 고치기 전 값)였고 `git status` 깨끗 · 브랜치 = `origin/main` · `git reflog` 4줄(새 클론) · stash 없음 · PR 없음이었습니다. ⚠️ 더 나쁜 것 — 같은 자리를 다시 고쳐 보니 처음 판이 **−27B 가 아니라 +2B** 였습니다. 즉 그때 쓴 문구는 이번에 고른 것과 **다른 문구**였고, ⟹ **내 요약은 «무엇을 하려 했나» 의 근거는 되지만 «파일이 지금 어떤 상태인가» 의 근거가 못 됩니다.** ✅ 이어서 일하기 전에 **반드시 다시 재세요** — `git status` · `git log origin/<브랜치>..HEAD` · `git reflog` · 열린 PR · 그리고 **그 기능의 실제 숫자**(예산 바이트·해시). ⛔ 직전 턴의 줄 번호·바이트를 그대로 인용하지 마세요 (앞 세션 메모의 `.ai-search-wrap` 줄 번호가 실제와 **688줄** 어긋나 있었습니다). ✅ 복원이 안 되는 범위는 **추측으로 되살리지 말고 사람에게 물으세요** — 추측으로 커밋하면 이 파일이 네 번 사고를 낸 「하기로 한 것이 «했다» 로 적히는」 모양이 그대로 재현됩니다. ⛔ 그리고 기준선을 만들 때 `git stash` 를 쓰지 마세요 — 커밋 안 한 편집을 함께 치웁니다(`git worktree add --detach <경로> origin/main` 으로). |
| 서버에 값을 실었는데 **명부 표에는 «—»** (칸은 만들었는데 한 번도 안 닿음) | 🔴 **관문이 «둘» 입니다 — «어느 API 를 부르는가» 와 «화면 매핑이 그 칸을 옮기는가».** 2026-09-21 실측: 2026-09-15 에 만든 학생 명부 「예약」 칸이 **도입 이래 100% «—»** 였습니다. 값을 싣는 곳은 `erp-list` 인데 **그 표가 실제로 부르는 것은 `/api/admin/students/unified`** 이고(erp-list 는 대조표·검색용), 게다가 화면이 응답을 `apiItems` 로 **다시 매핑하면서 칸을 «골라 담아»** 서버가 실어도 `sched` 가 행까지 못 옵니다. ⚠️ **에러가 안 납니다** — 칸은 그려지고 값만 «—» 라 «그 학생은 예약이 없나 보다» 로 읽힙니다(사장님 제보 jeong — 활성 5건인데 «—»). ⚠️ **전용 하니스 32종이 전부 초록이었습니다** — 정본 함수만 돌려 보고 «화면이 그 값을 받는가» 는 한 줄도 안 봤기 때문입니다(정본을 돌려 보니 「주 4회」를 정확히 만들고 있었습니다). ✅ 명부에 칸을 더할 때는 ① **그 표가 부르는 API 를 `grep` 으로 확인**(30초) ② 그 API 응답에 싣기 ③ `apiItems` 매핑에 한 줄 ④ 검사는 **매핑 객체를 오려 내 실제로 평가**해 «옮겨지는가» 를 답으로 묻고 **«서버가 안 주면 지어내지 않는가» 를 짝으로**(짝이 없으면 «항상 값 있는 척» 도 통과합니다 — 변이 Ⓘ 로 실측). ⛔ 라벨 문장을 화면에서 조립하지 마세요 — 상세 카드도 같은 값을 그리므로 서버 `label_ko`/`label_en` 을 «고르기만» 합니다. ⚠️ 왼쪽 네 칸(결제타입·수강시작·수강종료·수업회수)이 «—» 인 것은 **다른 사정**입니다(카페24 정본 칸 — 원본에 값이 없습니다). 그 칸을 예약값으로 «채우지» 마세요. 감시는 `test-harness/schedule_once_and_sched_col_harness.mjs` ①절(변이 4종 전부 실제 FAIL) |
| 끝난 수업이 **홈·주간 표에 계속 남음** · 하루짜리를 «매주 도는 수업» 으로 읽음 | 🔴 **셋이 겹칩니다.** ⓐ `/api/class/schedule/mine` 이 `status != 'cancelled'` 만 보고 **지난 날짜를 안 걸렀습니다** — 실측 2026-09-21 지난 날짜인데 「활성」인 행 **1,808건 · 학생 495명**(사장님 제보 「홈에 지난 9/14 수업만 나온다」). ⓑ 소비자가 **둘**(홈 카드 `js/idx-my-schedule.js` · 주간 표 `my-schedule.html`)인데 **둘 다 같은 사고**라, 화면이 아니라 **서버에서 거르는 것이 맞습니다**(판정을 두 곳에 복제하면 어긋납니다). ⓒ 관리자 스케줄 줄의 **「정규수업」 배지는 `class_type`(수업 «종류»)이지 «매주 반복» 이 아닙니다** — 반복 여부는 `schedule_kind` 이고 화면 차이가 🔁/📅 아이콘뿐이라 하루짜리(`one_off`)를 매주로 읽습니다. ✅ 거르는 판정은 **이미 계산한 `next_start_ts` 하나**를 재사용하세요(= 시작+길이+15분 유예가 지났는가) — ⛔ filter 에서 날짜를 다시 재면 두 곳이 조용히 어긋납니다. ⚠️ **날짜 형식이 깨지면 «남기는» 쪽으로 실패**시키세요(수업이 조용히 사라지는 것이 더 나쁩니다 — 실측 깨진 날짜 0건이지만 방향을 박아 둡니다). ⚠️ 내부 판정 칸(`_past`)은 **응답에 새지 않게** 떼고 주세요. ✅ 화면은 **「이 날 하루만」·「지난 수업」을 글자로** 말하게 하세요(`mgsOnceInfo`, `admin/student.html`) — 흰 카드에서 읽히는 색은 `#b45309`(대비 5.02)이고 `#fbbf24` 는 **1.67 이라 안 읽힙니다**. ⚠️ **문자열 하니스로는 못 잡습니다** — 값도 조건도 다 «있고» 틀린 것은 «무엇이 남는가» 뿐이라 수리 전에도 `--fast` 357종이 전부 초록이었습니다. 🪤 그 검사에서 **반례를 «사전순» 으로 확인하세요** — 「형식이 깨지면 안 지운다」를 `'2026/09/14'` 로 물었더니 그 값이 문자열로도 오늘보다 «커서» **형식 검사를 지우는 변이가 그대로 통과**했습니다(변이 Ⓔ 실측). 사전순으로 «작은» 깨진 값(`'2025/09/14'`)으로 물어야 잡힙니다. 감시는 `test-harness/schedule_once_and_sched_col_harness.mjs` ②③절(변이 **11종** 전부 실제 FAIL) |
| 학생 상세·학부모 대시보드·성적표가 **출결만 통째로 «0»** — 예약도 수업도 멀쩡한데 | 🔴 **`attendance` 의 두 칸은 뜻이 다릅니다 — `user_id` 는 «접속마다 새로 발급되는 기기 임시번호»(`u_oqipkl162d`)이고 계정은 «`account_uid`» 입니다.** 그런데 «학생 한 명» 을 찾는 자리들이 전부 `WHERE user_id = ?` 에 **계정**을 바인딩하고 있었습니다 ⟹ 실측 **0건**. ⚠️ **에러가 안 나고 «0건» 이 정상값처럼 보입니다** — 화면은 「출석 0일·출석률 0%」라고 말했는데 실제로는 최근 30일 **23일·108세션**이었습니다(2026-09-21 사장님 제보 「왜 jeong 은 출결 현황이 없고 각종 자료가 모두 비어있지? 강선생님과 화수목금 정규수업을 계속 하고 있었는데」). 🔴 **왜 몇 달 동안 아무도 몰랐나 — 카페24가 «예약» 을 attendance 에 미리 만들어 두는데 그 씨앗 행은 `user_id` 가 «계정» 입니다.** [잰 것 — 2026-09-21, 전체 180,885행] 계정형 user_id + `c24-` 방 **177,461행(실접속 0)** 대 기기번호 + `class-` 방 **439행(실접속 432)** ⟹ 지금까지 화면이 보여 주던 «출결» 은 대부분 **예약 씨앗**이었고, 카페24에 없는 학생만 통째로 비어 보였습니다(계정이 확인된 22명 중 옛 조건으로 잡히는 학생 **6명**). ⚠️ 씨앗은 `status` 가 **`present` 11.9만 · `scheduled` 5.8만** 이라 **`status <> 'scheduled'` 필터로는 안 걸러집니다.** ✅ 정본은 **`src/attendance-uid.ts`** 의 `ATTENDANCE_BY_UID`(=`(account_uid = ? OR user_id = ?)`) + `attUidBinds(uid)` 하나입니다 — 학생 상세 8곳·학부모 대시보드·월간 성적표·학습 인사이트·배지·스트릭 판정이 전부 그것을 씁니다. ⛔ **«바꾸지» 말고 «넓히세요»** — `account_uid` 만 보게 바꾸면 카페24 학생 화면이 **통째로 비어 지금보다 나빠집니다**(그 행에는 그 칸이 없습니다). ⛔ **쓰기(UPDATE·DELETE)에 쓰지 마세요** — 넓힌 조건으로 쓰면 남의 행을 건드립니다(하니스 ⑤-4가 못 박습니다). 🔴 **인덱스가 «반드시» 있어야 합니다** — `account_uid` 에 인덱스가 없어 그 OR 이 **풀스캔**입니다(실측: 단순 조회 한 번이 rows_read **180,885**). 날짜 범위가 붙으면 `joined_at` 인덱스가 잘라 줘 4,417행·5.3ms 지만, «전체 기간» 을 세는 자리(총 세션·배지)는 범위가 없어 그대로 풀스캔입니다 → 정본이 `idx_attendance_account_date` 를 멱등 생성합니다(⛔ 지우지 마세요). ⚠️ **`account_uid` 는 2026-08-12 부터** 기록되어 그 전 출석은 **원리상 복구 불가**입니다. ⚠️ 넓히면 카페24 학생은 «씨앗 + 실접속» 이 함께 잡혀 **세션 수가 늘어 보입니다**(둘 다 그 학생 것이라 더 정확해지는 방향). ⚠️ 대소문자를 무시하던 자리(학생 «본인» 화면 2곳)는 **`ATTENDANCE_BY_UID_NOCASE`** 로 — ⛔ 새 자리에 함부로 쓰지 마세요(`Kim`/`kim` 처럼 대소문자만 다른 실제 계정이 있습니다). ⚠️ **문자열 하니스로는 못 잡습니다** — 표도 SQL 도 호출도 다 «있고» 틀린 것은 «어느 칸을 보는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 감시는 `test-harness/attendance_account_uid_harness.mjs`(26종 — 조건을 **정본에서 오려 내 진짜 SQLite 에 실제로 돌리고**, 「계정으로 찾는다」 옆에 **「기기번호 자리에 계정이 든 옛 행(씨앗)도 그대로 찾는다」를 짝으로** 둡니다. 짝이 없으면 «account_uid 만 보기» 변이가 통과합니다. 변이 **12종** 전부 실제 FAIL). 🪤 **그 하니스의 «전수 검사» 가 세 번 뚫렸습니다 — 셋 다 «범위를 어떻게 자르는가» 였습니다:** ① 정규식(`` /`[^`]*`/ ``)으로 SQL 문자열을 뽑으면 **파일 중간의 큰 HTML 템플릿**(`${}` 안에 또 따옴표)에서 짝이 한 번 밀려 **그 뒤가 통째로 안 보입니다** ② 줄 창(`slice(i-3, i+8)`)으로 보면 **옆 줄이 정본을 쓰면 그 줄에 가려** 새 실수를 놓칩니다 ③ 파일 첫 글자부터 문자열을 모으는 방식도 ①과 같은 이유로 밀립니다 ⟹ ✅ **「FROM attendance」 자리마다 «가장 가까운 백틱까지» 좌우로 넓히세요**(`sqlAt`) — 밀림과 무관하고 SQL 안의 `'c24-%'` 도 안 잘립니다. 🪤 **ALLOW 를 «파일 단위» 로 두지 마세요** — 그 파일에 «새» 실수가 생겨도 전부 통과합니다(변이 Ⓔ 가 실제로 그렇게 뚫렸습니다). **«그 SQL 의 지문» 으로** 좁히세요. 🪤 **변이가 «크래시» 가 되면 ❌ 0건으로 위장합니다** — 조건의 `?` 개수를 바꾸는 변이는 prepare 가 던져 결과줄조차 안 나옵니다. **돌리는 자리를 `try` 로 감싸 «깔끔한 FAIL»** 로 만들고, 변이시험은 **❌ 개수와 종료코드를 «함께»** 보세요. 📋 **아직 정본을 안 쓰는 자리 둘**(하니스가 이름을 찍어 출력 — 사람이 정할 일): `computeAttendanceStreak` 의 재귀 CTE(주석이 `idx_attendance_user_date` 를 타는 것을 전제로 해 OR 로 넓히면 30회 재귀가 무거워질 수 있음) · `api-admin.ts` 학생 목록의 `user_id IN (…)` 4곳(OR 로 넓히면 바인드가 2배라 **D1 100개 한도**). ℹ️ 같은 화면의 「결석 0인데 결석일자 23개」는 **별개의 두 번째 결함**이었습니다 — `classes_per_week`(카페24 정본 칸)가 비어 예상수업수가 0이 되는데 캘린더는 평일 전부를 결석으로 그렸습니다. 지금은 **«—» 와 사유**로 말하고, 그 목록 제목도 「수업 기록이 없는 평일」로 바꿨습니다(«결석» 과 다릅니다 — 주4회면 평일 하루는 원래 수업이 없습니다). 📄 `docs/작업기록/260921_학생상세_jeong_출결이_전부_빈칸_진단.md`
| 수업이 끝난 밤인데 화면이 **「수업 전에 하세요」** 라고 함 (「오늘의 A.i 학습」) | 🔴 **«지금이 수업 전인가 뒤인가» 를 «계산만» 하고 읽는 곳이 저장소 전체에 0곳이었습니다.** `buildTodayPlan`(`src/today-plan.ts`)이 `phase`(before/in_class/after)를 정확히 계산해 놓고도, 할 일 목록은 phase 와 무관하게 언제나 «웜업(전) → 복습(후) → 단어(집)» 순서에 문구도 고정이었습니다. [잰 것 — 2026-09-21, 정본을 시각만 바꿔 실제로 돌림] 09:20 → `before` · 14:10 → `in_class` · 21:20 → `after` 로 **phase 는 세 경우를 정확히 갈랐는데 steps 는 글자 하나까지 같았습니다.** ⟹ 사장님이 오후 2시 수업을 마치고 밤 9시 20분에 여니 1번이 「🗣️ 수업 전 AI 웜업 — 14:00 수업 전에 10분」이었고, 히어로도 「오늘 14:00 수업이 **있어요**」라고 앞으로 있을 일처럼 말했습니다. ⚠️ **에러가 안 납니다** — 수업일 판정도 시각도 전부 «사실» 이고 틀린 것은 «순서와 말투» 뿐입니다. 🪤 **하니스는 `phase` «값» 을 세 시각으로 정확히 검사하고 있었습니다**(①-1 세 건 통과) — «그 값을 쓰는가» 만 아무도 안 봤습니다(「판정 함수가 옳은가만 물으면 배선은 아무도 안 본다」 그 자리). ✅ 고침은 **phase 로 순서·문구를 가르는 것** — 수업 전이면 웜업이 1번(예전 그대로), 시작된 뒤면 복습이 1번이고 웜업은 맨 뒤로 내려 「다음 수업 전에 하면 돼요」로 말합니다. ⛔ **웜업을 목록에서 «빼지» 마세요** — 다음 수업 전에 미리 해 두는 길이 사라집니다(빠지는 쪽이 훨씬 나쁩니다). ⛔ **화면에서 시각을 다시 재지 마세요**(`Date`·`getHours`) — 판정이 두 벌이 되어 서버와 어긋나는 날 화면이 조용히 거짓말합니다. 서버가 준 `phase` 만 «고르기만» 합니다. ✅ 검사는 「`after` 면 복습이 1번」 옆에 **「`before` 면 예전 그대로 웜업이 1번」·「지난 뒤에도 웜업이 목록에 남는다」·「세 phase 의 할 일이 서로 다르다」를 짝으로** 두세요 — 앞만 보면 «언제나 복습 먼저» 도, 뒤만 보면 «웜업을 통째로 빼기» 도 통과합니다. ⚠️ 이 저장소는 로컬에 `node_modules` 가 없어 그 ① 절이 **esbuild 가 없으면 통째로 건너뜁니다** — 새 검사를 넣었으면 **변이를 실제로 넣어 FAIL 이 나는지 따로 확인**하세요(그러지 않으면 «검사를 넣었다» 가 «검사한다» 로 읽힙니다). 감시는 `test-harness/today_plan_harness.mjs` ①-1b(정본을 실제로 돌림)·⑦(화면이 phase 를 읽는가). **변이 8종 전부 실제 FAIL** — 정본 4종(옛 코드 되돌리기 6 · 언제나 복습 먼저 3 · 웜업 빼기 5 · in_class 문구 돌려쓰기 1) · 화면 4종(옛 고정 문구 3 · **after↔before 문장 맞바꾸기 2** · **조건 항상거짓 2** · 화면이 시각 다시 재기 3). 🪤 **처음 판은 그 넷을 못 잡았습니다** — ⑦을 「그 글자가 있는가」로 물어서, 문장을 맞바꾸거나 `&& false` 로 죽여도 `p.phase === 'after'` 가 그 줄에 남아 **전부 통과**했습니다(= 사장님이 신고한 문장이 그대로 재현되는데 초록불). **삼항식을 괄호 짝으로 오려 내 `new Function` 으로 세 phase 를 돌려 «무슨 글자가 나오는가»** 로 물으세요 — ⚠️ 그때 `indexOf('=', …)` 는 `p.mode === 'class'` 의 `=` 를 먼저 잡습니다(`sub =` 를 앵커로). 🪤 그리고 **`find(...).whyKo` 를 그대로 읽지 마세요** — 웜업을 빼는 변이에서 «깔끔한 FAIL» 이 아니라 **하니스가 죽어 결과줄조차 안 나옵니다**. 📄 `docs/작업기록/260921_오늘의AI학습_수업끝난뒤에도_웜업먼저.md` |
| 캘린더·표가 **«신청서»(계획)를 그리는데 실제 수업은 하나도 없음** — 유령 카드가 종료일까지 매주 남음 | 🔴 **«계획» 과 «실체» 가 다른 표에 있으면, 계획만 보고 그리는 화면은 실체가 사라져도 계속 그립니다.** 2026-09-21 사장님 제보(jeong 금 14:20 체험수업): 학생 상세 캘린더의 수강신청 레이어가 `enrollments` 를 `started_at`·`end_date` «만» 보고 그려서, 그 신청이 만든 실제 수업 4건이 전부 취소된 뒤에도 10/11 까지 매주 초록 카드가 남았습니다. ⛔ **`enr.status` 로는 못 가립니다** — 실측에서 그 신청서는 여전히 `confirmed` 였고 취소된 것은 `class_schedules`(`source='adm-enroll:103'`) 쪽이었습니다. ⚠️ 제가 그 전날 PR 에 「`status` 검사가 없다 · 실피해 0건」이라고 적어 둔 바로 그 자리인데 **진단이 절반 틀렸고 심각도도 낮춰 적었습니다** — ⛔ **곁가지로 찾은 것의 심각도를 낮춰 적지 마세요**(「지금 터진 사고가 아니다」와 같은 뿌리. 그 한 줄 때문에 다음 사람이 다시 안 봅니다). 📊 [잰 것 — 2026-09-21] 신청서 14건을 `LEFT JOIN class_schedules ON source='adm-enroll:'||e.id` 로 전수 대조하니 13건은 만든 수업이 전부 살아 있고 **103 만 `made=4 · live=0`** 이었습니다. ✅ 고침은 **«그 신청이 만든 살아 있는 수업이 하나라도 있는가»** 를 묻는 것(정본 `mgsEnrHasLiveClass()`, `admin/student.html`) — `GET /api/admin/class-schedules` 가 `cs.status != 'cancelled'` 라 «살아 있는 것» 만 주므로 0건이면 «수업 없음» 입니다. 🔴 **그리는 방법은 같은 날 두 번 정해졌습니다 — 지금은 «안 그립니다».** 처음(#1059)은 «회색으로 낮추고 사실을 적기» 였는데 사장님이 화면을 보시고 **「체험수업이 없는데 나오는 것은 안돼. 캘린더에 나오지 않게 해줘」**(#1071)로 바꾸셨습니다. ⛔ 회색 카드로 되돌리지 마세요. ⚠️ **그래도 «그냥 감추기» 는 안 됩니다** — 감추면 「신청서와 수업이 어긋났다」를 아무도 못 봅니다. 그래서 캘린더 «위» 에 **«안 그린 N건» 한 줄**(`mgsSetGhostNote`, `#d-sched-ghost-note`)로 그 사실만 남깁니다 — ⛔ 그 줄을 지우지 마세요. ⚠️ 한 신청이 한 주·한 달에 여러 날 걸려도 **«1건»** 이라야 합니다(날짜가 아니라 **신청 id** 로 셀 것 — 날짜로 세면 「3건」이 됩니다). 🔴 **그 «N건» 을 세는 자리가 주간·월간에서 다르면 한 화면이 두 답을 합니다** — 판정을 «요일·시각 매칭 뒤» 에 두세요. 앞에 두면 **그 주에 원래 그려지지도 않았을 신청까지 세어** 「그리지 않았습니다」가 거짓이 됩니다(실측: 요일이 빈 신청을 주간만 「1건」). 🔴 **그리고 「그다음은 어디를 보라」가 실재하는 곳인지 확인하세요** — 저는 「아래 «수강신청 이력»」이라 적었는데 그것은 **다른 표**(카페24 ClassOrders)이고 **다른 탭**(📋 평가서) 안이며 **조건부라 없을 수도** 있었습니다. 그 신청이 실제로 남아 있는 곳은 **📝 등록·수강** 탭입니다 — 감추기를 막으려 넣은 줄이 «없는 곳» 으로 사람을 보낼 뻔했습니다(함정 대조가 잡음). ⛔ 탭 이름을 하니스에 손으로 적지 말고 **탭 버튼에서 읽어** 대조하고, **한국어와 영어가 같은 곳을 가리키는지 짝으로** 보세요(영어만 사실인 상태가 실제로 있었습니다). ⛔ **판정을 두 뷰에 복제하지 마세요** — 주간·월간이 같은 레이어를 쓰는데 월간에는 `aiSchedules` 레이어가 아예 없어 한쪽만 고치기 쉽습니다. 🔴 **fail-open 방향이 생명입니다** — 그 API 는 조회가 실패해도 **`{ok:true, count:0, items:[], warning:…}`** 를 줍니다. `ok` 만 보면 「수업이 없다」와 「못 물어봤다」가 같은 값이 되어 **멀쩡한 수강신청이 통째로 캘린더에서 사라집니다**. `warning` 까지 보고, 못 읽었으면 **예전대로 그리세요**. 🔴 **이 화면은 `adm-light-theme.css` 로 «밝은» 테마입니다** — 패널이 어두워 보이는 인라인 style 에 속아 `#cbd5e1` 같은 밝은 글자를 주면 **대비 1.36 으로 안 읽힙니다**(안내 줄을 그렇게 줬다가 함정 대조로 잡았습니다 — 이 화면이 이미 쓰는 **`#475467`** 에 배경 `#f1f5f9` 면 7.02). ⚠️ 그리고 **배경을 «격자선과 같은 색»으로 고르지 마세요** — 캘린더 «안» 에 그리는 것이라면 `#f1f5f9` 가 슬롯 격자선과 같아 묻힙니다(옛 회색 카드에서 실제로 밟았습니다). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무엇이 그려지는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 렌더 함수를 **중괄호 짝으로 오려 내 가짜 DOM 으로 실제로 돌려** 나온 HTML 을 보고, 「안 그린다」 옆에 **「살아 있으면 예전 그대로 그린다」·「못 읽었으면 예전 그대로 그린다」를 짝으로** 두세요(앞만 보면 «전부 안 그리기» 도 통과합니다 — 변이 Ⓕ 로 실측). 🪤 **그 검사가 만들며 세 번 헛돌았습니다** — ⓐ `aiOk` 를 `/aiOk = ([^;]+);/` 로 뽑았더니 **초기화 줄(`= false;`)을 먼저 물어** 늘 false 가 되었고 「못 읽음」 검사 셋이 **우연히** 통과했습니다(`aiR` 를 참조하는 식만 고를 것) ⓑ 색을 하니스에 **손으로 적어** 소스를 바꾸자 어긋났습니다(**소스에서 읽어** 올 것) ⓒ 「격자선과 다른 색인가」를 파일 전체로 물어 **요일 머리글 구분선**에 걸려 거짓 FAIL (카드가 «놓이는» 슬롯 격자선만 콕 집을 것). ⚠️ 반례를 «남의 번호» 하나로만 두면 `===` 를 `includes` 로 바꾸는 변이가 안 잡힙니다 — **접두사를 품는 번호**(`adm-enroll:1030` 대 `103`)를 반드시 섞으세요. 감시는 `test-harness/schedule_enroll_ghost_harness.mjs`(54종 — 변이 **14종** 전부 실제 FAIL: 주간·월간 숨김 제거 · 조건 뒤집기 · 안내 배선 삭제 · 안내를 언제나 숨김 · 전부 안 그리기 · 날짜로 세기 · fail-open 제거 · `warning` 무시 · 안 읽히는 글자색 · 안내 요소 삭제 · **판정을 요일매칭 앞으로** · **옛 표로 보내기** · **영어에서 안내 빼기**). 📄 `docs/작업기록/260921_캘린더_신청만_수업없음.md` · `docs/작업기록/260921_캘린더_유령카드_안그리기.md` |
| «한 단계 통과 확률» 을 **«최종 도달 확률» 로 옮겨 적음** — 없는 사고를 만들고 진짜 결함을 가림 | 🔴 **확률을 적을 때 «그 일이 몇 번 연속이어야 하는가» 를 세지 않으면 심각도가 통째로 뒤바뀝니다.** 2026-09-21 에 제가 레벨테스트를 두고 「순수 찍기로도 **C2 26.1% · B2 이상 59.8%**」라고 **세 곳**(PR #1073 본문·작업기록·보고)에 적었습니다. 실제로 규칙을 오려 내 20만 회 돌리니 **C2 0.019% · B2 이상 0.48%** 입니다(이론 0.032% · 0.469%). 26.17% 는 **«한 레벨 통과 확률»**(4문항 중 2개 이상, `1-0.75⁴-4·0.25·0.75³`)이고, 최상위에 닿으려면 그것을 **6번 연속**해야 합니다(`0.2617⁶`). ⟹ 「찍어서 최상위가 나온다」는 **없는 사고**였고, 그 문장이 진짜 결함을 가렸습니다. 🔴 **진짜 결함은 «절반만 맞혀도 최상위» 입니다** — 각 레벨 4문항 중 **2개(50%)** 면 통과라 찍기 기댓값(1개)보다 겨우 한 개 위입니다. 실측: **최상위에 닿는 가장 낮은 점수 = 50점**(각 레벨 2/4) · D1 에 `73점 → C2` · `64점 → C2` 가 실제로 남아 있고 · `51점 → A2` 인데 `50점 → B1`(뒤집힘). 모델로 재면 **과대평가 22.4% · 과소 3.6% · 정확 74.0%** 입니다. ⛔ **그렇다고 문턱을 올려서 «고치지» 마세요** — 3/4 로 올리면 과대는 2.9% 로 줄지만 **과소가 31.1%**, ±1 단계 밖이 7.2% → **23.0%** 로 나빠지고 점수-레벨 뒤집힘도 8.6% → 19.6% 로 두 배가 됩니다(`break` 라 한 레벨에서 미끄러지면 거기서 끝나기 때문). ⛔ 그리고 **판정할 «정답» 이 없습니다** — `teacher_score` 가 도입 이래 **0건**이라 「어느 규칙이 맞는가」를 우리 데이터로는 증명할 수 없습니다. 규칙을 바꾸는 것은 **사람이 정할 일**입니다. ⚠️ 모델(아는 레벨 0.85 · 나머지는 찍기)은 **가정**입니다 — 그 숫자를 「우리 진단의 정확도는 74%」로 옮겨 적지 마세요. 쓸 수 있는 것은 «규칙을 바꿨을 때의 변화» 뿐입니다. ✅ 감시는 `test-harness/leveltest_level_rule_harness.mjs`(16종 — 문항표·채점식·판정 루프를 **소스에서 오려 내 실제로 돌립니다**. **과대와 과소를 «짝으로»** 재고 합친 오차로 판정합니다 — 한쪽만 재면 문턱을 올렸을 때 「좋아졌다」로 보입니다. 변이 3종 전부 실제 FAIL: 문턱 3/4 · `break` 제거 · 문턱 1개). 🪤 그 하니스를 만들며 셋을 밟았습니다 — ⓐ 배열 리터럴을 «선언 줄의 첫 `[`» 로 찾으면 TypeScript 의 **`string[]`·`readonly string[]`** 이 먼저 걸려 **빈 배열**이 되고 아래 검사가 전부 헛돕니다(`= [` 부터 세세요) ⓑ `for` 루프를 **블록만** 자르면 `break` 가 밖에 놓여 `SyntaxError` 입니다(for 문 **전체**를 자를 것) ⓒ 「최상위에 닿는 최소 점수」를 **문턱을 손으로 적어** 계산했더니 문턱을 바꾸는 변이에서도 **50점 그대로**였습니다 — 오려 낸 판정 함수에 **모든 조합을 넣어 찾으세요**. ⚠️ 그리고 그 관찰(「64점으로 최상위가 나온다」)을 **FAIL 조건으로 두지 마세요** — 규칙을 엄격하게 고치는 정당한 수리가 빨간불이 됩니다. 📄 `docs/작업기록/260922_레벨계산규칙_잰것과_결론.md` |
| 화면이 **«하지 않는 일» 을 약속**함 — 「A와 B까지 합쳐 …됩니다」인데 B를 쓰는 코드가 0곳 | 🔴 **「보냈습니다라고 하는데 아무 데도 안 갔음」의 «말» 판입니다 — 기능이 아니라 «약속» 이 거짓입니다.** 레벨테스트 결과 화면이 **모든 학생에게** 「**선생님 1:1 평가**와 **발음 평가**까지 합쳐 최종 레벨이 확정됩니다」라고 말했는데, [잰 것 — 2026-09-22] `voice_coaching` 을 읽는 곳은 관리자 화면의 **«보여주기»(오버레이)뿐**이고 `final_level` 을 바꾸는 코드는 **0곳**입니다. 선생님이 그 점수를 «참고» 할 수는 있어도 «합쳐서 확정» 은 일어나지 않습니다 → 「선생님 1:1 평가로 최종 레벨이 확정되고, 발음 평가는 선생님이 함께 참고해요」로 고쳤습니다. ⛔ 반대로 **«없는 것» 취급하지도 마세요**(선생님이 실제로 봅니다) — 「말한다」 옆에 **「지우지도 않는다」를 짝으로** 검사하세요. 🔴 **그리고 같은 화면이 AI 학습만 하는 학생에게 «오지 않을 문자» 를 기다리게 했습니다** — 「선생님 1:1 평가 후 최종 레벨·확정 교재가 학부모님 문자로 안내됩니다」인데 그 학생에게는 선생님 단계가 없습니다. 2026-09-21 에 상태(`ai_done`)와 화면 넷(관리자·학부모·강사·티켓)은 갈랐는데 **정작 학생이 보는 화면**이 그대로였습니다 — **«구분» 을 만들면 그 사실을 보는 화면을 «전부» 세세요.** ✅ 서버가 응답에 `track` 을 실어 화면이 갈라 말합니다(⛔ `live_count` 까지 싣지 말 것 — 화면이 쓸 일이 없습니다). ⛔ `live_ai`·`unknown` 은 **한 글자도 바꾸지 마세요** — 로그인 없이 본 경우가 `unknown` 이라 «모르면 예전 동작» 이어야 합니다(짝으로 검사). 🪤 **그 부정 검사를 «원문» 으로 하면 뚫립니다** — 「발음 평가`</b>`까지 합쳐」처럼 **사이에 태그가 끼면** `[^<]*` 류가 못 넘어 **거짓 문구를 되돌려도 통과**합니다(2026-09-22 변이 Ⓖ 실측 0건 검출). **태그를 벗긴 «사람이 읽는 글자»** 로 판정하세요(「부정 검사는 주석을 벗긴 사본으로」의 HTML 판). ✅ 감시는 `leveltest_track_split_harness` ⑦절(분기를 오려 내 **가짜 DOM 으로 실제로 돌려** «무슨 글자가 들어가는가» 를 답으로 봅니다 — 「그 글자가 있는가」로 물으면 `if (false && …)` 한 글자에 뚫립니다. 변이 4종 전부 실제 FAIL). |
| 학습·게임 화면에서 **정답을 맞혀야만 다음으로** 갈 수 있게 만들 때 | 🔴 **못 쓰는 학생은 그 자리에 갇힙니다 — 그리고 화면은 «고장» 이라고도 말해 주지 않습니다.** 2026-09-21 사장님 「못 맞추면 다음으로 너머가는 버튼 만들어서 적당한 위치에 놓아줘」 — 재 보니 «버튼이 없는» 정도가 아니었습니다: 장면 탐험(`student-game-scene-quest.html`)의 「다음 장면 →」은 `award()` 가 도는 순간에만 열리고 `award()` 는 `D.check()` 가 `correct` 를 줄 때만 불립니다 ⟹ **정답을 직접 입력해야만** 열립니다. 「정답 배우기」는 답을 보여 줄 뿐이고 그 자리 문구가 「직접 입력해 문을 열어 보세요」 — **문을 안 엽니다.** ⟹ 철자를 못 쓰는 학생은 **새로고침 말고 나갈 길이 없었습니다.** ⚠️ **에러가 안 납니다** — 화면도 코드도 정상이고 틀린 것은 «나갈 수 있는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ **나가는 길을 그 화면 «안» 에** 두세요(웜업 오버레이 항목의 형제) — 여기서는 힌트·정답 배우기와 **같은 줄**에 「모르겠어요 · 넘어가기 →」. ⛔ **`award()` 를 재사용하지 마세요** — 「정답!」·문 열림·🔓 가 딸려 와 **거짓 축하**가 됩니다. ⛔ 「정답 배우기」가 곧바로 문을 열게 하지도 마세요(쓰기 연습이 통째로 사라집니다). ⛔ 「N번 틀려야 버튼이 나온다」도 안 됩니다 — **틀린 답조차 못 쓰는 학생**(철자를 아예 모름)이 그대로 갇혀 고치려던 문제가 절반 남습니다. ✅ 건너뛴 것은 **점수 0 · 복습 대상 · 문은 안 열림 · 화면이 「넘어갔어요」라고 사실대로** — 결과·선생님 요약에서도 「다시 연습」과 **갈라서** 적으세요(뭉치면 「힌트 쓰고 맞힘」과 「아예 못 씀」이 한 글자가 됩니다). 🔴 **그리고 그 순간 «전부 건너뛴» 경로가 처음으로 닿습니다** — 결과 화면이 하나도 못 맞힌 학생에게 「보물문이 열렸어요!」·「보고 **쓰고** 끝까지」라고 말하고 있었습니다(「상한을 조이는 변경은 그 실패 경로를 처음으로 닿게 만듭니다」의 형제). ⛔ 그렇다고 「안 열렸어요」처럼 **벌주는 말**로 바꾸지 마세요 — 끝까지 온 것은 사실입니다(「끝까지 왔어요!」). ⚠️ 그 제목에 `data-ko`/`data-en` 이 달려 있으면 **속성까지 함께 갱신**하세요(안 하면 🌐 에 옛 글자가 되살아납니다). ⚠️ **허브 적립을 함께 보세요** — 전부 건너뛰어도 `mangoi-game-complete` 가 나가 스트릭·퀘스트 해금이 붙습니다(이 게임은 포인트 API 가 0곳이라 **돈은 안 걸립니다**). 막을지는 **사람이 정할 일**. 감시는 `test-harness/scene_quest_harness.mjs`(가짜 DOM 으로 **실제 핸들러를 돌립니다** — 변이 20종 전부 실제 FAIL)와 `manual/scene-quest-skip-browser.mjs`(브라우저 38종 — 자동으로 안 돕니다, 사람이 부릅니다). 📄 `docs/작업기록/260921_장면탐험_못맞혀도_넘어가기.md` |
| 「**두 번 눌러도 안 변한다**」를 **원리상 안 변하는 값**으로 잴 때 | 🔴 **그 검사는 아무것도 안 봅니다 — 그런데 이름은 «재진입을 막는다» 라고 말합니다.** 2026-09-21 실측: 건너뛰기의 재진입 가드(`if(passed||!active||paused)return;`)를 **`if(false)` 로 통째로 죽여도** 하니스가 **exit 0** 이었습니다. `'double skip changes nothing'` 이 재던 것이 «**점수**» 하나였는데 **건너뛰기는 애초에 점수를 안 주므로** 그 값은 어떤 변이에도 못 움직입니다. ⟹ 진짜 피해는 «기록이 두 번 실리는» 것이었습니다(실측: 결과 목록 5 → **7행** · 선생님 요약 5 → **8줄** = 같은 장면이 「넘어감」으로 두 번). ✅ **«그 조작이 실제로 바꾸는 값»** 으로 물으세요 — 여기서는 「결과 목록 행수 == 장면 수」·「요약 줄수 == 장면 수」. 🪤 **그 검사를 넣어도 «그 상황에 닿지 않으면» 여전히 헛돕니다** — 같은 장면을 두 번 건너뛰는 시나리오가 **끝까지 가야** 결과 목록이 그려집니다(중간에서 멈추면 `passed` 가드만 뺀 변이가 그대로 통과했습니다 — 실측). ✅ 그리고 **가드의 축마다 따로** 변이하세요(`passed`·`paused`·`active`를 각각) — 한꺼번에 죽이면 «먼저 걸리는 축» 하나만 확인하고 나머지는 안 검사됩니다. ⚠️ 그때 **이미 다른 겹이 막고 있는 축은 «등가 변이»** 라 원리상 안 잡힙니다(여기서는 `active` — 결과 화면에서는 `passed` 가 먼저 막습니다). **못 잡았다고 구멍이 아니라 «관측 차이가 없다» 는 뜻**이니 그렇게 적으세요. 🔴 같은 뿌리의 형제 — **짝 검사에서 «한쪽만» 보는 것**: 언어 라벨을 `data-ko` 만 재고 있었는데 `data-en` 을 안 고치면 **EN 학생 화면에서만** 「The treasure gate is open!」(거짓 축하)이 되살아나고 `data-en` 이 없는 요소는 `innerHTML = null` 로 **본문이 통째로 빈칸**이 됩니다(실측 exit 0). **두 언어를 짝으로** 재세요 |
| **진행률 띠**(«얼마나 남았는지»)를 여러 화면에 «한 벌로» 얹을 때 | 🔴 **세 가지가 동시에 «화면이 거짓말하는» 쪽으로 무너집니다** — 2026-09-22 진행 띠(#1074)를 배포한 직후 함정 대조가 넷을 찾아 전부 재현됐고 #1077 로 고쳤습니다. **①  진행을 «알려 올 통로» 가 없는 화면에 띠를 그림** — 낚시(`english-mastery-suite`)·말하기 퀴즈(`speaking-quiz`)·장면 탐험대(`scene-quest`) 셋은 `js/game-track.js` 를 **안 싣습니다**(전수 grep 0건) ⟹ 진행 메시지가 **원리상 0건**이라 띠가 게임 내내 0 에 멎어 있다가 끝에 갑자기 가득 찹니다 = 화면이 «측정한 척» 합니다. ⚠️ 하필 #1074 커밋 메시지는 「그 셋은 이번엔 안 붙였습니다」라고 **정반대**를 적었는데 표에는 `scenequest:12`·`fish:5` 가 있었고 `speaking` 은 기본값 10 으로 떨어져 붙고 있었습니다(이 파일이 네 번 사고를 낸 「적어 둔 것 ≠ 사실」 그 형태). ✅ **손으로 적은 목록을 믿지 마세요** — 하니스가 registry 에서 그 게임의 파일 이름을 읽어 **그 파일을 실제로 열어** 통로가 있는지 기계로 대조합니다(⑧절). ⛔ 통로가 없으면 **0 으로 두어 아예 안 그립니다**(모르는 것을 지어내지 않습니다 — 「측정할 수 없는 값을 그럴듯하게 채우고 싶을 때」와 같은 뿌리). **②  분자와 분모의 «단위» 가 달랐습니다** — 표에 `spacemonster:{n:6, by:'seen'}` 를 적으며 근거로 삼은 게임 HUD 의 「1/6」은 문제 수가 아니라 **LEVEL** 이었고(`<div class="label">LEVEL</div>`), `by:'seen'` 의 seen 은 **«감싼 fetch 가 몇 번 불렸나»** 였습니다. 우주 괴물은 문장 하나에 hit + shadow 를 두 번 부르므로 **문장 3개에서 6/6 「다 했어요!」**(게다가 중복 방지 게이트가 `correct` 만 봐서 오답만 나오는 동안은 메시지가 아예 안 나갔습니다). ✅ 그 축을 없애고 단위를 **«맞힌 개수» 하나**로 통일하세요 — 학생이 게임을 옮겨도 숫자의 뜻이 안 바뀌는 것이 이 띠의 존재 이유입니다. ⛔ **화면 HUD 의 숫자를 «베껴 적지» 마세요**(그 숫자가 무엇인지 그 파일에서 확인할 것). ℹ️ 목표를 못 채운 채 게임이 먼저 끝나면 완료 신호가 띠를 채우므로 중간에서 안 멎습니다. **③  0 일 때 «감춘다» 가 안 먹었습니다** — `box.hidden = true` 를 썼는데 `.game-progress{display:flex}` 가 브라우저 기본 `[hidden]{display:none}` 을 «작성자 > UA» 로 이겼고, 채움은 폭이 미설정이라 block 자식이 부모 폭을 100% 먹어 **아바타 키우기에서 시작하자마자 «초록 가득»**(= 「다 했어요」)이 됐습니다. [잰 것 — 크로미움 390×844] `hidden` 속성은 true 인데 computed `display:flex` · 높이 20px · 채움 **358/358 = 1.000** · 글자는 빈 문자열. ✅ `[hidden]{display:none !important}` **와** «숨길 때 채움 폭을 0% 로 되돌리기» **두 겹**으로. ⚠️ 제 브라우저 검사가 **`hidden` «속성» 만 재고 통과**시켰습니다 — 「있다」와 「보인다」는 다른 값이니 **`getComputedStyle`** 로 재세요(이 표의 「`el.hidden = true` 인데 그대로 보임」을 읽고도 밟았습니다). 📌 **곁가지** — 🌐 를 눌러도 띠 글자가 «다음 정답이 올 때까지» 옛 언어였습니다 → 토글에서 다시 그립니다. ⛔ 그때 `data-ko`/`data-en` 을 쓰지 마세요(두 엔진이 `textContent` 를 갈아끼워 안쪽 `<b>` 가 DOM 에서 사라집니다). ⚠️ **문자열 하니스로는 ①③ 을 못 잡습니다** — 표도 값도 다 «있고» 틀린 것은 «통로가 있는가»·«보이는가» 뿐입니다. 감시는 `test-harness/game_progress_bar_harness.mjs`(43종 — ⑧ registry 를 열어 기계 대조 · ⑨ `[hidden]` 이 실제로 먹는가 · ⑩ 🌐. 변이 **7종** 전부 실제 FAIL — 통로 없는 게임에 목표 다시 넣기 · `[hidden]` 삭제 · 채움 0% 제거 · `d.seen` 되돌리기 · `by:'seen'` 되살리기 · 언어 다시그리기 제거 · **«전부 0» 으로 띠를 없애기(짝)**)와 `test-harness/manual/game-progress-bar-browser.mjs`(브라우저 24종 — **자동으로 안 돕니다, 사람이 부릅니다.** ⛔ 「되돌리면 재현된다」를 적을 때 **몇 겹을 지웠는지 함께** 적으세요 — [잰 것 — 2026-09-22 재현] `[hidden]` 규칙 **한 겹만** 지우면 **FAIL 7 · ratio 0.017**(두 번째 겹이 «가득» 은 막아 줍니다)이고, **두 겹을 «둘 다»** 지워야 **ratio 1.000(358/358)** 로 그 화면이 그대로 재현됩니다). ⚠️ 검사 범위를 «길이» 로 자르지 마세요 — ⑤절이 리스너를 `slice(1400)` 으로 잘라 462자가 새어 나갔습니다(중괄호 짝으로). 📄 `docs/작업기록/260922_게임진행띠_긴급수리.md` |
| 시험·퀴즈에서 **«정답을 보여주는»** 기능을 만들 때 (해설·오답노트·다시 보기) | 🔴 **그 기능은 «정답을 숨기는» 기존 설계의 정반대라, 아무 제약 없이 실으면 문제은행이 통째로 새는 길이 열립니다.** 2026-09-22 레벨테스트 실측: `/api/leveltest/questions` 는 정답(`a`)을 «일부러» 숨기는데(그 자리 주석이 「문항은행과 채점을 서버가 소유 — 클라이언트 조작 불가」로 못 박음) 채점 경로 `/api/leveltest/diagnose` 에 정답을 그냥 실으면 **답을 하나도 안 내고 한 번 불러 24문항 정답을 통째로** 받습니다 — ⚠️ **두 경로 다 무인증 공개**입니다(`src/index.ts` 허용목록). ✅ 창을 **세 겹**으로 좁히세요(정본 `src/leveltest-review.ts`) — ① 학생이 **실제로 고른** 문항만(안 푼 문항은 해설할 것도 없습니다) ② 그 값이 **보기 범위 안의 «정수»** 일 때만(`99`·`-1`·`1.5`·`x` 를 24개 보내 훑는 길을 막습니다) ③ **틀린 것만**(맞힌 문항의 정답은 본인이 고른 값이라 새 정보가 아닙니다). ⚠️ **0(A번)은 정당한 답입니다** — falsy 로 거르면 A를 고른 오답이 통째로 사라집니다(실측 18건). ⚠️ **그래도 「전부 답하면 틀린 것의 정답은 온다」는 남습니다 — 그것이 기능 자체입니다.** 막으려면 기능을 없애야 하니 **좁힌 만큼만 적고 완료형으로 「안전하다」고 쓰지 마세요.** ⛔ 그 남는 위험을 **«시험을 끝까지 봐야 하니 비용이 든다» 로 적지 마세요 — 틀렸습니다.** [잰 것 — 2026-09-22, 정본을 실제로 돌림] **24문항에 «전부 0번» 을 채운 본문으로 단 한 번** 부르면 정답키가 **100% 복원**됩니다(review 로 오는 18건은 `answer` 가 그대로 오고, 안 오는 6건은 «내가 고른 0 이 정답» 이라 저절로 확정). ⟹ 세 겹이 막는 것은 «**빈** 본문 1회» 이고 «**0 채운** 본문 1회» 는 그대로 통과합니다 — 자동 호출자에게 그 둘은 비용 차이가 없으니 이 변경은 유출을 «막은» 것이 아니라 **난이도를 낮췄습니다**(옛 경로는 `breakdown` 뿐이라 여러 번 필요했습니다). ℹ️ 완충은 `applyPlacementLevel` 이 «이미 레벨이 있으면 안 덮는» 것뿐이고, ⚠️ AI 학습만 하는 학생(`ai_done`)은 선생님 확정 단계가 없으며 **속도 제한도 없습니다** — 추가 제한은 **사람이 정할 일**(2026-09-22 미결). ⛔ **해설을 LLM 으로 만들지 마세요** — 학생이 그대로 배우는 문장이라 결정론(손으로 쓴 표)이어야 합니다(이 저장소의 반복 실측 「지시만으로는 안 지켜진다」). ⛔ 해설이 비면 **지어내지 말고** 정답만 말하세요. ⛔ **화면에서 정답을 다시 계산하지 마세요** — 정답은 문항 응답에 애초에 없습니다(화면은 «그리기만»). ⛔ **문항은행 타입을 두 곳에 적지 마세요** — 해설 칸을 늘렸는데 한쪽만 아는 상태가 조용히 생깁니다(정본 타입을 import). ⚠️ **0건에는 뜻이 «둘»** 입니다(다 맞힘 / 답을 고른 문항이 없음) — 한 문장으로 뭉치면 중간에 멈춘 학생에게 「전부 맞혔다」고 거짓말합니다. `correct === total` 로 가르세요. ⚠️ 그리고 **서버가 그 칸을 «안 보낸» 응답**(옛 서버·배포 중)에는 **아무것도 그리지 마세요** — 0건을 «만점» 으로 읽는 같은 거짓말입니다. ✅ 화면에 얹을 때는 **기존 CTA «아래»** 에 두세요 — 위에 끼우면 틀린 문항이 20개일 때 그 버튼이 통째로 스크롤 밖으로 밀립니다(대신 요약 밑에 「👇 아래에 N문항…」 한 줄로 길을 알려 줍니다). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀리는 것은 «무엇이 실리는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 판정을 **순수 함수로 빼서 실제로 돌리고**, 문항은행도 ⛔ 하니스에 베끼지 말고 **소스에서 오려 내세요**(문항이 바뀌면 그 복제가 조용히 낡습니다). ✅ 「싣는다」 옆에 **「안 싣는다」를 짝으로** — 앞만 보면 «전부 싣기»(유출), 뒤만 보면 «전부 안 싣기»(기능이 죽음)가 통과합니다. 감시는 `test-harness/leveltest_review_harness.mjs`(62종 — 변이 **18종** 전부 실제 FAIL)와 `manual/leveltest-review-browser.mjs`(브라우저 24종 — 자동으로 안 돕니다, 사람이 부릅니다). 🚤 **그 배선 검사를 «글자» 로 묻지 마세요 — `if (false) renderReview(d);` 한 글자에 뚫립니다**(2026-09-22 함정 대조 실측: 화면에 해설이 **한 줄도 안 나오는데** 자동 하니스는 **PASS 54 / FAIL 0 · exit 0**, 브라우저 하니스만 17건 FAIL — 그런데 `manual/` 은 게이트가 **안 물어 갑니다**). ✅ **`showResult` 를 오려 내 «가짜 renderReview» 로 실제로 돌려** 불렸는지 묻고, **«막으면 안 불린다» 를 짝으로** 두세요(짝이 없으면 그 검사가 늘 «불렸다» 로 헛돌아도 모릅니다). ⚠️ 그리고 **「변이 N종 전부 FAIL」이라고 적기 전에 그 N 에 «조건 뒤집기» 가 들어 있는지 세어 보세요** — 제 15종은 전부 «삭제·치환» 이라 이 구멍을 한 번도 안 쟀습니다. 🪤 그 검사를 만들며 **세 번 헛돌았습니다** — ⓐ 「`renderReview(d)` 가 있는가」로 호출을 물었더니 **«함수 선언» 이 잡혀** 호출을 통째로 지운 변이가 그대로 통과했습니다(이 표에 이미 두 번 적힌 함정을 **세 번째로** 밟음 — 부르는 함수 몸통을 잘라 «그 안에서» 묻고 「선언은 정확히 하나」를 짝으로) ⓑ 「그 호출이 `try` 안인가」를 «첫 번째 이름 위치» 로 물었더니 **`import` 줄이 잡혀** 늘 참이었습니다(try 블록을 중괄호 짝으로 잘라 «호출 위치가 그 범위 안인가» 로) ⓒ **중괄호 짝 자르기의 «꺾쇠(`<`/`>`) 세기» 가 HTML 을 조립하는 JS 함수에서 엉뚱한 `{` 를 잡습니다** — `'<div …>'` 문자열 때문에 짝이 안 맞아 **멀쩡한 코드가 «r is not defined» 로 거짓 실패**합니다. TS 와 달리 **JS 선언에는 반환 타입이 없으니 «선언 뒤 첫 중괄호» 부터 짝만 맞추세요.** 📄 `docs/작업기록/260922_레벨테스트_틀린문제_정답과해설.md` |
| «게이트를 끄면 달라진다» 로 재는 검사가 **포화하면 조용히 뜻을 잃음** | 🔴 **차이로 묻는 검사는 «그 축이 거의 다 채워지는» 날 숫자가 같아져 아무것도 안 재게 됩니다 — 그런데 초록불입니다.** 2026-09-22 실측: 낱말 사진 근거 게이트에서 「기능어를 빼는 규칙을 끄면 «더 많은 낱말» 이 붙는다」를 재고 있었는데, 사진이 늘어 교재 낱말을 거의 다 덮자 **4,527 → 4,527** 로 차이가 0이 되어 **그 규칙을 통째로 지워도 통과**했습니다(그 규칙이 지키는 것은 「thing→the·ones→on 처럼 `variants()` 가 만들어 낸 기능어가 설명마다 있으니 모든 사진에 붙는」 사고입니다). ✅ **포화와 무관한 축으로 옮겨 적으세요** — 여기서는 «몇 낱말» 이 아니라 **«몇 사진»** 으로 세니 정상 225 → 끄면 2,584 로 갈라집니다. ⛔ 문턱을 낮춰서 풀지 마세요(그 축이 이미 포화라 다음에 또 같아집니다). ✅ 반례는 **손으로 적지 말고 자동 생성**하세요(`variants()` 가 기능어를 만드는 교재 낱말을 전수로 골라냄) — 손으로 적으면 표가 자라도 그대로 낡습니다. ⛔ 그때 **「기능어 «자신» 은 절대 안 잡힌다」로 묻지 마세요** — 사실이 아니고(실측 27개: `use` 가 used·using 으로 변형돼 잡힘) 결함도 아닙니다. 그렇게 적으면 **멀쩡한 코드가 빨간불**이 됩니다. 📜 같은 줄기: 이 표의 「하니스가 «객체 모양» 을 정규식으로 못 박아 두어」 — 그쪽은 «모양», 이쪽은 «차이» 입니다 |
| 같은 표를 **여러 파일로 나눌** 때 (`…-plan.json` · `…-plan-2.json`) | 🔴 **읽는 곳이 둘(빌드·검사)인데 한쪽만 새 파일을 더하면 «빌드는 붙이는데 검사는 그 사진을 모르는» 상태가 되고 둘 다 조용합니다.** ✅ **목록을 만드는 규칙을 정본 한 곳**(`wordPlanFiles`, `scripts/scene-picture-evidence.mjs`)에 두고 빌드·하니스가 **같은 함수**를 부르게 하세요. ⛔ 어느 쪽에도 파일 이름을 손으로 적지 마세요. ✅ 감시는 **「빌드가 그 함수를 실제로 부르는가」·「이름을 손으로 안 적었는가」·「표가 둘 이상인가」(전제)** 를 짝으로 — 전제가 없으면 표가 하나로 되돌아갈 때 그 검사들이 조용히 뜻을 잃습니다. ⚠️ 부정 검사(«이름을 손으로 적지 않았다»)는 **주석을 벗겨 낸 사본**으로 판정하세요(설명 주석에 그 파일 이름이 들어갑니다). ⚠️ 그리고 그 벗기기가 **파일을 통째로 먹지 않았는지** 한 줄로 확인하세요(2장 「블록주석을 정규식 한 줄로 지웠더니」). 🔴 **덤 — 표를 더하면 «전체를 모아 한 번 계산하는» 값이 바뀝니다.** 그 근거 게이트는 «설명 80% 이상에 나오는 낱말»(껍데기)을 모집단 전체에서 세는데, [잰 것 — 2026-09-22] 표 하나일 때 **15개** · 둘을 합치니 **16개**(`a` 가 늘어남)였습니다. 여기서는 `a` 가 이미 기능어라 판정이 안 바뀌었지만, ⚠️ **껍데기가 «줄어드는» 쪽이 위험합니다** — 그 순간 그 낱말이 **이미 만든 사진 «전부» 의 근거로 되살아납니다**(= 「natural 이 모든 그림에 붙는」 사고). **새 표를 더할 때마다 껍데기 목록을 다시 재고 늘거나 준 낱말이 교재 낱말인지 확인하세요.** 📄 `docs/scene-curriculum-media/README-낱말사진.md` |
| 관리자 화면 **입력칸에 값을 넣었는데 «안 보임»** — 코드·기능은 멀쩡 | 🔴 **밝기 보정 페인터가 «배경만» 밝히고 `<input>`·`<select>` 의 글자는 «원리상» 안 고칩니다.** `js/adm-light-surfaces.js` 의 `lightenSurface()` 는 어두운 인라인 배경을 **인라인 `background-color:… !important`** 로 희게 칠하는데, 짝인 `fixText()` 는 첫 줄이 `if (!el.firstChild) return;` 이라 **자식 텍스트 노드가 없는 void 요소를 한 번도 안 봅니다**(`<select>` 도 자식이 `<option>` = 요소 노드라 같습니다). ⟹ 배경만 희어지고 글자는 어두운 테마용 `#e2e8f0` 그대로 → **흰 바탕에 흰 글자.** 📊 [잰 것 — 2026-09-22, `admin/student.html` 두 폼 7칸] 합성 대비 **1.14**(글자 `rgb(226,232,240)` · 배경 `rgb(244,246,251)`). ⚠️ **값은 들어 있습니다** — 그래서 사장님께는 「버튼이 작동 안 한다」로 보입니다(실측: 버튼도 PATCH·DELETE 도 정상이었습니다). ✅ **가르는 법은 한 줄**입니다 — `el.style.getPropertyPriority('color')` 가 `'important'` 면 페인터, 비면 CSS 싸움(「관리자 카드 안 글자에 색을 줬는데」 항목). 여기서는 `document.styleSheets` 를 훑어 매치되는 규칙이 **0건**이라 CSS 가 아님이 확정됐습니다. ✅ 고침은 **그 화면 입력칸을 처음부터 밝은 값**으로(`background:#ffffff; color:#101828; border:1px solid #cbd5e1`). ⛔ **페인터를 «input 도 보게» 고쳐서 풀지 마세요** — 그 파일은 관리자 화면 **전체**가 싣고, 고치면 그것을 부르는 HTML 의 `?v=` 가 전부 걸립니다. ⚠️ **문자열 하니스로는 원리상 못 잡습니다** — 색도 배경도 다 «있고» 틀린 것은 «둘이 같다» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. ✅ 검사는 **반투명 층을 합성해 대비를 재고**(2장 「어두운 화면에서 «글자가 읽히나»」), 「읽힌다」 옆에 **「버튼·API 는 그대로 동작한다」를 짝으로** 두세요 — 짝이 없으면 «입력칸을 통째로 지우기» 도 통과합니다. 감시는 `student_sched_calendar_harness` ①절(인라인 색 전수)과 `manual/student-schedule-calendar-browser.mjs` ①절(실제 대비) |
| 캘린더·목록이 **«반복 수업» 만 통째로 안 그림** (1회차는 보이는데) | 🔴 **`class_schedules.day_of_week` 표기가 «세 벌» 인데 읽는 쪽이 한 벌만 압니다 — 그리고 조용히 `continue` 합니다.** [잰 것 — 2026-09-22] 저장되는 값: **예약 등록 폼 경로 = 숫자 `'0'`~`'6'`**(`api-admin.ts` POST 의 `String(d)` · 화면 체크박스 `value="0"…"6"` — `/api/class/sessions/today` 가 그 형식을 전제) · **일정변경(PATCH)·운영 D1 정본 = `'Wed'`·`'Fri'`**(같은 파일 `normDow`) · **AI 명령 = `'mon'` 소문자**. 그런데 학생 상세 주간 캘린더는 `dowToIdx = {sun:0, mon:1, …}` 소문자 세 글자만 알아 **앞의 둘이 전부 `undefined` → 반복 수업이 한 칸도 안 그려졌습니다.** ⚠️ **1회차(`scheduled_date`)는 멀쩡히 보여서** 「가끔 되는데?」로 읽힙니다. ⚠️ 에러도 경고도 안 납니다. ✅ 판정을 **정본 하나**로 모으세요(`mgsDowIdx`·`mgsSchedHitsDate`, `admin/student.html`) — 숫자·영문·한글·쉼표 나열을 다 받고 **모르면 `null`**(지어내지 않음). 🔴 **날짜가 요일을 이겨야 합니다** — `sessions/today` 가 `if (scheduled_date) … else if (day_of_week)` 라 화면도 같아야 합니다. 요일을 먼저 보면 두 칸이 다 찬 행(`js/adm-q6.js` 가 날짜 행에도 `day_of_week` 를 보냅니다)이 **엉뚱한 요일에 «매주»** 그려집니다. ⚠️ 날짜 비교는 **로컬 `YYYY-MM-DD` 문자열**로 하세요 — `new Date('2026-09-23')` 은 UTC 자정이라 한국 시간대에서 하루 밀립니다(`toDateString()` 비교도 그 함정을 탑니다). ⛔ **옛 표에 값을 덧붙여서 풀지 마세요** — 표가 화면 «안» 에 남아 다른 뷰가 또 복제합니다. ⛔ **서버에서 표기를 통일하지도 마세요** — 운영 D1 에 이미 세 표기가 섞여 있어 마이그레이션이 걸린 별건입니다(읽는 쪽을 넓히는 것이 안전한 방향). 📌 **덧붙임 — 같은 화면의 «월간» 뷰에는 그 레이어가 아예 없었습니다**(`renderDSchedMonth` 에 `aiSchedules` 가 한 번도 안 나왔습니다) ⟹ 월간에서는 1회차조차 «구조적으로» 한 건도 안 보였습니다. **뷰가 둘이면 «둘 다» 그리는지 세어 보세요.** ⛔ 판정을 월간에 다시 적지 마세요. 📌 그리고 **등록 성공 뒤 `loadDStudentSchedule()` 을 안 불러** 「✅ 등록됐습니다」를 보고 가도 새로고침 전까지 그대로였습니다(일정변경 경로에는 원래 있었습니다) — «저장했다» 와 «화면이 그것을 다시 읽는다» 는 다른 일입니다. ⚠️ **문자열 하니스로는 못 잡습니다** — 표도 값도 다 «있고» 틀린 것은 «무엇이 그려지는가» 뿐입니다. ✅ 검사는 정본을 **오려 내 실제로 돌리고**, 「그린다」 옆에 **「취소된 예약·격자 밖은 안 그린다」를 짝으로** 두세요. 🪤 그때 카드를 **`innerHTML` 에서 시각 문자열로 세지 마세요** — 주간 격자의 «시간 눈금»(`>18:00<`)이 걸려 「취소분이 안 그려진다」가 거짓 FAIL 이 나고 거꾸로 「16:00 이 보인다」는 **저절로 참**이 됩니다(둘 다 실측). **`div[title^=…]` 로 DOM 에서** 세세요. 🪤 그리고 자리를 **`style.left` 문자열로 묻지 마세요** — 브라우저가 calc 를 정규화해 `calc(71.4286% + 19.1429px)` 로 돌려줍니다(`* 5 / 7` 을 기대했다가 거짓 FAIL). **카드 중앙 x 가 어느 요일 칸 안인가**로. 🪤 **「그 정본을 부르는가」만 물으면 `if (false) continue;` 한 글자에 뚫립니다**(실측: 자동 0건 검출) — **«항상 참/거짓» 인 죽은 조건이 없다**를 짝으로 두세요(⛔ `if (!정본(` 이라는 «식 모양» 을 글자로 못 박으면 정당한 리팩터가 빨간불). 🔴 **그런데 그 «죽은 조건» 검사로도 «가드를 «답을 안 쓰게»» 만드는 변이를 못 잡습니다** — `if (!정본(…)) continue;` 를 **`정본(…);`** 로 바꾸면 이름도 «있고» 죽은 조건도 «없습니다»(실측: 주간 카드 **2장 → 21장**인데 PASS 38 / FAIL 0). ✅ **가짜 DOM 에 격자를 실제로 그려 카드를 세세요** — 「그려야 할 것은 그린다(5장·수 4·월 1)」 옆에 **「취소·격자 밖은 안 그린다」를 짝으로**. 🪤 그때도 «없는가» 를 `innerHTML` 전체로 묻지 말고 **카드 title 만** 보세요(위 시간 눈금 함정을 그 자리에서 다시 밟았습니다). 🔴 **그리고 「날짜를 로컬로 읽는다」 검사는 UTC 에서 «원리상» 안 깨집니다** — 이 컨테이너도 CI(`ci.yml` 에 TZ 설정 없음)도 기본이 UTC 라 `toISOString().slice(0,10)` 으로 되돌려도 답이 같습니다(PASS 38 / FAIL 0). 하니스 맨 위에 **`process.env.TZ = 'Asia/Seoul';` 를 «Date 를 쓰기 전에»** 두세요(그러면 5건이 실제로 FAIL). 🔴 **새로고침 경로는 «셋» 입니다**(등록·일정변경·취소) — 2026-09-22 에 등록만 고치고 **취소를 빠뜨려** 제보의 절반이 그대로 남았습니다. `loadAiSchedules()` 는 목록 HTML 만 갈아끼우고 `_dSchedState.aiSchedules` 를 안 건드리므로 **둘 다** 불러야 캘린더가 따라옵니다. ⛔ 검사에서 두 경로만 못 박지 마세요 — 다음 사람에게는 «그 축이 덮여 있는» 것으로 보입니다(⛔ `slice(i, i+700)` 처럼 길이로 자르지 말고 중괄호 짝으로). ⛔ 그리고 **함수를 통째로 보지 마세요** — 호출을 지우지 않고 **`catch` 로 «옮기는»** 변이가 통과합니다(성공 갈래 «안» 만 자를 것). ⚠️ **요일 구분자는 서버 정본 `admDowMatches`(`api-admin.ts`)와 «같은 집합»**(`/[,\s/·]+/`)이라야 합니다 — 쉼표만 보면 `'1 3 5'`·`'Mon Thu'`·`'월/수'`·`'화·목'` 이 조용히 한 칸도 안 그려집니다. 감시는 `test-harness/student_sched_calendar_harness.mjs`(60종)와 `manual/student-schedule-calendar-browser.mjs`(23종 — 자동으로 안 돕니다, 사람이 부릅니다). 변이 **16종** 전부 실제 FAIL. 📄 `docs/작업기록/260922_학생상세_일정변경_취소_캘린더_안보임.md` |
| 강사를 **이름 글자로** 보내 이었더니 **남의 강사**가 붙음 (자유 입력 칸) | 🔴 **`name = ? OR name LIKE ? LIMIT 1` 은 «정확히 쳐도» 틀릴 수 있습니다** — `ORDER BY` 가 없어 일치하는 여럿 행 중 **아무거나** 하나를 줍니다. [잰 것 — 2026-09-22 운영 D1] `WHERE name='FAR' OR name LIKE '%FAR%' LIMIT 1` → **id 3 `HT FARRAH`(퇴사)**. 그런데 `FAR` 은 **id 22 재직 강사**입니다 — 이 표의 「퇴사 강사가 드롭다운에 그대로 떠서 잘못 이어짐」(2026-08-26, 스케줄 16건)의 그 쌍이 그대로 살아 있었습니다. ⚠️ **에러가 안 납니다** — 등록은 «성공» 이고 틀린 것은 «누가 붙었는가» 뿐입니다. ✅ 사람이 고르는 칸은 **드롭다운으로 두고 `teacher_id`(id) 를 보내세요** — 그러면 그 짐작을 아예 안 탑니다. ⛔ **자유 입력 폴백의 `LIKE` 경로를 지워서 풀지 마세요** — 목록을 못 받는 날 강사 칸이 통째로 막힙니다. 🔴 **그런데 `teacher_id` 만 보내면 서버의 `teacherName` 이 `null` 로 남아 «일회성 예약 휴가 충돌 검사»가 통째로 건너뛰어집니다**(자유 입력 경로는 받던 검사). ✅ id 가 오면 이름을 **`WHERE id = ?` 로 되찾으세요**. ⛔ 못 읽었을 때 **`body.teacher_name` 으로 떨어뜨리지 마세요** — 화면이 보낸 글자로 «막는» 검사를 돌리는 것입니다. ⚠️ **그 한 줄이 «다른 화면» 의 검사도 처음으로 켭니다** — `weekly-schedule.html` 은 이미 `teacher_id` 를 보내고 있어, 휴가 행이 생기는 날부터 그쪽 일회성 등록도 `409 teacher_unavailable` 로 막힙니다(그 화면은 `error === 'conflict'` 일 때만 다시 물어 **force 로도 못 뚫습니다**). [잰 것 — 2026-09-22] 휴가 행이 **0건**이라 «오늘은» 안 닿는 것뿐이니 ⛔ **「다른 화면에 영향 0」으로 적지 마세요.** ⚠️ 목록은 **`?include_inactive=1` 로 받으세요** — 그래야 `active` 칸이 내려옵니다. ⛔ 서버에서 걸러 달라고 하지 마세요(거르는 것은 화면 몫). 🚪 **퇴사 강사는 «기본으로 빼되 되돌릴 길을 화면에 두세요»** — 「퇴사 강사도 보기」 체크박스 + 감춘 «N명» 표시 + 체크하면 `<optgroup>` 에 «(퇴사)» 로. ⛔ **영영 빼지 마세요**(사람이 그 강사를 아예 못 고릅니다). ⛔ **서버에서 걸러 달라고 하지 마세요** — 그 체크박스가 조용히 무동작이 됩니다. ⛔ **`adm-teacher-links.js`(2026-09-09)와 «같은» 방식입니다 — 한쪽만 바꾸지 마세요**(두 화면이 서로 다른 말을 하면 다음 사람이 어느 쪽을 믿을지 모릅니다). ⚠️ 다만 그 화면에는 「이미 그 강사에게 이어진 계정은 «절대» 숨기지 않는다」는 예외가 있는데 **여기는 «새로 만드는» 폼이라 그 경우가 없습니다** — 예외를 베껴 오지 마세요. ⚠️ **`active` 가 NULL·빈 값이면 «재직»** 입니다 — `Number(null) === 0` 이라 **null 검사가 «먼저»** 여야 하고, 숨기는 쪽으로 실패하면 멀쩡한 강사가 조용히 사라집니다. ⚠️ **문자열 하니스로는 못 잡습니다** — SQL 도 값도 다 «있고» 틀린 것은 «누가 나오는가» 뿐입니다. ✅ 서버 SQL 은 **소스에서 오려 내 진짜 SQLite 에** 돌리고, 화면 함수는 오려 내 실제로 돌려 «무슨 값이 나가는가» 로 물으세요. 🔴 **그때 «질의가 있는가» 만 물으면 «결과를 안 받아 쓰는» 변이를 못 잡습니다** — `if (t?.name) teacherName = ...` 한 줄을 지우면 휴가 검사가 죽는데 그 검사들은 전부 초록이었습니다(실측). **블록 자체를 오려 내 가짜 `env.DB` 로 돌리세요.** ⚠️ 브라우저 스텁은 **포괄 스텁 «앞» 에** 두세요(뒤에 두면 `{ok:true, items:[]}` 가 이겨 **폴백 경로만** 검사합니다). ⚠️ 변이시험을 셀 때 **실패 표식을 확인하세요** — 하니스마다 다릅니다(`❌` 대 `FAIL`). 제가 `❌` 로 세서 «4종 전부 못 잡음» 으로 나왔는데 실제로는 전부 잡고 있었습니다 — **종료코드와 실패 줄을 함께** 보세요. 🪤 **그리고 가짜 `<select>` 를 «평범한 객체» 로 두면 「다시 그려도 고른 값이 남는가」가 원리상 헛돕니다** — `innerHTML` 을 갈아도 `.value` 가 그대로라 **보존 코드를 지워도 통과**합니다(실측). 진짜 `<select>` 는 options 를 갈아 끼우면 선택이 맨 앞으로 돌아가니 **가짜도 그렇게** 두세요. 감시는 `test-harness/class_teacher_picker_harness.mjs`(71종 — 변이 **22종** 전부 실제 FAIL)와 `manual/student-schedule-calendar-browser.mjs` ⑥절(브라우저 — 자동으로 안 돕니다, 사람이 부릅니다). 📄 `docs/작업기록/260922_담당강사_드롭다운_이름짐작제거.md` |
| 관리자·매니저 화면에서 수업을 **옮겼는데 다음 날 아침이면 시각이 되돌아가 있음** | 🔴 **«옮기는 코드» 가 아니라 «도장» 이 빠진 것입니다 — 카페24 미러가 밤에 자기 값으로 덮습니다.** 미러 sweep(`src/c24-mirror.ts`)은 `UPDATE class_schedules SET start_time=?, duration_min=?, teacher_id=? … WHERE id=? AND source=?` 로 **`source='c24-mirror'` 인 행만** 덮는데, 그것이 곧 «사람이 손대면 그 도장을 바꿔 두어야 한다» 는 뜻입니다(`MIRROR_SOURCE_MANUAL = 'c24-mirror:manual'`). 그런데 **`POST /api/admin/schedule-requests/decide` 가 그 도장을 한 번도 안 찍고 있었습니다**(2026-09-22 발견 — 그 경로는 「수업 연기·변경 요청」 승인이 쓰는 유일한 길입니다). 📊 [잰 것 — 2026-09-22, 운영 D1 SELECT] 앞으로 잡힌 활성 수업 **230건 중 190건(83%)이 미러 행**입니다 🔴 **그런데 «무엇이 되돌아가는가» 를 «추론» 으로 적으면 안 됩니다 — 제가 그렇게 적었다가 함정 대조가 잡았습니다.** 처음에 「옮긴 수업이 밤사이 되돌아간다 · 날짜는 새 날짜인데 시각만 옛날인 «반쪽 되돌림»」이라고 **세 곳(주석·이 표·작업기록)에 완료형으로** 적었는데 **[잰 것 — 2026-09-22, `planMirror` 를 있는 그대로 돌림] 그 일은 일어나지 않습니다.** 덮어쓰는 `update` 갈래는 행이 카페24와 **«같은 (날짜,시각) 자리에 그대로 있을 때만»** 닿습니다(`sameSlot`) — 한 번 옮기면 그 자리를 벗어나 `ok`(새로 만들기)로 갑니다. ✅ **도장이 실제로 막는 것은 «같은 날짜에 머무는» 수정입니다**(실측: 시각만 옮김 → 도장 없으면 `ok`·있으면 **`diverged`**(손 안 대고 사람에게 알림) · 연기 → `already`·**`manual_locked`** · **연기 뒤 카페24가 강사를 바꿈 → 도장 없으면 `update` 로 강사를 덮어씁니다**). 도장은 그대로 넣는 것이 맞고 **이유만 달랐습니다.** 🔴 **그리고 그 도장을 «날짜가 바뀌는 이동» 에 찍으면 오히려 «유령 수업» 이 되살아납니다.** [잰 것 — 진짜 SQLite] 미러의 부분 유니크 인덱스가 `ON class_schedules(notes) WHERE source='c24-mirror'` 라 **도장 찍은 행은 그 밖**입니다 ⟹ 도장이 없으면 야간 미러의 새 INSERT 가 UNIQUE 위반으로 **거절**되는데, 찍으면 **INSERT 가 성공해 옛 날짜에 그 수업이 다시 생깁니다**(2026-09-01 「미러가 «실제로 없는 수업» 을 만듦 — 강사가 20분 헛기다리고 학생 노쇼까지 찍힘」과 **같은 모양**). ✅ 그래서 **날짜가 그대로일 때만** 찍습니다(연기는 날짜가 안 바뀌므로 언제나). ⛔ 연기 갈래에까지 날짜 조건을 붙이지 마세요 — 그 순간 위 «강사 변경 덮어쓰기» 를 다시 못 막습니다. 🟡 **더 나은 길(별건·사람이 정할 일)**: `planMirror` 의 `manual` 찾기가 «(source, uid, **날짜**)» 라 날짜를 옮기면 못 찾습니다 — **`notes`(`c24:<수업번호>`)로 맞추면** 날짜를 옮겨도 도장이 일합니다. 미러의 심장을 고치는 일이라 안 건드렸습니다. ⚠️ **에러가 안 납니다** — 승인도 UPDATE 도 «성공» 이고, 틀린 것은 «내일 아침의 값» 뿐이라 그 자리에서는 아무도 못 봅니다. ✅ 도장은 **«같은 UPDATE 안»** 에서 찍으세요 — 뒤에 따로 `UPDATE … SET source=` 를 붙이면 그 사이에 sweep 이 돌 수 있고, 한 줄이 실패하면 반쪽만 남습니다. ⛔ **모든 행에 도장을 찍지 마세요** — 수강신청이 만든 행(`adm-enroll:*`)의 `source` 를 갈아치우면 그 수업이 «카페24가 만든 것» 으로 둔갑해 나중에 미러가 관리하려 듭니다. **미러 행일 때만**(`=== MIRROR_SOURCE`), 모르면(`source` 가 비면) **안 찍는 쪽으로** 실패하세요. ⛔ 반대로 **`source` 를 통째로 지워서 풀지 마세요** — 그 칸이 「이 행을 누가 만들었나」의 유일한 근거이고, 카페24 미러의 취소 단계(`planMirror`)가 그것으로 자기 행을 셉니다. 🪤 **하니스가 이것을 «원리상» 못 잡는 모양이 있습니다** — SQL 템플릿을 오려 내 진짜 SQLite 에 돌리면 「도장 SQL 이 옳은가」는 정확히 재지만, **«어느 갈래로 가는지 정하는 판정»** 은 한 번도 안 봅니다. 2026-09-22 변이시험 실측: `const _isMirror = …` 를 **`false` 로 바꾸거나**(= 도장을 영영 안 찍음) **`===` 를 `!==` 로 뒤집어도**(= 엉뚱한 행에 찍음) 그 절이 **PASS 43 / FAIL 0** 으로 통과했습니다. ✅ **판정식도 오려 내 실제로 돌리고**(미러면 true · 수강신청 행이면 false · `:manual` 이면 false · 모르면 false 를 **짝으로**), **삼항식 자체를 평가해** «미러일 때 도장 SQL 이 나오는가 · 아니면 도장 없는 SQL 인가» 를 짝으로 물으세요(두 갈래를 맞바꿔도 글자는 그대로입니다). ✅ 그리고 **「날짜가 그대로면 찍는다」 옆에 「날짜가 바뀌면 «안» 찍는다」를 짝으로** 두세요 — 짝이 없으면 «전부 도장» 이 통과해 유령 문이 열립니다(변이 실측). ⚠️ 그때 조건의 **«이름» 을 하니스에 못 박지 마세요** — 이동과 연기가 서로 다른 조건을 쓰므로 «무엇이든 한 이름 ? SQL : SQL» 로 찾고 이름은 **소스에서 읽으세요**. ⚠️ 그때 TypeScript 의 `as any` 를 벗겨야 `new Function` 이 돕니다. ⚠️ 그리고 **«부를 때» 까지 try 로 감싸세요** — 변이가 문법을 깨면 «깔끔한 FAIL» 이 아니라 하니스 크래시가 되어 결과줄조차 안 나옵니다. ✅ 그 전제(«sweep 이 `source=?` 로 자기 행만 덮는다»)도 함께 못 박으세요 — 그것이 바뀌면 이 도장이 뜻을 잃습니다. 감시는 `test-harness/manager_today_reschedule_harness.mjs` ①절 |
| 목록 한 줄에서 **바로 처리되는 버튼**(연기·변경·취소)을 만들 때 | 🔴 **«한 행 = 한 수업» 이 아닐 수 있습니다 — `class_schedules` 의 반복 행 하나는 «매주 전부» 를 뜻합니다.** 그 줄에 취소를 주면 한 번 눌러 **모든 주가 죽고 되돌릴 길이 없습니다**(`schedule_change_requests` 의 승인 경로도 같은 이유로 `isDated` 가 아니면 `applied='recorded'` 로 **기록만** 하고 실제로는 안 옮깁니다). ✅ **«여기서 옮길 수 있는 줄인가» 를 서버가 판정해 내려주세요**(`can_move`) — 화면이 추측하면 화면은 «된다» 고 하고 서버는 조용히 `recorded` 를 돌려줍니다. ⛔ 근거를 `schedule_kind` 로 잡지 마세요 — [잰 것 — 2026-09-22] 그 칸의 실측 값이 `one_off` 2,196 · `dated` 72 · `recurring` 4 로 **셋**이라 `=== 'one_off'` 는 72건을 잘못 막습니다. 승인 경로의 `isDated` 와 **같은 칸(`scheduled_date`)** 이어야 합니다. ✅ 못 주는 줄에는 **«왜 안 되는지» 를 보이는 글자로**(「매주 반복 — 시간표에서」) — 버튼만 없으면 «고장» 으로 읽힙니다. ⛔ **권한이 없는 계정에 «눌러도 403 나는 버튼» 을 주지 마세요** — 취소(`DELETE /api/admin/class-schedules/:id`)는 `isAgencyAllowedApi` 에 없어 지사·대리점은 403 입니다. 그 계정에는 버튼을 빼고 **「수업 «취소» 는 본사 계정에서 합니다」** 라고 적으세요. ⚠️ 그 게이트는 **«막는 쪽으로» 실패**해야 합니다(`can_move` 를 모르면 안 줌). 🔴 **«취소» 와 «연기» 를 같은 경로로 보내지 마세요** — 이 저장소에는 `request_type='cancel'` 로 접수한 요청을 승인하면 서버가 **`status='postponed'`** 로 적는 **옛 어긋남**이 있습니다(`decide` 의 그 분기). `postponed` 는 급여에서 `postponed_pay_percent`·`postponed_early_pay_percent` 를 타므로 **«취소» 를 그 길로 보내면 돈의 뜻이 바뀝니다.** 그래서 취소만 진짜 취소 경로(`DELETE`, `status='cancelled'` + 이력)로 보냅니다. ⛔ 그 어긋남 자체를 «정리» 하지 마세요 — 급여가 걸려 **사람이 정할 일**입니다. ✅ 되돌릴 수 없는 조작이니 **확인을 한 번** 받되, 그 문구가 «무슨 일이 일어나는지» 를 말하게 하세요(「그 시간에 다른 수업이 있으면 안 옮기고 알려 드립니다」). ⚠️ **「그 글자가 있는가」로 검사하지 마세요** — `if (!window.confirm(ask)) return;` 을 `if (false) return;` 으로 바꿔도 `window.confirm` 이라는 글자는 남습니다(조건까지 함께 볼 것). ✅ **새 판정을 만들지 말고 기존 경로를 그대로 쓰세요** — 여기서는 `/schedule-requests` + `/decide` 두 번. 그러면 겹침 검사·반복 보호·스코프 재확인·감사로그를 공짜로 받습니다(⛔ `PATCH /api/admin/class-schedules/:id` 는 겹침 검사가 **없고** 지사·대리점에 403 입니다). ⚠️ 대신 **«두 번» 이라 반쪽 성공이 생깁니다** — 접수는 됐는데 승인이 실패하면 **「요청은 저장됐습니다 — 어디서 마저 하세요」** 라고 말해야 합니다(안 그러면 결재함에 남아 있는데 화면은 «실패» 로만 보입니다). ⚠️ **«누가 냈는가» 를 본문으로 믿지 마세요** — 서버가 세션으로 확인해 관리자일 때만 `'admin'` 으로 적습니다(옛 코드는 값이 무엇이든 `'teacher'` 로 떨어뜨려 **관리자가 한 일을 강사 탓으로** 기록했습니다). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «어느 줄에 버튼이 붙고 무엇이 나가는가» 뿐이라 수리 전에도 `--fast` 가 전부 초록이었습니다. 🪤 **브라우저 검사에서 `page.click()` 을 그냥 쓰면 «크래시» 가 «검출» 로 위장합니다** — 요소가 안 보일 때 30초 타임아웃 + 예외로 죽어 **exit=1 인데 ❌ 가 0줄**입니다(무엇이 깨졌는지 안 보입니다). **짧은 timeout + try** 로 감싸 «그 자리를 이름으로 찍는» 깔끔한 FAIL 로 만드세요. 🪤 그리고 **하니스마다 실패 글자가 다릅니다**(`❌ FAIL` 대 `FAIL`) — 변이시험 runner 가 한쪽 모양으로만 세면 **멀쩡한 검출을 «못 잡음» 으로** 읽습니다(2026-09-22 에 실제로 그렇게 읽고 없는 구멍을 쫓았습니다). 감시는 `test-harness/manager_today_reschedule_harness.mjs`(자동 58종)와 `test-harness/manual/manager-today-reschedule-browser.mjs`(브라우저 35종 — **자동으로 안 돕니다, 사람이 부릅니다**). 변이 **13종** 전부 실제 FAIL(조건 뒤집기 3 · 항상참 1 · 항상거짓 1 포함) |
| 관리자 화면을 **iframe(창)으로 띄웠더니 창 안만 한국어** — 바깥은 EN 인데 | 🔴 **한국 국적 계정은 `adm-lang-boot.js` 가 «새 문서를 열 때마다» 국적(KR)으로 `mangoi_lang` 을 되돌립니다** — iframe 도 새 문서라, 바깥에서 🌐 EN 을 눌러 둬도 창 안은 KO 로 시작합니다(2026-09-23 「오늘 수업」 캘린더 창 실측). ✅ 창을 여는 쪽이 **`&lang=en\|ko` 를 넘기고** 안쪽은 그것을 **저장하지 않고 그 창에서만** 씁니다(본보기 `admin/student.html` 의 `_lang`). ⛔ `mangoi_lang` 에 저장해서 맞추지 마세요 — 다른 화면 언어까지 바뀌고, 다음 문서에서 또 되돌아갑니다. 🔴 **그리고 창 안의 «다른 화면으로 가는» 링크(← · 대시보드 · 목록)를 누르면 관리자 화면이 창 «안» 에 통째로 뜹니다** — `&embed=1` 일 때 숨기세요(`html.mg-embed`). ⛔ «새 탭에서 열기» 에는 `embed` 를 붙이지 마세요(그건 창이 아닙니다). 📄 `docs/작업기록/260923_오늘수업_수업캘린더칸_빈방입장_학원칸.md` |
| 여러 에이전트를 **병렬로** 돌려 파일을 만들게 했더니 **남의 결과가 섞임** | 🔴 **같은 폴더의 같은 이름 도우미 파일(`rec.py`·`b1.json`)을 서로 덮어씁니다 — 에러 없이 «남의 번호로 제출» 이 됩니다.** 2026-09-23 낱말 사진 14묶음 병렬 생성에서 실측: 한 묶음이 다른 묶음의 번호 12개를 대신 만들었고, 한 묶음은 남의 로그 12줄을 자기 결과에 섞었습니다. ✅ **에이전트마다 전용 하위 폴더**를 처음부터 지정하고, **제출 직전 «번호·문장이 자기 입력 파일과 같은가»** 를 확인하게 하세요. ✅ 끝나면 결과 전체를 **«번호↔낱말 일치 · URL 중복 없음»** 으로 기계 대조하세요(그것이 섞임의 유일한 신호입니다). ⚠️ 제출이 타임아웃 난 뒤 «다시 제출» 하면 첫 요청이 실제로 나갔는지 모르므로 **중복 과금**이 생길 수 있습니다 — 그 사실을 보고에 적으세요. 🔴 그리고 **결과에서 후보를 «다시» 만들 때 원래 칸(`replaces` 등)이 빠지지 않는지** 보세요 — 빠지면 옛 것이 조용히 남습니다(31장 실측, «새 것이 실제로 쓰이는가» 를 세어 잡음). 📄 `docs/작업기록/260923_낱말사진_뜻안맞는사진_전수교체_2047장.md` |
| 푸시 알림을 «보냈다» 는데 **폰에 한 번도 안 뜸** (`push_queue` 에 쌓이기만) | 🔴 **`push_queue` 에 INSERT 하는 것은 «보내기» 가 아닙니다 — 기기를 깨워야 합니다.** sw.js 는 push 이벤트가 와야 `/api/push/pending` 을 읽는데, 그 신호는 `broadcastWebPush(endpoints)`(`src/web-push.ts`)만 보냅니다. 2026-09-24 실측: 결재 `notify()` 가 INSERT 만 해서 결재 푸시 **24건 전부 `fetched_at` NULL** — 한 번도 안 떴고, 게다가 «넣었다» 를 «닿았다» 로 세어 문자 폴백도 안 돌았습니다. ✅ 넣은 뒤 반드시 `broadcastWebPush` + 만료(410·404) 구독 끄기, **깨우기가 0건이면 «못 닿음» 으로 세세요.** ✅ 확인: `SELECT tag, COUNT(*), SUM(fetched_at IS NOT NULL) FROM push_queue GROUP BY tag` — 두 숫자가 크게 벌어지면 이 함정입니다. 본보기 `sendPushToUser`(`api-notify.ts`). 감시 `approval_ai_filter_harness` ⑤ |
| 공휴일을 넣었는데 **그날 수업이 안 밀림** | 🔴 **공휴일 표가 셋이고 수업을 미는 것은 하나뿐입니다** — `enroll_holidays`(수강 운영 › 공휴일, 새벽 6시 `runHolidayShiftSweep` 이 읽는 **유일한** 표) · `calendar_events.holiday`(강사 › 캘린더, 보기용) · `holidays`(운영 인프라 모듈, Nager.Date 공식 공휴일, 보기·검증용). 뒤의 둘에만 있는 날은 **에러 없이** 수업이 그대로 잡힙니다. ✅ 2026-09-25 부터 수강 운영 공휴일 탭과 «📋 오늘 할 일» 이 그 «빠진 한국 공휴일» 을 원천 표시(캘린더·공식·둘다)와 함께 보여 주고, 사람이 «옮기기» 를 누르면 `enroll_holidays` 에 들어갑니다(정본 `calendarHolidaysMissing`, `src/enroll-ops.ts`). ⛔ **cron 이 뒤의 둘을 자동으로 읽게 하지 마세요** — 필리핀 공휴일·일괄 채우기 한 번에 여러 날 수업이 사람 확인 없이 밀리고 되돌리기 어렵습니다. ⛔ 필리핀(PH)은 후보에서 뺍니다. ⚠️ 두 원천은 **따로** 묻고, 한쪽이 실패하면 `failed` 로 «일부만 대조» 라고 말합니다(0건을 «다 옮겼다» 로 위장하지 않기). 감시는 `test-harness/enroll_ops_todo_harness.mjs`(진짜 SQLite, 변이 10종 실제 FAIL) |

---

## 3. 백엔드 파일 지도 — "이 기능 어디 있지?"

| 새 기능이 «상태를 하나 더» 만들었는데 **옛 정지 경로가 그것을 모름** | 🔴 **«멈춘 척» 했다가 곧 다시 말합니다 — 그리고 에러가 안 납니다.** 2026-09-11 실측: A.i 친구하기에 «문장 큐»(답을 문장 단위로 이어 읽는 것)를 넣었더니, 마이크를 열 때 이미 있던 `MangoiTTS.stop()` 한 줄이 **큐의 다음 문장을 못 막았습니다.** 그 한 줄은 새 기능이 생기기 «전» 에는 충분했습니다 — 깨진 것은 코드가 아니라 **그 줄이 서 있던 전제**입니다. ✅ 정지를 **한 곳으로 모으세요**(`stopSpeakingNow` — 큐·TTS·아바타 입을 함께 끊습니다). ⚠️ 그리고 **그 상태를 끄는 «모든 옛 경로»** 를 찾아 그 정본을 지나게 하세요(여기서는 마이크 버튼·타이핑·새 턴 셋). ⚠️ **문자열 하니스로는 못 잡습니다** — 함수도 값도 다 «있고» 틀린 것은 «무엇까지 끄는가» 뿐입니다. ✅ 검사는 «정지 정본이 큐를 끊는가» 와 «옛 경로가 그 정본을 지나는가» 를 **짝으로** 두세요(앞만 보면 옛 경로가 따로 멈춰도 초록입니다). 📜 같은 줄기: 이 표의 「같은 판정이 두 곳이면 한쪽만 고쳐진다」 — 그쪽은 «판정», 이쪽은 «정지» 입니다 |
| 두 화면이 달라서 **«통일» 하려 할 때** | 🔴 **그 차이가 «사고» 가 아니라 «사람이 승인한 결정» 일 수 있습니다 — 통일하면 그 결정을 되돌립니다.** 2026-09-11 실측: 웜업(브라우저 음성인식, 즉시 자막)과 A.i 친구하기(서버 Whisper, 말이 끝나야)가 달라 「브라우저 인식으로 통일」을 제안서에 적었는데, `ai-friend.html:1353` 주석이 **2026-07-26 에 그 반대로 결정한 이유**를 적어 두고 있었습니다 — 「직원 피드백: 억양 섞인 아이 발음을 브라우저가 "I like dog"를 "talk"로 오인식 … 실시간 자막은 사라지지만 **그만큼 정답률이 오른다(사장님 승인)**」. ✅ **통일 전에 «왜 갈렸는지» 를 먼저 찾으세요** — 그 자리 주석·`git log -S` 로 30초면 나옵니다. ✅ 결정이 있으면 **양쪽을 같게 만들지 말고, 못 하던 쪽에 «폴백» 을 덧대세요**(여기서는 웜업이 «빈손» 일 때만 서버 Whisper 로 한 번 더 — 되던 것은 한 글자도 안 건드립니다). ✅ 그리고 **그 되돌림을 하니스가 막게** 하세요(「A.i 친구하기에 브라우저 인식이 되살아나지 않았는가」 — 실제로 변이로 잡힙니다). ⛔ 「같게 만드는 것이 좋은 일」이라는 이유만으로 옛 결정을 지우지 마세요 |
| 파일 | 담당 |
|---|---|
| `src/index.ts` | 라우팅 진입점 + 관리자 인증 게이트 |
| `src/api-mango.ts` | 수업 입장(`class/*`), 번역, 통합검색 |
| `src/api-admin.ts` | 관리자 전반: 통계·KPI·급여·강사·팝업·스케줄·구독·레벨테스트 |
| `src/api-games.ts` | 단어장·마이크로러닝·복습퀴즈·배지·스트릭·음성코칭 |
| `src/api-points.ts` | 포인트·기프티콘·수업 별점평가 |
| `src/api-students.ts` | 학부모 대시보드·학생 로그인/가입·소셜로그인 |
| `src/api-lessons.ts` | 평가서·숙제·캘린더·AI 학습리포트 |
| `src/api-notify.ts` | 알림톡·웹푸시·채팅 영속화·카카오 |
| `src/api-ai.ts` | AI 영작첨삭·영어친구챗·AI 명령 라우터 |
| `src/video-call-room.ts` | 화상수업 Durable Object (실사용) |
| `src/signaling-room.ts` | 구 시그널링 DO (진단용, 삭제 금지) |
| `src/today-plan.ts` | 📅 «오늘의 A.i 학습» 정본(순수 함수) — 수업일/집/미배정 묶음(`HOME_WEEK`·`CLASS_DAY`)·레벨 키 매핑. API 는 `api-students.ts` 의 `/api/student/today`, 화면은 `public/today.html`. ⚠️ 표를 바꾸면 `docs/AI학습도구_학습계획표_*.html` 도 함께 |

화면(`cloudflare-deploy/public/`)은 HTML 53개. 주요 파일은 `index.html`(홈+화상수업), `admin.html`(관리자), `student.html`(학생).

---

## 4. 작업 방식

### 4-1. 브랜치

`main` 에 직접 커밋하지 마세요. 작업용 브랜치를 만들고 PR로 올립니다.

```bash
git pull
git checkout -b 작업내용-요약
```

### 4-1-1. 병합 — CI 게이트가 통과하면 Claude 가 물어보지 않고 병합합니다

> **2026-08-17 사장님 지시.** 그 전에는 「병합할까요?」를 매번 물었는데, CI 를 기다리는
> 2분 사이 `main` 이 움직여 충돌 → 다시 병합 → 또 기다리기를 **한 PR 에서 네 번**
> 반복한 일이 있었습니다(PR #182).

**동작:** 작업 → PR → CI 통과 시 병합 → 배포 확인 → 결과 보고.
중간에 확인을 구하지 않습니다.

**GitHub 의 「Allow auto-merge」는 쓸 수 없습니다.** 찾지 마세요 —
**비공개 저장소의 자동 병합은 유료 플랜(Pro 이상)에서만** 제공되고, 이 저장소는
개인 계정의 비공개 저장소라 Settings → General → Pull Requests 에 **체크박스가 아예
없습니다**(2026-08-17 확인: API 도 `Auto-merge is not enabled for this repository` 로 거절).
그래서 「예약해 두고 자리를 떠나는」 방식은 불가능하고, **CI 결과를 기다렸다가 직접
병합**해야 합니다. 결과는 같고, 기다리는 동안 `main` 이 움직이면 한 번 더 병합하면 됩니다
(그 충돌의 대부분은 2장 「원장」 함정의 정렬 수정으로 이미 사라졌습니다).

**⛔ 그래도 멈추고 사람에게 묻는 경우** — 「CI 통과」로는 판단할 수 없는 것들입니다.

| 상황 | 왜 |
|---|---|
| **CI 가 빨간불** | 원인을 찾아 고쳐서 초록불을 만든 뒤 병합합니다. **게이트를 우회하거나 테스트를 끄는 방식은 금지** |
| **진짜 충돌** — 둘이 «같은 파일» 의 버전을 각자 올린 경우 | 아무 쪽으로 몰면 한쪽 수정이 조용히 묻힙니다. («다른 파일끼리» 자리만 겹친 충돌은 양쪽을 다 남기고 그냥 진행 — 2장 원장 함정 참고) |
| **D1 데이터 변경**(DELETE/UPDATE/DROP) | 개발·운영이 같은 DB 입니다. 실제 학생 데이터라 **CI 가 막아 주지 못합니다** |
| **지우거나 되돌리는 변경** | 일부러 없앤 기능을 되살리는 것, 파일·기능 삭제 |
| **지시 범위를 벗어나는 것** | 「이것도 같이 고치면 좋겠다」 싶은 건 먼저 말합니다 |
| **1-3 공동 금지구역** | `public/index.html`, `src/index.ts`, `wrangler.toml`, `deploy.ps1`, `public/sw.js` — 사고 반경이 서비스 전체입니다 |

**⚠️ CI 통과 ≠ 화면이 맞다.** 작업 환경(원격 컨테이너)의 프록시가 `mangoi.ai` 를 막아서
**Claude 는 배포된 실제 화면을 못 봅니다.** 다만 「배포 워크플로 성공」이 끝은 아닙니다 —
러너 로그의 **업로드 파일 목록**까지는 읽을 수 있습니다(2장 「배포됐나」 항목).
2026-08-17 에 정확히 이 틈에서 사고가 났습니다 — 검증은 전부 통과했는데 사장님 PC 화면에는
시계·날씨가 아예 없었습니다(모바일 전용으로 만들어 놓고 PC 를 확인하지 않았음).
**화면이 바뀌는 작업은 병합 후 사람이 한 번 봐야 끝난 것입니다.**

### 4-2. 2인 담당 분담 — 같은 파일을 동시에 만지지 않는 것이 원칙

`index.html`(1.6MB)과 `admin.html`(1.3MB)이 전체 작업의 대부분을 차지합니다.
이 둘을 두 사람이 같이 만지면 충돌 해결이 사실상 불가능합니다. 그래서 영역을 나눕니다.

| 영역 | 담당 | 파일 |
|---|---|---|
| **관리자·운영·정산** | A (배포 권한자) | `public/admin.html`, `src/api-admin.ts`, `src/accounting-*.ts`, `src/api-payroll-auto.ts`, `src/org-settlement.ts`, `src/api-retention.ts`, `src/churn-*.ts`, **`public/work.html`**(결재 화면), **`src/api-approval.ts`**, **`src/approval-policy.ts`** |
| **학생 학습 콘텐츠** | B | `public/student-game-*.html`, `public/student-games.html`, `public/vocab.html`, `public/micro-quiz.html`, `public/review-quiz.html`, `public/warmup.html`, `public/ai-write.html`, `public/speech-coach*.html`, `public/suspect-mystery.html`, `public/battle-3d.html`, `src/api-games.ts`, `src/api-points.ts` |
| **🚫 공동 금지구역** | A만 | `public/index.html`(홈+화상수업), `src/index.ts`(라우팅+인증게이트), `src/video-call-room.ts`, `src/signaling-room.ts`, `wrangler.toml`, `deploy.ps1`, `public/sw.js` |

- 학습 콘텐츠는 게임마다 **파일이 독립**이라 충돌이 거의 없고, 사고가 나도 반경이 게임 하나입니다.
- 금지구역은 사고 반경이 서비스 전체입니다. 배포 권한자만 만집니다.
- **담당 밖 파일을 고쳐야 하면 먼저 상대에게 알리세요.** 원격이라 서로 뭘 하는지 안 보입니다.

### 4-3. ⚠️ 배포 커밋은 HTML 53개를 전부 건드립니다

`deploy.ps1` 은 캐시 무효화를 위해 **모든 HTML에 `<!-- BUILD:시각 -->` 주석을 새로 박습니다.**
그래서 배포 커밋 하나가 파일 59개를 바꾼 것처럼 보이지만, 대부분은 **1줄짜리 스탬프뿐**입니다.

- **배포 직후 `git pull` 하면 HTML 전부가 바뀐 것으로 나옵니다. 정상입니다.**
- 리뷰할 때는 스탬프를 빼고 보세요. 진짜 변경만 나옵니다.

```bash
git diff -I'BUILD:' main..작업브랜치
```

- 작업 중 배포가 일어나 충돌이 나면, 충돌 지점이 `</body>` 바로 위의 `<!-- BUILD: -->` 줄인지 먼저 확인하세요.
  스탬프 줄이면 **어느 쪽을 택해도 무방**합니다(다음 배포 때 어차피 새로 박힘).

### 4-4. 검증 — 코드를 고쳤으면 반드시

1. `cd cloudflare-deploy && node node_modules/typescript/bin/tsc --noEmit` — 컴파일 통과
   ⚠️ `npx tsc` 는 **"출력 없음"이 통과가 아닙니다**(npx 가 조용히 실패해도 똑같이 아무것도 안 찍힘)
   ✅ **`node_modules` 가 없는 컨테이너에서도 돌릴 수 있습니다** — `/opt/node22/bin/tsc` 가 전역으로
   깔려 있고, 막는 것은 `tsconfig.json` 의 `types: ["@cloudflare/workers-types"]` 하나뿐입니다
   (2026-09-21 실측). `{ "extends": "./tsconfig.json", "compilerOptions": { "types": [] } }` 짜리
   임시 tsconfig 로 덮어 `-p` 로 돌리세요(⛔ 쓰고 나면 지울 것 — 커밋에 딸려 가면 안 됩니다).
   ⚠️ 그때 오류가 **0건이 아닙니다**(2026-09-21 기준 126건, 전부 `Cannot find name 'D1Database'` 류).
   **«절대값» 이 아니라 «기준선 대비» 로** 읽으세요 — `git worktree add --detach <경로> origin/main`
   으로 사본을 만들어 **같은 명령을 돌리고 줄 단위로 `comm` 대조**해 «새 오류 0건» 을 봅니다
   (⛔ `git stash` 로 기준선을 만들지 마세요 — 커밋 안 한 편집이 함께 치워집니다).
2. **리포 루트에서** `node test-harness/run.mjs --fast` — 회귀 하니스 (약 90초, PASS 114 / SKIP 20)
   ⚠️ 하니스는 **리포 루트 `test-harness/`** 에 있습니다. `cloudflare-deploy/test-harness/` 에도
   같은 이름의 폴더가 있지만 그 안엔 `run.mjs` 가 **없습니다** — 1번의 `cd cloudflare-deploy` 를
   그대로 이어서 실행하면 `MODULE_NOT_FOUND` 가 납니다
3. 화면 변경이면 실제 브라우저에서 확인

**"고쳤습니다"라고만 말하지 말고, 무엇으로 확인했는지 함께 보고하세요.**
서버 배포 후에는 배포 스탬프(`BUILD_STAMP`)로 실제 반영 여부를 확인합니다.

### 4-5. 보고 방식

- 사실만. 테스트가 실패했으면 실패했다고 출력과 함께 말할 것
- 건너뛴 단계가 있으면 명시할 것
- 확인되지 않은 것을 "됐습니다"라고 하지 말 것

### 4-6. 판단을 기록으로 남기기 — 대화는 상대에게 안 갑니다

**클로드 코드의 대화 기록은 작업한 사람 쪽에만 남습니다.** 팀 요금제로 묶어도
서로의 대화가 보이지 않습니다. 그래서 코드에는 「택한 것」만 남고,
**「왜 다른 길로 안 갔는지」는 대화와 함께 사라집니다.**

작업이 끝나면 `/worklog` 로 `docs/작업기록/` 에 남기세요. 특히 다음 셋:

1. **왜 했나** — 어떤 제보·증상에서 출발했는지
2. **검토했다가 버린 방법과 그 이유** ← 가장 중요합니다
3. **확인한 방법** — 타입체크·하니스 결과, 사람이 화면에서 본 것

> 🔑 **새로 발견한 함정은 작업기록 말고 이 파일 2장 표에 넣으세요.**
> 작업기록은 사람이 찾아 읽어야 하지만, `CLAUDE.md` 는 클로드가 매번 자동으로 읽습니다.

공유 슬래시 명령·서브에이전트 목록은 [.claude/README.md](.claude/README.md) 를 보세요
(`/verify` 검증, `/worklog` 기록, `/newdoc` 목차 등록, `trap-check` 함정 대조).

---

## 5. 환경·문서 링크

- **📚 문서 전체 목차: [docs/INDEX.md](docs/INDEX.md)** — 이 저장소의 모든 문서는 여기서 찾습니다
- **🤝 팀 공유 방법: [docs/팀공유_가이드.md](docs/팀공유_가이드.md)** — 무엇을 어디에 두고 어떻게 공유하는지
- 유지보수 매뉴얼(사람용): [MAINTENANCE.md](MAINTENANCE.md)
- 환경변수·시크릿: [docs/ENVIRONMENT.md](docs/ENVIRONMENT.md)
- 장애 대응: [docs/TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md)
- 공동작업 안내(직원용): [docs/개발_공동작업_안내_직원용.md](docs/개발_공동작업_안내_직원용.md)
- 공유 클로드 설정 설명: [.claude/README.md](.claude/README.md) — 슬래시 명령·서브에이전트 목록

`.env`, `.dev.vars` 는 깃에 없습니다. 없으면 사람에게 요청하세요. **깃에 올리지 마세요.**

---

*이 파일은 두 사람이 공유합니다. 새 함정을 발견하면 여기에 추가하고 PR에 포함하세요.*
