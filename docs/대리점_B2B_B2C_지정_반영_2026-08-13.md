# 대리점 B2B/B2C 결제유형 일괄 지정 — 반영 완료 보고 (2026-08-13, 2026-08-14 갱신)

> 사장님이 확정해 주신 검토표 «B2B_B2C_검토표_260812.xlsx» (시트 `B2B검토`, 574행)를
> 운영 D1 `mango-db` 의 `centers.payment_type` 에 반영했습니다.
> 반영 SQL: [`cloudflare-deploy/migration-centers-payment-type-2026-08-13.sql`](../cloudflare-deploy/migration-centers-payment-type-2026-08-13.sql)

> **🔴 먼저 읽어 주세요 —** 첫 반영분이 **야간 동기화에 전부 지워졌습니다.**
> 원인을 찾아 코드를 고쳤고 데이터도 다시 넣었습니다. 자세한 건 §5 입니다.
> **그 코드 수정이 배포돼야** 다음 새벽에 또 지워지지 않습니다.

---

## 1. 반영 결과 (운영 DB 실측)

| 구분 | 최초 상태 | 현재 |
|---|---:|---:|
| B2B | 0 | **446** |
| B2C | 0 | **123** |
| 미지정(NULL) | 921 | 352 |
| 합계 | 921 | 921 |

- 검토표 574행 중 **569곳** 반영, **5행은 사장님 지시로 제외**(§3).
- 남은 352곳 중 347곳은 **검토 대상이 아니었습니다**. 검토표는 «활성 학생 3명 이상» 학원만
  담았고, 나머지는 학생 0~2명인 폐점·테스트·미사용 계정이라 미지정으로 둡니다.

## 2. 사장님이 판단해 주신 부분

검토표에서 제안값이 «미지정»이던 93곳을 전부 확정해 주셨습니다.

| 제안 → 확정 | 건수 |
|---|---:|
| 미지정 → B2C | 84 |
| 미지정 → B2B | 9 |

나머지 481곳(B2B 442 · B2C 39)은 제안값 그대로 확정되어, 고치신 곳은 없었습니다.

## 3. 테스트·관리용 계정 5행 제외 (2026-08-14 지시 반영)

이름상 실제 학원이 아닌 3개 계정을 미지정으로 되돌렸습니다.

| id | 학원명 | 결제건수 |
|---:|---|---:|
| 1 | 테스트 B2C | 20 |
| 28 · 91 · 845 | 미사용 (중복 등록 3줄) | 9 |
| 273 | N TEST대리점 | 1 |

- «미사용»은 **3줄이 전부** 미지정이어야 합니다. 결제내역 분리는 ID 가 아니라 **이름**으로
  매칭해서 `ORDER BY c.id LIMIT 1` 로 대표 한 줄을 뽑기 때문에, 한 줄만 남겨두면
  그 줄이 대표가 되어 그대로 B2B 로 잡힙니다. 그래서 «3곳»이 실제로는 **5행**입니다.
- 이 조치로 B2B 매출 집계에서 **30건이 빠졌습니다**
  (첫 보고에서 29건이라고 적었으나 20+9+1 = **30건**이 맞습니다).

## 4. 반영 전 검증

1. **검토표 자체 검사** — 574행 ID 중복 없음, 확정값이 `B2B`/`B2C` 아닌 칸 0개, 빈칸 0개.
2. **ID ↔ 학원명 대조** — 검토표를 만든 8/12 이후 `centers` 가 바뀌었을 수 있어,
   574개 ID 의 `centers.name` 이 검토표 학원명과 **글자 그대로** 같은지
   `id%8` 버킷 체크섬 8개로 대조 → **8/8 전부 일치, 건수도 574 일치**.
3. **이름 충돌 검사** — 같은 학원명이 여러 줄인 9개 그룹이 전부 같은 값인지 확인 → 충돌 0건.

---

## 5. 🔴 첫 반영분이 지워진 사고와 그 원인

### 무슨 일이 있었나

| 시각 (KST) | 일 |
|---|---|
| 08-14 03:29~03:33 | 1차 반영 (B2B 451 / B2C 123). 집계·결제내역 분리까지 정상 확인 |
| 08-14 03:45:51 | **야간 동기화가 `centers` 921행을 통째로 다시 써서 payment_type 전멸** |
| 08-14 04:0x | 되돌리기 작업 중 발견 — 전 행이 NULL 로 돌아가 있었음 |

`centers.updated_at` 이 921행 전부 `2026-08-14 03:45:51` 한 값으로 덮여 있는 것으로 확인했습니다.

### 원인 — `INSERT OR REPLACE`

`src/cafe24-sync.ts` `importCafe24Org()` 가 이렇게 돼 있었습니다.

```sql
INSERT OR REPLACE INTO centers (id, franchise_id, name, address, manager, active, created_at, updated_at)
```

SQLite 의 `REPLACE` 는 UPDATE 가 아니라 **«기존 행을 지우고 새 행을 넣는»** 동작입니다.
그래서 위 목록에 없는 `payment_type` 은 매일 새벽 **NULL 로 초기화**됐습니다.
동기화는 워커 cron `"0 18 * * *"`(UTC) = **매일 03:00 KST** 로 돌아갑니다.

### 이게 왜 중요한가

이건 제 작업만의 문제가 아닙니다. **기능 자체가 처음부터 동작할 수 없는 상태였습니다.**
관리자가 `PATCH /api/admin/centers` 로 결제유형을 지정해도 **그날 밤이면 사라집니다.**
8/12 에 만든 «대리점 결제유형 지정»(수정요청 #05)은 지금껏 한 번도 유지된 적이 없습니다.

### 고친 내용

`INSERT OR REPLACE` → **UPSERT**(`ON CONFLICT(id) DO UPDATE`)로 바꿔,
카페24가 주는 컬럼(`name`·`address`·`manager`·`active`·`franchise_id`)만 덮어쓰고
**D1 에서만 관리하는 값(`payment_type`, `country`)은 건드리지 않게** 했습니다.

`franchises` 도 같은 결함이 있어 함께 고쳤습니다(`opened_at` 이 매일 밤 지워지고 있었습니다).

### ⏰ 남은 조건 — 배포

**코드 수정은 배포돼야 효력이 생깁니다.** 지금 데이터는 다시 넣어 뒀지만,
수정이 배포되지 않은 채 **다음 03:00 KST** 가 지나면 또 지워집니다.

- 배포 전이라면: 위 마이그레이션 SQL 을 다시 돌리면 복구됩니다(몇 초).
- 저는 배포 권한이 없어 **배포하지 않았습니다.** `main` 병합 시 GitHub Actions 가 자동 배포합니다.

---

## 6. 실제로 무엇이 달라졌나 — 결제내역 B2B/B2C 분리

이 컬럼이 비어 있는 동안에는 분리 기능이 **사실상 동작하지 않았습니다.**

| 결제 11,640건 | 반영 전 | 현재 |
|---|---:|---:|
| B2B | **0건** | 11건 (196.8만원) |
| B2C | 11,640건 (100%) | 11,629건 |

반영 전에는 `centers.payment_type` 이 전부 비어 있어 학생 쪽 대체값(`students_erp.payment_type`)으로
넘어갔는데, 그 값이 `B2B%` 인 학생이 **한 명도 없어** 전건이 B2C 로 떨어졌습니다.

현재 B2B 로 잡히는 11건의 내역입니다. 테스트 계정은 §3 조치로 모두 빠졌습니다.

| 학원명 | 건수 | 금액 |
|---|---:|---:|
| N 안산 SLP 가정망고 | 5 | 1,518,000 |
| N 리시움학원수업 | 5 | 420,200 |
| N 안산 LAB | 1 | 30,000 |

## 7. 되돌리는 방법

최초 적용 전에는 921곳이 **전부** 미지정이었으므로 한 줄이면 됩니다.

```sql
UPDATE centers SET payment_type = NULL WHERE payment_type IS NOT NULL;
```

## 8. 관리자 화면 — 이미 있는 것 / 아직 없는 것

> ⚠️ **정정(2026-08-14).** 이 문서 최초본에 «관리자 화면에서 보거나 고칠 수 없다»고 적었으나
> **사실이 아닙니다.** `admin.html` 만 검색해서 `js/adm-core.js` 의 구현을 놓친 제 확인 부족이었습니다.

### 이미 있는 것 (수정요청 #05 때 함께 만들어져 있었음)

| 위치 | 기능 |
|---|---|
| `admin.html:7082` | 대리점 목록에 «결제유형» 열 |
| `js/adm-core.js:3344` `_ptCell()` | 행마다 B2B / B2C / 미지정 드롭다운 |
| `js/adm-core.js:3357` `ctSetPayType()` | 고르면 즉시 `PATCH /api/admin/centers` 저장, 실패 시 화면 값 롤백 |
| `admin.html:7064` | 신규 등록 폼의 결제유형 선택 |
| `admin.html:9793~` | 결제내역 `card-payments-b2b` / `card-payments-b2c` 분리 카드 |

즉 **한 곳씩 고치는 건 지금도 화면에서 됩니다.**

### 이번에 추가한 것 — 결제유형 필터 + 유형별 건수 (2026-08-14)

없던 것은 «거르기»였습니다. `GET /api/admin/centers` 의 필터가 검색어(`q`) 하나뿐이고
그 검색은 학원명·원장·주소·지사만 훑어서, **결제유형으로 목록을 좁힐 수단이 없었습니다.**
921건을 50개씩 넘겨 보는 구조라 미지정 352곳을 찾으려면 19페이지를 눈으로 훑어야 했고,
지정이 얼마나 남았는지 알 방법도 없었습니다.

대리점 목록 검색창 아래에 버튼 줄을 붙였습니다.

```
결제유형   [ 전체 921 ] [ B2B 446 ] [ B2C 123 ] [ 미지정 352 ]
```

- 버튼을 누르면 그 유형만 목록에 남습니다(서버에서 걸러서 페이징도 정확합니다).
- 숫자는 **검색어와 함께 움직입니다.** 예를 들어 «SLP» 로 검색하면
  전체 166 · B2B 38 · B2C 44 · 미지정 84 로 바뀌어, 그 검색 범위에 미지정이 몇 곳인지 바로 보입니다.
- 행에서 결제유형을 바꾸면 숫자와 목록이 즉시 다시 계산됩니다.
  «미지정»만 보는 중에 값을 지정하면 그 행은 목록에서 빠집니다.
- 마지막 페이지에서 목록이 줄어 페이지가 범위를 벗어나면 자동으로 앞 페이지로 당깁니다.

| 파일 | 변경 |
|---|---|
| `src/api-admin.ts` | `GET /api/admin/centers` 에 `payment_type=B2B\|B2C\|NONE` 필터 + 응답에 `counts` 추가. **건수 4종을 COUNT 쿼리 하나로** 구해 왕복이 늘지 않습니다 |
| `public/js/adm-core.js` | 필터 상태(`_ctState.pt`)·버튼 렌더(`_ctRenderPtFilter`)·`ctFilterPayType()`, 저장 후 자동 갱신 |
| `public/admin.html` | 버튼이 들어갈 `#ct-ptfilter` 자리 + `adm-core.js?v=67 → 68` |

> 새 대리점은 결제유형이 **미지정으로 시작**하므로 시간이 지나면 미지정이 쌓입니다.
> 이제 «미지정» 버튼 하나로 밀린 것만 골라 정리할 수 있습니다.

---

### 실행 기록

- 실행 경로: 운영 D1 `mango-db` (`80a12a77-4d79-4abd-aa9e-f5e39a7b5cf5`) 직접 UPDATE
- 1차(08-14 03:29~03:33 KST): `changes: 123` + `changes: 451` → **야간 동기화로 소실**
- 2차(08-14 04:0x KST, 테스트 계정 5행 제외): `changes: 123` + `changes: 446`
- 사후 집계 `SELECT payment_type, COUNT(*) FROM centers GROUP BY 1` → B2B 446 / B2C 123 / NULL 352
- 코드 변경: `src/cafe24-sync.ts` 1개 파일 (§5). `node tsc --noEmit` 통과(exit 0, 오류 0)
- 회귀 하니스(`test-harness/run.mjs --fast`)는 **돌리지 않았습니다** — 아래 참고
- 배포하지 않았습니다(`deploy.ps1` 미실행)

> ⚠️ **담당 영역 안내(A 께)** — `src/cafe24-sync.ts` 는 CLAUDE.md §4-2 의 제 담당 밖 파일입니다.
> 이걸 고치지 않으면 사장님이 지시하신 작업 자체가 매일 밤 무효가 되어 부득이 손댔습니다.
> 변경은 SQL 문 2개(UPSERT 전환)뿐이고 다른 로직은 그대로입니다. 검토 부탁드립니다.
