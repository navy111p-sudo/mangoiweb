# 도메인 조사 원자료

- **조사일**: 2026-08-15
- **방법**: DNS `NS` 레코드 질의 (dnspython). `NXDOMAIN` = 위임 없음 = 미등록 가능성 높음,
  `NOERROR` + NS 존재 = 등록됨
- **한계**: DNS 기반이므로 **등록만 하고 네임서버를 설정하지 않은 도메인**은 미등록으로
  보일 수 있습니다. 실제 구매 전 등록업체(가비아·후이즈·Cloudflare·Namecheap)에서
  재확인 필수. RDAP/WHOIS 는 이 환경의 egress 정책상 조회 불가였습니다.

## 핵심 답: `www.mangoai.ai`

**NXDOMAIN → 미등록 가능성 높음. 구매 가능해 보입니다.**

다만 권장하지는 않습니다.

| 이유 | 내용 |
|---|---|
| 비용 | `.ai` 는 레지스트리(Anguilla) 규정상 **최소 2년 등록**. 연 $65~$90 수준이라 초기 약 $150~$180 |
| 상표 충돌 | "MangoAI" 사용 주체 다수. **MangoAI(영상광고 스타트업)가 2026-02 Canva 에 인수**됨. Mango Animate 의 "Mango AI" 영상 생성기도 존재 |
| 철자 변경 | 현 브랜드는 `mangoi`. `mangoai` 는 i 가 하나 늘어 구전·전화 안내 시 혼동 |

`.ai` 를 꼭 쓴다면 **`mangoi.ai`** 가 브랜드 철자를 유지하므로 더 낫습니다 (역시 미등록).

## 미등록 (구매 가능)

| 도메인 | 대략 연 비용 | 메모 |
|---|---|---|
| `mangoi.ai` | 약 10만원 | **최우선 추천.** 현 브랜드 철자 유지 |
| `mangoai.ai` | 약 10만원 | 요청 도메인. 철자 변경 + 상표 충돌 |
| `mango-i.ai` | 약 10만원 | 하이픈은 구전 안내에 불리 |
| `mangoedu.ai` | 약 10만원 | 교육 업종 명시 |
| `mangokids.ai` | 약 10만원 | 아동 대상 명시 |
| `mangoai.kr` | 약 2만원 | 국가 도메인, 신뢰도 |
| `mangoai.co.kr` | 약 2만원 | 현 `mangoi.co.kr` 와 짝. **오타 방어용** |
| `mangoi.kr` | 약 2만원 | 최단. SMS 안내에 유리 |
| `mangoi.io` / `.net` / `.org` / `.app` / `.education` | 2~5만원 | 방어 등록용 |
| `mangoenglish.co.kr` | 약 2만원 | 업종 직관, 광고용 |
| `mangoaiedu.com` / `mangoiai.com` / `mangoi-ai.com` | 약 2만원 | `.com` 대안 |
| `hellomangoai.com` / `mangoaienglish.com` | 약 2만원 | 접두·접미 변형 |
| `mangoai.academy` / `.shop` / `.live` / `.world` / `.site` / `.link` / `.today` | 변동 | 우선순위 낮음 |

## 등록됨 (확보 어려움)

| 도메인 | 네임서버 | 판정 |
|---|---|---|
| `mangoai.com` | `ns3/ns4.afternic.com` | **애프터마켓 매물.** 프리미엄가 예상 |
| `mangoai.net` | `ns1/ns2.afternic.com` | 애프터마켓 매물 |
| `mangoai.xyz` | `ns1/ns2.afternic.com` | 애프터마켓 매물 |
| `mangoai.co` | Cloudflare | 타 AI 서비스 사용중 |
| `mangoai.io` | Cloudflare | 사용중 |
| `mangoai.app` | spaceship.net | 사용중 |
| `mangoai.org` | dyna-ns.net | 사용중 |
| `mangoai.dev` / `.pro` / `.store` / `.online` / `.club` | 각 등록업체 | 사용중 |
| `mangoenglish.com` | namebrightdns | 매물/파킹 |
| `mangoi.co` | abovedomains | 파킹 |

## 현재 망고아이 주소 실측

```
test.mangoi.co.kr   A  104.21.28.253, 172.67.147.240   (Cloudflare — 신규 운영 사이트)
mangoi.co.kr        A  118.219.234.180                 (구 서버)
www.mangoi.co.kr    CNAME → mangoi.co.kr               (구 서버)
mangoi.com          A  121.78.235.40                   (별개 호스팅)
www.mangoi.com      A  121.78.235.40
mango-i.com         NXDOMAIN                           (존재하지 않음 — 옛 문서 오기)
```

## 권장 순서

1. **비용 0원 / 즉시** — 이미 보유한 `www.mangoi.co.kr` 를 신규 Worker 에 연결.
   `test.` 서브도메인 노출 제거가 도메인 신규 구매보다 우선.
2. **약 4만원 / 이번 달** — `mangoai.co.kr` + `mangoai.kr` 오타 방어 등록.
3. **약 20만원(2년) / 여유 시** — `mangoi.ai` 를 마케팅·명함용으로.

다중 도메인을 보유하더라도 **정본은 하나**로 두고 나머지는 301 리다이렉트만 겁니다.
그러지 않으면 검색엔진에서 자기 도메인끼리 경쟁(중복 콘텐츠)합니다.

## 출처

- [.ai 도메인 가격 인상 — Domain Name Wire](https://domainnamewire.com/2026/02/02/ai-domain-name-prices-going-up-20/)
- [.ai 가격 비교 — DomainOffer](https://domainoffer.net/tld/ai)
- [Canva, MangoAI 인수 — CNBC](https://www.cnbc.com/2026/02/23/canva-acquires-cavalry-for-motion-graphics-and-mangoai-for-video-ads.html)
- [Mango AI (Mango Animate)](https://mangoanimate.com/)
- [.ai 도메인 등록 — Namecheap](https://www.namecheap.com/domains/registration/cctld/ai/)
