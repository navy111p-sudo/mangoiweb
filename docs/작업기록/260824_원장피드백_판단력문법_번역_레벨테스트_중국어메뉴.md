# 이보영 학원 원장 피드백 6건 → 이 PR 은 지적 2·6번만 (1·3~5번은 병렬 PR #443·#448)

- 날짜: 2026-08-24
- 작업자: Claude (사장님 지시)
- 브랜치 / PR: `claude/english-app-feedback-3xkw10` (PR #445)

## 1. 왜 했나

이보영 학원 원장님이 폰으로 학생 앱을 둘러보고 6가지를 지적했다
(분석 보고서: `docs/이보영학원_원장피드백_분석보고서_2026-08-24.pdf`).

1. 웜업 「뜻」이 "Let's warm up before class" → **「수업 전에 따뜻하게하자」** 직역
2. 영어 앱인데 **중국어 복습퀴즈**가 메뉴에 떠서 "뭐지" 싶음
3. 판단력 훈련 보기에 **마침표 등 문장부호 누락**
4. "Can I buy?" → "Can I buy it?" 이 맞음 (목적어 빠짐)
5. "want play" → "want to play" (**문법 오류**)
6. **레벨테스트**가 처음엔 있었는데 다시 못 찾음

사장님 추가 지시: 「문법은 최대한 고도화 — 영어교육 기관이라 문법 실수는 안 된다」.

## 1-1. ⚠️ 이 PR 의 범위가 중간에 줄었다 — 왜

처음엔 이 브랜치(PR #445)에서 6건 전부(실행 계획 1~4번, 지적 1·2·3·4·5·6 커버)를 고쳤다.
그런데 CI 가 GitHub Actions 계정 장애로 몇 시간째 멈춰 있는 사이(별도 기록: PR #447), **다른
두 세션이 같은 두 문제를 각자 별도 PR 로 독립적으로 고쳤다**:

- **PR #443** — 웜업 「뜻」 직역(지적 1)을 `mode:'learn'`(전용 의역 프롬프트 + 새 캐시 접두사
  `trl1:` + `curatedMeaning()` 고정 인사말)으로 고침
- **PR #448** — 판단력 훈련 문법(지적 3·4·5)을 `judgment-english.ts` 신설 + 생성 결과를 같은
  모델로 재검증(`englishLooksCorrect`)해 재생성 + **밴드 프롬프트의 «선택지 5단어 이하» 상한이
  "to" 를 떨어뜨리게 만든 근본 원인**까지 고쳐서 해결

두 PR 모두 이 PR 의 임시방편(웜업: 첫인사만 손질 + chat 모드 재사용 / 판단력: 생성 후 2차
LLM 으로 문장을 고쳐 쓰는 `polishScenarioEnglish`)보다 **근본 원인에 더 가깝고, 전용 하니스까지
갖췄다.** 세 PR 을 전부 병합하면 `api-judgment.ts`·`warmup.html` 이 충돌하거나, 같은 함수에
서로 다른 2차 교정 로직이 두 벌 쌓여 낭비·혼란이 생긴다.

→ **이 PR 은 지적 2·6번(다른 PR 이 안 건드린 부분)만 남기고, 웜업·판단력 문법 변경은
main 버전으로 되돌렸다.** 지적 1·3·4·5는 PR #443·#448 이 병합되면 함께 해결된다.

## 2. 무엇을 바꿨나 (좁힌 뒤 최종)

| 파일 | 내용 |
|---|---|
| `cloudflare-deploy/public/judgment.html` | (지적 6) ① 문제 화면 하단에 「🎯 레벨 다시 재기」 상시 버튼 ② `?placement=1` 로 들어오면 곧장 레벨 찾기 시작 |
| `cloudflare-deploy/public/js/idx-allmenu.js` | (지적 2·6) ① 「레벨 테스트」 타일 신설(→ `/judgment.html?placement=1`) ② 중국어 복습퀴즈 타일 `zh:true` — `mangoiZhLearner()` 일 때만 표시 ③ index.html 에 박힌 중국어 버튼 3곳(드로어·AI홈 퀵버튼·AI와 친구하기)도 여기서 숨김 |
| `cloudflare-deploy/public/review-quiz-cn.html` | 화면을 열면 `mangoi_zh_learner=1` 기록 — 이후 메뉴에 중국어 항목이 보임 |
| `cloudflare-deploy/public/index.html` | `idx-allmenu.js ?v=4→5` **버전 한 글자만** (공동 금지구역이라 코드 추가 없음) |
| `docs/이보영학원_원장피드백_분석보고서_2026-08-24.pdf` | 6건 전체 원인 분석 보고서(이 PR 이 원본 제공처) |

**되돌린 것** (main 버전 그대로, 코드 변경 없음): `src/api-judgment.ts`(→ #448 이 처리),
`public/warmup.html`(→ #443 이 처리).

## 3. 왜 그렇게 풀었나 — 검토했다가 버린 방법

- **(지적 2) 서버에서 중국어 수강생 판별** — 버림. `class_schedules`·`students_erp` 어디에도
  «이 수업/학생이 중국어» 라는 구조화된 칸이 없다(교재 이름·강사 국적뿐 → 추측 조인은
  «남의 이름» 함정과 같은 뿌리). 새 API 를 만들면 `src/index.ts`(공동 금지구역) 등록이 필요.
  → 화면 신호 2개로 판별: ① `mangoi_lang='zh'` ② 중국어 퀴즈 화면을 연 적 있음(`mangoi_zh_learner`).
  직접 주소·AI 명령 검색으로는 여전히 열리고, 한 번 열면 그 뒤로 메뉴에도 보인다.
  localStorage 접근 불가 환경에서는 **보이는 쪽**(수강생이 기능을 잃는 쪽이 더 나쁨).
- **(지적 2) index.html 의 버튼 3곳을 직접 수정** — 버림. 공동 금지구역 + 첫 화면 예산 여유가
  194B 라 코드를 넣을 수 없다. → 이미 로드되는 `idx-allmenu.js` 에서 숨김.
  「AI와 친구하기」 목록은 처음 열 때 생성되므로 body **childList** MutationObserver 로
  붙는 순간 한 번 가리고 disconnect (⚠️ body class 감시는 홈 전체를 멎게 한 전력 — CLAUDE.md 2장).
- **(범위 축소) 세 PR 을 그대로 다 병합** — 버림. `api-judgment.ts`·`warmup.html` 양쪽에서
  충돌하거나, 병합 순서에 따라 한쪽 PR 의 2차 교정 로직이 다른 쪽에 덮여 조용히 사라진다.
  더 근본적인 #443·#448 을 살리고 이 PR 은 겹치지 않는 부분만 남기는 쪽이 리뷰·회귀 감시
  모두 깨끗하다(제가 만든 브라우저 검사도 웜업/판단력 부분만 지우면 됐다 — 아래 4장).
- **(범위 축소) 이 PR 을 통째로 닫고 새로 만들기** — 버림. 지적 2·6 은 이 PR 에만 있는 유일한
  구현이고, 커밋 이력·PDF 보고서 원본도 여기 있다. 브랜치를 유지하고 안에서 좁히는 편이
  더 적은 변경으로 끝난다.

## 4. 확인한 방법

- 타입체크: `node node_modules/typescript/bin/tsc --noEmit` → 통과 (exit 0)
- 회귀 하니스: `node test-harness/run.mjs --fast` → **PASS 198 / SKIP 20 / FAIL 0**
- 실제 크로미움 렌더 **11건**(로컬 http.server + API 스텁, `/opt/pw-browsers` 크로미움,
  `test-harness/manual/feedback-menu-level-warmup-browser.mjs` — 웜업 관련 5건은 범위 축소로 제거):
  - 비수강생: 중국어 버튼 전부 숨김(드로어·퀵버튼 2곳 + 늦게 생기는 「AI와 친구하기」 목록까지),
    전체메뉴에 중국어 타일 없음 / `mangoi_zh_learner=1` 후 전부 복귀
  - 전체메뉴 「레벨 테스트」 타일 존재, `?placement=1` 진입 시 곧장 「레벨 찾기 1/6」
  - 문제 화면 「레벨 다시 재기」 버튼 노출 + 클릭 시 배치테스트 시작
- `src/api-judgment.ts`·`public/warmup.html` 이 `origin/main` 과 정확히 같은지
  `git diff origin/main -- <두 파일>` 로 확인(빈 diff).

## 5. 남은 것 / 주의할 것

- **PR #443·#448 이 병합돼야 지적 1·3·4·5(웜업 번역·판단력 문법)가 실제로 해결된다.** 이 PR
  만 병합하면 그 네 가지는 그대로 남는다 — 사장님께 병합 순서를 안내할 것.
- 중국어 복습퀴즈가 **처음부터 안 보이는 것이 정상**이 됐다. 중국어 수업 학생에게는
  `/review-quiz-cn.html` 링크를 한 번 안내해야 그 뒤로 메뉴에 보인다(강사·매니저 공지 필요).
- 전체메뉴의 「중국어 발음 코치」 타일은 이번에 안 건드렸다(지시 범위 밖). 같은 조건을 달지는
  사람이 결정.
- **CI 가 GitHub Actions 계정 장애로 멈춰 있었다**(모든 PR 이 3~5초 즉사, 별도 기록 PR #447) —
  이 PR 의 실제 CI 통과 확인은 장애가 풀린 뒤에 이루어진다.
