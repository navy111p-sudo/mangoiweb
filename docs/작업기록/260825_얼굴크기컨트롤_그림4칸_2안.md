# 얼굴 크기 컨트롤 — «레이아웃 그림 4칸»(2안) 적용

**2026-08-25** · 대상 `cloudflare-deploy/public/index.html`(수업화면) · 새 파일 `js/idx-vc-sizebar.js`

---

## 1. 왜 했나

사장님 제보 — 「얼굴화면 크기 조절 기능이 너무 많아. 지면을 너무 차지해서 헷갈려서 그래.」
4안을 만들어 제시(→ [얼굴화면_크기조절_UI_4안_2026-08-25.html](../얼굴화면_크기조절_UI_4안_2026-08-25.html))했고
**2안(레이아웃 그림 4칸)** 으로 진행 지시를 받았다.

### 손대기 전 실측

| | |
|---|---|
| 한 줄에 든 버튼 | **7개** — `flex-wrap:wrap` 이라 좁아지면 **두 줄** |
| 폰 **세로** | 바 전체가 `display:none` → 크기를 바꿀 방법이 **0개** (하단 독 시트를 여는 길뿐) |
| 폰 **가로** | 우상단 고정 + 글자 **8.5px**, `1/4`·`3/4` 를 숨겨 겨우 4개 |
| 같은 기능 중복 | 하단 독 「화면 분할」 시트에 **10개 항목**(`vcScreenSet`) |
| 얼굴 폭 규칙 | breakpoint 별로 **CSS 8개 블록**에 분산 |

핵심은 **버튼 7개가 성격이 다른 두 가지를 섞고 있었다**는 것 —
앞의 넷(`1/4·1/2·3/4·전체`)은 «얼굴이 차지하는 공간» 하나의 축이고,
뒤의 셋(`PIP·솔로·자유`)은 크기가 아니라 수업 중 거의 안 쓰는 특수 모드다.

## 2. 무엇을 했나

- 앞의 넷 → **결과를 그린 SVG 아이콘 4칸**(교재 크게 / 반반 / 얼굴 크게 / 모두 보기)
- 뒤의 셋 → **«⋯» 팝오버** 안으로
- 폰은 라벨만 떼어 **그림만** 남긴다(같은 컴포넌트, 구조 그대로)
- 폰 **세로**에도 되살렸다 — 아래 4장 참고
- 동작은 `js/idx-vc-sizebar.js`(**defer**), `index.html` 에는 마크업·CSS 만

**크기 계산·CSS 폭 규칙에는 손대지 않았다.** 누르면 기존 `vcSetVideoSize()` 를 그대로 부른다.

## 3. 검토했다가 버린 방법

| 버린 것 | 왜 |
|---|---|
| **1안 슬라이더** | 나중에 무단계로 넓힐 계획이 있으면 더 낫지만, 강사가 필리핀·중국인이라 **그림이 언어를 안 타는** 2안이 이겼다. 수업 중 실수로 끌릴 위험도 버튼보다 크다 |
| **4안 분할선 끌기** | 지면 절약은 최고지만 **CSS 폭 규칙 8곳**을 정리해야 하고, 그 파일이 공동 금지구역이라 사고 반경이 크다. 별건으로 미룸 |
| **`.video-size-bar` 클래스 이름 바꾸기** | ⛔ `idx-main.js` 가 이 클래스로 버튼을 찾고(`.video-size-bar button[onclick*="half"]` 등 **6곳**), 자유 크기 모드의 «제목줄 드래그» 도 이 요소다. 이름을 바꾸면 **에러 없이** 조용히 죽는다 → 클래스와 `onclick` 문자열을 **그대로 유지**하고 `.vsb2` 를 덧붙였다 |
| **옛 CSS 규칙을 지워서 풀기** | 폰 세로/가로에서 바를 숨기던 규칙들은 각각 다른 사고의 복구책이다. 지우지 않고 **덮어쓰기만** 했다 |
| **JS 를 `index.html` 인라인에 넣기** | 첫 화면 blocking 예산 여유가 **177바이트**뿐이다. 별도 파일 + `defer` 로 뺐다(마크업·CSS 는 예산에 안 잡힌다) |
| **`setInterval` 로 `vcScreenSet` 이 생겼나 기다리기** | 홈에 머무는 학생 폰을 계속 깨운다. `Object.defineProperty` 접근자로 **대입되는 순간** 낚아챈다 |
| **`MutationObserver` 로 body class 감시** | 2026-07-14 홈 전체 먹통 전력. 대신 함수를 감싸 «그 순간에만» 확인 |
| **공용 `css/mango-theme-light.css` 의 강조색 고치기** | 그 규칙은 다른 화면도 쓴다. **이 컨트롤에만** 진한 파랑을 스코프해서 줬다 |
| **하단 독 시트 10개 항목 이름 통일** | 제안서의 다음 단계이지만 **지시 범위 밖**이라 하지 않았다. 지금 바와 시트의 어휘가 다르다(아래 6장) |

## 4. ⚠️ 폰 세로에 바를 되살린 것 — 일부러 없앴던 것이다

2026-06-02 「휴대폰 세로 수업화면 정리」에서 **일부러** `display:none` 한 것이다.
이유는 «지면을 너무 차지» 였고 **그때는 버튼이 7개에 두 줄**이었다.
그림 4칸은 **한 줄 22px** 이라 그 이유가 사라졌고, 그동안 폰 세로에는 크기를 바꿀 길이 아예 없었다.

- 세로에서 네 크기가 실제로 동작하는 CSS 는 **이미 있었다** — `#vc-portrait-split-modes`(22vh/46vh/64vh/전체).
  하단 독 시트용으로 만들어져 있었을 뿐이다.
- ⏪ **되돌리려면 `<style id="vc-sizebar-2an">` 의 `@media (max-width:900px) and (orientation:portrait)` 블록 하나만 지우면 된다.** 나머지에 영향 없음.

## 5. 밟은 함정 — 문자열 하니스가 하나도 못 잡은 것들

브라우저 실측(`test-harness/manual/vc-sizebar-4seg-browser.mjs`, **90건**)이 잡았다.
타입체크·회귀 하니스 198건은 그동안 **전부 초록불**이었다.

1. 🔴 **`nth-child` 가 «부모 기준»이다.** 옛 규칙
   `body.vc-in-call .video-size-bar button:nth-child(1),(3){display:none!important}`(폰 세로)은
   «바의» 1·3번째가 아니라 **자기 부모의 1·3번째 버튼 아무거나**를 잡는다.
   «⋯»(`.vsb-more-wrap` 의 첫 자식)과 메뉴의 📌PIP(1번째)·⤡자유(3번째)가 **폰 세로에서 조용히 사라졌다.**
2. 🔴 **특이도를 잘못 셌다.** `.video-size-bar.vsb2` 는 클래스가 «2개» 라 **(2,2,0)** 인데,
   옛 규칙 `body.vc-in-call #vc-main-row > #vc-video-pane .video-size-bar` 는 **(2,2,1)** —
   **element 하나 차이로 진다.** `position` 은 먹는데 `flex-wrap:nowrap` 만 안 먹어
   «두 줄로 접힘» 이 그대로 남을 뻔했다. → `.vsb2.vsb2` 로 (2,3,0). 이 저장소에 같은 선례가 있다(`#vc-video-grid#vc-video-grid`).
3. 🔴 **색을 `!important` 로 잡았더니 라이트 테마를 이겼다.** 내 특이도(2,4,0)가
   `css/mango-theme-light.css`(0,2,1~0,4,1)를 이겨 **흰 배경에 흰빛 글자**가 됐다.
   → **색은 내가 정하지 않는다.** 이 블록은 «자리와 크기» 만 갖고 색은 두 테마에 맡긴다.
4. 🔴 **라이트 강조색이 원래 안 읽혔다.** `--mlt-sky-deep`(#0ea5e9) + 흰 글자 = **2.77:1**(본문 기준 4.5 필요).
   옛 7버튼 시절에도 같았지만, 2안에서는 «지금 어느 칸인가» 를 이 색 하나가 지고 간다.
   → 이 컨트롤에만 `#0369a1`(5.93:1). **공용 테마 파일은 안 건드렸다.**
5. 🔴 **`vcScreenSet` 은 «수업에 들어갈 때» 만들어진다.** 부팅 시점엔 `undefined` 라 그냥 감싸면
   **조용히 아무 일도 안 한다**(독 시트로 크기를 바꿔도 바 표시가 안 따라왔다).
   → `Object.defineProperty` 접근자로 대입 순간에 감싼다.
6. `asset_version_harness` — 파일을 고친 뒤 `?v=` 를 안 올려 FAIL. 아직 배포된 적 없는 새 파일이라
   원장 항목을 최종 내용으로 다시 기록했다.

## 6. ⏳ 남은 것 (하지 않았다)

- **하단 독 「화면 분할」 시트와 어휘가 다르다** — 시트는 아직 「1/4 화면·1/2 화면·참가자 전체 보기」,
  바는 「교재 크게·반반·얼굴 크게·모두 보기」. 제안서의 다음 단계이지만 지시 범위 밖이라 남겨 두었다.
  (상태는 어긋나지 않는다 — 시트로 바꿔도 바 표시가 따라오는 것은 확인했다)
- **CSS 폭 규칙 8곳 정리**(4안의 전제) — 별건.

## 7. 확인한 것

| | |
|---|---|
| 타입체크 | ⚠️ **못 돌렸다** — 이 컨테이너에 `cloudflare-deploy/node_modules` 가 없다. 다만 **`.ts` 파일은 한 개도 안 건드렸다**(HTML + 새 JS 뿐) |
| 회귀 하니스 | `--fast` **PASS 198 / FAIL 6** — 손대기 전과 **똑같다**(6건은 전부 이 컨테이너에 esbuild 가 없어서 나는 기존 실패) |
| 첫 화면 예산 | blocking **1562KB 그대로**, 여유 **177바이트 유지**(JS 를 defer 로 뺐으므로) |
| 브라우저 실측 | `manual/vc-sizebar-4seg-browser.mjs` **90건 PASS / 0 FAIL** — PC 1440×900 · 폰 가로 844×390 · 폰 세로 390×844 |
| 홈 화면 누출 | 세 폭 모두 수업 입장 전에는 **안 보이고 겹치지 않음** |

브라우저 검사가 실제로 본 것: 한 줄인가(줄 수·높이) · 그림 4칸이 다 보이는가 · 눌러서 **얼굴 폭이 실제로 커지는가**(PC 257→385→641→1425px, 폰 세로 186→388→540→793px) · 표시가 따라오는가 ·
«⋯» 메뉴가 열리고 **맨 위에 있는가**(`elementsFromPoint`) · 옛 셀렉터가 여전히 찾아지는가 ·
🌐 EN 전환에 라벨·툴팁이 따라오고 **아이콘이 안 죽는가** · **WCAG 대비**(다크 최저 5.17:1 · 라이트 최저 5.93:1).

⚠️ **실제 수업 입장은 헤드리스에서 못 한다**(WebRTC·촬영동의 모달). 화면 상태만 강제로 만들어 쟀다.
**진짜 수업에서 사람이 한 번 봐야 끝난 것이다.**

## 8. ⚠️ 공동 금지구역

`public/index.html` 은 CLAUDE.md 4-2 의 **공동 금지구역**(배포 권한자 A 전용)이다.
사장님 지시(「2안으로 진행해줘」)로 진행했으나, **병합·배포 판단은 배포 담당자 몫**이다.
