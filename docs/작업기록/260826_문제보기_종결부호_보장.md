# 문제·보기 문장에 종결부호 보장 (판단력·시험·복습퀴즈·단어장)

- 날짜: 2026-08-26
- 작업자: Claude Code (사장님 지시)
- 브랜치 / PR: `claude/sentence-period-rules-5i9got` / [PR #501](https://github.com/navy111p-sudo/mangoiweb/pull/501) (병합 `e4f3415`)
- 대상: `cloudflare-deploy/src/sentence-punct.ts`(신설) · `api-judgment.ts` · `api-exam.ts` · `api-games.ts`

---

## 1. 왜 했나

사장님이 판단력 훈련 화면을 보시고 물으셨다 — 「**답변의 각 문장 끝에 마침표를 찍어야 하니? 아니면 나둬도 될까?**」

실제 화면(`mangoi.ai/judgment.html`)이 이랬다.

| 자리 | 끝 |
|---|---|
| 상황문 `Elevator doors close on my coworker's briefcase.` | **`.` 있음** |
| 보기 A `What's wrong with you?` | **`?` 있음** |
| 보기 B `Sorry, that was my fault` | **없음** |
| 보기 C `You're really careless` | **없음** |
| 보기 D `Be more careful next time` | **없음** |

문제는 「마침표가 없다」가 아니라 **한 카드 안에서 규칙이 갈렸다**는 것이었다.
**물음표·느낌표는 «뜻을 지고 가는» 부호라 뺄 수 없다.** 그래서 가지런하게 만드는 길은
**마침표를 찍는 쪽 하나뿐**이고, 「전부 빼기」는 애초에 성립하지 않는다.

거기에 이 화면은 **영어를 가르치는 화면**이다. 보기는 버튼 «라벨» 이 아니라 학생이
실제로 입으로 할 **완결된 문장**이고, 옆의 상황문·해설은 이미 마침표를 찍고 있었다.

그렇게 답을 드리자 사장님이 범위를 넓히셨다 — 「**여기 뿐만 아니라 모든 문제나 답변에
마침표나 물음표 등등 자세히 구석구석 찾아보고 찍어줘.**」

---

## 2. 무엇을 바꿨나

### 정본 — `cloudflare-deploy/src/sentence-punct.ts` (신설, **import 가 하나도 없는 순수 모듈**)

`endSentence()` 하나가 정본이고 **«찍지 않는 쪽» 으로 실패**하도록 짰다.

| 입력 | 출력 | 근거 |
|---|---|---|
| `Sorry, that was my fault` | `Sorry, that was my fault.` | 평서문 |
| `Can you help me` | `Can you help me?` | 영어 의문문 — **첫 낱말**로 판정 |
| `이 문장이 맞나요` | `이 문장이 맞나요?` | 한국어 **종결어미** |
| `🎧 잘 듣고 알맞은 답을 고르세요` | `… 고르세요.` | 한국어 평서·명령 어미 |
| `你好` · `我很好` | **그대로** | 한자는 「。」를 쓴다 |
| `Sounds good 😊` · `Sorry,` | **그대로** | 붙일 수 없는 글자 |
| `The manager ____` | **그대로** | TOEIC 빈칸 |
| `고마워` · `뭐 해` | **그대로** | **반말은 평서·의문 어미가 같아 판정 보류** |
| `Mr. Kim` | `Mr. Kim.` | 약어 뒤에 낱말이 더 있으면 문장 끝이 아니다 |
| `Wait...` · `What's wrong with you?` | **그대로** | 이미 끝났다 |
| `"Sorry"` | `"Sorry."` | 통째로 싸인 따옴표는 **안쪽** |
| `정답: 你好 (nǐ hǎo)` | `정답: 你好 (nǐ hǎo).` | 끝에 달린 괄호 주석은 **뒤** |

보조로 `PUNCTUATION_PROMPT_RULE`(생성 프롬프트에 넣는 지시 한 줄)도 같은 파일에 뒀다.

### 적용한 자리 — **문장인 칸만**

| 파일 | 찍은 곳 | ⛔ 안 찍은 곳 |
|---|---|---|
| `api-judgment.ts` | 보기(`opts4`) · 상황문 · 해설 `why`·`why_ko` | — |
| `api-exam.ts` | ~~`question_text`~~ · `audio_script` | **`choice_a~d`**(TOEIC 식 낱말·구) · 🔴 **`question_text`(2026-08-26 되돌림 — 아래 3-⑦)** |
| `api-games.ts` 복습퀴즈 | `q` · `explain` · `audio_text` · `answer_text` · **`listen` 의 `opts` 만** | **`choice` 의 `opts`** (낱말) · `accept[]`(화면에 안 보임) |
| `api-games.ts` 단어장 | AI `example` · 유의어 `example` | **`korean`**(낱말 뜻) · 단어퀴즈 보기 |

---

## 3. 왜 그렇게 풀었나 — 검토했다가 버린 방법

### ⛔ ① 「한 규칙으로 전부 찍기」 — 가장 위험했던 길

구석구석 찾아보니 **같은 «보기» 칸인데 문장인 곳과 낱말인 곳이 섞여** 있었다.

- 판단력 보기 → `Sorry, that was my fault` (**문장**)
- 단어퀴즈 보기 → `어려운` · `你好` · `go to school` (**낱말**)
- 복습퀴즈 `opts` → **`choice` 는 낱말인데 `listen` 은 «들려준 문장 + 오답 문장»**

즉 **문장/낱말이 칸 이름으로 갈리지 않는다.** 일괄로 찍었다면 「어려운.」·「你好.」 가 되어
**고치려던 것보다 큰 고장**이 났을 것이다. 그래서 자리마다 «문장인가» 를 사람이 판단해 걸었다.

### ⛔ ② 「프롬프트에 한 줄 넣기」로 풀기

문항은 문제은행이 아니라 **LLM 이 요청마다 실시간 생성**한다. 이 저장소에는
**지시만으로는 안 지켜진다**는 반복 실측이 있다 — 단어 수(`situationFitsBand`)와
문법(`grammarCheckPrompt`)이 그래서 «세어 보고 / 다시 읽혀 보고 다시 뽑는» 구조가 됐다.

프롬프트만 넣으면 **절반만 찍힌 상태**가 되는데, 그건 지금보다 나쁘다
(지금은 최소한 「물음표만 있다」는 일관된 규칙이라도 있었다).
`PUNCTUATION_PROMPT_RULE` 은 **보조로만** 넣고 정본은 결정론 함수다.

### ⛔ ③ 한국어 어미 판정을 넓히기

「고마워」·「뭐 해」 같은 **반말은 평서문과 의문문의 어미가 같다.** 넓히면
**반말 의문문에 «.» 이 박힌다.** 그래서 확신이 서는 어미(`까`·`나요`·`가요`… / `요`·`다`·`오`…)만
넣고, 「…단어는」처럼 판정이 안 서면 **손대지 않는다.**

### ⛔ ④ 닫는 부호를 무조건 벗겨 내고 안쪽에 찍기 — **실제로 밟았다**

처음엔 「끝의 닫는 부호를 벗겨 내고 그 안쪽에 찍는다」로 짰다. `"Sorry"` → `"Sorry."` 는 맞는데,
`정답: 你好 (nǐ hǎo)` 가 **`정답: 你好 (nǐ hǎo.)`** 가 됐다 — 괄호 주석 안에 마침표가 들어간 것이다.
지금은 **통째로 한 쌍의 따옴표로 싸인 문장만** 안쪽에 넣고, 나머지는 **뒤**에 찍는다.

### ⛔ ⑤ `listen` 의 대본과 보기를 따로 처리하기

영어 복습퀴즈 프롬프트에 「듣기 보기에는 **들려준 문장 자체**가 정답 보기로 들어가야 한다」가 있다.
`audio_text` 만 다듬고 `opts` 를 그냥 두면 **정답 보기와 대본이 «마침표 하나 차이» 로 어긋난다.**
그래서 `listen` 일 때만 둘을 함께 다듬는다(`choice` 는 낱말이라 그대로).

### ⛔ ⑥ 이미 저장된 옛 문항을 일괄 UPDATE

개발·운영이 **같은 D1** 이라 사람 확인 없이 하지 않았다. 이번 변경은 **«앞으로 생성되는» 문항에만** 적용된다.

### 🔴 ⑦ 시험 문제문 — **한 번 찍었다가 되돌렸다** (2026-08-26 · PR #530)

이 문서를 쓴 뒤, 「옛 문항이 몇 건이냐」는 물음에 답하려고 D1 을 **SELECT** 하다가
실서비스 데이터에서 나왔다. `mt_questions` 10건 중 **5건이 «빈칸이 끝에 오는 미완성 문장»** 이었다.

| 문제문 | 보기 | 정답 |
|---|---|---|
| `I like to eat` | pizza / **apple** / book / dog | B |
| `The cat is very` | big / **happy** / old / car | B |
| `I go to school by` | car / **bus** / bike / plane | B |

`I like to eat` **+ `apple`** 로 완성되는 구조다. 프롬프트가 그렇게 시킨다
(`"TOEIC-style incomplete sentence"`). 여기에 마침표를 찍으면 **문제가 깨진다.**

그런데 같은 칸에 완결된 의문문(`What do I like?` · `Do I like dogs?`)도 섞여 있어
**둘을 가를 방법이 없다.** 빈칸이 `____` 로 오면 `endSentence` 가 걸러 내지만
**그냥 잘려서 오면 마지막 글자가 로마자라 정상 문장과 구별되지 않는다.**

⚠️ **이 문서의 원칙(「확신이 없으면 찍지 않는다」)을 정작 이 칸에서 어긴 것이다.**
`question_text` 에서 `endSentence` 를 뺐다. 듣기 대본(`audio_script`)은 완결된 발화라 그대로 둔다.

✅ **실제 피해는 없었다** — 그 5건은 PR #501 배포 «전»에 만들어져 마침표가 안 찍힌 상태였다.
위험했던 것은 새로 출제될 문항뿐이고 그 경로를 막았다. D1 은 SELECT 만 했다.

📌 **교훈** — 새 자리에 `endSentence` 를 쓰기 전에 **「이 칸이 완결된 문장만 담는가」를
실제 데이터로 먼저 확인할 것.** 프롬프트에 답이 적혀 있는 경우가 많다.

### ✅ 채점을 건드리지 않는다는 것을 먼저 확인했다

`answer_text` 를 다듬어도 안전한 이유는 채점 정규화 `rqNorm` 이
`replace(/[^a-z0-9가-힣一-鿿\s']/g, ' ')` 로 **구두점을 통째로 지우기** 때문이다.
같은 이유로 `countWords`(구두점 토큰 안 셈)·`normSituation`(영숫자만 남김)도 영향이 없다.
⚠️ **그 전제가 깨지면 채점이 흔들린다.**

---

## 4. 확인한 방법

| 검사 | 결과 |
|---|---|
| 타입체크 `tsc --noEmit` | **종료코드 0** |
| 신규 `test-harness/sentence_punct_harness.mjs` | **PASS 55 / FAIL 0** — 문자열 검사가 아니라 **함수를 실제로 돌린다** |
| 전체 하니스 `run.mjs --fast` | **PASS 212 / FAIL 0 / SKIP(E2E) 20** (병합 후 재실행) |
| CI 배포 게이트 | **success** — 03:26:44 → 03:28:22 (98초) |
| 배포 | **run #2702 success** (03:29:25 → 03:32:58) |
| **후속** [PR #530](https://github.com/navy111p-sudo/mangoiweb/pull/530) — 시험 문제문 되돌림 | CI **success**(101초) · 병합 `c431f3f` · 배포 **run #2729 success**(09:40:58 → 09:43:22, 취소 없이 직접 배포) · 하니스 **56/0** · 전체 **PASS 217 / FAIL 0** |

**배포 판정 근거** — 내 병합의 run #2701 은 «cancelled» 로 떴다. 5분 안에 PR 세 건(#501·#503·#504)이
몰려 `concurrency` 가 대기 run 을 정리한 것이라 **실패가 아니다.** 판정은 포함 관계로 했다:

```
git merge-base --is-ancestor e4f3415 4599094   →  ✅ 포함됨
```

`4599094` 가 성공한 run #2702 의 head 이고 그 안에 내 커밋이 들어 있다.

⛔ **화면은 확인하지 못했다.** 프록시가 `mangoi.ai` 를 막는다(실측: `CONNECT tunnel failed, 403`).

### 작업 중 하니스 기대값을 한 번 틀렸다

`「고마워」` → `「고마워.」` 를 기대하는 검사를 썼는데 실패했다. **코드가 아니라 시험이 틀린 것**이었다 —
반말은 판정을 보류하는 것이 설계다. 기대값을 `「Thank you」` 로 바꾸고, 「반말은 보류한다」를
검사 항목으로 **명시적으로 추가**했다.

---

## 5. 남은 것 / 주의할 것

### ⏳ 사람이 확인할 것

판단력 훈련에서 **문제를 새로 한 번 뽑아** 보기 네 개가 모두 `.` 또는 `?` 로 끝나는지 봐야 한다.
⚠️ **새로고침만으로는 안 보인다** — 이미 뽑아 둔 문제는 그대로이고, **새 문제를 받아야** 반영된다.

### ⏳ 옛 데이터는 그대로다 — **세어 봤다** (2026-08-26 · SELECT 만)

`endSentence` 의 판정 규칙을 SQL 로 옮겨 세었다. **실제로 바뀔 칸은 1,265개**다
(「종결부호가 없는 칸」과는 다른 숫자다 — 한자·이모지·`:`·반말은 함수가 손대지 않는다).

| 대상 | 전체 | 바뀜 |
|---|---:|---:|
| 복습퀴즈 `listen` 보기 | 864 | **680** |
| 복습퀴즈 `answer_text` | 325 | **266** |
| 복습퀴즈 `audio_text` | 216 | **170** |
| 복습퀴즈 `explain` | 666 | **108** |
| 복습퀴즈 `q` | 666 | **28** |
| 단어장 `example` | 205 | **8** |
| 시험 `question_text` | 10 | ~~5~~ **대상 아님**(위 3-⑦) |

행 단위로는 복습퀴즈 묶음 34개 중 **22개**(활성 29), 단어장 8행.

⚠️ **듣기 대본과 `listen` 보기는 반드시 함께** 고쳐야 한다 — 정답 보기가 대본과
«마침표 하나 차이» 로 어긋나면 안 된다.

📌 **손대면 안 되는 것도 세었다** — 복습퀴즈 `choice` 보기 **449건이 한 건도 빠짐없이 한자**였다.
우연이 아니라 코드가 영어 퀴즈에서 `choice` 를 0개로 강제하기 때문이다
(`nChoice = lim(c.choice, isZh ? 4 : 0)`). 「`choice` 는 낱말, `listen` 은 문장」이라는
이 작업의 설계가 데이터로 확인된 셈이다. 단어퀴즈 문제문 150건은 **전부 이미 `?`** 로 끝난다.

📌 **단어장 8건은 진짜 데이터가 아니었다** — 계정이 `__qa_vocab_…`·`guest`·`guest_7cdede…` 이고
단어가 `test.mangoi.co.kr`·`AI`·`EN`·`Q.`·`A.`·`A. AI` 다. QA·게스트 시험 흔적이라 고쳐도 보는 사람이 없다.

⛔ **판단력 훈련 `judgment_events` 는 대상이 아니다** — 문제은행이 아니라 «학생이 그때 무엇을 골랐나»
하는 **기록**이다. 고치면 기록을 조작하는 것이라 세지도 않았다.

🔴 **정리는 하지 않기로 했다**(2026-08-26 사장님 판단) — 새 문항은 이미 가지런하고,
옛 복습퀴즈는 시간이 지나면 새 것으로 대체된다. 하려면 백업 → 1묶음 시험 →
사람이 화면 확인 → 나머지 순으로. 운영 DB 라 UPDATE 는 사람이 결정한다.

### ⚠️ 이 모듈을 새 자리에 쓸 때

**«문장인가» 를 먼저 판단하고 부르세요.** 낱말 보기에 부르면 「어려운.」 이 된다.
`endSentence` 는 한자·이모지·빈칸을 스스로 걸러 주지만, **한국어 낱말**(「어려운」)은
평서 어미로 끝나면 마침표를 붙일 수 있다 — **호출하지 않는 것이 방어선**이다.

### ⚠️ 담당 영역

`api-games.ts` 는 CLAUDE.md 4-2 상 **B 담당**이다. 사장님 지시로 함께 고쳤으니 공유가 필요하다.
`src/index.ts`(공동 금지구역)는 **손대지 않았다.**

### 📌 CLAUDE.md 2장에 함정 한 줄을 넣었다 (PR #501 에 포함)

「문제·보기 끝에 **마침표를 일괄로 찍으려** 할 때」 — 낱말 보기가 고장난다는 것,
반말 보류, 괄호 주석의 부호 자리, 프롬프트로 풀면 안 되는 이유, `rqNorm` 전제까지.
