# 2026-08-27 — 지난 10일 작업분 전수 리뷰 + 버그 일괄 수리

## 왜 했나

사장님 요청: 「지난 열흘 작업한 것이 너무 많아 코드 정리가 필요하다. 버그를 없애고 스파게티를 정리해 달라.」
지난 10일(8/17~8/27) 커밋이 **1,051개** — 통째로 리팩토링하는 대신, 영역별 리뷰
에이전트 7개(서버 TS / 화상수업·홈 JS / 관리자 UI 3벌 / 학생 콘텐츠 / 정리목록)로
전수 검토하고 **확인된 실버그만** 골라 수리했다.

> ⚠️ 이 저장소는 «스파게티처럼 보이지만 일부러 그렇게 둔» 코드가 많다(CLAUDE.md 2장).
> 구조 개편·모양 정리는 하지 않았고, 의도된 중복(enroll-ops 의 LMS 제외식 복제,
> i18n 엔진 2벌 등)은 건드리지 않았다.

## 무엇이 나왔나 — PR 3벌

### PR #550 (병행 세션) — 강사 일지 재열람
어제 시작돼 있던 마이마이 8/26 요청분. 이 세션의 리뷰가 결함 3건을 찾아 수리했고
(빈 문자열 바인드 반쪽 가드 · AI 자동보고/일괄평가 행이 «내가 쓴 일지» 로 둔갑 ·
DB 실패가 «일지 없음» 으로 위장), `/api/eval/:id` 가 강사·관리자 확인 열람까지
`viewed_by_parent=1` 로 찍던 것도 self(서명토큰)일 때만으로 좁혔다.
병행 세션이 이 수리분까지 포함해 커밋·PR 을 열었다(같은 작업트리 공유).

### PR #551 — 금지구역 밖 일괄 수리 (서버 9건 · 화면 12건)
굵은 것만:
- **회계 월간 대사** `reconcileMonth` 에 `else verdict='alert'` 가 없어 **가장 크게
  어긋난 달일수록** 배너가 안 뜨고 마감 경고도 건너뛰던 것. 시차창 KST 하루 어긋남.
  `deposit_transfer` 에 확인된 자금이체(케이씨피M)가 실리던 것(1-3장 금지 부활 경로).
- **영업·인사**: 「3개월 유지 확인」 버튼 한 번에 계약의 나머지 칸이 씻기고 rep_id 가
  화면 드롭다운의 담당자로 **재배정**되던 것(유지 전용 부분 UPDATE 신설).
  `todayISO` 가 UTC 라 한국 아침에 «오늘»=어제. `program_started_at` 영구 수정불가.
  비본사 lead UPDATE 소유권 미검사. 징계판정의 `'H'>숫자` 정렬 오인.
- **비번찾기 4곳 대소문자**: 로그인은 NOCASE 인데 비번찾기·staff-reset·staff-contact 는
  정확일치 — «로그인은 되는데 비번찾기만 조용히 실패»(Mangoi_167 사고의 잔재).
  UPDATE 는 반드시 DB 표기(trow.username)로(0건 UPDATE 무음 함정).
- **주간스케줄**: 자리표시(LMS·시드) 칸에 진짜 수업을 배정할 수 있게 된 뒤로 같은
  키에 두 행이 오면 병합/덮어쓰기로 수업이 사라지거나 «LMS 점유» 로 둔갑 → 수업 우선.
  `addSlot` 이 서버 id 를 버려 **8/24 「삭제 서버연동」 커밋이 통째로 죽은 코드**였던 것.
  마법사 저장분(숫자 요일)이 주간 캘린더에서만 안 보이던 것(normDow 숫자 지원).
- **adm-core**: 「보이는 항목 전체 삭제」가 중복만보기 필터를 빼고 재계산해 **안 보이는
  신청까지** 복구불가 삭제(렌더러가 그린 `__enShown` 정본화 — `__ltShown` 규칙 확산).
  레벨테스트 🗑 onclick 에 공개폼 학생이름 문자열 주입(저장형 XSS 벡터).
  c24 지출결의 위임 리스너가 첫 로드의 클로저를 영영 보던 것.
- 죽은 링크 `/admin/sales-hr`(확장자 없음) 3곳, manager `[hidden]` 안전망 + 언어
  토글 재도색 누락 2구역, promo 포스터, student.html time 값, .gitignore 정비.

### PR #552 — ⚠️ 공동 금지구역 포함, 병합 전 사람 확인
- `/api/admin/vc/quality`·`relay` **관문 등록** — 핸들러만 있고 라우팅 허용목록·위임
  가드에 빠져 «조용한 404»: 회선품질 패널과 **중국 회선 강제 릴레이(vc_relay_force,
  class-851 대책)가 켤 방법이 없는 죽은 코드**였다. 동시에 강사 차단 목록에
  `/api/admin/vc/` 추가(전사 이름+지표 노출 차단 — trap-check 가 잡아 줌).
- **증폭 이중재생**: unmute 재시도 사다리(vcAddRemoteVideo)가 세 번째 자가복구
  경로였다 — 증폭 중 요소를 되살려 요소+게인 «두 번»(최대 4배).
- **공유 중 입장자 잘림**: 늦입장·재접속자는 `screen-share-state` 를 못 받아 배지가
  없고 ⑧ cover 예외를 못 받는다 → 공유자가 user-joined 4초 뒤 resync 재방송
  (**DO 무접촉** — DO 에 상태 저장하는 안은 금지구역·사고반경 때문에 버림).
- idx-vc-mobilefix 4건(얼굴효과 끄기 거짓경고 · ⑩ probe 영구잠김 · EN 판정 죽은 이름 ·
  ⑪ 첫 기록이 학생 선택 삭제).

## 검토했다가 버린 방법

- **전면 리팩토링** — 2장 함정 표가 증언하듯 «정리» 가 곧 사고인 저장소. 버그만 수리.
- **화면공유 상태를 DO 에 저장·재생**(pdfState 방식) — 올바른 근본 해결이지만
  video-call-room.ts 는 공동 금지구역이고 수업 절대 안 끊김 원칙상 사고반경이 크다.
  공유자 재방송(클라이언트만)으로 대체, DO 저장은 별건 과제로 남김.
- **학교출석 KPI risk 경계를 `<70` 으로** — 서버 fail 필터와 맞추려다, 화면 라벨
  네 곳이 전부 「70% 이하」로 통일돼 있음을 확인하고 **되돌렸다**(제품 언어가 정본).
- **retention 조인의 account_uid 보정, computeAutoScores 실패→0점, 분기 손익
  추정분 줄 등** — 실버그이나 회계·평가 화면의 뜻이 걸린 것이라 아래 «남긴 것» 으로.

## 확인한 방법

- tsc 0 · 하니스 fast PASS 221~222 · **trap-check 3회**(PR 마다 — #552 에서 빨강 1건
  «강사 차단 누락» 을 실제로 잡아 반영).
- ⚠️ 로컬(Windows) first_paint_budget FAIL 은 **CRLF 체크아웃 측정 부풀림** — CI(LF)는
  같은 커밋에서 초록. 로컬 빨강을 근거로 기준선을 올리지 말 것.
- 브라우저 실측(manual/ 검사)은 돌리지 않았다 — weekly-schedule·sales-hr 화면 변경은
  병합 후 사람이 한 번 봐야 끝난 것이다(4-1-1).

## 남긴 것 (수리 안 함 — 판단 필요)

1. **sales-hr retention 조인이 `attendance.user_id`(임시번호)를 씀** — 망고아이 방으로
   수업하는 학원은 유지 자동판정이 영영 «판정 불가». `account_uid` OR 보정 필요.
2. **computeAutoScores 가 DB 실패를 0점으로 굳힘**(확정 평가에 스냅샷됨).
3. **분기 손익 혼합 분기에서 판관비 내역 합 ≠ 합계**(추정분 줄 없음, opex_source 'actual').
4. **월간 드릴다운 «미분류 출금» 이 분류 후에도 그대로 보임**(resolveExpenseAccount 미적용).
5. **enroll.html 문자 갱신 링크가 대리점 단가 학생에게 본사 기본가를 표시**(결제 신뢰 문제
   — 견적을 토큰으로 풀려면 서버 지원 필요).
6. sentence-punct 「Have a great day」→ `?` 오판 가능 / schedule-conflict 긴수업 상한의
   kind 혼합(현재 상한 0이라 무해) / 대시보드 «회계 리포트 CSV 성격미확인 열» /
   sales-hr SVG 라이트테마 fills / adm-r25 손자 i18n(이월) / idx-vc-dupghost 상주
   interval(하니스가 잠근 기존 동작) / eval_written 그룹수업 같은 eval_id / manager
   `#toTeacher` hidden(이월 — [hidden] 안전망으로 함께 해소됨).

## 정리 후보 (파일 삭제는 사람 결정 — 실행 안 함)

미추적 잡동사니 ~2.0GB 전수 조사 완료(별도 보고). `cloudflare-deploy/public/` 안에는
미추적 파일 **0건**(배포 오염 없음). `.gitignore` 는 #551 에서 정비.
