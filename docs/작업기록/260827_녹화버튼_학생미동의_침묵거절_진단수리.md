# 260827 — 「녹화 버튼이 안 돼요」 = 학생 미동의 «침묵 거절» 진단 + 배지에 사유 표시

## 1. 왜 했나

Teacher Shas 제보: 「녹화 버튼을 눌렀는데 시작이 안 된다. 새로고침해도 녹화 옵션이 안 나타난다.」
스크린샷: 배지가 「Starting…」 → 「REC OFF · Tap to start」로 되돌아감.

## 2. 진단 — 잰 값과 판단을 구분해 적는다

**잰 값 (전부 SELECT · wrangler tail 실측):**

- Shas = 계정 `mangoi_171` (카페24 강사번호 193, 원부 17). `attendance` 실측: `class-971-20260827` 에 20:51 입장, 20:54 두 번 재입장(제보의 「새로고침」과 일치).
- `recordings` 에 **class-971 행이 0건** — 강사·학생 어느 쪽 시도도 DB 에 닿지 못함. (참고: Shas 는 8/24 까지는 `mangoi-class` 에서 정상 녹화했음 → 브라우저·권한 문제 아님)
- 같은 필리핀 사무실 IP(216.247.53.158)의 Teacher Hannah 는 같은 시간대(21:18) `class-1007` 에서 정상 녹화 시작·`upload/part` 성공(wrangler tail 실측) → **IP 속도제한·네트워크 차단 배제**.
- `consents` 실측: class-971 의 학생 `ubckt02`(김수희)가 21:04:27 에 **`recording_consent=0`, `guardian_status='declined'`** — 입장 시 동의 팝업을 거부. (20:51 시점엔 동의 행 자체가 없었음 — 그것도 차단 사유)
- Krystel 의 `class-979-20260827` 도 녹화 행 0건 — 학생 `ubckt01` 은 동의 행이 아예 없음. **같은 증상 한 건 더.**

**판단:** `/api/recordings/start` 의 동의 게이트(2026-08-12 사장님 결정 «동의 없으면 녹화 금지», `api-mango.ts` 3492행 부근)가 의도대로 거절한 것. 거절은 재시도 폭주를 막으려고 **일부러 HTTP 200 + `{ok:false, error:'consent_required'}`** 로 내려온다. 클라이언트(`mango-rec.js`)는 배지 탭도 `startRecording({auto:true})` 로 부르는데 auto 면 실패 알림이 전부 억제 → 예외도 알림도 없이 «눌러서 시작» 으로 되돌아간다. **몇 번을 눌러도 절대 성공할 수 없는 상태가 «고장» 으로 제보된 것.**

## 3. 무엇을 고쳤나 (게이트는 안 건드림)

`cloudflare-deploy/public/js/mango-rec.js`:

- `consentBlocked` 플래그 신설 — `startRes.error === 'consent_required'` 일 때만 켜짐. 성공하면/방을 나가면 꺼짐(다음 수업의 다른 학생까지 ⛔ 를 끌고 가지 않게).
- 배지가 «본문 글자» 로 사유를 말한다: 「⛔ 녹화불가 · 학생 미동의」 / 「⛔ No student consent — REC blocked」(진한 빨강 `#991b1b`). title 툴팁에도 긴 설명(한/영) — 단 툴팁은 마우스 전용이라 본문 글자가 정본.
- 탭 재시도는 그대로 살려 둠 — 학생이 뒤늦게 동의하면 그때부터는 성공한다.
- 일반 실패(`ok:false` 에 error 없음)는 기존 «눌러서 시작» 그대로 — 멀쩡한 실패까지 «학생 탓» 으로 표시하면 안 됨.
- 수동(비-auto) 경로의 alert 도 미동의일 때는 사유를 말하게 함.
- CSS 는 시트 **맨 끝**에 이중 클래스(`#mango-rec-badge.mango-rec-off.mango-rec-consent`)로 — `.mango-rec-off` 의 `background !important` 가 데스크톱·모바일 미디어쿼리 두 벌이라 «뒤에 온 것» 이어야 이긴다.
- `index.html` `?v=50 → 51` 범프.

## 4. 검토했다가 버린 방법

- **게이트를 완화(비동의여도 녹화)** — 사장님 결정(2026-08-12) 위반. 논외.
- **auto 실패 시 alert 띄우기** — 자동녹화는 입장 «즉시» 돌므로 수업 시작마다 팝업이 뜬다. 배지 상태로만.
- **미동의 시 주기적 자동 재시도** — 학생이 같은 세션에서 동의를 뒤집는 경로가 없어(mango-consent 는 한 번 물으면 재질문 안 함) 헛 API 콜만 쌓인다. 수동 탭 재시도로 충분.
- **차단 사유를 title 툴팁에만** — 폰에서 툴팁은 영영 안 보인다(CLAUDE.md 2장 함정). 본문 글자로.

## 5. 확인한 방법

- `test-harness/rec_restart_harness.mjs` 에 **②-b 절 신설**(mango-rec.js 를 가짜 DOM 에서 실제 실행): 미동의 거절 → `mango-rec-consent` 클래스 + 본문 글자에 사유 + 재시도 가능 + 일반 실패로 돌아가면 표시 걷힘. **19/19 PASS** (되돌리면 실제로 FAIL).
- `tsc --noEmit` 통과(TS 변경 없음), `node test-harness/run.mjs --fast` **PASS 224 / SKIP 20 / FAIL 0**.
- `asset-versions.json` 에 `?v=51` 원장 기록 포함 커밋.

## 6. 남긴 것 (사람 판단)

- **운영 답변**: Shas·Krystel 께 «학생(학부모)이 동의 팝업을 거부/미응답이라 정책상 녹화가 막힌 것, 고장 아님» 안내. 학생 쪽 동의를 받으면 즉시 정상.
- `ubckt01`(조연희)·`ubckt02`(김수희) — 원당 계열 계정으로 보이는데 실제 거부인지 실수 클릭인지는 사람이 확인할 일. 동의는 학생 화면에서 다시 받아야 한다(서버에서 UPDATE 로 «동의 처리» 하지 말 것 — 실제 학생 데이터).
- 수강신청 확정으로 새로 생기는 `class-*` 수업은 서버가 스케줄에서 학생을 채우므로 이 게이트가 확실히 걸린다 — 동의 없는 학생의 수업마다 같은 제보가 올 수 있음(이제 배지가 사유를 말하므로 오보고는 줄어들 것).
