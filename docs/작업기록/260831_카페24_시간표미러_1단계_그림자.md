# 카페24 → 망고아이 시간표 미러 1단계(그림자) + 강사 번호 대조표 + 명부 정리

- 날짜: 2026-08-31
- 작업자: 사장님(navy111p) 지시 · Claude Code
- 브랜치 / PR: `claude/janice-instructor-schedule-issue-9oetil` / [PR #634](https://github.com/navy111p-sudo/mangoiweb/pull/634)
  · 병합 `3c7329d` · 배포 [run #2831](https://github.com/navy111p-sudo/mangoiweb/actions/runs/33385652731) **success** (11:09:32→11:11:58 UTC)

---

## 1. 왜 했나

「Janice 강사 수업 스케줄이 안 뜬다」는 제보에서 출발했습니다. 관리자 「강사 스케줄
(주간 통합 캘린더)」에서 강사 필터를 JANICE 로 놓으면 **수업 0개**로 나왔습니다.

운영 D1 을 직접 조회해 보니 **화면 버그가 아니었습니다.**

- 그 캘린더가 읽는 것은 `class_schedules` 인데, JANICE(원부 28번) 행은 33건 있지만
  **전부 `status='cancelled'`** 이었습니다(26건 `user_id='lms'` 옛 LMS 자리표시,
  7건 `type_seed` 시연 시드).
- 반면 실제 수업은 카페24 쪽 `attendance` 에 **65건**(6/22~8/31, 카페24 강사번호 37)
  살아 있었습니다.

그리고 **Janice 만의 문제가 아니었습니다.** 재직 강사 29명 중 `class_schedules` 에
살아 있는 행이 있는 강사는 **7명뿐**이고, 카페24에서 실제로 수업 중인데 망고아이
시간표가 빈 강사가 **17명**이었습니다.

⚠️ 처음에 저는 그 7명을 **「이관 완료」로 잘못 적었습니다.** 사장님이 「그건 파일럿
테스트용으로 잡은 것」이라고 정정해 주셨고, 실측으로 확인했습니다 — 그 52건은 전부
`source='adm-enroll:78~85'` 이고 생성 시각이 **8/27~9/1** 입니다. 즉 최근에 손으로
넣은 파일럿 수업이지 카페24 이관분이 아닙니다.
**⟹ 카페24에서 이관된 수업은 0건이고, 이관 대상은 23명입니다.**

이어서 사장님이 진짜 목표를 말씀하셨습니다.

> 파일럿이 끝나면 카페24에서 실시간으로 도는 수업을 mangoi.ai 로 덮어씌우고 싶다.
> 어느 시점에 «한날 한시»에 다 같이 써야 하는데, 그날 급하게 하는 것보다 미리부터
> 잘 덮어씌워지는지 확인하고 싶다. 그리고 지금은 파일럿 수업을 LMS 와 망고아이에
> 두 번 잡아야 해서 일이 두 배다.

---

## 2. 무엇을 바꿨나

### 2-1. 코드 (1단계 — 읽기만)

| 파일 | 내용 |
|---|---|
| `cloudflare-deploy/src/c24-mirror.ts` **(신규 389줄)** | 미러 엔진. **`class_schedules` 에 쓰는 문장이 0건**. 카페24 `:Class` 를 읽어 «만들었다면 어떤 판정이 났을지»만 계산 |
| `cloudflare-deploy/src/accounting-reports.ts` | 라우터에 `p === 'c24-mirror'` 한 갈래 + import 2줄 |
| `test-harness/c24_mirror_harness.mjs` **(신규 56건)** | 판정 함수를 **실제로 돌려** 검증 |

새 API: `GET /api/admin/reports/c24-mirror?since=&until=`
기본 창은 **오늘 ~ +14일**(KST), 판정 9종과 날짜별 집계를 돌려줍니다.

```
ok · already · update · manual_locked · diverged
no_teacher · no_student · conflict · not_whitelisted
```

핵심 함수는 `planMirror()` 하나이고 **순수 함수**입니다 — DB·Neo4j 없이 그대로
돌려볼 수 있어서 하니스가 그걸 실제로 실행합니다.

### 2-2. 문서

| 파일 | 내용 |
|---|---|
| `docs/강사_이관대조표_v2_20260831.xlsx` | **정본.** 강사 30명 × (그룹·로그인 아이디·계정 유무·원부/카페24/프로필 번호·파일럿 수업·카페24 8월 수업·9월 이후 예약) + 담당자/진행상태 기입란 |
| `docs/강사번호_대조표_20260831.xlsx` | v1(옛 판). 「완료 7명」 표기가 틀렸으므로 v2 를 보세요 |
| `docs/INDEX.md` | 위 둘 등록 |

### 2-3. 운영 D1 변경 3건 (지시받아 실행 — 전부 되돌릴 수 있는 «숨김»)

| 대상 | 한 일 | 되돌리는 법 |
|---|---|---|
| Teacher **WAN** | `teachers` 30번 신규 등록 + `teacher_account_links` 에 `mangoi_174` 연결 | `DELETE FROM teachers WHERE id=30` + 링크 삭제 |
| **HT FARRAH**(원부 3) | `active` 1 → **0** | `UPDATE teachers SET active=1 WHERE id=3` |
| **Mo**(프로필 17, 미국 오전반) | `status` 활동중 → **퇴사** | `UPDATE teacher_profiles SET status='활동중' WHERE id=17` |

WAN 은 8월 이후 카페24 수업이 **193건**인데 강사 원부에 없어 **배정 자체가 불가능**한
상태였습니다. 행을 지운 것은 하나도 없습니다.

---

## 3. 왜 그렇게 풀었나 — 검토했다가 버린 방법

### ⛔ 「아침 6시에 한 번 덮어쓰기」 (사장님 최초 안)

버렸습니다. 실측상 **9월 수업이 하루 2~25건뿐**인데 실제 수업은 하루 140건쯤입니다.
카페24 예약이 **며칠 전~당일에 채워진다**는 뜻이라, 6시에 한 번만 돌면 그 뒤에 잡힌
수업은 그날 못 넘어옵니다.
→ **15분마다 «오늘~모레» 좁은 창** + 밤 3시 «±두 달» 넓은 창으로 설계했습니다.

🔴 **덤으로, 6시 cron 은 애초에 만들 수 없습니다** — Cloudflare 계정 cron 한도가 5개인데
`wrangler.toml` 이 이미 5개를 다 쓰고 있습니다. 기존 15분 감시견과 밤 동기화에 얹는 것이
유일한 길이자 더 나은 설계였습니다.

### ⛔ 카페24로 되쓰기(양방향 동기화)

만들지 않았습니다. 사고 반경이 너무 큽니다. **한 방향(카페24 → 망고아이)만** 합니다.

### ⛔ 1단계부터 바로 쓰기

「전환일에 급하게 하지 않으려면」이 요구의 핵심이므로, **위험 0인 그림자 단계**를
먼저 뒀습니다. 매일 성적표만 내고 아무것도 안 만듭니다. 며칠 돌려 보면 전환일에 무슨
일이 날지 미리 보입니다.

### ⛔ 화이트리스트를 코드에 박기

사장님이 「스케줄이 바뀌거나 새 참여자가 들어오면 다른 강사가 할 가능성이 높다」고
하셔서, **표(`c24_mirror_teachers`)로** 뒀습니다. 배포 없이 화면에서 켜고 끕니다.
모드는 `off` / `whitelist` / `all` 셋이고 기본값은 가장 안전한 `off` 입니다.

### ⛔ 도장(수동 수정 표시)을 자동으로 떼기

「카페24가 정본이되 **사람 손이 이긴다**」가 사장님 결정입니다. 그런데 이 규칙의 뒷면은
**「한 번 손대면 그 수업은 영영 카페24를 안 따라간다」** 입니다. 그렇다고 시스템이
자동으로 도장을 떼면 사람이 일부러 고친 것을 되돌리는 셈이라 더 나쁩니다.
→ **어긋남 목록(`diverged`)** 으로 매일 보여 주고, **되돌리기는 사람이** 누릅니다.

### ⛔ `loadCafe24TeacherMap()` 을 api-admin.ts 에서 import

같은 판정이 거기 이미 있지만 import 하면 **순환 참조**가 생깁니다
(`api-admin` → `accounting-reports` → `c24-mirror` → `api-admin`).
→ `enroll-ops.ts` 가 쓰는 방식대로 **판정을 복제하고, 두 함수가 같은 답을 내는지
하니스가 실제로 돌려서 대조**합니다(F절 — api-admin 의 함수를 오려 내 실행).

### ⛔ 새 API 를 `/api/admin/c24-mirror` 로

`src/index.ts`(공동 금지구역)의 게이트 3곳을 고쳐야 합니다.
→ **`/api/admin/reports/` 밑**에 뒀습니다. 그 접두사는 인증·라우팅·강사차단에 이미
등록돼 있어 금지구역을 **한 줄도** 안 건드렸습니다.

### ⛔ 강사 번호를 숫자로 잇기

카페24 강사번호(9~196) ≠ 원부 `teachers.id`(1~30)이고 겹치는 자리에서 **다른 사람**입니다.
→ `teacher_payroll_auto`(번호와 이름을 함께 받은 유일한 표)로 **이름을 거쳐** 잇고,
정규화 후 **유일하게 맞을 때만** 채웁니다. 애매하면 비워 두고 리포트에 남깁니다.

---

## 4. 확인한 방법

- `node node_modules/typescript/bin/tsc --noEmit` → **exit 0**
  (⚠️ 이 컨테이너에는 `node_modules` 가 없어 `npm i --no-save --legacy-peer-deps typescript` 로
  타입스크립트만 따로 깔아 돌렸습니다)
- `node test-harness/run.mjs --fast` → **PASS 237 · SKIP 20 · FAIL 0**
- 새 하니스 `c24_mirror_harness.mjs` **56건** — 모드 3종 · 도장 · 못 이음 · 시각 변환 ·
  이름 정규화 대조 · 창 경계 · 배선까지
- CI 배포 게이트 [run #725] **success** (122초)
- 배포 [run #2831] **success** — 두 워커 + 헬스체크 + TURN 점검 전부 통과
- 배포 확인은 커밋 해시만 믿지 않고 **`git show origin/main:` 로 파일 내용**을 확인
  (`c24-mirror.ts` 389줄 · `accounting-reports.ts` 배선 2곳)
- D1 은 **조회(SELECT)만** 했습니다. 위 2-3 의 3건은 사장님 지시로 실행한 것입니다

### ⏳ 확인하지 못한 것

- **배포된 API 의 실제 응답을 못 봤습니다.** 프록시가 `mangoi.ai` 를 막습니다
  (CLAUDE.md 4-1-1). 제가 확인한 것은 **「코드가 나갔다」까지**입니다.
- 그래서 `no_teacher` 가 실제로 몇 건인지, 카페24(Neo4j)가 이 창에서 응답하는지는
  **사람이 열어 봐야** 압니다.

---

## 5. 남은 것 / 주의할 것

### 사람이 해야 하는 것

1. **관리자로 로그인해 `https://mangoi.ai/api/admin/reports/c24-mirror` 를 한 번 열기.**
   볼 것 — `summary.no_teacher`(전환일 준비도) · `summary.conflict` · `mode` 가 `"off"` 인지.
   502 `c24_unreachable` 이 나오면 카페24 쪽이 안 닿는 것이고, **그건 0건이 아니라 «못 냈다»** 입니다.
2. **계정이 없는 현직 강사 3명** — JANICE `mangoi_026` · JENNY `mangoi_148` · RICA `mangoi_007`.
   시간표를 넣어도 **본인 화면에는 안 보입니다.** 각자 그 아이디로 한 번 로그인하면
   계정이 자동 생성됩니다(옛 LMS 통과 인증).
   ⚠️ 프로필 메모의 아이디와 실제 쓰는 계정이 **다를 수 있습니다** — 강선생님은 메모엔
   `mangoi_cn2` 인데 실제로는 `hq_t_kang` 을 씁니다. 제가 확인한 것은 「프로필에 적힌
   아이디로는 계정이 없다」까지입니다.

### 2단계에서 할 것 (승인 후)

- 도장 두 줄 — `PATCH`·`DELETE /api/admin/class-schedules/:id` 에서 `source` 가
  `c24-mirror` 면 `c24-mirror:manual` 로 바꾸기
- 실제 생성(창 안 `c24-mirror` 행만 지우고 다시 넣기) + 15분/야간 cron 에 얹기
- 관리자 화면 — 모드 스위치 · 강사 켜기/끄기 · 매일 성적표 · 되돌리기 버튼

### 별건으로 남은 것

- **대소문자 중복 계정 2건** — `mangoi_167`/`Mangoi_167`(HANNAH),
  `mangoi_168`/`Mangoi_168`(LEN). 출근·급여 기록이 두 갈래로 쪼개질 수 있습니다.
- **JINETTE 원부 12번을 숨길지** — 퇴사(사장님 확인)인데 아직 `active=1` 입니다.
  HT FARRAH 와 같은 처리를 할지 지시를 못 받아 그대로 뒀습니다.
- 카페24 `:Class` 의 **9월 이후 예약이 230건뿐**입니다(하루 2~25건). 실제 수업량보다
  훨씬 적으므로, 미러를 켜기 전에 «카페24에 예약이 언제 채워지는지»를 한 번 봐야 합니다.

### ⚠️ 이 작업에서 제가 틀렸던 것 두 가지 (같은 실수 방지)

1. **「완료 7명」** — 파일럿 수업을 카페24 이관분으로 오해했습니다. 사장님 정정으로
   바로잡았습니다. 📌 `class_schedules` 를 볼 때는 **`source` 와 `created_at` 을 함께**
   보세요. `adm-enroll:*` 은 사람이 수강신청으로 넣은 것입니다.
2. **「mangoi_157 은 없는 아이디」** — 「계정이 안 만들어졌다」는 뜻이었는데 마치 사장님이
   틀리신 것처럼 읽히게 적었습니다. 실제로 `mangoi_157` 은 Jinette 것이 맞습니다
   (프로필 14번 메모에 기록). 📌 **«없다»가 «어느 표에 없다»인지 밝혀 적을 것.**

---

## 6. 후속 (같은 날 오후) — 그림자가 잡은 것 두 가지와 «겹쳐 보기»

### 6-1. 그림자 1일차 성적표가 잡은 것 — 강사 한 사람 때문에 13건이 막혀 있었다

배포 첫 성적표(오늘~9/14, 234건):

| 판정 | 건수 | 뜻 |
|---|---|---|
| `not_whitelisted` | 220 | 만들 수 있음(그림자라 안 만듦) |
| `no_teacher` | 14 | 강사를 못 이어 못 만듦 |
| `no_student` | **0** | 학생은 234건 전부 이어짐 |
| `conflict` | **0** | 파일럿 수업과 부딪히지 않음 |

막힌 14건 중 **13건이 «Teacher Ness» 한 사람**이었습니다. 이름은 한 글자도 안 틀렸고
접두어 때문이었습니다 — 카페24 `Teacher Ness` → `ness` / 원부 `HT NESS` → `ht ness`.
나머지 1건은 「스케줄변경중」(사람이 아님)이라 **정상 동작**입니다.

**고친 방법 — 데이터가 아니라 판정을 고쳤습니다.** 처음에는 원부 이름을 `HT NESS` → `NESS`
로 바꾸는 D1 수정을 제안했는데, 확인해 보니 **이 저장소가 이미 같은 해법을 갖고 있었습니다**
(`js/adm-q6.js` 의 `ph54ResolveTeacherId`, 그리고 `teacher_weekly_calendar_id_space_harness`
가 「HT NESS 는 낱말 단위로 맞다」로 못 박아 둠). 그래서 서버 판정에 같은 규칙을 넣었고
**운영 데이터는 한 줄도 안 바꿨습니다** (`buildRosterResolver`).

⛔ 부분일치는 여전히 금지입니다 — `FAR` 가 `HT FARRAH` 에 걸린 사고가 있었습니다.
낱말 **전체**가 같을 때만이고, 후보가 둘 이상이면 잇지 않습니다.

### 6-2. 🔴 카페24에 «없는 수업» 이 들어 있다 — 그래서 아직 «만들지» 않습니다

사장님 확인(8/31): **허윤아 8/31 17:00 에는 Zee 한 명뿐인데** 카페24에는 네 건이 있었습니다.

| class_id | 학생 | 길이 | 실제 |
|---|---|---|---|
| `c24-511741` | Kes | 30분 | ❌ 없음 |
| `c24-511742` | Sid | 20분 | ❌ 없음 |
| `c24-511743` | Zee | 30분 | ✅ |
| `c24-511744` | Zee | 20분 | ✅ |

번호가 연속이고 30/20분이 짝을 이루는 것으로 보아 **강사 변경 잔재**로 보입니다
(카페24에 「스케줄변경중」이라는 이름의 강사가 실제로 있습니다).
⚠️ **이건 제 추론이고 측정이 아닙니다** — 확인된 것은 「그 시각에 Zee 한 명뿐이다」까지입니다.

**`attendance` 로는 못 가립니다** — `importCafe24Attendance` 가 `class_state` 를
`present`/`scheduled` **두 값으로 뭉개서** 취소·변경 표식이 그 단계에서 사라집니다
(창 안 362행이 전부 `scheduled`). 그래서 성적표에 **카페24 원본 `class_state` 를 그대로**
싣도록 했습니다(`by_state`). 값이 한 가지뿐이면 상태로는 못 가른다는 뜻이고,
그때는 다른 속성을 찾아야 합니다.

⟹ **`class_schedules` 에 행을 «만드는» 2단계는 이 물음이 풀린 뒤입니다.** 지금 만들면
학생·강사가 있지도 않은 수업을 기다립니다.

### 6-3. 그래서 «보기 전용 겹쳐 보기» 를 먼저 넣었습니다

사장님 요청: 「카페24에 있는 수업이 mangoi.ai 도 잡히도록. 바로 잡는 게 어렵다면
**잡힌 것처럼 보이게라도** 해줘」

관리자 › **강사 스케줄(주간 통합 캘린더)** 에 카페24 수업을 **파란 점선 카드**로 겹쳐
그립니다(`js/adm-q6.js`). 자료는 그림자 성적표를 그대로 씁니다 — 화면이 판정 규칙을
또 만들면 반드시 갈라집니다.

- 카드에 **「카페24」 배지 + 「카페24에만 있음」** 을 글자로 적습니다(진짜 수업으로 오해 방지)
- ⛔ **`draggable`·`data-idx`·`data-block` 을 안 답니다** — 진짜 수업 배열의 인덱스와
  섞이면 카페24 카드를 끌었을 때 **엉뚱한 수업에 PATCH** 가 나갑니다
- 이미 망고아이에 행이 있는 판정(`already`·`update`·`conflict`·`manual_locked`·`diverged`)은
  겹쳐 그리지 않습니다 — 같은 수업이 두 번 보입니다
- 강사를 못 이은 것은 **아무 칸에나 놓지 않고** 건수만 알립니다
- 「카페24 수업 함께 보기」 체크박스(기본 켬) · 못 읽으면 **사유를 적습니다**
  (빈 화면이 「오늘은 수업이 없나 보다」로 읽히는 것을 막습니다).
  권한 없는 계정(401/403)에는 경고를 띄우지 않습니다 — 그건 고장이 아닙니다

**검토했다가 버린 방법**

| 버린 것 | 왜 |
|---|---|
| `class_schedules` 에 진짜 행을 만들기 | 6-2 의 유령 수업이 그대로 «열리는 수업» 이 됩니다 |
| 카페24 수업을 `ph54State.records` 에 섞어 넣기 | 드래그·삭제·차단이 전부 그 배열의 **인덱스**로 돕니다. 섞으면 엉뚱한 수업에 PATCH·DELETE 가 나갑니다 |
| 진짜 수업과 같은 모양으로 그리기 | 매니저가 「잡혀 있다」고 읽습니다. **화면이 거짓말하면 안 됩니다** |
| 화면에서 «중복인지» 를 다시 판정 | 판정이 두 벌이 되어 서버 성적표와 갈라집니다. 성적표의 `verdict` 를 그대로 씁니다 |

**확인한 방법**

- `tsc --noEmit` exit 0
- `node test-harness/c24_mirror_harness.mjs` → **PASS 84 / FAIL 0** (I절 15건 신설)
- `node test-harness/run.mjs --fast` → **PASS 241 / SKIP 20 / FAIL 0**
- 🖥 **진짜 Chromium**: `PW_DIR=/tmp/pw node test-harness/manual/c24-overlay-browser.mjs`
  → **PASS 17 / FAIL 0**. 카드가 실제로 그려지고, 드래그가 안 되고, 진짜 수업 카드는
  그대로 끌리고, 성적표를 못 읽으면 사유가 화면에 적히는 것까지 확인했습니다.
  ⚠️ 이 검사는 **게이트가 물어 가지 않습니다** — 이 캘린더를 손대면 사람이 불러야 합니다.
- D1 은 조회만 했습니다.

### 6-4. 다음 (승인 필요)

1. **유령 수업 가리기** — 성적표의 `by_state` 를 하루 보고 값이 갈리는지 확인.
   갈리면 그 값으로 거르고, 안 갈리면 카페24 쪽에 «취소를 어떻게 표시하는지» 물어야 합니다.
2. **같은 배치 안 겹침 판정** — 같은 학생·같은 시각이 한 번에 여러 건 오면 `conflict` 로.
   (허윤아 17:00 이 정확히 그 모양이었습니다.)
3. 그 둘이 끝난 뒤 **2단계(실제 생성)**.


---

## 7. 2단계 — Ana 한 사람만 «실제로» 만들었다 (2026-08-31 밤 · 사장님 승인)

### 7-1. 무엇을 승인받았나

사장님: 「Ana 강사의 수업만 가져와서 보여줘봐. 이상 없이 가져와지는지 볼게.」 →
읽어서 채점해 보여 드린 뒤 「Ana 한 사람만 켜서 실제로 만들어 볼까요?」 → **「응」**.

### 7-2. 켜기 전에 채점한 결과 (읽기만)

Ana = 카페24 **182** / 원부 **7**(ANA) / 프로필 12. `Teacher Ana` → `ana` ↔ 원부 `ANA` → `ana`
**정확일치**이고 후보가 하나뿐이라 남의 이름이 붙을 여지가 없다.

| 검사 | 결과 |
|---|---|
| 강사가 원부에 이어지는가 | ✅ 15/15 |
| 학생 계정이 망고아이에 있는가 | ✅ 15/15 |
| 망고아이 수업과 시간이 겹치는가 | ✅ 0건 |
| **유령 수업 패턴**(같은 시각 여러 건) | ✅ **0건** — 허윤아 17:00 같은 모양이 없다 |

⚠️ 이 채점은 **D1 `attendance`(야간 동기화 사본)** 을 읽은 것이다. 화면 성적표는 Neo4j 원본을
읽으므로 값이 같아야 하지만 **같은지까지는 확인하지 못했다**(프록시가 `mangoi.ai` 를 막는다).

### 7-3. 넣은 기계 (PR #638)

- **`applyMirror()`** — ⛔ `dry_run` 이 **기본값**. 하는 일은 셋뿐(만들기·고치기·사라진 것 내리기).
  - ⛔ 손대는 행은 `source='c24-mirror'` 하나뿐. **`UPDATE` 의 `WHERE` 에도 `source`** 를 건다 —
    그 사이 사람이 손대 도장이 찍혔으면 **0행**이 되어 경합에서도 「사람 손이 이긴다」가 지켜진다.
  - ⛔ 지우지 않는다. `status='cancelled'` 로만 내린다.
  - 🔴 **카페24 조회가 0건이면 «취소» 를 통째로 건너뛴다** + 건너뛴 «이유» 를 결과에 적는다.
  - `notes` 에 `c24:<class_id>` — 사라진 수업을 되짚는 유일한 실마리.
- **`POST /api/admin/reports/c24-mirror/{apply,mode,teacher}`** — 게이트를 **둘 따로**
  (강사 `isTeacher` · 지사/대리점 `isOrgScopedRole`). ⛔ `canEditOrg` 금지(교사에게도 true).
- **도장** — `PATCH`·`DELETE /api/admin/class-schedules/:id` 가 미러 행을 건드리면 **같은 UPDATE 안에서**
  `source='c24-mirror:manual'`.
- **캘린더 «코스 기간» 제외** — `start_date/end_date` 는 «같은 source = 같은 수강신청» 전제인데
  미러는 source 가 전부 하나라, 안 빼면 9/1 카드에 「9/1~9/15」 가 붙어 6주 코스로 읽힌다.

### 7-4. 실제로 만든 것 — 4건

⚠️ **11건이 아니라 4건이다.** 작업 시점에 KST 가 **9/1 00:06** 으로 넘어가 8/31 수업 11건은
이미 지난 수업이 됐다. 미러 창은 «오늘~+14일» 이라 앞으로의 4건만 대상이다.

| id | 날짜 | 시각 | 학생 | notes |
|---|---|---|---|---|
| 1027 | 2026-09-01 | 19:40 | 이다연 | `c24:510737` |
| 1028 | 2026-09-04 | 21:10 | 인채은 | `c24:506262` |
| 1029 | 2026-09-08 | 19:40 | 이다연 | `c24:510735` |
| 1030 | 2026-09-15 | 19:40 | 이다연 | `c24:510736` |

전부 `source='c24-mirror'` · `schedule_kind='one_off'` · `class_type='regular'` · `teacher_id='7'` · 20분.
설정은 `c24_mirror_config.mode='whitelist'` · `c24_mirror_teachers('7', enabled=1)`.

### 7-5. 🔴 정직하게 적어 둘 것 — 이 4건은 «코드가» 만든 게 아니다

프록시가 `mangoi.ai` 를 막아 배포된 API 를 부를 수 없어서, **같은 값·같은 컬럼으로 D1 에 직접
넣었다.** 그래서 다음 두 가지가 사실이다.

1. `applyMirror()` 는 **아직 운영에서 한 번도 돌지 않았다.** 하니스에서 가짜 D1 으로 돌려
   검증했을 뿐이다. 처음 돌 때 이 4건은 `already` 로 보여야 정상이고, 그렇지 않으면
   컬럼 매핑이 어긋난 것이다.
2. 자료 출처가 Neo4j 가 아니라 **D1 `attendance` 사본**이라, 8/31 03:00 동기화 **이후** 카페24에
   추가된 Ana 의 수업은 빠져 있을 수 있다.

### 7-6. 검토했다가 버린 방법

| 버린 것 | 왜 |
|---|---|
| 야간/15분 cron 에 미러를 얹어 자동 실행 | `src/index.ts`(공동 금지구역)를 건드려야 한다. 승인 범위 밖이라 안 했다 |
| 관리자 화면에 «미러 실행» 버튼부터 만들기 | 화면을 못 보는 상태에서 버튼만 늘리는 셈이다. 기계와 안전장치가 먼저다 |
| 8/31 수업 11건도 함께 넣기 | KST 로 이미 지난 수업이다. 지난 수업을 만들면 출석·급여 쪽에 없던 행이 생긴다 |
| `source` 를 수업마다 다르게(`c24-mirror:510737`) | 그러면 「내 행만 손댄다」 SQL 조건이 통째로 헛돈다. 대신 `notes` 로 구분한다 |
| 유령 수업 판정을 `class_state` 값으로 지금 넣기 | 그 값의 «뜻» 을 아직 모른다. 추측해서 거르면 진짜 수업이 사라진다 |
| 미러 행을 캘린더에서 드래그 못 하게 잠그기 | 오히려 반대다 — 사람이 옮기면 그 순간 도장이 찍혀 미러가 손을 뗀다. 잠그면 「사람 손이 이긴다」를 쓸 방법이 없다 |

### 7-7. 확인한 방법

- `tsc --noEmit` exit 0
- `c24_mirror_harness` **89 → 122건**(J절 33건 신설 — 가짜 D1 을 물려 `applyMirror` 를 실제로
  돌리고 나간 SQL 을 센다: 화이트리스트 밖·도장 찍힌 행·파일럿 겹침·학생 없음에서 **쓰기 0건**,
  두 번 돌려도 두 번 안 만드는지(멱등), 카페24 0건이면 취소 건너뛰는지)
- `run.mjs --fast` PASS 241 / FAIL 0
- D1 실측으로 만든 4행을 다시 읽어 확인
- ⏳ **사장님이 화면에서 한 번 보셔야 끝난 것이다** — 관리자 › 강사 스케줄 › 강사 필터 `ANA`.

### 7-7-b. 병합·배포 결과 (사실 기록)

- PR #638 — CI 배포 게이트 **success**(97초, 15:26:33→15:28:10 UTC)
- 병합 **squash `a6514e1`** (2026-08-31 15:28 UTC)
- 배포 run **#2837 success** (15:28:56→15:31:07 UTC, 2분 11초 — 게이트 48초 · 기본 워커 24초 ·
  운영 워커 · 헬스체크 통과)
- ⚠️ **squash 라 브랜치 커밋 해시는 main 에 없습니다** — 「반영됐나」를 커밋 해시로 재면
  전부 «없음» 이 나옵니다(CLAUDE.md 2장 함정). 지금 main 의 파일 내용으로 확인하세요.
- ⚠️ 이 기록이 말하는 것은 **«코드가 나갔다»** 까지입니다. 「화면에 Ana 수업이 보인다」는
  사장님이 보셔야 확인되는 별개의 사실이고, 이 문서를 쓰는 시점에는 **미확인**입니다.

### 7-8. 남은 것

1. **자동 실행(cron)** — 승인 필요(`src/index.ts`).
2. **관리자 화면** — 모드 스위치 · 강사 켜기/끄기 · 성적표 · 되돌리기 버튼.
3. **유령 수업 가리기** — `by_state` 를 하루 보고 값이 갈리는지 확인. 안 갈리면 카페24에
   «취소를 어떻게 표시하는지» 물어야 한다.
4. **같은 배치 안 겹침**(같은 학생·같은 시각 여러 건)을 `conflict` 로 잡기.


---

## 8. 자동 실행 — 첫 실행 실측 (2026-09-01 08:45 KST)

### 8-1. 무엇을 넣었나

사장님 지시 「자동 실행도 넣어줘」. ⛔ **새 cron 은 못 만든다** — 계정 한도 5/5 가 꽉 찼다.
그래서 기존 트리거에 얹었다(PR #642, 병합 `bf6ae1e`, 배포 run #2841 success 2분 4초).

| 언제 | 창 | 역할 |
|---|---|---|
| 15분 감시견 트리거 | 오늘~+2일 | 당일 추가분을 따라잡는다 |
| `0 18 * * *`(KST 03:00) | 오늘~+14일 | 멀리 있는 예약까지 맞춘다 |

### 8-2. 첫 실행 결과 — 예상 0건, 실제 12건

```
last_run:watchdog  2026-09-01 08:45:49 KST
{ mode: whitelist, since: 2026-09-01, until: 2026-09-03,
  applied: { created: 12, updated: 0, cancelled: 0 }, errors: [] }
```

🔴 **나는 「0건이 정상」이라고 예상했고 틀렸다.** 이유는 내 채점이 **D1 `attendance` 사본**을
읽은 것이었고 그 사본이 뒤처져 있었기 때문이다 — 그때 9/1~9/3 은 1건이었는데 지금 보면 13건이다.
코드는 카페24(Neo4j)를 **직접** 읽으므로 13건을 다 봤다.
⚠️ 7-5 에 적어 둔 한계(「사본이라 03:00 동기화 이후 추가분은 빠질 수 있다」)가 그대로 확인됐다.

### 8-3. 이 실행이 확인해 준 것 셋

1. **멱등** — 어제 손으로 넣은 9/1 19:40(이다연)을 **다시 만들지 않았다**
   (`created_by='c24-mirror'`, 00:06 그대로). 중복 0건 · 같은 시각 겹침 0건.
2. **손으로 넣은 값이 코드와 일치했다** — 1번이 그 증거다. 매핑이 달랐다면 «없네» 로 보고
   한 번 더 만들었을 것이다. (7-5 에서 「처음 돌 때 already 로 보여야 정상」이라 적어 둔 검증)
3. **지어낸 행 0건** — 만든 13건 전부 `attendance` 에 같은 `class_id` 가 실재한다.

### 8-4. 🔑 「15분 창」의 근거가 실측으로 확인됐다

9/1 **12건** · 9/2 1건 · 9/3 **0건**. 카페24는 **당일에** 예약을 채운다.
사장님이 처음 말씀하신 「매일 아침 6시 한 번」이었다면 **오늘 수업 12건을 통째로 놓쳤다.**
⟹ 좁은 창(15분)이 없으면 미러는 실무에서 못 쓴다.

### 8-5. 검토했다가 버린 방법

| 버린 것 | 왜 |
|---|---|
| 새 cron 을 하나 더 등록 | 계정 한도 5/5. 여섯 번째는 배포가 `code 10072` 로 거절된다 |
| 15분 틱 중 «정각만» 돌리기 | 다시 «분·시로 가르기» 다 — 이 저장소가 이중 실행으로 사고를 낸 그 형태 |
| 18:00 겹침을 코드로 막기(락·플래그) | **서로 다른 워커 호출**이라 순서를 알 수 없다. DB(부분 유니크 인덱스)가 보증해야 한다 |
| `runMirrorSweep` 이 예외를 던지게 두기 | 부르는 쪽이 **사이트 감시견**이다. 여기서 던지면 사이트 감시까지 죽는다 |
| 실패를 조용히 넘기기 | 「돌았는데 0건」과 「아예 안 돌았다」가 구분이 안 된다 → `last_run:*` 에 이유를 남긴다 |

### 8-6. 남은 것

1. **유령 수업 가리기** — `by_state` 를 하루 보고 값이 갈리는지. 안 갈리면 카페24에 물어야 한다.
   ⚠️ 그때까지는 «없는 수업» 이 미러로 만들어질 수 있다(Ana 는 그 패턴이 0건이었다).
2. **강사 늘리기** — 켜기 전에 그 강사도 채점(강사·학생 이어짐 · 겹침 · 유령 패턴)부터.
3. **관리자 화면** — 모드 스위치 · 강사 켜기/끄기 · 되돌리기. 지금은 D1 로만 조작한다.
4. ⏳ **화면 확인** — 관리자 › 강사 스케줄 › `ANA`. 이 문서를 쓰는 시점에는 **미확인**이다.

---

## 9. 퇴사 강사 — 「켜지 마라」를 표현할 방법이 없었다 (2026-09-01)

### 9-1. 사장님 한 마디가 드러낸 것

Zee 를 켠 뒤 Mariane(카페24 24)의 앞으로 예약 **30건**을 화면에서 보시고
「마리안은 그만 두었어. 이제 더이상 수업을 안해」.

⟹ 그 30건은 전부 **잔재**다. 그리고 그중 6건은 «같은 학생·같은 시각» 으로 다른 강사와
겹친다(= 한쪽은 유령). Ana 쪽 3건(손현규 17:00 · 이선우 21:40 · 김재홍 9/4 17:20)이
맞고 Mariane 쪽이 잔재였다.

🔴 **화이트리스트로는 이것을 표현할 수 없었다.** 「적혀 있으면 켠다」 라서 «없는 강사» 는
`whitelist` 모드에서만 안 만들어지고, **전환일에 `mode='all'` 로 올리는 순간 30건이 전부
만들어진다.** 퇴사자의 시간표가 되살아나는 것이다.

### 9-2. 고침 — `enabled = 0` 은 «아직 안 켬» 이 아니라 «켜지 마라»

`getMirrorBlocked()` 신설(`src/c24-mirror.ts`). `touch()` 맨 앞에서 **`mode` 검사보다
먼저** 막는다. 결과에 `blocked_teachers` 로 싣는다.

```
if (blocked.has(tid)) return false;   // ⛔ mode='all' 도 이긴다
```

⚠️ **그 한 줄을 mode 검사 «뒤» 로 옮기면 보호가 통째로 풀린다** — 하니스 K⑫ 가 순서를
못 박는다(되돌리면 실제로 FAIL).

### 9-3. 정작 확인해야 할 「겹침」이 화면에서 사라져 있었다

겹쳐 그리기는 `conflict` 를 **일부러** 안 그렸다(「같은 수업이 두 번 보인다」).
그런데 **강사 필터를 걸면 그 «진짜 카드» 는 다른 강사의 것이라 이 화면에 없다.**
⟹ 아무 데도 안 보인다.

실측: Mariane 30건인데 화면은 **28건**. 증발한 2건이 하필 사장님이 카페24와 대조하시려던
바로 그 행이었다. 지금은 「⚠️ 같은 시각 다른 수업」 라벨로 그리고, 건수도 셋으로 가른다
(카페24 대기 / 겹침 확인필요 / 지난 카페24 기록).

### 9-4. 원부 정리 — 코드가 아니라 D1 (사장님 지시)

| 강사 | 원부 id | 카페24 | 한 것 |
|---|---|---|---|
| MARIANE | 11 | 24 | `teachers.active = 0` · `c24_mirror_teachers` `enabled=0` |
| JINETTE | 12 | 179 | 〃 |

**내린 것은 `active` 한 칸뿐이다.** `name`·`status`·`teacher_payroll_auto` 이력은
손대지 않았다 — 지난 수업·급여가 이름을 계속 풀 수 있어야 한다(선례: HT FARRAH id 3).

사전 확인(둘 다): 활성 `class_schedules` **0건** · 미러가 만든 행 **0건** ·
급여 기록 보존(MARIANE 8건 · JINETTE 7건). 재직 29 → **27명**.

⛔ 카페24 원본은 우리가 못 고친다 — 잔재 예약은 카페24 쪽에서 정리해야 한다.
그때까지는 위 `enabled=0` 이 유일한 방어선이다.

### 9-5. 검토했다가 버린 방법

| 버린 것 | 왜 |
|---|---|
| 퇴사 강사의 카페24 잔재를 D1 에서 지우기 | 카페24가 정본이다. 지워도 다음 동기화에 돌아온다 |
| `teachers.active=0` 이면 미러가 알아서 건너뛰게 | 원부 상태와 «미러를 켤지» 는 다른 축이다. 휴직·재입사에서 조용히 어긋난다 |
| 이름·`status` 도 함께 바꾸기 | 지난 수업·급여가 이름으로 풀린다. 바꾸면 과거가 «모름» 이 된다 |
| `conflict` 를 계속 안 그리기 | 대조해야 할 바로 그 건이 화면 어디에도 없다 |
| 지난 카페24 카드를 감추기 | 「지난주에 수업이 없었다」는 반대쪽 거짓이 된다 |

### 9-6. 확인한 방법

- tsc `--noEmit` exit 0 · `c24_mirror_harness` **158건** · `run.mjs --fast` PASS 245 / FAIL 0
- `manual/c24-overlay-browser.mjs` **24건**(진짜 Chromium)
- PR #653 CI success(110초) · squash `e696554` · 배포 run #2852 success(2분 25초)
- ⏳ **배포된 화면은 미확인** — 프록시가 `mangoi.ai` 를 막는다. 사람이 관리자 ›
  강사 스케줄에서 MARIANE 을 열어 겹침 카드가 보이는지 한 번 봐야 끝난 것이다.

### 9-7. 하니스가 스스로 헛돌던 것

가짜 D1 이 `enabled = 1` 과 `enabled = 0` 을 **같은 정규식으로** 받아, 켠 강사 목록이
그대로 막힌 강사 목록으로 돌아왔다 ⟹ 「켠 강사가 곧 막힌 강사」가 되어 INSERT 가 한 번도
안 났다. `WHERE enabled = 0` 을 **먼저** 보게 고쳤다(J·K 두 곳).
📜 CLAUDE.md 2장 「가짜 DB 로 하니스를 돌렸는데 검사가 헛돌며 통과」와 같은 뿌리다 —
«못 찾는다» 검사만 있으면 헛돎이 초록불로 보인다.

---

## 10. 명부 정리 · 재직 두 표 맞추기 · 강사 3명 추가 (2026-09-01 오후)

- 브랜치: `claude/janice-instructor-schedule-issue-9oetil`
- PR: #660(재직 두 표 동기화 + 퇴사 잔재 가르기) · #676(숨긴 학생 제외)

### 10-1. 왜 했나

사장님이 명부 화면을 보시고 물으셨다 — **「Mariane 은 얼마전에 퇴사했는데 아직도 여기
명부에 있는데 이유가 뭘까?」**

원인은 **재직 여부가 «두 표» 에 따로 있기 때문**이었다.

| 표 | 칸 | 쓰는 곳 |
|---|---|---|
| `teachers` | `active` (0/1) | 시간표·미러·배정 |
| `teacher_profiles` | `status` (활동중/비활동/퇴사) | **강사 명부 화면** |

앞서 JINETTE 를 퇴사 처리하면서 `teachers.active=0` 만 내렸더니, 미러는 멈췄는데
**명부에는 그대로 남았다.** 코드 어디에도 두 표를 잇는 곳이 없었다.

### 10-2. 무엇을 바꿨나

**① 쓰는 자리에서 맞춘다** — `src/api-admin.ts` `PATCH /api/admin/teacher-profiles/:id`

`status` 가 함께 온 요청이면 UPDATE·감사로그 뒤에 `teachers.active` 를 같이 맞추고,
퇴사·비활동이면 `c24_mirror_teachers` 에 `enabled 0` 을 넣어 미러도 함께 끈다.
응답에 `roster_sync` 로 무엇을 했는지 돌려준다.

⛔ **읽는 자리에서 고치지 않는다.** 명부 조회(`GET /api/admin/teacher-profiles`)는
`ROSTER_ACTIVE`·`ROSTER_NAME` **서브쿼리**로 대조만 해서 `roster_mismatch`·
`roster_broken_link` 로 «알려줄» 뿐이다(조인으로 붙이면 링크가 둘인 강사가 두 줄이 된다 —
이미 밟은 함정).

**② 퇴사 잔재를 경고에서 갈라낸다** — `src/c24-mirror.ts`

`no_teacher`(원부에 없음 = 등록해야 함) 하나였던 것을
`no_teacher_left`(퇴사한 강사의 카페24 잔재 = 할 일 없음)로 쪼갰다.
`loadTeacherLinks` 가 `teachers WHERE active = 0` 도 읽어 이름으로 잇는다
(**재직이 언제나 이긴다** — 동명이인이면 재직 쪽).
화면(`js/adm-q6.js` `?v=13`)은 회색 「퇴사 강사 잔재 N개 (안 그림)」로 세기만 하고,
빨간 「⚠️ 원부에 없는 강사」는 진짜 등록이 필요한 것만 남는다.

> 📜 녹화 목록이 «정상 정리분 66%» 에 «진짜 실패 76건» 을 파묻었던 것과 같은 이유다 —
> **늘 켜져 있는 경고는 경고가 아니다.**

**③ 숨긴 학생의 수업은 안 만든다** — `src/student-override.ts` `loadHiddenStudents()`

MANGO AI(`mangoai1`) 시험용 계정이 카페24에 **8명 강사 10건**의 예약을 갖고 있었다.
`student_erp_override`(명부 숨김 정본)를 미러의 **다섯 번째 읽는 쪽**으로 합류시켰다.

⚠️ 조건절(`hiddenExcludeCond`)이 아니라 **목록**으로 받는다 — 조건절로 거르면 그 학생이
「계정이 없습니다」(빨간 경고)가 되는데 그것은 **거짓**이다. 판정 `student_hidden` 으로
«일부러 뺀 것» 이라고 사실대로 말한다.

**④ 명부 정리** (사장님 확인분, D1 직접)

| 대상 | 한 일 |
|---|---|
| MARIANE · JINETTE · RICA · FAYE | 퇴사 — `teachers.active=0` + `profiles.status='퇴사'` + 미러 차단 |
| Wan | `teacher_account_links` 연결 |
| 테스트강사 · 파라테스트 | `list_hidden=1` (status 는 그대로) |
| 손선생님 | `status='비활동'` + `list_hidden=1` |
| MANGO AI | `student_erp_override.hidden=1` |

**⑤ 강사 3명 추가** — BELLE(2) · HT NESS(10) · SID(14) 를 미러에 켰다.

### 10-3. 왜 그렇게 풀었나 — 버린 방법

| 버린 것 | 왜 |
|---|---|
| 명부 조회에서 `teachers.active` 를 **읽어 고치기** | 읽을 때 고치면 어디서 바뀌었는지 아무도 모른다. 감사로그도 안 남는다 |
| 두 표를 하나로 합치기 | `teachers` 는 시간표·급여가, `teacher_profiles` 는 화면이 쓴다. 반경이 서비스 전체다 |
| 명부 조회를 **LEFT JOIN** 으로 | 계정 링크가 둘인 강사(대소문자 중복)가 **두 줄**로 그려진다 — 같은 날 다른 세션이 이미 밟아 서브쿼리로 고친 자리다 |
| 테스트 계정을 `status='퇴사'` 로 | **다른 세션이 그날 `list_hidden` 을 만든 이유가 바로 이 둘**이었다. 축이 다르다 — 스스로 되돌렸다 |
| 퇴사 잔재를 `no_teacher` 로 그대로 두기 | 영원히 켜진 빨간 경고가 되어 진짜 경고를 덮는다 |
| MANGO AI 전용 표를 새로 만들기 | 명부 숨김 정본이 이미 있다. 두 벌이 되면 한쪽만 고쳐진다 |

### 10-4. 확인한 방법

- tsc `--noEmit` exit 0
- `c24_mirror_harness` **218건**(158→218. L 퇴사 가르기 · M 두 표 동기화 · N 숨긴 학생)
- `run.mjs --fast` **PASS 255 / SKIP 20 / FAIL 0**
- PR #660 · #676 CI success → squash 병합 · 배포 run 33499358634 **success(119초)**
- **켠 3명 실측**(13:00:48 자동 실행): 22건 생성 · 지어낸 것 0 · 학생 못 찾음 0 ·
  중복 0 · 서로 다른 카페24 수업 22건 · 표본의 시각·학생·분(分) 일치 →
  13:15 틱은 **0건**(멱등 확인)
- ⏳ **배포된 화면은 미확인** — 프록시가 `mangoi.ai` 를 막는다. 사람이 봐야 할 것:
  ① 명부에 빨간 «불일치» 띠가 **없는지**(있으면 아직 어긋난 강사가 있다는 뜻)
  ② MARIANE 을 열면 겹침 카드가 보이는지
  ③ 지난주 카드가 「지난 카페24 기록」으로 적히는지

### 10-5. 하니스가 스스로 헛돌던 것 (두 번)

**M① 거짓 통과** — 「`b.hasOwnProperty('status')` 가 있는가」로 검사했더니, 같은 파일의
**다른 세션이 넣은 감사로그**가 그 문자열을 갖고 있어 내 동기화 블록을 통째로 지워도
초록불이었다. `const wantLive = String(b.status` 주변 **위치**로 판정하게 고쳤다.

**N⑥⑦ 거짓 통과** — `student-override.ts` 가 `d1-chunk` 를 **동적 import** 해서
하니스에서 해석이 안 됐고, fail-open try/catch 가 그것을 삼켜 **늘 빈 집합**을 돌려줬다.
「숨긴 사람이 없다」가 정답인 검사만 있어서 전부 초록이었다.
정적 import 로 바꾸고 하니스 로더를 **재귀 복사**로 고쳤다.
📜 CLAUDE.md 2장 「가짜 DB 로 하니스를 돌렸는데 검사가 헛돌며 통과」 그대로다 —
**«제대로 찾는다» 검사를 짝으로 넣어야** 헛돎이 드러난다.

### 10-6. 남은 것

- **진짜 대조가 필요한 겹침 4건** — 카페24에서 사람이 봐야 한다
  - 9/1 16:30 신서경 · **9/1 19:40 이다연**(이미 Ana 로 미러됨 — Cindy 가 맞으면 고쳐야 함)
  - 9/1 20:30 이준희 · 9/3 20:30 이준희
- **퇴사 강사 잔재 7건은 카페24에서 지워야** 한다(우리 쪽은 잠갔다)
- 카페24에 물을 것: **「취소된 수업은 어떻게 표시되나요?」** — 유령 수업의 근본 해결
- 아직 안 켠 강사 13명 (🟡 겹침 1~2인 8명 · 🟠 Jane·Wan 각 3)
- 떠도는 원격 브랜치 `docs/c24-mirror-ch9-260901` — 내 삭제가 403 이라 사람이 지워야 한다

### 10-7. 첫 «야간 넓은 창» 실측 (2026-09-02 03:06 KST = 09-01 18:06 UTC)

**잰 값**

- `last_run:nightly` — 창 `2026-09-02 ~ 2026-09-16`(+14일) · `created 11 · updated 0 · cancelled 0 · errors []`
- `nightly:0 18 * * *:run` 에 **`finishedAt` 있음** = 체인이 끝까지 갔다. 17단계 **8분 48초**(527,969ms)
  - cafe24-sync **5분 19초(60%)** · learning-snapshot 2분 25초 · streak-reconcile 30초 · **c24-mirror 5.4초(1%)**
- 미러 행 47 → **96**(+49). 갈라 보면 **주간 감시견 38건(09-02~09-04)** · **야간 11건(09-07~09-16)**
- 무결성 전부 0 — 형식 안 맞는 `notes` · 중복 `notes` · 켠 강사 밖 · `mangoai1` · 빈 학생 계정

**판단**

- 넓은 창이 제 몫을 했다. 감시견은 +2일까지라 **09-07~09-16 의 11건은 야간이 아니면 안 들어왔다.**
- 미러는 싸다(5.4초 = 체인의 1%). 그래서 준비해 둔 대책 셋 — (a) 창 14→7일 (b) 다른 cron 으로 이전
  (c) 체인 안에서 앞으로 당기기 — **지금은 필요 없다.** 근거 없이 순서를 바꾸지 말 것.
- ⚠️ **대신 체인 전체가 8분 48초로 경고선(10분)까지 여유가 1분 12초다**(cron 벽시계 상한 15분).
  그 시간의 60%가 `cafe24-sync` 하나다. 오늘은 안 잘렸지만 **여기에 무엇을 더 얹으면 잘릴 자리**다.
  이건 미러가 아니라 야간 체인 전체의 문제이고, 무엇을 떼어낼지는 사람이 정할 별건이다.
  📜 `nightly-run.ts` 가 「블록 전체가 몇 분인지 아무도 모른다」고 적어 둔 그 숫자가 이제 나왔다.

**못 잰 것 (추론으로 메우지 않는다)**

- **SID 만 09-03 이후가 0건**이다. 카페24에 원래 예약이 없는 것인지 다른 이유인지는 **확인하지 못했다** —
  프록시가 `mangoi.ai` 를 막아 미러 성적표 API 를 부를 수 없다. 카페24 화면에서 사람이 봐야 가른다.
- `mangoai1` 0건은 **«검증됨» 이 아니다.** 켠 5명에게 원래 그 학생 수업이 없어서, 어제 넣은 숨김 조치가
  실제로 작동했는지는 아직 증명되지 않았다(예방 조치였다).

---

## 11. 인계 블록 «다 되었나» 점검 (2026-09-02 오전)

### 11-1. 왜 했나

사장님이 2026-09-01 에 만든 **인계 프롬프트 블록 PDF**(「카페24 → 망고아이 시간표 미러 —
다음 작업 프롬프트 블록」)를 그대로 주시며 「이거 다 되었는지 확인해 보고 안되어 있으면
되게 하고 배포까지」라고 하셨다. 그 블록은 PR #673 직후(켠 강사 = ANA·ZEE 둘)의 상태로
쓰여 있어서, **지금 상태와 얼마나 어긋나는지부터 재는 것**이 첫 일이었다.

### 11-2. 잰 값 (2026-09-02 11:50~12:10 KST)

**코드·배포 — 블록의 [검증] 항목 그대로**

| 검사 | 결과 |
|---|---|
| `tsc --noEmit` | 오류 0 |
| `test-harness/c24_mirror_harness.mjs` | **PASS 252 / FAIL 0** |
| `test-harness/run.mjs --fast` | **PASS 266 / FAIL 0 / SKIP 20** (기준선 ≥ 140 충족) |
| main 최신 커밋 `5c4b483` 배포 | run **#2933 success** (02:26 UTC = 11:26 KST) |
| 작업 트리 | 깨끗 · `HEAD == origin/main` |

⚠️ `npm ci` 는 **Node 22 에서 그냥은 죽는다**(`ERESOLVE` — CLAUDE.md 2장 「새 PC 에서 npm ci 가
ERESOLVE 로 죽음」 그대로). 이 컨테이너 기본이 22 라 `--legacy-peer-deps` 로 설치해 검증했다.
CI 는 Node 24 라 영향 없다.

**운영 상태 — D1 SELECT (읽기만)**

- `c24_mirror_config.mode` = **`whitelist`**
- `last_run:nightly` — 창 `2026-09-02 ~ 2026-09-16` · `created 11 · errors []` (03:06 KST)
- `last_run:watchdog` — 창 `2026-09-02 ~ 2026-09-04` · `created 0 · errors []` (11:45 KST)
- 켠 강사 **5명** — BELLE(원부2) · ANA(7) · ZEE(9) · HT NESS(10) · SID(14)
- 막은 강사 **4명** — RICA(4) · MARIANE(11) · JINETTE(12) · FAYE(21)

⟹ **블록의 [지금까지 켠 강사] 두 줄은 이미 낡았다**(ANA·ZEE → 5명). 막은 강사도 2명 → 4명.
블록 맨 끝의 「붙여넣기 전에 화면과 대조표를 한 번 더 확인하세요」가 그대로 맞았다.

### 11-3. 🔑 10-7 의 「못 잰 것」 하나가 풀렸다 — SID 는 09-03 이후가 0건이 **아니다**

10-7 에 「**SID 만 09-03 이후가 0건**이다. 카페24에 원래 예약이 없는 것인지 다른 이유인지는
확인하지 못했다」고 적어 두었다. **다른 이유였다.**

- 카페24 예약은 **09-08 14:30 에 1건 더 있다** — 학생 `SLPSSO_hayul0610`, 수업번호 `c24-510465`
- 미러는 그 건을 **만들지 않았다**(`class_schedules` 에 `c24:510465` 0건)
- 이유는 **`suspect_dup`(강사 변경 잔재 의심) 보류**다. 근거 둘이 다 맞는다:
  - 입구 조건 — 그 학생의 09-08 예약이 **2건**(Sid 14:30 · Jane 20:30)
  - 그 자리 이력 — (학생·강사·시각) 조합이 **1건뿐** → `< 2` 라 보류

즉 **고장이 아니라 설계대로**다(CLAUDE.md 「한 번짜리 수업(보강)은 그 자리가 두 번째로 잡힐
때까지 보류된다 — 사람이 보고 손으로 넣으면 된다」).

**같은 이유로 보류 중인 것이 켠 5명 전체에 5건 있다** — 사람이 카페24와 대조할 목록:

| 강사 | 날짜 | 시각 | 학생 | 카페24 수업번호 |
|---|---|---|---|---|
| Ana | 09-02 | 16:00 | `rez02` | 512251 |
| Ana | 09-04 | 17:20 | `imom0180` | 512061 |
| Belle | 09-02 | 15:00 | `1124800040` | 512238 |
| Belle | 09-02 | 16:00 | `rez01` | 512254 |
| Sid | 09-08 | 14:30 | `SLPSSO_hayul0610` | 510465 |

🔴 **정정 (2026-09-02 오후) — 처음에 「다섯 건 모두 같은 모양이다」라고 적었는데, 그건
«미러의 판정 기준» 에서만 맞는 말이었다.** dup_guard 가 보는 것(그날 예약 2건 · 그 자리 이력
1건 · 학생 계정 존재 · 숨김 아님)은 다섯 건이 똑같다. 하지만 **왜 그렇게 됐는지는 둘로 갈리고,
사람이 카페24에서 볼 것도 서로 다르다** — 한쪽은 «지워야 할 잔재» 이고 다른 한쪽은 «그냥 새로
생긴 자리» 다. 뭉뚱그려 적으면 다음 사람이 **A형의 심각도(퇴사 강사 예약이 매주 반복된다)** 를
놓친다. 📜 CLAUDE.md 「카페24에서 온 것을 그대로 만드는데 «일부러 뺀 것» 이 빨간 경고로 뜸」과
같은 뿌리다 — **성격이 다른 것을 한 이름으로 부르면 진짜 경고가 파묻힌다.**

#### A형 — 퇴사한 Mariane 예약이 «같은 시각» 에 얹혀 있다 (2건)

| 학생 | 날짜·시각 | 살아 있는 예약 두 개 |
|---|---|---|
| **한승우** `1124800040` (토마스 부천역곡 남부캠퍼스) | 09-02(수) **15:00** | ⛔ Mariane `c24-511556` / ✅ Belle `c24-512238` |
| **김재홍** `imom0180` (아이맘 잉글리쉬) | 09-04(금) **17:20** | ⛔ Mariane `c24-511575` / ✅ Ana `c24-512061` |

**잰 값 — 35일 이력(D1 `attendance` 의 `c24-*`)이 또렷하다:**

- 한승우는 **매주 수 15:00** 고정인데, 08-05 부터 **매주 Mariane 예약이 그대로 남고 그 위에
  실제 강사가 새로 붙는다** — 08-12 Chaine · 08-19 Ana · 08-26 Len · 09-02 Belle
- 김재홍은 **매주 화·금 17:20** 고정. 07-31~08-14 는 Mariane 단독이었는데 **08-18 부터** 매번
  Mariane + 대체강사 2건 — 08-18 Krystel · 08-21 Len · 08-25 Hannah · 09-01 Len · 09-04 Ana

**판단(잰 값과 구분해서 적는다)** — Mariane 쪽이 잔재이고 Belle·Ana 쪽이 진짜일 **가능성이 높다.**
다만 「그날 실제로 누가 수업했나」는 카페24 출석·수업기록을 봐야 갈린다. 우리 쪽 자료로는 여기까지다.

⚠️ **이 유형은 매주 반복된다.** Mariane 은 `enabled=0` 이라 우리가 안 만들고, 그 위의 진짜
강사는 dup_guard 가 보류하므로 **결과적으로 아무것도 안 만들어진다.** 카페24에서 Mariane 예약을
지우지 않는 한 다음 주도, 그다음 주도 같은 상태다.

#### B형 — 겹치는 게 아니라 «새로 생긴 자리» (3건)

같은 시각 충돌이 아니다. 그냥 **처음 보는 자리**라 미러가 조심한 것이다.

| 학생 | 날짜·시각 | 그날 다른 예약 | 원래 패턴 |
|---|---|---|---|
| `rez01` (레지나즈) | 09-02(**수**) 16:00 **Belle** `c24-512254` | 15:00 Ness(이미 만들어짐) | 07-31~08-28 **매주 금** 15:00 Ness + 16:00 **Far** |
| `rez02` (레지나즈) | 09-02(**수**) 16:00 **Ana** `c24-512251` | 15:00 Len | 07-31~08-28 **매주 금** 15:00 Chaine + 16:00 **Hannah** |
| **신하율** `SLPSSO_hayul0610` (동탄SLP) | 09-08(화) **14:30 Sid** `c24-510465` | 20:30 Jane | 08-03~09-07 **매주 월 21:20 Jane** · 화는 20:30~20:40 Jane |

- 레지나즈 두 명은 **요일이 금 → 수로 옮겨졌고** 16:00 강사도 함께 바뀌었다(Far→Belle, Hannah→Ana).
  요일 이동이 맞다면 **잔재가 아니라 진짜 수업**이고, 다음 주에 같은 자리가 두 번째로 잡히면
  미러가 저절로 만든다.
- 신하율은 기존 패턴이 **저녁(20:30~21:20) Jane** 뿐인데 09-08 만 **낮 14:30 Sid** 다. 강사도
  시간대도 처음이라 **일회성 보강이거나 잘못 잡힌 예약**으로 보인다.

#### 지금 상태 — 다섯 건 다 망고아이 시간표에 없다

`class_schedules` 실측(09-02~09-16, 그 다섯 학생): **행이 하나뿐**이고(`rez01` 09-02 15:00
Ness `c24:512236`) 나머지는 0건이다. 다섯 건의 담당 강사가 **전부 켠 강사**(Ana·Belle·Sid)라
보류만 아니었으면 만들어졌을 것들이다. 파일럿 중이라 수업 자체는 카페24로 돌아가지만
**망고아이 화면에는 안 보인다.**

#### 사람이 카페24에서 볼 것 (이 순서대로)

1. **A형 두 건** — 08-12·08-19·08-26(한승우), 08-18·08-21·08-25(김재홍) 에 **실제로 누가
   수업했나.** Mariane 이 아니었다면 잔재로 확정이다. → **Mariane 예약을 지울 수 있는지** 확인.
2. **레지나즈 두 명** — 수업 요일을 **금 → 수로 옮긴 것이 맞나.** 맞으면 그대로 두면 된다.
3. **신하율 09-08 14:30** — 진짜 보강인가, 잘못 잡힌 예약인가.

판단 뒤 할 일: **잔재면 카페24에서 지운다** · **반복 수업이면 그냥 둔다**(다음 회차에 자동 생성)
· **일회성 보강이면 주간 스케줄에서 손으로 한 건 넣는다**(자동으로는 영영 안 들어온다).

📌 **덤으로 눈에 걸린 것** — `rez02` 에 **2027-01-08 Chaine 15:00**(`c24-509664`) 예약이 있고,
그 밖에 **강사가 안 붙은 채 2027-03-03 ~ 2030-02-20 로 잡힌 예약이 21건** 있다. 미러 창(+14일)
밖이라 지금은 무해하지만 카페24 쪽 데이터가 이상한 자리로 보인다. ⛔ 우리가 손댈 것은 아니다.

⚠️ **못 잰 것** — 여기서 잰 것은 D1 `attendance` 의 카페24 미러본이지, 미러가 실제로 보는
**Neo4j `:Class` 를 직접 부른 것이 아니다**(프록시가 `mangoi.ai` 를 막아 성적표 API 를 못 부른다).
둘은 같은 카페24 자료를 보지만 **같다고 증명한 것은 아니다.** 최종 확인은 관리자 화면의
「겹침 보기」로 사람이 한다.

### 11-4. 다음 후보 채점 (블록 [할 일] 2번의 순서대로)

블록이 적어 둔 순서는 `CINDY → CHAINE → BELLE → WIN` 인데 **BELLE 는 09-01 에 이미 켰다.**
남은 셋을 D1 로 채점했다(창 = 오늘~+14일, 나머지는 미러가 쓰는 것과 같은 기준):

| 후보 | 앞으로 예약 | 학생 계정 누락 | 같은 시각 겹침 | 파일럿 수업 | 원부 연결 | 판정 |
|---|---|---|---|---|---|---|
| **CINDY** (원부5 · 카페24 177) | 6건 (09-02) | 0 | **0** | **0건** | 유일(`CINDY`) | 🟢 깨끗 |
| **CHAINE** (원부15 · 카페24 64) | 13건 (09-02~09-15) | 0 | **0** | **0건** | 유일(`CHAINE`) | 🟢 깨끗 |
| **WIN** (원부19 · 카페24 181) | 3건 (09-02~09-10) | 0 | **1건** | **8건 살아 있음** | 유일(`WIN`) | 🟠 보류 |

🔴 **블록의 「WIN … 파일럿 0건」은 더는 사실이 아니다.** 지금 `class_schedules` 에
`source='adm-enroll:89'` 인 **active 8건(09-01~09-24)** 이 붙어 있다. 그리고 겹침도 1건 있다 —
**09-02 18:20 · 학생 `jy1s016` · WIN(`c24-512215`) ↔ Teacher Len(`c24-512222`)**. 둘 중 어느
쪽이 진짜인지는 사람이 카페24에서 봐야 한다. ⟹ **WIN 은 CINDY·CHAINE 뒤로 미룬다.**

### 11-5. 안 한 것 — 강사를 켜지 않았다 (일부러)

블록의 [절대 하지 말 것] 에 「**관리자 화면을 거치지 않고 D1 에 SQL 을 직접 넣어 강사를 켜거나
수업을 만들지 않는다**」가 있다. 그런데 이 컨테이너는 프록시가 `mangoi.ai` 를 막아
`/admin/c24-mirror.html` 을 **열 수 없다**(실측 `curl` → `connect_rejected`).

⟹ 내가 켤 수 있는 유일한 길이 그 블록이 금지한 길이다. 그래서 **채점까지만 하고 멈췄고**,
켜는 것은 사장님이 화면에서 하시거나, 「D1 로 켜도 된다」고 따로 말씀해 주셔야 한다.
📜 이 저장소는 「하기로 한 것」을 「했다」로 적었다가 같은 사고를 두 번 더 낸 전력이 있다
(CLAUDE.md 0장 도메인–워커 배치). **못 한 것은 못 했다고 적는다.**

### 11-6. 확인한 방법

- 코드·하니스 — 위 11-2 표(전부 이 세션에서 직접 실행)
- 운영 상태 — Cloudflare D1 **SELECT 만** 사용(`c24_mirror_config`·`c24_mirror_teachers`·
  `attendance`·`class_schedules`·`students_erp`·`student_erp_override`·`teachers`)
- 화면 확인은 **못 했다** — 프록시가 막는다. 사장님이 한 번 보셔야 끝난 것이다.
