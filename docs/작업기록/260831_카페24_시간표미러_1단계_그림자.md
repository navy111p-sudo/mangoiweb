# 카페24 → 망고아이 시간표 미러 1단계(그림자) + 강사 번호 대조표 + 명부 정리

- 날짜: 2026-08-31
- 작업자: 사장님(navy111p) 지시 · Claude Code
- 브랜치 / PR: `claude/janice-instructor-schedule-issue-9oetil` / [PR #634](https://github.com/navy111p-sudo/mangoiweb/pull/634)
  · 병합 `3c7329d` · 배포 [run #2831](https://github.com/navy111p-sudo/mangoiweb/actions/runs/33385652731) **success** (11:09:32→11:11:58 UTC)

---

## 1. 왜 했나

「Janice 강사 수업 스케줄이 안 뜬다」는 제보에서 출발했습니다. 관리자 「강사 스케줄
(주간 통합 캘린더)」에서 강사 필터를 JANICE 로 놓으면 **수업 0개**로 나왔습니다.

운영 D1 을 직접 조회해 보니 **화면 버그가 아니었습니다.**

- 그 캘린더가 읽는 것은 `class_schedules` 인데, JANICE(원부 28번) 행은 33건 있지만
  **전부 `status='cancelled'`** 이었습니다(26건 `user_id='lms'` 옛 LMS 자리표시,
  7건 `type_seed` 시연 시드).
- 반면 실제 수업은 카페24 쪽 `attendance` 에 **65건**(6/22~8/31, 카페24 강사번호 37)
  살아 있었습니다.

그리고 **Janice 만의 문제가 아니었습니다.** 재직 강사 29명 중 `class_schedules` 에
살아 있는 행이 있는 강사는 **7명뿐**이고, 카페24에서 실제로 수업 중인데 망고아이
시간표가 빈 강사가 **17명**이었습니다.

⚠️ 처음에 저는 그 7명을 **「이관 완료」로 잘못 적었습니다.** 사장님이 「그건 파일럿
테스트용으로 잡은 것」이라고 정정해 주셨고, 실측으로 확인했습니다 — 그 52건은 전부
`source='adm-enroll:78~85'` 이고 생성 시각이 **8/27~9/1** 입니다. 즉 최근에 손으로
넣은 파일럿 수업이지 카페24 이관분이 아닙니다.
**⟹ 카페24에서 이관된 수업은 0건이고, 이관 대상은 23명입니다.**

이어서 사장님이 진짜 목표를 말씀하셨습니다.

> 파일럿이 끝나면 카페24에서 실시간으로 도는 수업을 mangoi.ai 로 덮어씌우고 싶다.
> 어느 시점에 «한날 한시»에 다 같이 써야 하는데, 그날 급하게 하는 것보다 미리부터
> 잘 덮어씌워지는지 확인하고 싶다. 그리고 지금은 파일럿 수업을 LMS 와 망고아이에
> 두 번 잡아야 해서 일이 두 배다.

---

## 2. 무엇을 바꿨나

### 2-1. 코드 (1단계 — 읽기만)

| 파일 | 내용 |
|---|---|
| `cloudflare-deploy/src/c24-mirror.ts` **(신규 389줄)** | 미러 엔진. **`class_schedules` 에 쓰는 문장이 0건**. 카페24 `:Class` 를 읽어 «만들었다면 어떤 판정이 났을지»만 계산 |
| `cloudflare-deploy/src/accounting-reports.ts` | 라우터에 `p === 'c24-mirror'` 한 갈래 + import 2줄 |
| `test-harness/c24_mirror_harness.mjs` **(신규 56건)** | 판정 함수를 **실제로 돌려** 검증 |

새 API: `GET /api/admin/reports/c24-mirror?since=&until=`
기본 창은 **오늘 ~ +14일**(KST), 판정 9종과 날짜별 집계를 돌려줍니다.

```
ok · already · update · manual_locked · diverged
no_teacher · no_student · conflict · not_whitelisted
```

핵심 함수는 `planMirror()` 하나이고 **순수 함수**입니다 — DB·Neo4j 없이 그대로
돌려볼 수 있어서 하니스가 그걸 실제로 실행합니다.

### 2-2. 문서

| 파일 | 내용 |
|---|---|
| `docs/강사_이관대조표_v2_20260831.xlsx` | **정본.** 강사 30명 × (그룹·로그인 아이디·계정 유무·원부/카페24/프로필 번호·파일럿 수업·카페24 8월 수업·9월 이후 예약) + 담당자/진행상태 기입란 |
| `docs/강사번호_대조표_20260831.xlsx` | v1(옛 판). 「완료 7명」 표기가 틀렸으므로 v2 를 보세요 |
| `docs/INDEX.md` | 위 둘 등록 |

### 2-3. 운영 D1 변경 3건 (지시받아 실행 — 전부 되돌릴 수 있는 «숨김»)

| 대상 | 한 일 | 되돌리는 법 |
|---|---|---|
| Teacher **WAN** | `teachers` 30번 신규 등록 + `teacher_account_links` 에 `mangoi_174` 연결 | `DELETE FROM teachers WHERE id=30` + 링크 삭제 |
| **HT FARRAH**(원부 3) | `active` 1 → **0** | `UPDATE teachers SET active=1 WHERE id=3` |
| **Mo**(프로필 17, 미국 오전반) | `status` 활동중 → **퇴사** | `UPDATE teacher_profiles SET status='활동중' WHERE id=17` |

WAN 은 8월 이후 카페24 수업이 **193건**인데 강사 원부에 없어 **배정 자체가 불가능**한
상태였습니다. 행을 지운 것은 하나도 없습니다.

---

## 3. 왜 그렇게 풀었나 — 검토했다가 버린 방법

### ⛔ 「아침 6시에 한 번 덮어쓰기」 (사장님 최초 안)

버렸습니다. 실측상 **9월 수업이 하루 2~25건뿐**인데 실제 수업은 하루 140건쯤입니다.
카페24 예약이 **며칠 전~당일에 채워진다**는 뜻이라, 6시에 한 번만 돌면 그 뒤에 잡힌
수업은 그날 못 넘어옵니다.
→ **15분마다 «오늘~모레» 좁은 창** + 밤 3시 «±두 달» 넓은 창으로 설계했습니다.

🔴 **덤으로, 6시 cron 은 애초에 만들 수 없습니다** — Cloudflare 계정 cron 한도가 5개인데
`wrangler.toml` 이 이미 5개를 다 쓰고 있습니다. 기존 15분 감시견과 밤 동기화에 얹는 것이
유일한 길이자 더 나은 설계였습니다.

### ⛔ 카페24로 되쓰기(양방향 동기화)

만들지 않았습니다. 사고 반경이 너무 큽니다. **한 방향(카페24 → 망고아이)만** 합니다.

### ⛔ 1단계부터 바로 쓰기

「전환일에 급하게 하지 않으려면」이 요구의 핵심이므로, **위험 0인 그림자 단계**를
먼저 뒀습니다. 매일 성적표만 내고 아무것도 안 만듭니다. 며칠 돌려 보면 전환일에 무슨
일이 날지 미리 보입니다.

### ⛔ 화이트리스트를 코드에 박기

사장님이 「스케줄이 바뀌거나 새 참여자가 들어오면 다른 강사가 할 가능성이 높다」고
하셔서, **표(`c24_mirror_teachers`)로** 뒀습니다. 배포 없이 화면에서 켜고 끕니다.
모드는 `off` / `whitelist` / `all` 셋이고 기본값은 가장 안전한 `off` 입니다.

### ⛔ 도장(수동 수정 표시)을 자동으로 떼기

「카페24가 정본이되 **사람 손이 이긴다**」가 사장님 결정입니다. 그런데 이 규칙의 뒷면은
**「한 번 손대면 그 수업은 영영 카페24를 안 따라간다」** 입니다. 그렇다고 시스템이
자동으로 도장을 떼면 사람이 일부러 고친 것을 되돌리는 셈이라 더 나쁩니다.
→ **어긋남 목록(`diverged`)** 으로 매일 보여 주고, **되돌리기는 사람이** 누릅니다.

### ⛔ `loadCafe24TeacherMap()` 을 api-admin.ts 에서 import

같은 판정이 거기 이미 있지만 import 하면 **순환 참조**가 생깁니다
(`api-admin` → `accounting-reports` → `c24-mirror` → `api-admin`).
→ `enroll-ops.ts` 가 쓰는 방식대로 **판정을 복제하고, 두 함수가 같은 답을 내는지
하니스가 실제로 돌려서 대조**합니다(F절 — api-admin 의 함수를 오려 내 실행).

### ⛔ 새 API 를 `/api/admin/c24-mirror` 로

`src/index.ts`(공동 금지구역)의 게이트 3곳을 고쳐야 합니다.
→ **`/api/admin/reports/` 밑**에 뒀습니다. 그 접두사는 인증·라우팅·강사차단에 이미
등록돼 있어 금지구역을 **한 줄도** 안 건드렸습니다.

### ⛔ 강사 번호를 숫자로 잇기

카페24 강사번호(9~196) ≠ 원부 `teachers.id`(1~30)이고 겹치는 자리에서 **다른 사람**입니다.
→ `teacher_payroll_auto`(번호와 이름을 함께 받은 유일한 표)로 **이름을 거쳐** 잇고,
정규화 후 **유일하게 맞을 때만** 채웁니다. 애매하면 비워 두고 리포트에 남깁니다.

---

## 4. 확인한 방법

- `node node_modules/typescript/bin/tsc --noEmit` → **exit 0**
  (⚠️ 이 컨테이너에는 `node_modules` 가 없어 `npm i --no-save --legacy-peer-deps typescript` 로
  타입스크립트만 따로 깔아 돌렸습니다)
- `node test-harness/run.mjs --fast` → **PASS 237 · SKIP 20 · FAIL 0**
- 새 하니스 `c24_mirror_harness.mjs` **56건** — 모드 3종 · 도장 · 못 이음 · 시각 변환 ·
  이름 정규화 대조 · 창 경계 · 배선까지
- CI 배포 게이트 [run #725] **success** (122초)
- 배포 [run #2831] **success** — 두 워커 + 헬스체크 + TURN 점검 전부 통과
- 배포 확인은 커밋 해시만 믿지 않고 **`git show origin/main:` 로 파일 내용**을 확인
  (`c24-mirror.ts` 389줄 · `accounting-reports.ts` 배선 2곳)
- D1 은 **조회(SELECT)만** 했습니다. 위 2-3 의 3건은 사장님 지시로 실행한 것입니다

### ⏳ 확인하지 못한 것

- **배포된 API 의 실제 응답을 못 봤습니다.** 프록시가 `mangoi.ai` 를 막습니다
  (CLAUDE.md 4-1-1). 제가 확인한 것은 **「코드가 나갔다」까지**입니다.
- 그래서 `no_teacher` 가 실제로 몇 건인지, 카페24(Neo4j)가 이 창에서 응답하는지는
  **사람이 열어 봐야** 압니다.

---

## 5. 남은 것 / 주의할 것

### 사람이 해야 하는 것

1. **관리자로 로그인해 `https://mangoi.ai/api/admin/reports/c24-mirror` 를 한 번 열기.**
   볼 것 — `summary.no_teacher`(전환일 준비도) · `summary.conflict` · `mode` 가 `"off"` 인지.
   502 `c24_unreachable` 이 나오면 카페24 쪽이 안 닿는 것이고, **그건 0건이 아니라 «못 냈다»** 입니다.
2. **계정이 없는 현직 강사 3명** — JANICE `mangoi_026` · JENNY `mangoi_148` · RICA `mangoi_007`.
   시간표를 넣어도 **본인 화면에는 안 보입니다.** 각자 그 아이디로 한 번 로그인하면
   계정이 자동 생성됩니다(옛 LMS 통과 인증).
   ⚠️ 프로필 메모의 아이디와 실제 쓰는 계정이 **다를 수 있습니다** — 강선생님은 메모엔
   `mangoi_cn2` 인데 실제로는 `hq_t_kang` 을 씁니다. 제가 확인한 것은 「프로필에 적힌
   아이디로는 계정이 없다」까지입니다.

### 2단계에서 할 것 (승인 후)

- 도장 두 줄 — `PATCH`·`DELETE /api/admin/class-schedules/:id` 에서 `source` 가
  `c24-mirror` 면 `c24-mirror:manual` 로 바꾸기
- 실제 생성(창 안 `c24-mirror` 행만 지우고 다시 넣기) + 15분/야간 cron 에 얹기
- 관리자 화면 — 모드 스위치 · 강사 켜기/끄기 · 매일 성적표 · 되돌리기 버튼

### 별건으로 남은 것

- **대소문자 중복 계정 2건** — `mangoi_167`/`Mangoi_167`(HANNAH),
  `mangoi_168`/`Mangoi_168`(LEN). 출근·급여 기록이 두 갈래로 쪼개질 수 있습니다.
- **JINETTE 원부 12번을 숨길지** — 퇴사(사장님 확인)인데 아직 `active=1` 입니다.
  HT FARRAH 와 같은 처리를 할지 지시를 못 받아 그대로 뒀습니다.
- 카페24 `:Class` 의 **9월 이후 예약이 230건뿐**입니다(하루 2~25건). 실제 수업량보다
  훨씬 적으므로, 미러를 켜기 전에 «카페24에 예약이 언제 채워지는지»를 한 번 봐야 합니다.

### ⚠️ 이 작업에서 제가 틀렸던 것 두 가지 (같은 실수 방지)

1. **「완료 7명」** — 파일럿 수업을 카페24 이관분으로 오해했습니다. 사장님 정정으로
   바로잡았습니다. 📌 `class_schedules` 를 볼 때는 **`source` 와 `created_at` 을 함께**
   보세요. `adm-enroll:*` 은 사람이 수강신청으로 넣은 것입니다.
2. **「mangoi_157 은 없는 아이디」** — 「계정이 안 만들어졌다」는 뜻이었는데 마치 사장님이
   틀리신 것처럼 읽히게 적었습니다. 실제로 `mangoi_157` 은 Jinette 것이 맞습니다
   (프로필 14번 메모에 기록). 📌 **«없다»가 «어느 표에 없다»인지 밝혀 적을 것.**

---

## 6. 후속 (같은 날 오후) — 그림자가 잡은 것 두 가지와 «겹쳐 보기»

### 6-1. 그림자 1일차 성적표가 잡은 것 — 강사 한 사람 때문에 13건이 막혀 있었다

배포 첫 성적표(오늘~9/14, 234건):

| 판정 | 건수 | 뜻 |
|---|---|---|
| `not_whitelisted` | 220 | 만들 수 있음(그림자라 안 만듦) |
| `no_teacher` | 14 | 강사를 못 이어 못 만듦 |
| `no_student` | **0** | 학생은 234건 전부 이어짐 |
| `conflict` | **0** | 파일럿 수업과 부딪히지 않음 |

막힌 14건 중 **13건이 «Teacher Ness» 한 사람**이었습니다. 이름은 한 글자도 안 틀렸고
접두어 때문이었습니다 — 카페24 `Teacher Ness` → `ness` / 원부 `HT NESS` → `ht ness`.
나머지 1건은 「스케줄변경중」(사람이 아님)이라 **정상 동작**입니다.

**고친 방법 — 데이터가 아니라 판정을 고쳤습니다.** 처음에는 원부 이름을 `HT NESS` → `NESS`
로 바꾸는 D1 수정을 제안했는데, 확인해 보니 **이 저장소가 이미 같은 해법을 갖고 있었습니다**
(`js/adm-q6.js` 의 `ph54ResolveTeacherId`, 그리고 `teacher_weekly_calendar_id_space_harness`
가 「HT NESS 는 낱말 단위로 맞다」로 못 박아 둠). 그래서 서버 판정에 같은 규칙을 넣었고
**운영 데이터는 한 줄도 안 바꿨습니다** (`buildRosterResolver`).

⛔ 부분일치는 여전히 금지입니다 — `FAR` 가 `HT FARRAH` 에 걸린 사고가 있었습니다.
낱말 **전체**가 같을 때만이고, 후보가 둘 이상이면 잇지 않습니다.

### 6-2. 🔴 카페24에 «없는 수업» 이 들어 있다 — 그래서 아직 «만들지» 않습니다

사장님 확인(8/31): **허윤아 8/31 17:00 에는 Zee 한 명뿐인데** 카페24에는 네 건이 있었습니다.

| class_id | 학생 | 길이 | 실제 |
|---|---|---|---|
| `c24-511741` | Kes | 30분 | ❌ 없음 |
| `c24-511742` | Sid | 20분 | ❌ 없음 |
| `c24-511743` | Zee | 30분 | ✅ |
| `c24-511744` | Zee | 20분 | ✅ |

번호가 연속이고 30/20분이 짝을 이루는 것으로 보아 **강사 변경 잔재**로 보입니다
(카페24에 「스케줄변경중」이라는 이름의 강사가 실제로 있습니다).
⚠️ **이건 제 추론이고 측정이 아닙니다** — 확인된 것은 「그 시각에 Zee 한 명뿐이다」까지입니다.

**`attendance` 로는 못 가립니다** — `importCafe24Attendance` 가 `class_state` 를
`present`/`scheduled` **두 값으로 뭉개서** 취소·변경 표식이 그 단계에서 사라집니다
(창 안 362행이 전부 `scheduled`). 그래서 성적표에 **카페24 원본 `class_state` 를 그대로**
싣도록 했습니다(`by_state`). 값이 한 가지뿐이면 상태로는 못 가른다는 뜻이고,
그때는 다른 속성을 찾아야 합니다.

⟹ **`class_schedules` 에 행을 «만드는» 2단계는 이 물음이 풀린 뒤입니다.** 지금 만들면
학생·강사가 있지도 않은 수업을 기다립니다.

### 6-3. 그래서 «보기 전용 겹쳐 보기» 를 먼저 넣었습니다

사장님 요청: 「카페24에 있는 수업이 mangoi.ai 도 잡히도록. 바로 잡는 게 어렵다면
**잡힌 것처럼 보이게라도** 해줘」

관리자 › **강사 스케줄(주간 통합 캘린더)** 에 카페24 수업을 **파란 점선 카드**로 겹쳐
그립니다(`js/adm-q6.js`). 자료는 그림자 성적표를 그대로 씁니다 — 화면이 판정 규칙을
또 만들면 반드시 갈라집니다.

- 카드에 **「카페24」 배지 + 「카페24에만 있음」** 을 글자로 적습니다(진짜 수업으로 오해 방지)
- ⛔ **`draggable`·`data-idx`·`data-block` 을 안 답니다** — 진짜 수업 배열의 인덱스와
  섞이면 카페24 카드를 끌었을 때 **엉뚱한 수업에 PATCH** 가 나갑니다
- 이미 망고아이에 행이 있는 판정(`already`·`update`·`conflict`·`manual_locked`·`diverged`)은
  겹쳐 그리지 않습니다 — 같은 수업이 두 번 보입니다
- 강사를 못 이은 것은 **아무 칸에나 놓지 않고** 건수만 알립니다
- 「카페24 수업 함께 보기」 체크박스(기본 켬) · 못 읽으면 **사유를 적습니다**
  (빈 화면이 「오늘은 수업이 없나 보다」로 읽히는 것을 막습니다).
  권한 없는 계정(401/403)에는 경고를 띄우지 않습니다 — 그건 고장이 아닙니다

**검토했다가 버린 방법**

| 버린 것 | 왜 |
|---|---|
| `class_schedules` 에 진짜 행을 만들기 | 6-2 의 유령 수업이 그대로 «열리는 수업» 이 됩니다 |
| 카페24 수업을 `ph54State.records` 에 섞어 넣기 | 드래그·삭제·차단이 전부 그 배열의 **인덱스**로 돕니다. 섞으면 엉뚱한 수업에 PATCH·DELETE 가 나갑니다 |
| 진짜 수업과 같은 모양으로 그리기 | 매니저가 「잡혀 있다」고 읽습니다. **화면이 거짓말하면 안 됩니다** |
| 화면에서 «중복인지» 를 다시 판정 | 판정이 두 벌이 되어 서버 성적표와 갈라집니다. 성적표의 `verdict` 를 그대로 씁니다 |

**확인한 방법**

- `tsc --noEmit` exit 0
- `node test-harness/c24_mirror_harness.mjs` → **PASS 84 / FAIL 0** (I절 15건 신설)
- `node test-harness/run.mjs --fast` → **PASS 241 / SKIP 20 / FAIL 0**
- 🖥 **진짜 Chromium**: `PW_DIR=/tmp/pw node test-harness/manual/c24-overlay-browser.mjs`
  → **PASS 17 / FAIL 0**. 카드가 실제로 그려지고, 드래그가 안 되고, 진짜 수업 카드는
  그대로 끌리고, 성적표를 못 읽으면 사유가 화면에 적히는 것까지 확인했습니다.
  ⚠️ 이 검사는 **게이트가 물어 가지 않습니다** — 이 캘린더를 손대면 사람이 불러야 합니다.
- D1 은 조회만 했습니다.

### 6-4. 다음 (승인 필요)

1. **유령 수업 가리기** — 성적표의 `by_state` 를 하루 보고 값이 갈리는지 확인.
   갈리면 그 값으로 거르고, 안 갈리면 카페24 쪽에 «취소를 어떻게 표시하는지» 물어야 합니다.
2. **같은 배치 안 겹침 판정** — 같은 학생·같은 시각이 한 번에 여러 건 오면 `conflict` 로.
   (허윤아 17:00 이 정확히 그 모양이었습니다.)
3. 그 둘이 끝난 뒤 **2단계(실제 생성)**.


---

## 7. 2단계 — Ana 한 사람만 «실제로» 만들었다 (2026-08-31 밤 · 사장님 승인)

### 7-1. 무엇을 승인받았나

사장님: 「Ana 강사의 수업만 가져와서 보여줘봐. 이상 없이 가져와지는지 볼게.」 →
읽어서 채점해 보여 드린 뒤 「Ana 한 사람만 켜서 실제로 만들어 볼까요?」 → **「응」**.

### 7-2. 켜기 전에 채점한 결과 (읽기만)

Ana = 카페24 **182** / 원부 **7**(ANA) / 프로필 12. `Teacher Ana` → `ana` ↔ 원부 `ANA` → `ana`
**정확일치**이고 후보가 하나뿐이라 남의 이름이 붙을 여지가 없다.

| 검사 | 결과 |
|---|---|
| 강사가 원부에 이어지는가 | ✅ 15/15 |
| 학생 계정이 망고아이에 있는가 | ✅ 15/15 |
| 망고아이 수업과 시간이 겹치는가 | ✅ 0건 |
| **유령 수업 패턴**(같은 시각 여러 건) | ✅ **0건** — 허윤아 17:00 같은 모양이 없다 |

⚠️ 이 채점은 **D1 `attendance`(야간 동기화 사본)** 을 읽은 것이다. 화면 성적표는 Neo4j 원본을
읽으므로 값이 같아야 하지만 **같은지까지는 확인하지 못했다**(프록시가 `mangoi.ai` 를 막는다).

### 7-3. 넣은 기계 (PR #638)

- **`applyMirror()`** — ⛔ `dry_run` 이 **기본값**. 하는 일은 셋뿐(만들기·고치기·사라진 것 내리기).
  - ⛔ 손대는 행은 `source='c24-mirror'` 하나뿐. **`UPDATE` 의 `WHERE` 에도 `source`** 를 건다 —
    그 사이 사람이 손대 도장이 찍혔으면 **0행**이 되어 경합에서도 「사람 손이 이긴다」가 지켜진다.
  - ⛔ 지우지 않는다. `status='cancelled'` 로만 내린다.
  - 🔴 **카페24 조회가 0건이면 «취소» 를 통째로 건너뛴다** + 건너뛴 «이유» 를 결과에 적는다.
  - `notes` 에 `c24:<class_id>` — 사라진 수업을 되짚는 유일한 실마리.
- **`POST /api/admin/reports/c24-mirror/{apply,mode,teacher}`** — 게이트를 **둘 따로**
  (강사 `isTeacher` · 지사/대리점 `isOrgScopedRole`). ⛔ `canEditOrg` 금지(교사에게도 true).
- **도장** — `PATCH`·`DELETE /api/admin/class-schedules/:id` 가 미러 행을 건드리면 **같은 UPDATE 안에서**
  `source='c24-mirror:manual'`.
- **캘린더 «코스 기간» 제외** — `start_date/end_date` 는 «같은 source = 같은 수강신청» 전제인데
  미러는 source 가 전부 하나라, 안 빼면 9/1 카드에 「9/1~9/15」 가 붙어 6주 코스로 읽힌다.

### 7-4. 실제로 만든 것 — 4건

⚠️ **11건이 아니라 4건이다.** 작업 시점에 KST 가 **9/1 00:06** 으로 넘어가 8/31 수업 11건은
이미 지난 수업이 됐다. 미러 창은 «오늘~+14일» 이라 앞으로의 4건만 대상이다.

| id | 날짜 | 시각 | 학생 | notes |
|---|---|---|---|---|
| 1027 | 2026-09-01 | 19:40 | 이다연 | `c24:510737` |
| 1028 | 2026-09-04 | 21:10 | 인채은 | `c24:506262` |
| 1029 | 2026-09-08 | 19:40 | 이다연 | `c24:510735` |
| 1030 | 2026-09-15 | 19:40 | 이다연 | `c24:510736` |

전부 `source='c24-mirror'` · `schedule_kind='one_off'` · `class_type='regular'` · `teacher_id='7'` · 20분.
설정은 `c24_mirror_config.mode='whitelist'` · `c24_mirror_teachers('7', enabled=1)`.

### 7-5. 🔴 정직하게 적어 둘 것 — 이 4건은 «코드가» 만든 게 아니다

프록시가 `mangoi.ai` 를 막아 배포된 API 를 부를 수 없어서, **같은 값·같은 컬럼으로 D1 에 직접
넣었다.** 그래서 다음 두 가지가 사실이다.

1. `applyMirror()` 는 **아직 운영에서 한 번도 돌지 않았다.** 하니스에서 가짜 D1 으로 돌려
   검증했을 뿐이다. 처음 돌 때 이 4건은 `already` 로 보여야 정상이고, 그렇지 않으면
   컬럼 매핑이 어긋난 것이다.
2. 자료 출처가 Neo4j 가 아니라 **D1 `attendance` 사본**이라, 8/31 03:00 동기화 **이후** 카페24에
   추가된 Ana 의 수업은 빠져 있을 수 있다.

### 7-6. 검토했다가 버린 방법

| 버린 것 | 왜 |
|---|---|
| 야간/15분 cron 에 미러를 얹어 자동 실행 | `src/index.ts`(공동 금지구역)를 건드려야 한다. 승인 범위 밖이라 안 했다 |
| 관리자 화면에 «미러 실행» 버튼부터 만들기 | 화면을 못 보는 상태에서 버튼만 늘리는 셈이다. 기계와 안전장치가 먼저다 |
| 8/31 수업 11건도 함께 넣기 | KST 로 이미 지난 수업이다. 지난 수업을 만들면 출석·급여 쪽에 없던 행이 생긴다 |
| `source` 를 수업마다 다르게(`c24-mirror:510737`) | 그러면 「내 행만 손댄다」 SQL 조건이 통째로 헛돈다. 대신 `notes` 로 구분한다 |
| 유령 수업 판정을 `class_state` 값으로 지금 넣기 | 그 값의 «뜻» 을 아직 모른다. 추측해서 거르면 진짜 수업이 사라진다 |
| 미러 행을 캘린더에서 드래그 못 하게 잠그기 | 오히려 반대다 — 사람이 옮기면 그 순간 도장이 찍혀 미러가 손을 뗀다. 잠그면 「사람 손이 이긴다」를 쓸 방법이 없다 |

### 7-7. 확인한 방법

- `tsc --noEmit` exit 0
- `c24_mirror_harness` **89 → 122건**(J절 33건 신설 — 가짜 D1 을 물려 `applyMirror` 를 실제로
  돌리고 나간 SQL 을 센다: 화이트리스트 밖·도장 찍힌 행·파일럿 겹침·학생 없음에서 **쓰기 0건**,
  두 번 돌려도 두 번 안 만드는지(멱등), 카페24 0건이면 취소 건너뛰는지)
- `run.mjs --fast` PASS 241 / FAIL 0
- D1 실측으로 만든 4행을 다시 읽어 확인
- ⏳ **사장님이 화면에서 한 번 보셔야 끝난 것이다** — 관리자 › 강사 스케줄 › 강사 필터 `ANA`.

### 7-8. 남은 것

1. **자동 실행(cron)** — 승인 필요(`src/index.ts`).
2. **관리자 화면** — 모드 스위치 · 강사 켜기/끄기 · 성적표 · 되돌리기 버튼.
3. **유령 수업 가리기** — `by_state` 를 하루 보고 값이 갈리는지 확인. 안 갈리면 카페24에
   «취소를 어떻게 표시하는지» 물어야 한다.
4. **같은 배치 안 겹침**(같은 학생·같은 시각 여러 건)을 `conflict` 로 잡기.
