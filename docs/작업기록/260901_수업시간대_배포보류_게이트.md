# 수업 시간대에는 배포를 보류한다 — 김선우 학생 「대화가 이어지질 못했어요」에서 출발

- 날짜: 2026-09-01
- 작업자: 사장님(navy111p) + Claude Code
- 브랜치 / PR: `claude/internet-error-investigation-f46nd4` → [PR #708](https://github.com/navy111p-sudo/mangoiweb/pull/708) 병합 (`a3e9a3e`, squash)

---

## 1. 왜 했나

사장님 제보 — **「김선우는 수업이 너무 안 되었어요. 인터넷 오류가 너무 심해 대화 자체가 이어지질 못했어요. 이유가 뭔지 확실하게 확인해줘」**

해당 수업을 특정했습니다: 김선우 = `jye46712`, 2026-09-01 21:20 KRYSTEL, **`class-1078-20260901`**
(사장님이 주간 스케줄 화면으로 KRYSTEL·화요일 21시를 확인해 주셔서 방을 확정했습니다.)

### 잰 것 — 그 방의 실측

**① 학생 화면이 사실상 멈춰 있었다 (녹화 크기가 증거)**

| 녹화 | 길이 | 크기 | 실효 비트레이트 |
|---|---|---|---|
| 학생(김선우) 화면 | 29:56 | 26.2MB | **117 kbps** |
| 강사(Krystel) 화면 | 22:48 | 227.1MB | **1,328 kbps** |

녹화는 «그 사람 화면에 보이던 것» 을 인코딩합니다(목표 2,500kbps). 강사 화면은 정상인데 학생 화면만 **1/11**.

**② 회선 계측 (`vc_quality` 21틱)**

| | 김선우 | 강사 Krystel |
|---|---|---|
| 내가 보낸 손실 평균 / **최대** | 1.35% / **40.5%** | 0.03% / 7% |
| 영상이 아예 안 나간 틱(`novideo`) | **70** | 9 |
| 받은 소리 «메운» 비율 평균 / 최대 | 7.3% / **14.4%** | 7.4% / **15.8%** |
| 받은 영상 멈춤 횟수(`rx_freeze`) | **142회** | **128회** |
| RTT 최대 | 419ms | 349ms |

손실 스파이크(20~40%)는 **학생 쪽에서만** 납니다. 강사는 송신 0%인데 학생에게서 오는 것을 최대 5.5% 잃었습니다.

**③ 1:1 수업인데 참가자가 3명 — 유령 접속**

강사 녹화의 `participant_names` = `["교사 Teacher - Krystel", "krxyduks3iein20rd2j0yj", "김선우"]`.
가운데는 이름표(`vc-label-<uid>`)가 없어 id 가 그대로 들어간 것 = **화면에 타일이 없는 유령**.
같은 시간 다른 방: `class-1086` **5명**, `class-988`·`996` 3명.

**④ 수업 도중 운영 워커를 계속 재배포했다**

19:33~22:06 사이 **22회**. 김선우 수업(21:15~21:50) 안에만 4회. 배포 직후 끊긴 것:

| 운영 워커 재배포 | 그 직후 |
|---|---|
| 21:43:57 | **+8초** `class-1070` 강사 · **+37초** `class-1078` 강사(Krystel) |
| 21:38:03 | +27초 `class-996` Teacher Kaye |
| 21:40:25 | +30초 `class-996` 재입장 |
| 21:02:39 | +41초 `class-1094` 강사 |

`class-996` 은 강사가 10분 사이 네 번 끊겨 녹화가 **4:03 / 3:18 / 0:18** 로 토막났습니다(사장님이 보내주신 첫 스크린샷).

### 배제한 것

- **TURN 정상.** 21:46:36 배포 로그 실측 — 기본·운영 둘 다 `kv-cache → ✅ 정상`. 무료 폴백 아니었습니다.
- **방 갈림 아님.** 강사·학생이 같은 방에 있었고 강사는 21:21:38~21:44:34 **한 번도 안 끊겼습니다.**

### 결론 — 두 겹

- **주된 원인은 김선우 학생 쪽 업링크**(측정). 이건 코드로 못 고칩니다.
- **그 위에 우리 몫이 겹쳤다**(측정된 상관 + 추론). 이 작업이 고친 것은 **우리 몫뿐**입니다.

> ⚠️ 「배포 → DO 재시작 → 유령」 사슬은 **코드를 읽고 세운 추론**입니다.
> 배포 시각과 끊긴 시각이 30초 안팎으로 겹친 것은 실측이지만, **로그로 직접 잇지는 못했습니다.**
> 완료형으로 적지 마세요.

---

## 2. 무엇을 바꿨나

### `.github/scripts/class-window.mjs` (신설) — 판정 정본

- 수업 시간대 **13:00 ~ 01:20 KST** 면 보류. 자정을 넘으므로 되감기 판정.
- 우회: `FORCE_NOW` · 커밋 메시지의 `[deploy-now]`
- CLI 종료코드 — `--exit-on-hold` 를 줄 때만 **보류 = 2**, 안 주면 **항상 0**
- 실측 근거·사고 서술이 이 파일 머리말에 있습니다.

### `.github/workflows/deploy.yml`

- 배포 5개 step(base · production · 헬스체크 2 · TURN 점검)에 `steps.class_window.outputs.hold != 'true'`
- 보류돼도 **초록불**, 그리고 **배포 게이트(tsc·회귀 하니스·`?v=`)는 그대로 돕니다**
- `schedule: '30 16 * * *'` = **01:30 KST 몰아 배포**
- `workflow_dispatch` 에 `force_now` 입력

### `deploy.ps1` — `[0c]` 게이트 (공동 금지구역, 사장님 지시로 수정)

- 같은 모듈을 `--exit-on-hold` 로 부르고 종료코드로 판정. **PowerShell 로 복제하지 않습니다.**
- 보류(2)면 파일 하나 안 건드리고 `exit 1`. 판정 실패(0·2 아님)도 막습니다.
- 우회 `-ForceNow`. ⛔ `-SkipSmoke` 로는 안 꺼집니다.

### `test-harness/deploy_class_window_harness.mjs` (신설, 60종)

### 곁가지 (이 변경 때문에 걸려서 함께 고친 것)

- `test-harness/deploy_live_drift_gate_harness.mjs`
  - `param(...)` 을 **모양 통째로** 정규식에 박아 둬서 스위치를 하나 더하자 FAIL → 뜻으로 묻도록
  - 0b 검사 구간이 `# [0] 배포 전 안전 게이트` 까지라 그 사이에 낀 `[0c]` 를 함께 먹고 있었음 → 0a·0b 가 아닌 첫 `[0x]` 배너에서 끊도록
- `CLAUDE.md` 2장 함정 표에 1건 등재

---

## 3. 왜 그렇게 풀었나 — 검토했다가 버린 방법

| 버린 것 | 이유 |
|---|---|
| **보류를 «실패(빨간불)» 로** | main 이 매 문서 커밋마다 빨개집니다. 게이트 실패와 구분이 안 되고, 이 저장소는 재실행이 토큰 권한으로 403 이라 사람이 Actions 화면을 눌러야 합니다 |
| **보류하고 «그냥 넘어가기»(몰아 배포 없이)** | `deploy.yml` 2026-08-07 주석이 정확히 이것을 경고합니다 — 「나갔어야 할 게 안 나간 것」은 터지기 전까지 아무도 모릅니다 |
| **몰아 배포를 «바뀐 게 있을 때만»** | 상태를 어디 저장할지가 문제이고, 없으면 «안 나갔는데 안 나간 줄 모르는» 상태가 됩니다. 01:30 은 카페24 예약 수업 0건이라 매일 재배포해도 무해합니다 |
| **판정을 PowerShell 로 다시 쓰기** | 같은 판정이 두 곳이면 한쪽만 고쳐집니다(이 저장소가 반복해 밟은 함정). 종료코드로만 주고받습니다 |
| **창을 예약표(`class_schedules` 최대 22:40)로 정하기** | 🔴 **틀립니다.** 접속을 분 단위로 세니 23:30–23:50 · **00:00–00:20(22건)** · 00:40–01:10 수업이 실재했습니다. 처음에 23:30 을 끝으로 잡았다가 몰아 배포(23:35)가 **수업 한복판**에 떨어질 뻔했습니다 — 고치려던 사고를 그대로 재현할 뻔 |
| **`deploy.ps1` 에서 사람에게 물어보기(prompt)** | 비대화형 실행에서 멈춰 버립니다. 거절 + 「언제 하면 되는지」 안내가 이 파일의 다른 게이트(`0a`·`0b`)와 같은 방식입니다 |
| **`deploy.ps1` 게이트를 fail-open 으로** | 판정이 실패했는데 통과시키면 게이트가 «있는 척» 만 합니다. node 가 아예 안 도는 경우는 `[0a]` 에서 이미 멈추므로, 여기 오면 이 파일 문제입니다 |

### ⛔ 다음 사람이 조심할 것

**몰아 배포 크론은 반드시 창 «밖» 이어야 합니다.** 창 안이면 보류를 풀러 오는 그 실행마저 스스로 보류해 **배포가 영영 안 나갑니다.** 하니스 ④가 크론 시각을 판정에 실제로 넣어 막습니다.

---

## 4. 확인한 방법

- 타입체크 `node node_modules/typescript/bin/tsc --noEmit` — **통과**
  (이 컨테이너에 `node_modules` 가 없어 `npm ci --legacy-peer-deps` 로 설치 후 실행. CLAUDE.md 2장 Node 22/24 함정)
- 회귀 하니스 `node test-harness/run.mjs --fast` — **PASS 261 / FAIL 0 / SKIP 20**
- 새 하니스 60종 — 문자열이 아니라 **판정 모듈을 실제로 돌리고**, 종료코드는 **node 를 띄워** 확인
- **변이시험 9종 전부 진짜 FAIL** (헛도는 검사가 아님)

| 되돌린 것 | FAIL |
|---|---|
| 자정 되감기 제거 | 22건 |
| 몰아 배포 크론을 창 안으로 | 1건 |
| CI 배포 step 의 게이트 제거 | 1건 |
| `force_now` 배선 제거 | 1건 |
| 창 시작을 14:00 으로 | 1건 |
| `deploy.ps1` 게이트 호출 제거 | 2건 |
| 보류(2)에서 안 멈추게 | 1건 |
| `-SkipSmoke` 로 꺼지게 | 1건 |
| 플래그 없이도 종료코드 2 를 내게 | 1건 |

### 🟢 운영에서 실제로 동작한 것을 확인했습니다

병합 커밋(`a3e9a3e`)의 배포 run [33518025708](https://github.com/navy111p-sudo/mangoiweb/actions/runs/33518025708), **23:12:40 KST**:

```
✅ 수업 시간대 배포 보류 판정            success
✅ 배포 게이트 (tsc · 회귀 하니스 · ?v=)  success   ← 검사는 그대로 돌았다
⏭ Deploy base Worker                    skipped
⏭ Deploy production Worker              skipped
⏭ Health check — base / production      skipped
⏭ TURN 경로 점검                         skipped
전체 결론: success
```

지금까지 같았으면 23:14 에 운영 워커가 재배포되어 그 시각 진행 중이던 수업이 끊겼을 것입니다.

---

## 5. 남은 것 / 주의할 것

### 아직 확인 못 한 것 (사람이 봐야 함)

1. 🔴 **`deploy.ps1` 의 PowerShell 문법을 파서로 검증하지 못했습니다** — 이 컨테이너에 `pwsh` 가 없습니다.
   중괄호·괄호·따옴표 균형만 기계적으로 확인했습니다(추가분 `{}` 4/4 · `()` 16/16 · `"` 44, 원본 대비 불균형 0).
   ✅ 확인법: **01:20 KST 이후**에 로컬 배포를 한 번 돌려 `[0c/7] … 수업 시간대 아님 — 배포 진행` 이 뜨는지 보기.
   ℹ️ 문법 오류면 PowerShell 이 파일 전체를 먼저 파싱하므로 **스크립트가 아예 시작 안 하고** 즉시 오류를 냅니다(반쯤 배포되는 상태는 안 생깁니다).
2. **01:30 KST 몰아 배포의 첫 실행** — 이 저장소에서 `schedule` 트리거는 처음이라 첫 회는 눈으로 봐야 합니다.

### 이 작업이 **고치지 않은 것**

- 🔴 **김선우 학생의 업링크** — 주된 원인이고 그대로입니다. 학부모께 유선 연결·공유기 재부팅·같은 시간 다른 기기의 다운로드 끄기를 안내하는 것이 다음 수업에 제일 크게 작용합니다.
- **유령 참가자 자체** — 배포가 줄면 발생 빈도는 줄지만, DO 재시작 시 `user-left` 를 못 보내는 구조는 그대로입니다. 고치려면 `src/video-call-room.ts`(공동 금지구역)를 손봐야 하므로 사람이 결정할 일입니다.
- **녹화 webm 에 총 길이·탐색 색인이 없는 것** — 별건(CLAUDE.md 2장에 이미 등재).

### 창을 바꿀 때

`CLASS_WINDOW_KST` 상수 한 줄만 고치면 되지만, **`deploy.yml` 의 `schedule` 크론도 함께** 봐야 합니다(위 ⛔). 하니스 ④가 어긋나면 FAIL 냅니다.

---

# 후속 (2026-09-02) — 창을 좁히고, 몰아 배포를 네 번으로

## 6. 첫날 밤에 무슨 일이 있었나

**막는 쪽은 완벽했습니다.** 23:12~00:23 에 병합된 8건이 전부 보류됐고(배포 5 step `skipped`), 실행은 초록불이었습니다.

🔴 **푸는 쪽이 늦었습니다.** 01:30 예정이던 몰아 배포가 **04:27:38** 에 실행됐습니다 — **177분 지연**. 그동안 main 커밋 21건(코드 8건)이 라이브에 없었습니다.

- 01:53 · 02:46 두 번 확인할 때는 `event=schedule` run 이 **0건**이었고, 그래서 사장님께 「안 나갔습니다」라고 **두 번 잘못 보고**했습니다. 04:27 에 늦게 걸린 것을 09:05 에야 확인했습니다.
- MCP 로 수동 실행은 **403 Resource not accessible by integration** — 제가 못 돌립니다(CLAUDE.md 2장 기록과 동일).

⟹ **GitHub 의 `schedule` 은 우선순위가 낮은 대기열입니다.** 지연되거나 아예 누락될 수 있어서, 하나에만 기대면 「나갔어야 할 게 조용히 안 나가는」 상태가 그대로 재현됩니다.

## 7. 무엇을 바꿨나

### ① 몰아 배포를 하루 네 번으로 (`deploy.yml`)

창 밖에 흩어 뒀습니다 — **23:17 · 02:41 · 07:09 · 13:23 KST**. 마지막 회차는 창이 열리기 37분 전 «마지막 기회» 입니다.

> 📌 **이 네 시각은 그 뒤 전부 바뀌었습니다 → 11장.** 「창 밖이니 안전하다」는 전제가 틀렸습니다.
> 지금 값은 `deploy.yml` 의 `schedule` 목록 하나가 정본입니다.

⚠️ 분을 `:00`·`:30` 이 아닌 값으로 둔 것은 **알려진 완화책**입니다(정각·반각에 전 세계 크론이 몰립니다). ⛔ 다만 **그 효과를 우리가 잰 적은 없습니다.** 잰 것은 「177분 지연」 하나뿐입니다.

### ② 창을 «화·목 14:00~23:00» 으로 (`class-window.mjs`)

2026-09-02 사장님 지시입니다. 처음에는 «매일 13:00~01:20» 이었는데, 그때는 카페24 예약(`c24-*`)까지 함께 세어 잡았습니다 — 그 방들은 우리 워커의 DO 를 안 씁니다.

🔴 **요일은 실측과 다릅니다. 그래도 화·목으로 뒀습니다 — 사장님이 실측을 보고 정하셨습니다.**

| 앞으로 잡힌 수업 (2026-09-02~) | 건수 | 시간대 |
|---|---|---|
| 목 | 45 | 14:00~21:50 |
| **수** | **40** | 14:20~22:10 (전부 `c24-mirror`) |
| 화 | 30 | 14:00~21:50 |
| 금 | 7 | 17:00~21:40 |
| 월 | 5 | 13:00~21:40 |
| 토·일 | 0 | — |

⟹ **수·금·월 수업은 이 게이트가 보호하지 않습니다.** 그날 배포하면 진행 중인 수업이 끊깁니다.
⛔ 이것을 «다른 날은 수업이 없다» 로 바꿔 적지 마세요. 사실은 **「그날 끊겨도 감수한다」** 입니다. 그 문장이 바뀌면 수요일에 수업이 끊긴 이유를 아무도 못 찾습니다.

### ③ 시각을 안내 문구에서 없앰

`class-window.mjs` 요약 · `deploy.yml` 배포 요약 · `deploy.ps1` 안내에서 **몰아 배포 시각과 창 끝 시각을 뺐습니다.** 2026-09-01 에 크론만 옮기고 주석이 옛 시각으로 남아 「언제 나가는지」를 거짓으로 말한 적이 있습니다(#721). 정본은 `deploy.yml` 의 `schedule` 목록 하나뿐이고, 하니스가 «베껴 적지 않았는가» 를 검사합니다.

## 8. 이번에 버린 방법

| 버린 것 | 이유 |
|---|---|
| 크론이 «나갈 게 있을 때만» 배포하게 하기 | 「마지막으로 배포된 커밋」을 어디에 적을지가 새 문제입니다(태그·캐시·Actions API). 새벽 회차는 수업이 없어 무의미한 재배포가 무해하므로, 복잡도를 늘리지 않는 쪽을 택했습니다 |
| 크론을 늘리는 대신 «보류될 때 문자» | 발송 시크릿이 Actions 쪽에 없고, 알림을 늘리면 무뎌집니다. 근본은 «풀어 주는 장치가 하나뿐» 인 것이라 그쪽을 고쳤습니다 |
| 크론을 아예 빼고 «창 밖 첫 push» 에만 기대기 | 오늘처럼 새벽에 push 가 끊기면 그대로 밀립니다 |
| 요일을 실측대로 월~금으로 넓히기 | 사장님이 실측(수 40건 포함)을 보고 화·목으로 정하셨습니다. 제 판단으로 넓히지 않았습니다 |

## 9. 검증

- 타입체크 통과 · `run.mjs --fast` **PASS 264 / FAIL 0 / SKIP 20**
- `deploy_class_window_harness` **67종**
- 변이시험 5종 전부 진짜 FAIL — 요일 검사 제거(5건) · 크론을 창 안으로(2건) · 한 시간대로 몰기(1건) · 크론 1개로(1건) · 안내 문구에 시각 베끼기(1건)

## 10. 아직 확인 못 한 것

1. **`deploy.ps1` PowerShell 문법** — 여전히 미검증(`pwsh` 없음). 화·목 14:00~23:00 밖에 한 번 돌려 주세요.
2. **네 번으로 늘린 크론이 실제로 도는지** — 첫 회차를 아직 못 봤습니다.
3. **수·금·월 수업이 배포로 끊기는 것** — 구조상 그대로입니다. 그 요일에 배포할 때는 사람이 시간을 봐야 합니다.

---

## 11. 후속 (2026-09-02 오후) — 마지막 크론이 실제 수업과 3분 차이였습니다

### 무엇이 잘못돼 있었나

10장 2번(「크론 첫 회차 확인」)을 준비하다 **창 밖 시각을 검증**하면서 나왔습니다.

`class_schedules`(active · LMS·시드 제외)에서 **창(화·목 14:00~23:00) 밖에 시작하는 수업**을 세니 둘이었습니다.

| 수업 | 요일·시각 | 끝 | 회차 |
|---|---|---|---|
| 장지웅 (`adm-enroll:85`) | **월 13:00** (20분) | **13:20** | 8/31 · 9/7 · 9/14 · 9/21 |
| c24-mirror 1건 | 22:40 (20분) | 23:00 | — |

**마지막 몰아 배포 크론이 `13:23 KST` 였습니다 — 장지웅 수업이 끝나고 3분 뒤입니다.**
월요일은 창이 안 막는 날이라 다른 방어도 없습니다. 즉 **이 게이트가 막으려던 사고를
게이트가 데려온 크론이 스스로 낼 뻔했습니다.**

제가 13:23 을 고른 근거는 「창 열리기 30분 전 마지막 기회」 하나였고, **그 월요일 수업을
보지 않고 잡은 값**이었습니다.

### 🔴 그리고 같은 결함이 하나 더 있었습니다 — 방향만 반대

`trap-check` 서브에이전트가 잡았습니다. **첫 크론 `23:17 KST` 가 야간 접속
`23:20` 이 시작되기 3분 전**이었습니다.

⚠️ 더 나쁜 것은, **`deploy.yml` 주석이 그 시간대를 이미 적어 두고 있었다**는 점입니다 —
「📜 처음에 23:35 KST 로 잡았다가 물렸다 — 실측하니 23:20~23:40 · 23:30~23:50 ·
00:00~00:20 수업이 실재해서」. 제가 만든 검사 표가 **이 저장소가 이미 아는 것**을
빠뜨렸고, 표에 붙인 「새 수업이 생기면 손으로 더해야 한다」는 주석이 그 누락을
덮고 있었습니다.

D1 재실측(`attendance`, `last_seen_at` 있는 행)으로 확인했습니다 — 23:05 · 23:17 ·
23:21 · 23:52 · 23:56 · 00:02~00:50 · 01:17~01:39 에 실접속이 있습니다(대부분
공용 연습방 `mangoi-class`). **23:00 직후에는 안전한 자리가 없습니다.**

### 고침 — 네 시각을 전부 옮겼습니다

| 옛 | 새 | 왜 |
|---|---|---|
| 23:17 | **02:05** | 야간 접속(23:20~01:20)이 끝난 뒤 첫 기회 |
| 02:41 | **05:47** | 접속이 가장 뜸한 구간(05:14~08:45 실측) |
| 07:09 | **09:11** | |
| 13:23 | **12:35** | 창이 «열리기» 전 마지막 기회 |

⚠️ **「창이 닫히고 바로」는 포기했습니다** — 23:00 에 창이 닫혀도 야간 접속이
01:20 까지 있어서, 보류된 커밋은 이제 **3시간 5분** 뒤에 나갑니다.

🔴 **「N분 전에 끝난다」로 안심하면 안 됩니다.** 같은 문서 7장이 **177분 지연**을
재 두었습니다. **25~45분만 밀려도 12:35 회차는 13:00 수업 «안»에서 돕니다.**
즉 위험을 없앤 것이 아니라 **종류를 맞바꾼 것**입니다 — 13:23 은 「지연되면 수업에서
멀어지는」 값이었고 12:35 는 「가까워지는」 값입니다. 월요일은 창이 안 막으므로
다른 방어가 없습니다. 이 맞바꿈은 **사람이 알고 있어야 합니다.**

### 왜 검사를 하나 더 넣었나

시각만 옮기면 다음 사람이 「창 밖이니까」 하고 되돌립니다. 그래서 하니스에
**`OUT_WINDOW_CLASSES`**(창 밖에 실재하는 수업 표)를 두고 모든 크론이 그 구간과
**앞뒤 15분** 이상 떨어져 있는지 검사합니다(배포에 2분 30초가 걸립니다).

⚠️ **그 표는 완전하지 않습니다** — D1 을 읽지 않고 2026-09-02 에 잰 값을 손으로
적어 둔 것입니다. 창 밖에 새 수업이 생기면 **사람이 더해야** 합니다. 그래도
「손으로 옮기다 되돌리는 것」은 막습니다.

### 검토했다가 버린 것

| 버린 것 | 이유 |
|---|---|
| 크론을 13:50 으로 옮기기 | 수업(13:20 끝)과는 30분 떨어지지만 **창 열리기 10분 전**이라 「30분 이상 여유」 조건에 걸립니다. 창이 열리는 순간 배포가 진행 중이면 그때 붙은 수업이 끊깁니다 |
| 창을 월요일까지 넓히기 | 사장님이 **화·목** 으로 정하셨습니다(9장). 제 판단으로 넓히지 않았습니다 |
| 하니스가 D1 을 읽어 자동으로 검사 | 하니스는 네트워크·자격증명 없이 도는 것이 전제입니다. CI 에서 D1 을 읽게 하면 **조회가 실패할 때 조용히 통과**하는 검사가 됩니다(이 저장소가 여러 번 밟은 형태) |
| 「창 밖이면 안전」이라는 전제를 그대로 두기 | 그 전제가 틀렸다는 것이 이번 건입니다. 창은 **화·목만** 막습니다 |

### 검증

- 타입체크 통과 · `run.mjs --fast` **PASS 268 / FAIL 0 / SKIP 20**
- `deploy_class_window_harness` **88종**(60 → 67 → 88)

검사에 안전장치 둘을 더 넣었습니다 — 둘 다 **없으면 조용히 초록**이던 것입니다.

| 넣은 것 | 없으면 |
|---|---|
| 표가 비어 있지 않다 | 표를 비우면 이 절이 통째로 사라지는데 **67 PASS / 0 FAIL** |
| 구간이 자정을 넘지 않는다 | `['23:30','00:20']` 한 줄로 넣으면 13분 전인 크론도 **통과** |

변이시험 4종 전부 진짜 FAIL:

| 되돌린 것 | FAIL |
|---|---|
| 크론을 23:17 로 되돌리기 | 1건 (야간 접속 3분 전) |
| 크론을 13:23 으로 되돌리기 | 1건 (수업 끝나고 3분) |
| 표 비우기 | 1건 |
| 자정 넘는 구간을 한 줄로 | 1건 |
