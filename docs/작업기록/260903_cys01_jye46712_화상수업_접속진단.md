# cys01 · jye46712 화상수업 접속 문제 진단 (2026-09-03)

> 입력: 사장님이 올린 진단 프롬프트 PDF(`cys01 / jye46712 화상수업 접속 문제 진단 프롬프트`, 2026-09-03 작성).
> 제보 ① cys01 — 9/3 수업에서 강사와 만나지 못함. ② jye46712 — 들어오긴 했으나 초반 접속 불안정.
> 이 세션은 Cloudflare D1 조회 도구(MCP)가 붙어 있어 PDF 가 «막혀 있다» 고 적은 두 제약 중 D1 쪽은 풀렸고,
> **`mangoi.ai` 화면은 여전히 못 봅니다**(프록시 403). 아래는 전부 **SELECT 만** 돌려 얻은 것입니다(D1 변경 0건).

## 0. 한 줄 결론

| 학생 | 방 갈림 | 강사 미배정 | 노쇼 오판 | 실제로 일어난 일 |
|---|---|---|---|---|
| **cys01** (최윤서, `class-1087-20260903`, JANE 21:20) | ✗ 아님 | ✗ 아님 — JANE(`mangoi_114`)이 21:11:04~21:39:35 방에 있었음 | 해당 없음 | **학생 쪽 흔적이 0건** — 출석·시선·회선품질·녹화·강사호출 어디에도 없음. 강사는 28분간 혼자 기다림(상대 0명). 학생이 **입장 단계까지 오지 못함**(로그인·수업 카드·버튼 «이전» 단계이거나 아예 접속 안 함) |
| **jye46712** (김선우, `class-1079-20260903`, KRYSTEL 21:20) | ✗ 아님(강사 host `mangoi.ai`, 학생 host 는 기록 안 됨 — §3) | ✗ 아님 — Krystel 이 21:13:26~21:41:04 방에 있었음 | **✓ 오판 맞음** — 21:20:34 「강사 미입장」 알림은 강사가 7분 전부터 있었으니 오판 | 학생 화면은 21:20:34 에 켜졌는데 **21:31 까지 실제 접속(소켓·미디어) 흔적이 없음** → 21:34:31 부터 연결돼 **21:34~21:41 7분만 수업**. 이후 강사 퇴장 뒤 재입장 73초, 그 다음 엉뚱한 방 `meet-1234` 로 |

## 1. cys01 — 잰 것

| 표 | 결과 |
|---|---|
| `class_schedules` | id 1087 · 2026-09-03 21:20 · 20분 · teacher_id 6(JANE) · `adm-enroll:91` (9/1 12:11 수강신청 확정 자동생성, 9/1·3·8·10… 화목) |
| `teachers`·`teacher_account_links`·`teacher_profiles` | 6 = JANE ↔ 계정 `mangoi_114`(8/27 연결, 프로필 메모 「아이디:mangoi_114」). 오늘 17:27 로그인 성공 |
| `attendance` (강사) | `class-1087-20260903` · `교사 mangoi_114` · host **mangoi.ai** · 21:11:04 → 21:39:35 (28분, status left). 21:05:20 에 `class-1408-20260903`(배윤아 20:40) 에 9초 들렀다 나옴 |
| `attendance` (학생) | **0행** — cys01 로도, 이름으로도, `class-1087` 방의 학생 행으로도 없음 |
| `vc_quality` | 그 방 **0행**(강사 행도 0 — 이 표는 상대가 있을 때만 쌓이므로 «강사가 끝까지 혼자» 와 일치) |
| `recordings` | 2753 · 강사 브라우저 · 21:12:02 · **26초** · participant_ids `[강사 DO번호, cys01]` — ⚠️ `cys01` 은 서버가 예약에서 채운 값(`api-mango.ts` `/api/recordings/start` 3837행) 이라 **학생이 있었다는 증거가 아님** |
| `class_no_show` | 286 · 「결석 위험(자동감지)」 student cys01 · 21:31:02 · waited 11. **강사 호출 행(teacher 쪽) 없음** — 수업 카드로 입장을 누르면 그 즉시 `/api/notify/no-show`(`reason:student_entered`) 가 한 줄 남기는데(30분 dedupe) 그것도 없음 |
| `consents` | 9/1 21:40:55 동의(Windows Chrome). 오늘 동의창에 걸린 것 아님 |
| 배포 | 21:10~21:45 KST 에 배포 run 없음(run #2997 15:24, #2998 17:14, #2999 04:40 익일) — DO 재시작 배제 |

**판단.** 서버가 가진 어떤 표에도 오늘 cys01 의 흔적이 없습니다. 수업 카드 → 「입장」 을 눌렀다면 남는 강사 호출 행조차 없으므로, 오늘은 **입장 버튼 이전 단계에서 막혔거나 접속 자체를 안 한 것**입니다(⚠️ 「방 코드 직접 입력」 경로는 그 행을 안 남기므로 그 경로였을 가능성은 남습니다).
어느 화면에서 막혔는지는 **학생·학부모에게 물어야** 합니다 — 로그인이 됐는지, 홈에 「오늘 내 수업」 카드가 떴는지, 눌렀을 때 무슨 문구가 나왔는지(캡처).

**참고 — 9/1 첫 수업도 비슷했습니다.** `class-1086-20260901`: 강사 호출 행 21:19:32(= 입장 누른 시각으로 추정) → 학생 쪽 첫 실접속 흔적 **21:41:05**(녹화 시작·시선 행) → 21:41~22:00 수업. 즉 **버튼을 누른 뒤 21분 동안 화면은 켜졌는데 접속이 안 됐고**, 강사 JANE 은 21:23 에 들어와 21:28 나갔다가 21:28:40 재입장해 22:00 까지 기다렸습니다. 그리고 그날 20:17 에는 공용방 `mangoi-class` 에 `CYS01`(대문자 — 손으로 친 것)로 11초 들어왔다 나간 기록이 있습니다. ⟹ 첫 수업부터 «입장 경로를 못 찾는» 사람으로 보입니다(추론).

## 2. jye46712 — 잰 것

| 시각(KST) | 무엇 | 근거 |
|---|---|---|
| 21:13:26 | 강사 Krystel(`mangoi_169`) 입장, host mangoi.ai | attendance 3478982 (→ 21:41:04 퇴장) |
| 21:20:34 | 「강사 미입장」 행(teacher, waited 5, push 발송됨) | class_no_show 284. `vcStartWaitingMonitor` 가 입장 누른 «즉시» 부르는 `student_entered` 호출은 waited 0 → 서버가 `|| 5` 로 5 를 적으므로, **입장을 누른 시각이 21:20:34** 인지 «21:15:34 부터 5분 기다린 것» 인지 이 행만으로는 못 가름(같은 방 30분 dedupe). 다른 표에 21:15~21:31 흔적이 없으니 **21:20:34 에 누른 쪽**이 더 맞아 보임 |
| 21:20:34 ~ 21:31 | **학생 쪽 실접속 흔적 0** — 출석·시선·회선품질·녹화 전부 없음 | `body.vc-in-call` 은 미디어 획득·WebSocket 보다 **앞**(idx-main.js 2870 → 3531 → 3584)에 켜지므로 «화면이 켜졌다 ≠ 방에 들어갔다» |
| 21:31:02 | 「결석 위험(자동감지)」 student 행 | class_no_show 285 |
| 21:31:21 | `lee` 라는 이름의 student 행 4초(host mangoi.ai, 로그인 없음) | attendance 3478985 — **누구인지 미확인**(학생 기기의 다른 이름? 알림을 받은 직원?) |
| 21:31:36 / 21:34:31 | 학생 DO 번호 행 둘(이름·host 없음 — §3) | attendance 3478986 · 3478987(=녹화 2755 의 첫 참가자 번호와 일치) |
| 21:33:58 ~ 21:41 | **강사와 연결** — 양쪽 `vc_quality` peers=1, 학생 녹화 2755(415초) | 학생: 손실 평균 0~1.5%(최대 17.8%) · RTT 105~202ms · 영상 없던 틱 0~8/15 · 멈춤 2~6회/분. 강사: RTT 98~254ms · 끊겨 메워진 소리 1~8% |
| 21:41:04 | 강사 퇴장(수업 종료 21:40) | attendance |
| 21:41:34 ~ 21:42:22 | 학생 재입장 73초(녹화 2756) → 21:42:53 `meet-1234` 방으로 | attendance 3478991 · 3478992, recordings 2757 |

**판단.** 방은 같았고 강사는 있었습니다. 학생 브라우저가 **입장을 누른 뒤 약 11분(늦어도 16분) 동안 소켓·미디어까지 못 갔고**, 21:34 부터 7분 수업했습니다. 연결된 뒤의 회선은 나쁘지 않습니다(9/1 의 117kbps 업링크 붕괴와는 다른 모양 — 그날 RTT 413·손실 4.3% 까지 갔던 것과 비교해 오늘은 RTT 100~200·손실 1% 안팎).
어디서 11분이 갔는지는 서버 기록으로 못 가릅니다 — 후보는 ① 카메라·마이크 권한 창 ② 카메라 획득 실패·재시도 ③ WebSocket 접속 실패·재시도. 삼성 인터넷(9/1 동의 UA `SamsungBrowser/29.0`)이라면 ① 이 흔합니다(추론). 학생 화면 콘솔이나 「어떤 화면에서 멈춰 있었나」 를 물어야 합니다.

## 3. 곁에서 발견한 결함 — 로그인한 학생의 출석 join·checkin 이 전부 거절되고 있음 (코드+데이터 근거, 라이브 403 응답은 직접 못 봄)

PDF 의 «host 칸 비교» 를 하려다 발견했습니다. **오늘 학생 출석 22행 중 19행이 이름·host·account_uid 가 전부 비어 있습니다**(9/1 도 35행 중 18행). 그 빈 행은 `/api/attendance/join`·`checkin` 이 만든 것이 아닙니다 — 둘은 항상 `Host` 헤더를 씁니다. 빈 행을 만드는 곳은 **`/api/gaze-score` 의 «행이 없으면 하나 만들어 둔다» 폴백**(`api-mango.ts` 982행 — username·host·account_uid 를 안 씀) 뿐입니다.

왜 join 이 행을 못 만드나 — `_attnSoftAuthOk(b.user_id, b)`(498행) 가 **토큰의 uid(계정, 예 `jye46712`) 와 `user_id`(기기 식별자 `u_…` 또는 DO 임시번호) 를 «같은가» 로 비교**합니다. 로그인한 학생은 `mango-attendance.js` 가 `mango_token` 을 실어 보내므로 토큰은 있고, `user_id` 는 계정이 아니라 기기 번호라 **언제나 다릅니다 → 403 `uid_mismatch`**. 로그인 안 한 사람(토큰 없음)과 교사(관리자 세션)만 통과합니다. 2026-07-19 도입 때 라이브 검증이 「본인토큰 200」이었는데(`docs/보안_PII_감사.md` 62행) 그 검증은 `user_id = 계정` 으로 보낸 것으로 보이고, 실제 클라이언트는 그렇게 안 보냅니다.

데이터가 그대로 말합니다 — `attendance.account_uid` 가 한 번이라도 적힌 계정은 **전 기간 4개**(`jeong`·`student`·`delaware` 2행·`Lee` 1행)뿐이고, 최근 7일 `vc_quality` 에 잡힌 로그인 학생 **11명** 중 출석에 account_uid 가 남은 사람은 관리자인 `jeong` 하나입니다(delaware·juju5731·ubckt02·bellet00·heyst·jye46712·cys01 전부 빈 행뿐).

영향
- PDF 3장의 «host 두 값이면 방 갈림» 진단이 **학생에게는 성립하지 않습니다**(host 가 안 남음).
- 2026-08-12 에 넣은 `account_uid`(녹화 참가자·동의를 계정에 잇는 근거)가 **실제 학생에게는 한 번도 기록된 적이 없습니다.**
- `checkin` 도 같은 게이트라 «수업 시간 내 입장 = attended» 확정·결석 복구가 로그인 학생에게는 안 돕니다(빈 행은 `present` 로만 남음).
- 반대로 이 «빈 행» 덕에 `absent-sweep`·녹화 공용방 참가자 채우기는 완전히 죽지는 않았습니다(행 자체는 있으므로).

고치는 방향(9/3 시점엔 제안만 — **9/4 에 §7-3 로 적용했습니다**)
- 비교 대상을 `b.user_id` 가 아니라 **`b.account_uid`** 로 — 토큰이 있으면 `토큰 uid === account_uid`(없으면 통과, 다르면 403). `user_id` 는 애초에 계정이 아니라는 것이 536행 주석에 이미 적혀 있습니다.
- 확인 방법: 고친 뒤 하루치 `SELECT SUM(account_uid IS NOT NULL) FROM attendance WHERE role='student' AND date=?` 가 0 을 벗어나는지. 지금은 0 입니다.
- ⚠️ 하니스가 못 잡는 종류입니다 — 함수도 조건도 «있고» 틀린 것은 «무엇과 비교하는가» 뿐. 가짜 요청(토큰 uid ≠ user_id, account_uid = 토큰 uid)을 넣어 **실제로 돌리는** 검사를 함께 두어야 합니다.

## 4. 오늘 «결석 위험(자동감지)» 118건에 대해

class_no_show 의 student 자동감지 행이 오늘 118건인데 그중 117건은 `c24-mirror` 예약(카페24 수업 — 우리 방을 안 거치므로 출석이 없는 것이 정상)이고, 망고아이 수업은 cys01·jye46712 둘뿐입니다. 즉 이 표로 «오늘 결석이 많다» 고 읽으면 안 됩니다(CLAUDE.md 「🔴 실시간 수업 현황이 비었는데 수업은 돌고 있음」과 같은 사정).

## 5. 확인한 방법 · 못 한 것

- D1 `mango-db` 를 MCP 로 **SELECT 만** 30여 회(attendance·class_schedules·teachers·teacher_account_links·teacher_profiles·vc_quality·recordings·class_no_show·consents·students_erp·admin_login_history). 변경 0건.
- GitHub Actions `deploy.yml` 실행 목록으로 배포 시각 대조.
- 코드 대조: `api-mango.ts`(join 508~554 · checkin 640~772 · speaking-time 886~935 · gaze 982 · recordings/start 3826~3897), `mango-attendance.js`, `mango-gaze.js`, `idx-main.js`(2267~2281 · 2352~2475 · 2583~2870 · 3584), `api-notify.ts`(402~461), `auth-token.ts`(184~190).
- ✗ `mangoi.ai` 실제 화면·학생 기기 콘솔은 못 봤습니다. ✗ 403 응답을 라이브로 재현하지는 못했습니다(프록시). ✗ 「lee」 행이 누구인지 미확인.
- 코드 변경 0줄. 이 문서와 목차 등록만 커밋.

## 6. 다음 사람이 할 일

1. cys01 학부모에게 「어느 화면에서 막혔는지」 캡처 요청(로그인 → 홈 카드 → 입장 버튼 순).
2. jye46712 는 다음 수업(9/8 월 21:20) 때 **입장을 누른 뒤 카메라 권한 창이 뜨는지** 지켜보게 안내. 삼성 인터넷이면 크롬으로 열어 보는 것도 한 방법(추론).
3. ~~§3 의 `_attnSoftAuthOk` 비교 대상 수정~~ → 9/4 §7-3 로 적용. ~~배포 뒤 하루치 `account_uid` 가 실제로 쌓이는지 D1 로 확인~~ → **§7-4 ① 에서 확인했습니다**(9/4 12:48 `jye46712`).
4. **오늘(9/4) 19:20 `class-851` 정우영 수업에서 알림 억제를 눈으로 확인**하세요 — 그 자리가 과거 오판 14건을 낸 곳입니다. 볼 것: `SELECT room_id, waited_min, notified_push FROM class_no_show WHERE room_id='class-851-20260904'` 가 **행은 남되 `notified_push=0`** 인가. (⚠️ 강사가 학생보다 늦게 들어오면 억제가 «안 되는 것이 정상» 입니다 — 그때는 알림이 맞습니다.)

---

## 7. 후속 (2026-09-04) — 녹화 파일 확인 · 수리 2건

### 7-1. 사장님이 올린 녹화 3개를 직접 풀어 본 결과

| 파일 | DB | 실제 | 내용 |
|---|---|---|---|
| `meet-1234` (59,663B) | 2757 · 21초 | 영상 1280×720 빈 화면, 소리 9초 무음 | 강사 퇴장 뒤 엉뚱한 방에 혼자 들어간 구간 — 내용 없음(정상) |
| `class-1079` 본편 (15,728,640B) | 2755 · 06:55 | **4분 50초(289.7초)까지만** — 정확히 5MiB×3 조각. **마지막 약 2분 유실** | 초당 3.7장(폰 성능). 강사 타일이 6초·4분 6초에 검정, 46초~3분 25초·4분 47초 정상. 소리 끝까지, 피크 1.0(클리핑) |
| `class-1079` 재입장 (656,706B) | 2756 · 01:13 | 55초, 전부 검정, 무음 | 강사 21:41:04 퇴장 뒤 혼자 — 내용 없음(정상) |

- 재생 시간 표시 없음·탐색 불가는 MediaRecorder WebM 형식 한계(CLAUDE.md 2장 항목).
- 처음 «프레임 8장이 전부 같다» 고 잰 것은 **seek 기반 추출의 오류**였습니다(큐 없는 WebM 에서 seek 이 같은 키프레임에 떨어짐). 16배속 재생으로 다시 세니 1,071 프레임·해시 전부 다름 → 정지화면 아님. **큐 없는 WebM 은 seek 으로 재지 말고 재생하며 세세요.**
- 꼬리 2분 유실의 정확한 경로(마지막 파트 업로드 중 페이지 이동 / 3회 재시도 실패)는 브라우저 로그 없이는 못 가릅니다 — 코드로 «고치지 않았습니다». `recording_parts` 장부는 8/27 이후 안 쓰이고 있어 근거가 안 됩니다.
- 관리자 목록의 「고아」 100~120MB 3건은 DB 로 주인을 못 밝혔습니다(연결 기록이 없는 파일). 시각만 이 학생의 접속 순간과 겹칩니다.

### 7-2. 수업 상태가 나빴던 이유 (같은 저녁 다른 방과 대조)

| 방 (강사) | RTT | 영상 멈춤/분 | 끊겨 메운 소리 | 강사 영상 못 보낸 비율 |
|---|---|---|---|---|
| 1009 (Hannah, 국내) | 121ms | 1.0 | 0.7% | 1% |
| 1071 (Win, 국내) | 100ms | 1.8 | 0.8% | 0% |
| **1079 (Krystel)** | 164ms | 4.6 | 4.2% | **23% (21:37 은 73%)** |
| 973·989·997·1054 (필리핀 강사) | 156~191ms | 4~7 | 5~12% | 1~6% |

⟹ ① Krystel 의 영상 송출 끊김(녹화의 검정 타일과 시각 일치) ② 학생 폰 성능·회선(입장 11분, 자기 영상 30% 못 보냄, 손실 최대 17.8%, 1080p 옛 녹화 코드가 캐시로 남음) ③ 필리핀 강사 방 전체가 국내 강사 방보다 3~5배 나쁨(#793 과 같은 뿌리). 「그것 때문」이라는 인과는 추론입니다.

### 7-3. 수리 (코드)

1. **출석 본인확인 비교 대상을 계정으로** — 이 수리는 같은 진단에서 갈라져 나간 **PR #795**(`docs/작업기록/260904_출석_소프트인증_기기식별자_비교_403수리.md`)가 먼저 `main` 에 병합했습니다. 제 브랜치의 같은 수정(`_attnClaimedAccount` 헬퍼)은 **#795 를 정본으로 두고 버렸습니다** — 두 수정의 뜻은 같고(join·checkin 은 `b.account_uid`, 비어 있으면 통과, consents 는 `b.user_id`), 감시도 #795 의 `attendance_soft_auth_harness.mjs`(42종)가 맡습니다.
2. **「강사 미입장」 알림 억제** — `no-show-truth.ts` `teacherLiveInRoom()` (정본 `teacherPresenceByRoom` 재사용 · 3분 창) 이 참이면 `api-notify.ts` no-show 가 push·알림톡을 **둘 다** 건너뛰고 기록은 남김. 응답에 `teacher_present`.
3. `waited_minutes:0` 이 `|| 5` 로 5 가 되던 것 — 0 은 0. 즉시 호출 문구는 「방에 들어왔어요」.

버린 것: (a) 알림 억제 판정에 `attendance.role='teacher'` 쓰기 — CLAUDE.md 가 금지(학생이 강사 역할을 받는 전례). (b) 억제 시 `class_no_show` 행을 안 남기기 — «학생이 못 본 것은 사실» 이고 읽는 쪽이 같은 함수로 오판 표시를 붙이므로 기록 유지. (c) 학생 폰에서 자동 녹화 끄기/480p — 정책 판단(학부모 약속·미성년자 기록)이라 사장님 결정으로 남김. (d) 「미입장」 5분을 소켓 접속 «뒤» 부터 세기 — `idx-main.js`(blocking, 첫 화면 예산 여유 ~100B) 수정이 필요해 이번엔 서버 쪽 억제로 대신.

4. **미래 시각을 «살아 있음» 으로 세지 않기**(2026-09-04 배포 뒤 §7-4 재판정 중 발견) — `p.to`(=`COALESCE(left_at,last_seen_at)`)는 접속이 찍을 때만 서버 시각이고 **카페24 예약 씨앗은 `left_at` 이 예약 종료 시각이라 미래**입니다(실측 **387~397건** — 예약 시각이라 시간이 흐르면 줄어듭니다). `teacherLiveInRoom` 이 60초 넘는 미래를 거부합니다(`FUTURE_SLACK_MS` — ⚠️ 60초에 «잰» 근거는 없습니다. 막으려는 값이 «분~년» 단위라 그 사이 아무 데나 잡아도 갈립니다). ⚠️ **오늘의 사고는 아닙니다** — 그 행은 **전부 `c24-*` 방**이라 `class-*` 번호로 묻는 이 함수와 구조적으로 안 만나고, 이 함수가 보는 **`username`** 칸에 강사 이름이 붙은 것도 0건입니다. ⚠️ **`teacher_name` 칸은 366건 채워져 있습니다**(씨앗 INSERT 가 그 칸은 넣고 `username` 에는 학생 이름, `role` 에는 `student` 를 박습니다) — 그 숫자를 보고 «0건이 아닌데?» 로 읽지 마세요. ⚠️ 지금 코드에 «미러가 `class-*` 에 씨앗을 만드는» 경로는 **없습니다**(`c24-mirror.ts` 는 `attendance` 를 읽기만 합니다). 막아 두는 것은 그 전제가 깨질 때를 위해서입니다. ⛔ `teacherPresenceByRoom`(사후 리포트)은 안 건드렸습니다.

확인: `noshow_teacher_present_harness` **25/25**(변이 실제 FAIL: kakao 게이트 제거 1건 · waited `\|\| 5` 로 되돌림 1건 · 미래 방어 제거 1건 · 여유를 0 으로 1건) · #795 의 `attendance_soft_auth_harness` 42/42(병합 뒤 재확인) · 관련 하니스 `no_show_false_alarm` 39/39 · `attendance_checkin` 54/54 · `attendance_last_seen` 35/35 · `public_api_auth` 16/16 · `shared_room_recording_participants` 33/33 · `attendance_no_fabrication` 27/27 · `--fast` **PASS 264 / FAIL 13**(#795 병합 뒤 재실행)(13 전부 `node_modules` 부재 — esbuild·typescript. `today_plan` 2건은 깨끗한 HEAD 에서도 동일). ⚠️ `tsc` 는 이 컨테이너에서 못 돌림(CI 가 확인). ⚠️ 라이브 403 재현·실제 알림 억제는 배포 뒤 D1 로 확인해야 합니다 — `SELECT SUM(account_uid IS NOT NULL) FROM attendance WHERE role='student' AND date=?` 가 0 을 벗어나면 됨.

### 7-4. 배포 뒤 실측 (2026-09-04)

배포: PR #795(출석 403) → run #3004 03:56 UTC · PR #798(노쇼 억제) → run #3006 **04:22 UTC(13:22 KST) 성공**.

**① 출석 403 수리가 실제로 동작했습니다.** 9/4 12:48:40 KST `mangoi-class` 방에 `user_id=u_iyeuu18a2v`(기기 식별자)·**`account_uid=jye46712`**·`host=mangoi.ai` 인 학생 행이 찍혔습니다. `account_uid` 와 `host` 는 join·checkin 만 쓰는 칸이고(시선 API 폴백은 둘 다 안 씀), 토큰 uid ≠ `user_id` 인데 통과했으므로 **고친 그 경로가 지난 것**입니다. 전 기간 `account_uid` 가 남은 계정이 4개(`jeong`·`student`·`delaware`·`Lee`, 전부 8/25 이전)였는데 **`jye46712` 가 다섯 번째**로 붙었습니다 — 진단 대상이던 그 학생입니다.

**② 「강사 미입장」 알림 47건 전수 재판정.** 정본 `teacherPresenceByRoom`·`teacherLiveInRoom` 을 D1 실데이터에 물려 **실제로 돌렸습니다**(가짜 D1 + node 타입 제거).

| 판정 | 건수 | |
|---|---|---|
| 새 코드가 막았을 것(알림 순간 3분 내 강사 생존) | **27건** | 57% |
| 강사는 있었지만 3분보다 전 | 3건 | 6% |
| 강사 흔적 없음 | 3건 | 6% |
| 판정 불가(그 시각까지 이름 붙은 출석행 없음) | 14건 | 30% |

막았을 27건에서 강사는 알림보다 **가운뎃값 4분 12초** 먼저 들어와 있었습니다(가장 짧은 것 17초 · 가장 긴 것 7시간 19분 — ⚠️ 최댓값은 각주가 필요합니다. `from` 은 그 방에서 이름이 맞는 **모든** 접속의 최소 `joined_at` 이라 같은 날 앞 세션이 있으면 부풀어집니다. 가운뎃값은 그 영향을 안 받습니다). **14건이 사장님 수업(「중국어 강선생님」)** 입니다. 「판정 불가」 14건에는 **강사가 알림 «뒤» 에 들어온 진짜 미입장**이 섞여 있습니다(예: `class-1070-20260901` 강사 458초 뒤 · `class-1086-20260901` 218초 뒤 · `class-988-20260901` 247초 뒤) — 그 알림들은 제 역할을 한 것이고, 정본이 «이름 붙은 행이 없으면 «없었다» 로 단정하지 않는다» 는 규칙 때문에 «불가» 로 떨어집니다.

⛔ **이 재판정을 «지금 있는 데이터» 로 그냥 하면 틀립니다 — 실제로 한 번 틀렸습니다.** `p.to` 는 그 방 강사 접속의 «최종» 퇴장 시각이라 수업 끝까지 있었으면 알림보다 20~30분 «뒤» 이고, 그러면 `nowMs - p.to` 가 음수라 `<= freshMs` 가 항상 참이 되어 **40건(85%)이 전부 «막았을 것»** 으로 나왔습니다. 알림 시각 기준으로 «그 뒤에 들어온 행은 빼고 `out_at` 은 그 시각으로 잘라» 그때의 서버 상태를 재현해야 합니다. ⚠️ 그것도 낙관적 근사입니다 — `last_seen_at` 은 30초마다 갱신되므로 최대 30초 뒤처집니다(3분 창에서는 판정이 거의 안 바뀝니다).

⛔ **그러다 정본의 견고성 결함을 하나 찾았습니다** — 위 §7-3 4번(미래 시각). 재판정 스크립트: `scratchpad/run-presence.mjs`(일회용, 커밋 안 함 — ⚠️ **그래서 이 숫자는 지금은 아무도 재현할 수 없습니다.** 다시 재려면 `class_no_show`(teacher) 전수 + 그 방들의 `attendance`(username 있는 행) + `teacher_account_links` 를 SELECT 해 정본에 물리면 됩니다). 자기검증으로 «알려진 오판 1건을 잡는가»·«근거 없는 방 1건을 안 잡는가» 를 둘 다 확인한 뒤에만 숫자를 냅니다.

**③ 아직 못 본 것.** 새 코드가 실제로 알림을 억제하는 것은 **아직 한 번도 안 봤습니다** — 배포(13:22 KST) 뒤 망고아이 정규 수업이 없었습니다. 오늘 금요일 정규 수업은 **19:20 정우영(`class-851`, 중국어 강선생님)** 한 건뿐이고, 그 수업은 위 «막았을 27건» 중 **14건**을 낸 바로 그 자리라 확인에 딱 맞습니다. 볼 것: 그 방 `class_no_show` 행이 **남되** `notified_push=0` 인가, `waited_min` 이 0 으로 찍히는가.
