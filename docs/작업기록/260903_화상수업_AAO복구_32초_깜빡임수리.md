# 화상수업 「화면이 나왔다 안 나왔다」 — 음성전용(AAO) 복구를 8초 → 32초로 (2026-09-03)

- 사장님 제보: class-1016-20260903(Teacher Farrah ↔ ysyt01 유세영, 14:00) 캡처 두 장 —
  「아직도 필리핀 교사와 한국 학생 수업에서 필리핀 화면이 좋지 않아. 문제가 뭐지」 →
  「화면이 나왔다 안 나왔다 on and off 를 계속함」(두 번째 캡처: 강사 타일에
  「I can't use the camera — Camera tracks exist, but video is not coming in」).
- 바뀐 파일: `cloudflare-deploy/public/js/idx-main.js`(vcAAOApply 2줄 + 주석) ·
  `index.html`·`teacher.html`(`idx-main.js ?v=47→48`) ·
  `test-harness/vc_quality_blindspot_harness.mjs`(⑫절 신설 13건) · `test-harness/asset-versions.json`

---

## 1. 잰 것 (운영 D1 `vc_quality`, room=class-1016-20260903, 14:01~14:21, 강사·학생 각 20건)

| 항목 | 이 수업 | 같은 날짜대 정상 수업(class-1086·996) |
|---|---|---|
| RTT 평균 | 강사 609ms · 학생 588ms | 100~150ms |
| RTT 최악(1분 평균) | 강사 1,091ms · 학생 1,267ms | 200ms 미만 |
| 끊겨서 메워진 소리 `rx_conceal` | 30~44% | 0.3~1.6% |
| 영상 멈춤 `rx_freeze` | 분당 6~21회 | 분당 1회 안팎 |
| 손실 p95 | 학생 34% · 강사 12% | 0~6% |
| 음성전용(AAO) `aao` 칸 | **0/1 이 6번 교차**(강사·학생 각각) | 0회 |

같이 확인해 배제한 것:
- TURN 중계 — 그날 11:50 배포(run #2989) 로그 `기본 → kv-cache ✅` · `production → kv-cache ✅`. 무료 폴백 아님.
- 방 갈림 — 서로 영상이 오갔다. 유령 연결 — 강사 peers 1, 학생 peers 2.
- 강사 노트북 녹화 부담 — #776 이 그날 11:47 배포돼 이 수업부터 적용. 녹화는 학생 쪽 1건뿐.

## 2. 거기서 내린 판단

- **회선 자체가 21분 내내 극도로 나빴다**(측정). RTT 1초는 화질 조절로 풀 수 있는 범위 밖이다.
- 「나왔다 안 나왔다」는 **AAO 가 켜졌다 꺼졌다를 되풀이한 것**(측정 — `aao` 칸 0/1 교차 6회.
  ⚠️ 그 칸은 60초마다 한 번 찍는 점 표본이라 6회는 **하한**이고 «8초 주기» 자체를 본 것은 아니다).
  [추론] 진입은 오디오 손실 8%↑ 3틱(12초), **복구는 3%↓ 2틱(8초)** 이라 회선이 계속 흔들리면
  «끔 → 8초 뒤 켬 → 다시 끔» 이 된다 — 코드상 성립하는 기전이고 하니스 ⑫가 그것을 실제로 돌려 보인다.
- [추론] 방에 연결이 셋(출석에 학생 세션 2건 · REC 배지 「3명」 · 이름 없는 «참가자» 칸).
  사장님이 관리자 수업 관찰로 보고 계셨다면 참관자 한 명은 강사 업로드 한 갈래를 더 쓴다 —
  이미 한계인 필리핀 회선을 더 눌렀을 수 있다. 확정은 못 했다.

## 3. 고친 것 (사장님 「1번 진행해줘」)

`vcAAOApply` — 복구 조건 `A.good >= 2` → **`A.good >= 8`**(32초 연속 손실 3% 미만).
화질 회복(`__qGood >= 8`)과 같은 «8틱» 이다(같은 규칙은 아니다 — 화질 쪽은 애매 RTT 틱에 «멈추기만»,
AAO 는 3~8% 애매 틱에 카운트를 0 으로 되돌린다). 진입 시 `A.good = 0` 으로 카운트를 새로 센다.
⛔ 진입(sev>=3)은 그대로 — 더 빠르게 끄면 스파이크 한 번에 영상이 꺼진다.
⚠️ **맞바꿈(설계 판단)** — 음성전용으로 머무는 동안 영상 통계가 없어 화질 단계(`__qStep`)도 바닥에 머문다.
복구 뒤 화질이 한 단계씩 올라오는 데 단계당 32초가 더 걸리므로 «나쁜 구간 한 번 → 얼굴이 선명해지기까지» 는
전보다 길어진다. 깜빡임을 없애는 대가로 택했다.

함정 대조 검사(trap-check)가 짚은 것: 공동 금지구역 `index.html` 의 `?v=` 한 줄이 diff 에 있음(캐시 무효화에
필수, #771·#772 와 같은 방식 — 사장님(배포 권한자) 지시로 4-1-1 표 안에서 진행) · 주석의 단정형 추론(→ [추론] 표시로 고침) ·
`A.good = 0` 초기화는 시뮬이 아니라 문자열 한 줄로만 지키는 방어적 코드(진입 틱은 늘 bad 틱이라 시뮬로는 못 가른다).

효과(하니스 실측, class-1016 모양 «나쁨 16초 → 조용 24초» × 6):
옛 복구는 4분에 **12회** 켜졌다 꺼졌다, 지금은 **1회** 들어가 머문다. 진짜 조용해지면(32초) 돌아온다.

## 4. 검토했다가 버린 것

| 버린 것 | 왜 |
|---|---|
| 진입도 함께 늦추기(sev 3→5틱) | 소리를 지키는 구제장치가 늦어진다. 이번 사고는 «켜지는 것» 이 문제였지 «꺼지는 것» 이 아니다 |
| 복구를 «시간» 으로(진입 후 60초 홀드) | 8틱 연속 조용함이 이미 32초 홀드이고 손실을 보므로 «시간만 지나면 켬» 보다 낫다 |
| `video-call/js/webrtc.js` 도 같이 고치기 | 옛 진단용 화면. AAO 문턱(12%)도 9/1 에 동기화하지 않은 채라 그 파일은 «따로 사는» 것으로 둔다 |
| 화면을 자동으로 작게 | 받는 쪽 크기는 인코더에 안 간다(#771 에서 이미 버림) |
| 근본 해결(SFU 경유) | 별건 — 시크릿·브라우저 구현이 필요하다(9/2 C안 2단계). 이번엔 «지금 코드가 만드는 깜빡임» 만 |

## 5. 확인한 방법

- `vc_quality_blindspot_harness` **142/142**(⑫절 13건 신설 — 판정 3줄과 `vcAAOApply` 를 소스에서
  **오려 내 실제로 돌린다**. 되돌림: `>= 2` 로 바꾸면 같은 4분에 12회 깜빡여 실제로 FAIL)
- `vc_lowstep_relay_harness` 20/20 · `vc_stability_checklist_harness` 50/50 · `home_bg_preload` 12/12 ·
  `asset_version` 220/220 · `first_paint_budget` 유지 · `idx-main.js` 구문(new Function) OK
- `node test-harness/run.mjs --fast` — 결과는 아래 «검증 결과» 줄.
- tsc — `.ts` 변경 0줄(JS 만). 이 컨테이너에 `node_modules` 가 없어 못 돌렸다. CI 가 대신 돈다.
- 함정 대조 검사(trap-check) — 아래 줄.

## 6. ⏳ 사람이 확인해야 끝나는 것

- 다음 필리핀 수업에서 **음성전용으로 갔을 때 「연결이 회복되어 영상을 다시 켭니다」 토스트가
  8초마다가 아니라 한참 뒤에 한 번** 뜨는지. D1 로는 `vc_quality.aao` 가 0/1 을 자주 오가지 않는지.
- 이 수리는 회선을 좋게 만들지 않는다. RTT 600~1,200ms 는 강사 쪽 회선(유선 연결 여부)과
  SFU 경유(별건)의 문제다. 강사 회선 기록은 9/1 부터 남기 시작해 Farrah 강사 기록이 이 수업
  하나뿐이라 「늘 나쁜지 · 오늘만 나빴는지」는 다음 한두 수업으로 갈린다.
