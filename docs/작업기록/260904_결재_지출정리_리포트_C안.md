# 결재 「지출 정리」(C안) — 무슨 돈을 얼마나 썼나

**2026-09-04** · 사장님 「다음 진행해」
관련: [260904_결재_진행추적_멈춘이유_표시.md](260904_결재_진행추적_멈춘이유_표시.md) (B안·A안·지출 항목 입력)

---

## 1. 왜 지금 했나 — 「한 달 뒤에」라고 말했었습니다

바로 앞 작업에서 **「C안은 한 달치가 쌓이면 만드는 것이 맞습니다」**라고 말씀드렸습니다.
사장님이 「다음 진행해」라고 하셔서 그 판단대로 진행했고, **대신 «오늘의 사실» 을 정직하게
말하는 화면**으로 만들었습니다.

### 잰 것 (2026-09-04, 운영 D1 `approval_requests` 전수 3건)

| id | 분류 | 상태 | 항목 | 금액 | 지출일 | 영수증 | 올린 사람 |
|---|---|---|---|---|---|---|---|
| 1 | 지출 정산 | 승인 | — | PHP 1 | — | 없음 | mgr_melca |
| 2 | 긴급 소통 | **대기** | — | — | — | — | admin (8/30 그 건) |
| 3 | 물품 구입 | 승인 | — | **PHP 2,800** | 2026-09-03 | 있음 | mgr_karl |

### 거기서 내린 판단

- 「빈 화면이 될 것」이라는 제 걱정은 **틀렸습니다.** 실제 구입 1건(PHP 2,800)이 있고,
  리포트는 오늘 **「승인 2건 · PHP 2,801 / 항목 없음 2건 / 영수증 없음 1건」**을 말합니다.
- 그리고 오늘 이 화면이 하는 가장 중요한 말은 합계가 아니라 **「항목이 안 붙었습니다」**입니다.
  그것이 바로 앞 작업에서 넣은 항목 입력을 «쓰게 만드는» 신호입니다.

---

## 2. 무엇을 만들었나

`/work` 아래에 **「지출 정리」 접이 패널**. 문서함과 같은 자리·같은 방식입니다.

| 무엇 | 어디 |
|---|---|
| 합계 정본 `summarizeApprovals()` (순수 함수) | `src/approval-policy.ts` |
| 기존 목록 경로에 `view=report` | `src/api-approval.ts` |
| 「지출 정리」 패널 · 통화별 카드 · 항목별/달별 표 · 「말하지 않는 것」 | `public/work.html` |

화면이 말하는 것:

1. **승인 — 쓰기로 확정** / **대기 — 아직 아닌 돈** (통화별로 나란히)
2. **지출 항목별** (이름 + 회계 계정 — 통장 지출과 대조하려고)
3. **달별** (승인된 것만)
4. 🔴 **「이 합계가 말하지 않는 것」** — 항목 없음 · 금액 없음 · 영수증 없음 ·
   지출일이 없어 올린 날로 잡은 건 · 상한에 걸려 잘린 것
5. 같은 기간 **엑셀 내려받기**

---

## 3. 검토했다가 버린 방법

| 안 한 것 | 왜 |
|---|---|
| **통화를 환산해 하나의 숫자로** | 환율을 우리가 모릅니다. 지어내면 그 숫자가 그대로 판단 근거가 됩니다. → **통화별로 나란히** (PHP · KRW) |
| **승인·대기·반려를 합쳐 「이번 달 지출」** | 대기는 아직 안 쓴 돈, 반려는 안 쓴 돈입니다. 합치면 그 문장이 거짓이 됩니다 → 갈라서 적고 **반려는 어느 합계에도 안 넣습니다** |
| **금액 없는 건을 0원으로** | 「모른다」와 「0원」은 다른 사실입니다 → 합계에서 빼고 **「금액 없음 N건」**으로 따로 말합니다 |
| **항목 없는 건을 「기타」로** | «안 고름» 과 «기타를 고름» 은 다른 사실입니다. 섞으면 기타가 부풀어 「기타가 제일 크다」가 됩니다 → **「항목 없음」** 줄로 따로, 그리고 맨 뒤에 |
| **SQL `GROUP BY` 로 집계** | 빠르지만 **`canView` 를 못 겁니다** — 인사·급여가 합계에 섞입니다. → 행을 읽어 코드로 세고, **상한(2,000행)**을 두고 넘으면 「잘렸다」고 말합니다 |
| **새 API 경로** | 관문이 셋이고 그중 둘이 공동 금지구역입니다(A안과 같은 이유) → 기존 `GET /api/approval/requests` 에 `view=report` 만 |
| **새 화면(`/spending` 등)** | 확장자 없는 주소는 `src/index.ts` 세 곳에 등록해야 합니다(금지구역) → `/work` 안의 접이 패널 |
| **첫 화면에서 미리 부르기** | 이 화면은 「첫 화면 API 한 번」 설계입니다 → 접혀 있다가 **누를 때만**, 그리고 한 번만 |
| **기간을 비워 두고 전 기간** | 「이번 달 얼마 썼나」에 답을 못 합니다 → 기본이 **이번 달 1일부터** |
| **금액 순 정렬** | 통화가 여럿이면 비교할 공통 잣대가 없습니다(환산 금지) → **건수 순**, 그다음 이름순 |
| **화면에서 통화를 더하기** | 서버 규칙을 화면이 무너뜨립니다 → 화면은 나열만 |

---

## 4. 검사 쪽에서 걸린 것 — 제 검사가 **두 번 헛돌았습니다**

변이시험(되돌려 보기)에서만 드러났습니다. 둘 다 **통과하는데 아무것도 안 보고 있던** 상태입니다.

### ① 「`canView` 뒤에 집계하는가」가 언제나 참이었습니다

`api-approval.ts` 전체에서 `indexOf('if (!canView(actor')` 를 했는데 그 문자열이 이 파일에
**네 곳**(828·861·1249·1414행) 있고 **첫 번째가 훨씬 앞**입니다. 그래서 집계를 canView
**앞으로** 옮겨도 늘 «뒤» 로 판정됐습니다 — 인사·급여가 합계로 새는 변이가 통과했습니다.

→ **그 라우트만 중괄호 짝으로 잘라** 그 «안» 에서 비교합니다.
⚠️ 그때 앵커를 `path === '…/requests'` 로 잡으면 **POST 라우트가 먼저** 걸립니다(941행) —
메서드까지 넣어야 합니다.

### ② 「모르는 것을 말하는가」가 자기 문구를 잡고 있었습니다

`/no_category/.test(work)` 였는데, 조건을 `if (false)` 로 죽여도 **안내 문구 안에 그 이름이
그대로 남아** 통과했습니다. → **조건 자체**(`if (s.no_category)`)를 봅니다.

### ③ 브라우저 검사도 하나 — 「반려 금액이 안 보인다」

`t.indexOf('99,999') < 0` 이었는데, 반려가 승인에 **더해지면** 103,499 가 되어 그 글자가
사라집니다. → **승인 칸의 값 자체**를 봅니다.

> 📌 그리고 **변이시험 자체가 틀렸던 것도 한 번** 있었습니다 — `normCurrency(currency)` 를
> 바꾸려 했는데 그 문자열이 파일에 세 곳 있어 **엉뚱한 줄**(248행)이 바뀌었습니다.
> 「변이를 넣었는데 FAIL 이 안 난다」면 **변이가 정말 그 자리에 들어갔는지**부터 보세요
> (그래서 변이 스크립트마다 `assert` 를 답니다).

---

## 4-2. 🔴 함정 대조 검사가 **진짜 유출 버그**를 잡았습니다

제가 「⛔ SQL GROUP BY 로 하면 canView 를 못 걸어 인사·급여가 합계에 섞인다」고
**주석까지 적어 놓고**, 바로 그 줄에서 **거르기 «전» 행(`page`)** 을 넘기고 있었습니다.
거른 결과는 `items` 인데, 바로 위 엑셀(`csvResponse(items)`)만 맞게 쓰고 있었습니다.

```ts
summary: summarizeApprovals(page as any[]),   // ← canView 이전
```

**실측(정본을 그대로 돌려):** 경영진이 아닌 본사 계정이 `scope=all` 로 열면
인사·급여 **750,000 이 승인 합계에**, **900,000 이 대기 합계에** 그대로 섞였습니다.
`scope=mine` 은 SQL 이 이미 본인 행만 주므로 안 샙니다. **에러는 안 났습니다.**

### 그리고 제 하니스가 그 버그를 «검사하는 척» 했습니다

```js
const iSum = route.indexOf('summarizeApprovals(page');   // ← 버그를 글자로 못 박음
check('합계는 canView 로 거른 뒤에 낸다', iView < iSum);
```

«텍스트 위치» 만 보고 «거른 «결과» 를 쓰는가» 는 안 봤습니다. 그래서 62건 전부 통과했고,
**올바르게 고치면 오히려 2건 FAIL** 이 나는 상태였습니다.

→ 이제 canView 루프가 채우는 **변수 이름을 소스에서 읽어** 대조하고,
「엑셀과 리포트가 같은 행을 보는가」도 함께 봅니다. 되돌리면 **3건 실제 FAIL**.

> 📜 이것이 CLAUDE.md 2장 「⛔ «불렀는가» 만 보지 말고 «그 결과를 조건으로 쓰는가» 도
> 보세요」와 「주석을 믿지 마세요 — 「숨겼다」고 적힌 것과 실제로 숨겨진 것은 다릅니다」가
> **같은 커밋 안에서 동시에** 일어난 경우입니다.

### 같은 검사가 짚어 준 나머지 (전부 고침)

| 무엇 | 왜 문제인가 | 어떻게 |
|---|---|---|
| 「모르는 것」이 **없는 줄을 가리킴** | `no_category` 는 반려까지 세는데 `by_category` 는 반려를 뺍니다 → 「위 「항목 없음」 줄이 그것입니다」인데 **그 줄이 없습니다** | 세는 쪽도 승인·대기만 |
| 기간은 «올린 날», 달은 «지출일» | 이달 1일~ 로 열어도 지난달 줄이 나오고, 지난달에 올린 이달 지출은 빠집니다 | 감추지 않고 **달별 표 옆 주석**으로 말합니다 |
| 엑셀은 500건, 리포트는 2,000건 | 「같은 기간」이라 적었는데 행 수가 달라 조용히 어긋납니다 | 넘으면 「말하지 않는 것」에 적습니다 |
| 결재선 조회가 `swallowErrors` | 청크가 실패하면 chain 열람 건이 빠져 **총액이 말없이 줄어듭니다** | 「단계를 못 읽었다」를 내려보내 화면이 말합니다 |
| `offset` 이 report 에도 먹음 | 주소로 넣으면 «앞부분» 이 아니라 **«중간만»** 센 합계인데 `truncated` 가 그걸 못 말합니다 | report 는 offset 무시 |

⚠️ **「말하지 않는 것」 상자에 «늘 있는 사실» 을 넣지 마세요** — 달 눈금 안내를 거기 넣었더니
상자가 **언제나 뜨게** 되어 그 상자를 아무도 안 읽게 됩니다(브라우저 짝 검사가 잡았습니다).
그 상자는 «이번 기간에 빈 곳» 만 적는 자리입니다.

---

## 5. 검증

| 무엇으로 | 결과 |
|---|---|
| `tsc --noEmit` | exit 0 |
| `run.mjs --fast` | **PASS 284 · FAIL 0 · SKIP 20** (기준선 그대로) |
| 신규 `approval_report_harness` | **72건** — 정본을 **실제로 돌려 숫자를 셉니다**. ⑦절은 위 D1 실측 3건을 그대로 넣습니다 |
| 신규 `manual/approval-report-browser` | **36건** — 자동으로 안 돕니다, 사람이 부릅니다 |
| **변이 22종** | **전부 실제 FAIL** (자동 15 · 브라우저 7) — 🔴 그중 하나가 **위 유출 버그를 그대로 되돌린 것**입니다 |
| **trap-check** | 🔴 1건(위 유출) · 🟡 5건 — 전부 고쳤습니다 |

**짝으로 검사합니다** — 「반려는 안 센다」만 보면 **아무것도 안 세는 코드**가 통과합니다.
그래서 「승인은 실제로 센다」를 언제나 함께 셉니다. 「모르는 것을 말한다」도 마찬가지로
**「다 채워진 달에는 안 뜬다」**를 짝으로 둡니다.

---

## 6. 남은 것 · 사람이 정할 일

- ⚠️ **배포된 화면은 제가 못 봅니다**(프록시가 `mangoi.ai` 차단). `/work` → 「얼마나 썼나 보기」를
  눌러 숫자가 나오는지 사장님이 한 번 봐 주셔야 합니다.
- **오늘 이 화면이 말할 가장 중요한 것은 「항목 없음 2건」입니다** — 앞으로 결재를 올릴 때
  항목을 고르면 그만큼 «항목 없음» 이 줄고 항목별 줄이 채워집니다.
- 기존 3건의 항목을 채울지는 **사장님이 정하실 일**입니다(개발/운영이 같은 DB라 제가
  `UPDATE` 하지 않습니다).
- **통장·법인카드 지출과 나란히 놓는 것**은 아직 안 했습니다. 항목의 회계 계정 이름이
  `EXPENSE_CATEGORIES` 와 같으므로 이을 수는 있지만, 「결재로 올라온 지출」과 「통장에서
  나간 돈」이 **같은 돈인지**는 사람이 판단할 문제라 자동으로 잇지 않았습니다.
- **D안(한 화면 요약)** 이 마지막으로 남았습니다 — A·B·C 의 숫자를 모으는 일입니다.
