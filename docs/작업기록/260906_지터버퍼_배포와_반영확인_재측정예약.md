# 지터버퍼(#835)·진단도구(#833) 병합·배포와 «반영 확인», 그리고 9/9 재측정 예약 (2026-09-06)

> 사장님 지시 차례: 「배포되면 rx_conceal 다시 재줘」 → 「CI 통과하면 병합해줘」
> → 「배포된 파일에 실제로 반영됐는지 확인해줘」 → 「833도 병합해줘」
>
> ⚠️ 이 문서는 **«코드를 어떻게 고쳤나» 가 아니라 «어떻게 내보내고 확인했나»** 입니다.
> 코드 쪽 판단(왜 지터버퍼인가, 무엇을 버렸나)은 전날 기록에 있습니다 —
> [260905_수신지터버퍼_늦게온패킷을_기다려주기.md](260905_수신지터버퍼_늦게온패킷을_기다려주기.md).
> 여기서는 그 내용을 **되풀이하지 않습니다.**

- 날짜: 2026-09-06 (일) 13:20~14:00 KST
- 작업자: 사장님 지시 · Claude Code
- 브랜치 / PR:
  - `claude/vc-rx-jitter-buffer` → **PR #835** → 병합 `b937bb8` → 배포 run **#3051**
  - `claude/mangoai-video-enhancement-r0kcr4` → **PR #833** → 병합 `e4b27b5` → 배포 run **#3052**

---

## 1. 왜 했나

전날(9/5) 지터버퍼를 만들어 PR 까지 올렸지만 **병합하지 않은 채로 두었습니다.**
그 상태에서 사장님이 「배포되면 rx_conceal 다시 재줘」 하셨는데, 재려고 보니
**배포가 안 돼 있었습니다.** 즉 이 작업의 출발점은 「재 달라」였고, 재기 위해
먼저 «내보내고, 나갔는지 확인하는» 일을 해야 했습니다.

---

## 2. 무엇을 했나

### 2-1. PR #835 — 충돌 풀고 병합

PR 을 열어 둔 사이 `main` 이 두 번 움직였고(#834 결재, #837 docs) `docs/INDEX.md` 가 충돌했습니다.

- **양쪽 항목을 다 남기고** 첫 칸 중복을 세어 확인(342 = 기준점 339 + 내 1 + main 2, 중복 0건)
- 그 김에 제 목차 줄에 **틀리게 적혀 있던 숫자 둘**을 바로잡았습니다
  — 하니스 `PASS 28` → 실제 **34**, 「변이 5종」 → **6종**
- CI 초록불 확인 후 squash 병합 → `b937bb8`

### 2-2. PR #833 — 최신 main 위에서 «다시» 검증하고 병합

CLAUDE.md 「오래 열려 있던 PR 을 «CI 초록불이니까» 하고 병합하지 말 것」에 따라
**#835 가 들어간 최신 main 을 넣고 처음부터 다시** 돌렸습니다.

특히 하나를 일부러 다시 봤습니다 — `tools/video-enhance-probe/selftest.py` ⓪절은
**`idx-main.js` 를 읽어** 화질 사다리 기댓값을 만듭니다. 방금 그 파일을 고쳤으니
헛돌 수 있어서 확인했고, **47건 그대로 통과**했습니다(사다리와 지터버퍼는 서로 다른 자리).

병합 → `e4b27b5`. 운영 코드 변경 **0건**(`tools/` 17파일 + `docs/INDEX.md` 1줄, 삭제 0줄).

### 2-3. 배포 시각

둘 다 **일요일 13:30~14:00 KST** 에 내보냈습니다. 배포는 진행 중인 모든 수업의
DO 를 재시작시키므로 시각이 중요한데, D1 실측으로 **9/5·9/6 은 `vc_quality` 기록 0건**
(금~일 접속 없음)임을 먼저 확인했습니다. 끊길 수업이 없는 시각이었습니다.

### 2-4. 「고치기 전」 기준선 재확인

재측정의 비교 대상이므로 다시 뽑았습니다(9/1~9/4 · 626분).
**전날 PR 표와 한 자리도 안 틀렸습니다.**

| 왕복 지연 | 분 | 소리 끊김 | 진짜 손실 | 늦어서 버린 것 |
|---|---:|---:|---:|---|
| <120ms | 128 | 2.37% | 0.14% | 2.23%p · 17.5배 |
| 120–200 | 284 | 5.46% | 0.44% | 5.02%p · 12.5배 |
| 200–300 | 104 | 9.55% | 0.75% | 8.81%p · 12.8배 |
| 300–450 | 78 | 7.82% | 0.43% | 7.39%p · 18.1배 |
| **450ms+** | **32** | **33.01%** | **0.97%** | **32.05%p · 34.1배** |

### 2-5. 9/9(수) 재측정 예약 + 사람이 읽는 보고서

- 예약: `trig_016FQWDmYauisseCw6HBwXXe` · `2026-09-09T01:00:00Z`(10:00 KST)
- 보고서(10세 수준 · 그림 한 장): 아티팩트 「늦게 온 소리」
  <https://claude.ai/code/artifact/dbd0afa7-c923-4108-bfb7-e116aec86504>

---

## 3. 왜 그렇게 풀었나 — 검토했다가 버린 방법

| 버린 것 | 왜 |
|---|---|
| **재측정을 오늘(9/6) 하기** | 표본이 **화·목에 몰려** 있습니다(9/1 화 335분 · 9/3 목 305분). 9/5·9/6 은 **0건**이라 오늘 재면 「고친 뒤」 데이터가 한 줄도 없습니다. → 월·화 수업이 돈 뒤인 **9/9(수)** 로 |
| **재측정을 «밴드 평균» 으로만 판정하기** | 🔴 사람별 편차가 **0.36%(mangoi_114) ~ 32.5%(ysyt01)로 90배**입니다. 그날 회선 나쁜 사람이 **안 들어오기만 해도** 밴드 평균이 내려갑니다. 그걸 「버퍼 덕분」으로 읽으면 **전제가 틀렸는데 맞았다고** 결론내는 것 → 예약 내용을 고쳐 **같은 사람끼리 짝지어** 비교하도록, 양쪽에 다 있는 사람이 없으면 **「비교 불가」로 적도록** 못 박았습니다 |
| **실서비스 주소를 받아 «반영 확인»** | 프록시가 `mangoi.ai`·`test.mangoi.co.kr`·`*.workers.dev` 를 **전부 403**(정책 거부)으로 막습니다. `$HTTPS_PROXY/__agentproxy/status` 의 `recentRelayFailures` 에 `connect_rejected … mangoi.ai:443` 로 남습니다. **우회하면 안 되는 조직 정책**이라 다른 근거를 찾았습니다(4장) |
| **`?v=` 를 안 올리고 넘어가기** | 안 올리면 자산이 «안 바뀐 것» 이라 업로드가 0건이 되고, 그러면 **배포는 초록불인데 아무것도 안 나갑니다**(5장 새 함정) |
| **폴링 스크립트로 CI·배포 감시** | 이 컨테이너는 `api.github.com` 도 프록시가 막아 **폴링이 늘 헛돕니다.** 두 번 다 「440초 안에 결론 안 남」이라는 **거짓 경보**를 냈고, MCP 도구로 재확인하니 이미 성공해 있었습니다. ⟹ GitHub 상태는 **MCP 도구로만** 보세요(이미 CLAUDE.md 2장에 있는 규칙이고, 오늘 그 실사례를 두 번 만들었습니다) |
| **`git stash`·폴더째 되돌리기로 충돌 정리** | `test-harness/asset-versions.json` 원장을 함께 날리는 전례가 있습니다. 파일을 하나씩 다뤘습니다 |

---

## 4. 확인한 방법

### 4-1. 「배포된 파일에 실제로 반영됐나」 — 프록시가 막혔을 때의 근거 사슬

실서비스 주소를 못 여니 **세 겹**으로 확인했습니다.

**① 병합된 커밋에 코드가 있나** — `git show origin/main:cloudflare-deploy/public/js/idx-main.js`

| 찾은 것 | 건수 |
|---|---|
| `mode === 'buf'` (새 3번째 상태) | 3 |
| `__vcRxBufMs` (되돌리기 손잡이) | 2 |
| `__qLate` (막힘 카운터) | 3 |
| `rb < 300` (뒤집힘 방지 가드) | 2 |
| `__rxLowLat` (옛 2상태 흔적) | **0** ✅ |

`index.html`·`teacher.html` 둘 다 `idx-main.js?v=51`.

**② 그 파일이 두 워커에 실제로 올라갔나** — 러너 로그에 **파일 이름이 그대로** 찍힙니다.

```
🌀 Found 3 new or modified static assets to upload
   + /teacher.html
   + /js/idx-main.js      ← 이것
   + /index.html
✨ Success! Uploaded 3 files (1139 already uploaded)
```

| 워커 | 결과 | Version ID |
|---|---|---|
| `webrtc-unified-platform` | Uploaded 9.75초 | `995ed12a…` |
| `webrtc-unified-platform-prod` | Uploaded 9.42초 | `19f2a2cb…` |

**③ 배포 후 점검** — 헬스체크 base/prod **HTTP 200** · TURN `kv-cache` **정상**(둘 다, 무료 폴백 아님)
· `BUILD_STAMP 2026-09-06T04:32:40Z` · 보류 판정 `false`(수업 시간대 아님 → 실제 배포).

### 4-2. 게이트·하니스

| 검사 | #835 | #833 |
|---|---|---|
| `vc_latency_tuning` | PASS 34 | — |
| `vc-netem-lab/selftest.py` | — | PASS 25 |
| `video-enhance-probe/selftest.py` | — | PASS 47 |
| `asset_version` | 221건 통과 | 통과 |
| 첫 화면 예산 | 통과 | — |
| `home_bg_preload` | PASS 12 | — |
| `--fast` | PASS 272 / SKIP 20 기준선 유지 | 같음 |
| CI (러너, `node_modules` 있음) | success 98초 | success 123초 |

⚠️ **이 컨테이너의 `tsc` 는 못 돌립니다** — `cloudflare-deploy/node_modules` 가 없어
`MODULE_NOT_FOUND` 입니다(환경 사유, 기준선). 러너에서는 배포 게이트가 정상 통과했습니다.

### 4-3. 배포 실행

| | #3051 (지터버퍼) | #3052 (진단도구) |
|---|---|---|
| 결과 | success · 3분 42초 | success · 2분 21초 |
| 보류 판정 | 통과(일요일) | 통과(일요일) |
| 업로드 | 자산 3건(위 ②) | 운영 자산 변화 없음 |

---

## 5. 🪤 새로 발견한 함정 — CLAUDE.md 2장에 한 줄 추가했습니다

**「배포됐나」를 «워크플로 초록불» 로 판정하면 안 됩니다.**

Workers 정적자산은 **«바뀐 것만»** 올라갑니다. 그래서 `?v=` 를 안 올렸거나 파일이
그대로면 `Found 0 new or modified static assets` 로 **아무것도 안 올라가는데 배포는
초록불**입니다. 판정은 러너 로그의 그 줄과 **그 아래 파일 목록**으로 하세요 —
프록시가 실서비스 주소를 막는 이 환경에서 **가장 강한 근거**이기도 합니다.

⚠️ CLAUDE.md 4-1-1 의 「배포 워크플로 성공까지만 확인 가능합니다」는 **과소평가**였습니다.
그 줄도 함께 고쳤습니다.

---

## 6. 남은 것 / 주의할 것

- 🔴 **효과는 아직 안 쟀습니다.** 오늘 한 것은 «내보내고 나갔는지 확인» 까지입니다.
  9/9(수) 10:00 KST 에 `rx_conceal` 을 **같은 사람끼리** 재서, 내려갔으면 전제가 맞은 것이고
  **안 내려갔으면 되돌리는 것이 맞습니다.**
  되돌리기는 배포 없이 콘솔 한 줄 — `window.__vcRxBufMs = 0`
- ⚠️ **450ms+ 구간은 배포 전에도 32분뿐**이라, 며칠치로는 「내려갔다」를 단정하기 어렵습니다.
  얇으면 얇다고 적기로 예약 내용에 못 박았습니다.
- ⚠️ **대가(+300ms 지연)는 D1 에 안 남습니다.** 「말이 겹친다」·「반응이 느리다」는
  사람에게 물어야 합니다 — 사장님·강사께 여쭐 항목으로 예약에 넣었습니다.
- ⚠️ **`tools/vc-netem-lab` 은 netem 을 실제로 걸어 본 적이 없습니다**(이 컨테이너에
  `sch_netem` 없음). `tc` 명령 «생성» 은 검증했지만 «적용» 은 진짜 리눅스에서 처음입니다.
- ⚠️ **AI 복원 모델 3종은 한 번도 안 돌려 봤습니다**(프록시가 가중치 호스트 차단).

---

## 7. 같은 날 오후 — 그 뒤에 정한 것 (14:00~23:00 KST, #840 병합 뒤)

### 7-1. ⛔ 검토하고 버린 것 — 「해상도 720p 이하 · 15fps 로 낮추라」

사장님이 일반론(「대역폭을 줄이려면 해상도와 프레임을 낮추라」)을 가져오셨습니다.
**코드와 실측으로 대조한 결과 두 가지 이유로 버렸습니다.**

**① 이미 그 아래에 있습니다** — 올리라는 말에 가깝습니다.

| 권고 | 망고아이 현재 | 어디 |
|---|---|---|
| 720p 이하 | 기본 모드 `low` = **`scale: 2`**(절반 해상도) | `idx-main.js:4922` `vcQualityCaps()` |
| 15fps | 기본 모드 `low` = **`fps: 15`**, 비트레이트 **400kbps**(폰 250) | 같은 줄 |
| — | 손실·RTT 를 4초마다 재서 **5단계**로 더 내림(바닥 1/4 해상도·5fps) | `vcAdaptiveQuality()` |
| — | 오디오 손실 12% 넘으면 **영상을 끄고 소리만**(AAO — CLAUDE.md 1-4 제거 금지) | 〃 |
| 녹화 | 강사 자동녹화 없음(#776) · 학생 **10fps 그리기·0.9Mbps**(#838, 같은 날) | `mango-rec.js` |

**② 원인이 아닙니다** — 2장 표의 그 숫자입니다. 450ms+ 구간에서 보내는 쪽 손실 3.3% ·
진짜 오디오 손실 **0.97%** · 소리 끊김 **33.01%**. 회선이 «좁아서» 버리는 것이면 손실이 높아야
하는데 1% 뿐이라, 패킷은 **도착했는데 늦어서** 버려진 것입니다. 해상도·프레임을 더 깎아도
늦게 오는 것은 그대로 늦게 옵니다. 전날 PR #835 의 «버린 방법» 첫 줄이 정확히 이것이었습니다.

**③ 더 내리면 잃는 것** — 녹화를 720p 아래로 내리면 교재 폭이 896 → 672px 로 줄어 글자를
잃습니다(#838 이 **그래서** 해상도는 두고 fps 만 깎았습니다). 카메라는 5단계 바닥이 있어
**더 내릴 칸이 없습니다.**

⚠️ 단서 하나 — 비트레이트를 낮추면 혼잡 큐의 대기가 줄어 지터가 «조금» 줄 수는 있습니다.
그러나 그 지렛대는 적응 사다리가 **이미 동적으로** 당기고 있고, 실측에서 RTT 가 손실 1% 미만인
틱에도 360~435ms 로 «평평» 했습니다 — 큐가 아니라 **거리**의 모양입니다.

### 7-2. ✅ 사장님 확정 — 「네가 말한대로 가보자」

| 9/9 결과 | 그날 할 것 |
|---|---|
| 같은 사람 기준으로 **내려감** | 두 질문을 여쭙기 — 300ms 가 맞는 값인가 · 200~300ms 구간(전 9.55%)까지 넓힐 것인가 |
| **안 내려감** | 되돌리기(`window.__vcRxBufMs = 0`) + 다음 수는 대역폭이 아니라 **«경로»** — 왜 450ms 인가 · TURN 이 `:3478 tcp` 로 잡히는 이유 |
| 표본 얇음 | 「아직 모름」 — 억지로 결론 안 냄 |

### 7-3. 예약 보강 (`trig_016FQWDmYauisseCw6HBwXXe`)

- **경로 데이터도 함께 뽑게** — `vc_quality` 의 `path`(relay/direct/mixed) · `turn`(host:port proto) ·
  `relay_ticks` · `path_ticks`. 안 내려갔을 때의 다음 수를 **그날 바로** 볼 수 있게.
  ⚠️ `path_ticks = 0` 은 «모름» — direct 로 읽지 않기(CLAUDE.md 2장).
- **⛔ 「해상도·fps 를 더 낮추자」는 제안이 나오지 않게** 못 박음 — 위 7-1 의 결론.
- 결과를 아티팩트 **두 개**에 반영(「늦게 온 소리」 5번 절 · 「소리 고치기 진행표」 다섯째 칸).

### 7-4. 사람이 읽는 보고서 하나 더 — 「소리 고치기 진행표」

<https://claude.ai/code/artifact/32fdd3d6-f553-448b-a16f-cd71aea1d507>

사장님 「오늘 문제가 얼마나 해결되었는지 10세 수준으로」. **다섯 칸 중 네 칸**(문제 찾기 → 이유 →
고치는 법 → 붙이기 ✅ · **정말 나아졌나 재기 ⏳**)으로 그리고, 바로 밑에 「네 칸이 끝났지만
마지막 칸이 제일 중요해요 — 안 나아졌으면 약을 다시 빼는 게 맞아요」를 적었습니다.
⛔ «해결됨» 으로 읽히지 않게 — 첫 문장이 「정말 나아졌는지는 아직 몰라요」입니다.
사장님께 부탁 두 가지(덜 끊기는지 · 대답이 한 박자 늦는 느낌이 있는지)도 거기 있습니다.

---

## 8. 9/9 재측정 — 효과는 «판정 불가», 대신 «우회로» 가 보였습니다

- 시각: 2026-09-09 10:00 KST — 9/6 에 걸어 둔 예약(`trig_016FQWDmYauisseCw6HBwXXe`)이 자동 실행. D1 `vc_quality` **SELECT 만**.
- 배포 후 표본: **9/7(월) 36분·4명 + 9/8(화) 164분·10명 = 200분·14명.** 배포 전 626분의 약 1/3.
- 배포 상태 확인: `origin/main` 의 `idx-main.js` 에 `mode === 'buf'` 3건 — 버퍼는 그대로 살아 있음.

### 8-1. 잰 것

**밴드별 (배포 전 9/1~9/4 → 배포 후 9/7~9/8)**

| RTT | 분 (전→후) | conceal 전 | conceal 후 | aloss 후 | 배수 후 |
|---|---|---:|---:|---:|---:|
| <120ms | 128 → 71 | 2.37% | 0.84% | 0.42% | 2.0 |
| 120–200 | 284 → 51 | 5.46% | 5.38% | 3.04% | 1.8 |
| 200–300 | 104 → 37 | 9.55% | 3.12% | 0.23% | 13.6 |
| **300–450** (buf 구간) | 78 → 38 | **7.82%** | **1.06%** | 0.09% | 11.5 |
| **450ms+** | 32 → **2** | 33.01% | 17.78% | 0 | — |

**같은 사람 8명 (양쪽 기간에 다 나온 사람만)**

| uid | 전 분 | 전 conceal | 전 RTT | 후 분 | 후 conceal | 후 RTT | 후 경로 |
|---|---:|---:|---:|---:|---:|---:|---|
| ysyt01 (유세영) | 20 | 32.49% | 588 | 18 | **0.70%** | **312** | direct 18/18 |
| mangoi_018 (Farrah) | 29 | 27.84% | 494 | 24 | **0.04%** | **235** | direct 24/24 |
| mgr_karl | 8 | 17.50% | 489 | 9 | 4.22% | **88** | relay 3 / direct 6 |
| Mangoi_167 (Hannah) | 22 | 6.34% | 193 | 21 | 0.08% | 134 | direct |
| mangoi_162 (Kaye) | 43 | 6.00% | 159 | 19 | 0.23% | 120 | direct |
| heyst | 42 | 5.36% | 170 | 18 | 1.59% | 138 | relay 7 / direct 8 / mixed 3 (udp+tcp) |
| **jeong** | 26 | 2.59% | 400 | 19 | **10.74%** ↑ | 210 (최대 544) | **relay 19/19 · tcp:3478** |
| delaware | 19 | 0.48% | 121 | 21 | 0.13% | 224 | direct |

**경로별 (배포 후)** — 배포 전은 626분 중 **585분이 «모름»**(`path` 칸이 9/3 부터 기록)이라 전후 비교 불가

| 경로 | 분 | 사람 | conceal | aloss | 배수 | RTT |
|---|---:|---:|---:|---:|---:|---:|
| relay-**tcp** | 34 | 4 | **10.46%** | 0.54% | **19.4** | 206 |
| relay-other | 16 | 4 | 4.14% | 4.49% | 0.9 | 149 |
| mixed | 9 | 3 | 2.03% | 2.34% | 0.9 | 135 |
| **direct** | 140 | 9 | **0.61%** | 0.61% | 1.0 | 179 |

배포 후 conceal 상위 3명: MAIMAI(mangoi_033) 11.13% · jeong 10.74% · student 8.44% — **셋 다 relay 100%, `turn.cloudflare.com:3478 tcp`.**

### 8-2. 거기서 내린 판단 (잰 것과 갈라서)

1. **효과 판정 — 불가.** 겨냥한 구간(300~450)의 conceal 이 7.82 → 1.06 으로 내려갔고 같은 사람 8명 중 7명이 좋아졌지만, **그 사람들의 RTT 도 절반**이 됐습니다(유세영 588→312 · Farrah 494→235 · Karl 489→88). 버퍼는 RTT 를 바꾸지 못하므로 이건 **«길이 좋아진 것»** 일 수 있고, 배포 전 `path` 가 «모름» 이라 그 가설도 확정할 수 없습니다(Farrah 는 배포 전 6분이 relay-tcp 로 잡혀 있어 가능성이 있음). 450ms+ 는 **2분**이라 아예 비교가 안 됩니다.
2. **되돌리지 않습니다.** buf 가 켜지는 구간에서 나빠진 사람이 없고, 지연 +300ms 에 대한 불만도 아직 없습니다(못 잰 것 — 아래).
3. **넓히지도 않습니다.** 「200~300 구간까지」·「300ms 가 맞는 값인가」 두 질문은 효과가 확정된 뒤의 질문이라 보류합니다.
4. **새 신호 — relay-tcp.** 배포 후 소리가 가장 많이 빈 세 사람이 전부 TCP 우회로였고, 그 인구의 conceal/aloss 배수가 **19배**(= 버퍼가 겨냥한 «늦어서 버린 것» 모양)인데 **RTT 가 206 이라 버퍼 게이트(`rb >= 300`)에 걸리지 않습니다.** direct 인구는 배수 1.0 — 늦은 패킷이 없어 버퍼가 살릴 것도 없습니다. ⟹ **게이트가 «먼 사람»(RTT)을 보는데, 늦은 패킷이 실제로 많은 사람은 «우회로 탄 사람»(path) 입니다.** 이건 추론이 아니라 배포 후 200분 실측이지만, 표본이 relay-tcp 34분·4명이라 얇습니다.
5. **jeong(사장님 계정) 악화** 2.59 → 10.74%, freeze 1.8 → 4.6/분, relay-tcp 100%, RTT 최대 544. 그날 환경(와이파이·장소)을 사장님께 여쭤야 합니다.

### 8-3. 다음

- **우회로 조사** — 왜 세 사람이 UDP 가 아니라 `:3478 tcp` 로 잡히는가(9/5 부터 미결). 이건 코드가 아니라 네트워크(방화벽·UDP 차단)일 수 있습니다.
- **버퍼 게이트에 `path === 'relay'` 축을 넣을지** — 대가(+300ms)가 걸린 결정이라 **사람이 정할 일.** 넣기 전에 relay-tcp 인구의 «늦은 패킷» 이 정말 버퍼로 살아나는지 콘솔 `[vc-latency]` 로 한 사람만 먼저 확인하는 것이 순서입니다.
- **1주 뒤(9/16) 재측정** — 이번엔 밴드 평균이 아니라 **«같은 사람 + 같은 경로»** 로만 비교. 배포 전 path 가 비어 있으니 «배포 후 첫 주(9/7~) vs 둘째 주» 가 실제 비교 창입니다.
- ⛔ 해상도·fps 는 더 내리지 않습니다(9/6 결론 유지).

### 8-4. 못 잰 것

- **대가(+300ms 체감)** — D1 에 안 남습니다. 사장님·강사께 여쭐 것.
- **buf 가 실제로 켜졌는지** — 콘솔 `[vc-latency] 수신 지연 buf 300ms` 에만 찍히고 D1 칸이 없습니다. rb ≥ 300 인 사람은 배포 후 **유세영 한 명**(최소 RTT 304)뿐이라 켜진 인구 자체가 1명입니다.
- **배포 전 경로** — 585/626분 모름.

### 8-5. 🪤 새 함정 — CLAUDE.md 2장에 한 줄

**「효과는 배포 뒤에 재겠다」고 미룰 때, 배포 «전» 기록에 «갈라 볼 축» 이 없으면 뒤에 가서 아무것도 못 가릅니다.** 이번에 그 축은 RTT 와 path 였고, path 는 9/3 부터 쌓여 배포 전 표본의 93% 가 비어 있었습니다. 예약을 걸 때 「무엇이 함께 바뀌면 판정이 무효인가」를 먼저 적고 그 칸이 쌓이고 있는지 확인해야 합니다.

---

## 9. 9/16 예약 발화 → 9/21 2차 재측정 — 「계속 보류」가 규칙대로의 답입니다

> ⚠️ **9/16 10:00 KST 예약은 정상 발화했습니다**(`trig_01JydMkv9VV8pSqASd3ZJ4uN` · SUCCEEDED → `run_once_fired`).
> 그 세션에서 측정·문서 편집까지 했지만 **커밋 전에 컨테이너가 회수되어 편집이 전부 사라졌습니다**
> (새 클론 · `git status` 깨끗 · reflog 4줄 · stash 없음 · PR 없음).
> 그래서 앞 세션이 보고한 숫자를 **인용하지 않고 2026-09-21 에 다시 쟀습니다.**
> 덤으로 둘째 주가 이틀이 아니라 **한 주 꽉 찬** 표본이 됐습니다.

### 9-1. 잰 것

D1 `vc_quality`, `rx_conceal >= 0` 인 분(minute) 단위 요약. 창 = 9/7(월)~9/20(일) KST, **SELECT 만**.
w1 = 9/07~9/13 · w2 = 9/14~9/20 (양쪽 다 실질 평일 5일 — 주말은 수업이 없어 표본 0).

| 경로 | w1 분 | w1 conceal | w1 aloss | w2 분 | w2 conceal | w2 aloss |
|---|---|---|---|---|---|---|
| direct | 421 | 0.89% | 0.84 | 494 | 0.65% | 0.37 |
| mixed | 34 | 2.83% | 0.90 | 119 | 2.09% | 0.81 |
| relay-udp | 30 | 4.46% | 0.57 | 54 | 0.40% | 0.06 |
| relay-unk | 26 | 3.63% | 2.22 | 51 | 0.61% | 0.03 |
| **relay-tcp** | **34** | **10.46%** | 0.54 | **14** | **4.46%** | 0.41 |

**9/6 에 적어 둔 병합 규칙과 대조**(§5 · CLAUDE.md 2장 「고치려던 원인이 저절로 사라졌을 때」):

| 조건 | 기준 | w2 실측 | 판정 |
|---|---|---|---|
| relay-tcp 분량 | 30분 이상 | **14분** | ✗ |
| conceal | 5% 이상 | **4.46%** | ✗ |
| conceal / aloss | 3배 이상 | **10.9배** | ✓ |

⟹ **3 중 1 충족 → 미달.**

🔴 **그리고 1차 신호의 «정체» 가 바뀌었습니다.** w1 의 relay-tcp 34분을 날짜·방·사람으로 쪼개니:

| 날 | 방 | 사람 | 분 | conceal |
|---|---|---|---|---|
| 2026-09-07 | `meet-…` | jeong (사장님) | 11 | 8.41% |
| 2026-09-07 | `mangoi-class` (공용방) | jeong | 8 | 13.94% |
| 2026-09-07 | `mangoi-class` (공용방) | MAIMAI | 8 | 11.13% |
| 2026-09-07 | `meet-…` | student (시험계정) | 5 | 7.34% |
| 2026-09-07 | `meet-…` | mgr_karl | 2 | 12.96% |

**전부 하루(9/7) · 전부 회의방·공용방 · 내부 인원 4명 · 예약 수업(`class-…`) 0분 · 학생 0명.**
보고서 두 편이 「소리가 제일 많이 빈 세 사람」이라 부른 그 셋이 바로 이들입니다.

w2 의 relay-tcp 14분에서 **처음으로 진짜 수업이 잡혔습니다** —
Krystel 9/18 `class-…` **9분 · conceal 3.64% / aloss 0.57**(6.4배) · MAIMAI 1분 · Farrah 1분.
나머지 3분은 `meet-…`·`demo-1`.

⚠️ **w2 에 `rb >= 300` 인 사람이 0명입니다** — 8분 이상 접속한 14명의 «최소 RTT» 최댓값이 **189ms**(lee).
⟹ 버퍼의 «먼 사람» 갈래는 그 주 **한 번도 안 켜졌습니다**(«3틱 막힘» 갈래는 D1 에 안 남아 모릅니다).

같은 사람·같은 경로(direct, 양쪽 5분 이상) **10명 중 9명 conceal 하락**, 1명 소폭 상승.
**그러나 RTT 가 함께 내려갔습니다** — jeong 372→222 · ysyt01 223→87 · Farrah 172→104 · 강선생님 366→303.

### 9-2. 거기서 내린 판단 (잰 것과 갈라서)

1. **규칙대로 보류를 잇습니다.** 미달한 둘이 «분량»과 «세기» 라 약한 미달이 아닙니다.
2. **규칙의 전제가 약해졌습니다.** 「relay-tcp 인구가 실재한다」가 근거였는데 그 인구는 **9/7 하루, 우리가 시험한 방**이었습니다. 학생이 그 길을 탄 증거는 w2 의 Krystel 9분이 처음이고 그것도 문턱 아래입니다.
3. **direct 쪽 개선은 버퍼 효과로 못 읽습니다** — 1차와 똑같이 RTT 가 함께 움직였고, 애초에 direct(w2 평균 161ms) 인구는 게이트에 안 걸립니다.
4. 🔴 **새로 보인 것 — conceal 이 높다고 다 버퍼 대상이 아닙니다.** Teacher Win 은 w2 에 conceal **5.06%** 인데 aloss 가 **6.52%**(0.78배)입니다. 버퍼가 겨냥한 «늦어서 버린» 모양(conceal ≫ aloss)이 **아니라 진짜 손실**이라 기다려도 안 살아납니다. ⛔ 앞으로 「conceal 이 높으니 버퍼」로 읽지 말고 **반드시 conceal/aloss 비를 함께** 보세요.

### 9-3. 사람이 정할 일

- **「언제까지 보류하나」** — 2주 연속 미달이고 그 신호가 시험방이었으므로
  ⓐ 규칙을 그대로 두고 계속 지켜보기 ⓑ 보류 브랜치를 닫기
  ⓒ 게이트를 «relay-tcp 면 켜기» 로 바꿔 Krystel 같은 인구를 덮기 — 셋 중 하나를 고르셔야 합니다.
  ⛔ 대가가 **+300ms 지연**이라 저 혼자 정하지 않았습니다.
- 보류 브랜치 `claude/student-teacher-screen-size-w6qio0` 는 원격에 **살아 있습니다**(`c8319d3`). 지우지 않았습니다.

### 9-4. 못 잰 것

- **버퍼가 실제로 켜졌는지** — 여전히 D1 칸이 없습니다(콘솔에만). 위 「0명」은 `rb` 갈래 이야기이고 «3틱 막힘» 갈래는 모릅니다.
- **+300ms 체감** — 1차 때 여쭙고 답을 못 받았습니다. 사장님·강사께 다시 여쭐 것.
- 9/19(토)·9/20(일)은 수업이 없어 표본 0 — w2 는 실질 **9/14~9/18** 입니다.

---

## 관련

- 전날 코드 작업: [260905_수신지터버퍼_늦게온패킷을_기다려주기.md](260905_수신지터버퍼_늦게온패킷을_기다려주기.md)
- 사람이 읽는 보고서: 「늦게 온 소리」 <https://claude.ai/code/artifact/dbd0afa7-c923-4108-bfb7-e116aec86504> · 「소리 고치기 진행표」 <https://claude.ai/code/artifact/32fdd3d6-f553-448b-a16f-cd71aea1d507>
- PR: [#835](https://github.com/navy111p-sudo/mangoiweb/pull/835) · [#833](https://github.com/navy111p-sudo/mangoiweb/pull/833)
