# 수업 중 「상담」이 «수업에서 나가기» 가 되던 마지막 두 자리

**날짜** 2026-09-09 · **PR** [#900](https://github.com/navy111p-sudo/mangoiweb/pull/900)
**앞선 작업** [260902_관제탑_화면코드_js분리.md](260902_관제탑_화면코드_js분리.md) — 같은 함정을 8개 파일에서 고친 건

---

## 1. 왜 했나

2026-09-02 에 사장님이 관리자 「수업 관찰」 화면 사진을 보내 주셨습니다 —
**참관 탭은 이미 열렸는데** 「⚠ 브라우저가 새 창을 막았습니다」 상자가 떠 있었고,
그 안의 링크를 누르면 **탭이 하나 더** 열렸습니다.

브라우저에서 직접 재 보니 원인은 팝업 차단이 아니었습니다.

| 호출 | 반환값 | 탭 |
|---|---|---|
| `window.open(u,'_blank','noopener')` | **null** | 1 → 2 (**열림**) |
| `window.open(u,'_blank')` | object | 2 → 3 |

`noopener` 를 **«기능 문자열»** 로 주면 표준상 탭은 열리는데 반환값이 `null` 입니다.
⟹ 반환값으로 «막혔나» 를 판정하는 코드는 **언제나 «막혔다»** 가 됩니다.

그때 8개 파일을 고쳤지만 **`public/index.html` 두 자리는 못 고쳤습니다** —
CLAUDE.md 4-2 의 **공동 금지구역**이라 배포 담당 승인이 필요했습니다.
그래서 신설 하니스가 그 두 자리를 «사람 결정 대기» 로 **이름을 찍어 출력**하게 두었고,
2026-09-09 에 사장님이 **「그래」** 로 승인해 주셨습니다.

### 반경이 왜 컸나

`window.openKakao` 를 부르는 곳이 **홈과 수업 화면 둘 다**입니다
(`js/vc-dock.js` 의 「💬 상담」·`js/idx-grid-menu.js` — 둘 다 그것으로 early-return).
그런데 이 두 자리의 폴백이 하필 **더 나쁜 변종**이었습니다.

```js
if (!w) { try { location.href = url; } catch(_){} }
```

`w` 가 **항상** null 이므로 이 줄이 **항상** 돌았습니다 ⟹
**카톡 탭이 열리고 + 보던 화면까지 카톡으로 이동.**
수업 중이면 그것이 곧 **«수업에서 나가기»** 였습니다.

⚠️ **다만 이것은 코드·브라우저 실측에서 나온 판단이고, 실제로 그 때문에 수업을 나간
제보가 있었는지는 확인하지 못했습니다.** 잰 것과 거기서 내린 판단을 갈라 적습니다.

---

## 2. 무엇을 고쳤나

`cloudflare-deploy/public/index.html` 두 자리 — `toKakao()` 와 `window.openKakao`.

```diff
-    try { w = window.open(url, '_blank', 'noopener'); } catch(e){}
-    if (!w) { try { location.href = url; } catch(_){} }
+    try { w = window.open(url, '_blank'); } catch(e){}
+    if (w) { try { w.opener = null; } catch(e){} } else { try { location.href = url; } catch(_){} }
```

앞서 8개 파일에 적용한 것과 **똑같은 두 줄**입니다.

⚠️ **두 자리의 무게가 같지 않습니다 — 문서가 같게 읽히면 반경을 과대평가합니다.**
`toKakao()` 는 첫 줄이 `if (window.openKakao) return window.openKakao();` 이고
`window.openKakao` 는 **무조건 실행되는 IIFE** 가 정의하므로, 정상 경로에서
`toKakao` 자신의 `window.open` 은 **거의 안 닿습니다.** 실제 사고를 낸 자리는
**`openKakao` 하나**이고 `toKakao` 는 «그 전제가 깨질 때» 를 위한 방어로 함께 고친 것입니다.

- 기능 문자열에서 `noopener` 를 빼고 **`w.opener = null`** 로 같은 보호를 겁니다.
  고치기 전 코드도 그 보호를 하려 했는데 `w` 가 null 이라 **한 번도 안 돌았습니다.**
- **인앱 브라우저 폴백은 그대로 살립니다** — 카톡·문자앱은 진짜로 null 을 돌려줍니다.
  바뀐 것은 「이제 그 null 이 **진짜** 다」는 것뿐입니다.

### 손대지 않은 것

| 자리 | 왜 |
|---|---|
| `rel="noopener"`(11300·14111행, HTML **속성**) | 무해합니다. 함정은 «기능 문자열» 쪽이고 ①절 검사도 속성은 안 잡습니다 |
| 17387행 `window.open(...,'noopener')` | **반환값을 안 씁니다.** 안 쓰는 호출은 그대로 둬도 무해합니다 |

---

## 3. 검토했다가 버린 것

### ⛔ 「index.html 은 금지구역이니 `vc-dock.js` 쪽에서 감싸자」

`window.openKakao` 를 defer 파일에서 덮어쓰는 길은 실제로 있습니다
(가면 CDN 을 그렇게 우회한 전례 — CLAUDE.md 「blocking 파일을 못 고칠 때」).
**버린 이유**: ① 이번엔 사장님 승인을 받을 수 있었고 ② 덮어쓰기는 **그 이름이 바뀌면
조용히 헛돌아** 원래 경로로 되돌아갑니다(에러도 안 납니다). 정본을 고칠 수 있으면
정본을 고치는 쪽이 맞습니다.

### ⛔ 「경고 상자(「브라우저가 새 창을 막았습니다」)를 없애자」

**버린 이유**: 진짜로 막히는 경우가 실제로 있습니다(팝업 차단·인앱 브라우저).
지우면 그때 **조용히 실패**합니다 — 그게 애초에 그 코드가 생긴 이유입니다.
하니스 ③절이 그 문구가 남아 있는지 지금도 검사합니다.

### ⛔ 「설명 주석을 두 자리에 자세히 달자」

**버린 이유 — 예산.** 아래 5장.

---

## 4. 하니스

`test-harness/popup_open_return_harness.mjs`

- `PENDING_FILES`(«사람 결정 대기» 목록)를 **비웠습니다.**
  그 파일 자신이 「⛔ 고쳤으면 이 목록에서 지우세요」라고 지시하고 있었습니다.
- 대신 ②절 `GUARDED` 에 **`index.html (openKakao)`** 을 올렸습니다 —
  목록에서 빼기만 하면 되돌아가도 아무도 모르므로, **보호를 못 박는 쪽으로 옮긴** 것입니다.
- pending 안내 문구가 `index.html` 을 특정하고 있던 것을 일반화했습니다
  (그 파일이 목록에서 빠졌으니 그 문구는 이제 거짓입니다).

### 🪤 그런데 그 하니스가 두 자리에서 헛돌고 있었습니다 (2026-09-11 정정)

병합 «뒤» 함정 대조가 잡았고, **제가 되돌려 실측해 확인**했습니다 — 둘 다 `21/21 초록`.

| 변이 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 가드를 지우지 않고 **`// w.opener=null` 줄주석으로만** 남기기 | 🔴 통과 | ✅ FAIL 2 |
| **폴백(`location.href`)을 통째로 제거** | 🔴 통과 | ✅ FAIL 1 |

- **줄주석** — `stripBlock` 이 **블록주석만** 벗겨서, 주석 처리된 가드를 ②절이
  «있다» 로 봤습니다. CLAUDE.md 「부정 검사는 주석을 벗겨 낸 사본으로」의 그 자리입니다.
  ⟹ `stripComments` 로 바꿔 **줄주석까지** 벗깁니다. ⚠️ 그때 `//` 를 무턱대고 지우면
  문자열 안의 `https://` 가 잘려 **멀쩡한 코드가 «없다» 로** 판정되므로 따옴표 안인지도
  함께 추적합니다.
- **폴백** — ②(«막힌다»)만 있고 **«나간다» 짝이 없었습니다.** noopener 를 뺀 것은
  반환값을 믿을 수 있게 하려던 것이지 폴백을 없애려던 것이 아닙니다. 그 폴백이
  애초에 이 코드가 생긴 이유입니다(인앱 브라우저는 **진짜로** null). ⟹ ④절 신설.
  ⛔ ④를 나머지 네 자리로 넓히지 마세요 — **실측으로** 그 넷의 창에는 `location.href`
  가 없습니다(폴백이 «안내 상자» 라 구조가 다릅니다). 넓히면 멀쩡한 코드가 빨간불입니다.

### 변이시험 (고친 뒤 · 기준선 PASS 23 / FAIL 0)

| 변이 | 결과 |
|---|---|
| `toKakao` 사고 완전 재현(noopener + 가드 제거) | FAIL 2 |
| `toKakao` **가드만** 제거 | FAIL 1 |
| `openKakao` 사고 완전 재현 | FAIL 2 |
| `openKakao` **가드만** 제거 | FAIL 1 |
| 가드를 **줄주석으로만** 남기기 | FAIL 2 |
| **폴백 제거** | FAIL 1 |

**6종 전부 실제로 exit 1** 입니다. 되돌린 뒤 기준선(23/0)으로 돌아오는 것도 확인했습니다.
⛔ 사본은 `cp` 로 떠서 되돌렸습니다 — `git checkout --` 은 **커밋 안 한 이번 작업까지**
날립니다(CLAUDE.md 의 그 함정).

---

## 5. ⚠️ 첫 화면 예산이 거의 없습니다

| 시점 | blocking | 더 넣을 수 있는 바이트 |
|---|---|---|
| 처음 쟀을 때(9/9) | | 325 |
| 9/11 `main` 병합 시점(`406ac56`) | 1,613,268 | **43** ← 그 사이 다른 브랜치들이 써 버림 |
| 고친 뒤(`e73f9ae`) | 1,613,303 | **8** |

이 수정이 더한 양은 **+35B** 입니다. 게이트(`first_paint_budget_harness`)는 통과합니다.

> 🪤 **처음엔 이 표에 44 · +39 · 9 라고 적었습니다 — 셋 다 틀렸습니다**(함정 대조가 잡아
> 커밋별로 다시 재서 정정). 44·9 는 **한 칸씩 낙관적**이었습니다 — 하니스는
> `Math.round(b/1024) > 1575` 라 **1,613,312B 부터 FAIL** 이므로 통과하는 마지막 값은
> 1,613,311B 이고, 여유는 `1,613,311 − b` 입니다. 「9바이트 남았다」를 믿고 9B 를 넣었으면
> 1,613,312 = `round(1575.5)=1576` 으로 **그 자리에서 빨간불**이었습니다.
> +39 는 아예 다른 중간 상태를 잰 값이었습니다(실측 +35).

⚠️ **9/9 에 잰 값으로는 안 들어갔습니다.** 그날 「넣고도 126B 남는다」로 쟀지만,
PR 이 이틀 열려 있는 동안 `main` 여유가 **43바이트**로 줄어 병합하자마자 초과로
빨간불이 났습니다. CLAUDE.md 가 못 박아 둔 그대로입니다 — *「같은 파일을 만지는 작업이
겹칠 때는 **병합한 «뒤» 에 다시 재세요**」*.

그래서 이 수정에는 **설명 주석을 한 줄도 못 넣었습니다.** 코드에서 공백까지 걷어내
**+35B** 로 줄여 겨우 들어왔습니다(주석 한 줄 = 37B 라 35+37 = 72 > 43 으로 초과).
⚠️ 압축 «전» 판이 몇 바이트였는지는 **재확인 못 했습니다** — 그 커밋이 남아 있지 않습니다.
⟹ 그 두 자리의 설명은 이제 **CLAUDE.md 2장 「`window.open` 이 안 열림」 줄이 유일**하고,
되돌아가는 것은 `popup_open_return_harness` ②절이 막습니다.

> 다음에 이 파일에 뭘 넣으려면 **주석 한 문단만으로도 게이트가 빨간불**입니다.

누가 무엇을 defer 로 뺄지는 사람이 정할 일이라 **별건으로 남깁니다.**
⛔ `--update` 로 기준선을 올려 푸는 것은 마지막 수단입니다 —
그 예산은 필리핀 회선을 지키려고 둔 것입니다.

---

## 6. 같은 단정이 여러 곳에 있었습니다

CLAUDE.md 가 못 박은 그대로 **한 곳만 고쳐서는 안 끝났습니다.**

| 곳 | 뭐라고 적혀 있었나 |
|---|---|
| `CLAUDE.md:168` | 「🔴 `window.openKakao` 는 **아직 이 상태입니다**(승인 대기)」 |
| `docs/INDEX.md:166` | 「🔴 `window.openKakao` 는 **못 고쳤습니다** — 승인 대기」 |
| 하니스 머리말·pending 문구 | 같은 취지 |
| `docs/작업기록/260902_관제탑_화면코드_js분리.md` | 「### 🔴 아직 못 고친 것 … 지금은 **카톡 탭이 열리고 + 보던 화면까지 이동합니다**」 |

「고쳤다」를 적을 때는 **그 반대를 적어 둔 곳을 전부 세는 것**이 같은 작업의 일부입니다 —
안 그러면 다음 사람이 옛 문장을 믿고 다시 안 봅니다.

🪤 **그런데 여기 처음엔 「넷 다 고쳤습니다」라고 적었고, 그 문장 자체가 틀렸습니다.**
**다섯 번째**(2026-09-02 작업기록 본문)가 세지 않은 채 **현재형으로** 남아 있었습니다.
함정 대조가 잡았습니다 — 그 문서의 «요약»(INDEX)은 과거형으로 잘 고쳐 두고
**본문만** 놓친 것이라, 「세었다」는 말이 오히려 다시 안 보게 만들 뻔했습니다.
⛔ 옛 기록을 지우지는 않습니다 — 제목에 «(2026-09-09 수리됨)» 을 달고 한 줄 덧붙였습니다.

---

## 7. 확인한 것

| 무엇 | 결과 |
|---|---|
| `tsc --noEmit` | 통과 |
| `node test-harness/run.mjs --fast` | **PASS 324 / FAIL 0** (SKIP 20 = E2E, 정상) — 9/11 `main` 병합 뒤 다시 돌린 값 |
| `popup_open_return_harness` | PASS **23** / FAIL 0 · 대기 목록 **0건**(21 → 23: ④절 2건 추가) |
| 변이시험 | **6종 전부 실제 FAIL**(4장 표) · 되돌리면 기준선 23/0 복귀 |
| `first_paint_budget_harness` | 통과 (여유 **8바이트** — 5장) |
| 배포 | run [#3180](https://github.com/navy111p-sudo/mangoiweb/actions/runs/34629993976) success · 러너 로그에 `+ /index.html` 업로드 · 헬스체크 200 |
| `git diff` 삭제줄 | 전부 이 수정의 것 — 그 두 자리 4줄뿐 |

### 🙋 사람이 봐 주셔야 하는 것

이 컨테이너는 프록시가 `mangoi.ai` 를 막아 **배포된 화면을 볼 수 없습니다.**
배포 뒤 **수업 중에 「💬 상담」을 눌러**,

1. 카톡이 **새 탭**에서 열리고
2. **수업 화면이 그대로 남는지**

한 번만 확인해 주세요.

---

## 8. 곁가지 — 원격 브랜치가 «이미 병합된 이력» 이었습니다

푸시가 non-fast-forward 로 거절됐습니다. 원격 브랜치에 일주일 전 커밋 두 개가
남아 있었는데, 그 내용은 이미 **squash 로 main 에 들어가 있었습니다**
(squash 병합이라 해시가 main 어디에도 없습니다 — CLAUDE.md 「커밋 해시로 판정하면」).

버리기 전에 **그 커밋이 추가한 줄이 지금 main 에 실제로 있는지** 파일별로 세었습니다
(`?v=` 도 v=34·v=27 로 이미 그보다 앞서 있었습니다). 잃을 것이 없음을 확인하고
`--force-with-lease` 로 밀었습니다.

⛔ 해시가 없다고 «안 한 것» 으로 읽지 마세요 — **지금 코드를 보는 것**이 판정입니다.
