# 「오늘은 이 방으로」 — 예약된 학생을 하루만 다른 회의방으로 (2026-09-10)

> 사장님 지시: 「A, B 두 개 모두 만들어줘」
> (제안 보고서 「회의방 입장, 세 가지 길」의 A안 · B안)

---

## 1. 왜 만들었나

사장님 질문이 출발점입니다 — **「방 이름 어떻게 학생이 바꿔서 들어가지?」**

답을 하려고 입장 경로를 읽다가, **길이 사실상 없다**는 것을 확인했습니다.

### 1-1. 방 번호를 넣을 창이 «0.22초» 만 열린다

`index.html` 의 원클릭 입장 블록 주석이 그 이유를 이미 적어 두고 있었습니다.

| 시각 | 무슨 일이 |
|---|---|
| 0ms | 로비가 열린다 — ⚙️「방 코드 직접 입력」은 **접혀 있다** |
| **+50ms** | `vcRestoreCredentials` 가 **방 코드 칸을 강제로 비운다** |
| **+220ms** | **자동으로 입장한다** — 코드가 비었으니 예약방 또는 공용방으로 |

⟹ 사람이 톱니를 펼쳐 번호를 칠 시간이 **구조적으로 없습니다.**
어제(9/9) 넣은 노란 안내줄(「⚙️ 에 번호를 넣으세요」)은 **자동로그인이 꺼진 학생에게만**
실제로 쓸 수 있는 말이었습니다.

### 1-2. 「로그인 두 번」은 홈 로그인이 없을 때만 생긴다

홈에 로그인돼 있으면 로비의 아이디·비밀번호는 **코드가 이미 채웁니다**
(`index.html` 의 `ph59` · `js/idx-vc-roomcode.js` 의 `armAutoEnter`).
그래서 이 작업의 목표는 「로그인을 없애는 것」이 아니라 **「번호를 넣을 자리를 만드는 것」** 입니다.

---

## 2. 무엇을 만들었나

### A안 — 서버가 「오늘은 이 방」이라고 대신 답한다 (학생 화면 코드 0줄)

로비는 **이미** 「빈 칸이면 오늘 예약 방을 물어보고 자동으로 채우는」 동작을 합니다
(`js/idx-main.js` — 「빈 방코드 → 오늘 예약 방으로 자동 교정」).
**그 답만 바꿉니다.** 그래서 학생이 하는 일은 «평소처럼 「수업 입장」을 누르는 것» 뿐입니다.

| 조각 | 어디 |
|---|---|
| 판정 정본 | **`src/class-room-override.ts`** (새 파일) |
| 표 | `class_room_override` — **`PRIMARY KEY (schedule_id, ymd)`** |
| 적용 | **세 곳** — `api-mango.ts`(학생 입장) · `api-teacher.ts`(강사 [수업 입장]) · `absent-sweep.ts`(결석 감지) |
| 지정 API | **`PUT /api/admin/class-schedules`** (본문 `action`) |
| 선생님 화면 | `teacher.html` 「오늘의 수업」 각 줄의 **「🚪 오늘은 다른 방으로」** |
| 학생 안내 | `js/idx-vc-roomcode.js`(**defer**) 의 `assignedBanner()` |

### B안 — 링크 (이미 있는 것을 그 자리에 붙임)

지정이 끝나면 서버가 **`d.link`** 를 함께 돌려주고, 선생님 화면이 그것을 보여 주며
휴대폰이면 **공유 시트**(`navigator.share`), PC 면 **클립보드**로 넘깁니다.
도메인은 **서버가 `SITE_ORIGIN` 정본으로** 만듭니다 — 화면이 조립하지 않습니다
(어제 그 조립이 갈려서 로그인이 깨지던 것을 고쳤습니다).

---

## 3. 일부러 그렇게 한 것 (되돌리기 전에 읽어 주세요)

### 3-1. 만료를 «표의 열쇠» 에 박았다

`ymd` 가 열쇠의 일부라 **「오늘 하루」가 구조적으로 보장**되고 **따로 지우는 작업이 없습니다.**

⛔ 「만료 컬럼 + 청소 크론」 으로 바꾸지 마세요 — 이 저장소는 «지우는 일을 잊어 다음
수업까지 끌고 가는» 사고를 여러 번 냈습니다. 게다가 cron 은 **5개를 다 써서 새로 못 만듭니다.**

### 3-2. `src/index.ts`(공동 금지구역)를 한 줄도 안 건드렸다

새 경로를 만들면 라우팅 허용목록에 한 줄이 필요한데 그 파일은 금지구역입니다.
그 목록의 등록은 **메서드를 안 가리므로**, 이미 등록된 `/api/admin/class-schedules`(정확일치)의
**비어 있는 PUT** 에 얹었습니다(실측: 그 경로는 GET·POST 만 씁니다 — CLAUDE.md 2장의 그 길).

⛔ 그래서 본문에 `action` 을 두고 **모르는 값은 거절**합니다. 안 그러면 나중에 이 경로에
다른 뜻을 넣을 때 «모르는 요청» 이 조용히 흘러 들어갑니다.

### 3-3. 실패 방향을 **자리마다 다르게** 잡았다

| 자리 | 실패하면 | 왜 |
|---|---|---|
| `applyRoomOverrides`(학생 입장) | **막지 않는다**(fail-open) | 학생이 «입장을 누르는» 경로다. 던지면 수업 입장이 통째로 막힌다. 지정이 안 걸리면 예약방으로 가므로 «고치기 전» 과 같을 뿐 |
| `teacherOwnsSchedule`(권한) | **막는다**(fail-closed) | 「남의 학생을 다른 방으로 보내는」 조작이라 되돌릴 수 없다 |

⛔ 이 방향을 서로 바꾸지 마세요.

### 3-4. 담당 강사 판정에 **이름 매칭을 쓰지 않았다**

근거를 결정론적인 둘로만 제한했습니다 — ① `teacher_account_links` 연결(대소문자 무시 완전일치)
② `class_schedules.teacher_id` 에 로그인 계정명이 그대로 들어간 행.

⛔ `api-teacher.ts` 의 «낱말경계 이름 매칭» 은 여기서 **안 씁니다.** 거기는 «내 수업을 보여 준다» 라
못 보면 문의하면 끝이지만, 여기는 되돌릴 수 없습니다. 강사 번호가 세 갈래인 이 저장소에서
이름으로 이으면 조용히 남의 수업이 붙습니다. 연결이 없는 강사는 본사에 요청하면 됩니다.

### 3-5. 지정 버튼을 [수업 입장] 과 **줄을 나눠** 두었다

2026-08-08 에 [연기] 버튼을 뺀 이유가 «[수업 입장] 바로 옆이라 잘못 누른다» 였습니다.
같은 실수를 반복하지 않으려고 **아래 줄 · 작은 글씨 · 오른쪽 정렬**로 두었습니다.

### 3-6. 🔴 QR 은 **안 만들었습니다**

B안에 「QR 을 더할 수 있다」고 제안했지만 재 보고 **안 하기로** 했습니다.

- 이 저장소가 `admin.html` 에서 쓰는 방식은 **`api.qrserver.com`**(제3자 서비스)입니다.
  그건 **아이 수업 링크를 제3자에게 보내는** 것이고, 중국 회선에서 막히면 그 자리가 **조용히 빕니다.**
- 손으로 QR 인코더를 쓰는 것은 되지만, **이 환경에서 «맞는지 재 볼 방법이 없습니다»**
  (스캔·디코드를 못 합니다). 못 재는 것을 «된다» 고 넣지 않았습니다.

⟹ 대신 **링크 복사 + 공유 시트**로 같은 일을 하게 했습니다. QR 이 꼭 필요하면
«어느 QR 서비스를 쓸지» 는 사람이 정할 일입니다.

---

## 4. ⚠️ 사장님이 정하셔야 할 것 하나

**회의방(`meet-…`)은 예약 수업방이 아니라서 «출석 포인트 · 복습퀴즈 안내 · 강사 AI 코칭» 이 꺼집니다**
(`__vcIsMeetingRoom`).

지금은 **꺼진 채로** 두고 **화면이 그 사실을 말하게** 했습니다 — 선생님에게도(지정할 때),
학생에게도(입장 배너). 감추면 「수업은 했는데 포인트가 0이다」가 되고 아무도 이유를 모릅니다.

**바꿀 수 있습니다** — 「서버가 지정한 방은 수업으로 쳐 준다」로. 서버가 지정한 방이라
학생이 임의로 만든 회의방과 **구별됩니다**(`room_override` 표시가 이미 응답에 실려 있습니다).
다만 그건 **「어디까지를 수업으로 볼 것인가」** 를 정하는 일이라 제가 혼자 정하지 않았습니다.

---

## 5. 확인한 방법

`test-harness/class_room_override_harness.mjs` — **51종 PASS / 0 FAIL**

- **① 방 이름 규칙이 화면과 «같은 말» 을 하는가** — 「글자가 있는가」가 아니라
  화면(`js/idx-vc-room.js`)의 `normalize()` 를 **오려 내 실제로 돌리고**, 서버 정본을
  `node --experimental-strip-types` 로 **컴파일 없이 실제로 돌려** 14가지 입력의 답을 대조합니다.
  (2026-09-09 사고가 정확히 이 규칙이 갈려서 났습니다.)
- **③ 적용** — 가짜 D1 로 실제로 돌려 «갈아 끼우는가 · 지정 없는 수업은 안 건드리는가 ·
  표에 이상한 값이 있으면 무시하는가 · **조회가 던져도 던지지 않는가**» 를 봅니다.
- **④ 권한** — «막는다» 만 넣으면 «전부 막기» 도 통과하므로 **«제대로 통과시킨다» 를 짝으로** 두었습니다.
- **⑤ 배선** — `applyRoomOverrides` 가 세션을 다 만든 «뒤», `current` 를 고르기 «전» 인지
  **위치로** 확인합니다(뒤로 가면 학생이 옛 방으로 갑니다).
- **⑥ 화면** — 버튼이 «있는가» 가 아니라 «그 함수를 실제로 부르는가», 그리고
  「성공이라고 말했는가(`d.ok !== true`)」로 가르는지까지 봅니다.

**변이시험 4종 전부 실제 FAIL**

| 변이 | 결과 |
|---|---|
| 서버 규칙에서 `toLowerCase()` 제거 (화면과 갈림) | **FAIL 3** |
| fail-open 제거 (조회 실패 시 던지게) | **FAIL 1** |
| 권한이 «모르면 통과» 하게 | **FAIL 1** |
| `applyRoomOverrides` 를 `current` 선택 «뒤» 로 옮김 | **FAIL 1** |

그 밖 — `page_contract` ✅(끊긴 버튼 0) · `asset_version` ✅ 224/224 ·
`first_paint_budget` ✅(학생 안내를 **defer** 에 둬서 blocking 0 바이트) ·
`teacher.html` 인라인 스크립트 3개 문법검사 0오류.

---

## 5-2. 🔴 함정 대조가 잡은 것 — **처음 판은 강사와 학생을 다른 방에 앉혔습니다**

제가 학생 쪽(`sessions/today`)만 갈아 끼웠는데, **방 번호를 스스로 만드는 곳이 여럿**이었습니다.
`api-teacher.ts:525` 는 그 파일 안에서 **자기 주석으로** 이렇게 못 박고 있었습니다 —

> 🔑 결정론적 방 번호 — api-mango.ts 의 sessions/today 와 **반드시 같은 식**.
> 다르면 강사와 학생이 서로 다른 방에 들어가 수업이 성립하지 않는다.

**그 불변식을 제 변경이 깼습니다.** 지정한 그 선생님이 자기 화면에서 [수업 입장]을 누르면
**옛 방**으로 가고, 확인창은 「학생은 아무것도 안 해도 됩니다」라고만 했습니다.
증상은 **「같은 수업인데 둘 다 참여자 1명」** — 이 저장소가 네 번 사고 낸 그 모양이고,
간헐이 아니라 **매번**입니다.

더 나쁜 것이 하나 더 있었습니다 — `absent-sweep.ts` 가 예약방의 출석을 보므로
**학생이 지정된 방에 멀쩡히 있는데 「결석 위험」이 찍히고**, 그 `class_no_show` 행을
`no-show-truth.ts` 가 읽어 **그 수업의 수업료가 0원**이 됩니다.

✅ 세 곳(`api-mango`·`api-teacher`·`absent-sweep`)이 전부 지정을 보게 했습니다.
🪤 그때 `absent-sweep` 은 행에 **`schedule_id` 를 안 담고 `id` 만** 담고 있어서 헬퍼가
**에러 없이 늘 헛돌** 뻔했습니다(CLAUDE.md 「헬퍼에 행을 넘겼는데 아무 일도 안 일어남」) —
같이 고쳤고, ⑦절이 **「그 행이 `schedule_id` 를 담는가」** 로 못 박습니다.

### 그 밖에 함정 대조가 잡은 것 (전부 수리)

| 잡힌 것 | 왜 나쁜가 |
|---|---|
| **하니스 배선 검사가 글자만 봐서** `if (mine !== true **&& false**)` 로 **담당 확인을 무력화해도 51/51 초록** | 이 기능의 유일한 권한 방어선이 안 지켜지고 있었습니다 → **«그 결과를 조건으로 쓰는가»** 로 |
| 같은 이유로 **지사 차단 무력화**도 통과 | 위와 같음 |
| ⑥-10 이 **선언줄**(`var typedAtJoin = '';`)에 걸려 **캡처 한 줄을 지워도 통과** | 스스로 번호를 친 사람에게도 「선생님이 옮겼다」가 뜹니다 → **«입력칸에서 실제로 읽는가»** 로 |
| ⑥-5·⑥-6 이 **«길이로 자르기»**(`[\s\S]{0,2600}`) | 함수가 448자만 자라면 거짓 통과, 지금도 **옆 함수를 스캔**해 거짓 실패 위험 → **중괄호 짝**으로 |
| ⑥-11 이 **식 모양을 글자 그대로** 못 박음 | `MEET_RE` 로 정리하는 무해한 리팩터에 거짓 FAIL |
| 권한이 **차단목록**이라 `getAdminActor` 가 스코프 조회를 삼키면 `staff` 로 떨어져 **담당 확인이 통째로 건너뛰어짐** | D1 이 한 번 흔들리면 fail-open → **「본사인가」를 양성으로** 묻게(`role === 'hq'`) |
| 예약 조회 실패를 `.catch(() => null)` 로 뭉쳐 **「그 수업이 없습니다」라는 거짓 사유** | `lookup_failed`(503)로 갈라 말하게 |
| **`note` 가 죽은 칸인데 학생 응답으로 나감** | 누가 채우는 순간 «선생님 메모가 학생 화면으로» 가는 길 → **받지 않게** |
| B안 링크의 `navigator.share` 를 **`alert` 뒤**에 부름 | 그 시점엔 제스처가 사라져 거절되는데 `.catch(){}` 가 **조용히 삼킵니다** → 클립보드 먼저 + 공유는 **사람이 한 번 더 누를 때** |
| 표가 생기기 전 **hot path 에서 매 요청 `console.warn`** | 학생 29,000명 경로 → isolate 당 한 번만 |

**변이시험 7종 전부 실제 FAIL** — 서버 규칙 `toLowerCase()` 제거(3) · fail-open 제거(1) ·
권한 «모르면 통과»(1) · 호출을 `current` 뒤로(1) · **담당 확인 무력화(1)** ·
**지사 차단 무력화(1)** · **`typedAtJoin` 캡처 삭제(1)**.

---

## 6. 아직 못 한 것 · 못 잰 것

- **실제 브라우저로 눌러 본 적이 없습니다.** 이 환경의 프록시가 `mangoi.ai` 를 막아
  배포된 화면을 제가 못 봅니다. 선생님 화면에서 한 번 눌러 봐 주셔야 끝입니다.
- **D1 표는 첫 지정 때 만들어집니다**(`ensureRoomOverrideTable`). 배포 직후 SQL 로 조회하면
  `no such table` 이 나는데 **배포 실패가 아닙니다**(`attendance.host` 와 같은 사정).
- **관리자 화면(`admin.html`)에는 아직 안 붙였습니다** — 지금은 **강사 화면**에서만 지정합니다.
  API 는 본사 계정도 그대로 쓸 수 있으니, 관리자 카드에 붙이는 것은 이어서 하면 됩니다.
- 지정을 **누가 언제 했는지**는 표에 남지만(`created_by`·`created_at`) **화면에서 볼 곳이 없습니다.**
- 🟡 **아직 «옛 방» 을 가리키는 곳이 남아 있습니다** — 수업 관찰·관제탑의 **참관 버튼**(`classes-now.ts`)과
  **수업 리마인더 링크**(`lesson-reminder.ts`), 관리자 화면의 «직접 입장». 지정된 수업에서는
  그 버튼들이 **빈 방**을 가리킵니다. 사고(급여 0원·서로 못 만남)는 아니지만 «선생님이 안 보인다» 는
  오해를 만들 수 있습니다. 하니스 ⑦절이 그 목록을 **이름을 찍어 출력**합니다(FAIL 로는 안 만듭니다).
- **타입체크(`tsc`)를 못 돌렸습니다** — 이 컨테이너에 `cloudflare-deploy/node_modules` 가 없습니다.
  `class-room-override.ts` 는 하니스가 실제로 import·실행하지만 `api-admin.ts`·`api-mango.ts`·
  `api-teacher.ts`·`absent-sweep.ts` 변경분은 **CI 러너가 게이트에서 돌립니다.**
