# 2026-09-13 — 버그·피드백 접수함 「신고자」 칸이 「-」로만 나오던 건

> 사장님 제보(캡처): 관리자 › 🐞 버그·피드백 접수함 › 신고 목록의 **신고자 칸이 전부 「- / 교사」**.
> 「여기에 왜 이름이 나오지 않지?」

## 1. 잰 것

**D1 `bug_reports` 전수(4행) — 2026-09-13**

| id | reporter_role | reporter_uid | reporter_name | page_url | 접수(KST) |
|---|---|---|---|---|---|
| 4 | teacher | **NULL** | **NULL** | `test.mangoi.co.kr/?vc_autojoin=1&vc_role=teacher&vc_room=class-895-20260825&vc_name=교사+mangoi_167…` | 08-25 21:10 |
| 3 | teacher | **NULL** | **NULL** | `…vc_room=demo-1&vc_name=교사+mangoi_167…` | 08-25 18:56 |
| 2 | teacher | **NULL** | **NULL** | `test.mangoi.co.kr/` | 07-30 18:56 |
| 1 | teacher | **NULL** | **NULL** | `test.mangoi.co.kr/` | 07-28 18:05 |

- 4건 **전부** 계정·이름이 NULL. 화면은 정직하게 「-」를 그렸습니다 — 화면 버그가 아닙니다.
- 4건 전부 `page_url` 이 홈(`/`) = **홈 화면의 🐞 FAB**(`index.html` `bugReportFab`)에서 보낸 것.
  `teacher.html` 의 「문제 신고하기」(8월 말 멜카 요청 5번으로 신설)로 보낸 건은 아직 0건.
- 3·4번은 주소에 `vc_name=교사 mangoi_167` 이 **그대로 들어 있습니다** — 이름을 알 수 있었는데 안 적힌 것.

## 2. 원인 — 「로그인 세션이 두 갈래」 함정 (CLAUDE.md 2장)

홈 FAB 의 `submit()` 이 신고자를 이렇게 읽습니다:

```js
var u=(window.getCurrentUser?window.getCurrentUser():null)||{};
… reporter_uid:u.uid||'', reporter_name:u.name||'' …
```

`getCurrentUser()`(`js/idx-user-session.js`)는 **`localStorage['mangoi_logged_user']`** 를 읽는데
그것은 **학생 전용 키**입니다. 교사·본사·지사는 `mangoi_admin_session` **쿠키**(+ localStorage
`mangoi_admin_session`)로 로그인하므로 그 키가 없고 → `u = {}` → 빈 문자열 → 서버가 NULL 로 저장.

서버 `POST /api/bug-report`(`api-admin.ts`) 머리말은 이렇게 적혀 있었습니다:

> 공개 — **교사에겐 admin 세션이 없어** 신원은 clientside 전달

**사실이 아닙니다.** 교사는 admin 세션이 «있습니다». 그 거짓 전제 때문에 서버는 본문 값만 믿었고,
본문은 학생 키를 읽어 늘 비어 있었습니다. 요청은 `credentials:'include'` 로 쿠키를 **이미 싣고**
있었으니 서버는 처음부터 «누가 보냈나» 를 알 수 있었습니다.

## 3. 고친 것

### 3-1. 서버 — `src/api-admin.ts` `POST /api/bug-report`
세션 쿠키로 신원을 **먼저** 채웁니다(`getAdminActor`). 규칙:

| 상황 | uid | name | role |
|---|---|---|---|
| 세션 있음 | `actor.username` | `admin_account.name` → 없으면 본문 name → 없으면 username | 강사면 `teacher`, 아니면 `admin` |
| 세션 없음(학생·비로그인) | 본문 값 | 본문 값 | 본문 값 |
| 세션 조회가 던짐 | 본문 값 | 본문 값 | 본문 값 |

- 세션이 있으면 본문에 남의 이름을 적어도 **서버 값이 이깁니다**(위조 방지).
- 조회 실패는 삼킵니다 — **신고가 막히는 쪽이 더 나쁩니다**(fail-open, 보안 게이트가 아님).
- 머리말의 거짓 전제를 정정해 적었습니다.

### 3-2. 화면 — `public/js/adm-bugreports.js` (`?v=3`)
서버 수리 «전» 에 쌓인 4건은 둘 다 NULL 이라 여전히 「-」입니다. 읽을 때 알 수 있는 데까지 보여 줍니다:
① name → ② uid → ③ `page_url` 의 `vc_name`(화상수업 입장 표시이름). ③ 은 **계정이 아니라 주소에
적힌 표기**라 «주소에서» 라고 함께 적습니다. 결과: 3·4번은 「교사 mangoi_167 · 교사 · 주소에서」,
1·2번은 그대로 「-」(주소에 아무것도 없음 — **지어내지 않습니다**).

⛔ D1 을 UPDATE 로 «채우지» 않았습니다(1-1). 그 두 건이 `mangoi_167` 인 것은 주소 표기일 뿐
로그인 계정으로 확인된 것이 아니라, 사람이 보고 판단할 일입니다.

## 4. 검토했다가 버린 방법

| 방법 | 왜 안 했나 |
|---|---|
| `index.html` FAB 이 `mangoi_admin_session` localStorage 도 읽게 고치기 | 공동 금지구역 + 첫 화면 예산 여유 **9바이트**. 서버 한 곳으로 두 화면(홈 FAB · `teacher.html`)이 함께 고쳐집니다 |
| 교사에게 `mangoi_logged_user` 를 만들어 주기 | CLAUDE.md ⛔ — 학생 전용 기능이 통째로 열립니다 |
| 세션이 있어도 본문 값이 있으면 본문 우선 | 세션 있는 사람이 남의 이름으로 신고할 수 있습니다. 서버가 아는 값이 이겨야 합니다(변이 Ⓑ가 이것) |
| 옛 4건을 D1 UPDATE 로 `mangoi_167` 채우기 | 개발·운영 같은 DB(1-1) + 주소 표기는 계정 확인이 아닙니다 → 읽을 때만 보탬 |
| 무인증 경로에 게이트 달기 | 학생·비로그인 신고를 막게 됩니다. 필요한 것은 «막기» 가 아니라 «누구인지 알기» |

## 5. 확인한 방법

- 신설 `test-harness/bug_report_identity_harness.mjs` **PASS 27 / FAIL 0** — POST 핸들러 블록과
  `brReporterOf` 를 소스에서 **오려 내 가짜 request·env 로 실제로 돌립니다.** 짝: 「세션이 있으면
  서버 값이 이긴다」↔「없으면 본문 값 그대로」·「던져도 저장된다」↔「그때 값은 본문 값」.
  D1 실측 행(3번 주소)을 그대로 넣어 화면 폴백을 확인.
- **변이 4종 전부 실제 FAIL**: Ⓐ actor 블록 삭제(옛 코드) 6건 · Ⓑ 본문 우선으로 뒤집기 2건 ·
  Ⓒ 예외를 안 삼킴 2건 · Ⓓ 화면 폴백 삭제 2건. (Ⓒ는 처음에 하니스를 «크래시» 시켰고 —
  CLAUDE.md 가 금지한 모양 — 깔끔한 FAIL 로 고쳤습니다.)
- `asset_version_harness` 226/226 (원장에 `adm-bugreports.js?v=3` 기록).
- `run.mjs --fast` 를 `origin/main` 워크트리와 나란히 돌려 ⚠ 목록을 `comm` 대조 → **새로 빨간 것 0건**
  (PASS 314→315 는 새 하니스 1개. ⚠15개는 양쪽 동일 = `node_modules` 없는 컨테이너 사유).
- ❌ **못 한 것**: `tsc`(이 컨테이너에 `node_modules` 없음 — CI 가 봄) · 배포 화면 확인(프록시가
  `mangoi.ai` 차단 — 교사 계정으로 신고 한 번 보내 접수함에 이름이 뜨는지 **사람이 한 번 봐야** 끝).

## 6. 남은 것 (사람 결정)

- 옛 1·2번(7/28·7/30 「very laggy」)은 누가 보냈는지 **알 방법이 없습니다** — 주소에도 표기가 없습니다.
- `getAdminActor` 는 스코프 조회(`getScope`→`autoSeedOne`)를 함께 타서, 이 공개 경로에서도
  세션 있는 계정에 `admin_scope` 행이 없으면 심어집니다 — 다른 관리자 호출과 같은 동작이라
  새로 생기는 위험은 아니지만, 그 함수의 «이름 못 읽으면 `'none'` 심기» 문제(CLAUDE.md 2장 미결)는
  그대로입니다.
