# SIU 교재 업로드 검증 · AI 웜업에 «단계별로» 붙이기 (2026-09-15)

## 1. 어디서 시작했나

사장님이 화면 두 장을 주시며 **「이거 모두 확인해줘」** → 이어서 **「이제 다 A.i웜업 진행해줘」**.

- 화면 ①: 수업 중 교재 라이브러리 — 왼쪽이 `🇬🇧 English › SIU BASIC (31)` 과 `📁 SIU (29)` **두 덩어리로 갈려** 보임
- 화면 ②: 업로더 분류 결과 — `29 textbook groups · 438 files` · `1 file excluded`

## 2. 잰 것 (운영 D1 실측)

### 2-1. SIU 업로드는 성공했다
`textbook_files` 에서 `[SIU BASIC%` 로 묶어 세니 **29권 · 438장 · 전부 active**(2026-09-15 19:11~19:18 업로드).
권별 파일 수를 전부 더하니 **438** — 업로더가 말한 숫자와 1장도 안 틀립니다.

### 2-2. 「두 덩어리」는 고장이 아니다
`js/idx-x3.js` 실측:
- `_serverFilesToBooks()` 가 **서버** 교재의 `publisher`·`textbook` 을 **대괄호 이름 그대로** 넣고,
  `_srvGuessCategory()` 가 이름에 `siu` 가 있으면 분류를 **"SIU"** 로 만듭니다 → `📁 SIU (29)`
- `classifyFile()`(업로더)은 **상위 폴더명**을 출판사로 써서 로컬(IndexedDB) 쪽은 `SIU BASIC (31)`

⟹ **같은 교재의 «내 PC 판»과 «서버 판»이 나란히 보이는 것**입니다. 다른 선생님에게 보이는 것은 서버 쪽 29권뿐.

### 2-3. 🔴 아직 남은 것
- **`SIU BASIC 012` 가 서버에 없습니다** (001~030 중 012만 빔)
- 로컬 31 대 서버 29 → **2권이 서버에 안 올라갔습니다**(업로더의 `1 file excluded` 와 관련 있어 보임 — 추론)
- **이름에 공백 두 칸이 3건**: `SIU BASIC 008 - Celebrations  Special Days` · `011 - Fashion  and Appearance` · `026 - Should  you`

### 2-4. 🔴 「웜업에 붙인다」의 진짜 장벽
웜업이 문장을 가져오는 곳은 `review_quizzes` 입니다. 전수:

| 교재 | 문항 |
|---|---|
| BTS 1 001~008 | 각 1 |
| BTS 2 Korea · BTS 12 Korea | 2 · 1 |
| SIU BOOKS(옛 통짜본) | 1 |
| 다락원(중국어) | 15 |
| **SIU BASIC 001~030** | **0** |

active 전체 **29건 · 14교재**. ⟹ **교재 이미지 438장을 올려도 웜업은 SIU 문장을 한 줄도 못 받습니다.**

## 3. 사장님이 고르신 것

1. **SIU 노출** → 「단계별로 나눠서」(BTS = 낮은 단계, SIU = 높은 단계)
2. **SIU 문장** → 「지금은 주제만」(교재 OCR 은 안 함)

## 4. 한 일 (`cloudflare-deploy/public/warmup.html`)

- `SIU_BOOKS` 표 29권 — `book`(D1 이름 **그대로**) · `en`(AI 에게 갈 주제) · `ko`(학생 안내)
- `SIU_BAND_MIN = 5` — 1~4단계는 BTS 를 위에, 5~8단계는 SIU 를 위에. **나머지는 접어 둡니다**(없애지 않음)
- `bookIdOf()` — BTS 는 숫자, SIU 는 문자열. ⛔ `parseInt` 금지(NaN→0→자유 대화)
- `btsBookOf()` 가 두 표를 다 뒤짐 · `bookTitleOf()` · `allBooks()`
- `applyBtsBook()` — SIU 면 `WCTX.textbook` 에 **D1 이름 그대로**, `WCTX.level` 은 **빈 값**, 과 조회 **안 함**
- 시작 버튼 위 «지금 교재» 줄과 «이어가기» 칩이 SIU 도 말함

## 5. 검토했다가 버린 것

| 안 | 왜 버렸나 |
|---|---|
| BTS 34권 + SIU 29권을 **한 목록**에 | 7살 학생 목록에 「Job interview」가 올라옵니다. 그리고 어제 「교재 목록을 아무도 못 찾는다」를 고친 자리라 34→63 이면 그 수리를 되돌리는 셈 |
| SIU 권마다 **난이도(band)를 매기기** | SIU BASIC 은 전부 같은 한 레벨입니다. 가를 자료가 없는데 매기면 **지어내는 것** |
| `_btsVol` 을 **문자열 id 로 전면 리팩터** | 동작하는 코드(하니스 77+81종)를 크게 흔듭니다. `bookIdOf` 한 곳으로 타입을 정규화하는 쪽이 반경이 작습니다 |
| 낮은 단계에서 SIU 를 **아예 감추기** | 어른이 쉬운 교재를, 아이가 어려운 교재를 고르는 일이 실제로 있습니다 |
| 교재 이름을 **런타임에 서버에서 받기** | 웜업 설정 화면에 네트워크 의존이 생깁니다. BTS_BOOKS 가 이미 «이 화면의 정본» 으로 문서화돼 있어 같은 방식 유지 |

## 6. 확인한 방법

- `warmup_bts_book_harness` **PASS 110 / FAIL 0** (77 → 110, ⑨절 32종 신설)
- `manual/warmup-bts-book-browser` **PASS 94 / FAIL 0** (82 → 94, ⑪절 신설) · **두 번 연속** 동일
- `--fast` **PASS 324** · FAIL 16 — `origin/main` 워크트리와 `comm` 대조해 **목록이 한 줄도 안 다름**(전부 `esbuild`/`node_modules` 환경 사유)
- 변이시험 — **자동 8종 · 브라우저 3종 전부 실제 FAIL**
  - 자동: parseInt 되돌리기(6) · 조건 항상거짓(2) · **조건 항상참(3)** · D1 이름 안 보내기(1) ·
    없는 Lv 지어내기(1) · 과 조회 가드 제거(1) · 표 비우기(2) · 없는 012 넣기(1)
  - 브라우저: siuFirst 항상거짓(3) · 항상참(4) · SIU 갈래 죽이기(1)

### 6-1. 검사가 헛돌던 것 — 함께 고침
1. **`bookIdOf is not defined`** 로 자동 하니스가 **9건 크래시** → 새 헬퍼를 소스에서 오려 내 주입
2. 브라우저 검사가 **「교재 버튼 33개」** 로 개수를 못 박아 거짓 FAIL → 「표에 있는 교재가 안 빠졌는가」로,
   기대값은 **소스에서 읽기** + 「표를 읽었다」 전제 검사
3. `no(...)` 는 브라우저 하니스에 없음 → **크래시**(결과줄조차 안 나옴) → `t(..., false)` 로
4. 「지금 교재 줄이 SIU 라고 말한다」를 **이름만으로** 물어 SIU 판정을 죽여도 통과(93/0) → **📗 표시**를 짝으로

### 6-2. 정정 — 그리고 «고쳤다» 가 한 번 거짓이었습니다
- `String(a)===String(b)` 를 `===` 로 되돌리는 것은 **등가 변형**입니다(`bookIdOf` 가 이미 정규화).
  「변이 N종 전부 FAIL」의 N 에 **넣지 않았습니다.**
- 🔴 **그 주석을 고쳤다가 «낡은 백업» 에 다시 덮였습니다.** 변이시험을 `cp /tmp/warmup.bak.html` 로
  되돌렸는데 그 사본이 **주석을 고치기 «전»** 것이었습니다. 그래서 소스에는 사실이 아닌 주석이
  그대로 남았는데 이 문서에는 「고쳤습니다」라고 적혀 있었고, **함정 대조가 잡았습니다.**
  ⟹ CLAUDE.md 가 네 번 사고를 낸 「«하기로 한 것» 이 «했다» 로 적히면 아무도 다시 안 본다」 그대로입니다.
  ✅ 교훈: **변이시험 백업은 «편집할 때마다» 다시 뜨세요.** `cp` 로 떠 두라는 규칙의 뒷면입니다.

### 6-3. 함정 대조가 잡은 «검사가 원리상 안 도는» 구멍 2건 (실측)
둘 다 **자동 하니스가 110/0 으로 통과**하던 자리입니다.

| 변이 | 고치기 전 | 고친 뒤 |
|---|---|---|
| `btsRenderBookNow` 의 SIU 갈래 되돌리기 (화면이 「📘 지금 교재 **BTS siu015**」라고 거짓말) | PASS 110 / **FAIL 0** | **FAIL 2** |
| `pickInRest` 의 ` open` 제거 (고른 교재가 접힌 채 숨음) | PASS 110 / **FAIL 0** | **FAIL 1** |
| 「지금」 표시(`on`) 제거 | — | **FAIL 1** |

- 원인 ①: ⑧절의 `run(vol,…)` 을 `0 · 1 · zh` 로만 불러 **SIU 를 한 번도 안 넣었습니다.**
- 원인 ②: ⑨절의 `runList(band)` 가 **언제나 `_btsVol = 0`** 을 넘겨, `item()` 의 `on` 과
  `pickInRest` **두 갈래가 원리상 한 번도 실행되지 않았습니다.**
  → `runList(band, vol)` 로 넓혀 「고른 교재가 접힌 쪽」 상황을 실제로 만들고,
  **「안 골랐으면 안 펴진다」를 짝으로** 두었습니다(없으면 «언제나 open» 도 통과).

### 6-4. 함께 고친 것 (함정 대조 🟡)
- **정적 안내가 BTS 만 말하던 것** — 「BTS 교재를 고르면」 → 「교재를 고르면」.
  5단계 이상 학생은 그 줄 **바로 밑**이 SIU 목록이라, 한 화면이 딴말을 하고 있었습니다.
- **`btsPickLesson` 방어 한 줄** — 지금은 SIU 가 도달하지 않지만(과 목록을 안 부름),
  오면 `「BTS siu015 (Habits)」` 가 되어 **이 기능의 전제가 에러 없이 무너집니다.**

### 6-5. 함정 대조가 «못 잰» 것 (남겨 둡니다)
운영 D1(29권·438장·012 없음·공백 두 칸·`review_quizzes` 0건) · 배포 화면 ·
브라우저 하니스(그 세션에 playwright/CDP 없음). **제가 이 세션에서 잰 값들이고**,
브라우저 검사는 제가 CDP 로 직접 돌려 94/0 을 확인했습니다.

## 7. 곁다리로 한 것 (사장님 승인)

BTS 5 의 공백 두 칸 2건을 rebook 규칙 그대로 정정 — `BTS 5 005  (Seasons)`·`006` → 공백 1칸.
**46건**(24+22) `name` 만 UPDATE, R2 원본 무손상, 충돌 0, `admin_audit_logs` 에 1줄 기록.

## 8. 남은 것 — 사람이 정할 일

1. **SIU 012 + 서버에 안 올라간 2권** — Mai 가 다시 올려야 합니다
2. **SIU 이름 공백 두 칸 3건**(008·011·026) — 고치면 `SIU_BOOKS` 표의 `book` 칸도 **함께** 고쳐야 합니다
3. **SIU 문장(review_quizzes) 0건** — 교재 OCR 또는 사람 입력. 이것 없이는 「오늘 배울 핵심 문장」이 안 나옵니다
4. **`SIU BOOKS`(954장)·`Shake it up 2-18-25`(1,709행)** 옛 통짜본 정리 — 삭제는 300장씩이라 여러 번 눌러야 합니다

---

## 9. 후속 (같은 날 밤) — SIU 교재 이름 셋 수리 + 「문장이 없다」는 틀린 말이었음

사장님 「응」(§8 의 이름 수리 승인)으로 진행하다 **더 큰 것**이 나왔습니다.

### 9-1. 잰 것

| 잰 것 | 값 |
|---|---|
| D1 `textbook_files` SIU BASIC 권수 | **29권**(001~011 · 013~030 — **012 가 없음**) |
| 겹공백이 든 행 | **44행** = 008 **14** · 011 **15** · 026 **15** |
| 그 이름을 쓰는 다른 표 | **0곳** — `textbook_hidden_books`·`review_quizzes` 의 SIU 값은 별개인 `SIU BOOKS`(잔재 4장, 이미 숨김) |
| 저장소의 SIU 제목·문장 정본 | `public/js/speech-data-siu-basic.js` 의 `SIU_BASIC_SENTENCES` — **30과 × 100문장 = 3,000문장** |
| 그 파일을 쓰는 곳 | **발음 코칭(`speech-coach.html`) 하나** |
| D1 29권 ↔ 정본 30과 이름 대조 | **22권 일치**(대소문자만 다름) · 7권 어긋남 |

어긋난 7권: `008` and 빠짐 · `013` `&` 대 `and` · `027` `the` · **`022`·`023`·`024`·`025` 는 정본에 아예 없음.
정본에만 있는 4과: `Law and Order` · `Money` · `Plans` · `Relationships`(+ D1 에 없는 `012 What If`).

### 9-2. 「SIU 문장 0건 → OCR 이 필요」는 **틀린 말이었습니다**

§8 과 사장님께 드린 보고에 그렇게 적었는데, **`review_quizzes` 에 0건인 것은 맞지만 «문장이 없다» 는 사실이 아니었습니다** —
발음 코칭이 쓰는 3,000문장이 저장소에 이미 있었습니다. 저는 표 **하나만** 세고 결론을 냈습니다.

⚠️ **그렇다고 그대로 갖다 쓸 수는 없습니다.** 번호가 021~025 구간에서 어긋나(위 표) **번호로 이으면 남의 과 문장이 붙습니다.**
그리고 그 문장들이 «교재 원문» 인지는 **안 쟀습니다** — 한 과가 한 문형을 되풀이하는 «패턴 연습문» 으로 읽힙니다
(1과는 전부 수여동사: *gave his grandmother a tablet* / *wrote my pen pal a letter* …).
⟹ **웜업에 붙일지는 사람이 정할 일**로 남깁니다. 붙인다면 **이름 완전일치로만**, 안 맞으면 «모름».

### 9-3. D1 수리 (사장님 승인 · 44행)

```sql
-- 각각 instr(name, '…') = 1 로 그 묶음만, replace 로 이름 부분만
[SIU BASIC 011 - Fashion  and Appearance] → [SIU BASIC 011 - Fashion and Appearance]   -- 15행
[SIU BASIC 026 - Should  you]             → [SIU BASIC 026 - Should you]               -- 15행
[SIU BASIC 008 - Celebrations  Special Days] → [SIU BASIC 008 - Celebrations and Special Days]  -- 14행
```

확인: 겹공백 SIU 행 **0건**, 장 수 그대로(14·15·15).

🔴 **008 은 «겹공백» 이 아니었습니다** — 승인받은 대로 공백만 줄이면 `Celebrations Special Days` 라는 **없는 제목**이 됩니다.
정본이 같은 과를 `Celebrations and Special Days` 로 부르고, 옆의 011 도 그 사람이 `and` 를 쓰는 것을 보여 주므로
**낱말 `and` 를 되살리는 쪽**으로 골랐습니다. 이것은 «공백 수리» 가 아니라 **내용 판단**이라 여기 적어 둡니다 —
Mai 의 원래 폴더명이 정말 달랐다면 `UPDATE` 한 줄로 되돌아갑니다.

⛔ `&` 가 벗겨졌다는 가설은 **013 `Food & eating` 이 살아 있는 것**으로 기각했습니다(업로더는 `&` 를 안 벗깁니다).

### 9-4. 함께 고친 것

`warmup.html` 의 `SIU_BOOKS` 세 줄(`book`, 008 은 `en` 도)을 **같은 커밋에서** 맞췄습니다.
⛔ 한쪽만 고치면 서버 `warmupLessonContext` 가 `LOWER(textbook)=LOWER(?)` **정확일치**라 조용히 안 붙습니다.
ℹ️ 오늘은 SIU `review_quizzes` 가 0건이라 그 창이 무해했지만, 문장이 생기는 날 바로 물립니다.

### 9-5. 확인한 방법

- `warmup_bts_book_harness` **PASS 116 / FAIL 0**(표를 소스에서 읽으므로 이름이 바뀌어도 따라옵니다)
- `--fast` 를 **`origin/main` worktree 와 나란히** 돌려 대조 → **PASS 330 = 330 · FAIL 16 = 16(이름 목록 완전 동일)**
  — 그 16 은 이 컨테이너에 `cloudflare-deploy/node_modules` 가 없어 나는 **환경 사유**이고 tsc 도 같은 이유로 못 돌았습니다
- D1 은 `SELECT` 로 전후를 각각 셌습니다(위)

### 9-6. 남은 것

- **SIU BASIC 012(`What If`)** — 서버에 없습니다. 정본 파일이 그 제목을 알려 주므로 Mai 에게 이름까지 짚어 줄 수 있습니다.
- 이름을 고쳤으므로 **Mai 가 옛 폴더명 그대로 다시 올리면 같은 책이 두 묶음**이 됩니다.
- 3,000문장을 웜업에 붙일지 — **사람이 정할 일**(9-2).
