# 📞 야간 동기화가 학생 전화번호를 덮어쓰던 것 (2026-09-15)

> 사장님 지적(화면 캡처): 「`ON CONFLICT ... DO UPDATE` 가 카페24에 있는 모든 학생의 user_id 에
> 대해 무조건 돌아가서, 파일럿테스트 학생들 번호를 넣어도 그날 밤 빈 값으로 되돌아간다.
> 그리고 안전한 통로(`setOverridePhones`)가 수강신청 등록·수정 두 곳뿐이다.」 → **「이거 처리해줘」**

---

## 1. 지적이 맞았습니다 — 코드로 확인

`cloudflare-deploy/src/cafe24-sync.ts` 의 `importCafe24Students()` 가 이랬습니다.

```sql
ON CONFLICT(user_id) DO UPDATE SET
  ..., parent_phone = excluded.parent_phone,
  student_phone = excluded.student_phone, phone = excluded.phone, ...
```

`excluded.*` 는 **카페24가 방금 보낸 값**입니다. 그런데 카페24 원본의 번호 칸은 비어 있습니다
(2026-09-10 실측: `students_erp` 29,485행의 번호 칸 네 개가 **전부 0건**).
⟹ 매일 밤 03:00 KST 에 우리가 넣은 번호가 **빈 값으로 되돌아갔습니다.**
에러가 한 줄도 안 나고 「그냥 사라진」 것처럼만 보입니다.

### 🔴 그런데 지적보다 한 겹 더 깊었습니다 — DELETE 가 먼저 돕니다

UPSERT 「위」에 DELETE 가 있습니다.

```sql
DELETE FROM students_erp WHERE created_at = CAFE24_STUDENT_SENTINEL
  AND (password_hash ...) AND (parent_user_id ...) AND (eval_band ...)
  AND (level ...) AND (textbook ...)
```

보존 목록에 **번호가 없었습니다.** 그래서 카페24 학생 행은 먼저 지워지고 새로 들어옵니다
— 그러면 UPSERT 는 `ON CONFLICT` 가 아니라 **«새 INSERT»** 가 됩니다.

> ⟹ **SET 절만 고치면 카페24 학생 29,462명 전원에게 효과가 0입니다.**
> 두 곳이 짝입니다. 이 파일이 이미 「순서가 전부다」라고 적어 둔 그 자리입니다.

---

## 2. 고친 것 — 세 줄기

| # | 무엇 | 왜 |
|---|---|---|
| ① | UPSERT 를 `COALESCE(NULLIF(TRIM(excluded.x), ''), students_erp.x)` 로 | 카페24가 값을 주면 갱신(정본 원칙 유지), **빈 값이면 우리 값을 지킴** |
| ② | DELETE 보존 조건에 `parent_phone`·`student_phone`·`phone` **세 줄** | ①이 발동할 수 있게 하는 짝. 없으면 효과 0 |
| ③ | 그 세 칸에 **멱등 ALTER**(DELETE 보다 «앞») | 칸이 없는 DB 에서 DELETE 가 `no such column` 으로 죽고 `nightlyCafe24Refresh` 가 삼켜 **동기화가 통째로 조용히 멈춤** |

`src/index.ts`·`public/index.html` 등 **공동 금지구역은 한 줄도 안 건드렸습니다.**

### ⛔ 버린 길

- **override 통로만 늘리기** — 지적의 두 번째 절입니다. 그런데 `students_erp` 에 번호를 넣는
  경로가 자가가입(`api-students.ts`)·레벨테스트 체험계정(`leveltest-schedule.ts`)·AI 운영비서
  (`ai-command.ts`)까지 있어, 통로를 늘려도 **그 셋은 계속 샙니다.** 뿌리(동기화)를 막으면
  어느 경로로 들어간 번호든 살아남습니다.
  ℹ️ 지적 시점 기준 「두 곳」은 지금 **네 곳**입니다(수강신청 등록·수정 + 학생 상세 연락처 탭 2곳).
- **번호 칸을 SET 목록에서 통째로 빼기** — 나중에 카페24가 번호를 채우면 그 갱신이 영영 안 옵니다.
- **`COALESCE(students_erp.x, excluded.x)`(방향 반대)** — 한 번 값이 들어가면 카페24 갱신이
  영영 안 와서 **번호가 바뀐 학생이 옛 번호로 굳습니다.**
- **D1 을 직접 UPDATE 해서 번호 되살리기** — 개발·운영이 같은 DB(1-1). 코드로 막는 것이 먼저입니다.

---

## 3. 맞바꾼 것 — 사람이 알아야 할 두 가지

1. **DELETE 보존을 넓힌 대가** — 카페24를 떠난 학생이라도 번호가 있으면 행이 남고 `status` 가
   옛 값에 굳습니다. `status` 로 세는 명부·정산·리포트가 그 사람을 계속 «재학» 으로 셉니다.
   오늘 반경은 0건이지만 **이 수리의 목적이 «번호를 채우는 것»** 이라 채울수록 늘어납니다.
2. **COALESCE 는 «카페24가 번호를 지운 것» 과 «원래 없는 것» 을 구별하지 못합니다.**
   번호가 바뀌어 카페24에서 빈 값이 오면 **옛 번호가 남아 틀린 번호로 학부모에게 갑니다.**
   「틀린 번호가 빈칸보다 나쁘다」는 이 저장소 원칙과 정면으로 걸리는 자리입니다 —
   카페24가 번호를 채우기 시작하면 다시 볼 것.

둘 다 주석과 `CLAUDE.md` 2장에 적어 두었습니다.

---

## 4. 검증

- `phone_sync_harness` **39/0** (회귀 검사 7건 추가 — 가짜 그래프 + **진짜 SQLite** 로 두 번 동기화)
- `student_ai_onboarding_harness` **38/0** · `enroll_parent_phone_harness` **41/0**
- `--fast` 를 `origin/main` **워크트리와 나란히** 돌려 `▶`+`⚠` **이름 목록**을 `comm` 으로 대조
  → 양쪽 ▶343 · ⚠16, **새로 빨간 것 0건**(16건은 `node_modules` 없는 환경 사유)

### 변이시험 — 되돌리면 실제로 FAIL 나는가

| 변이 | 결과 |
|---|---|
| COALESCE 제거(옛 코드로) | **FAIL 3** |
| DELETE 보존 세 줄 전부 삭제 | **FAIL 3** |
| COALESCE 방향 뒤집기 | **FAIL 2**(짝 검사) |
| 보존 `parent_phone` 줄만 무력화 | **FAIL 1** |
| 보존 `student_phone` 줄만 무력화 | **FAIL 1** |
| 보존 `phone` 줄만 무력화 | **FAIL 1** |

---

## 5. 🪤 함정 대조가 잡은 것 — 같은 PR 에서 고쳤습니다

### ⓐ 보존 조건 세 줄 중 «한 줄만» 지우면 **아무도 안 잡았습니다**

처음 fixture 의 대상 학생이 세 칸을 **전부** 갖고 있어서, 한 줄만 남아도 행이 보존됐습니다
(실측: 한 줄씩 지워도 **36/0 통과**). 그런데 실제 데이터는 **칸이 하나만 찬 행이 흔합니다** —
`createTrialStudent()` 는 `phone` **하나만** 넣고, 그게 이 수리의 동기인 `lt15`·`lt16`·`lt18`
의 모양 그 자체입니다. 자가가입은 `parent_phone`+`phone`(student_phone 없음).

⟹ 번호가 **한 칸만** 찬 학생 셋(`s7`·`s8`·`s9`)을 fixture 에 넣어 세 줄이 각각 잡히게 했습니다.

> 📌 「변이 3종 전부 FAIL」은 사실이었지만, **그 3종에 «한 줄씩» 이 없었습니다.**

### ⓑ 같은 단정이 «다섯 파일» 에 남아 거짓이 됐습니다

이 저장소가 네 번 사고를 낸 「적어 둔 것 ≠ 사실」 그 형태라 전부 정정했습니다.

| 자리 | 고친 방향 |
|---|---|
| `CLAUDE.md` 2장 | 「매일 밤 덮입니다」 → 「**2026-09-15 «전» 에는** 덮였고, 지금은 ①②로 막았다」 + 위 맞바꿈 두 가지 + 「fixture 에 한 칸짜리 학생이 필요하다」 |
| `student-override.ts` (머리말·`setOverridePhones`) | ⛔ **금지는 유지**하고 «이유» 만 사실로. 그리고 **자가가입·레벨테스트·ai-command 는 이 표에 안 쓴다**(그 번호들에게는 이번 보존이 유일한 보호막)를 덧붙임 |
| `notify-contacts.ts` · `lesson-reminder.ts` | 시제 정정(「덮으므로」 → 「덮었고, 2026-09-15 에 막았지만 카페24가 값을 주면 그쪽이 이긴다」) |
| `enroll_parent_phone_harness.mjs` | 검사는 유지하고 실패 메시지의 이유만 정정 |

### ⓒ 제 주석이 «잰 것» 보다 넓게 말했습니다

「lt15·lt16·lt18 이 번호를 잃었고 **그래서** 7일간 671건을 찾고도 0건 발송」 —
671→0 은 그 세 계정 때문이 아니라 **29,485행 전부에 번호가 없어서**입니다. 문장을 끊었습니다.

### ⓓ 하니스가 «크래시» 하고 있었습니다

`student_ai_onboarding_harness` 가 이 DELETE 문을 오려 내 돌리는데 fixture 테이블에 번호 칸이
없어 **깔끔한 FAIL 이 아니라 스택트레이스**로 죽었습니다(그 아래 검사가 통째로 사라짐).
fixture 를 넓히고 `try/catch` 로 감쌌으며, DELETE 가 못 돌았으면 그 뒤 두 검사를 **`skip`** 으로
돌립니다 — 아무도 안 지워졌기 때문에 ✅ 가 나오는 것은 「확인 안 한 것」이 「문제없음」으로
섞이는 모양입니다. ALTER 위치 검사에도 세 칸을 더했습니다.

### ⓔ 옛 검사 하나가 뜻을 잃을 뻔했습니다

짝 검사를 위해 `s1` 의 번호를 덮어썼더니, 바로 위의 「다시 동기화해도 번호가 남아 있다」가
«카페24가 덮어썼다» 를 재게 되어 새 검사와 완전히 겹쳤습니다 → 짝 검사를 **`s4`** 로 옮겨
옛 검사가 원래 재던 것을 지키게 했습니다.

---

## 5-b. 📊 야간 cron 실측 (2026-09-16) — **돌았고 아무 번호도 안 잃었지만, «이 수리가 살렸다» 는 아직 증명이 아닙니다**

> 배포가 세 번 보류된 뒤(창: 화·목 14:00~23:00 KST) **창이 닫힌 뒤 첫 실행**에 나갔고,
> 그 다음 야간 동기화가 이 수리의 **첫 실제 실행**이었습니다.

### 잰 것

**배포** — run `34993152582` · 2026-09-16 01:09~01:12 KST · `hold=false`

```
Total Upload: 3577.73 KiB / gzip: 890.42 KiB
Uploaded  webrtc-unified-platform-prod (10.84 sec)
Current Version ID: a787d007-4df6-40c8-bcaf-79defc84ee65
base 200 · prod 200
```

배포된 커밋 `55b8767` 에 `957b55a` 가 ancestor 로 포함 · `cafe24-sync.ts` 안에
①COALESCE 3줄 ②DELETE 보존 **3줄** ③멱등 ALTER 3줄이 실재하고 순서도
**ALTER → DELETE → UPSERT** 입니다(⛔ 행 번호로 적지 않습니다 — 이 파일은 계속 자랍니다).

**야간 판** — `corpcard_meta['nightly:0 18 * * *:run']`

| 값 | |
|---|---|
| 시작 | 2026-09-16 **03:00:44** KST |
| `cafe24-sync` 단계 | **371초**(6분 11초) |
| 종료 | 03:08:12 KST · `last_ok` 기록됨 |

**`students_erp` 전후** — 같은 집계 쿼리를 그대로 두 번(SELECT 만)

| | 01:15 KST (배포 직전) | 03:25 KST (동기화 뒤) |
|---|---|---|
| 전체 행 | 29,500 | **29,512** |
| 센티넬 행 | 29,496 | **29,508** |
| `parent_phone` 있음 | 3 | **3** |
| `student_phone` 있음 | 4 | **4** |
| `phone` 있음 | 0 | 0 |

**번호가 있는 행 넷 — 전후 동일**

| 계정 | 이름 | 출처 | 다섯 칸(pw·parent·band·level·textbook) | 번호 |
|---|---|---|---|---|
| **`jeong`** | 정우영 | **카페24 센티넬** | **pw · level 있음** | student_phone 11자 — 그대로 |
| `yahee`·`yahee1`·`yahee2` | 정예희 | ours(비센티넬) | pw 있음 | parent+student — 그대로 |

**곁가지로 잰 것** — `student_erp_override` 에는 `jeong` 만 행이 있고 그 칸은 **`parent_phone`**(출처 `enroll`, 09-11 14:14)입니다.
⟹ `jeong` 의 `student_phone` 과 `yahee*` 셋의 번호는 **override 가 안 들고 있습니다**(= 이 보존이 유일한 보호막인 값들).

### 판단 (잰 것에서 내린 것 — 줄을 나눠 둡니다)

✅ **최악 경로는 일어나지 않았습니다.** 「멱등 ALTER 가 실패 → DELETE 가 `no such column` 으로 죽고
`nightlyCafe24Refresh` 가 삼켜 **동기화가 조용히 멈춘다**」가 이 수리의 가장 나쁜 실패였는데,
센티넬 행이 29,496 → **29,508 로 «되돌아왔습니다».** DELETE 가 그 행들을 먼저 비우고 INSERT 가
되채우는 구조라, 루프가 중간에 끊겼으면 이 숫자가 **만 단위로 줄어듭니다.**
(⛔ 「+12 늘었으니 끝까지 돌았다」로 적지 마세요 — +12 만으로는 첫 페이지 완주까지입니다.
 논증을 지고 가는 것은 «29,508 로 돌아왔다» 쪽입니다.)

✅ **아무 번호도 안 잃었습니다** — 3·4·0 이 전후 동일하고 `jeong` 의 값도 그대로입니다.

🔴 **그런데 「이 수리가 그 번호를 살렸다」는 증명이 아닙니다.** `jeong` 은 `password_hash`·`level` 이
차 있어 **옛 다섯 칸 조건에서도 이미 보존되던 행**입니다 — 옛 DELETE 도 그 행을 안 지웁니다.
⟹ 이번에 더한 «번호 세 줄» 이 **«새로» 남긴 행은 0건**입니다.
옛 코드에서 그 번호를 지웠을 것은 DELETE 가 아니라 **UPSERT 의 «무조건 덮기»** 인데,
그건 **카페24 payload 에 `jeong` 이 들어 있을 때만**입니다. 그리고 그 소속은 **못 쟀습니다** —
`username`·`korean_name` 은 동기화 «직후» `applyStudentErpOverrides` 가 되입히고(그래서 '정우영'),
`updated_at` 은 29,512행이 **전부 같은 상수**라 «오늘 갱신됐나» 를 가릴 칸이 없습니다.

✅ **증명하려면 어떤 행이 필요한가** — **번호가 있으면서 다섯 칸이 전부 빈 센티넬 행**이면
DELETE 보존이 증명되고, 그 행이 payload 에도 있으면 COALESCE 까지 증명됩니다.
지금 그런 행이 0건이라 **그런 행이 처음 생기는 날이 이 세 줄의 첫 시험**입니다.

⚠️ `yahee*` 셋은 **이 DELETE·UPSERT 가** 안 건드리는 대조군입니다(비센티넬 + 카페24에 없는 계정).
⛔ 「어느 코드에서도 안 건드린다」로 넓히지 마세요 — `applyStudentErpOverrides` 는 override 행이
있는 학생을 건드립니다(그 셋은 override 행이 없어 지금은 해당 없음).

⚠️ **곁가지 대안 경로 하나** — `GET /api/admin/students/import-cafe24?offset=N` 은 `offset` 을 사람이
정하므로 `offset>0` 으로 부르면 DELETE 없이 INSERT 만 돕니다(이론상 「행은 늘었는데 DELETE 는 안 돈」
상태). 03:00:44 야간 판 기록이 있어 cron 으로 보는 것이 압도적이지만, 「행 수가 움직였다」를
근거로 쓸 때는 이 경로를 한 번 떠올리세요.

---

## 6. ❌ 못 한 것 (**«문제없음» 과 섞지 마세요**)

- **타입체크(tsc)** — 이 컨테이너는 Node 22 에 `cloudflare-deploy/node_modules` 가 없습니다
  (`.nvmrc` 는 24 · CLAUDE.md 2장의 알려진 제약). **CI 게이트 ①이 봐야 합니다.**
  ℹ️ 다만 `phone_sync_harness` 가 이 파일을 타입 제거로 **실제 import 해 실행**하므로 파싱·런타임은 통과합니다.
- ✅ **운영 D1 실측 — 2026-09-16 에 했습니다**(§5-b). 그전에는 프록시가 `mangoi.ai` 를 막아
  못 쟀는데, Cloudflare D1 MCP(`d1_database_query`)로 **SELECT 만** 직접 물어 쟀습니다.
- ✅ **Cloudflare D1 에서의 실제 실행 — 2026-09-16 03:00 cron 으로 «돌았습니다»**(§5-b).
  ⚠️ 그래도 **`out.students` 값 자체는 못 봤습니다** — `console.log` 로만 남고
  `head_sampling_rate = 0.05` 라 Workers 로그에는 20번 중 19번 안 남습니다.
  근거는 그 값이 아니라 **D1 에 실제로 남은 행**입니다(그쪽이 더 셉니다).
- 🔴 **«이 수리가 번호를 살렸다» 는 아직 못 쟀습니다**(§5-b 판단) — 번호가 든 유일한 센티넬 행
  `jeong` 이 옛 코드에서도 보존되던 모양이라, 지금 D1 에는 이 세 줄을 시험하는 행이 **0건**입니다.
- ❌ **`public/js/adm-core.js` 의 같은 문장**(「카페24 동기화가 매일 밤 덮어써서 하룻밤이면 사라진다」)
  — 이제 거짓이지만 **안 고쳤습니다.** 주석 한 줄을 고치려면 `asset_version_harness` 때문에
  `admin.html` 의 `?v=` 를 올려야 하고, 그러면 관리자 전원이 그 큰 파일을 다시 받습니다.
  ⚠️ 그 줄의 **조언 자체는 여전히 맞습니다**(override 에도 적어야 한다) — 틀린 것은 «이유» 뿐입니다.
  ⟹ `adm-core.js` 를 다른 일로 만질 때 함께 고치세요(**사람이 정할 일**).
- ❌ **카페24 payload 에 `jeong` 이 있는지** — 가릴 칸이 없습니다(이름은 override 가 되입히고
  `updated_at` 은 전 행이 같은 상수). 그것이 COALESCE 쪽을 증명하는 마지막 한 조각입니다.
- ❌ **번호가 «바뀐» 학생** — 카페24가 빈 값을 주는 동안은 옛 번호가 남는다는 맞바꿈(§3)이
  실제로 어떻게 보이는지는 못 쟀습니다. 카페24에 번호가 0건이라 그 경우가 아직 없습니다.

---

## 7. 📌 사람이 정할 일로 남긴 것

- 카페24가 번호를 채우기 시작하면 위 **맞바꿈 두 가지**를 다시 볼 것.
- 자가가입·레벨테스트·AI 운영비서 세 경로를 `student_erp_override` 에도 쓰게 할지
  (지금은 이번 보존이 유일한 보호막입니다).
- 관리자 연락처 폼이 **override 번호를 보여주지 않는** 것 — 그것이 먼저 고쳐져야
  「빈 칸 = 지우기」를 열 수 있습니다(2026-09-15 앞선 작업기록의 미결과 같은 자리).
