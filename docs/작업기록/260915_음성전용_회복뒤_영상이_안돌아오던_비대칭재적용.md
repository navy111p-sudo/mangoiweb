# 음성전용(AAO) 에서 회선이 회복돼도 영상이 안 돌아오던 것 — «끄기» 만 다시 걸고 «켜기» 는 한 번뿐이었다

- 날짜: 2026-09-15
- 브랜치: `claude/festive-bell-brttqe`
- 고친 파일: `cloudflare-deploy/public/js/idx-vc-qlog.js`(defer) · `cloudflare-deploy/public/index.html`(`?v=` 한 글자) · `test-harness/aao_freeze_harness.mjs`

---

## 1. 왜 했나

제보 넷을 받았습니다.

1. 수업이 2~3분 주기로 끊긴다
2. 회선이 약하면 «음성만 전송» 이 되고 영상이 검은 화면이 된다
3. **회복된 뒤에도 영상이 자동으로 돌아오지 않는다**
4. 원인이 교사 회선인지·학생 회선인지·서버 판정인지 로그로 가릴 수 없다

먼저 **①·②가 이미 고쳐져 있다**는 것을 확인했습니다 — 그래서 다시 고치지 않았습니다.

| 제보 | 상태 | 근거 |
|---|---|---|
| ① 2~3분 끊김 | **이미 수리됨**(2026-09-15, `55b8767`) | `livenessVerdict()` 순수함수 + `LIVENESS_NOPING_STALE_MS` 600초 유예. `vc_ghost_liveness_harness` 57종 |
| ② 검은 화면 | **이미 수리됨**(2026-09-10) | `track.enabled=false`(검정) → `encodings[0].active=false`(마지막 장면 멈춤). `aao_freeze_harness` |
| ③ **회복 뒤 영상 안 돌아옴** | **이번에 고침** | 아래 |
| ④ 진단 불가 | 일부 보완 | 아래 |

---

## 2. 찾은 원인 — 재적용이 «한쪽» 만 있었다

`js/idx-vc-qlog.js` 의 4초 타이머가 이랬습니다.

```js
function vcAaoTick() {
    var A = window.__vcAAO;
    if (A && A.active) { try { vcAAOVideo(0); } catch (_) {} }   // «끄기» 만 다시 건다
```

- **끄기**: AAO 중이면 4초마다 다시 걸림 → 튼튼함
- **켜기**: `vcAAOApply()`(`idx-main.js`)의 복구 갈래에서 **딱 한 번** `vcAAOVideo(1)`

그런데 그 한 번의 `setParameters` 는 `.catch(function () {})` 로 **거절을 통째로 삼켰습니다.**
그리고 `active=true` 로 되돌리는 코드는 저장소에 `vcAAOVideo` **한 곳뿐**이고 그것을 부르는 곳도 한 곳뿐입니다.

⟹ **그 한 번이 거절되면 되살릴 사람이 아무도 없습니다.** 소리는 멀쩡하고 영상만 마지막 장면에
멈춘 채 수업이 끝나며, **에러도 화면 표시도 로그도 남지 않습니다.**

### 왜 하필 그 한 번이 거절되나 — [추론, 측정 아님]

`getParameters()` 는 스냅샷이고 `setParameters()` 는 그 스냅샷이 최신일 때만 받습니다.
그런데 같은 sender 에 `getParameters→setParameters` 를 거는 곳이 여럿입니다.

| 부르는 곳 | 파일 | 주기 |
|---|---|---|
| `applyStep`(화질 단계) | `idx-main.js` | **4초** |
| `vcAAOVideo` | `idx-vc-qlog.js` | **4초** |
| `vcBg`(가상배경)·저전력 | `idx-main.js` | 이벤트 |

`applyStep` 의 화질 «회복»(`__qGood >= 8`)과 AAO 의 «복구»(`good >= 8`)는 **둘 다 «약 32초 조용함»**
이 조건이라, **같은 틱에 함께 발화할 개연성이 높습니다.**

⚠️ **이 마지막 한 단계는 코드를 따라간 추론이고 라이브에서 잰 것이 아닙니다.**
확정하려면 이번에 넣은 `[vc-aao] setParameters 거절` 로그를 봐야 합니다.
⚠️ 다만 **원인이 무엇이든 «한 번뿐인 켜기» 가 되돌릴 길 없는 구조**라는 것은 추론이 아닙니다.

---

## 3. 고친 것

전부 `js/idx-vc-qlog.js`(**defer**)입니다. `idx-main.js` 는 **한 줄도 안 건드렸습니다**
(blocking 이고 첫 화면 예산 여유가 8바이트라 주석도 못 넣습니다).

### ① 4초 타이머가 «양방향» 을 다시 건다 — 이번 수리의 핵심

```js
var want = (A && A.active) ? 0 : 1;
if (!(want && vcAaoSfuCut())) { try { vcAAOVideo(want); } catch (_) {} }
```

`vcAAOVideo` 가 **자기 주석에 이미 적어 둔 설계**(«상태를 지정» 하는 방식)를 타이머가 안 따르고
있던 것입니다. 이제 한 번 거절돼도 **4초 뒤 스스로 되살아납니다.**
평소에는 `cur === want` 조기반환이라 **아무 일도 하지 않습니다**(불필요한 `setParameters` 0회).

### ② SFU 가 mesh 를 끊어 둔 동안에는 «켜지» 않는다

`idx-vc-sfu.js` 의 `cutMeshVideo()` 가 **같은 `vcPeerConnections`** 의 영상 sender 에
`encodings[].active=false` 를 겁니다. 대칭 재적용을 그냥 넣으면 **SFU 가 일부러 끈 것을 4초마다
되살려 영상이 두 갈래(mesh+SFU)로 나갑니다.** `vcAaoSfuCut()` 가드로 막았습니다.

### ③ `setParameters` 거절을 더는 삼키지 않는다

`[vc-aao] setParameters 거절 — uid … 켜기/끄기 …` 로 남깁니다.
②의 원인 추론을 **다음 사람이 사실로 확정할 수 있는 유일한 근거**입니다.

### ④ 「켰다」와 「나간다」를 가른다 — `vcAaoVerify()`

`active=true` 로 되돌린 것을 **복구 성공으로 판단하지 않습니다.** 되살린 뒤 30초 동안
`outbound-rtp.framesSent` 가 실제로 느는지 보고, 3틱(≈12초) 동안 안 늘면 말합니다.

```
[vc-aao] 영상을 되살린 뒤 12초 동안 프레임이 안 나갑니다 — uid … · framesSent …
```

---

## 4. 검토했다가 버린 방법

| 안 | 왜 버렸나 |
|---|---|
| **`vcAAOApply`(idx-main.js)에서 복구를 여러 번 재시도** | `idx-main.js` 는 blocking 이고 첫 화면 예산 여유가 **8바이트**입니다. 한 줄도 못 넣습니다 |
| **프레임이 안 오면 `restartIce()`·재협상** | 이 자리는 «회선이 방금 나빴던» 곳입니다. 저장소가 이미 「대역폭이 나쁜 상황에서 연결을 다시 맺는 것은 최악」이라고 못 박아 뒀습니다. **되살리는 일은 ①이 하고, ④는 «말하는» 일만** 합니다 |
| **`vcAAOVideo` 안에 SFU 가드를 넣기** | 그러면 `vcAAOApply` 의 «한 번뿐인» 직접 호출 동작까지 바뀝니다. 그건 **기존 동작**이라 이번 범위 밖입니다. 새로 «되풀이» 하는 쪽(타이머)만 막았습니다 |
| **재적용 주기를 더 짧게** | 4초면 충분하고, `setParameters` 를 자주 부르면 인코더가 흔들립니다(그 파일 주석의 경고) |
| **`?v=` 를 안 올리기** | immutable 캐시라 최대 1년 옛 파일이 남습니다. 고쳐도 **아무에게도 안 갑니다** |

---

## 5. 확인한 방법

### 변이시험 — **실제 소스를 되돌려** 빨간불이 나는지

| 변이 | 결과 | 잡은 검사 |
|---|---|---|
| «켜기» 재적용을 옛 한쪽짜리로 되돌리기 | **FAIL 4, exit=1** | **C-21** |
| SFU meshCut 가드 빼기 | **FAIL 3, exit=1** | **C-22** |
| 프레임 확인 통째로 빼기 | **FAIL 2, exit=1** | **C-23** |
| 사람이 끈 카메라 가드 빼기 | **FAIL 1, exit=1** | **C-25** |
| 거절을 도로 삼키기(`.catch(function(){})`) | **FAIL 2, exit=1** | **C-26** |
| 평상시 틱 «바뀔 때만» 가드 빼기 | **FAIL 2, exit=1** | **C-27** |
| (원상복구) | **PASS 89 / FAIL 0, exit=0** | — |

`aao_freeze_harness` 는 **가짜 DOM 에서 그 절을 실제로 돌립니다**(문자열 검사가 아닙니다).

### 짝 검사 — 한쪽만 두면 반대 사고가 통과합니다

- C-21 «복구 뒤 다시 켠다» ↔ **C-17 «AAO 중에는 끈다»**
- C-22 «SFU 중엔 안 켠다» ↔ **C-18 «AAO 가 아니면 안 건드린다»**
- C-23 «프레임이 안 늘면 말한다» ↔ **C-24 «늘면 말하지 않는다»** · **C-25 «카메라 끈 사람에겐 말하지 않는다»**
- C-27 «평상시엔 없는 클래스를 안 지운다» ↔ **C-29 «붙어 있으면 제대로 지운다»**(가드가 «영영 안 지움» 이 되면 안 됩니다)

### 함정 대조(trap-check)가 잡아 **같은 작업 안에서 고친 것 둘**

🔴 **둘 다 제 변경이 만든 것입니다.**

1. **고침 ③(거절을 안 삼킴)에 검사가 없었습니다.** 옛 `.catch(function(){})` 로 되돌려도
   **87/0 으로 통과**했습니다 — CLAUDE.md 에 「⛔ 실패를 삼키지 마세요 — 그 거절 한 줄이
   원인을 사후에 가릴 유일한 근거」라고 **제가 규칙으로 못 박아 놓고** 지키는 검사가 없었습니다.
   → **C-26** 신설(가짜 sender 가 실제로 거절하게 만들어 «말하는가» 확인).
   ⚠️ 처음 만든 C-26 은 `active:false` 인 sender 에 «끄기» 를 걸어 **`cur === want` 조기반환으로
   setParameters 가 아예 안 불렸습니다** — 검사가 헛돈 것을 실행해 보고 알았습니다.
2. **`vcAaoSelfMark(!want)` 가 평상시에도 4초마다 돌게 됐습니다.** 「끄기」 갈래가 조건 없이
   `classList.remove('vc-aao-on')`(없는 토큰)을 불러, **`documentElement` 에 `subtree:true` 로 걸린
   `mango-worldclock.js` 관찰자를 수업 중 4초마다 깨웁니다.** 무한루프는 아니지만(그 콜백은
   `toggle(t, force)` 라 상태가 같으면 0회) **홈이 두 번 멎은 뿌리가 이 계열**이라 가드를 넣었습니다.
   → **C-27**(안 지운다) + **C-29**(붙어 있으면 지운다) 짝.

### 회귀

| 검사 | 결과 |
|---|---|
| `aao_freeze_harness` | 74 → 89 → **92종**(2026-09-16 프라이버시 짝 C-25b·C-25c 추가), FAIL 0 |
| `vc_ghost_liveness_harness`(2~3분 끊김 수리) | **57 통과 / 0 실패** — 커밋 당시와 같음 |
| `first_paint_budget_harness` | **✅ 유지** (blocking 1575KB 그대로 — defer 만 커짐) |
| `asset_version_harness` | 226건 ✅ |
| `run.mjs --fast` **기준선 대조** | ▶ 346 · ✅ **346** · ⚠ **0** → **새로 생긴 FAIL 0건** |

🔴 **정정(2026-09-16) — 여기 적혀 있던 세 문장이 그날 환경 때문이었고 지금은 사실이 아닙니다.**
원문은 「⚠ 16건은 기준선에서도 빨간불(`node_modules` 없음)」·「타입체크는 못 돌렸다」였습니다.
2026-09-16 에 `npm ci --legacy-peer-deps` 로 `node_modules` 를 깔아 **환경 사유 실패가 0 이 됐고**,
그 상태에서 다시 쟀습니다 — **내 브랜치 346/0 · 기준선(`origin/main`) 346/0 으로 동일**,
하니스 «목록» 도 `comm` 으로 대조해 양쪽 346개가 **완전히 같습니다**(사라지거나 늘어난 검사 0건).
**타입체크도 실제로 돌려 `tsc --noEmit` exit 0** 입니다(그날의 「`.ts` 를 안 건드렸다」는 그대로 참).
⛔ 그러니 이 저장소에서 「환경 때문에 못 돈다」를 **기본값으로 적지 마세요** — 깔면 돕니다.

---

## 6. 아직 남은 것 / 위험

- 🔴 **원인의 마지막 한 단계는 여전히 추론입니다.** 배포 뒤 Workers 로그에서
  `[vc-aao] setParameters 거절` 이 실제로 찍히는지 봐야 «경합» 이 사실이 됩니다.
  ⚠️ `head_sampling_rate = 0.05` 라 **20건 중 19건은 로그가 안 남습니다.**
- 🔴 **정정(2026-09-16) — 「브라우저에서 «실제로 얼굴이 돌아오는지» 는 못 쟀다」는 이제 사실이 아닙니다.**
  배포된 `mangoi.ai` 는 여전히 프록시가 막지만, **그럴 필요가 없었습니다** — 크로미움 안에서
  진짜 `RTCPeerConnection` 둘을 로컬로 붙이면 «회선» 없이도 이 수리의 전부를 잴 수 있습니다.
  `test-harness/manual/vc-aao-realpc-browser.mjs` 를 그렇게 만들었고 아래 8장에 실측이 있습니다.
  ⚠️ 남은 것은 «진짜 회선 붕괴의 재현» 뿐인데, 이 수리가 겨냥한 것은 회선이 아니라
  **`setParameters` 가 거절될 때의 재적용**이라 그 자리는 코드로 정확히 주입해 쟀습니다.
- ⚠️ `vcAAOApply` 의 **직접 호출**(`vcAAOVideo(1)`)은 SFU meshCut 중에도 여전히 켭니다 —
  **고치기 전부터 그랬고** 이번에 «되풀이» 만 막았습니다. 바꾸려면 별건입니다.
- ⚠️ ④의 경보는 **콘솔 로그뿐**입니다. 화면·D1 에는 안 남습니다(사람이 보러 가야 보입니다).

---

## 7. 실제 수업에서 확인할 것

1. 회선이 나빠져 「음성만 전송」 안내가 뜬 뒤, **회복되면 영상이 실제로 돌아오는가**
2. 강사 콘솔에 `[vc-aao] setParameters 거절` 이 찍히는가 → 찍히면 **경합 추론이 사실**
3. `[vc-aao] 영상을 되살린 뒤 12초 동안 프레임이 안 나갑니다` 가 찍히는가
   → 찍히면 `active` 말고 **다른 것**(인코더·트랙)이 막고 있다는 뜻
4. 카메라를 끈 학생에게 3번이 **안** 찍히는가(거짓 경보 확인)

---

## 8. 최신 `main` 병합과 «진짜 PeerConnection» 통합 검사 (2026-09-16)

### 8-1. 병합 — rebase 가 아니라 merge

브랜치가 `main` 보다 **4 커밋 뒤**(merge-base `96f678d`)여서 `git merge origin/main` 했습니다.
⛔ **rebase 를 쓰지 않은 이유**는 둘입니다 — 이 브랜치는 **이미 `origin` 에 올라가 있어**
rebase 면 force-push 가 되고, 저장소 관례 자체가 「main 을 브랜치에 병합한 커밋을 push」입니다
(CLAUDE.md 2장에 그 표현이 여러 번 나옵니다).
⚠️ **정정** — 처음에 이 자리를 「CLAUDE.md 가 force-push 를 금지한다」고 적었는데 **그런 줄은 없습니다**
(`grep -E 'force-push|rebase' CLAUDE.md` = 0건). 근거는 «저장소 관례» 와 이 세션의 PR 규칙입니다.
⛔ 없는 규칙을 근거로 적으면 다음 사람이 그것을 찾으러 갑니다.

**충돌 0건.** 겹친 파일은 `CLAUDE.md`·`docs/INDEX.md` 둘뿐이고 자동 병합됐습니다.
⚠️ 그래도 **«자동 병합됐다» 를 «뜻이 맞다» 로 읽지 않았습니다**(CLAUDE.md 의 그 함정) —

| 확인 | 결과 |
|---|---|
| `git diff --numstat origin/main -- CLAUDE.md` | **1 / 0** — 내 행 한 줄 추가뿐 |
| `git diff --numstat origin/main -- docs/INDEX.md` | **1 / 0** — 내 목차 한 줄 추가뿐 |
| `INDEX.md` 같은 문서가 두 번 실렸는가 | 중복 4건인데 **`origin/main` 에도 그대로 4건** → 병합이 만든 것이 아님(별건) |
| 내 코드 파일이 병합으로 바뀌었는가 | `git diff c53acab HEAD -- <내 파일 4개>` **비어 있음** |
| 2~3분 끊김(liveness) 수리 생존 | `livenessVerdict`·`autoSeenOf`·`LIVENESS_NOPING_STALE_MS=600초` **그대로**, 하니스 57/0 |

### 8-2. 왜 «진짜 RTCPeerConnection» 이 따로 필요했나

`aao_freeze_harness` 는 가짜 sender 로 잽니다 — `setParameters` 가 객체를 바꿔치기만 하므로
**«active 를 되돌렸다» 까지가 전부**이고, 정작 이 수리의 목적인 **«프레임이 다시 흐른다»** 는
한 번도 재지 않습니다. 그래서 크로미움 안에서 진짜 `RTCPeerConnection` 둘을 로컬로 붙이고
받는 쪽 `framesDecoded` 와 **화면 픽셀**로 쟀습니다(`manual/vc-aao-realpc-browser.mjs`, 29종).

**[잰 것 — 2026-09-16, 크로미움 1194 · 가짜 카메라 320×240@15]**

| 단계 | 잰 것 |
|---|---|
| ① 정상 | 두 PC `connected` · `framesDecoded` 증가 · 받는 화면 luma 55 (검지 않음) |
| ② 평상시 4초 틱 6회 | 진짜 sender 의 `setParameters` **0회** · 자기 타일 속성 쓰기 **0회** · 그 사이 영상은 계속 흐름 |
| ③ 음성전용 | `encodings[0].active=false` · 받는 프레임 **증가 0** · 트랙은 `live`·`enabled` 그대로 · **소리(`packetsReceived`)는 계속 늚** |
| ④ 회복 | 틱 한 번에 `active=true` → **프레임이 실제로 다시 늚** · 화면 다시 그려짐 (새로고침 없음) |
| ⑤ 거절 주입 | 첫 «켜기» 를 거절시키면 꺼진 채 남고 **`[vc-aao] setParameters 거절` 이 콘솔에 찍힘** → **다음 틱이 다시 켜서 영상 복귀** |
| ⑥ 프라이버시 | 사람이 카메라를 끄면 luma **< 6**(검정) · 회복 틱 3회 뒤에도 **트랙 `enabled=false` 유지 · 여전히 검정** · 그래도 `active=true`(짝) · 카메라를 다시 켜면 **바로 나감**(짝) |
| ⑦ 두 «탭» | 받는 쪽을 **다른 탭(다른 렌더러)** 으로 옮겨도 같음 — 흐름 → 멈춤 → **회복(새로고침 없이)** |

### 8-3. 변이시험 — 이 검사가 헛돌지 않는다

⚠️ **진짜 소스에 넣어 돌렸습니다**(사본을 `cp` 로 떠 두고 되돌림 — `git` 은 «방금 쓴 것» 을 모릅니다).

| 변이 | 결과 |
|---|---|
| Ⓐ 4초 틱을 옛 «한쪽짜리 재적용» 으로 되돌리기 | **FAIL 9건** — D-2 가 **«증가 0»** 으로 **실사고 증상 그대로**(다른 탭 G-4 도 함께) |
| Ⓑ `p.encodings[0].active = want;` 뒤에 `if (want) s.track.enabled = true;` | **FAIL 2건** — F-3 의 화면이 «검정»(luma<6)에서 «밝음»(>8)으로 뒤집힘 → 끈 카메라의 얼굴이 실제로 나감 |
| Ⓒ «바뀔 때만» 가드 둘(인코더·DOM) 빼기 | **FAIL 2건** — B-1·B-2 |
| Ⓓ 오디오 인코딩까지 «함께» 끄기(= 수업이 통째로 무음) | **FAIL 1건** — C-4. ⚠️ 이 짝을 넣기 «전» 에는 **27/0 으로 무방비**였습니다 |

되돌린 뒤 **29/0 · 92/0 기준 점수로 복귀**하는 것까지 확인했습니다.

⛔ **프레임 수·luma 의 «절대값» 을 문서에 적지 마세요** — 가짜 카메라가 «움직이는 무늬» 라 회차마다
달라집니다(같은 변이가 luma 49~55, D-2 가 `42→42`·`43→43`·`60→60`). 뜻은 «증가 0»·«검정→밝음» 입니다.
처음에 절대값을 적었다가 함정 대조가 «다른 변이의 값이 섞였다» 로 잡아 주었습니다.

⚠️ 변이는 **저장소 파일을 고치지 않고** 사본을 `QLOG_SRC` 로 가리켜 돌렸습니다 —
`git status` 가 내내 깨끗했고, `public/` 안에 백업이 남지 않습니다(`deploy.ps1` 이 그 폴더를 통째로 올립니다).

### 8-4. 이번에 «검사가 없던 자리» 를 하나 메웠습니다

프라이버시는 그때까지 **추론**이었습니다 — 「사람이 끈 카메라는 «트랙»(`track.enabled=false`)이고
AAO 는 «인코딩»(`encodings.active`)이라 축이 다르니 안전하다」. 맞는 말이지만 **그것을 지키는
검사가 한 줄도 없어서**, `s.track.enabled = true` 한 줄을 더하는 «영상을 확실히 되살리자» 식
변이가 그대로 통과했습니다. 그래서 짝으로 못 박았습니다 —

- `aao_freeze_harness` **C-25b**(트랙을 다시 켜지 않는다) + **C-25c**(짝: 그래도 인코딩은 되살린다)
- `manual/vc-aao-realpc-browser.mjs` **F-2·F-3**(픽셀) + **F-4·F-5**(짝)

🔴 **그리고 함정 대조가 «영상만 보고 소리를 안 보는» 더 큰 구멍을 하나 더 찾았습니다.**
기능 이름이 «음성전용» 인데, 오디오 인코딩까지 함께 끄는 한 줄을 넣어도 **두 하니스가 전부 통과**했습니다
(브라우저 27/0 · `aao_freeze` 92/0). 실서비스에 들어가면 **음성전용 구간에서 수업이 통째로 무음**이 되는데
「영상이 멈춘다」·「다시 나간다」·「프라이버시」는 전부 초록이라 아무도 못 봅니다.
✅ 받는 쪽 `inbound-rtp(kind:'audio')` 의 `packetsReceived` 로 **A-6(전제) + C-4(짝)** 를 넣었고,
그 변이가 이제 **실제로 C-4 에서 FAIL** 합니다.

⛔ **짝을 지우지 마세요** — C-25b 만 두면 «영영 안 켜기» 도 통과하고, 그것은 카메라를 다시 켠
학생의 영상이 안 나가는 반대 사고입니다.

### 8-5. 그래도 사람이 확인해야 하는 것

- 🔴 **실제 수업에서의 회선 붕괴는 여전히 재현하지 못했습니다.** 위 ③은 «AAO 판정이 내려진 뒤» 를
  잰 것이지 «회선이 실제로 나빠져 그 판정이 내려지는 과정» 이 아닙니다(그쪽은 `idx-main.js` 의
  적응 루프 담당이고 이 수리의 범위 밖).
- 🔴 7장의 네 가지(Workers 로그에 `setParameters 거절` 이 실제로 찍히는가 등)는 **그대로 남습니다.**
- ⚠️ 이 브라우저 검사는 **`manual/` 이라 게이트가 물어 가지 않습니다** — 사람이 부릅니다.

---

## 9. 최신 `main` 재병합 — 같은 함수를 둘이 고쳤다 (2026-09-16)

작업 중 `main` 이 3커밋 더 나갔고 그중 **`4c9711980` 이 같은 `vcAaoTick` 을 고쳤습니다**
(「음성전용에서 마지막 얼굴 유지」 — 같은 4초 틱에 «한 장 떠 두기» 를 얹음).
⟹ 이번에는 **충돌 4건**이 났고, 전부 «뜻» 을 보고 풀었습니다.

| 파일 | 어떻게 풀었나 |
|---|---|
| `js/idx-vc-qlog.js` | **둘 다 유지.** 내 «양방향 재적용 + 프레임 확인» 은 그대로 두고, main 의 «마지막 얼굴 스냅» 블록을 그 뒤에 붙였습니다. ⛔ main 쪽 옛 한 줄(`if (A && A.active) vcAAOVideo(0)`)만 버렸습니다 — 내 `want` 로직이 그 일을 **포함**합니다(둘 다 두면 같은 일을 두 번 합니다). |
| `test-harness/aao_freeze_harness.mjs` | **둘 다 유지.** 다만 main 의 변이 「4초 타이머의 «AAO 일 때만» 가드를 빼기」는 **앵커(`if (A && A.active) {`)가 사라져** 그대로 두면 「변이가 소스에 안 걸렸다」로 FAIL 합니다 → 같은 뜻인 내 변이가 대신하므로 버렸습니다. 「N초 전」 변이는 main 이 그 구간을 재구성해 **main 쪽 형태만** 소스에 맞아 그쪽을 채택했습니다. |
| `CLAUDE.md` | 「검은 화면」 행을 **main 이 5160 → 7288자로 확장**했고 나는 그 행을 **한 글자도 안 건드렸습니다**(merge-base 와 동일함을 코드로 확인) → **갱신판을 쓰고 내 새 행만 덧붙였습니다.** 표 행 438 → 439. |
| `test-harness/asset-versions.json` | 🔴 **진짜 충돌** — 양쪽이 «같은 `?v=14`» 에 **서로 다른 내용**을 올렸습니다(해시 셋이 전부 다름). main 의 `v=14` 는 **이미 main 에 있어 배포 대상**이므로 그것을 남기고, 병합본은 **`v=15` 로 올렸습니다.** ⛔ `v=14` 로 두면 그 주소를 이미 받은 사람에게 immutable 캐시가 **1년간 틀린 파일**을 줍니다. |

### 9-1. 이번에 메운 구멍 — 자동 게이트에도 「소리는 그대로」

8장에서 브라우저 검사에만 넣었던 오디오 짝을, 이번에 **게이트가 실제로 돌리는**
`aao_freeze_harness` 에도 넣었습니다(**B-4b·B-4c**). 넣기 전 실측으로 오디오까지 끄는 변이가
**92/0 으로 통과**했습니다 — `manual/` 은 게이트가 물어 가지 않으므로 그 구멍은 그대로였습니다.
⛔ `makePc` 의 오디오 sender 를 «매번 새 객체» 로 되돌리지 마세요 — 그 순간 «건드렸는가» 를
셀 수가 없어 이 검사가 원리상 무력해집니다(그래서 «안정된 객체 + 호출 계수» 로 뒀습니다).

### 9-2. 병합 뒤 잰 것 (2026-09-16)

`tsc --noEmit` exit 0 · `--fast` **▶346 ✅346 ⚠0** ·
`aao_freeze` **126/0**(내 92 + main 검사 합쳐짐) · `vc_ghost_liveness` **57/0** ·
`body_class_observer` **9/0** · `observer_camera_guard` **139/0** ·
`vc_quality_blindspot` **232/0** · `asset_version` **226/0** ·
브라우저(진짜 RTCPeerConnection) **29/0**.
오디오 변이를 실제로 넣으면 **B-4b·B-4c 가 FAIL**(126 → 124/2), 되돌리면 126/0 복귀.
