# 웜업 3안 배포 준비 보완 — 2026-09-16

## 요청과 범위
사용자가 기존 2b65002 작업의 누락 수정·검증·배포까지 요청했다.
대상은 교재로 웜업 / 자유 대화 진입 화면이다. 교재 문장 데이터 제작이나 D1 변경은 포함하지 않는다.

## 확인과 수정
- main과 원본/작업 브랜치의 변경 파일을 3-way 대조: 자동 병합 가능. 최신 교재 목록(BASIC 012 포함)을 보존한다.
- 보고에 있었던 tap() 도우미가 푸시된 파일에는 없었다. 클릭 실패를 기록하고 나머지 검사를 계속하도록 보완했다.
- 최초 PR CI 실제 실패: subtitle_meaning_harness가 warmup style 주석에 남은 CJK 홑낫표 2자를 검출. 해당 주석 한 곳만 수정했다.
- 로컬 통합 소스의 warmup_bts_book_harness: PASS 158 / FAIL 0.
- 로컬 Chromium 다운로드가 시간 초과되어 GitHub Actions에 별도 브라우저 검증 작업을 추가했다. 기존 배포 게이트는 유지했다.
- 기존 BTS 브라우저 검사는 별도 HTTP/CDP 서버가 필요하다. 준비 작업과 결과 요약 검사를 함께 넣어 건너뜀을 성공으로 간주하지 않는다.
- 두 갈래 브라우저 정상 63개 검사, 기존 BTS 브라우저 검사, 클릭 배선 제거 변이와 원복 후 63/0 검사를 실행한다.
- 변이는 실행 전 소스를 보관하고 finally에서 원복한다. 종료 코드뿐 아니라 FAIL 요약까지 요구한다.

## 하지 않은 것
- 이전 보고의 검사 결과를 이번 실행 결과로 재사용하지 않는다.
- main에 직접 쓰거나 배포 게이트를 해제하지 않는다.
- 수업 보호를 강제로 해제하지 않는다. D1·도메인·시크릿·화상수업 코드는 변경하지 않는다.

## 최종 증거
최종 CI, 브라우저 검사, 병합 및 실제 배포 결과는 PR #1015와 연결된 Actions 로그에서 확인한다.
이 기록 작성 시점에는 최종 검사가 진행 중이며 배포 완료를 의미하지 않는다.
