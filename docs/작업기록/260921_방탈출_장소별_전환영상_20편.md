# 방탈출 — 장소마다 «그 방의» 전환 영상 20편 (2026-09-21)

## 1. 왜 했나

2026-09-21 에 「캐비닛을 열라고 했을 때 그림이 그대로예요」를 고쳐(PR #1051)
단계마다 정지 사진이 바뀌게 했습니다. 그 뒤 사장님이 **「continue with the 20 clips」**
로 전환 «영상» 20편을 승인하셨습니다(5개 장소 × 4단계).

그 전에 제가 보고드린 막는 것이 하나 있었습니다 —
**영상을 골라 트는 줄이 «장소» 를 안 봅니다.** 그래서 영상을 20편 만들어도
한 편도 안 닿는 상태였습니다. 이번 작업은 그 배선 수리와 영상 20편을 함께 합니다.

## 2. 잰 것

- `student-game-escape-voice.html` 의 그 줄이
  `s.id==='drawer'?'drawer':(s.id==='books'?'books':…)` 로 **`G.loc.id` 가 한 글자도 없었습니다.**
  ⟹ 옥상에서 공구함을 열어도 «서재 책상 서랍» 영상이 납니다. 에러는 안 납니다.
- 하니스 ④절(배선 검사)이 그것을 **원리상 못 봅니다** — 그 절이 `playActionClip` 을
  **no-op 스텁**으로 두고 있어서 «무엇을 틀었는가» 를 아무도 안 묻고 있었습니다.
- 비용: 잔액 **843.35 → 293.35 = 정확히 550 크레딧**(20편 × 27.5). 생성 실패 0건.

## 3. 한 것

### 3-1. 배선 — «장소:단계» 로 고르게
정본은 `clipKeyFor(locId, stepId)` 하나입니다.
`ACTION_CLIPS['rooftop:drawer']` 가 있으면 그것을, 없으면 예전처럼 공용 영상으로
떨어집니다(서재 `study` · `key` · `books` 단계).

- ⛔ 폴백을 없애지 마세요 — 그 셋이 그 순간 «영상 없음» 이 됩니다.
- ⚠️ 그 호출은 `try` 로 감쌌습니다 — 거기서 던지면 위에서 이미 `G.busy=true`·
  `clearHotspots()` 가 돌아 **게임이 그대로 잠깁니다**(바로 윗줄 `EscapeAudio.step`
  이 감싸인 이유와 같은 자리).

### 3-2. 영상 20편
- 모델 `flux_3_video` · 5초 · 1280×720 · 16:9 · 소리 없음.
- **그 방의 «닫힌» 사진을 시작, «열린» 사진을 끝으로 물려서 뽑았습니다.**
  🔴 **잰 것은 거기까지입니다** — 실제로 방·각도·조명이 정지 그림과 맞는지는
  **아무도 안 봤습니다**(§6). 「딱 맞는다」로 읽지 마세요.
- 문구에는 장소마다 «그 물건 이름» 을 그대로 넣었습니다
  (교실 사물함 · 화장실 청소도구함 · 창고 공구함 · 옥상 정비함 · 운동장 창고,
   그리고 지도 / 거울 / 사다리 / 배관 / 공 바구니).
- 카메라 고정(`Static locked-off camera`)은 **지시**이지 결과가 아닙니다 —
  확대·이동이 들어가면 정지 그림에서 영상으로 넘어갈 때 화면이 튑니다.

### 3-3. 검사 — 「무엇을 틀었는가」를 답으로 묻게
`escape_step_art_harness.mjs`:
- `runScene` 의 `playActionClip` 을 **no-op → 기록기**로 바꿨습니다.
- **⑤절 신설** — `ACTION_CLIPS`·`clipKeyFor` 를 **소스에서 오려 내**(⛔ 표를 하니스에
  베껴 적지 않습니다) `advance` 를 표에 실재하는 열쇠 전부로 실제로 돌려
  «무슨 영상을 틀었는가» 를 봅니다. 짝으로 「없는 짝은 공용으로 떨어진다(셋)」·
  「둘러보기 단계엔 안 튼다」, 그리고 **「같은 주소를 두 번 쓰지 않는다」**.
- 자동 32종 → **42종**.

## 4. 검토했다가 버린 것

| 버린 길 | 왜 |
|---|---|
| 영상을 우리 CDN(`/img`)으로 옮겨 담기 | 프록시가 힉스필드 CDN 을 **403** 으로 막아 제가 못 받습니다. CI 러너를 거치면 되지만 리포가 수십 MB 늘고, 지울 수 없는 임시 브랜치가 또 하나 남습니다(푸시 프록시가 브랜치 삭제를 거부). **사람이 정할 일**로 남겼습니다 — 기존 6편도 같은 방식이고, 파일이 지워져도 정지 그림이 남아 게임은 돕니다 |
| 폴백 없이 «언제나 장소:단계» | 서재·`key`·`books` 단계가 그 순간 영상 0편이 됩니다. 변이시험에서 실제로 4건 FAIL |
| 단계 이름 조건에 `G.loc.id` 를 끼워 넣어 길게 쓰기 | 장소가 늘 때마다 그 줄이 길어집니다. 표(`ACTION_CLIPS`)에 한 줄 더하면 끝나게 두는 쪽이 낫습니다 |
| 검사를 「`clipKeyFor` 가 있는가」 문자열로 | `if(false && …)` 한 글자에 뚫립니다. 실제로 돌려 «답» 으로 물어야 합니다 |

## 5. 확인한 방법

**변이 10종 — 전부 제가 직접 넣어 실제 FAIL**(되돌리면 0건). `runScene` 을 고쳤으므로
옛 6종도 «다시 재야» 하는 상태였고, 그래서 열 개를 모두 새로 쟀습니다.

| 변이 | FAIL |
|---|---|
| Ⓐ 영상 배선을 옛 «단계 이름만» 으로 되돌리기 | 1 |
| Ⓑ 영상 폴백 제거(언제나 `장소:단계`) | 4 |
| Ⓒ 옥상 4편을 표에서 빼기 | 1 |
| Ⓓ 장소를 무시(언제나 `classroom`) | 2 |
| Ⓔ `playActionClip` 호출 제거 | 4 |
| ⑤ voice advance 결과를 장소 기본 사진으로 | 3 |
| ⑥ voice showStep before 를 장소 기본 사진으로 | 1 |
| ⑦ voice 문 한 박자 제거 | 2 |
| ⑧ school advance 결과를 `s.scene` 으로 | 2 |
| ⑨ school showStep before 를 `s.scene` 으로 | 1 |
| ⑩ zombie 캐비닛 분기 제거 | 3 |

- `escape_step_art_harness` **PASS 42 / FAIL 0**.
- 회귀: `run.mjs --fast` 를 **`origin/main` 워크트리와 나란히** 돌려 FAIL «목록» 을
  `comm` 으로 대조 — 양쪽 **똑같이 16건**이고 **새로 생긴 것 0건**입니다
  (그 16건은 이 컨테이너에 `node_modules` 가 없어 나는 환경 사유).
- 타입체크는 이 변경에 `.ts` 가 없어 해당 없음(CI 가 돌립니다).
- 함정 대조(`trap-check`)를 돌려 **한글 깨짐 8곳 · 완료형 과장 3곳 · 「조용히 멈춘다」
  오기술 · 변이 근거 부족**을 잡았고 이 문서와 규칙서·목차·소스를 그대로 고쳤습니다.

## 6. 아직 안 한 것 / 사람이 볼 것

- 🔴 **영상이 «그 방·그 물건» 이 맞는지 아무도 못 봤습니다.** 이 컨테이너는 그 CDN 이
  403 이고 H.264 디코더도 없습니다. 게다가 **검사도 «어느 열쇠를 넘겼는가» 까지만**
  봅니다 — `'rooftop:drawer'` 에 교실 영상 주소를 넣어도 초록입니다(같은 주소 중복만
  한 줄로 막아 두었습니다). **사장님이 직접 보셔야 끝납니다.**
- ⚠️ **주소가 죽으면 조용하지 않습니다** — `#sceneVideo` 는 `inset:0` 에 거의 검정이고
  `playActionClip` 이 `show` 를 **먼저** 붙인 뒤 에러나 **4.8초 타이머**에서야 뗍니다
  ⟹ hang 하는 회선에서 **최대 4.8초** 검은 상자가 정지화면을 덮습니다.
  정지화면과 게임 진행 자체는 클립과 무관해 계속 돕니다.
  그 주소에는 서명·만료 쿼리가 **없어** 위험은 「만료」가 아니라 «제공자가 파일을 지우면» 입니다.
- ⚠️ **회선 부담이 늘었습니다** — `painting` 단계는 전에 영상이 없었고, 전에는 6편을
  모든 장소가 나눠 써 캐시가 재사용됐는데 이제 장소 전용이라 장소를 바꿔 가며 놀면
  재사용이 사라집니다(`preload="none"` 이라 «틀 때만» 받습니다).
  **클립 용량은 못 쟀습니다**(프록시 403).
- 🟡 외부 CDN 주소가 **6 → 26개**로 늘었습니다. 우리 서버로 옮길지는 사람이 정할 일.
- 🧹 임시 브랜치 `tmp/escape-art-2609` 는 푸시 프록시가 삭제를 거부합니다 —
  깃허브 화면에서 지워 주세요.
