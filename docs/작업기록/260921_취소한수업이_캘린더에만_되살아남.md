# 취소한 수업이 학생 상세 «캘린더» 에만 되살아나던 것 — 원인과 수리

- 날짜: 2026-09-21
- 제보: 사장님 — 「아직도 이게 왜 나타나? 이유가 뭐지??」
  (관리자 › 학생 상세 › `jeong` 정우영 › 주간 스케줄 › **금요일 14:20 「체험수업」**)
- 관련: 같은 날 오전 [출결이 전부 «0» 으로 보이던 건](260921_학생상세_jeong_출결이_전부_빈칸_진단.md)
  — **다른 건**입니다. 그쪽은 고쳐져 이 화면에 「출석 세션 108회」가 정상으로 나옵니다.

---

## 0. 🔴 이 문제는 «오늘 오전에 이미 찾아 놓고 안 고친 것» 이었습니다

같은 날 오전 작업기록
[260921_예약칸_지난수업_하루짜리표시.md](260921_예약칸_지난수업_하루짜리표시.md) **§5
「곁가지로 찾았지만 안 고친 것」** 에 이 건이 그대로 적혀 있습니다 —
「캘린더의 수강신청 레이어에 `status` 검사가 한 줄도 없다 … **실피해는 0건** … 사람이 정할 일」.

그리고 **몇 시간 뒤 사장님이 바로 그 카드를 가리키며** 물으셨습니다.

> 🔴 **「실피해 0건」은 «DB 가 안 깨진다» 는 뜻이었지 «사람에게 아무 일도 안 생긴다» 는
> 뜻이 아니었습니다.** 화면이 없는 수업을 그리고 있으면 그것을 보는 사람은 매번
> 「이게 왜 있지?」를 혼자 판단해야 합니다 — **그것이 피해입니다.**
>
> ⟹ **«데이터 피해 0건» 을 근거로 «사람이 정할 일» 로 미루지 마세요.**
> 화면이 사실과 다르게 말하고 있으면 그것부터 고치는 것이 먼저입니다.
> (데이터를 손대는 것과 화면이 사실을 말하게 하는 것은 **다른 일**이고,
>  뒤엣것은 승인이 필요 없습니다.)

---

## 1. 무엇이 문제였나 — 한 화면이 두 표를 각각 보고 서로 다른 말을 했다

학생 상세 화면에는 «수업» 을 보여 주는 자리가 **둘** 있습니다.

| 자리 | 무엇을 보나 | 취소된 수업은? |
|---|---|---|
| 위쪽 **주간·월간 캘린더** | **`enrollments`**(신청서) | **그대로 그림** ← 문제 |
| 아래쪽 **스케줄 목록** | `class_schedules`(실제 수업) | 서버가 이미 거름 |

캘린더는 신청서의 «매주 금요일 14:20» 을 보고 카드를 그립니다.
그런데 **실제 수업을 전부 취소해도 신청서는 `confirmed` 로 남습니다** — 두 표 사이에 배선이 없습니다.

`GET /api/admin/class-schedules` 는 `cs.status != 'cancelled'` 로 취소분을 **이미 거르고** 있어서,
같은 화면 아래쪽 목록에서는 사라지고 **캘린더에만 남았습니다.**

### D1 실측 (2026-09-21, SELECT 만)

```
enrollments 103   status=confirmed  type=체험수업  days_of_week=금  time=14:20  end_date=2026-10-11
  └ 그 신청이 만든 실제 수업 (source='adm-enroll:103')
      class_schedules 2332  2026-09-11 15:00  cancelled
      class_schedules 2333  2026-09-18 14:20  cancelled
      class_schedules 2334  2026-09-25 14:20  cancelled   ← 사장님이 X 치신 그 카드
      class_schedules 2335  2026-10-02 14:20  cancelled
```

**네 건 전부 취소인데 신청서만 살아 있었습니다.**

### 반경 — 전수로 셌습니다

신청 **14건 전부 `confirmed`** 이고, 그중 «수업이 만들어졌다가 전부 취소» 인 것은 **1건**
(= 바로 이 건)뿐이었습니다. 나머지 13건은 살아 있는 수업이 있습니다.

⟹ **저장소 전체에서 유일한 사례**라 반경은 작지만, 구조가 남아 있으면 언제든 재발합니다.

---

## 2. 왜 하니스가 못 잡았나

표도 값도 함수도 전부 «있습니다». 틀린 것은 **«무엇이 그려지는가»** 하나뿐이고
두 코드 다 «자기 표» 를 정직하게 그릴 뿐입니다 — 에러도 경고도 안 납니다.
수리 전에도 `--fast` 가 전부 초록이었습니다.

---

## 3. 고친 것

### ① 서버는 «사실» 만 준다 — 정본 `src/enrollment-class-count.ts`

> ⚠️ **처음엔 `api-admin.ts` 안에 인라인으로 넣었습니다.** 그 뒤 사장님이 두 번째 화면을
> 짚으셨고(§8) 함정 대조가 세 번째를 찾아, **조회와 판정을 한 파일로 옮겼습니다**
> — `attachEnrollmentClassCounts()` · `isEnrollmentGone()`.

`GET /api/admin/enrollments` 응답의 각 신청에 두 칸을 더했습니다.

- `class_total` — 그 신청이 만든 수업이 몇 건인가
- `class_active` — 그중 살아 있는(취소 아닌) 것이 몇 건인가

```sql
SELECT substr(source, 12) AS enr_id,
       COUNT(*) AS total_n,
       SUM(CASE WHEN status = 'cancelled' THEN 0 ELSE 1 END) AS active_n
  FROM class_schedules
 WHERE source LIKE 'adm-enroll:%'
   AND instr(?, ',' || substr(source, 12) || ',') > 0
 GROUP BY source
```

- `source` 형식이 `adm-enroll:<신청id>` 이고 `'adm-enroll:'` 이 11글자라 **`substr(...,12)`** 부터가 id.
- ⛔ **`IN` 목록을 안 만듭니다**(D1 바인드 100개 한도) — 콤마 문자열 **한 개**를 `instr` 로 봅니다.
- ⚠️ 양쪽에 콤마를 둬서 **앞자리가 겹치는 신청**(103 대 1031)이 새지 않게 했습니다.
- ⚠️ 조회가 실패하면 **칸을 안 싣습니다** → 화면이 예전대로 그립니다(fail-open).

### ② 판정은 화면 한 곳 — `public/admin/student.html`

```js
function enrCalHidden(enr) { ... }
```

- ① 신청서 자체가 끝난 것(`cancelled`·`canceled`·`rejected`·`ended`·`expired`) → 숨김
- ② **«수업이 있었는데 전부 취소»**(`total > 0 && active === 0`) → 숨김
- 그 밖에는 **전부 그대로 그립니다.**

**주간·월간 두 캘린더가 이 함수 하나를 함께 씁니다.**

---

## 4. 검토했다가 «버린» 방법과 그 이유

| 버린 방법 | 왜 |
|---|---|
| **D1 에서 `enrollments` 103 의 status 를 cancelled 로 UPDATE** | 개발·운영이 같은 DB 라 사람 승인 사항(CLAUDE.md 1-1). 그리고 데이터를 고쳐도 **구조가 그대로**라 다음에 또 납니다. 화면이 사실을 말하게 하는 것이 먼저입니다 |
| **캘린더에서 enrollments 카드를 통째로 없애기** | 아직 수업을 안 만든 신청(방금 확정)이 캘린더에서 사라집니다 — «되던 것» 을 깨는 방향 |
| **«수업이 0건» 도 숨기기** | 위와 같은 이유. 숨기는 것은 «있었는데 전부 취소» 뿐입니다 |
| **서버에서 아예 그 신청을 안 내려주기** | 목록·정산 등 다른 소비처가 그 신청을 못 보게 됩니다. 서버는 사실만 주고 판정은 그리는 쪽이 합니다 |
| **`status` 검사만 넣기** | 이 건은 신청서가 `confirmed` 라 그것만으로는 **안 사라집니다.** 그래서 ②가 필요합니다 |

---

## 5. 확인한 방법

- 타입체크 `tsc --noEmit` — 통과
- 새 하니스 `test-harness/enroll_calendar_cancelled_harness.mjs` — **PASS 39 / FAIL 0**
  - 판정 함수와 **서버 블록을 둘 다 오려 내 실제로 돌립니다**(문자열 검사 아님)
  - SQL 은 **진짜 SQLite**(`node:sqlite`)에 실사고 데이터를 넣고 돌립니다
  - 「숨긴다」 옆에 **「살아 있는 수업은 그대로 그린다」·「수업 0건도 그린다」·
    「서버가 칸을 안 주면 예전대로 그린다」를 짝으로** 둡니다
- **변이시험 14종 전부 실제 FAIL** / 이름만 바꾸는 무해한 리팩터는 통과
- 브라우저 검사 `test-harness/manual/enroll-calendar-cancelled-browser.mjs` — **PASS 11 / FAIL 0**
  - 진짜 크로미움으로 **주간·월간을 실제로 그려** «카드가 눈에 보이는가» 를 잽니다
    (「판정이 옳다」와 「화면에 안 나온다」는 다른 값입니다)
  - ⑤ **판정을 꺼 보면 그 카드가 다시 나타납니다** — 위 ②가 헛돌지 않았다는 증거입니다
  - ⚠️ 카드를 `title` 로 세다가 **월간이 늘 0개**로 나와 멀쩡한 화면이 한때 빨간불이었습니다
    (주간 카드에만 title 이 있습니다 → 두 뷰가 함께 쓰는 **색**으로 셉니다)
- 전체 회귀 `--fast` — **▶ 363 · ⚠ 0건**, `origin/main` 워크트리(▶ 362)와 이름 `comm` 대조해
  **새로 생긴 FAIL 0건**(363 = 362 + 새 하니스 1로 수가 정확히 맞습니다)

### 하니스가 세 번 헛돌았고, 그때마다 고쳤습니다

1. **「선언과 호출의 깊이가 같은가」로 물었더니 멀쩡한 코드가 빨간불** —
   호출은 «함수 안의 for 루프 안» 이라 깊이 2가 정상입니다.
   물어야 할 것은 **«선언 자체가 최상위인가»** 였습니다.
2. **「조회하는가」만 묻고 「응답에 싣는가」를 안 물어**, 조회는 하면서 `items` 에
   **안 싣는** 변이가 그대로 통과했습니다(= 이 사고가 그대로 재현되는데 초록불).
   → 블록을 오려 내 가짜 D1 으로 돌려 «무엇이 실렸는가» 를 답으로 묻고,
   **«다른 값을 주면 그 값이 실린다» 를 짝으로** 뒀습니다(하드코딩 방지).
3. **변이가 «크래시» 가 되어 ❌ 0건으로 위장**했습니다 —
   `instr` 조건을 지우면 `?` 가 0개가 되는데 `prepare().all(인자1개)` 가 던집니다.
   → 바인드 개수는 **돌리기 전** 에 보고, 실행은 `try` 로 감쌌습니다.
   ⚠️ **그것만으로는 부족했습니다** — 실행은 감쌌는데 **그 결과를 꺼내 쓰는 줄**이 밖에 있어,
   행이 비는 변이에서는 여전히 던졌습니다(함정 대조가 지적). 지금은 기본 객체로 받습니다.

4. **정본을 오려 돌리면서 그 함수가 기대는 상수를 안 주었습니다** — `ReferenceError` 가
   정본의 `try/catch` 에 삼켜져 «칸을 안 싣는다» 로 보이고, 그 절이 통째로 헛돕니다.
   → 상수를 **주입**하고 **«삼켜진 경고가 0건인가» 를 전제 검사**로 두었습니다.

5. **CSV 가 사유를 잃는 변이를 한동안 못 잡았습니다**(§8) — 검사를 파일 전체에 걸었더니
   같은 파일의 셀 렌더러(`_enrTd`)가 대신 걸려 통과했습니다.
   → **내보내기 함수 몸통만 중괄호 짝으로 잘라서** 봅니다.

---

## 6. 사장님이 한 번 봐 주셔야 끝입니다

작업 환경의 프록시가 `mangoi.ai` 를 막아 **배포된 화면을 제가 못 봅니다.**

**관리자 › 학생 상세 › `jeong` › 주간** 에서 **금요일 14:20 「체험수업」 카드가 사라졌으면** 성공입니다.
(화·수·목·금 **19:20** 의 정규수업 4건은 그대로 있어야 합니다 — 스크롤을 내리면 보입니다.)

---

## 7. 남은 것 — 사람이 정할 일

- **`enrollments` 103 의 `status`** 는 여전히 `confirmed` 입니다.
  화면은 이제 사실을 말하지만, «신청서도 취소로 정리할지» 는 D1 쓰기라 사장님이 정하실 일입니다.
- **근본은 «수업을 전부 취소하면 신청서도 따라가게» 하는 배선**입니다.
  다만 「수업을 하나 취소한 것」과 「과정을 접은 것」은 다른 뜻이라,
  자동으로 묶으면 반대 사고(멀쩡한 신청서가 취소됨)가 납니다 — **별건**으로 남깁니다.
- **신청서를 읽는 곳을 전수로 셌습니다**(`grep 'FROM enrollments' src/`) — 화면에 그리는 통로는
  §8 의 셋이 전부입니다. 나머지는 주문번호·id 조회와 시연 씨앗(demo seed)이라 해당 없습니다.
- ⚠️ **「수강신청 관리」 카드는 일부러 안 건드렸습니다** — 거기는 상태 칩(확정·취소…)을 그대로
  보여 주는 «정리하는 화면» 이라 감추면 오히려 안 됩니다.
  다만 그 카드에 «수업 4건 중 0건 살아있음» 같은 표시를 넣으면 사람이 찾기 쉬워집니다 —
  서버가 이미 `class_total`·`class_active` 를 실어 주므로 **화면 한 줄**이면 됩니다(사람이 정할 일).

---

## 8. 🔴 후속 — 사장님 「같은 문제이것 같아」·「이거 재발 안하도록 반드시 모두 수정해줘」

캘린더를 고친 직후 사장님이 **두 번째 화면**을 짚으셨습니다 —
학생 «명부» 표의 「수강신청」 칸에도 그 「체험수업」이 그대로 있었습니다.

### 통로가 «셋» 이었습니다

| # | 통로 | 그리는 화면 |
|---|---|---|
| ① | `GET /api/admin/enrollments` (`api-admin.ts`) | 학생 상세 › 주간·월간 캘린더 |
| ② | `GET /api/admin/students/unified` (`api-admin.ts`) | 학생 명부 표 「수강신청」 칸 |
| ③ | `GET /api/admin/student/:uid/full` (`api-mango.ts`) | **같은 화면** 🗓️ 종료·연장 탭 「활성 패키지」 |

①만 고쳤을 때 ②는 사장님이, ③은 **함정 대조가** 찾았습니다.
③이 특히 나쁩니다 — **같은 화면이 위(캘린더)에서는 «없다», 아래(활성 패키지)에서는 «있다»** 고 말합니다.

> 📌 규칙서의 「고쳤다를 적기 전에 부르는 곳을 «전부» 세세요」를
> 이번에는 `grep` 으로 세고 **하니스에 못 박았습니다**(Ⓕ절 — 「세 통로가 전부 그 정본을 부르는가」).

### 고친 것

- **정본 `src/enrollment-class-count.ts` 신설** — 조회(`attachEnrollmentClassCounts`)와
  판정(`isEnrollmentGone`)을 한 파일에 두고 셋이 그것만 부릅니다.
  ⛔ SQL 을 호출부에 복제하지 않습니다(하니스 Ⓕ-4 가 막습니다).
- **③ 「활성 패키지」** — 화면이 `enrCalHidden()` 으로 거릅니다(`admin/student.html`).
- **② 「수강신청」 칸** — 서버가 `isEnrollmentGone()` 으로 내리되
  **조용히 지우지 않고** `enroll_hidden_package`·`enroll_hidden_reason` 을 같은 행에 실어
  화면이 「— 수업이 전부 취소됨」이라고 **말하게** 했습니다(`adm-core.js` `_enrTd`, v=238).
  **CSV 내보내기도 그 사실을 싣습니다.**

> ⛔ 그냥 «—» 로 두면 «원래 신청이 없는 학생» 과 **같은 글자**가 되어 아무도 정리하지 않습니다
> (규칙서 「감추면 아무도 정리하지 않습니다」).

### 🪤 이때 밟은 함정

1. **라벨 헬퍼를 `renderStudentTable` 안에 뒀습니다.**
   표와 **CSV 내보내기(`smExportStudentsCsv`)는 서로 다른 함수**라,
   그대로 뒀으면 내보내기가 `ReferenceError` 로 죽습니다 → **최상위로** 옮겼습니다.
   (규칙서 「함수 선언을 다른 함수 «안» 에 넣으면…」 — 저장소에서 또 밟았습니다.)
2. **두 화면이 다른 파일이라 판정 함수를 공유할 수 없습니다.**
   → 「같은 규칙」을 두 곳에 두고 **하니스가 둘을 나란히 돌려 48가지 경우를 전수 대조**합니다(Ⓔ절).
   상태 목록은 손으로 적지 않고 **신청 관리 화면의 `EN_STATUS_META`·`EN_LIVE` 를 읽어** 대조합니다
   — 그 화면에 «끝난 상태» 가 하나 늘었는데 판정이 모르면 같은 사고가 그대로 재발하기 때문입니다.

## 9. 「반드시 모두」 — 남은 두 화면은 **감추지 말고 «말하게»**

사장님 「이거 재발 안하도록 반드시 모두 수정해줘.」 로 신청서를 그리는 화면을 **전수**로 세었더니
넷이었고, 성격이 **둘로 갈렸습니다.**

| 성격 | 화면 | 무엇을 하나 |
|---|---|---|
| **감춘다** | 캘린더(주간·월간) · 학생 명부 「수강신청」 칸 · 🗓️ 종료·연장 「활성 패키지」 | 그리면 «지금 있는 수업» 으로 읽히므로 내린다 |
| **말한다** | 학생 상세 「등록·수강」 표 · 관리자 「수강신청 관리」 카드 | 사람이 **정리하는 장부**라 감추면 찾을 길이 없다 |

> ⛔ **장부에서 감추면 안 됩니다** — 그 신청을 찾아 취소·정리할 입구가 사라집니다.
> 그렇다고 아무 말도 안 하면 「확정인데 수업이 없다」가 조용히 남습니다.
> ⟹ **감추지 않고 「⚠ 수업 4건 전부 취소됨」을 글자로** 말하게 했습니다
> (`admin/student.html` 의 `enrClassNote()` · `adm-core.js` 의 `clsChip`, v=239).
> ⚠️ 서버가 `class_total`·`class_active` 를 안 실으면 **아무 말도 안 합니다**(fail-open).

### 🟡 판정이 «모르는 상태» — `refunded`

판정(`isEnrollmentGone`)은 「끝난 상태 목록에 있으면 숨김 · **모르는 값은 그대로 그림**」입니다.
그 목록에 **`refunded` 가 없습니다** — `api-pay-refund.ts:504` 가 환불 때 그 값을 씁니다.
실측(2026-09-21) 반경은 **0행**(신청 14건이 전부 `confirmed`)이지만, 환불이 한 건이라도 나면
그 신청이 캘린더에 조용히 계속 그려집니다.

> ⛔ **말없이 목록을 넓히지 않았습니다** — 넓히는 순간 그 학생 수업이 화면에서 사라지고,
> 「환불 신청분을 캘린더에서 내릴 것인가」는 **사람이 정할 일**입니다(환불 정산 중에도 수업이 돌 수 있습니다).
> ✅ 대신 하니스 Ⓚ절이 **서버가 실제로 쓰는 상태를 `src/` 에서 읽어**,
> 판정도 모르고 「계속 그린다」 목록(`pending`·`confirmed`·`active`)에도 없는 이름을
> **FAIL 이 아니라 «이름으로 찍어»** 출력합니다(선례 `popup_open_return_harness`).
> 새 상태를 쓰기 시작하면 **그날 그 줄에 이름이 뜹니다.**

🪤 그 추출을 처음엔 «그 파일 어딘가의 `pending`+`confirmed` 배열» 로 넓게 잡았더니
**딴 표의 상태가 섞였습니다** — `api-admin.ts` 의 `leveltest_applications` 목록(`['pending','proposed','confirmed']`)이
잡혀 신청에 쓰지도 않는 **`proposed` 가 「사람 결정 대기」로 찍혔습니다.**
⟹ 그 `UPDATE` 보다 **«앞» 에 있는 것 중 가장 가까운 배열 하나**만 봅니다.

### 최종 검증

- `tsc --noEmit` **exit 0**
- `enroll_calendar_cancelled_harness.mjs` — **PASS 112 / FAIL 0**
- `manual/enroll-calendar-cancelled-browser.mjs` — **PASS 16 / FAIL 0**
  (주간·월간 + 🗓️ 종료·연장 탭을 **실제로 그려** 재고, 판정을 꺼 보아 다시 나타나는지까지)
- **변이 19종 전부 실제 FAIL** — 서버 판정 무력화 · `substr` 경계 12→11 · `instr` 좁히기 제거 ·
  조회는 하고 안 붙임 · fail-open 깨기 · 세 통로 호출 제거(각각) · 사유 안 실음 ·
  화면 매핑 제거 · 셀 되돌리기 · 라벨 헬퍼를 함수 안으로 · CSV 에서 사유 제거 ·
  **장부 경고 조건 뒤집기(2곳) · 장부 경고 가드 제거 · 장부에서 감추기 · 장부 배선 제거 · 만든 표시를 안 싣기**

## 10. 🔀 같은 카드를 **다른 세션이 «회색» 으로** 먼저 병합했습니다 (#1059)

작업 중 `main` 이 움직였습니다 — #1059 가 **같은 캘린더 카드**를
「살아 있는 수업이 0건이면 **회색 + 「신청만 · 수업 없음」**」으로 병합했습니다.

⛔ **지우지 않고 둘 다 남겼습니다.** 둘은 같은 말이 아니라 **겹치지 않는 반쪽**입니다 —

| 판정 | 언제 | 무엇을 |
|---|---|---|
| `enrCalHidden()` (이 PR) | «있었는데 **전부 취소**» | **감춥니다** |
| `mgsEnrHasLiveClass()` (#1059) | «살아 있는 수업 **0건**» | **회색 + 「신청만 · 수업 없음」** |

감춰진 신청은 아래 루프까지 못 오므로 회색 코드가 **아예 안 돕니다.**
⟹ **회색이 실제로 닿는 것은 «아직 수업을 안 만든 신청»** 쪽이고,
그 경우는 규칙서가 **「⛔ «수업이 0건» 을 숨김으로 읽지 말 것」** 이라 못 박은 자리라
«감추지 않고 말하는» 것이 맞습니다. ⟹ 합치는 것이 **어느 한쪽보다 낫습니다.**

> ⚠️ #1059 루프 안의 주석 「⛔ 감추지 않는다」는 그대로 두면 **거짓 문장**이 됩니다
> (이제 그 위에서 감추는 판정이 하나 더 돕니다) — 그래서 두 판정 사이에
> **「이 주석은 «그 회색 판정» 에 대한 말이다」** 를 적어 두었습니다.

### 🪤 병합에서 밟은 것

1. **`git checkout --merge` 로 되살린 충돌 표시는 `HEAD`/`origin/main` 이 아니라 `ours`/`theirs`** 입니다.
   그걸 모르고 `<<<<<<< HEAD` 를 찾는 스크립트를 돌려 «해결 실패» 한 파일을 그대로 `git add` 할 뻔했습니다
   → 해결 뒤 **`<<<<<<<`·`>>>>>>>` 가 0인지** 확인하고 add 하세요.
2. **#1059 의 하니스가 병합 직후 죽었습니다** — 그 가짜 DOM 에 `enrCalHidden` 이 없어
   `ReferenceError` 로 «전제» 하나가 빨간불(= 그 절이 통째로 헛돎).
   **함께 오려 넣고**, ⑧절에 **「전부 취소는 안 그린다」+「아직 안 만든 것은 회색으로 말한다」를 짝으로** 더했습니다.
   변이(양쪽 `continue` 제거)로 **두 하니스가 각각 2건씩 실제 FAIL** 하는 것을 확인했습니다.

### ❌ 사람이 봐야 끝나는 것

프록시가 `mangoi.ai` 를 막아 배포 화면을 제가 못 봅니다.

- **학생 상세 › `jeong` › 주간** — 금 14:20 「체험수업」 카드가 **없어야** 합니다(정규수업 4건은 그대로).
- **학생 상세 › `jeong` › 🗓️ 종료·연장** — 「활성 패키지」가 **「체험수업」이 아니어야** 합니다.
- **학생 관리 › 학생 목록 › `jeong` 행** — 「수강신청」 칸이 **「— 수업이 전부 취소됨」** 이어야 합니다.
- **학생 상세 › `jeong` › 「등록·수강」 표** — 그 행이 **그대로 있고**, 상태 배지 아래에
  **「⚠ 수업 4건 전부 취소됨」** 이 보여야 합니다(⛔ 여기서 사라지면 반대 사고입니다).
- **관리자 › 수강신청 관리 카드** — 그 신청 줄 패키지 아래 회색 줄 끝에
  **「⚠ 수업 4건 전부 취소됨」** 이 붙어야 합니다.
