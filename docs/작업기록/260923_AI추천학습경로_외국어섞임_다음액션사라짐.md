# AI 추천 학습 경로 — 다음 액션에 한자·베트남어 · 다시 들어가면 사라짐 (2026-09-23)

## 왜 했나
사장님 제보(`parent.html` 「🎯 AI 추천 학습 경로 (이번 주)」 — 학생 `lee`):
1. 「⚡ 다음 액션」에 `면談을진행`·`khuyến유` — 한자·베트남어가 섞여 나옴
2. 로그아웃했다가 다시 들어가면 AI 추천 항목이 안 나타남

## 원인 (코드로 확인)
- ① `POST /api/admin/ai-analyze/student`(`src/api-admin.ts`)가 Llama 3.3 에게 「한국어로」라고 **지시만** 하고 결과를 안 봤다.
- ② 12시간 캐시에 저장하는 INSERT 칸 목록에 **`next_action` 이 없었다**(표에도 칸이 없음). 첫 응답은 방금 만든 값을 보내 보이지만, 12시간 안에 다시 누르면 저장본이 나가 주황 줄이 통째로 안 그려졌다. 게다가 분석은 **버튼을 눌러야만** 불러온다.
- ③ (덤) 「출석 3회로 낮다」는 실제 수업 기록이 아니라 **출석 포인트 로그(`point_rule_log`)** 를 센 값이었다.

## 한 일
- 새 정본 `src/ai-analysis-clean.ts` — 한자·가나·라틴 확장(베트남어)·키릴·태국 문자 판정, 섞인 항목을 **통째로** 빼기, 옛 저장본의 `raw_response` 에서 다음 액션 되살리기.
- 라우트: 섞였으면 **한 번만** 다시 만들고(섞인 칸이 줄 때만 채택, 재시도 실패는 첫 답을 거름), 그래도 섞인 항목은 뺌. 요약까지 못 살리면 캐시에 저장하지 않음(그 안내문이 12시간 나가지 않게).
- 표에 `next_action` 칸 멱등 ALTER + INSERT 에 포함(칸을 못 만든 환경은 칸 없이라도 저장). 캐시 응답도 거르고 옛 저장본은 되살림 → 배포 즉시 다시 보임.
- 출석: `attendance` 정본 조건(`ATTENDANCE_BY_UID`)으로 **수업한 날 수**, 미래 예약 씨앗·`scheduled` 제외. 조회 실패면 0 이 아니라 「확인 못 함 — 판단하지 마세요」로 프롬프트에 넣음.

## 함정 대조가 찾아 고친 것
- 재시도 답을 «섞인 칸 수» 로만 고르면 칸이 아예 빠진 답이 이겨 멀쩡한 요약을 잃음 → «거른 뒤 남는 양»(`cleanScore`, 요약을 가장 무겁게)으로 견줌.
- 옛 저장본의 요약이 걸러지면 12시간 동안 「다시 눌러 주세요」만 나옴(화면은 `force_refresh` 를 안 보냄) → 그때는 저장본을 버리고 새로 만듦.
- 라틴 확장 범위가 넓어 café·résumé 같은 차용어까지 빠짐 → 베트남어 전용 글자(ă đ ơ ư + U+1EA0~1EF9)로 좁힘.
- 「수업한 날」 에 카페24 예약 씨앗이 섞임 → 프롬프트 라벨을 「수업 기록이 있는 날(카페24 예약 포함 — 결석 여부 모름)」 로 사실대로. 씨앗을 뺄지는 사람이 정할 일(빼면 카페24 학생이 0 이 되어 «출석 낮음» 으로 읽힘).

## 검토했다가 버린 것
- 섞인 글자만 잘라 내기 → 「면을진행」 같은 비문이 남아 버림.
- 다시 들어올 때 자동으로 분석 불러오기(제안의 3번) → 저장본 없는 학생은 열 때마다 AI 비용. 사장님과 합의해 보류.

## 확인
- 신규 `test-harness/ai_analysis_clean_harness.mjs` PASS 52 / FAIL 0 — 정본을 실제로 실행, 재시도 블록을 오려 내 가짜 AI 로 실행, 「뺀다」 옆에 「멀쩡한 것은 남긴다」 짝.
- 변이 17종 전부 실제 FAIL(조건 `|| true`·요약 가중치 0·옛 넓은 범위·캐시 요약 버림 제거 포함, 판정 끄기·조건 뒤집기·재시도 끄기·무조건 채택·예외 전파·INSERT 칸 제거·캐시 복구 제거·캐시 가드 제거·미래 씨앗 포함 등).
- tsc: `origin/main` 기준선과 대조해 새 오류 0건(126=126, 줄번호만 이동).
- `--fast`: 기준선 PASS 358 → 359(수정 후 재측정 동일), ⚠ 17개 양쪽 같은 목록(환경 사유).
- ❌ 실제 AI 응답·배포 화면은 못 봄(프록시 차단) — 사람이 한 번 눌러 봐야 끝.
