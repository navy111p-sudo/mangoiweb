# 낱말 사진 — 뜻과 안 맞는 사진 전수 확인·교체 (2,047장)

- 날짜: 2026-09-23
- 요청: 사장님 스크린샷 5장(eleven→사과, possible→사다리, morning→헬스장, garlic→통닭, deadline→시계+서류)
  「이런 단어들과 실사 이미지가 매칭이 안되는 것들 모아서 다시 고쳐줘」
- 담당 영역: B(학습 콘텐츠) — `scripts/`, `docs/scene-curriculum-media/`, `public/img/scene-words/`, payload

## 1. 원인은 둘이었습니다

1. **고르는 차례** — 그 낱말을 그리려고 만든 전용 사진(rank 0)이 있어도, 문장 장면 사진·클립 스틸이
   먼저 뽑혔습니다(`morning` ← 「Good morning」 문장이 아니라 헬스장 장면). → 커밋 ef55107:
   `build-scene-curriculum.mjs` 가 **전용 사진(rank 0)을 먼저** 고르게 고침. 하니스에 짝 검사
   («전용 사진이 있으면 그 사진을 먼저 쓴다» 줄 수 > 30,000) 추가 — 되돌리면 실제로 FAIL.
2. **전용 사진 자체가 약하거나 틀림** — 다의어의 엉뚱한 뜻, 개수로 수 나타내기(사과 15개=eleven),
   추상어에 흔한 장면, 가짜 글자, 아이에게 부적절한 장면.

## 2. 한 일 (잰 것)

| 단계 | 대상 | 결과 |
|---|---|---|
| 1차 눈 확인 | 전용 사진 4,158장(접촉시트 139장, 검토자 7) | 불합격 **1,740** (그중 그릴 수 없는 조각·고유명사 61) |
| 1차 재생성 | 1,677 교체 + 전용 사진이 없던 **370낱말** 새로 = 2,049 | **2,047장** 생성 (nsfw 거절 34 → 문장 다시 써서 32 성공, `body`·`harry` 2개는 옛 사진 유지) |
| 2차 눈 확인 | 새 2,047장(접촉시트 69장, 검토자 4) | 불합격 **397** (그릴 수 없는 조각 `sri`·`meta` 2) |
| 2차 재생성 | 395 | **376장** 생성 (nsfw 19 → 2차 사진 유지) |
| 3차 눈 확인 | 새 376장 | 불합격 **6**(위험 3·실존인물/브랜드 2·뜻 1) → 6장 다시 만들어 눈으로 확인 |

- 번호: 18001~20320(1차) · 21001~21406(2·3차). 새 표 `docs/scene-curriculum-media/word-image-plan-5.json`(2,047항).
- 바꾼 옛 사진은 plan 표에서 빼고 webp 도 지웠습니다(1,677 + 376 + 6).
- 근거 게이트 `verify-word-prompts`: **2,047/2,047 통과 · 껍데기 16 → 16**(줄지 않음).
- manifest 대조: `wordForms 4539`·`wordPictureForms 4536`·`cardRows 25`·`wordPictureRows 39877` **그대로** ·
  `imageAssets 4710 → 5076` · `maxBookBytes 219,397 → 223,872`(상한 350,000).
- 사장님이 든 5개 낱말을 화면 그대로 다시 봄: eleven=숫자초 11 케이크 · possible=트로피 든 아이 ·
  morning=창밖 해돋이 · garlic=통마늘 · deadline=보고서 제출. 전부 뜻에 맞음.
- 하니스: `scene_curriculum` **680,603** · UI **147** · `word_quiz_pictures` **20/0** ·
  `--fast` 를 `origin/main` worktree 와 이름 `comm` 대조 → **새 FAIL 0 · 사라진 FAIL 0**(양쪽 17건은 환경 사유).
- `.ts` 변경 0건이라 tsc 해당 없음.

## 3. 밟은 것

- **nsfw 거절은 «아이 + 속상한 감정» 조합에서 몰렸습니다**(`hurt`·`fever`·`punishment`·`peer`…).
  어른·사물 장면으로 다시 쓰면 대부분 통과. ⚠️ 거절 사유는 제공자가 안 주므로 추론입니다.
- **재시도 후보가 «옛 사진 번호»(`replaces`) 칸을 잃었습니다** — 생성 결과에서 후보를 다시 만들며
  그 칸이 빠져 옛 사진 31장이 남아 있었습니다. «새 사진이 실제로 쓰이는가»(2,047 중 사용 수)를 세어서 잡았습니다.
- **병렬 생성 에이전트가 같은 scratchpad 파일을 덮었습니다**(한 묶음이 남의 번호 12개를 만듦).
  → 에이전트마다 전용 폴더 + 제출 전 번호·문장 재확인. 전 결과를 «번호↔낱말·URL 중복» 으로 전수 대조(0건).
- 제출 타임아웃 후 재제출로 **중복 과금 가능성**(묶음당 최대 12장, 여러 묶음). 크레딧 정확한 소모는 못 쟀습니다.
- 병렬 검토자마다 기준이 조금씩 달라 불합격률이 9%~42%로 갈렸습니다(고급 어휘가 몰린 묶음이 높음).

## 4. 남은 것 (사람이 정할 일)

- **그릴 수 없는 63개**(1차 61 + `sri`·`meta`): plus·prejudice·pride·wuthering·tolkien·potter·harry·jane·
  netflix·disney·marvel·lumpur·minh … — 작품 제목·고유명사 조각이라 찍을 장면이 없습니다. 옛 사진 그대로.
- **nsfw 로 두 번 막힌 21개**는 2차 사진 그대로(`body`·`feel`·`hurt`·`normal`·`peer` 등 — 2차 검토에서 뜻이 약하다고 본 것 포함).
- 고급 추상어(SIU ADVANCE: significantly·framework 류)는 «그럴듯한 장면» 이 한계입니다.
- 임시 브랜치 `tmp/word-img-b5`·`b6`·`b7` 은 푸시 프록시가 삭제를 거부해 사람이 지워야 합니다.
- ❌ 배포된 화면은 프록시 403 이라 사람이 한 번 봐야 끝.
