# 「단어랑 사진이 아직도 매칭이 안 돼」 — 빌려 쓰는 낱말에 전용 사진 (1차 347장)

- 날짜: 2026-09-23
- 브랜치: `claude/modest-cray-nbq85a`
- 담당 영역: **B**(학생 학습 콘텐츠). 공동 금지구역은 한 글자도 안 건드렸습니다.

## 1. 왜 했나

**2026-09-22 사장님**(스크린샷 3장)
> 단어랑 사진이랑 아직도 매칭이 되지 않아. **노트북인데 어떻게 가방맨 학생**이고
> **name인데 판이 나오고** **사과가 15개인데 왜 eleven**이야 이런 문제에 대해서 넌 어떻게 생각해???

재 보니 **한 가지 버그가 아니라 서로 다른 원인 셋**이었습니다.

| 보기 | 원인 | 이 PR 에서 |
|---|---|---|
| `notebook` → 가방 멘 학생 | **rank 1** — 자기 사진이 없어 **남의 사진을 빌려 씀**. 근거 게이트는 「그 설명에 notebook 이 있다」를 정직하게 통과시킵니다 | ✅ 고침 |
| `name` → 글자 없는 빈 명패 | 프롬프트를 **낱말만 보고** 써서 모델이 «없는 것» 을 그림 | 2026-09-22 에 401번째로 고쳤고, **화면 문구**가 아직 남음 → **사람이 정할 일** |
| `eleven` → 사과 15개 | **모델이 개수를 못 셉니다.** 빌려 쓰는 것이 아니라 자기 장면 사진이 있음 | ❌ 안 함 → **사람이 정할 일** |

**2026-09-22 사장님 「B로 가게 해줘」** — 선택지 A(빌려 쓰는 사진을 떼기)가 아니라
**B(그 낱말들에 전용 사진을 만들기)** 입니다.

## 2. 왜 A 를 안 골랐나 (버린 길)

- **A — 빌려 쓰는 사진을 안 붙이기**: 한 줄로 끝나고 「엉뚱한 사진」이 0 이 됩니다. ⛔ 그런데
  **`notebook`·`boy`·`backpack` 처럼 지금 멀쩡히 보이는 사진까지 사라집니다**(967개 낱말 형태,
  줄 수천 개가 🎨 그림문자 카드로 내려감). 사장님이 B 를 고르신 이유.
- **E — 근거 게이트를 «예문 뜻까지» 보게 하기**: 2026-09-22 에 두 신호를 재 봤고 둘 다 게이트가
  못 됩니다(「«빈·없는» 모양」은 판별력 없음 · 「예문 내용어 공유」는 표 전체 68% 가 안 겹쳐
  거짓 FAIL 2천 건). 사진을 볼 수 없으니 **원리상 기계로 못 막습니다.**
- **C(수 낱말 사진 떼기)·D(화면 문구 누그러뜨리기)**: 지시 밖이라 **손대지 않았습니다.**

## 3. 한 것

- 대상 **967개 낱말 형태** 가운데 **347개**에 전용 사진을 만들어 붙였습니다(번호 **16001~16354**).
  남은 **620개**는 다음 묶음.
- 표는 `docs/scene-curriculum-media/word-image-plan-3.json`(347). 목록 규칙 정본은
  `scene-picture-evidence.mjs` 의 `wordPlanFiles` 한 곳이라 **빌드·하니스가 같은 함수**를 씁니다.
- 사진은 `cloudflare-deploy/public/img/scene-words/16001–16354.webp`.
- 새 도구 `scripts/verify-word-prompts.mjs` — **크레딧을 쓰기 «전»** 에 ① 근거 게이트 통과
  ② **껍데기 목록이 줄지 않았는지**를 함께 봅니다(껍데기가 줄면 그 낱말이 **이미 만든 사진
  전부의 근거로 되살아납니다**).

### 🔴 안 만든 낱말 7개 — 그 7개는 «고쳐지지 않았습니다»

⚠️ 처음에 이 자리에 「🎨 카드로 남습니다」라고 적었는데 **사실이 아니었습니다**(잰 뒤 정정).
그 7개는 카드가 아니라 **예전처럼 남의 사진을 빌려 쓰고** 있고, **6개는 그 사진이 «다른 뜻»** 입니다 —
**사장님이 신고한 증상 그대로**입니다.

| 낱말 | 교재 예문 | 지금 보이는 사진 |
|---|---|---|
| `potter` | Have you read **Harry Potter**? | «techniques» — 물레를 돌리는 **도공** 🔴 |
| `ring` | **The Ring** was very frightening. | «today» — 달력에 그린 **동그라미** 🔴 |
| `heights` | Wuthering **Heights** are classics. | «increase» — **높아지는 동전 더미** 🔴 |
| `hundreds` | the late eighteen **hundreds** | «hundred» — **동전 100개** 🔴 |
| `r` | **J.R.R.** Tolkien | «bicycle» — 빨간 자전거 🔴 |
| `pure` | Most of them are **pure fiction**. | «happiness» — 웃는 아이 ⚠️ |
| `clear` | it is a **clear** night | «animal» — 숲속 빈터의 사슴 ⚠️ |

⛔ **왜 안 만들었나** — 앞 다섯은 **작품 제목·이름의 조각**이라 찍을 장면이 없고(Harry Potter 를
찍으면 저작물을 그리게 됩니다), `clear` 는 틀에서 오는 **껍데기**라 구조적으로 게이트를 못 지나며,
`hundreds` 는 「1800년대」라는 **연대**입니다.
⛔ **사진을 떼서 카드로 만들지 마세요** — 2026-09-21 사장님 「🎨 카드는 쓰지 말고 실사만」 지시와
정면으로 어긋납니다. **사람이 정할 일**로 남깁니다.

## 4. 잰 것

**프롬프트 게이트** — 347/347 통과 · 껍데기 **15 → 15**(변동 없음).

**payload before/after 대조**(`origin/main` 의 payload 를 `git show` 로 떠서 비교)

| | 값 |
|---|---|
| 줄 | 39,910 → **39,910** |
| 사진 잃음 / 생김 | **0 / 0** |
| 사진 → 카드 / 카드 → 사진 | **0 / 0** |
| 사진이 바뀐 줄 | **1,015**(전부 목표 낱말) |
| 그 1,015줄이 받은 것 | **자기 사진 1,015** · 빌린 것 0 · 장면 사진 0 · 없음 0 |
| **목표 낱말 «밖» 에서 바뀐 줄** | **0** |

**manifest** — `imageAssets 3772 → 4115` · `maxBookBytes 219819 → 220392`(상한 350,000) ·
`maxBookGzipBytes 41148 → 41244`(상한 80,000) · 책 85 그대로 · **책 단위 칸(용량 빼고) 변화 0**.

> `imageAssets` 가 +347 이 아니라 **+343** 인 까닭: `12101`·`12668`·`12682`·`12402` 네 장이
> **빌려 주던 낱말이 없어져 안 쓰이게** 됐습니다. 그 네 장의 «원래 주인» `yoga`·`pizza`·`red`·`live`
> 는 전후 모두 **클립 스틸**을 쓰므로 잃은 것이 없습니다(파일은 예비로 저장소에 남습니다).

**새 도구가 실제로 FAIL 하는가 (변이시험)** — 「검사를 넣었다」가 「검사한다」로 읽히지 않게 셋을 실제로 넣어 봤습니다.

| 변이 | 결과 |
|---|---|
| 프롬프트에서 낱말을 바꿔 씀(`models` → `somethingelse`) | **탈락 1 · exit 1** |
| 껍데기 낱말(`clear`)로 후보를 만듦 | **탈락 1 · exit 1** |
| 틀을 안 쓴 프롬프트 2,000개를 넣어 **껍데기를 줄임** | **껍데기 15 → 1 · exit 1**(경고 문구 출력) |
| 원본 347개(대조) | **통과 347 · exit 0** |

⚠️ 그 exit 를 `| tail` 에 물려 읽었다가 **`exit=0` 으로 잘못 봤습니다**(파이프의 종료코드는
마지막 명령의 것 — CLAUDE.md 2장에 이미 있는 함정). 파일로 받아 다시 재니 **exit 1** 이었습니다.

**사람 눈으로 본 것** — 347장을 접촉인화(contact sheet)로 만들어 **전부 눈으로 봤습니다**
(1차 116장은 앞 세션, 2·3차 231장은 이번). **«낱말과 전혀 다른 물건» 은 0건**입니다.
위험한 다의어를 골라 교재 예문과 나란히 대조도 했습니다 — 예: `stones` 는 **바둑돌**인데
예문이 「To play baduk, I need a board and a set of stones.」 라 맞습니다.

**하니스**
- `scene_curriculum_harness` **PASS 636,651 · FAIL 0**(exit 0)
- `scene_curriculum_ui_harness` **PASS 147 · FAIL 0**(exit 0)
- `--fast` 를 **`origin/main` worktree 와 이름 `comm` 대조** → **새 FAIL 0건 · 사라진 FAIL 0건 ·
  공통 17건**(양쪽 다 `node_modules` 가 없는 환경 사유). 돈 하니스 **377 = ✅ 360 + ⚠ 17** 로
  요약줄의 PASS 360 과 산술 일치.
- **`tsc` 는 돌리지 않았습니다** — 이 PR 에 `.ts`/`.tsx` 가 **0개**라 결과가 main 과 같습니다
  (`git status` 로 확인). 「돌렸다」고 적지 않습니다.

## 5. 밟은 함정 (다음 사람을 위해)

1. 🔴 **목록 파일 끝에 줄바꿈이 없어 «마지막 한 장» 이 두 번 연달아 빠졌습니다.**
   러너 스크립트가 `list.map(...).join("\n")` 으로 목록을 쓰는데, `while read` 는 줄바꿈으로
   끝나지 않은 마지막 조각을 읽고도 **거짓을 돌려줘** 루프가 그 자리에서 끝납니다
   (실측으로 재현 확인). 1차 117→**116**, 2차 118→**117**, 매번 `16120 rooms`.
   러너는 `exit 0` 이라 **어디에도 실패로 안 보였고**, 알아챈 것은 «받은 장수» 뿐이었습니다.
   → `join("\n")+"\n"` 으로 고쳐 3차 **114/114**. CLAUDE.md 2장에 한 줄 넣었습니다.
2. 🔴 **README 에 적혀 있던 「plan-1+2 = 껍데기 16개(`a` 가 늘어남)」이 사실이 아니었습니다.**
   정본을 실제로 돌려 재니 **15개**이고 `a` 는 설명의 **76.2%**(문턱 80%)라 껍데기가 아닙니다 —
   `a` 를 근거에서 빼는 것은 껍데기가 아니라 `stopwords.json` 입니다. 결론(「판정은 안 바뀌었다」)은
   맞았지만 **숫자와 까닭이 틀렸습니다.** README·INDEX 두 곳을 고쳤습니다.
3. ⚠️ **백그라운드 `sleep` 을 걸어 놓고 그 합만큼 시간이 흐른 줄 알았습니다** — CLAUDE.md 에
   이미 적힌 함정인데 그대로 밟았고, `date -u` 를 찍고서야 **실제 경과가 3분**인 것을 알았습니다.
   그 뒤로는 **원격 ref 를 폴링하는 `until` 루프**로 기다렸습니다.
4. ⚠️ 임시 브랜치를 만들 때 **작업트리를 한 글자도 안 건드리는 방식**으로 했습니다
   (`GIT_INDEX_FILE` + `read-tree`/`update-index`/`commit-tree`/`push <sha>:refs/…`).
   지난번에는 `git checkout -B` 가 커밋 안 한 작업 201개를 임시 커밋으로 쓸어 담았습니다.
5. ⚠️ **base 를 새 main 으로 옮길 때** `git reset --soft` 만으로는 부족합니다 — 인덱스가 그 사이
   main 이 바꾼 13개 파일의 **옛 내용**을 들고 있어 그것을 «되돌리는» PR 이 됩니다.
   그 13개를 **경로를 하나씩 적어** `git checkout origin/main -- <paths>` 로 가져왔습니다
   (⛔ `-- .` 금지).

## 6. 아직 안 한 것 · 사람이 정할 일

- **남은 620개 낱말** — 다음 묶음(번호는 16355~ 로 이어 씁니다).
- 🔴 **`eleven` 류 수 낱말**(약 11개) — 모델이 개수를 못 세므로 **사진을 떼는 것**(선택지 C)이
  유일한 답입니다. 지시가 없어 손대지 않았습니다.
- 🔴 **화면 문구** — 사진 옆에 「AI 그림 · 그림 설명이 이 낱말을 가리켜요」라고 **단정**하는데,
  사장님이 든 셋 모두에서 그 문장이 거짓이었습니다(선택지 D).
- **빌려 쓰는 낱말 1,347개의 «예문 뜻»** 은 여전히 아무도 안 봤습니다(이 PR 로 347개가 자기
  사진을 받아 그만큼 줄어듭니다).
- ⚠️ **임시 브랜치 `tmp/word-img-b1`·`-b2`·`-b3`·`tmp/word-img-name`·`tmp/word-img-fix`·
  `tmp/word-img-2` 는 푸시 프록시가 «삭제» 를 거부해 못 지웠습니다** — 사람이 지워 주세요.
  워크플로는 그 브랜치에서만 돌고 main 에는 없습니다.
- ❌ **배포된 화면은 못 봤습니다**(이 컨테이너는 `mangoi.ai` 가 프록시 403). 화면이 바뀌는
  작업이라 **사람이 한 번 봐야 끝입니다** — 특히 사장님이 신고한 `notebook`.
