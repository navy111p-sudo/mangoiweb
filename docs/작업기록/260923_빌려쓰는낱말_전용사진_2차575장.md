# 「실사만 가야해」 — 빌려 쓰는 낱말에 전용 사진 (2차 575장)

- 날짜: 2026-09-23
- 브랜치: `claude/modest-cray-nbq85a`
- 담당 영역: **B**(학생 학습 콘텐츠). 공동 금지구역은 한 글자도 안 건드렸습니다.
- 바뀐 것: 사진 **575장** · payload 66권 + manifest · `word-pictures.json` 색인 · 표 1개. **`.ts` 0건.**

## 1. 왜 했나

2026-09-22 사장님 「**B로 가게 해줘**」(= 빌려 쓰는 사진을 «떼는» A 가 아니라 «전용 사진을 만드는» B)
· 2026-09-23 「**실사만 가야해**」(🎨 그림문자 카드로 때우지 말 것) · 「**이어서 가자**」.

1차(#1103)에서 347장을 붙였고, 이 PR 이 그 **다음 묶음 575장**입니다.

## 2. 잰 것 — 1차 병합 뒤 상태

[잰 것 — 2026-09-23, `origin/main` payload 전수 · 정본 `scripts/scene-picture-evidence.mjs` 의 `variants()`]

| 무엇 | 값 |
|---|---|
| 남의 사진을 빌리는 줄(rank 1) | **4,754** |
| 빌리는 낱말형 | **807** |
| 그중 «정본 `variants()` 로 봐도 다른 낱말» | **518** |
| 거친 어간(`(ies\|es\|s\|ing\|ed)$`)으로 셀 때 | **620** |

⚠️ **1차 작업기록의 「남은 620개」는 틀린 값이 아닙니다** — 그것은 «거친 어간» 기준이고,
정본 `variants()` 로 다시 세면 **518** 입니다. 두 숫자는 «같은 낱말인가» 를 무엇으로 보느냐만
다릅니다. ⛔ 어느 쪽이든 **재는 방법을 함께 적지 않으면** 다음 사람이 두 숫자를 모순으로 읽습니다.

## 3. 한 것

- 전용 사진 **575장**(번호 **16355~16929**) — `cloudflare-deploy/public/img/scene-words/`
  (합계 21.1MB · 평균 38KB · 80KB 초과 0건)
- 표는 `docs/scene-curriculum-media/word-image-plan-4.json`(575).
  목록 규칙 정본은 `wordPlanFiles` 한 곳이라 **빌드·하니스·사전검사가 같은 함수**를 씁니다.
- 번호는 **쓰지 않은 천 단위 안**(16xxx 2차 구간)에서 이어 붙였습니다 — `reuseClip` 전용 자리를
  뺏지 않습니다.
- 프롬프트는 **그 낱말이 교재에서 쓰인 «예문» 을 읽고** 썼습니다(2026-09-22 교훈).
  그래서 다의어가 예문 쪽 뜻으로 나왔습니다 — 아래가 확인한 본보기입니다.

| 낱말 | 교재 예문 | 나온 사진 |
|---|---|---|
| `model` | Is this the newest model? | 진열대의 새 휴대폰(패션모델 ✗) |
| `tie` | He showed his daughter how to **tie** her shoelaces | 신발끈 묶기(넥타이 ✗) |
| `cloud` | He backed up all his important photos to **cloud** storage | 데이터센터(하늘 구름 ✗) |
| `cell` | …photovoltaic **cell**… | 태양광 패널(세포 ✗) |
| `staircase` | The escalator is like a moving **staircase** | 에스컬레이터(맞습니다) |
| `summit` | …cybersecurity **summit**… | 회의(산 정상 ✗) |
| `apart` | reconnected with an old friend after ten years **apart** | 재회의 포옹 |
| `patient` | My mother is kind and **patient** | 아이를 기다려 주는 엄마(환자 ✗) |

## 4. 잰 것 — 붙인 뒤

[잰 것 — 2026-09-23, 같은 방법으로 다시]

| 무엇 | 전 | 후 |
|---|---|---|
| 빌리는 줄 | 4,754 | **2,262** |
| 빌리는 낱말형 | 807 | **350** |
| 정본 `variants()` 기준 «남의 낱말» | 518 | **61** |
| 거친 어간 기준 | 620 | **163** |

**manifest 전후 대조 — 바뀐 칸은 셋뿐입니다:**

```
maxBookBytes      218,270 → 221,485   (상한 350,000)
maxBookGzipBytes   40,582 →  41,154
imageAssets         4,115 →   4,566
```

✅ `wordForms 4546` · `wordPictureForms 4543` · `cardOnlyForms 3` · `wordRows 39910` ·
`wordPictureRows 39885` · `cardRows 25` · `cardPictogramRows 17` — **전부 그대로**.
✅ **사진을 «잃은» 줄 0건**(줄 단위 대조) · **책별 `wordPictures` 줄어든 책 0** ·
bytes 늘어난 책 65 · 줄어든 책 0.

⚠️ 「사진이 붙은 낱말 수」가 안 늘어나는 것이 **정상**입니다 — 이 575장이 겨냥한 낱말들은
«남의 사진을 이미 빌려 쓰고 있던» 낱말이라, 바뀐 것은 **«어느 사진을 가리키는가»** 뿐입니다.
⛔ 그래서 이 작업의 성과를 「사진 붙은 낱말이 늘었다」로 읽지 마세요 — **«빌림이 줄었다»** 로 읽으세요.

⚠️ `imageAssets` 가 **+451**(575가 아님)입니다 — 새 사진 124장은 아직 어느 줄에서도 rank 1 이
아닙니다(그 낱말의 줄이 «그 낱말을 실제로 담고 있는» 다른 사진을 계속 고릅니다).
**사고가 아니라 «고르는 차례» 의 문제**이고, 남는 61도 대부분 그 형태입니다 — 본보기:

```
laptops ← calls (w12042) ×10     heads ← shower (w12704)
cells   ← target (w12121)        rivers ← rapid  (w12225)
clear   ← animal (w12544) ×11    textures ← feel (w12619)
```

`clear`·`textures` 는 **프롬프트 «틀» 이 그 낱말을 담고 있어 구조적으로 못 고칩니다**
(틀: `Clear recognizable subjects, realistic textures and soft daylight`).

## 5. 크레딧을 쓰기 «전» 에 게이트를 통과시켰습니다

`node scripts/verify-word-prompts.mjs <후보.json>` → **후보 575 · 통과 575 · 탈락 0 ·
껍데기 15 → 15**(안 줄어듦). 프롬프트를 다시 쓸 때마다 **다시** 돌렸습니다(총 2회 + 교체 1회).

⚠️ 껍데기가 «줄면» 그 낱말이 **이미 만든 사진 전부** 의 근거로 되살아납니다 — 그래서
«줄지 않았는가» 를 매번 봅니다.

## 6. 눈으로 전수 확인 — 1장 교체

접촉시트 15장(5열 × 최대 8줄)을 크로미움으로 그려 **575장을 전부** 봤습니다.

🔴 **`showers`(16902) 한 장을 교체했습니다.** 프롬프트는 「Steam rising from hot showers in a
changing room」였는데 모델이 **수건·속옷 차림의 사람 셋이 있는 샤워실**을 그렸습니다 —
뜻은 맞지만 **아이들이 보는 화면에 쓸 수 없습니다.** 사람이 아예 없는 문장으로 다시 써서
게이트를 다시 지나고 새로 만들었습니다.

```
전: Steam rising from hot showers in a changing room
후: Steam rising in an empty tiled locker room where a row of chrome shower heads
    drips into open showers
```

⚠️ **게이트는 이것을 원리상 못 잡습니다** — 게이트가 묻는 것은 «그 낱말을 담았는가» 뿐이고
«아이 화면에 써도 되는가» 는 **사람이 봐야** 합니다. 그래서 접촉시트 전수 확인을 뺄 수 없습니다.

⚠️ 남겨 둔 약한 것들(교체하지 않음 — 뜻은 맞습니다):
`cover`(파도가 모래를 덮는데 사람·개가 크게 보임) · `grasses`(풀밭인데 코끼리가 주인공) ·
`example`·`examples`·`spread`(추상어라 사진으로 가르기 어려움).

## 7. 제공자 실패율 — [잰 것]

- 1차 제출 575건 중 **116건 실패(20.2%)**, 그중 **4건이 `nsfw`**(내용 필터 거절).
- 재제출 116건 중 **1건만 다시 `nsfw`**(`cast`) → 다시 써서 통과.
- ⟹ 평범한 `failed` 는 **같은 프롬프트로 재제출하면 대개 됩니다.** `nsfw` 는 **다시 써야** 하고,
  다시 쓴 프롬프트는 **반드시 `verify-word-prompts.mjs` 를 다시 지나야** 합니다.
- 다시 쓴 **5건**(원문 → 새 문장):

| 번호 | 낱말 | 원문 → 새 문장 |
|---|---|---|
| 16533 | says | 「A mother leans close and says something to her child」 → 「A teacher leans toward a small group and says a few words while they listen」 |
| 16651 | museums | 「Visitors in the grand hall of one of the city museums」 → 「Banners on the stone facade of two large art museums beside a bright plaza」 |
| 16682 | worry | 「A parent comforting a child whose face is full of worry」 → 「A student at a desk chewing a pencil, worry plain on her face before a test」 |
| 16684 | forced | 「A family carrying bundles, forced to leave a village」 → 「A jar lid stuck so tight it has to be forced open with both hands」 |
| 16669 | cast | 「A child's arm in a white plaster cast resting on a table」 → 「A white plaster cast propped on a table beside a pair of crutches in a sunny room」 |

⚠️ **왜 걸렸는지는 못 쟀습니다** — 제공자가 사유를 안 줍니다. 위 다섯은 전부 평범한 문장이라
«사람+아이» 조합이 흔한 방아쇠로 «보입니다» 가 그것은 **추론**입니다.

## 8. 버린 것 (사람이 정할 일)

- `textures` — 프롬프트 «틀» 이 `realistic textures` 를 담고 있어 **구조적으로 게이트를 못 지납니다**
  (1차의 `clear` 와 같은 자리). 크레딧을 쓰기 전에 빠졌습니다.
- `potter` · `r` · `shah` · `pure` — 낱말 자체가 사진으로 가를 수 없거나 고유명사 조각.
- 남은 11개(그 낱말의 전용 사진이 아직 없는 것):
  `designers finally potter pure r reporter ring router shah textures weekly`
- 1차의 7건은 그대로 남아 있습니다.
- 남은 61개(빌림) 중 대부분은 «그 낱말을 실제로 담은» 다른 사진을 고르는 것이라 **고장이 아닙니다** —
  전용 사진이 rank 1 이 되게 «고르는 차례» 를 손볼지는 별건입니다.

## 9. 확인한 방법

| 무엇 | 결과 |
|---|---|
| `scene_curriculum_harness` | **PASS 636,651** (exit 0) |
| `scene_curriculum_ui_harness` | **PASS 147** (exit 0) |
| `word_quiz_pictures_harness` | **PASS 13 / FAIL 0** |
| `run.mjs --fast` | PASS 363 · SKIP 20 — **`origin/main` 워크트리와 ⚠ 목록을 `comm` 대조해 «새로 빨간 것 0건»**(양쪽 17건은 이 컨테이너에 `node_modules` 가 없어 나는 환경 사유) |
| 타입체크 | `.ts` 변경 **0건**이라 해당 없음 |
| 눈으로 | 접촉시트 15장 전수 |

⚠️ **`--fast` 를 `\| tail` 에 물려 읽지 마세요** — 파일로 받아 `▶ … ⚠` 줄을 **이름으로** 대조해야
«내가 깨뜨린 것» 과 «원래 빨간 것» 이 갈립니다.

⚠️ **사진을 갈아 끼운 뒤에는 `build-scene-curriculum.mjs` 와 `build-word-pictures.mjs` 를 «둘 다»**
다시 돌리세요 — payload 가 사진 파일의 **바이트 수**를 들고 있어, 한 장만 바꿔도
`scene_curriculum_harness` 가 「낱말 사진 크기가 실제 파일과 같다」로 **크래시**합니다(실제로 밟았습니다).

## 10. 임시 브랜치

프록시가 CDN 을 403 으로 막아(CLAUDE.md 2장) 러너가 내려받았습니다 — `tmp/word-img-b4`
(워크플로 `tmp-fetch-word-b4.yml` · 목록 `tools/tmp/word-img-fetch.json`).
**그 둘은 main 에 올리지 않았습니다** — 작업 브랜치는 `git checkout FETCH_HEAD -- <이미지 폴더>` 로
**사진만** 가져왔습니다.

⚠️ 푸시 프록시가 브랜치 «삭제» 를 거부해 `tmp/word-img-*` 가 여러 개 남아 있습니다
(**사람이 정할 일** — GitHub 화면에서 지우면 됩니다).
