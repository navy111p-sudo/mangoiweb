# 장면 탐험대 — 나머지 84권 낱말 사진 전수 검수 · 뜻 안 맞는 299장 재생성 (2026-09-23)

## 1. 왜 했나
앞 작업(#1121, BTS 1)에서 「나머지 84권은 아직 안 봤습니다 — 같은 형태(추상어를 글자대로 그림)가 더 있을 것」이라고 남겼습니다.
그 나머지를 전수로 봤습니다.

## 2. 어떻게 봤나 (잰 것)
- 낱말 사진이 붙은 **4,426장**(낱말 ↔ 사진 짝, 예문 포함)을 접촉시트 93장으로 만들어 에이전트 6개가 나눠 봄.
  뜻이 다르면 WRONG_SENSE · 낱말이 안 보이면 NOT_SHOWN · 글자 깨짐·개수 틀림은 BROKEN · 아이 화면에 부적절은 INAPPROPRIATE.
- 표시된 **350장**(7.9%) — NOT_SHOWN 176 · WRONG_SENSE 140 · BROKEN 25 · INAPPROPRIATE 9.
  표시된 것을 제가 다시 접촉시트로 확인(예: 「stars」 빈 화면, 「bad」 썩은 사과인데 예문은 사탕과 이, 달력 숫자 깨짐).
- 대표 사례: `quarter` 파이 조각(예문은 3개월) · `platform` 기차 승강장(게임 플랫폼) · `jams` 잼 토스트(교통 체증) ·
  `tigers`·`wood` 골프 선수 · `california` 와인잔(→ 해변) · `shoulder`·`take`·`shiny` 노출 사진 · `quit` 담배.

## 3. 고친 것
- **299장 재생성**(gpt_image_2_5, 4:3) — 예문의 뜻대로 프롬프트를 다시 씀. 새 번호 **17101~17404**
  (7일 캐시에 옛 사진이 남지 않게 — #1121 과 같은 이유로 덮어쓰지 않음).
  - 그 사진의 표 낱말이 곧 그 낱말이면 **제자리 교체**(222장, 표의 index·prompt·url 교체, 옛 파일 222개 삭제 — 참조 0건 확인).
  - 남의 사진을 빌려 쓰던 낱말이면 **전용 사진 추가**(77장, `word-image-plan-4.json` 끝에).
- 크레딧 전 `verify-word-prompts.mjs` — 304개 중 `clear`·`subjects` 2개 탈락(틀 문장 «Clear recognizable subjects» 의
  껍데기 낱말이라 원리상 근거가 못 됨) → 뺌. 껍데기 15→15(그 뒤 재시도분도 15→15).
- **nsfw 거절 14건**(두 번 모두) — 아이·신체 접촉 장면이 걸림. 사람이 없는 장면으로 다시 써 11건 성공.
  `pressure`(또래 압력)·`related`(위험과 관련)·`normal`(스트레스는 정상)은 사람 없이 뜻을 못 그려 **뺐습니다**(옛 사진 그대로).
- 러너(임시 브랜치 `tmp/word-img-260923c`)로 받아 옴. 80KB 넘은 3장(17210·17216·17361)은 q68 로 다시 받음.

## 4. 확인
- manifest 전후 **동일**: wordForms 4539 · wordPictureForms 4536 · cardRows 25 (잃은 낱말 0).
- 교재 payload 대조: 목표 299낱말 **전부 새 사진으로 착지**. 목표 밖으로 바뀐 낱말 13개
  (hospitals·improves·hundreds·rivers·affects·textures·performing·protections·supporting·affected·resulting·videos·adopted)
  — 빌려 쓰던 옛 사진이 교체돼 다른 사진(근거 게이트 통과)으로 옮겨 감.
- 새 사진 299장 전부 접촉시트로 눈 확인 — 글자 깨짐·부적절 없음.
- `scene_curriculum` 636,533 PASS · `scene_curriculum_ui` 147 · `word_quiz_pictures` 20/0 · `asset_version` 239/0 ·
  `--fast` FAIL 17 = 기준선과 **같은 17개**(모두 이 컨테이너에 node_modules 가 없어 나는 환경 사유).

## 5. 못 한 것 (사람이 정할 일)
- **클립·장면 스틸 22장**은 낱말 사진보다 먼저 뽑혀(고르는 차례 ① 교재 안 장면) 새 사진으로 못 바꿉니다:
  orange·small·light·younger·live·laugh·fish·bed·high·field·turn·stage·forest·life·close·families·boxing·lamp·morning·garlic·towels·tracker.
- **못 그리는 낱말 24개**(prompt 없음) — way·means·course·earlier·probably·real·based·pure·still·often·having·yet·depends·per·least·never·quite·qualities,
  제목 조각 potter(Harry Potter)·ring(The Ring)·heights(Wuthering Heights), **술 카드 3개**(`bts-20` alcohol·beer·wine — 낱말 자체가 술이라 둘지 뺄지는 사람이 정함).
- 위 3개(pressure·related·normal)와 clear·subjects 는 옛 사진 그대로입니다.
- 에이전트 두 개가 같은 작업 파일 이름을 써서 **31건이 중복 생성**됐습니다(결과는 안 씀). 크레딧 실측: 1,530.75 → 1,442.25(**88.5 사용**, 그중 중복 약 7.75).
