# 장면 탐험 — 이미 가진 실사 사진으로 문제 406개 추가 (2026-09-23)

## 왜 했나
사장님 요청: 「기존에 힉스필드에 있는 실사 이미지들 사용해서 수준별·레벨별로 최대한 많이. 동영상은 나중에, 일단 실사 사진으로만」.
장면 탐험(`student-game-scene-quest.html`)은 12장면뿐이었습니다.

## 쓸 수 있는 사진을 셈 (잰 것)
- `img/scene-clips/` 행동 장면 사진 **547장**(clip-plan.json, 재사용 2편 제외). 사람이 무언가를 하는 사진이라 단어·구문·문장 3단계가 됩니다.
- `img/scene-words/` 낱말 사진 약 4,578장은 **같은 화면의 「BTS · SIU 그림 단어장」이 이미 씁니다**. 물건 하나만 찍은 사진이라 «행동 문장» 을 만들 수 없어 이번에는 뺐습니다.

## 한 일
- 에이전트 12명이 547장을 **사진을 직접 열어 보고** 정답을 썼습니다. 설계 문구(prompt)와 실제 사진이 다른 경우가 많았습니다(예: 5001 은 «책상을 가리킨다» 가 아니라 «공책에 손을 댄다»). 사진을 따랐습니다.
- **141장은 건너뜀** — 사람·동물의 행동이 없는 사진(풍경·물건만), 얼굴 클로즈업, 손만 보여 불분명한 것.
- 결과 **406문제** — 초급 BTS 334 · 중급 SIU Basic 41 · 고급 SIU Advance 31. 전체 418장면(기존 12 포함).
- 원본 `docs/scene-curriculum-media/scene-quest-bank.json` → `scripts/build-scene-quest-bank.mjs` → `public/js/scene-quest-bank.js`(188KB, 이 게임 화면에서만 실립니다).
  문장 정답은 `subj × (aux + phrase)` 조합으로 코드가 만듭니다(학생의 다양한 주어 표현을 받기 위해).
- 화면: 「탐험할 세계」에 초급·중급·고급 실사 사진 3개 추가. 영상이 없는 문제는 「▶ 영상 보기」·「그림으로 보기」를 감추고 문구를 «사진» 으로 바꿉니다.
- 빌드가 «영어 단서가 정답 낱말을 흘리는가» 를 검사해 17건을 손으로 고쳤습니다.

## 검사
- `test-harness/scene_quest_bank_harness.mjs` 2,057종 — 모든 모범답이 정답·부정문은 오답, 사진 파일 존재, 수준별 deck, 가짜 DOM 으로 실제 실행해 «사진 문제면 영상 버튼 감춤 / 영상 장면이면 보임» 짝.
  변이 2종(감추기 줄 삭제 · 재생 가드 삭제) 실제 FAIL 확인.
- 기존 `scene_quest_harness` 통과(모든 장면에 대해 16,727건).
- `run.mjs --fast` 의 ⚠ 17건은 전부 `origin/main` 에서도 똑같이 실패(환경 사유 — esbuild 등 없음).
- 사진 2장(7093·7405)을 직접 열어 정답과 대조.

## 못 한 것 / 사람이 볼 것
- 406문제 전부를 사람이 검수하지는 않았습니다(에이전트가 사진을 보고 썼고, 표본만 확인). 이상한 문제가 보이면 json 에서 `skip:true` 로 빼고 빌드를 다시 돌리세요.
- 영상은 없습니다. 원하시면 사진에서 6초 영상을 만드는 것은 별건(비용 발생).
- 같은 정답 낱말(laptop 등)이 여러 문제에 반복됩니다.

## 2차 (2026-09-24) — 중급·고급 783문제 추가 (사장님 「중급·고급 문제도 더 많이」)
- 원천: 낱말 사진 `img/scene-words/` 중 **SIU 교재용이면서 설계 문구에 사람이 나오는 것 793장**(중급 237 · 고급 556).
  원래 «그 낱말을 보여 주려고» 만든 사진이라 교재 낱말이 추상어(regardless·stakeholders…)인 경우가 많아, 정답 낱말은 **사진에 실제로 보이는 구체 명사**로 골랐습니다(교재 낱말이 보이는 물건이면 그것을 씀).
- 에이전트 16명이 사진을 직접 열어 작성, **10장 건너뜀**(사람 없음·콜라주 등). 첫 실행이 사용량 한도로 14묶음이 중간에 끊겨, 10장마다 저장·이어쓰기로 다시 돌렸습니다.
  ⚠️ 두 묶음(b4 48장 · b13 20장)은 끊긴 실행이 써 둔 답을 되살린 것이라 그 실행에서 사진을 다시 열지는 않았습니다(표본 21071 은 직접 대조해 맞음).
- 결과 은행 **1,189문제** = 초급 334 · 중급 277 · 고급 578. 전체 장면 **1,201개**(기존 12 포함).
- 은행 파일 661KB(gzip 약 200KB) — 이 게임 화면에서만 받고 `?v=` 로 캐시됩니다.
- 하이픈 낱말(x-ray)은 «xray»·«x ray» 도 정답으로 받게 변형을 자동으로 더했습니다.

## 3차 (2026-09-24) — 초급 1,188문제 추가 (사장님 「초급 문제도 더 많이」)
- 원천: 낱말 사진 중 **BTS 교재용이면서 설계 문구에 사람이 나오는 것 1,265장** → 술·흡연·무기 낱말과 제외 목록(`no-example-sentences`·`no-card-words`)에 걸린 4장을 빼고 **1,261장**.
- 에이전트 16명이 사진을 직접 열어 작성(초급 규칙: 쉬운 명사, 1~4낱말 행동, 아이가 이해할 쉬운 단서). 에이전트가 **63장** 건너뜀(사람 없음·행동 없음·부상·술잔·환상 장면 등).
- 합칠 때 제가 **10장을 더 뺐습니다**(아동용 보수 판단): 12152 술로 보이는 음료 · 16746·17124 운동복 차림 · 16610·18091 주사 · 19076 공포영화 · 19492 빈곤 장면 · 19479 두 아이 중 누가 하는지 모호 · 18068 어려운 낱말(check) · 18690 잠옷 차림.
- **단서가 정답을 흘리는 문제**: 빌드는 `word[0]` 만 보는데, 합칠 때 **모든 정답 변형**을 대조하니 66문제에서 동의어가 단서에 들어 있었습니다(backpack 단서 「a bag you…」 + 정답 목록의 bag). 모범답(`word[0]`)은 남기고 **흘린 동의어만 정답 목록에서 뺐습니다**.
- 부정문 변형 1건(18740 「saying no」)은 빌드가 잡아 뺐습니다.
- 결과 은행 **2,377문제** = 초급 **1,522** · 중급 277 · 고급 578. 전체 장면 **2,389개**(기존 12 포함).
- ⚠️ 은행 파일이 **1.34MB(gzip 약 334KB)** 로 커졌습니다. 이 게임 화면에서만 받고 `?v=3` 으로 캐시됩니다. 더 늘릴 때는 수준별로 나눠 받는 것을 검토하세요(별건).
- 검사: `scene_quest_bank_harness` 11,915종 · `scene_quest_harness` 98,472건 · `asset_version` 240/240 · `--fast` 새 실패 0(⚠ 17건은 기존과 같은 환경 사유). 사진 3장(12174·16275·19064)을 직접 열어 대조.
- 못 한 것: 1,188문제 전수 사람 검수 없음. 「웃고 있어요」 같은 약한 행동 문제가 몇 개 섞여 있습니다.

## 4차 (2026-09-24) — 수준별로 나눠 받기 (사장님 「수준별로 나눠 받게 바꿔줘」)
- 한 덩어리 `scene-quest-bank.js`(1.34MB · gzip 약 334KB)를 **`scene-quest-bank-bts.js`(664KB · gzip 약 175KB) · `-siu-basic.js`(156KB · 약 48KB) · `-siu-advance.js`(348KB · 약 105KB)** 셋으로 나눴습니다.
- **화면을 열 때 받는 사진 문제는 0바이트**입니다. «탐험 시작» 을 누를 때 **고른 수준 하나만** 받습니다(그림·영상 세계는 아무것도 안 받음 · 「깜짝 탐험 · 모든 장면」만 셋 다).
- 주소(`?v=`)의 정본은 화면 HTML 의 `<template id="sq-banks">` 입니다 — 템플릿 안 `<script>` 는 실행도 다운로드도 안 되고, `asset_version_harness` 가 `src="…?v="` 를 그대로 지킵니다. ⛔ 주소를 js 에 다시 적지 마세요.
- 받는 동안 버튼을 잠그고 «사진 문제를 불러오는 중이에요…», 못 받으면 **«불러오지 못했어요. 다시 눌러 주세요»** 라고 말하고 시작하지 않습니다(그림 세계로 몰래 바꾸지 않음). 파일은 왔는데 내용이 비어도 받은 것으로 치지 않습니다.
- 검사: `scene_quest_bank_harness` 11,943종(가짜 DOM 으로 실제 실행 — 처음 0개 · 초급만 받음 · 두 번 눌러도 한 번 · 실패 뒤 다시 누르면 다시 받음 · 깜짝 탐험은 셋 다 와야 시작). 변이 8종 중 6종 실제 FAIL, 2종(«비어도 성공» · 콜백 빠뜨림 한 갈래)은 화면의 이중 확인 때문에 결과가 같은 등가 변이 → 자료 함수에 직접 묻는 검사를 더해 콜백 쪽은 잡힘.
  🪤 하니스를 만들며 두 번 헛돌았습니다 — ① 가짜 요소의 `hidden` 기본값이 false 라 «게임이 시작됐다(play 보임)» 가 늘 참이었음 → `intro.hidden===true` 로 판정 ② «오류를 무시하고 시작» 변이는 하니스를 **크래시**시켜 ❌ 가 0건으로 세어짐 → 받은 뒤 처리를 try 로 감싸 깔끔한 FAIL 로.
- 실제 크로미움(로컬 서버): 처음 열 때 은행 요청 0건 · 초급 누르면 `bank-bts.js?v=1` 한 건 · 중급 차단 시 안내 문구가 보이고 시작 안 함 · 사진이 그 수준 것.

## 배포 (2026-09-24 · 추석 연휴)
- 사장님 「오늘은 추석 연휴라서 수업이 없으니 지금 바로 배포해줘」 — 목요일 보류 창(12~16시) 안이라, 커밋 메시지의 `[deploy-now]` 표시로 보류를 우회했습니다(클로드는 Actions 의 Run workflow 를 누를 권한이 없음 — 403).
- 함께 나간 것: 2차 중급·고급(#1135) · 3차 초급(#1138) · 수준별 나눠 받기(#1143) 와 그 사이 main 의 다른 병합분.

## 5차 (2026-09-24) — 고급 530문제 추가 (사장님 「고급 문제도 더 많이」)
- 원천: SIU ADVANCE 교재용 낱말 사진 중 **아직 문제로 안 쓴 827장 전부**. 2차는 설계 문구에 «사람» 낱말이 있는 사진만 골랐는데 그렇게 남은 것은 29장뿐이라, 이번에는 **사람 여부를 문구가 아니라 사진으로** 가르게 했습니다(교수·치료사·장보는 손님처럼 문구 정규식이 못 잡는 사람이 많았습니다).
- 에이전트 16명이 사진을 직접 열어 작성 — **530장 작성 · 287장 건너뜀**(대부분 사람 없음. 그 밖에 수영복·혈액관·주사·무기(권총집·칼)·실제 인물로 보이는 정상·콜라주 등).
- 합칠 때 제가 **10장을 더 뺐습니다**(학생용 보수 판단): 13146 수술 · 14932 부서진 차 · 15031 추모 · 20170 넘어진 선수 · 14849 다투는 장면 · 21252 레오타드 · 13171 스포츠브라 차림 · 18229 잔해 속 사람 사진 · 18446 콜라주 · 19798 실제 브랜드 로고.
- **단서가 정답의 머리 낱말을 흘리는 18문제**(「denim jacket」 ← 「a blue jacket…」 등)는 단서를 다시 썼고, 동의어가 단서에 든 84문제는 흘린 **동의어만** 정답 목록에서 뺐습니다(모범답은 그대로).
- 결과 은행 **2,907문제** = 초급 1,522 · 중급 277 · 고급 **1,108**(578→1,108). 전체 장면 **2,919개**(기존 12 포함). 고급 파일 `scene-quest-bank-siu-advance.js` 348KB → **665KB**, `?v=2`. 초급·중급 파일은 안 바뀌어 그 학생들은 다시 받지 않습니다.
- 검사: `scene_quest_bank_harness` 14,593종 · `scene_quest_harness` 123,531건 · `asset_version` 243/243 · `--fast` 실패 20건이 **origin/main 사본과 이름까지 같음**(새 실패 0). 사진 2장(16917·19665)을 직접 열어 대조.
- 못 한 것: 530문제 전수 사람 검수 없음. 사람이 작게 나오거나 손만 나오는 문제가 몇 개 섞여 있습니다.

## 6차 (2026-09-24) — 중급 186문제 추가 (사장님 「중급 문제도 더 많이」)
- 원천: SIU BASIC 교재용 낱말 사진 중 **아직 문제로 안 쓴 426장 전부**(5차와 같은 방식 — 사람 여부는 사진으로 가림).
- 에이전트 16명이 사진을 직접 열어 작성 — **190장 작성 · 236장 건너뜀**. 중급 사진은 음식·물건·풍경이 많아 고급보다 건너뜀 비율이 높습니다(대부분 «사람 없음». 그 밖에 수영복·무기(창)·혈액·흡연 폐 모형·와인·칵테일로 보이는 음료 등).
- 합칠 때 제가 **4장을 더 뺐습니다**: 16459 스포츠브라 차림 · 14586 불타는 들판 · 16805 와인잔 · 12018 행동이 «서 있기» 뿐.
- 단서가 정답 머리 낱말을 흘린 3문제(puzzle piece·board game·washing machine)는 단서를 다시 썼고, 동의어가 단서에 든 24문제는 흘린 동의어만 정답 목록에서 뺐습니다.
- 결과 은행 **3,093문제** = 초급 1,522 · 중급 **463**(277→463) · 고급 1,108. 전체 장면 **3,105개**. 중급 파일 156KB → **264KB**, `?v=2`.
- 검사: `scene_quest_bank_harness` 15,523종 · `scene_quest_harness` 132,384건 · `asset_version` 243/243 · `--fast` 실패 20건이 origin/main 기준과 이름까지 같음(새 실패 0). 사진 1장(16697)을 직접 열어 대조.
- ⚠️ 이제 중급 교재용 사진은 **남은 것이 없습니다**(426장 전부 확인). 더 늘리려면 사진을 새로 만들어야 합니다(비용 드는 별건).
