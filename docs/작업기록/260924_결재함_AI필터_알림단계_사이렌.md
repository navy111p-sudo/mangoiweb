# 결재함 AI 필터 · 알림 단계 · 사이렌 (2026-09-24)

## 왜 했나
사장님 요청: 「바빠서 자주 못 본다 · 주로 휴대폰 · 장지웅 부장 결재 / 정우영 대표 확인 · 잊으면 수업 입장처럼 알람·사이렌 · 필리핀에서 올린 것 중 이상한 건 AI 가 걸러 반려 · 최대한 반려 없이 결재」.
제안서: https://claude.ai/artifact/WksJiVhGo8ddHPMujDTgtz → 「모두 추진」.

## 잰 것 (운영 D1, 2026-09-24)
- 결재 8건 전부 승인 · 평균 44.8시간 · 최장 278시간 · 24시간 넘긴 건 2건
- **장지웅 부장(`mgr_jjw`) 전화번호 없음 · 푸시 구독 없음** → 어떤 알림도 안 닿고 있었음
- 🔴 **결재 푸시 24건 전부 `push_queue.fetched_at` NULL** — `notify()` 가 대기열에 넣기만 하고 `broadcastWebPush` 로 기기를 깨우지 않아 **한 번도 폰에 뜬 적이 없었다.** 대표님 푸시가 «켜짐» 이어도 마찬가지였다.

## 한 것
1. **D1** `admin_account.phone` ← 장지웅 부장 번호(사장님이 직접 주심, 비어 있을 때만 UPDATE, 1행)
2. **푸시가 실제로 뜨게** — `notify()` 가 넣은 뒤 `broadcastWebPush` 로 깨움. 한 곳도 못 깨우면 «못 닿음» 으로 세어 문자 폴백이 돈다. 만료 구독은 끔.
3. **알림 단계** (정본 `approval-policy.ts` 의 `nudgePlan`·`nudgeLevel`): 단계가 열리고 4시간 푸시 → 8시간 문자 → 12시간 사이렌(화면) → 24시간 경영진 승격(전에는 72시간). 긴급은 같은 비율로 짧게(1시간 사이렌·2시간 승격). 밤 22~8시 KST 에는 문자·사이렌 단계로 안 올린다(긴급 예외). 새 칸 `nudge_level`·`nudge_seq`.
4. **하루 두 번 요약** 9시·17시 KST — 경영진·결재권자에게 «대기 N건 · 가장 오래된 것 · 🟢 바로 승인 가능 N건». KV `approval_digest:<날짜>:<시>` 로 한 번만. KV 가 없으면 안 보낸다.
5. **🚦 신호등** `signalOf` — 🟢 깨끗 / 🟡 사람이 확인 / 🔴 고치면 되는 것(영수증 없음 · 영수증과 10% 넘게 다름 · 7일 안 같은 금액 · 설명 없음 · 미래 날짜).
6. **꼼꼼 점검** `runChecks` 추가: 설명 없음 · 미래 날짜 · 60일 넘은 지출 · 영수증 날짜 불일치 · 7일 중복.
7. **AI 내용 검토** `aiReview` — 필리핀 매니저의 돈 나가는 건만, llama-3.3. 결과는 **🟡(info) 표시로만** 들어간다.
8. **올리기 전 점검** `POST /api/approval/precheck` + 화면 — 🔴/🟡 면 올리기 전에 「이것부터 고쳐 주세요」. 「그래도 올리기」 가능. 점검이 실패하면 막지 않는다. (사장님 「반려 없이 결재」 — 반려를 줄이는 가장 좋은 자리가 올리기 전이다)
9. **자동 반려** — KV `approval_autoreject`: `shadow`(기본·표시만) / `on` / `off`. `on` 이면 🔴 건을 «AI 자동 점검» 메모와 함께 되돌림(`decided_by='ai-auto'`). 경영진·긴급·인사급여·취소 결재는 절대 안 되돌린다.
10. **대표님께 이상한 것만 즉시** — 1단계(부장 혼자 확정) 소액 건에 신호가 있을 때만 푸시(`approval-watch`). 영수증 못 읽음은 제외(소음).
11. **반려 사유** 버튼 2개 추가 · 버튼 사유는 영/한 둘 다 메모로 · 한국어로만 쓴 사유는 AI 가 영어를 덧붙임.
12. **사이렌** (work.html) — `siren_at` 이 지나면 전체 덮개 + 3초마다 경보음, 「지금 결재」·「30분 뒤」. 304 에도 매 분 보고, 숨긴 탭도 5분마다 새로 받는다.

## 버린 것과 이유
- **AI(대화 모델)가 반려를 정하게 하기** — 같은 건도 매번 다르게 판단해 멀쩡한 건이 막힌다. AI 는 🟡 표시만, 반려는 계산 규칙만.
- **자동 반려를 바로 켜기** — 제안서대로 2주 연습(shadow) 뒤 두 분이 보고 켠다. 켜는 법: KV `SESSION_STATE` 에 `approval_autoreject`=`on`.
- **관리자 화면(admin.html) 사이렌** — 1.3MB 공동 파일이라 이번엔 결재함(/work)에만. PC 에서는 /work 탭을 열어 두면 울린다.
- **새 cron** — 한도 5/5. 기존 15분 트리거 안의 `runApprovalSlaSweep` 에 얹었다(index.ts 무수정).

## 남은 것 (사람이 정할 일)
- 장 부장님 휴대폰에서 /work 「알림 받기」 켜기(아이폰은 홈 화면 추가 먼저) — 문자는 이제 번호가 있어 간다.
- 반복 지출 틀 · 결재 전 AI 질문 · 반려 사유 체크리스트 · 전화(ARS) 알림 — 다음 작업.
- 2주 뒤 🔴 표시가 맞았는지 보고 자동 반려 `on` 여부 결정.

## 확인한 방법
- tsc(types 제거 임시 설정) 126줄 = 기준선과 같음
- 새 하니스 `approval_ai_filter_harness` 51종 전부 통과, 변이 3종(🟡 되돌리기 · 기기 안 깨우기 · 밤 쉬기 제거) 실제 FAIL
- 결재 관련 하니스 12개 · `approval_policy_harness` 147종 통과(문자 검사 1줄을 뜻으로 고침 — 8시간 문자는 «지연» 문자)
- 화면 스크립트 `new Function` 문법 통과
- ⚠️ 실제 폰 알림·사이렌 소리는 배포 뒤 사람이 확인해야 함
