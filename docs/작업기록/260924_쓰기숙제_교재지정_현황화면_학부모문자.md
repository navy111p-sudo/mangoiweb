# 2026-09-24 쓰기 숙제 활용 4종 — 교재 지정 · 현황 화면 · 제안서 · 학부모 안내 문자

## 왜
- 사장님 「추천안대로 활용하게 해줘」 → 네 가지 모두 선택.

## 한 것
1. **교재 지정** — 관리자 › 학생 상세 › 교재 탭에 권 고르기(교재 85권, manifest 에서 읽음). 저장은 기존 `bulk-assign-textbook`(한 명, `only_empty:false`).
   - 학생 쪽: `/api/student/today?scenebook=1`(본인·관리자 게이트 «뒤») → `js/scene-curriculum.js` v11 `serverBook()` 이 **서버 지정이 바뀌었을 때만** 기기 기억을 덮습니다. URL `book=` 이 여전히 이기고, 1.5초 안에 못 받으면 예전대로.
2. **쓰기 숙제 현황** — `/admin/scene-homework.html`(본사 전용, 사이드바 「쓰기 숙제 현황」). 학생별 횟수·문제·정답률·다시 볼 낱말(최대 5). API `GET /api/admin/reports/scene-homework`(정본 `src/scene-homework.ts`, `src/index.ts` 무수정 — `/reports/` 접두사).
3. **제안서 공유 문서** — Claude Docs 로 작성(가격·체험기간·수수료 빈칸). 공유는 사람이 Share 메뉴에서.
4. **학부모 안내 문자** — 같은 화면의 「대상 확인」(dry-run) → 「보내기」. 서버는 `confirm===true && dry_run===false` 일 때만 발송, 강사·조직 계정 403, 번호 중복 제거, 한 번 보낸 학생은 `scene_homework_notice_log` 로 다시 안 보냄.
   - 함정 대조가 잡은 셋을 고쳤습니다: ① 형제(같은 번호)가 «다음 실행» 에 새 대상이 되던 것 → 보낸 «번호» 도 뺌 ② 두 번 동시 클릭 → 번호 선점 표(`scene_homework_notice_claim`, PRIMARY KEY)로 한쪽만 발송, 실패하면 선점 해제 ③ 후보마다 이름을 순차 조회 → 한 번에 조회 + 번호는 10개씩 병렬, 후보 400명 초과면 거절(반쪽 발송 방지). 명부에서 숨긴 학생도 뺍니다.
   - ⚠️ **Claude 는 보내지 않았습니다** — 되돌릴 수 없고 돈이 드는 발송이라 사람이 누릅니다.

## 잰 것 (D1 SELECT, 2026-09-24)
- 학생 29,526명 · 교재 칸 채워진 학생 0 · 학부모 번호 있는 학생 한 자릿수(erp 5 · override 5, 겹침 포함) · `scene-words` 기록 0건(배포 직후).
- ⟹ 문자 대상은 많아야 10명 안팎. 번호를 모으는 것이 먼저입니다.

## 사람 확인
- 문자 대상은 «학부모 번호가 있는 모든 학생» 입니다(재학 여부·숙제 수행 여부와 무관). 좁힐지는 사람이 정할 일.
- 교재 지정은 `students_erp.textbook` 에 «BTS 12» 처럼 **짧은 이름**을 씁니다. 이 칸은 웜업 문맥·복습퀴즈 물어보기도 읽습니다 — 라이브러리 묶음 이름과 달라 그쪽에서는 «문장 없음» 으로 떨어집니다(교재가 비어 있을 때와 같은 동작). 화상수업 자동 로드는 2026-08-06 부터 꺼져 있어 영향 없음.

## 버린 것
- 새 교재 배정 API — 기존 `bulk-assign-textbook` 로 충분(카페24 동기화가 `textbook` 을 보존).
- 학원 원장용 현황 화면 — 스코프 자르기가 필요한 별건. 파일럿 반응 뒤.

## 확인
- `scene_homework_harness` 108종(모듈 esbuild 실행 · `buildSceneHomework` 진짜 SQLite · 발송 조건식·게이트식을 오려 내 평가 · serverBook 가짜 fetch).
- `--fast` FAIL 0 · tsc 기준선 126.
- 브라우저: 현황 화면 1280·390·EN, 학생 상세 교재 탭(86개 옵션·BTS 3 미리 선택·POST 본문 확인).
- 곁가지: 사이드바 href 항목은 손자(`secs`)가 필수(`sidebar_three_level_harness`) → 화면에 `sh-list`·`sh-notify` 구역 id.
- ❌ 배포 화면은 사람이 한 번 봐야 끝.
