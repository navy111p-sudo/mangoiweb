# 260924 학생 구분 — 화상영어(+AI) / A.i 학습만

## 왜
사장님: 본사·대표지사·지사·대리점 관리자 화면에서 «화상영어(+AI 포함)» 학생과 «A.i 학습만» 학생을 나눠 보고 싶다.
샘플 4개·계획서·플로우차트(비공개 아티팩트)를 보여 드리고 «추천 조합» 으로 진행.

## 무엇을
- 판정 정본: `src/student-track-roster.ts` — **AI 사용료 청구와 같은 규칙**(`ai-billing.ts` 의 `LIVE_UIDS_SQL`)
  - 화상반 = 활성 예약(자리표시 제외) ∪ 최근 30일 카페24 수업(`c24-%`) ∪ 최근 30일 망고아이 수업(`class-%` account_uid)
  - A.i반 = 재원(`enrolledCond`) − 화상반 · 재원 아님 = none · 조회 실패 = unknown(⛔ ai_only 로 떨어뜨리지 않음)
- API 새 경로 없음 — 기존 `GET /api/admin/students/unified` 에 `track=1`·`summary=1`·`summary_only=1`·`shop=` 선택 인자(스코프·숨김은 그대로)
- 지사·대리점(`branch.html`): 대시보드 두 카드 + 학원별 막대, 학생 명부 탭(전체/화상+AI/AI만/재원 아님). 요약은 1.8초 뒤(첫 화면 2요청 계약 유지)
- 본사(`admin.html` › 학생 관리 › 🎥 학생 구분): 지사 → 학원 → 명부로 파고들기(`js/adm-student-track.js`)
- 곁가지 수리: branch 명부 이름이 늘 «(no name)» — 없는 `s.username` 을 읽고 있었음

## 버린 방법
- students_erp 에 트랙 칸 저장 — 카페24 야간 동기화가 덮음
- `student-track.ts` 에 넣기 — import 없는 순수 파일이라 leveltest 하니스가 통째로 실행(넣었다가 실제로 깨짐)
- 청구와 다른 «좁은» 규칙 — 화면마다 숫자가 달라짐

## 확인
- 신규 `test-harness/student_track_roster_harness.mjs` 53종(판정·요약 SQL 을 진짜 SQLite 로 실행, 청구 명단과 일치) · 변이 8종 전부 실제 FAIL
- tsc 새 오류 0 · `--fast` FAIL 목록이 origin/main 과 동일
- 운영 D1 SELECT: 요약 359ms · 학원 418 · 화상 537 · A.i 7,294
- 로컬 크로미움으로 branch(라이트/다크·400px)·admin 패널 스크린샷
- ❌ 배포 화면은 사람이 한 번 봐야 끝

## 사람이 정할 일
- 「최근 30일」 기준이 맞는지 · 주간 미사용 AI 학생 표시
- (기존) getScope 실패 시 'none' fail-open · 강사도 unified 를 부를 수 있음
