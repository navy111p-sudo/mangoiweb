# 학생·학부모 연락처 적재 요청

**받는 분:** 카페24 서버 담당자 (망고아이 학생 데이터를 넘겨 주시는 분)
**보내는 곳:** 망고아이 개발 (워커 쪽 컬럼·동기화는 이미 준비돼 있습니다 — 원본에 값만 실어 주시면 됩니다)
**날짜:** 2026-09-01

---

## 1. 한 줄 요약

**학생 29,461명 전원의 연락처가 비어 있습니다.** 그래서 미납 안내·결석 알림·수업 품질 안내 등
**문자와 알림톡을 한 통도 보낼 수 없습니다.** 학생 전화번호 · 학부모 전화번호 두 칸만이라도
채워 주시면 바로 살아납니다.

## 2. 실측 근거 (2026-09-01, 운영 D1 `students_erp` 29,461행 전수)

| 칸 | 채워진 행 |
|---|---|
| `student_phone` (학생 전화) | ❌ **0건** |
| `phone` (학생 전화, 옛 칸) | ❌ **0건** |
| `parent_phone` (학부모 전화) | ❌ **0건** |
| `kakao_id` / `parent_kakao_id` | ❌ **0건 / 0건** |
| `parent_user_id` (학부모 계정 연결) | ❌ **0건** |
| `shop_name` (소속 매장) | ✅ 29,060건 (98.6%) — 이건 잘 오고 있습니다 |

즉 **연결·동기화 문제가 아니라 원본에 값이 없거나 넘겨주는 항목에서 빠져 있는 것**입니다.
망고아이 야간 동기화(`importCafe24Students`, 매일 03:00 KST)는 이미 이 칸들을 컬럼 목록에 넣어
받고 있고, 값이 오면 그대로 저장합니다. **워커 쪽에 추가 작업은 없습니다.**

> 📌 참고 — 2026-08-18 에 한 번 「번호를 살렸다」고 수리한 적이 있습니다. 그때 한 것은
> **칸과 조회를 추가한 것**이고, 원본에 값이 없어 결과는 그대로 0이었습니다.
> 같은 오해가 반복되지 않도록 적어 둡니다.

## 3. 왜 필요한가 — 지금 못 하고 있는 것

| 기능 | 지금 상태 |
|---|---|
| 미납 안내 · 독촉 문자 | 받는 사람 0명 |
| 결석·노쇼 알림 | 받는 사람 0명 |
| 수업 품질 안내 (「인터넷이 불안정합니다」) | 보낼 수 없음 → **화면으로만** 알리는 중 |
| 학부모 대시보드 연결 | 자녀 연결이 0건이라 사실상 미사용 |

2026-09-01 실사고 예: 한 학생(`juju5731`)의 회선 손실이 최악 32% 로 측정됐는데,
**그 학생에게 알릴 방법이 시스템 안에 하나도 없었습니다.** 소속 매장(`shop_name`)조차 비어 있어
대리점을 통한 연락도 불가능했습니다.

## 4. 부탁드리는 것 (우선순위 순)

| 순위 | 칸 | 형식 | 왜 |
|---|---|---|---|
| 1 | **`parent_phone`** (학부모 전화) | `01012345678` (하이픈 없어도 됩니다) | 미납·결석·품질 안내가 전부 학부모에게 갑니다. **이 한 칸이 제일 급합니다** |
| 2 | **`student_phone`** (학생 전화) | 같음 | 고학년 학생 본인 안내용 |
| 3 | `parent_name` (학부모 성함) | 문자열 | 문자 첫머리 호칭 |
| 4 | `shop_name` 누락분 | 문자열 | 1.4%가 비어 있어 그 학생은 대리점 경유도 안 됩니다 |

- 기간·범위: **재원생(`status='active'`) 전체**
- 하이픈·공백은 저희가 정리합니다. **형식보다 «값이 있는 것»이 중요합니다.**

## 5. 알려 주셨으면 하는 것

1. 카페24 원본에 이 값이 **있는데 안 넘어오는 것**인지, **원본에도 없는 것**인지
2. 원본에 있다면 어느 필드명으로 들어 있는지 (저희가 매핑을 맞추겠습니다)
3. 개인정보 취급 절차상 별도 동의·계약이 필요한 부분이 있는지

## 6. ⛔ 저희가 하지 않을 것

- **번호를 추측하거나 다른 표에서 끌어와 채우지 않습니다.** 잘못된 번호로 문자가 나가는 것은
  안 보내는 것보다 나쁩니다(엉뚱한 사람에게 아이 이름과 학원 정보가 갑니다).
- **「0명에게 보냈다」를 성공으로 처리하지 않습니다.** 발송 기능은 받는 사람이 0명이면
  그렇다고 화면에 말합니다.

---

📄 같은 형식의 선례: [카페24 회계데이터 적재요청 (2026-08-18)](카페24_회계데이터_적재요청_2026-08-18.md)
