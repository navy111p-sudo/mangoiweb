# 같은 방, 다른 교실 — 8/25 class-895 수업에서 학생·강사가 만나지 못한 이유

- **날짜**: 2026-08-25
- **제보**: 사장님 (화면 사진 2장)
- **수업**: 예약 895 · 8월 25일(화) 21:30 · 학생 `delaware`(김연숙) ↔ 강사 `mangoi_167`(Farrah)
- **방 번호**: `class-895-20260825` (두 화면이 완전히 같음)
- **증상**: 둘 다 정상 입장했고 방 번호도 같은데 **서로 보이지 않음. 양쪽 모두 「참여자 1명」**

> 📌 이 문서는 **진단과 제안까지**입니다. 수리는 전부 공동 금지구역
> (`src/index.ts`·`wrangler.toml`·`deploy.ps1`·`public/index.html`)과 배포 권한이 걸려 있어
> 실행 여부·순서는 배포 담당이 정합니다.

---

## 1. 결론

**두 사람이 서로 다른 «서버 방»에 앉아 있었습니다.**

이 저장소는 Worker 를 **두 벌** 배포하고(`webrtc-unified-platform` · `-prod`),
화상수업 방(Durable Object)은 **스크립트마다** 만들어집니다. 그래서
`idFromName("class-895-20260825")` 이 워커마다 **다른 방**을 엽니다.
접속 주소(`location.host`)가 곧 어느 워커냐 = 어느 교실이냐입니다.

**2026-08-19 중국인 강선생님 건과 같은 뿌리의 재발**입니다
(`docs/작업기록/260819_강선생님_수업방_안보임_워커두벌_DO분리.md`).
🔴 **2026-08-26 정정 — 그때의 「옮겨서 해결」은 실제로 일어나지 않았습니다.**
저장소 세 곳(`CLAUDE.md`·`MAINTENANCE.md`·`wrangler.toml`)에 완료형으로 적혀 있었지만,
**8/26 대시보드 실측**에서 `test.mangoi.co.kr` 이 여전히 **기본 워커**에 붙어 있는 것이 확인됐습니다
(main 커밋 `855eca3`). 즉 이 함정은 **6일 내내 그대로 살아 있었고**, 8/25 에 재발한 것입니다.

```
webrtc-unified-platform (기본)      ← test.mangoi.co.kr        24h 호출 7.26k
webrtc-unified-platform-prod (운영) ← mangoi.ai · www.mangoi.ai 24h 호출 21.47k
```

**그래서 이 보고서의 「(가) 강사 주소 확인」은 이미 답이 나왔습니다** —
강사 `mangoi_167` = `test.mangoi.co.kr`(기본 워커), 학생 `delaware` = `mangoi.ai`(운영 워커).
**진단이 대시보드 실측으로 확정됐습니다.**

---

## 2. 확정된 사실

| 판정 | 사실 | 근거 |
|---|---|---|
| 확정 | 방 번호가 두 화면 모두 같다 | 방 번호는 `class-{예약id}-{YYYYMMDD}` 로 결정론적 — `api-mango.ts:1672`, `api-teacher.ts:445` |
| 확정 | 「참여자 1명」은 **서버가 실시간으로 보내 준 값**이다 | `room-joined`·`user-joined`·`user-left` 마다 `updateUserCount()` — `js/idx-main.js:4065·4149·4164` |
| 확정 | 상대가 같은 방에 들어왔다면 숫자가 2가 되고 채팅에 «○○님이 입장했습니다» 가 뜬다 | `js/idx-main.js:4150`. **둘 다 없었다 = 같은 DO 에 없었다** |
| 확정 | 학생은 `mangoi.ai`(운영 워커) 였다 | 학생 화면 하단 전체화면 안내 문구가 `mangoi.ai:` 로 시작 |
| 유력 | 강사는 **주소창이 없는 창**에서 수업 중이다 | 창 제목 «Mangoi 화상솔루션 - Mangoi Video Solution» = 크롬 「창으로 열기」 바로가기 형태. 본인도 도메인을 알 수 없다 |
| 위험 | 기본 워커가 살아 있다 | `.github/workflows/deploy.yml:136·164` — 폐기되지 않았고 겉보기에 완전히 정상 동작한다 |

### 왜 오래 못 찾는가

**D1·KV·R2 는 두 워커가 같은 id 를 공유합니다.** 출석·토큰·녹화는 한 곳에 쌓이므로
`attendance` 에는 두 사람이 **같은 방에 나란히** 남습니다. 갈리는 것은 DO 하나뿐이라
**DB 만 보면 완벽하게 정상**으로 보입니다. 8/19 건이 7/24~8/19 여덟 번의 수업 동안
발견되지 않은 이유가 이것입니다.

---

## 3. 지운 후보

| 의심한 것 | 판정 | 근거 |
|---|---|---|
| 방 번호가 서로 달랐다 | ✕ | 같은 문자열 + 예약번호로 자동 계산 |
| 강사 계정 연결 오류로 남의 수업에 갔다 | ✕ | 연결이 틀렸다면 895 방 자체에 못 들어간다 |
| 정원 초과로 한쪽이 튕겼다 | ✕ | `room-full` 안내 후 연결 종료 — `video-call-room.ts:501` |
| 같은 브라우저로 인식돼 서로 쫓아냈다 | ✕ | clientId 는 탭 단위 `sessionStorage` 난수 — 기기 간 겹치지 않음 (`js/idx-main.js:5917`) |
| 입장 검증(`verify-room`)이 방을 바꿨다 | ✕ | 그 게이트는 «알리기» 만 하고 `vcRoomId` 를 바꾸지 않는다 |
| 같은 방인데 P2P 만 실패했다 | ✕ | 그러면 「참여자 2명」 + 입장 메시지가 떠야 한다 |
| **서로 다른 워커의 방에 있었다** | **유력** | 남은 설명이자 8/19 사고와 증상이 완전히 일치 |
| 같은 방이지만 시간이 안 겹쳤다 | 확인 필요 | 출석 기록 한 번이면 갈린다 (4장) |

---

## 4. 확정하려면 — 세 가지 확인

> ⚠️ 작업 환경 프록시가 `mangoi.ai` 접속을 막습니다(실측 CONNECT 403). 운영 D1 조회 권한도 없습니다.
> 아래는 **사람이 확인해야** 확정됩니다.

### (가) 강사 화면의 주소 — ✅ **대시보드 실측으로 이미 확정됨 (2026-08-26)**

> `test.mangoi.co.kr` = 기본 워커 · `mangoi.ai` = 운영 워커임이 확인됐습니다(main `855eca3`).
> 강사는 앞쪽, 학생은 뒤쪽이었습니다. **아래는 다른 강사를 점검할 때 쓰세요.**

앱 창의 **⋮ 메뉴**를 열거나 창 안에서 `Ctrl+L`. `mangoi.ai` 가 아니면 그 자리에서 원인 확정.
특히 `…workers.dev` 가 보이면 100% 이 사고.

### (나) 출석 기록의 시간 겹침 — ✅ **2026-08-26 실측 완료: 겹칩니다**

Cloudflare MCP 커넥터로 운영 D1 을 **직접 조회**했습니다(SELECT 만).
`attendance` / `room_id='class-895-20260825'` — 13행.

| 기기(user_id) | 표시이름 | 역할 | 입장 | 구간 |
|---|---|---|---|---|
| `u_wtifepqcnz` | 교사 mangoi_167 | teacher | 5회 | 16:27:13 ~ 21:30:37 |
| `u_pti3gfpckr` | 교사 / 교사 mangoi_167 | teacher | 4회 | 21:07:44 ~ 21:41:35 |
| `u_ua6l7s4bvn` | delaware | student | 2회 | 21:21:05 ~ 21:22:32 |
| `u_7qbc6rf5kc` | (이름없음) | student | 1회 | 21:22:22 ~ 21:25:37 |
| `u_coq9jj5gqv` | (이름없음) | student | 1회 | 21:38:35 ~ 21:38:56 |

**겹침 판정 결과 — 강사·학생이 동시에 같은 방에 기록된 구간이 7건**

| 사람1 | 사람2 | 겹친 구간 | 겹침 |
|---|---|---|---|
| 교사 | (학생) | 21:22:22 ~ 21:25:37 | **195초** |
| 교사 mangoi_167 | (학생) | 21:22:22 ~ 21:25:37 | **195초** |
| 교사 | delaware | 21:21:35 ~ 21:22:32 | **57초** |
| delaware | 교사 mangoi_167 | 21:21:47 ~ 21:22:32 | **44초** |
| 교사 mangoi_167 | (학생) | 21:38:35 ~ 21:38:56 | 20초 |

→ **[겹침] 확정.** 「시간이 어긋났다」는 후보는 지워졌습니다.
같은 시각에 같은 방 번호로 기록돼 있으면서 양쪽 화면 모두 「참여자 1명」이었습니다.

⚠️ **다만 이것이 «어느 주소였는지» 까지 말해 주지는 않습니다.**
접속 주소는 어디에도 기록되지 않습니다(6-3 안전망 2번이 그래서 필요합니다).
호스트 확정은 (가)로 해야 합니다 — 겹침이 확인된 지금은 **(가)가 마지막 한 칸**입니다.

📌 **기기가 여러 대인 것도 함께 보입니다.** 교사 기기 2대가 21:03~21:41 사이에
**9회** 들락날락했고 학생 기기도 3개입니다. 「안 보여요 → 나갔다 들어와 보세요」를
반복한 흔적이며, 서로 못 만난다는 증상과 일치합니다.

### (다) 수업이 «진행 중» 일 때 — 즉시 판별

```
https://mangoi.ai/api/room-media/class-895-YYYYMMDD
https://webrtc-unified-platform.navy111p.workers.dev/api/room-media/class-895-YYYYMMDD
```

한쪽에만 교재가 보이면 = 두 사람이 다른 방에 있는 것.
(관리자 세션이 있으면 `/api/room-status/<방>` 로 참여자 수까지 직접 비교 가능)

---

## 5. 🔴 급여 — 실측 결과, 보호 장치가 이 건을 **못 거릅니다**

노쇼 알림은 실제로 떴습니다.

```
class_no_show #26 · room class-895-20260825 · missing_role=teacher
teacher_name='HANNAH' · student_name='김연숙' · waited_min=5
알림 시각 2026-08-25 21:21:02 KST
```

`src/no-show-truth.ts` 가 출석과 대조해 «오판» 으로 걸러 주게 돼 있습니다.
**그 함수를 실제로 컴파일해 위 실데이터로 돌려 봤습니다.**

```
판정 결과: { present: false, ... }
=> 「강사가 없었다」로 확정 (오판 표시 안 됨)

[대조] 예약 강사명이 'mangoi_167' 이었다면: { present: true, minutes: 315 }
```

**왜 못 거르나 — 이름이 두 갈래이기 때문입니다.**

| | 값 |
|---|---|
| 예약(`class_schedules.teacher_id=24`)이 말하는 강사 | **HANNAH** |
| 화상수업 출석부에 찍힌 이름 | **교사 mangoi_167** |

낱말 경계로 맞추므로 `MANGOI_167` 과 `HANNAH` 는 안 맞습니다 →
`hit` 이 비고 → `present:false` = **「없었다」로 확정**됩니다.
급여 계산은 `present === true` 일 때만 되돌리므로(`api-admin.ts:2065`)
이 수업은 **되돌려지지 않습니다.**

⚠️ 강선생님 건들이 잘 걸러진 것은 **우연히 이름이 맞았기 때문**입니다 —
예약 「중국어 강선생님」 ↔ 출석 「교사 강선생님」. 이름 규약이 사람마다 다릅니다.

**같은 모양이 하나 더 있습니다** (8월 노쇼 19건 대조):

| 방 | 예약 강사명 | 출석부 이름 | 걸러지나 |
|---|---|---|---|
| `class-895-20260825` | HANNAH | 교사 mangoi_167 · 교사 | ❌ 「없었다」로 확정 |
| `class-915-20260824` | HANNAH | 교사 **M**angoi_167 | ❌ 같은 모양 |
| `class-919-20260824` | ANA | (출석 기록 없음) | 「모름」 |
| 강선생님 건 13건 | 중국어 강선생님 | 교사 강선생님 | ✅ 오판으로 걸러짐 |

### 계정 공유 — 사장님 확인: **Farrah 가 HANNAH 계정을 쓰고 있습니다** (2026-08-26 확인)

D1 로 뒷받침되는 사실입니다.

**① Farrah 는 본인 계정이 이미 있습니다**

| | 계정 | 원부 |
|---|---|---|
| Farrah | `mangoi_018` (Teacher - Farrah) | `teachers.id=3` HT FARRAH |
| HANNAH | `mangoi_167` (Teacher - Hannah) | `teachers.id=24` HANNAH |

**② 같은 IP 가 두 계정에 로그인했습니다** (8/25 `admin_login_history`)

```
216.247.53.158  mangoi_167  17:25:28   ← 남의 계정
216.247.53.158  mangoi_167  21:31:48   ← 문제의 수업 시각
216.247.53.158  mangoi_018  21:54:09   ← 본인 계정 (수업 끝난 뒤)
143.44.192.250  mangoi_167  13:21 · 16:04 · 16:05   ← 다른 사람(같은 계정)
```

같은 계정에 **서로 다른 IP 두 곳**이 붙어 있고, 출석부의 교사 기기도 2대였습니다
(`u_wtifepqcnz` 16:27~ ↔ 16:05 로그인, `u_pti3gfpckr` 21:07~ ↔ 17:25 로그인).
**한 수업의 「교사」 자리에 두 사람이 동시에 있었습니다.**

**③ 왜 그러는지 — 배정이 원인입니다. 규율 문제가 아닙니다**

| 강사 | 배정된 수업 | 활성 | 기간 |
|---|---|---|---|
| `teacher_id=24` HANNAH | **85건** | 8건 | 2026-08-12 ~ 10-15 |
| `teacher_id=3` HT FARRAH | **4건** | 4건 | **2026-08-26** ~ 09-16 |

Farrah 본인 id 로 잡힌 수업은 **오늘(8/26)부터** 시작합니다.
어제까지는 **0건**이었습니다 — 본인 계정으로 로그인하면 **수업이 아예 안 보입니다.**
그래서 HANNAH 계정으로 들어갈 수밖에 없었습니다.

**④ 무엇이 망가지나 — 수업은 안 망가지고, «누가 했는지»가 망가집니다**

- 출석·수업 기록·강사 90일 지표가 전부 **HANNAH 앞으로** 쌓입니다
- 급여도 계정 단위라 **Farrah 의 수업이 HANNAH 실적**이 됩니다
- 위 노쇼 오판 필터가 못 걸러지는 것도 같은 뿌리입니다(이름이 계정아이디로 찍힘)
- 사후에 «실제로 누가 했나» 를 알 방법은 **IP 대조뿐**입니다

ℹ️ 화상수업이 끊기지는 않습니다 — 동시접속 1세션(`SINGLE_SESSION`)은
**학생 토큰에만** 걸리고 교사는 쿠키 세션이라 서로를 끊지 않습니다(`auth-token.ts`).
즉 계정 공유는 이번 「서로 못 만남」의 원인이 **아닙니다.** 별개의 문제입니다.

**⑤ 범위는 아직 작습니다 — 8/24 부터입니다**

출석부에 `mangoi_167` 계정으로 찍힌 교사 입장은 **8/24~8/25, 방 6개**뿐이고,
`mangoi_018`(Farrah 본인)로 들어온 기록은 **한 건도 없습니다.**
지금 정리하면 소급 범위가 이틀입니다.

**⑥ 해야 할 일** — ⛔ D1 수정은 전부 사장님 승인이 필요합니다(개발·운영이 같은 DB)

1. **배정을 옮긴다** — Farrah 가 실제로 맡은 수업을 `teacher_id=3` 으로.
   이게 되어야 본인 계정으로 수업이 보이고, 계정을 빌릴 이유가 없어집니다
2. **계정 공유 중단 안내** — 1번이 끝난 뒤에. 먼저 막으면 수업에 못 들어갑니다
3. **8/24~8/25 기록 정정 여부 판단** — 급여 산정 전에. 이틀치입니다
4. **노쇼 2건 손보기** — `class-895-20260825` · `class-915-20260824`
   (지금 「강사 없었음」으로 확정돼 있어 수업료 0원 대상입니다)
5. **`Mangoi_167` 대소문자 중복 계정 정리** — 링크가 대문자 쪽에만 붙어 있습니다
6. **[코드 제안] 노쇼 오판 판정을 «계정 → 원부 이름» 으로 풀어서 비교**
   지금은 이름 문자열끼리만 맞추므로 표기 규약이 사람마다 다르면 뚫립니다.
   `teacher_account_links` 를 한 번 거치면 `mangoi_167 → HANNAH` 로 풀려 제대로 걸러집니다

## 6. 해결 방안

### 6-1. 오늘 — 응급 처치

1. **강사 바로가기 재생성** — 지금 창을 지우고 `https://mangoi.ai` 에서 새로 「창으로 열기」
2. **강사 전원에게 한 번 확인** — 「수업 들어갈 때 주소가 mangoi.ai 맞나요?」 한 사람만의 문제가 아닐 수 있음
3. **휴대폰 앱 최신 버전 확인** — 옛 APK 에는 옛 주소가 박혀 있음(현재 최신 2.5)

### 6-2. 근본 수리 — 둘 중 하나 (사람이 결정)

#### A안 · 워커를 한 벌로 합친다 — **권고**

기본 워커 배포를 멈추고 모든 도메인을 운영 워커로 모읍니다. 방 목록이 하나가 되므로
이 사고가 **구조적으로 불가능**해집니다.

- ⚠️ **먼저 옮겨야 할 것 — 자동 작업(cron) 5종이 기본 워커에만 있습니다.**
  `[env.production.triggers] crons = []` 이라, 그냥 끄면 야간 동기화·미납 독촉·NPS·감시견이
  **전부 멈춥니다.**
- 배포 확인 주소 3곳도 함께: `deploy.ps1:77` · `deploy-full.ps1:130` · `deploy.yml:164·174`
- ⛔ 도메인은 **대시보드**에서만. `wrangler.toml` 에 `routes` 를 적으면 적지 않은 도메인이
  다음 배포에 지워집니다(CLAUDE.md 2장).

#### B안 · 화상수업 연결만 정본 주소로 고정 — 임시

`createWebSocket`(`js/idx-main.js:126`)이 `location.host` 대신 `mangoi.ai` 를 쓰게 합니다.
어느 주소로 페이지를 열었든 **교실은 한 곳**으로 모입니다.

- 기술적으로 가능: `/ws/video-call` 은 인증 게이트가 없고 쿠키를 쓰지 않아 교차 오리진이어도 깨지지 않음
- ⚠️ **위험**: 중국·필리핀 회선에서 `mangoi.ai` 만 막히면 그 수업이 **통째로** 못 붙습니다.
  실패 시 원래 주소로 되돌아가는 폴백이 반드시 필요
- A안이 늦어질 때의 임시 처방

### 6-3. 안전망 — A안·B안 어느 쪽을 하든 함께

이것이 없어서 8/19 에 여덟 번, 이번에 또 놓쳤습니다.
**막는 것보다 «났을 때 바로 알아채는 것» 이 더 급합니다.**

1. **입장 전 주소 경고** — `class-…` 방에 들어갈 때 현재 호스트가 정본 목록
   (`src/site-url.ts` 의 `SITE_HOSTS`)에 없으면 «이 주소로는 선생님과 만날 수 없어요»
   + [mangoi.ai 로 이동]. 주소창이 없는 앱 창에서 **사람이 볼 수 있는 유일한 신호**
2. **접속 주소를 기록** — 출석(또는 `vc_quality`)에 «어느 주소로 들어왔는가» 한 칸.
   그러면 노쇼 리포트가 «강사 A주소 / 학생 B주소» 라고 스스로 말해 줍니다.
   지금은 **어디에도 안 남아 사후 추적이 불가능**합니다
3. **수업 중 엇갈림 감지** — 입장 시 공유 D1 에 «지금 이 방에 있다» 를 적고,
   «DB 엔 두 명인데 내 교실엔 한 명» 이면 «상대는 다른 주소로 들어와 있어요» 안내.
   D1 은 두 워커가 **공유**하므로 이 방법만이 갈림을 **수업 도중에** 잡습니다
4. **감시 확장** — `test-harness/worker_do_split_harness.mjs` 는 지금 설정 파일만 봅니다.
   위 1·2 가 코드에 살아 있는지까지 못 박아야 다음 사람이 실수로 지우지 못합니다

---

## 7. 검증 방법

- 스크린샷 2장 판독
- 소스 대조: `cloudflare-deploy/wrangler.toml` · `src/index.ts`(4585·2028행) ·
  `src/video-call-room.ts`(490~560행) · `public/js/idx-main.js`(126·4048~4170·5917행) ·
  `.github/workflows/deploy.yml`
- 2026-08-19 작업기록 대조
- **하지 못한 것**: 라이브 사이트 접속(프록시 차단) · 운영 DB 조회(권한 없음)

## 8. 같이 볼 것

- `docs/작업기록/260819_강선생님_수업방_안보임_워커두벌_DO분리.md`
- `CLAUDE.md` 2장 「같은 방 번호인데 서로 안 보인다」
- `test-harness/worker_do_split_harness.mjs`


---

## 📌 덧붙임 (2026-09-08 재실측) — 위 표의 두 값이 그 뒤 바뀌었습니다

이 문서는 **2026-08-25~26 시점의 진단 기록**입니다. 본문은 그대로 둡니다(그때 무슨 일이
있었나가 사라지면 안 되므로). 다만 아래 두 줄은 오늘 D1 을 다시 재니 달라져 있어 덧붙입니다.

| 위 본문 | 2026-09-08 재실측 |
|---|---|
| Farrah 의 원부 = `teachers.id=3` HT FARRAH | **`teachers.id=22` FAR** — `teacher_account_links` 에서 `mangoi_018 → FAR(22)` |
| `teacher_id=3` 배정 수업 **활성 4건**(8/26~9/16) | **활성 0건** — 그 16건은 **전부 `cancelled`**(`adm-enroll:60·68·70`) |
| — | `teachers.id=3` 은 이제 **`active=0`**(퇴사). `attendance.username` 에 `HT FARRAH` 표기 **0건** |

🔴 그리고 **`FAR`(22) 와 `HT FARRAH`(3)는 같은 사람입니다** — 2026-09-08 사장님 확인.
(`teacher_profiles.id=27` 이 `korean_name='Teacher Far'` · `english_name='Teacher Farrah'`.)

⛔ **그렇다고 이 문서가 말하는 「부분일치 금지」를 풀면 안 됩니다.** 그 규칙이 지키는 것은
이 쌍이 아니라 **학생 동명이인**(김민서 71명 · 김민준 56명)과 `HT NESS`/`HT FARRAH` 가
낱말 `HT` 를 나눠 갖는 경우입니다. 같은 사람이어도 **이어야 할 행은 활성(22)** 이지
퇴사 행(3)이 아닙니다 — 죽은 행에 이으면 수업·급여가 거기 붙습니다.

ℹ️ 노쇼·급여 판정에서 그 둘을 「같은 사람」으로 잇는 자리는 따로 있습니다 —
`src/no-show-truth.ts` 의 `NAME_ALIASES`(«완전일치 쌍» 표). 판정 성격이 달라 복제하지 않습니다.
📄 `docs/작업기록/260908_강사이름_별칭표_노쇼오탐과_급여0원.md`
