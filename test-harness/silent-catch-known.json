{
  "known": [
    "ai-command.ts|);",
    "ai-command.ts|}",
    "ai-command.ts|} else if (studentName && !existingName) {",
    "api-admin.ts|).bind(dup.id).run();",
    "api-admin.ts|).bind(t.uid, t.name, t.mbti, t.hobby, t.style, t.intro, t.p",
    "api-admin.ts|// teacher_profiles 에 등급 컬럼(기존 배포 DB 호환)",
    "api-admin.ts|await env.DB.prepare(`INSERT INTO attendance_qr_tokens (toke",
    "api-admin.ts|await env.DB.prepare(`INSERT OR IGNORE INTO payroll_deductio",
    "api-admin.ts|await env.DB.prepare(`UPDATE counseling_bookings SET status ",
    "api-admin.ts|const p = String(app.desired_date).split('-');",
    "api-admin.ts|if (existed) updated++; else inserted++;",
    "api-admin.ts|if (method === 'GET' && path === '/api/admin/attendance/toda",
    "api-admin.ts|if (path === '/api/admin/referrals' || path === '/api/admin/",
    "api-admin.ts|if (path.startsWith('/api/admin/counseling/')) {",
    "api-admin.ts|if (path.startsWith('/api/counseling/')) {",
    "api-admin.ts|let actorName = 'admin';",
    "api-admin.ts|let actorName2 = 'admin';",
    "api-admin.ts|scheduleMoved += (upd?.meta?.changes as number) || 0;",
    "api-admin.ts|updated++;",
    "api-admin.ts|});",
    "api-games.ts|).bind(Date.now(), row.user_id, row.source_word).run();",
    "api-games.ts|).bind(vocabId, String(s.word || '').trim(), String(s.meanin",
    "api-games.ts|.bind(row.user_id, id, correct ? 1 : 0, now).run();",
    "api-games.ts|.bind(row.user_id, v?.id || null, isCorrect ? 1 : 0, Date.no",
    "api-games.ts|await ensureMicroLearnSchema();",
    "api-games.ts|const quizzes = (((rs.results as any[]) || [])).map((row: an",
    "api-games.ts|have.add(code);",
    "api-games.ts|if (ds.length && (ds[0] === today || ds[0] === today - 1)) {",
    "api-judgment.ts|}), { expirationTtl: SCENARIO_RECENT_TTL });",
    "api-mango.ts|(여기서 재시도까지 하면 실패가 겹칠 때 수업 중 D1 쓰기가 늘어난다) */",
    "api-mango.ts|.bind(Date.now(), payload.jti).run();",
    "api-mango.ts|enrollCreated++;",
    "api-passkey.ts|if (!stu) return json({ ok: false, error: 'user_not_found' }",
    "api-passkey.ts|let chMeta: any = {};",
    "api-pay.ts|}",
    "api-points.ts|.bind(now, JSON.stringify(body)).run();",
    "api-points.ts|const cnt: any = await env.DB.prepare(`SELECT COUNT(*) AS c ",
    "api-points.ts|}",
    "api-reports.ts|.bind((sentStudent || sentParent) ? 'sent' : 'skipped', sent",
    "api-reports.ts|let row: any = null;",
    "api-retention.ts|upserted++;",
    "api-retention.ts|}",
    "api-students.ts|await env.DB.prepare(`UPDATE students_erp SET password_hash ",
    "api-students.ts|await env.DB.prepare(`UPDATE students_erp SET wf_verified_at",
    "api-uptime.ts|(실제로 이런 장애가 제일 지독하다 — 사이트는 멀쩡해 보이는데 감시·정산·알림이 전부 멈춘다.) */",
    "cafe24-sync.ts|// 첫 페이지에서만 1회 — 이후 페이지는 컬럼이 이미 존재하므로 불필요.",
    "class-audit.ts|} catch (err: any) {",
    "index.ts|).bind('DEBUG:' + log.join('|'), size, recId).run();",
    "index.ts|).bind(errMsg, recIdNum).run();",
    "index.ts|.bind(lang, word, ko, pinyin, Date.now()).run();",
    "index.ts|await env.SESSION_STATE.put(hkey, JSON.stringify(history), {",
    "index.ts|if (env.SESSION_STATE) await env.SESSION_STATE.put(hkey, JSO",
    "index.ts|let body: any = {};",
    "index.ts|let lc = { textbook: reqTextbook, level: reqLevel, lesson_no",
    "index.ts|try { await env.SESSION_STATE.put(TURN_CACHE_KEY, JSON.strin",
    "index.ts|}",
    "lesson-reminder.ts|.bind(c.room_id, c.id, c.user_id || null, now).run();",
    "lesson-reminder.ts|.bind(key, ymd, g.items.length, now).run();",
    "leveltest-schedule.ts|});",
    "leveltest-ticket.ts|await env.SESSION_STATE?.put(k, String(v), { expirationTtl: ",
    "livekit-bridge.ts|).bind(req.room_id, req.user_id, req.user_name, req.role, no",
    "retention.ts|}"
  ]
}
